# 핸드오프 2026-09-17 (2) — Phase 91 ①② 배포 완료 · 다음은 ⑤ `dp` 단축키

## ▶ 다음 세션이 할 일

**⑤ `dp` 단축키 (POS 화면).** 서버는 **이미 끝났다** — `POST /sales` 가 `deudaPago` 를 받는다.

시작점 — 이 한 파일에 필요한 게 다 있다:
```
.planning/phases/91-cobro-de-deuda-pos/91-01-carril-de-caja.md
  → 「⑤ `dp` 단축키 — 다음 세션이 바로 착수할 수 있게」 절
```
거기에 **서버가 요구하는 정확한 요청 모양**(어기면 400), 붙일 자리 4곳의 파일·줄번호,
권고 구현, 착수 전 확인할 것이 실측으로 적혀 있다. 탐색을 반복하지 말 것.

★ 가장 쉽게 틀리는 것 셋:
1. `items` 는 **비어 있어야** 한다. 서버가 제네릭 줄을 직접 만든다(D-2 를 구조로 강제).
2. `receiptNo` **필수**이고 **재시도에도 같아야** 한다 → 카트 상태에 담는다.
   제출 때마다 새로 만들면 `uq_credit_payments_receipt` 가 복제를 못 막는다.
3. 결제수단은 **정확히 하나**. `credito`·`favor`·`senia` 는 거부된다.

---

## 배포 상태 (2026-09-17)

**①②③④(서버측) 전부 운영 배포 완료.** 앞선 push 로 Jenkins api #906 · front #742 SUCCESS.
이번 push 는 ①의 4차 P2 + ② 전체 + CODEX 4·5차 수정.

**DB 마이그레이션은 세 곳(로컬 5432 · 운영 5434 · staging) 모두 적용 완료.**
`sales.credit_payment_id` + `uq_sales_credit_payment`. 운영 195행 전부 NULL, INVALID 인덱스 0.

---

## 남은 작업

| | 내용 | 비고 |
|---|---|---|
| ⑤ | `dp` 단축키 + POS 화면 | 위 참조. 서버 준비 완료 |
| ⑥ | 통계 현금 기준 전환 (D-5) | ★ **반만 하면 이중계상** — 지금 외상 판매가 이미 기간 매출에 들어 있다 |
| ⑦ | 미수금 표시 (D-7) | |
| — | 회수 판매 **취소 역분개** | 지금은 명시적 거부. FIFO 배분을 되돌리는 설계 필요 |
| — | **5차 P1 — 조회가 둘이다** | `registerCashOperation`(잠금 없음) vs `assertCanLand`(잠금 있음). 내 fail-closed 추론은 **검증 안 함** — 먼저 재 볼 것 |
| — | Seña 환불 방향 | A/B/C 안 정리됨. 운영 사용 0건이라 급하지 않다 |
| — | `registerCobro` 재시도 멱등 | ② 와 함께 처리하려 했으나 미착수. 운영 실행 0건 |

---

## ★★ 이번에 배운 것 — 반드시 읽을 것

**CODEX 를 6라운드 돌렸고, P1 4 · P2 4 · P3 1 을 받았다. 그중 3건이 «내가 그 라운드에서
새로 만든 결함»이었다.** 한 번 받고 끝냈으면 전부 그대로 배포됐다.

특히 **두 번** 같은 형태를 만들었다:
1. **침묵 폴백을 새로 만들었다** — 2차: 무효 지점 헤더가 조용히 홈 지점으로.
   4차: `skipCashLanding` 이 기록과 함께 **판정까지** 꺼 버렸다(①이 고친 결함의 재생산).
2. **그 결함을 정답으로 고정한 시험을 같이 썼다** — 2차와 4차 모두.
   내 시험이 내 결함을 지켜 주고 있었다.

⤷ **고친 코드를 다시 CODEX 에 넘길 것.** 그리고 시험을 쓸 때 「무엇이 사라진다」가 아니라
  「무엇이 거절된다」를 단언하고 있는지 볼 것.

★ **CODEX 자동 훅(PostToolUse)은 여전히 호출되지 않는다.** 스크립트는 정상이다.
  띄우는 법은 메모리 `verification-and-codex-are-automated-hooks` 에 명령까지 적어 뒀다.
  push 전에 `.team/reviews/.auto-codex.heads` 를 **매번 직접** 확인할 것.
