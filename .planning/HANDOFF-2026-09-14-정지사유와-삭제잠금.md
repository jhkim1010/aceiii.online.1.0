# 핸드오프 — 2026-09-14 · 정지 사유·삭제 잠금·검증 게이트

앞 핸드오프: `HANDOFF-2026-09-13-guia-alcanzable-y-roles.md`

이 세션은 **운영 장애(Sager 로그인 불가)에서 시작해** 그 뿌리를 따라가다
「같은 사실을 두 곳이 소유한다」 형태를 **다섯 번** 만났다. 그리고 그 사이
**검증 게이트 자체가 두 군데 뚫려 있던 것**을 찾아 막았다.

---

## 0. 배포 상태

| 빌드 | 결과 |
|---|---|
| api #896~#901 | 전부 SUCCESS |
| front #732~#738 | 전부 SUCCESS |

★ **미push 커밋** (2026-09-14 저녁 갱신):
  · api `6ce906a` · `bfa7695` · `f0380fd` · `f00005c` · `9eba587` · `35b0727`
  · app `72564f4`   · root 이 문서 + 서브모듈 포인터(미커밋)
  ★ **CODEX 최종 검토(`35b0727`) 지적 0건.** 다섯 바퀴 만에 깨끗해졌다.
  마이그레이션(`users.suspended_reason`)은 **이미 양쪽 적용**돼 있다.

★★ **정정 — 「P1·P2·P3 마감」은 사실이 아니었다.** `bfa7695` 자체의 CODEX 검토가
  **새 P1 두 건**을 냈는데(수동 정지 되살리는 경쟁 조건 · `inactive` 관리자 복구),
  이 문서가 그것을 「마감」으로 적고 §5 에서 **첫 작업으로 push 를 지시**했다.
  그대로 따랐으면 미해결 P1 이 배포됐다. root `a111972` 검토가 이것을 P1 으로 짚었다.
  ⤷ `f0380fd` 에서 둘 다 고쳤다(판정을 잠근 행으로 · 허용 목록 가드).
  ⤷ **그 커밋의 재검토가 또 P1 1건 + P2 2건을 냈다** — 셋 다 「잠금 범위가 판정
    범위보다 좁다」는 같은 자리다. `f00005c` 에서 마저 고쳤다(§3).
  ⤷ **배울 것**: 「마감」은 CODEX 보고서를 **읽고 나서** 쓰는 말이다. 고쳤다는
    사실과 검토가 끝났다는 사실은 다르다.

---

## 1. ★★★ 가장 중요 — 검증 게이트가 두 군데 뚫려 있었다

**이것부터 읽어야 한다.** 이 세션 전반의 커밋 상당수가 검증 없이 지나갔다.

### ① NODE_OPTIONS 가 node 를 죽여 훅 두 개가 침묵했다
`--require` preload 임시파일이 사라지면 **모든 node 실행이 즉사**한다. 두 훅은
첫 줄에서 `node -e` 로 입력 JSON 을 파싱하고 실패를 `2>/dev/null` 로 삼킨다 →
`CMD` 가 비고 → **커밋 게이트도 CODEX 자동 검토도 아무 말 없이 통과**.
유일한 흔적은 `.auto-codex.heads` 가 2026-09-10 에 멈춘 것뿐이었다.
→ `node-sano.sh` 로 **실제로 돌려 보고** 고친다. 실패하면 **소리를 낸다.**

### ② `git add X && git commit` 은 게이트를 통째로 뚫는다
훅은 **PreToolUse** 라 명령 실행 **전**에 돈다 → 같은 명령 안의 `git add` 결과가
index 에 없다 → `staged` 가 비어 **tsc·eslint·jest 가 하나도 안 돌고** 통과.
**실증**: 일부러 실패하는 spec 을 그 형태로 커밋했더니 막히지 않았다(`0100d26`).
`git add` 를 따로 하니 정상적으로 막혔다.
→ 같은 명령에 `add`/`stage` 가 있으면 **거절**한다. 시험 4건 + 오탐 대조군.

★ **습관을 그렇게 들일 것**: `git add` 는 **항상 커밋과 별개의 Bash 호출**로.
★ 게이트가 돌았는지 확인하려면 `/tmp/vbc-api-jest.log` 의 **mtime** 을 본다.

---

## 2. 운영 장애 — Sager 로그인 불가 (해결)

**원인**: 2026-08-30 자정 크론이 체험 만료(+유예 3일)로 자동 정지.
**진짜 결함**: 되살릴 수단인 「Extender Prueba」가 **한 번도 동작한 적이 없었다** —
세 곳이 매장 관리자를 `roles.slug === 'admin'` 으로 찾는데, 가입 흐름이 만드는
관리자의 slug 는 **`store_owner`** 다(운영: store_owner 9 / admin 8).

★★ 사고의 형태: **정지시키는 쪽과 되살리는 쪽이 다른 규칙을 썼다.** 크론의
  `where: { slug: 'admin' }` 은 부모 include 가 `required:false` 라 **아무것도
  거르지 않았다**(kim 이 그날 정지된 것이 증거). 「아무나 정지, admin 만 연장」.

→ `store-admin-user.ts` 로 판정을 한 곳에. 역할 집합은 기존 `ADMIN_ROLES` 재사용.
→ **Sager(15)·kim(16) 은 `status='active'` 로 복구**(SQL, 사용자 승인).
  active 라 크론 대상에서 빠진다. 나머지 3곳(mana·Asado·naty)은 그대로 뒀다.

★ kim 은 **coolsistema 본인이 만든 시험 매장**이다(가입 email `coolsistema.arg@gmail.com`,
  2026-08-11 22분 사용, 판매 2건). 사용자 지시로 **그대로 둔다.**

---

## 3. 정지 사유 기록 — `users.suspended_reason` (CODEX P2·P3 를 한 컬럼으로)

`suspended` 가 되는 길이 둘인데 **결과 상태가 똑같아** 두 곳이 추론하고 있었다:
  · 로그인 안내가 **모든** 정지를 「체험 만료」로 말했다
  · 체험 연장이 **모든** 정지를 되살렸다(미납으로 막아 둔 매장까지)

**마이그레이션**: `2026-09-14-users-suspended-reason.sql` — nullable + CHECK NOT VALID.
**로컬 5432 · 운영 5434 양쪽 적용 완료.** 백필은 **근거가 분명한 것만**
(자정 정각 + trial_ends 과거) → 운영 3명이 `trial_expired`.

- 쓰는 쪽: 크론 `'trial_expired'` / 수동 정지 `'manual'`
- 읽는 쪽 둘이 **같은 함수**(`users/suspension-reason.ts`)를 쓴다
- 옛 행(null)은 **체험 만료라고 말할 때만** 추론. 아니라고 단정하지 않는다

★ 1차 수정은 **반쪽이었다** — 일괄 UPDATE 만 걸렀고 관리자 본인·매장 행은
  무조건 되살렸다(CODEX P1). `bfa7695` 가 되살릴 id 를 골라 그것만 바꾸고,
  관리자가 체험 만료 정지가 아니면 **거절**(`SUSPENSION_NO_ES_POR_PRUEBA`),
  매장 행은 **되살릴 사람이 있을 때만** 열도록 했다.

★★ 그런데 **그것도 반쪽이었다**(`bfa7695` 의 CODEX 검토, P1 x2):
  · 가드가 `status === 'suspended'` 만 봐서 **`inactive` 관리자**가 통과했다 —
    `extendTrial()` 이 무조건 `status='trial'` 로 덮으므로 소프트 비활성화한
    관리자가 되살아난다. 운영 store 9 에 id 21·22 **두 명** 있다(닿는다).
  · 판정을 트랜잭션 **밖**에서 하고 `WHERE id IN (...)` 로만 갱신해, 그 사이
    들어온 수동 정지를 덮어썼다(pm2 4워커).
  ⤷ `f0380fd`: 판정을 `store/trial-extension-plan.ts` 하나로 뽑고, 서비스는
    트랜잭션 안에서 `FOR UPDATE` 로 **다시 읽은 행**으로 판정한다. 관리자는
    허용 목록(`active`·`trial`·체험 만료 정지)으로만 통과하고, 모르는 상태값은
    거절한다(`ADMIN_NO_MODIFICABLE`). 프론트가 그 사유를 표시한다(app `72564f4`).
  ⤷ 검사도 함께 바꿨다 — 소스 문자열 대신 **판정을 돌려 안내 판정과 대조**한다.
    돌연변이 3건으로 「가드를 지우면 실제로 죽는지」 확인했다.

★★★ **그것도 아직 반쪽이었다**(`f0380fd` 의 CODEX 검토, P1 1 + P2 2). 셋 다
  **잠금 범위가 판정 범위보다 좁은** 같은 자리에서 나왔다:
  · `active` 관리자를 `trial` 로 강등 — `extendTrial()` 이 무조건 덮었다.
    `confirmFixedCustomer` 가 `active` 로 빼 놓은 고정 고객이 다시 크론 대상이
    된다(크론 조건은 `status='trial'` + 만료일 과거).
  · 잠금 대상을 트랜잭션 **밖** id 목록으로 좁혀, 그 사이 추가·이동된 사용자가
    잠기지도 판정되지도 않았다.
  · 관리자 **역할**을 트랜잭션 밖에서 읽어, 역할이 회수·이전되면 옛 관리자를 늘렸다.
  ⤷ `f00005c`: 존재 확인 말고는 **트랜잭션 밖에서 아무것도 읽지 않는다.**
    `where: { storeId }` 전체를 `FOR UPDATE`(id 오름차순) → 역할 재조회 → 관리자
    재선정 → 판정 → 갱신. 목표 상태는 판정기가 정하고 `extendTrial` 은 그것을
    **필수 인자**로 받는다(기본값을 두면 다음 호출부가 같은 실수를 한다).

★★★ **또 반쪽이었다**(`f00005c` 의 검토, P1 2 + P2 2) — 셋을 고치고 하나는
  고치지 않은 채 **한계를 코드에 적었다**:
  · [P1] 연장 커밋 **직후 크론이 다시 정지**시킨다. 크론이 만료 매장을 잠금 없이
    조회하고 그 결과를 그대로 믿었다. API 는 성공인데 매장은 닫혀 있다 —
    **Sager 장애와 같은 모양이다.** ⤷ 크론이 매장 행을 `FOR UPDATE` 로 잠그고
    **잠근 뒤 만료 조건을 다시 확인**한다. 이미 연장됐으면 건너뛰고 로그를 남긴다.
  · [P2] 락 순서가 `confirmFixedCustomer`(Store→Users)와 **반대**라 교착.
    ⤷ 연장도 `Store → Users(id↑)`. **매장 행이 세 경로의 공용 직렬화 지점**이 됐다.
  · [P2] `role.store_id` 미확인 → 남의 매장 역할로 관리자 판정. 전역 역할에
    `admin`·`gerente`·`superadmin` 이 실재한다. ⤷ 이 매장 역할만 인정.
    (운영 실측: 교차 연결 **0건** — 지금 동작은 그대로. 그래도 막았다.)
  · [P1 · **미수정**] 역할 변경 경합. 완전히 막으려면 `UserRole` 쓰기 **7곳**
    (가입 포함)이 같은 매장 행 잠금을 잡아야 한다 — 별건. 피해는 「만료일 하나」이고
    재실행으로 복구된다. **코드에 한계로 적어 뒀다**(해결했다고 쓰지 않기 위해).

★★★ **그리고 그 수정이 회귀를 만들 뻔했다**(`9eba587` 의 검토, P1):
  테넌트 필터를 `v.role?.storeId === storeId` 로 썼는데, 컨트롤러가
  `@Param('id') storeId: number` — **타입만** number 고 `ParseIntPipe` 가 없어
  런타임 값은 **문자열**이다. `3 === '3'` 이 거짓이라 **모든 역할이 걸러져
  언제나 404** 가 된다 — 이 기능이 2주간 죽어 있던 것과 **똑같은 모양**이다.
  ⤷ `35b0727`: `ParseIntPipe` + `rolPerteneceATienda()`(숫자로 맞춰 비교). **둘 다** 한다.
  ⤷ **CODEX 최종 검토 지적 0건.**

★ **다섯 바퀴 동안 「마감」이 네 번 마감이 아니었다.** 형태가 매번 같다 — 고친
  자리만 보고 **그 형태를 전수로 세지 않았다.** 이번 건들의 형태:
  ① 판정 범위 ≠ 갱신 범위  ② 잠금 범위 < 판정 범위  ③ 비교하는 두 값의 **타입**이 다르다.

---

## 4. 매장 완전 삭제 — **잠갔다**

`STORE_PURGE_STATUS = 'blocked'`. 실측 근거(스테이징에서 **실제 삭제까지** 해 봤다):
- `store_id` 보유 실제 표 **152개** 중 이 경로가 훑는 것은 **57개**
- 실데이터 매장은 FK 위반으로 **13곳에서 순차적으로 죽는다** (운영 14곳 중 13곳)
- 통과하는 단순 매장은 **고아 행을 남긴다**. 실제 COMMIT 으로 확인 —
  staging store 20 삭제 후 `stock_balances` 20행이 **없는 매장을 참조**
- 백업 17개 중 **복원이 되돌리는 것은 3개**뿐 → 17개 표 영구 손실

★★★ **`global_clients` 는 절대 건드리지 않는다**(사용자 절대 지시). 그 FK 가
  `ON DELETE NO ACTION` 이라 **매장 행 삭제 자체가 불가능**하다. 사용자 결정:
  매장 행을 지울 때는 **「가」— `created_by_store_id` 만 NULL**(고객 정보 불변).

★ 여는 조건 셋을 `STORE_PURGE_STATUS` 선언 옆에 적어 뒀다.

---

## 5. 다음 세션이 할 일 (우선순위)

1. **push 승인 요청** — CODEX 최종 지적 0건. api 5커밋 + app 1커밋 + 루트
   (서브모듈 포인터 + 이 문서)를 **함께** 올린다. 어느 쪽이 먼저 나가도 안전하다
   (프론트는 사유 표시만 추가, API 는 옛 프론트에서 종전 문구로 떨어진다).
   마이그레이션은 이미 양쪽 적용돼 있다.
   ★ 배포 후 확인: superadmin → Registraciones → 「Extender Prueba」가 실제로
     동작하는가(404 가 아닌가). 이 기능은 **한 번도 성공한 적이 없었다** —
     배포가 곧 검증이다. 대상은 정지 3곳(mana·Asado·naty) 중 하나.
2. **왕복 도구 P1 3건** — `scripts/store-roundtrip.sh` 의 `diff` 가 **불일치에도
   종료코드 0** · 빈 스냅샷 둘이 합격 · 없는 storeId 가 0건 스냅샷.
   합격 기준 도구가 이러면 자동화에 걸었을 때 **언제나 통과**한다. 여기부터
3. **W2 백업 완전성** — 발자국 211개 전수 + 시퀀스·삽입 순서 (지금 3개 표가 미포함)
4. **W3 복원 엔진** — 이 일의 **본체**. id 보존 in-place, UNIQUE 충돌(users.email),
   REDACTED NOT NULL 8개, FK 삽입 순서. 지금 3/20
5. W4 삭제 범위 분류표 + 커버리지 테스트 / W5 soft delete+유예 / W6 배치 실행기
6. `vendedor_devices` 활동 집계 (지금 0행이라 급하지 않음)

### 살아 있는 것 (정리하려면 말할 것)
- 스테이징 DUMMY 매장 **299개**(DUMMY-001 은 삭제 시험에 소모됨, 고아 행 20 남음)
- 스테이징 더미 매장 320 `guia.demo@ventago.test`

---

## 6. 이번에 배운 것

1. **「같은 사실을 두 곳이 소유한다」를 다섯 번 만났다** — 매장 관리자 판정(3곳) ·
   정지 사유(추론 2곳) · purge 잠금(서버/프론트) · 집계 창(서버/프론트) ·
   연장 판정(함수/SQL). **지적 하나를 받으면 그 형태를 전수로 세라**가 반복 확인됐다.
2. **반쯤 고친 것은 고친 것이 아니다 — 네 번 연속 그랬다.** 매번 「마감」이라 적고
   매번 CODEX 가 P1 을 냈다. 마지막 것은 **내가 만든 회귀**였다(타입 불일치로
   기능이 다시 항상 404). **「마감」은 검토 보고서를 읽고 나서 쓰는 말이다.**
7. **검증 도구가 없어도 「0건」으로 보인다.** `eslint --format compact` 는 이
   버전에 없어 안내문만 뱉는다 — 거기 grep 을 걸면 언제나 깨끗하다. 두 번
   헛통과했고 실제 오류는 커밋 게이트가 잡았다. 「내 줄만 깨끗한가」는
   **기준선과 개수를 대조**해서 본다.
3. **동어반복 검사를 썼다.** 「두 판정이 같다」를 재면서 한쪽이 다른 쪽을 부르게
   해 놨다 — 언제나 참이다. 진짜 위험은 **소비자가 그 함수를 안 쓰는 것**이었고
   실제로 그랬다.
4. **소스 문자열 검사는 돌연변이로 재야 한다.** purge 잠금 검사가 조건만 바꿔도
   통과했다 — 호출 토큰으로 바꾸니 잡혔다.
5. **「닿지 않는다」와 「막혀 있다」는 다르다.** 다매장 직원의 `trial_ends_at` 이
   전부 NULL 이라 P1 이 지금 데이터로는 안 터진다 — 그래도 고쳤다.
6. **화면이 숫자의 뜻을 말해야 한다.** 앱 현황이 「접속 중」이 아니라 「서버와
   대화한 앱」임을 화면에 적었다. 누적 22 vs 실사용 1 — 22배 과대평가가 가능하다.
