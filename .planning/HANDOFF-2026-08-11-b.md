# 핸드오프 — 2026-08-11 (2차 세션)

성격: **직전 핸드오프의 「남은 일」 소화.** 사용자가 고른 범위 4건 전부 완료 + 배포·검증.

> 배포된 것은 전부 **빌드 성공 + 컨테이너 재생성 + 배포본 실물 확인**까지 했다.
> 새로 **발견**된 것은 마지막 「새로 드러난 것」에 있다 — 하나는 지금 재현되는 500 이다.

---

## 1. VentaVista 필터 — 체크박스가 목록에 아무 영향이 없던 것 ✅

**원인**: 두 번째 그룹 `Crédito` 토글이 **기본 ON** 이면서 `paymentSlugs` 를 외상 3종으로
**덮어썼다**(`SalesListView.tsx:253-255`). 첫 그룹을 어떻게 눌러도 요청은
`paymentSlugs=credito,senia,favor` 고정. 외상 0원인 날은 목록이 통째로 비고,
요약은 이 필터를 안 타서 "요약 7건 / 목록 Sin datos" 라는 모순이 남았다.

**결정(사용자)**: 합집합 + Crédito 기본 OFF.
- Crédito 토글은 외상 3종을 첫 그룹 선택에 **더한다**. 아무것도 안 고르면 전체.
- OFF = 필터 없음 = 외상 포함 전체이므로 Phase 44 의도("진입 시 외상도 보인다")는 유지.
- **"4개 모두 = 전체" 지름길 제거.** PAYMENT_OPTIONS 는 매장 결제수단의 일부일 뿐
  (mercadopago·tarjeta-debito 없음) → 4개 선택과 필터 해제는 결과가 다르다.
- 조립을 `views/sales/list/paymentFilter.ts` 순수 함수로 분리 — 요청과 안내가 같은 값을 본다.

**0건 안내**: 걸린 필터를 이름으로 나열 + "요약은 이 필터를 적용하지 않는다" + Quitar filtros.
날짜는 이유로 세지 않는다(요약도 같은 날짜라 차이를 설명 못 한다).

`front ae73f47` (#603 SUCCESS)

---

## 2. `branch_agents.is_online` stale ✅

**★ 임계값 판정만 넣으면 안 됐다.** `last_seen_at` 은 setOnline/setOffline 때만 갱신됐다 —
갱신 경로 없이 임계값을 걸면 오래 붙어 있던 **정상** 에이전트가 전부 오프라인 오판된다.
갱신 경로가 먼저다.

1. **하트비트** (`PrintGateway`, 워커마다 60초) — 자기 워커 에이전트의 last_seen_at 만 갱신.
   ★ `@Interval` 을 쓰지 않았다: `main.ts:222-234` 가 **비리더 워커의 cron/interval 을
   전부 삭제**하는데(실측: 워커 1·2·3 에서 22개 해제) 소켓은 워커마다 흩어져 붙는다.
   스케줄러에 얹으면 리더 아닌 워커의 에이전트가 5분 뒤 통째로 죽는다.
2. **판정 단일 출처** (`print/agent-liveness.ts`) — `is_online AND (now-last_seen) <= 5분`.
   목록 API·터미널 라우팅·테스트 인쇄·`/print/temp` 게이트가 전부 같은 함수를 쓴다.
3. **리퍼** (리더 전용 `@Cron` 1분) — stale 행을 내리고 `agent_status_changed` push.
   내릴 때마다 언제부터 신호가 없었는지 warn. agent_type 예외 없음 —
   edge 도 15초 health probe 가 `AgentKeyGuard` 에서 last_seen_at 을 갱신한다(확인함).

`api 1317317` (#672 SUCCESS)

---

## 3. 별건 4건 ✅ (`api 33c997d` #673 / `front f0efb27` #604)

### 3-1. `sales-stock-guard.spec` 이 **0 tests 로 침묵**하고 있었다
`stocks.model` 이 `ProductBranch` 를 import 하는데 그 모델이 `products.model` ↔
`products-branch.model` 순환 안에 있다 → 진입 순서에 따라 **초기화 도중**에 읽혀
`INGRESO_LEDGER_NOTE_PREFIXES` 가 undefined → 구조분해가 던져 suite 자체가 안 돌았다.
**실패가 아니라 0건이었다.** 17개 테스트가 아무것도 안 지킨 채 통과처럼 보였다.
→ 상수를 import 없는 leaf `stocks/stocks.constants.ts` 로 분리. 모델은 re-export 만 하므로
  기존 34개 import 는 그대로 동작한다. **모델 파일에 상수를 새로 넣지 마라.**

### 3-2. 매장을 활성화해도 로그인이 안 되던 경로
`PUT /store/:id` 는 Store row 만 갱신하고 Users.status 를 안 건드렸다. 화면은 성공이라
말하는데 유저는 계속 suspended. 코드에는 경고 로그만 있었고 고쳐지지 않았다.
→ 활성/비활성 **전이 시에만** `usersService.activate/deactivateStoreUsers` 위임.
  ★ 값이 그대로일 때 위임하면 **개별로 정지시킨 유저까지 일괄 활성화**된다(회귀).
    FormData 문자열 `'true'` 정규화 **뒤**에 판정하는 순서도 테스트로 고정.

### 3-3. [보안] X-Forwarded-For 첫 값을 rate-limit 키로 쓰던 것
운영 nginx 실측: `X-Real-IP $remote_addr`(**덮어씀**) / `XFF $proxy_add_x_forwarded_for`(**덧붙임**).
즉 XFF 첫 값 = 요청자 입력 → 헤더만 바꿔가며 버킷을 무한 생성해 로그인 제한을 통째로 우회.
같은 파싱이 세션의 지점 IP 등록 4곳에도 있었다.
→ `common/net/client-ip.ts` 의 `trustedClientIp()` 단일 출처
  (x-real-ip → XFF **마지막** 값 → socket peer).
  정상 트래픽은 값 동일(운영 `branch_ip_registries` 전수 확인 — 사설 대역 0건 → 기존 등록 IP 안 어긋남).
  전제: **nginx 가 유일 진입점**. 실측으로 외부에서 5002/5001 미도달, 443 만 열림.
  **앱 포트를 열게 되면 이 신뢰를 먼저 다시 설계해야 한다.**

### 3-4. `.fuse_hidden` 잔여 파일 6개 삭제 + .gitignore
`app.module`·`mobile-auth.service`·`menuRegistry` 등 **살아있는 파일의 오래된 사본**이
같은 디렉터리에 커밋돼 있었다. 참조 0건 확인 후 제거.

---

## 4. 승인 대기 로그인 안내 ✅ (`api 4562b82` #674 / `front ef1af8f` #605)

신청자는 오타를 냈을 때와 **똑같은 화면**을 봤다(users 행은 승인 시점에야 생기고,
Phase 63 이 두 실패를 같은 401 로 통일했으므로).

**열거 방지를 지키는 방법**: 비밀번호가 맞을 때만 알린다. `pending.passwordHash` 는 이미
저장돼 있고 승인 시 재사용되므로 올바른 비밀번호를 아는 사람 = 본인이다.

**★ 타이밍 누출도 같이 막았다.** 종전은 미존재=bcrypt 0회 / 존재=1회 —
문구로 막은 열거가 **응답 시간으로 그대로 새고 있었다**. 이제 모든 경로가 정확히 1회
(신청 없으면 더미 cost-10 해시). 운영 실측: 존재 0.7526s / 미존재 0.7585s — 구별 불가.

**★ 만료는 status 로 판정하지 않는다.** `expired` 전이가 제품 어디에도 없어 만료된 신청이
`contact_pending` 으로 남아 있다(운영 3건). 상태값만 믿으면 만료자에게 "심사 중입니다"라는
**거짓 안내**를 하게 된다. `expires_at` 을 같이 본다.

**중복 정책(사용자 결정)**: 진행 중일 때만 차단.
`start()` 가 CUIT 만 보던 것을 email·username 까지 확장. 종료된 신청은 막지 않는다(재신청 정상).
어느 항목이 겹쳤는지는 안 알려준다(열거). DB 부분 UNIQUE 인덱스로 동시 요청까지 차단.
기한 지났는데 상태가 남은 유령 행은 재신청 시점에 `expireStaleFor()` 가 치운다 —
안 치우면 인덱스가 **본인의 재신청까지** 거부한다. 크론을 새로 만들지 않았다.

**모듈 순환 회피**: OnboardingModule → AuthModule 이 이미 있으므로 역방향 금지.
모델만 의존하는 `PendingLookupModule`(조회 전용)을 떼어 AuthModule 이 그것만 쓴다.

**프론트**: 토스트가 아니라 **지속형 카드**. 신청자는 토큰 없이 `/onboarding/status` 를
열 수 없어 이 화면 말고 상태를 볼 곳이 없다. 문구가 아니라 `code` 로 분기한다.

**마이그레이션**: `2026-08-11-pending-registrations-active-unique.sql`
→ 로컬 5432 + 운영 5434 **양쪽 적용 완료, 인덱스 8개 일치 대조 확인.**

---

## 새로 드러난 것

### ★★ 온라인주문에 **부모 fallback 이 명시적으로 있다** — 지금 재현되는 500
미회신이던 CODEX 자문(`review-request-madre-other-write-paths.md`)의 회신을 받고
**직접 코드로 검증**했다. `EnvioRegistroModal.tsx:110-122`:

```ts
// 분해 결과가 0개면 parent 한 건으로 fallback
return rows.length > 0 ? rows : [{ productId: p.id, ... }]
```

→ `online_order_items.product_id`(부모) → `holdStock()` (**주문 생성 트랜잭션 안**,
`online-orders.service.ts:427`) → `ProductBranch.findOrCreate(부모)` → `Stocks.create`
→ `trg_stocks_leaf_only` 거부 → **500**.

오늘 고친 판매 마드레 500 과 **같은 결함**이고, 실패 시점은 발송이 아니라 **주문 생성**이다.
판매는 `api 3915ae4` 로 막았지만 이 경로는 열려 있다.
(불행 중 다행: 이 경로는 원본 오류를 ERROR 로 남기고 재전파하므로 원인이 덮이지는 않는다.)

**과거 데이터는 보정 불필요** — 운영에서 "현재 부모인 상품에 붙은 원장 행"을 전수 조회한
결과 9개 상품이 나왔으나 **net 이 전부 0**(2026-08-08 구조 전환의 `migration_transfer`
짝 보정으로 이미 상쇄). 미해소 잔액 0건.

전체 분석·우선순위는 `.gsd/review-request-madre-other-write-paths.md` 하단.
요약 우선순위: ① 온라인주문 프론트 fallback 제거(가장 작고 급함) ② 판매 취소의 legacy
부모 품목 명시 거부 ③ 재고 조정 API 경계 400 ④ 생산은 **단순 400 금지**(변형 배분 모델 선행).

### 프린터 에이전트 6대가 전부 offline
21:00 시점 실측. 마지막 신호는 18:54(컨테이너 재시작 시 disconnect). 영업 종료라면 정상이나,
`is_online` 판정을 고친 지금은 화면이 실제 상태를 보여줄 것이므로 다음 영업일에 확인 요망.

---

## 남은 일 (직전 핸드오프에서 이월 + 신규)

- **★ 온라인주문 부모 fallback** (위) — 가장 급함
- **AFIP 인증서 `nam` 8/17 만료** — 이제 6일 미만. `AFIP_CERT_ALERT_EMAIL` 미설정. (미착수)
- Phase 78 Wave 2 (L0 카탈로그 + 드리프트 게이트) 미착수
- Phase 77(솔리시투데스) 보류 — Wave 0 결정 D1/D4/D6 미확정
- 재설치 중인 별도 서버 — 사양 확인 후 D3 재판단. **스왑을 처음부터 넣을 것**
- CODEX 미검토 쓰기 경로: `code-import`, `productStock.service`, 공용 `stocks.service`
- 판매 트랜잭션 첫 실패가 앱 로그에 안 남는 문제(직전 핸드오프 1번) — 여전히 미해결

---

## 배포 요약 (전부 빌드 성공 + 컨테이너 재생성 + 배포본 실물 확인)

| 커밋 | 내용 | 빌드 |
|---|---|---|
| front `ae73f47` | VentaVista 결제수단 필터 + 0건 안내 | #603 |
| api `1317317` | is_online 하트비트 + 임계값 + 리퍼 | #672 |
| api `33c997d` | 별건 4건 (spec 순환 / store 활성화 / XFF / fuse) | #673 |
| front `f0efb27` | .fuse_hidden 3개 + .gitignore | #604 |
| api `4562b82` | 승인 대기 로그인 안내 | #674 |
| front `ef1af8f` | 승인 대기 지속형 카드 | #605 |

DB: `2026-08-11-pending-registrations-active-unique.sql` — 로컬 5432 + 운영 5434 적용, 대조 완료.

---

## 작업 방식 메모

- **뮤테이션 확인을 이번에도 새 테스트마다 했다** — 11종 전부 사멸 확인.
  특히 "전이 판정 제거 → 항상 위임" 뮤테이션이 잡아낸 것이 3-2 의 회귀 위험이다.
- **CODEX 회신은 근거까지 대조했다.** 온라인주문 부모 fallback 주장은 코드를
  끝까지(프론트 → DTO → holdStock → Stocks.create) 따라가 확인했고, "과거 데이터 보정
  필요" 부분은 실측으로 **불필요**로 뒤집었다(net 전부 0).
- **이번에도 반복된 형태는 "부재가 침묵한다"** 였다:
  spec 이 0건으로 안 돌기 / 필터 0건에 설명 없기 / is_online 이 죽은 소켓에 true /
  매장 활성화가 성공이라 말하고 로그인은 거부 / 만료 전이가 기록되지 않아 거짓 안내.
- **grep 은 `-a` 를 붙여야 한다** — 이번에도 한 번 조용히 0건이 나왔다(코크핏 import 확인).

---

# 추가 세션 (같은 날, 이어서) — 남은 것 4건

사용자 지시: "차례대로 남은 것을 진행해줘." 4건 전부 완료·배포·검증.

## 5. CODEX 자문 문서화 ✅ (`e59303f`)
VCode 채번 자문 결과를 `.gsd/review-request-vcode-branch-numbering.md` 에 기록.
CODEX 가 내 A안(신규 컬럼)을 기각하고 **D안**(기존 `daily_number` 의 의미 전환)을 냈다.

## 6. ★ 온라인주문 부모 fallback ✅ (`api 986e0d0` #675 / `front d08e2b5` #607)
판매 마드레 500 과 **같은 결함**이 온라인주문에 열려 있었고, 실패 시점이 **주문 생성**이었다.
프론트에서 새는 곳이 하나가 아니라 **셋**이었다(명시적 fallback / 변형 조회 실패 시 /
variantQuantities 부재 시). 셋 다 막고 전송 자체를 차단.
백엔드도 트랜잭션 진입 전 400 — **API 가 최종 방어선**이다.
판정은 `products/madre-guard.ts` 로 뽑아 판매와 공유(정책은 경로별로 유지).

## 7. 채번 단일화 ✅ (`api 3ccdac9`)
온라인주문 미러가 **다른 채번**을 하고 있었다: advisory lock 없음 + **where 에 날짜 조건 없음**
(주석은 "오늘" 인데 실제로는 전 이력 최대값+1) + `sale_day_local` 미저장.
→ `DailyNumberService` 로 단일화. 지점별 채번 전환 전에 반드시 필요했던 정리다.

## 8. AFIP 인증서 ✅ (`api 07cae50` #676)
**핸드오프의 "nam 8/17 만료"는 사실이지만 Ventago 장애 위험이 아니다.**
`/app/certificates` 110개는 다른 시스템과 공유하는 디렉터리이고, Ventago 의
`afip_issuers` 는 **2개뿐**이다 — `coolsistema`(10/20, 69일) / `coolsyncrohomo1`(691일).
이미 만료된 34개와 60일 내 만료 32개는 **전부 남의 것**이다.

감시 자체는 정상 동작 중이었다(오늘 08:10 Telegram 발송 로그 확인). 빠진 건 이메일뿐.
- `.env` 에 `AFIP_CERT_ALERT_EMAIL=junghokim10@gmail.com` 추가(사용자 승인).
  백업: `.env.bak.2026-08-11`. 컨테이너 반영 확인.
- 경보가 남의 것에 묻히지 않도록 `inUse` 구분 — 제목의 긴급도도 **사용 중 기준**.
  조회 실패 시엔 **전부 사용 중으로 간주**한다(조용한 축소 금지).

## 배포 (전부 빌드 성공 + 컨테이너 재생성 + 배포본 실물 확인)

| 커밋 | 내용 | 빌드 |
|---|---|---|
| api `986e0d0` / front `d08e2b5` | 온라인주문 부모 fallback 차단 | #675 / #607 |
| api `3ccdac9` | 채번 DailyNumberService 단일화 | (#675 동반) |
| api `07cae50` | AFIP 경보 사용분 구분 | #676 |
| front `3f7a74f` | VentaVista 표시 개선 | #606 |

## 다음 세션 우선순위

1. **판매 취소의 legacy 부모 품목** — 과거 판매를 취소하면 여전히 500.
   자동 전개 금지(변형 배분 정보 없음). 명시적 거부 + 운영 보정 목록이 맞다.
2. **재고 조정 API 경계** — 화면은 leaf 만 주지만 API 는 부모 PB 를 안 막는다.
3. **생산 경로** — 단순 400 금지. 변형 배분 모델 선행.
4. CODEX 미검토 쓰기 경로: `code-import`, `productStock.service`, 공용 `stocks.service`
5. 지점별 채번(2단계) — 두 번째 지점이 실제 운영에 들어갈 때.
   선행: terminal NULL 정책 → `sales.branch_id` → cutover 설계 → 오프라인 귀속 강제
6. Phase 78 Wave 2 / Phase 77 미착수
7. 판매 트랜잭션 첫 실패가 앱 로그에 안 남는 문제 — 여전히 미해결
