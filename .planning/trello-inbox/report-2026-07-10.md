# Trello 반자동 유지보수 루프 리포트 — 2026-07-10

## ⚠ 동기화 실패 — 수정 작업 없이 종료

- `latest.json` 의 `syncedAt`: **2026-07-06T17:42:01.618Z**
- 실행 시각(UTC): **2026-07-10T12:13Z**
- 경과: 약 **3일 19시간** (24시간 초과)

launchd 의 06:50 Trello 동기화가 **2026-07-06 이후 갱신되지 않았습니다.** 오래된 스냅샷을 기준으로 카드를 트리아지/수정하면 이미 이동·수정된 카드를 잘못 처리할 위험이 있어, 이번 실행은 **트리아지 및 자동 수정을 건너뜁니다.**

### 조치 필요 (Mac 에서 실행)

```bash
node tools/trello-sync.js
```

- 동기화 로그 확인: `/tmp/trello-sync.log`
- launchd 스케줄(06:50)이 최근 3일 이상 미실행/실패했는지 함께 점검 필요:

```bash
launchctl list | grep -i trello
tail -50 /tmp/trello-sync.log
```

동기화가 정상 갱신(`syncedAt` 이 24시간 이내)되면 다음 스케줄 실행에서 정상적으로 트리아지 및 자동 수정이 재개됩니다.

## 참고

- `triage-state.json` 은 이번 실행에서 **변경하지 않았습니다** (마지막 트리아지: 2026-07-07).
- 마지막 정상 스냅샷 기준 미해결 승인 대기 항목(예: B3 `mes_material_supplier_payments` OWNER 확인, B8 아카이브 추정 카드 등)은 동기화 복구 후 재검토합니다.

---

## 재확인 (2026-07-11 02:36Z 실행)

같은 날 두 번째 스케줄 실행에서도 **동기화가 여전히 복구되지 않았습니다.**

- `latest.json` 의 `syncedAt`: **2026-07-06T17:42:01.618Z** (변화 없음)
- 실행 시각(UTC): **2026-07-11T02:36Z**
- 경과: 약 **4일 9시간** (24시간 임계 계속 초과)

두 번 연속 동일한 오래된 스냅샷이 관측되어, launchd 06:50 동기화 잡이 **일시적 실패가 아니라 지속적으로 멈춘 상태**로 보입니다. Mac 에서 아래를 직접 실행해 원인을 확인해 주세요:

```bash
cd ~/Trabajos_Programming/ACE_online_1.0
launchctl list | grep -i trello       # 잡 로드/활성 여부
node tools/trello-sync.js             # 수동 동기화 (오류 즉시 확인)
tail -n 50 /tmp/trello-sync.log       # 최근 실패 로그
```

의심 원인: launchd 잡 언로드/비활성, Trello API 토큰·키 만료(401), 네트워크/Node 오류. 동기화 정상화 후 다음 사이클에서 트리아지·검증·자동수정이 재개됩니다. 이번 실행에서도 카드 수정·`triage-state.json` 변경은 없습니다.

---

## ✅ 점검 및 복구 (2026-07-11 02:40Z — 사용자 "점검해줘")

동기화를 **agent-runner 큐 경유로 강제 실행하여 복구 완료**했습니다.

**진단 결과 (근본 원인 격리):**
- `agent-runner` launchd 잡은 **정상 가동 중** — 큐 마지막 작업 `j30`(commit-push-main)이 2026-07-11 00:39Z 완료 확인. 즉 Mac 은 켜져 있고 러너는 살아 있음.
- `trello-sync.js` 스크립트·`.trello-credentials.json`·`com.ventago.trello-sync.plist`(06:50) 모두 파일상 정상.
- 큐에 `ping`+`trello-sync` 투입 → **둘 다 exit 0**. sync 출력: `OK — 리스트 9개, 카드 12개`. → **Trello 토큰·네트워크 모두 정상.**
- ∴ 유일한 실패 지점은 **`com.ventago.trello-sync` 06:50 launchd 스케줄만 안 firing** (agent-runner launchd 와 달리). 마지막 정상 sync 가 07-06 14:42(수동/러너 실행 시각)였고 이후 06:50 산출물이 없음 → 이 잡이 로드 해제됐거나 부팅 이후 재등록되지 않은 것으로 추정.

**결과:** `latest.json` `syncedAt` = 2026-07-11T02:40:09Z (신선). 리스트 9 / 카드 12.

**영구 수정 (Mac 에서 직접 — launchd 재등록):**
```bash
cd ~/Trabajos_Programming/ACE_online_1.0
launchctl list | grep -i ventago            # trello-sync 가 목록에 없으면 미로드 확정
cp tools/com.ventago.trello-sync.plist ~/Library/LaunchAgents/
launchctl bootout gui/$(id -u)/com.ventago.trello-sync 2>/dev/null   # 기존 잔재 제거(있으면)
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.ventago.trello-sync.plist
launchctl kickstart -p gui/$(id -u)/com.ventago.trello-sync          # 즉시 1회 테스트
tail -n 30 /tmp/trello-sync.log
```
> agent-runner 는 켜져 있는 한 큐로 언제든 `trello-sync` 를 돌릴 수 있으니, 위 launchd 복구 전까지 임시로는 러너 큐가 백업 경로가 됩니다.

**MCP 설치 관련:** 레지스트리에 Trello 전용 MCP 커넥터는 없습니다(보드류는 Miro·Smartsheet 뿐). 현재 key/token REST 스크립트가 유일한 연동 경로이므로, 별도 MCP 설치 대신 위 launchd 스케줄 복구가 정답입니다.
