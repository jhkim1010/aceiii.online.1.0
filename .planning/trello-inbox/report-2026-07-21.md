# Trello 반자동 유지보수 루프 — 리포트 2026-07-21

## ⚠ 동기화 실패

`latest.json` 의 `syncedAt` 이 **2026-07-19T20:10:52Z** 로, 현재(2026-07-21T20:34Z) 기준 약 **48.4시간 전**입니다. 24시간 임계값을 초과했으므로 **동기화 실패**로 간주하고, 이번 실행에서는 트리아지·수정 작업을 진행하지 않았습니다.

### 조치 필요 (사용자 Mac 에서 실행)
```bash
node tools/trello-sync.js
```
- 실행 로그 확인: `/tmp/trello-sync.log`
- launchd 스케줄(06:50 자동 sync)이 최근 이틀 돌지 않은 것으로 보입니다. `/tmp/trello-sync.log` 가 없거나 마지막 기록이 07-19 라면 launchd 에이전트 미등록/중단 가능성(메모리 `project_trello_sync_launchd` 참조 — plist 를 `~/Library/LaunchAgents` 에 bootstrap 했는지 확인).

### launchd 점검 명령 (참고)
```bash
launchctl list | grep -i trello
tail -n 30 /tmp/trello-sync.log
```

## 요약
- 최신 스냅샷이 48시간 이상 오래되어 신규/갱신 카드 판정이 신뢰 불가 → 수정 착수 안 함.
- 보드 실시간 상태를 반영하지 못하므로 이미 해결/신규 카드 분류를 건너뜀.
- 재동기화 후 다음 실행에서 정상 트리아지가 진행됩니다.

## 참고 (직전 상태, 07-20 트리아지 기준 — 재검증 전)
동기화만 복구되면 다음 실행에서 아래 대기 항목들이 재검토됩니다.
- **needs-repro (사용자 한 줄 설명 대기):** Pago de proveedores(6a567a1d), Lotes(6a57a408), Cambiar precio de los productos(6a58eb9a), App vendedor(6a58f98c), Error de vendedor(6a591d7b), Ventas suspendidas(6a592a90), VentaGo Zebra Agent sin icono(6a59189c).
- **hechosPending=true (다음 sync 가 Hechos 로 이동 예정):** Talleres corte→local(6a57a7a6, already-fixed / Phase 58 배포 확인됨).
- **승인 대기 (push/릴리스):** print-agent 이진화 릴리스 태그(6a50f71f), zebra-agent 상품검색 릴리스(6a50ec34).
