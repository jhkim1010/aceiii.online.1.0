# Trello 유지보수 리포트 — 2026-07-20

- 동기화: `latest.json` syncedAt = **2026-07-19T20:10:52Z** (약 24시간 전, 임계값 이내 → 정상 처리)
- 보드 카드 19건 / triage-state 47건
- **신규 카드: 0건** (전 카드 기존 추적 대상)
- 자동 수정: **0건** (검증 통과한 신규 버그 없음 — 아래 사유 참조)

---

## 1. 새 카드
없음. 마지막 신규 유입은 2026-07-16(Venta Control Online 3건 + VentaGo 1건)이며 모두 이미 트리아지 완료 상태입니다.

---

## 2. 이미 해결됨 (확인 요망)

### ✅ Talleres — "¿Cómo sabría si un corte está terminado?" (`6a57a7a6`, [카드](https://trello.com/c/ZBew9gDP))
- 증상: cut line 이 전 단계 완료로 보이는데 **local(매장)으로 보낼 방법이 없음**.
- 해당 기능은 "완제품 입고(Ingresar a stock)" 로 이미 구현되어 있었고, 그동안 **프론트 커밋이 origin/main 에 없어** 운영에 노출되지 않았던 상태였습니다.
- **2026-07-20 검증 결과**: 프론트 `ee28a97` (IngresoStockDialog + LoteDetailDrawer 버튼)이 Phase 58 통합 push `d018a58` 에 포함되어 **origin/main 반영 확인**, Jenkins front `#445 SUCCESS` → **운영 배포 완료**.
- 백엔드 `b187165` (POST `/talleres/lotes/:id/ingreso-stock`)는 이전부터 배포됨.
- 조치: triage-state `hechosPending=true` 설정 → 다음 06:50 sync 가 카드를 **"Hechos (Corregidos en nube)"** 로 이동시킵니다.
- **사용자 확인 요망**: 운영 화면 `Talleres → Lotes → 로트 상세 → "Ingresar a stock"` 버튼 노출 및 창고/매장 선택 후 재고 반영 여부.

### ✅ Hechos 리스트로 이동 확인 (done-external 확정)
| 카드 | 커밋 | 상태 |
|---|---|---|
| Impresion de precios (`6a591931`) | api `602173e` (origin/main) | Hechos 이동 확인 |
| Impresion de ticket de venta (`6a5675a3`) | front `37f1a99` (origin/main) | Hechos 이동 확인 |
| Reimprimir ticket (`6a566b67`) | front `37f1a99` (origin/main) | Hechos 이동 확인 |

---

## 3. 수정 완료
없음. 이번 실행에서 **코드 수정·커밋·브랜치 생성은 하지 않았습니다.**

사유: 미해결 카드 7건이 모두 `needs-repro`(스크린샷/실데이터 의존)로, "존재 검증" 게이트를 통과하지 못했습니다. 검증 없는 추측성 수정은 금지 규칙에 해당합니다.

---

## 4. 보류 (needs-repro) — 사용자 한 줄 확인 필요

| 카드 | 필요한 정보 |
|---|---|
| **Pago de proveedores** (`6a567a1d`) | 어느 결제 경로인지 — taller(subcon) 정산 vs materia-prima 공급업체 결제(`mes_material_supplier_payments`). 유사 버그는 `e320088`(07-06 배포)로 수정됨 → 회귀인지 별개 경로인지 구분 필요 |
| **Lotes** (`6a57a408`) | "talleres asignados 를 표시/선택 못 함" 의 정확한 지점 — 드롭다운이 비어있는지 / 필터가 잘못됐는지 / 기선택이 안 되는지 |
| **Cambiar precio de los productos** (`6a58eb9a`) | ★아래 코드 조사 결과 참조 |
| **App vendedor** (`6a58f98c`) | 설명 없이 스크린샷 1장 — 증상 한 줄 설명 |
| **Error de vendedor** (`6a591d7b`) | `vendedor@cool` 유저의 `users.name` / `last_name` 실제값. 앱 표시는 별도 Flutter repo 로직 |
| **Ventas suspendidas** (`6a592a90`) | 카드 설명이 "wc" 로 잘려 증상 미완성 — 기대 동작 보완 |
| **VentaGo Zebra Agent sale sin icono** (`6a59189c`) | ★아래 코드 조사 결과 참조 |

### 코드 조사 결과 (이번 실행 신규)

**Cambiar precio de los productos** — `/precios` → `src/views/codigo-vista/CodigoVistaView.tsx`
- `handlePriceBlur` 는 **탭(blur) 시 해당 가격 레벨 1건만** `POST /products/bulk-update-prices` 로 저장합니다.
- 다른 가격 레벨의 재계산은 **별도 "비율 적용(applyRatios)" 버튼** 경로에만 존재합니다.
- 즉 "PRECIO 1 을 바꾸고 탭 → 나머지 가격 자동 갱신"은 **현재 설계상 미구현**이며, 버그라기보다 **기능 요청**일 가능성이 높습니다.
- 이미지가 자동으로 들어간 증상은 `saveAllChanges` 의 갤러리 업로드 경로가 의심되나 재현 불가.
- **확인 요망**: (a) 나머지 가격 자동 재계산이 기대 동작이 맞는지(맞다면 feature 로 전환하여 스펙 작성) (b) 이미지가 어느 조작 직후에 생겼는지.

**VentaGo Zebra Agent sale sin icono** — `zebra-agent/package.json`, `main.js`
- `build.icon = renderer/assets/icon-512.png` (512×512 실제 존재), `tray-icon.png` 존재, `createTray()` 정상 — **print-agent 와 동일한 설정**이며 설정상 결함은 발견되지 않았습니다.
- 따라서 **아이콘 리소스가 추가되기 이전 빌드본이 설치되어 있거나**, Windows 아이콘 캐시 문제일 가능성이 높습니다.
- **조치안**: zebra-agent 릴리스 태그 재발행 → 매장 PC 재설치 후 재확인 (릴리스 태그는 승인 대기 항목).

---

## 5. 승인 대기 (사용자가 직접 실행)

1. **zebra-agent / print-agent 릴리스 태그 발행** — 다수 카드(아이콘, 라벨 4모드, 자동 업데이트, RAW 인쇄 픽스)가 에이전트 재빌드에만 의존해 대기 중입니다.
2. **Talleres "Ingresar a stock" 실기 확인** (운영 화면).
3. 운영 DB / SSH / docker 조작 — 이번 실행에서 필요 없었습니다.

---

## 6. 참고 — 마지막 로그 파일 점검 (`api-ventago/logs/combined-2026-07-20.log`)

로컬 dev 환경에서 알려진 병목이 여전히 관측됩니다 (Trello 카드와는 무관, 운영 아님):

```
[SlowQuery] 🔴 [45352ms] table=sync_outbox  SELECT ... status='pending' ...
[DatabasePool] ⚠️ 커넥션 대기 발생! size=8 using=2(3%) available=0 waiting=2 max=80
[OnlineOrdersExpiryCron] cron 본체 실패: Operation timeout
[Outbox] tick 스킵 — 이전 tick 실행 중 (연속)
```

- `sync_outbox` pending 조회가 3s → 45s 로 악화. pool 은 max=80 대비 using=2 로 여유가 있으나 **느린 쿼리가 커넥션을 붙잡아 waiting 이 발생**하는 형태입니다.
- 권장(승인 대기): `sync_outbox (status, next_retry_at)` 복합 인덱스 확인 + 처리 완료 행 아카이빙. 로컬 재현이므로 운영 영향은 별도 확인 필요.

---

## 7. 사용자 직접 실행용 검증 명령

```bash
# 프론트 lint (샌드박스에서 실행 불가)
cd ~/Trabajos_Programming/ACE_online_1.0/ventago-app && npm run lint

# 백엔드 lint
cd ~/Trabajos_Programming/ACE_online_1.0/api-ventago && npm run lint

# sync_outbox 인덱스 확인 (로컬 PG18)
psql -p 5432 -d ventago -c "\d sync_outbox"
psql -p 5432 -d ventago -c "SELECT status, count(*) FROM sync_outbox GROUP BY status;"
```
