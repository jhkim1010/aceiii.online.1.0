# Trello 유지보수 루프 리포트 — 2026-07-27

동기화: 2026-07-27 09:50 UTC (정상, 24h 이내). 카드 22건 / 리스트 11개.
시스템 점검: `api-ventago/logs/error-2026-07-27.log` **비어 있음**(에러 0), combined 로그 정상 — pool 낭비 없음.

## 새 카드 (2건)

| 카드 | 리스트 | 분류 | 처리 |
|---|---|---|---|
| **Codigo Vista** (6a635c22, 07-24) | Producto Control OnLine | bug | ★수정 완료 (아래) |
| **Registro de tienda** (6a64bb6f, 07-25) | Admin de tienda | bug? | needs-repro (설명 없음) |

## 수정 완료 (1건)

### Codigo Vista — CÓDIGO 컬럼 미표시 + 가로 스크롤로도 못 봄 (6a635c22)
- **검증된 메커니즘** (코드 정독, 파일 2026-06-11 이후 무변경 → 증상 현존):
  1. CÓDIGO 셀이 `width 고정 + overflow:hidden + ellipsis` → 컬럼 폭보다 긴 SKU 는 잘리고, **셀 내부 잘림은 테이블 가로 스크롤로 볼 수 없음** — 카드의 "tampoco se puede recorrer mas a un costado" 와 정확히 일치.
  2. 컬럼 폭이 localStorage 에 영구 저장 + 드래그 최소 40px → 실수로 좁힌 폭(코드 사실상 안 보임)이 세션을 넘어 지속.
- **픽스** (GSD Plan→Execute→Review, SPEC: `.gsd/spec-trello-6a635c22-codigo-vista.md`):
  - 저장된 컬럼 폭 로드 시 60px 최소 클램프(정합성 보정) + 드래그 최소값 40→60 통일
  - CÓDIGO 셀에 전체 코드 Tooltip — 잘려도 hover 로 항상 확인 가능
- **브랜치/커밋**: ventago-app `fix/trello-6a635c22` → **8740d40** (로컬만, push 안 함)
- **검증**: `npx eslint src/views/codigo-vista/CodigoVistaView.tsx` → 오류 0. main 워킹트리 원상 복구(클린).
- **주의**: 카드 스크린샷은 Trello 인증 필요로 미열람 — 폭 문제가 아닌 다른 원인(예: 데이터)일 가능성도 있으므로 배포 후 실화면 확인 요망.

## 보류 (needs-repro)

- **Registro de tienda (6a64bb6f, 신규)** — 설명 없이 스크린샷 1장. 매장가입 흐름은 현재 활발히 개발 중(최신 커밋 97088fc '가입 마지막에 승인 대기 안내 화면')이라 이미 해결됐을 가능성도 있음. **어떤 증상인지 한 줄 설명 요청.**
- 기존 7건 (Pago de proveedores, Lotes, Cambiar precio, App vendedor, Error de vendedor, Ventas suspendidas, Zebra sin icono) — 07-22 재검증 이후 활동 없음, 상태 유지.

## 완료 확인 (Hechos Semanales)
- Talleres, Impresion de precios, Impresion de ticket, Reimprimir ticket — done-external 유지, 변동 없음.

## 승인 대기 (push / 배포 / 운영 DB)
- **ventago-app `fix/trello-6a635c22` (8740d40) push + Jenkins front 빌드** — 승인 시 main 병합 후 push.

## 사용자 검증 명령 (Mac)
```bash
# 1) 픽스 diff 확인
cd ~/Trabajos_Programming/ACE_online_1.0/ventago-app && git diff main fix/trello-6a635c22

# 2) lint 재확인
npx next lint --file src/views/codigo-vista/CodigoVistaView.tsx

# 3) 승인 시 병합·push (Jenkins front-coolsistema 자동 트리거)
git merge fix/trello-6a635c22 && git push origin main

# 4) 샌드박스 브리지가 남긴 stale git lock 잔재 정리 (커밋 과정에서 다수 생성됨)
find .git -name '*.lock*' -not -path '*/objects/*' -delete
```
