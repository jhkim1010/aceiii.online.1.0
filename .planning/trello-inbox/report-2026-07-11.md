# Trello 반자동 유지보수 리포트 — 2026-07-11

- 동기화: `latest.json` syncedAt = **2026-07-11T02:47:30Z** (실행 시각과 동일, 정상 ✓)
- 보드 카드 수: 12개 / 신규·미처리: **4건** (모두 신규 버그 신고)
- 이번 실행 자동수정: **1건 커밋** (2개 카드 동시 해결) — push/배포는 승인 대기

---

## 1. 새 카드 (신규 트리아지)

| 카드 | 리스트 | 분류 | 처리 |
|------|--------|------|------|
| [Ticket](https://trello.com/c/ii9rSMG8) `6a50f71f` | Venta Control Online | bug | ✅ 수정 완료 (577fbdf) |
| [Apretar ticket con el ctrl+r](https://trello.com/c/DdByURr1) `6a4e6bae` | Venta Control Online | bug | ✅ 수정 완료 (577fbdf) |
| [Crear venta Online](https://trello.com/c/8NrGkeTy) `6a4e89d3` | Venta Control Online | bug | 🟡 이미 해결됨 (확인 요망) |
| [VentaGo Zebra Agent](https://trello.com/c/7eeUFAm9) `6a50ec34` | Producto Control OnLine | bug | ⏸ 보류 (needs-repro / 설계 결정 필요) |

> Manuales·Meta·Permisos 등 기존 카드는 상태 변동 없음(이미 트리아지됨).

---

## 2. 수정 완료 (검증 통과 — push·배포 승인 대기)

### 🖨 Ticket 버튼이 Print Agent 로 안 감 + 인쇄 흐림 (카드 2건, 단일 커밋)
- **카드:** `6a4e6bae` (ctrl+r 로 전체 페이지 인쇄) + `6a50f71f` (티켓 흐림)
- **근본 원인:** `Repaso de Ventas del Día`(Ctrl+R 토글) 패널의 **Ticket 버튼이 `window.print()`** 를 호출.
  브라우저가 **페이지 전체를 인쇄**하고(→ 6a4e6bae 증상), 브라우저 렌더 특성상 **얇은 글씨가 흐리게** 출력됨(→ 6a50f71f 증상). 폰트 배율(145%)을 바꿔도 브라우저 인쇄라 해결 안 됨.
- **수정:** `ventago-app/src/views/homes/components/SaleReview/SaleReviewPanel.tsx`
  현재 표시 중인 판매(`currentSale`)를 `/print/temp` 페이로드로 변환해 **Print Agent(ESC/POS)로 라우팅**. POS `PaymentSummary` 의 기존 패턴 재사용. Agent 오프라인 시에도 throw 안 함(판매 흐름 미차단) + 중복 클릭 가드 + 버튼 disabled 처리.
- **브랜치 / 커밋:** `fix/trello-6a4e6bae` / **`577fbdf`** (repo: ventago-app) — **로컬 커밋만**
- **검증:**
  - ESLint 통과 (`SaleReviewPanel.tsx` 대상 **0 error**, 기존 exhaustive-deps 경고 1건은 line 182 기존 코드로 이번 변경과 무관)
  - `window.print()` 잔재 없음(주석만 남음)
- **⚠ 주의:** Print Agent 가 **설치되지 않은** 환경에서는 여전히 인쇄 불가/흐림. 카드는 Agent 사용을 전제로 하므로 라우팅 전환이 올바른 해법. Agent 미설치 폴백(window.print)을 유지할지는 별도 결정 사항.
- **사용자 직접 검증 명령 (Mac):**
  ```bash
  # 1) 브랜치 확인 (현재 샌드박스가 ventago-app 을 이 브랜치로 체크아웃해 둠)
  cd ventago-app && git status
  # 2) 린트
  npm run lint
  # 3) UI 검증: 로그인 → Ctrl+R (Repaso) → Ticket 버튼 → 온라인 comandera 로 출력되는지 확인
  #    (window.print 대화상자가 뜨지 않고 Print Agent 로 전송되면 정상)
  ```

---

## 3. 이미 해결됨 (확인 요망)

### 🟡 온라인 판매 생성 시 Nuevo 컬럼에 실시간 미표시 — `6a4e89d3`
- **신고:** 온라인 판매 새로 만들면 보드 "Nuevo" 에 안 뜨고 F5 눌러야 보임.
- **코드 검증 (DespachoBoard.tsx):** 이 증상은 **이미 픽스되어 main 에 반영됨**:
  1. `/envios` Socket.io `envio_updated` 구독 → 신규 카드 실시간 병합 (line 244–254)
  2. 보드 진입/지점 전환 시 `mutate()` 강제 재검증 (line 217–222) — 코드 주석이 *정확히* "새로고침 전엔 안 보이는 원인" 을 설명하며 이 로직으로 해결했다고 문서화
  - 백엔드 `createFromPos` → `emitCard()` → `envio_updated` emit 도 존재 (online-orders.service.ts:665)
- **배포:** main `38a9e71` 에 포함 (Phase 44/45).
- **확인 요망:** 운영 빌드가 최신 main 인지 + 재발 시 재현 절차 필요:
  - 어느 화면의 "Nuevo" 인가? (Despacho 보드 vs 다른 목록)
  - 판매 지점과 보드가 보고 있는 지점(교차 출고 fulfillmentBranchId)이 일치하는가? (불일치 시 다른 지점 룸으로 emit → 안 보임)
  - 브라우저 콘솔 `[DespachoBoard] 조회 지점` 로그로 어떤 지점 보드를 보는지 확인 가능.

---

## 4. 보류 (needs-repro / 설계 결정)

### ⏸ VentaGo Zebra Agent — 시스템 코드 / 당일 생성 코드 미검색 — `6a50ec34`
- **신고:** 시스템에 로드된 코드를 못 찾음. 당일 생성한 코드도 안 나옴.
- **코드 검증:** zebra-agent 는 `get_stock_today` → `getProductsWithStockAddedToday()` 만 사용.
  이 쿼리는 **오늘(operationDate) 입고 이력(type=null, stock>0)이 있는 상품만** 반환 = **"오늘 입고분" 델타 리스트** (설계상 제한, `productStock.service.ts:682`).
  → 임의의 기존 상품 코드를 **일반 검색하는 기능이 없음**. 이는 버그라기보다 **기능 갭**.
- **"당일 생성분 미표시":** 상품을 만들어도 **입고(stock) 기록의 operationDate 가 오늘이 아니면**(타임존/입고 미기록) 리스트에 안 뜸 → **데이터/타임존 의존 → 재현 필요**.
- **결정 필요 (자동수정 보류 사유):**
  - (a) zebra-agent 에 상품 코드 **일반 검색 엔드포인트** 추가 (SKU/이름으로 조회) — 신규 기능
  - (b) "오늘 입고분만" 현 동작 유지 + UX 안내 문구
  - 어느 방향인지 확정되면 다음 실행에서 GSD 로 착수 가능.

---

## 5. 절대 금지 항목 (이번 실행에서 미수행 — 승인 대기 목록)

- ⛔ **git push** — `fix/trello-6a4e6bae` (577fbdf) 는 로컬 커밋만. 검토 후 `./push-both.sh` 로 배포 필요.
- ⛔ 운영 서버 상태 변경 / 운영 DB DDL·DML — 해당 없음.
- ⛔ Trello 카드 이동/삭제 — 미수행. (수정 완료 2건은 hechosPending=false 로 두어 자동 이동 대상 아님 — push·배포 확인 후 별도 처리.)

## 6. 승인 필요 액션 요약

1. **`fix/trello-6a4e6bae` (577fbdf) 검토 → push/배포** (ventago-app). Ticket 인쇄 2건 해결.
2. **온라인 판매 F5 이슈(`6a4e89d3`)** — 운영 최신 빌드 확인 + 재발 시 재현 절차 회신.
3. **Zebra Agent 코드 검색(`6a50ec34`)** — (a) 일반 검색 추가 vs (b) 현행 유지 결정.

---

### ℹ️ 참고: 서브모듈 브랜치 상태
샌드박스 파일시스템 제약(`.git/index.lock` unlink 불가)으로 `ventago-app` 서브모듈이 현재 **`fix/trello-6a4e6bae`** 브랜치에 체크아웃된 상태로 남아 있습니다. Mac 에서 정리하려면:
```bash
cd ventago-app
rm -f .git/index.lock   # 샌드박스가 남긴 잔여 lock (Mac 에서는 삭제 가능)
git checkout main       # 커밋 577fbdf 는 fix/trello-6a4e6bae 에 안전히 보존됨
```
