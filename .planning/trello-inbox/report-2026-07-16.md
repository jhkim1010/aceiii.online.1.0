# Trello 반자동 유지보수 리포트 — 2026-07-16

- **동기화**: `latest.json` syncedAt = `2026-07-16T09:50:03Z` (24시간 이내, 정상)
- **보드 카드**: 16개 / triage-state 총 38개 추적
- **이번 실행**: 신규 버그 5건 트리아지 · Hechos 이동 2건 정리 · **자동 수정 2건(로컬 커밋)**

> ⚠️ 샌드박스 git 제약: 이 클라우드 환경은 `.git` 파일 unlink 가 막혀 있어(`Operation not permitted`) 작업 트리 되돌리기가 안 됩니다. **커밋된 두 fix 브랜치는 정상·격리 확인 완료**이나, ventago-app 작업 트리(main1)에 잔여 수정(ProductList/SaleReviewPanel/print.service)이 uncommitted 로 남아 있습니다. Mac 에서 브랜치 전환 전 `git checkout -- .` 로 정리하세요(변경 내용은 이미 fix 브랜치에 커밋되어 있어 유실 아님).

---

## 1. 수정 완료 (로컬 커밋 — push 안 함)

### 🟢 6a5675a3 — Impresion de ticket de venta (판매 티켓에 고객명 안 나옴)
- **증상**: 판매 티켓 인쇄 시 Cliente 이름이 안 찍히고 항상 `Consumidor Final`.
- **근본 원인**: `ProductList.tsx` 자동인쇄 payload 의 `invoice.client` 가 `selectedClient?.name` 을 읽음. 그러나 `selectedClient` 객체 속성은 **`fullname`** (InfoClient `clientSchema` / `useSaleProducts` 기준). → `.name` 은 항상 `undefined` → `'Consumidor Final'` 폴백.
- **수정**: `invoice.client` 를 `selectedClient?.fullname` 우선(레거시 `.name` 폴백)으로 변경. print-agent formatter 는 `data.invoice.client` 가 있으면 이미 렌더함 → agent 재빌드 불필요.
- **브랜치/커밋**: `fix/trello-6a5675a3` · **`09d61b0`** (ventago-app, 1 file, +4/-1)
- **검증(사용자 실행)**:
  ```bash
  cd ventago-app && git checkout fix/trello-6a5675a3 && npm run lint
  # 실기: 고객을 지정한 판매 생성 → 티켓에 해당 고객명 인쇄 확인
  ```

### 🟢 6a566b67 — Reimprimir ticket (재인쇄가 견적서처럼 나옴 + vendedor 안 보임)
- **증상**: 티켓 재인쇄 시 `PRESUPUESTO TEMPORAL` 경고 배너 + 제목이 `Presupuesto` 로 나와 견적서처럼 인쇄됨. 재인쇄(Venta)로 나와야 하고 vendedor 도 안 보임.
- **근본 원인**: `SaleReviewPanel.buildTicketPayload` 가 `/print/temp` 로 보내며 **`ticketType` 을 지정하지 않음**. print-agent formatter(`print-agent/src/formatter.js:892, 910`)가 `ticketType !== 'invoiced'` 분기에서 `PRESUPUESTO TEMPORAL` 배너 + `Presupuesto` 제목을 렌더. (nueva-venta 자동인쇄 payload 는 이미 `ticketType:'invoiced'` 를 보내고 있어 정상.)
- **수정**: `TempTicketPayload` 에 `ticketType` / `invoice.number` 필드 추가 + `buildTicketPayload` 에 `ticketType:'invoiced'` + `invoice.number`(dailyNumber) 추가 → `Venta # NNNN` 재인쇄로 렌더. **print-agent 재빌드 불필요**(기존 formatter 가 이미 invoiced 분기 지원).
- **잔여**: `vendedor` 는 `currentSale.seller.name → user.name` 폴백 유지. 여전히 안 보이면 sale 데이터에 seller 가 미로드된 **별개 데이터 이슈** — 재인쇄 시 seller 관계가 로드되는지 확인 필요.
- **브랜치/커밋**: `fix/trello-6a566b67` · **`09e94cc`** (ventago-app, 2 files, +10/-1)
- **검증(사용자 실행)**:
  ```bash
  cd ventago-app && git checkout fix/trello-6a566b67 && npm run lint
  # 실기: 판매 상세/Repaso(ctrl+r)에서 Ticket 재인쇄 → 'Venta #' 로 나오고 배너 없음 확인
  ```

> ESLint: 두 수정 모두 `lines-around-comment`(주석 위 빈 줄)·`newline-before-return` 규칙 준수하도록 빈 줄 처리 완료.

---

## 2. 이미 해결됨 (확인 요망)

### 🔵 6a57a7a6 — Talleres: "corte 를 local 로 보낼 방법이 없음" → **기능 이미 구현됨**
- 카드: "corte 가 끝났는지 어떻게 알까 / cut line 은 전 단계 완료로 보이는데 local 로 보낼 방법이 없음".
- **이미 구현**: '완제품 입고(Ingresar a stock)' — 로트를 **창고/매장(local) 재고로 보내는 다이얼로그**.
  - 프론트: `ee28a97` (IngresoStockDialog + LoteDetailDrawer 'Ingresar a stock' 버튼, 2026-07-14)
  - 백엔드: `b187165` (`POST /talleres/lotes/:id/ingreso-stock`)
- **배포 상태**: 백엔드는 **origin/main 배포됨** ✅ / 프론트는 **로컬 브랜치 `main1` 에만 있고 origin push 안 됨** ⛔ → **프론트 배포 후 카드 해결 예상**.
- 조치: 수정 불필요. `main1` 배포(push) 후 실기 확인.

### 🔵 6a54ff4f / 6a551b5f — 'Hechos Semanales' 리스트로 이동됨 → done-external
- **6a54ff4f (Impresion de venta, dailyNumber)**: Hechos 로 이동. 단 우리 수정 브랜치 `fix/trello-6a54ff4f`(106bd6a)는 **아직 origin/main push 안 됨** — 사용자가 별도 반영했거나 이동이 선반영일 수 있음. **실제 운영 배포 여부 확인 요망**.
- **6a551b5f (Zebra Agente)**: Hechos 로 이동 → 외부 해결. (이전 needs-repro.)

---

## 3. 보류 — 재현/설명 필요 (needs-repro)

### 🟡 6a567a1d — Pago de proveedores (지불해도 vincular 안 되고 deuda 로 남음)
- 코드만으로 판정 불가: **어느 pago 경로인지 스크린샷 필요**.
- 유사 증상의 **taller(subcon) 정산 차감 버그는 이미 `e320088`(2026-07-06, origin/main 배포됨)로 수정**됨 — settlement 을 vendor 기반으로도 집계 + mark-paid 시 `talleres_payments` 생성.
- 카드 lastActivity(07-14)가 배포 이후이므로:
  - ① **taller 정산**이면 → 배포본에서 재확인(회귀 여부).
  - ② **materia-prima 공급업체 결제**(`mes_material_supplier_payments`, `material-supplier-payment.service.ts`)면 → **별개 경로**로 아직 미조사.
- **요청**: 스크린샷의 화면(어느 Pagos 탭)으로 경로 확정 → 그 후 착수.

### 🟡 6a57a408 — Lotes: "asignados 된 talleres 를 표시/선택 못 함"
- `'asignados'` 의미가 모호: (a) taller 드롭다운이 비어있음 / (b) etapa 에 배정된 taller 만 필터되어야 함 / (c) 기선택이 안 됨 — 중 무엇인지 불명확.
- taller 목록은 `useTalleresVendors('/talleres/vendors/all')` 로 로드, 발송은 `LoteDetailDrawer → EnvioFormDialog` 경유.
- **요청**: 화면상 정확 지점(드롭다운 위치·비어있음 vs 미선택) 한 줄 설명.

---

## 4. 승인 대기 (절대 자동 실행 안 함)

| 항목 | 내용 |
|------|------|
| **push** | `fix/trello-6a5675a3`(09d61b0), `fix/trello-6a566b67`(09e94cc) → origin main 반영 |
| **push** | `main1` 브랜치(완제품 입고 프론트 ee28a97 포함) origin push → 6a57a7a6 해결 |
| **확인** | 6a54ff4f 픽스(fix/trello-6a54ff4f, 106bd6a) 실제 운영 배포 여부 — Hechos 이동됐으나 미push |
| **배포** | 위 push 후 Jenkins front build → 운영 반영 |

운영 DB DDL/DML·docker restart·마이그레이션·Trello 카드 삭제: **해당 없음**.

---

## 부록 — 마지막 로그 파일 확인
- `api-ventago/logs/` 최신: `combined-2026-07-15.log`, `error-2026-07-15.log`. 위 수정과 관련된 client/print/reprint/supplier 에러 항목 없음(정상). PostgreSQL pool 관련 변경 없음 — 이번 수정은 프론트 페이로드 필드만 조정으로 pool 무영향.
