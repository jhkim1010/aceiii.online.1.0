# Trello 유지보수 루프 리포트 — 2026-07-06

동기화: 2026-07-05T23:11Z (정상, 24h 이내) · 카드 25건 · 신규/갱신 카드 **0건**

## 새 카드
없음 (전 카드 07-05 트리아지 상태 유지).

## 수정 완료 (로컬 커밋만 — push 는 승인 대기)

### B4 — Mesas en modo restaurante: 회전 시 이름 사라짐 (https://trello.com/c/kfJu5KIJ)
- **원인**: `TableCard.tsx` 에서 이름 라벨은 회전 밖(카드 위)에 있지만, 회전된 카드 본체(rect/oval 90° 등)의 시각적 영역이 layout box 위로 (w−h)/2 만큼 확장 → DOM 순서상 뒤에 그려지는 불투명 카드 본체가 이름을 덮음.
- **수정**: 이름 라벨에 `zIndex:2` + 반투명 흰 pill 배경 (회전 카드 위에서도 가독 확보).
- **브랜치/커밋**: ventago-app `fix/trello-6a3584cd` / `fe94ac4`
- 참고: 편집기(SalonEditor)는 이름이 회전 박스 안에 있어 180° 회전 시 뒤집혀 보임 — 별도 개선 여지 (기능상 표시는 됨).

### B5 — Prefijo(SKU 접두어) 수정이 저장 안 됨 (https://trello.com/c/7vJkl9OU)
- **원인**: `ConfigurationController` 에 @Auth() 가드 부재 → `CrudController.update()` 의 `@GetUser() user` 가 undefined → `if (!user) return null;` 로 **에러 없이 조용히 스킵**. 전역 JWT 가드 없음(전역은 rate-limit 가드뿐). 목록 조회(by-store)만 @Auth() 가 있어 정상 → "저장만 안 되는" 증상과 정확히 일치.
- **수정**: 클래스 레벨 `@Auth()` 적용 (MaterialSupplierPaymentController 와 동일한 검증된 패턴). 소비처(BasicDataCard, Configurations 화면)는 모두 인증 프론트 → 파급 없음.
- **브랜치/커밋**: api-ventago `fix/trello-69d90504` / `2562576`

## 이미 해결됨 / 확인 요망

### B3 — Materia Prima → Pagos 오류 (https://trello.com/c/iZhJ6D1H)
- 코드 확인: Pagos 탭은 `/materia-prima/payments` → `mes_material_supplier_payments`(+`mes_material_suppliers`) 사용.
- 07-02 운영 ALTER OWNER 수정은 `mes_material_suppliers`/`credit_payments` 만 확인됨. **`mes_material_supplier_payments` 소유권은 미확인** — 아래 조회로 확인해 주세요 (postgres 소유로 나오면 ALTER OWNER 필요):
```bash
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"SELECT tablename, tableowner FROM pg_tables WHERE tablename LIKE 'mes_material%' OR tablename = 'credit_payments';\""
```

## Trello 자동 정리 (사용자 지시 2026-07-07 갱신: archive 아님 → Hechos 이동)
- 샌드박스에서 Trello API 직접 호출은 네트워크 차단으로 불가 → **`tools/trello-sync.js` 에 Hechos 이동 단계 추가** (매일 06:50 Mac launchd 실행 시 처리, `node --check` 통과).
- 동작: triage-state 에서 `hechosPending: true` 카드를 **"Hechos (Corregidos en nube)" 리스트로 이동** (archive/삭제 안 함, 리스트는 이름으로 탐색) 후 `movedToHechosAt` 기록.
- 오늘 세팅: **B1/B2/B7 (가격 레벨 3건, main push 완료분)** → 다음 06:50 sync 때 자동 이동. 즉시 처리 원하시면: `node tools/trello-sync.js`
- 규칙: 앞으로 "수정 + push 완료" 판정 카드만 hechosPending 세팅 (로컬 커밋 단계에서는 세팅 안 함).

## 승인 대기 (절대 금지 목록)
1. **push**: ventago-app `fix/trello-6a3584cd`, api-ventago `fix/trello-69d90504` (→ push 후 해당 카드 hechosPending 세팅 가능)
2. **운영 DB**: B3 관련 `mes_material_supplier_payments` 소유권 확인 → 필요시 `ALTER TABLE mes_material_supplier_payments OWNER TO coolsistema;` (마이그레이션 파일로 커밋 후 실행)

## 보류
- B6/B8/B9: 다음 실행에서 순차 처리 (한 실행당 2건 제한).

## 사용자 검증 명령 (샌드박스에서 실행 불가한 것)
```bash
# 프론트 lint (B4)
cd ventago-app && git checkout fix/trello-6a3584cd && npm run lint

# 백엔드 빌드 (B5)
cd api-ventago && git checkout fix/trello-69d90504 && npx tsc --noEmit

# 기능 확인: (1) salón 에서 회전된 mesa 이름 표시, (2) Configuración → Prefijo 26 으로 수정 → 재조회 유지
```

## 기타 참고
- 로컬 dev 로그(error-2026-07-03.log): `/api/mes/materials` 의 `purchase_unit` 컬럼 없음 500 다수 — 원단 이중 단위 Phase A 마이그레이션이 로컬 DB에 미적용 상태로 개발 중 발생한 것으로 보임. 운영 배포 전 마이그레이션 순서 주의.
- 샌드박스에서 서브모듈 git lock 파일(unlink 불가) 이슈 → `.git/*.stale*` 파일이 남아 있을 수 있습니다. 무해하며 `rm ventago-app/.git/*.stale* api-ventago/.git/*.stale*` 로 정리 가능합니다.
