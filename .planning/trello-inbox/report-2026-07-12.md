# Trello 유지보수 루프 리포트 — 2026-07-12

## ⚠ 동기화 실패 — 수정 작업 중단

| 항목 | 값 |
|------|-----|
| `latest.json` syncedAt | **2026-07-11T02:47:30Z** |
| 실행 시각 (UTC) | 2026-07-12T23:48Z |
| 경과 | **45.0시간** (임계값 24시간 초과) |
| 오늘자 보드 스냅샷 | `board-2026-07-12.json` **없음** (최신: `board-2026-07-11.json`) |

launchd 의 06:50 Trello 동기화가 **2026-07-11, 2026-07-12 이틀 연속 실행되지 않았거나 실패**한 것으로 보입니다.
스킬 규칙에 따라 **트리아지·자동 수정은 수행하지 않고 종료**했습니다. (오래된 스냅샷 기준으로 수정하면 이미 해결/이동된 카드를 다시 건드릴 위험이 있습니다.)

### 사용자 조치 필요 (Mac 에서 실행)

```bash
cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0
node tools/trello-sync.js          # 수동 동기화
tail -50 /tmp/trello-sync.log      # 실패 원인 확인 (API 키 만료 / 네트워크 / launchd 미기동)
launchctl list | grep -i trello    # launchd 잡 등록 상태 확인
```

동기화 성공 후(`board-2026-07-12.json` 생성 확인) 이 루프를 재실행하면 정상 트리아지가 진행됩니다.

의심 원인 후보:
1. Trello API 키/토큰 만료 (401)
2. launchd 잡이 로드 해제됨 (`launchctl load ~/Library/LaunchAgents/…trello…plist` 필요)
3. Mac 절전/종료로 06:50 트리거 스킵

---

## 오늘 수행한 작업

없음 (동기화 실패로 중단). 코드 변경·커밋·브랜치 생성 전무.

---

## 승인 대기 (이전 실행에서 이월 — 변동 없음)

| 카드 | 상태 | 대기 항목 |
|------|------|-----------|
| `6a50ec34` VentaGo Zebra Agent (코드 검색 불가) | `fixed-local` | **push 미완** — `fix/trello-6a50ec34` (api-ventago `53cccbf` + root/zebra-agent `efb42df`). push + zebra-agent 릴리스 태그 승인 필요 |
| `6a50f71f` Ticket 흐림 | `pushed-main` | **print-agent 릴리스 태그 미발행** → 매장 미배포. 실기 인쇄 농도 검증 필요 |
| `6a4e6bae` Ctrl+R Ticket 라우팅 | `pushed-main`, `hechosPending: true` | 다음 정상 sync 시 "Hechos (Corregidos en nube)" 로 자동 이동 예정 (현재 sync 실패로 보류) |
| `6a46650432abd1687e429d04` | `bug-verify-prod` | 운영 DB `mes_material_supplier_payments` 테이블 owner 가 `coolsistema` 인지 확인 필요 (postgres 소유 시 500) |

### 검증용 명령 (사용자 직접 실행)

```bash
# zebra-agent 픽스 lint 확인
cd api-ventago && npx eslint src/app/print --ext .ts
cd ../ventago-app && npm run lint

# 운영 DB 소유권 확인 (read-only)
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -c \"\\dt+ mes_material_supplier_payments\""
```

---

## 다음 실행 시 확인 사항

- `latest.json` 의 `syncedAt` 이 24시간 이내인지 (최우선)
- sync 복구 후 `6a4e6bae` 가 Hechos 리스트로 이동됐는지
- 07-11/07-12 사이 신규 카드가 있는지 (현재 스냅샷으로는 확인 불가)
