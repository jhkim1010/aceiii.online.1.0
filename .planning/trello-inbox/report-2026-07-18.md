# Trello 반자동 유지보수 루프 리포트 — 2026-07-18

## ⚠ 동기화 실패 — 수정 작업 없이 종료

`latest.json` 의 `syncedAt` 이 **2026-07-17T13:46:54Z** 로, 실행 시각(2026-07-18T20:09Z) 기준 **약 30.4시간 전**입니다.
태스크 규칙(24시간 초과 = 동기화 실패)에 따라 **트리아지·자동 수정 작업을 수행하지 않고 종료**합니다.

### 조치 필요 (사용자)
- Mac 에서 수동 동기화 실행: `node tools/trello-sync.js`
- 동기화 로그 확인: `/tmp/trello-sync.log`

### 근본 원인 (기억된 이슈)
자동 sync 가 launchd 에 정식 등록되지 않아 매일 06:50 스케줄이 돌지 않는 것으로 파악되어 있습니다
(메모리: `project_trello_sync_launchd.md`). 오늘 07-18 06:50 sync 도 실행되지 않아 `latest.json` 이
07-17 수동 실행분에 머물러 있습니다.

**즉시 복구**
```bash
cp tools/*.plist ~/Library/LaunchAgents/
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/<plist파일명>.plist
launchctl kickstart -k gui/$(id -u)/<label>   # 즉시 1회 실행 테스트
```
plist 파일명·label 은 `tools/` 디렉터리에서 확인하세요.

**근본 해결(권장)**: sync 를 Mac launchd 대신 운영 서버 cron 으로 이관하면 Mac 전원 상태와 무관하게 매일 동기화됩니다.

---

## 참고 — 이전 실행(07-17) 대비 보드 변화 (트리아지 미수행, 정보용)

동기화가 stale 이므로 아래는 **현재 stale 데이터 기준 관찰**일 뿐이며, 이번 실행에서 정식 트리아지/검증은 하지 않았습니다.
동기화 복구 후 다음 실행에서 정식 처리 예정입니다.

- 신규로 보이는 미처리 카드 후보(triage-state 에 없음, 확인 필요):
  `6a58eb9a` Cambiar precio de los productos / `6a58f98c` App vendedor / `6a591d7b` Error de vendedor /
  `6a592a90` Ventas suspendidas / `6a59189c` VentaGo Zebra Agent sale sin icono / `6a591931` Impresion de precios /
  `6a590f1d` Error en cobdetalles / `6a59110f` Signo2 / `6a59276a` Backups
- 이 카드들은 동기화 복구 후 정식 트리아지 대상입니다.

*이번 실행은 동기화 stale 로 인해 수정/커밋/브랜치 생성 없이 종료되었습니다.*
