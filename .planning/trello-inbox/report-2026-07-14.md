# Trello 유지보수 루프 리포트 — 2026-07-14

> 첫 실행(13:18Z)은 동기화 지연(27.5h)으로 중단. 사용자가 `node tools/trello-sync.js` 수동 실행 → **syncedAt 2026-07-14T13:24:43Z** 로 갱신되어 정상 트리아지 재개.
> 사용자 지시: **이미 해결된 항목은 재검토·재수정하지 않음** — 이번 실행은 신규 카드만 처리했습니다.

## 요약

| 구분 | 건수 |
|---|---|
| 신규 카드 | 3 |
| 수정 완료 (로컬 커밋) | 2 |
| 정보 부족 (needs-repro) | 1 |
| 승인 대기 (push/배포) | 이월 + 신규 3 커밋 |

---

## 신규 카드 (3건, 모두 07-13 등록)

| 카드 | 리스트 | 내용 | 판정 |
|---|---|---|---|
| [`6a54ff4f` Impresion de venta](https://trello.com/c/xcvaq6iy) | Venta Control Online | 티켓이 판매 전역번호(N°71)로 인쇄, 당일 번호(4) 아님 | **버그 확인 → 수정** |
| [`6a55061f` Talleres](https://trello.com/c/CeZkAeUy) | Producto Control OnLine | Recibir 눌러도 상태가 recibido 로 안 바뀜 | **버그 확인 → 수정** |
| [`6a551b5f` Zebra Agente](https://trello.com/c/aWPDZXqw) | Producto Control OnLine | 설명 없음(이미지 2장만) | **needs-repro** |

---

## 수정 완료 (로컬 커밋만 — push 미실행)

### 1. `6a54ff4f` — 티켓 번호가 당일 순번이 아님

**근본 원인 (검증됨):** `sales` 테이블에는 `number` 컬럼이 **없습니다** (`.planning/intel/db-schema-tables.md:1504` — 존재하는 건 `daily_number`). 그런데 인쇄 payload 가 `createdSale?.number || createdSale?.id` 로 만들어져 **항상 전역 PK(id)로 폴백** → "Venta # 71". 화면(판매목록·Repaso)은 `dailyNumber` 를 쓰기 때문에 4로 정상 표시되어 **인쇄 경로만 갈렸습니다**.

같은 원인이 **재인쇄(Reimprimir)** 경로에도 있어 함께 고쳤습니다 (`buildInvoiceData` 가 `Ticket-${sale.id}` 사용).

| repo | 브랜치 | 커밋 | 파일 |
|---|---|---|---|
| ventago-app | `fix/trello-6a54ff4f` | `106bd6a` | `src/views/homes/components/ProductList/ProductList.tsx` (자동 인쇄 payload → `dailyNumber`) |
| api-ventago | `fix/trello-6a54ff4f` | `a9b2314` | `src/app/sales/sales-create.service.ts` (`buildInvoiceData` → `dailyNumber`) |

레거시 행(`daily_number = 0`)만 `id` 로 폴백하도록 남겼습니다. **print-agent 재빌드 불필요** (formatter 는 서버가 준 번호를 그대로 렌더).

**검증 방법:** POS 에서 확정 판매 1건 → 티켓의 번호가 화면 판매목록의 `#N`(당일 순번)과 일치하는지 확인. 판매내역에서 Reimprimir 도 동일 번호인지 확인.

### 2. `6a55061f` — Talleres Recibir 가 상태를 바꾸지 않음

**근본 원인 (검증됨):** `EnviosTab.tsx` 의 **Recibir 버튼에 `onClick` 자체가 없었습니다** (dead button — 요청이 서버에 도달조차 안 함; `api-ventago/logs/error-2026-07-11.log` 0바이트, recepcion 관련 에러 없음이 이와 일치). 백엔드 `POST /talleres/recepciones` 는 정상이며 트랜잭션 안에서 `pendingQuantity` 차감 + `status` → `PARTIAL`/`COMPLETED` 전이를 수행합니다 (`recepcion.service.ts:80-89`). 정상 수령 플로우는 사이드바에서 제거된 레거시 뷰(`talleres_EnviosListView.tsx`)에만 남아 있었던 **미완성 마이그레이션**입니다.

> DB 값 도메인에 `'recibido'` 는 없습니다 — `talleres_envios.status` = `PENDING | PARTIAL | COMPLETED | CANCELLED`. "Recibido" 는 UI 표시 문자열.

| repo | 브랜치 | 커밋 | 변경 |
|---|---|---|---|
| ventago-app | `fix/trello-6a55061f` | `00d6a82` | `views/talleres/envios/RecepcionFormDialog.tsx` **신규** + `views/talleres/tabs/EnviosTab.tsx` 배선 |

수령 다이얼로그(수령/불량 수량·일자·메모) → `POST /talleres/recepciones` → 저장 후 SWR `'/talleres/envios/all'` 재검증으로 상태·Pendiente 즉시 반영. 서버 검증(초과 수량 400)을 프론트에서도 선차단해 불필요한 왕복·pool 소모를 줄였습니다.

**검증 방법:** Talleres → Envios 탭 → PENDING/PARTIAL 행의 **Recibir** → 전량 입력 후 저장 → 상태가 `Completado` 로 바뀌고 행 액션이 "Recibido" 로 전환되는지, 부분 수령 시 `Parcial` + Pendiente 감소하는지 확인.

**ESLint:** 변경 3개 파일 `eslint` 실행 결과 **0 findings**.

---

## 정보 부족 (needs-repro)

### `6a551b5f` — "Zebra Agente"

카드에 **설명 텍스트가 없고 스크린샷 2장만** 첨부되어 증상을 특정할 수 없습니다. 다음 중 무엇인지 카드에 한 줄 추가 부탁드립니다:

- 라벨 인쇄가 안 됨(프린터 미검출/오프라인) / 라벨 내용·레이아웃 오류 / 상품 검색이 안 됨 / 앱이 서버에 연결 안 됨

참고로 zebra-agent **상품 코드·이름 검색 기능**은 이미 `fix/trello-6a50ec34` 브랜치에 구현돼 있으나 **아직 push·릴리스되지 않았습니다**. 이 카드가 그 건이라면 릴리스만 하면 해결됩니다.

---

## 승인 대기 (push / 배포 / 운영 DB)

| 항목 | 상태 | 필요한 승인 |
|---|---|---|
| **신규** `fix/trello-6a54ff4f` (ventago-app `106bd6a` + api-ventago `a9b2314`) | 로컬 커밋 | push → Jenkins 배포 |
| **신규** `fix/trello-6a55061f` (ventago-app `00d6a82`) | 로컬 커밋 | push → Jenkins 배포 |
| `fix/trello-6a50ec34` (zebra-agent 상품 검색) | 로컬 커밋(이월) | push + zebra-agent 릴리스 태그 |
| print-agent 티켓 농도 픽스 (`9dc194e`) | main 반영(이월) | 릴리스 태그 + 실기 확인 |
| Jenkins DI 크래시 픽스 | (메모리) | `origin/main` push 미완 시 Jenkins 재실행 = 서버 재다운 위험 |
| 운영 DB `mes_material_supplier_payments` owner | 조회 필요(이월) | `coolsistema` 소유 확인 (조회성, 무해) |

---

## ⚠ 샌드박스 git 제약 — Mac 에서 정리 필요

이 클라우드 샌드박스는 마운트 FS 에서 **파일 unlink 가 금지**되어 git 이 `index.lock` / `HEAD.lock` 을 지우지 못했습니다. **커밋은 3건 모두 정상 생성**되었으나 worktree 정리에 실패했습니다.

```bash
cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0

# 1) stale lock 제거 (git 프로세스 없는지 확인 후)
rm -f ventago-app/.git/index.lock ventago-app/.git/HEAD.lock api-ventago/.git/index.lock

# 2) 커밋 확인
git -C ventago-app log --oneline -1 fix/trello-6a54ff4f   # 106bd6a
git -C ventago-app log --oneline -1 fix/trello-6a55061f   # 00d6a82
git -C api-ventago log --oneline -1 fix/trello-6a54ff4f   # a9b2314

# 3) worktree 정리 (변경분은 위 브랜치에 이미 커밋됨)
git -C ventago-app checkout main
git -C ventago-app checkout -- .
rm -f ventago-app/src/views/talleres/envios/RecepcionFormDialog.tsx   # 00d6a82 에 포함됨
git -C api-ventago checkout -- src/app/sales/sales-create.service.ts

# 4) 검증
cd ventago-app && npm run lint && npm run build
cd ../api-ventago && npm run lint && npm run build
```

> ventago-app 의 현재 HEAD 는 `fix/trello-6a54ff4f` 에 머물러 있습니다(HEAD.lock 때문에 되돌리지 못함). 위 3) 단계로 `main` 복귀하시면 됩니다.

---

## 2차 실행 (20:08Z) — 변경 없음

동기화 상태 정상 (`syncedAt 2026-07-14T13:24:43Z`, 약 6.7h 전). 보드에 카드 **11장**, 모두 triage-state 에 이미 추적 중.

- **신규 카드**: 0건
- **lastActivity 갱신 카드**: 0건 (최신 활동은 07-13, 이미 1차 실행에서 처리됨)
- **Hechos* 리스트 카드**: 0건
- **수정 작업**: 없음 (신규/갱신 버그 없음)

### 승인 대기 (이월, 사용자 push 필요) — 상태 유지

| 우선 | 카드 | 브랜치 | 커밋 | 검증 상태 |
|---|---|---|---|---|
| B12 | `6a54ff4f` Impresion de venta | `fix/trello-6a54ff4f` | `106bd6a`(ventago-app) + `a9b2314`(api-ventago) | 커밋 정상 확인 |
| B13 | `6a55061f` Talleres Recibir | `fix/trello-6a55061f` | `00d6a82`(ventago-app) | 브랜치·커밋 정상 |
| B11 | `6a50ec34` Zebra 상품검색 | `fix/trello-6a50ec34` | `53cccbf`(api-ventago) + `efb42df`(root/zebra-agent) | 커밋 정상, 릴리스 태그 미발행 |

> 2차 실행에서 세 브랜치 커밋의 존재를 재확인했습니다(무결). B12 워크트리 브랜치는 Mac 에서 정리되었으나 커밋은 도달 가능.

### 정보 부족 (needs-repro) — 상태 유지

- `6a551b5f` **Zebra Agente** — 카드에 설명 없이 스크린샷 2장뿐. 증상 특정 불가. 사용자 확인 요청: 인쇄 실패 / 라벨 내용 오류 / 상품 검색 / 서버 연결 중 무엇인지 한 줄 설명 부탁드립니다.

### 참고 — 보드에서 사라진 대기 버그 (B6/B8/B9)

`69d909f2`(B6) · `69cbbccb`(B8) · `69ca68e4`(B9) 는 이전 `bug-queued` 상태였으나 현재 보드 11장에 없음(다른 리스트로 이동 또는 아카이브 추정). Hechos* 동기화 결과도 0건이라 완료 여부 불명 — 재등장 시 재트리아지합니다.
