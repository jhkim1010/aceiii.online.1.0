# Trello 반자동 유지보수 리포트 — 2026-07-22

- **동기화**: `syncedAt` 2026-07-22T09:50:01Z (실행 시각 기준 약 10시간 전, 정상 — 24h 이내)
- **보드 카드 수**: 19 / triage-state 추적 카드: 47
- **이번 실행 결과**: 신규 카드 0건 · 자동 수정 0건 · 재검증 7건 · 승인 필요 항목 없음 (push 없음)

---

## 1. 새 카드
**없음.** 동기화된 19개 카드는 모두 이미 triage-state 에 추적 중입니다. lastActivity 가 직전 트리아지(2026-07-20) 이후로 갱신된 미처리 카드도 없습니다. (Hechos Semanales 의 3개 카드는 07-19/07-22 이동되었으나 이미 done-external / already-fixed 로 기록됨.)

## 2. 이미 해결됨 (확인 요망)
직전 실행에서 기록된 상태 유지. 이번 실행에서 새로 "이미 해결"로 판정된 카드는 없습니다. 참고로 다음은 이미 Hechos 로 이동·기록 완료 상태입니다:

- `6a57a7a6` **Talleres** (Hechos, already-fixed)
- `6a591931` **Impresión de precios** (Hechos, done-external)
- `6a5675a3` **Impresión de ticket de venta** (Hechos, done-external)
- `6a566b67` **Reimprimir ticket** (Hechos, done-external)

## 3. 수정 완료
**없음.** 아래 「보류」 사유대로, 이번 배치의 미해결 버그 카드 중 **코드만으로 증상이 확정되는(검증 통과) 건이 0건**이었습니다. 검증 없는 수정은 규칙상 금지이므로 수정에 착수하지 않았습니다.

## 4. 승인 대기 (push / 배포 / 운영 DB)
**없음** — 이번 실행에서 생성된 수정 브랜치·커밋이 없습니다.

> 참고(이전 실행 잔여, 이번 실행 대상 아님): `fix/trello-6a54ff4f`, `fix/trello-6a55061f`, `fix/trello-6a50ec34` 등은 로컬 커밋만 있고 origin/main push 여부는 이전 리포트 참조.

## 5. 보류 (사유) — 재검증 상세

각 카드의 **실제 코드를 정독**하여 증상 현존 여부를 판정했습니다(추측 아님).

### `6a591d7b` Error de vendedor — needs-repro (코드 경로 정상 확인)
증상: 폰 앱에서 `vendedor@cool` 로그인 시 이름이 **"venta2"** 로 표시. 그러나 카드 본문상 admin → Usuarios 에는 정명 **"Vendedor Uno"**.
- 표시 경로 전 구간 정상 확인:
  - 백엔드 `getMe`/`login` → `name: user.name` 반환 (`api-ventago/.../mobile-auth.service.ts:368, :219`)
  - Flutter DTO → `json['name'] → MobileUser.name` 정확 매핑 (`mobile-sales-app/.../auth_dto.dart:113`)
  - 홈 화면 → `user?.name` 표시 (`home_screen.dart:28`)
  - 앱 시작 시 `/mobile/me` 재조회로 scope/user 복원 (`scope_provider.dart:64-72`) → 개명은 다음 실행에 반영됨
- admin 이 "Vendedor Uno" 를 보이므로 `users.name` 은 정상. 따라서 **"venta2" 는 코드 미스매핑이 아님** → 폰의 낡은 세션(이전 다른 계정 로그인 잔존) 또는 계정 개명 전 캐시 잔상으로 판단.
- **사용자 재현/확인 절차**: 폰에서 로그아웃 → `vendedor@cool` 로 재로그인. 그래도 "venta2" 면 운영 DB 에서 해당 계정 중복/토큰 매핑 확인:
  ```sql
  -- 운영(조회): 사용자 실행
  SELECT id, name, email, username FROM users WHERE email ILIKE '%vendedor%';
  ```
- 카드 3번째 증상("ventas suspendidas 에 Consumidor final")은 카드 `6a592a90` 와 혼재된 QA 메모로 보이며 별건 처리.

### `6a58eb9a` Cambiar precio de los productos — needs-repro (기대행위 미확정)
증상: precio 1 을 9000 으로 바꾸고 Tab → 나머지 precio 가 갱신 안 됨 + "no hubo ningún cambio" 토스트가 뜨는데 값은 남음.
- **판정 불가 사유 2가지**:
  1. "나머지 precio 자동 재계산" 기대 자체가 **매장 마진연동 정책에 의존** — 가격이 독립이면 현재 동작이 정상. 기대 재계산 규칙 확정 필요.
  2. 토스트 문구 `no hubo ningún cambio` 가 ventago-app 소스에 **존재하지 않음**(grep 0건) → 스크린샷 문구가 다른 컴포넌트이거나 의역일 가능.
- 착수 전 사용자에게 "precio 1 변경 시 나머지 가격이 자동 재계산되어야 하는가?"를 확인 요망.

### `6a592a90` Ventas suspendidas — needs-repro (본문 미완성)
카드 본문이 "...miss click wc" 로 끊겨 있고 스크린샷만 존재. 두 suspended sale 전환 시 무엇이 잘못되는지 특정 불가. 구체 증상 재기술 요청.

### `6a58f98c` App vendedor — needs-repro (정보 부재)
설명(desc) 공백 + 첨부 1개뿐. 고칠 대상이 카드에 기술되어 있지 않음.

### `6a59189c` VentaGo Zebra Agent sale sin icono — needs-repro (설정 정상)
증상: 설치된 Zebra Agent 가 아이콘 없이 표시.
- **코드 정독 결과**: zebra-agent 의 아이콘 설정이 **정상 동작하는 print-agent 와 완전 동일**. 둘 다 `build.icon = renderer/assets/icon-512.png`(512×512 유효 PNG), `win.icon` 미지정, `BrowserWindow` 에 `icon` 미지정.
- 즉 zebra 만의 코드 결함이 아니므로 코드만으로 "결함 확정" 불가 → 패키징/윈도 아이콘 캐시 의존.
- **검증법**: `npm run build:win`(zebra-agent) 재빌드 후 재설치. 여전하면 Windows 아이콘 캐시 초기화(`ie4uinit.exe -show`) 후 확인.
- (선택 개선) 양 에이전트 `BrowserWindow` 에 `icon` 명시를 추가할 수 있으나, print-agent 도 함께 바꿔야 대칭이 유지되고 실기 검증이 필요하므로 **검증 전 미적용**.

### `6a567a1d` Pago de proveedores / `6a57a4085` Lotes — needs-repro (변동 없음)
관련 서브모듈(api-ventago / ventago-app / mobile-sales-app) 최근 커밋에 이 증상 수정이 없어 상태 불변. 각 카드의 이전 트리아지 노트(경로 확정 필요/UI 지점 모호) 그대로 유효.

---

## 검증용 명령 (사용자 Mac 에서 직접 실행)
샌드박스에서는 로컬 eslint/DB 실행이 불가하여 참고용으로 남깁니다. 이번 실행은 코드 변경이 없어 lint 대상이 없습니다.

```bash
# 참고: 재빌드로 확인할 카드(6a59189c zebra icon)
cd zebra-agent && npm run build:win   # 산출물: build/VentaGO-Zebra-Agent-Setup.exe

# 참고: 운영 users 조회(6a591d7b) — 조회는 안전
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -c \"SELECT id,name,email,username FROM users WHERE email ILIKE '%vendedor%';\""
```

## 이번 실행에서 하지 않은 것 (규칙 준수)
- git push / 운영 SSH 상태변경 / 운영 DB DDL·DML / docker restart / 마이그레이션 / Trello 카드 삭제·이동: **없음**.
- 작업 트리의 기존 uncommitted 변경 파일(ventago-admin-app, mobile-sales-app, despacho-app 등)은 **건드리지 않음** — 이번 실행은 `.planning/trello-inbox/` (triage-state.json, 본 리포트)만 갱신.
