---
phase: 05-data-import
plan: "03"
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - ventago-app/src/pages/configuracion/importar/index.tsx
  - ventago-app/src/views/import/ImportView.tsx
  - ventago-app/src/views/import/components/FileUploader.tsx
  - ventago-app/src/views/import/components/ImportPreview.tsx
  - ventago-app/src/views/import/components/ImportResult.tsx
  - ventago-app/src/navigation/vertical/index.ts
autonomous: false
requirements: [FEAT-04]
must_haves:
  truths:
    - "admin/superadmin만 /configuracion/importar 페이지에 접근 가능"
    - "JSON 파일을 드래그앤드롭 또는 파일 선택으로 업로드 가능"
    - "업로드된 JSON이 클라이언트에서 파싱되어 미리보기에 요약 표시"
    - "잘못된 JSON 파일 업로드 시 에러 메시지 표시 (API 호출 없음)"
    - "지점 선택 + 파일 파싱 완료 시에만 'Importar' 버튼 활성화"
    - "'Importar' 클릭 시 POST /import/migrate 호출 → 결과 리포트 표시"
    - "결과 리포트에 created/skipped/errors 건수와 에러 상세 표시"
  artifacts:
    - path: "ventago-app/src/pages/configuracion/importar/index.tsx"
      provides: "/configuracion/importar 페이지 엔트리"
    - path: "ventago-app/src/views/import/ImportView.tsx"
      provides: "임포트 메인 뷰 (3단계 스테퍼: 업로드 → 미리보기 → 결과)"
    - path: "ventago-app/src/views/import/components/FileUploader.tsx"
      provides: "JSON 파일 드래그앤드롭 + 파일 선택 컴포넌트"
    - path: "ventago-app/src/views/import/components/ImportPreview.tsx"
      provides: "파싱된 데이터 요약 카드 + 샘플 테이블"
    - path: "ventago-app/src/views/import/components/ImportResult.tsx"
      provides: "임포트 결과 리포트 (created/skipped/errors)"
  key_links:
    - from: "ventago-app/src/views/import/ImportView.tsx"
      to: "/import/migrate"
      via: "apiConnector.post call on import"
      pattern: "apiConnector\\.post.*import/migrate"
---

<objective>
관리자 페이지에 JSON 파일 업로드 기반의 데이터 임포트 UI를 구축한다.
3단계 스테퍼 플로우: 파일 업로드 → 미리보기(검증) → 실행 + 결과 리포트.
</objective>

<context>
@.planning/phases/05-data-import/05-CONTEXT.md
@.planning/phases/05-data-import/05-02-PLAN.md
@api-ventago/src/app/import/import.controller.ts — POST /import/migrate API
@api-ventago/src/app/import/dto/import-migrate.dto.ts — 요청 DTO 구조

프론트엔드 패턴 참고:
@ventago-app/src/services/api.service.ts — apiConnector
@ventago-app/src/hooks/useAuth.ts — 인증 훅
@ventago-app/src/views/admin/stores/hooks/useBranch.ts — 지점 목록 훅
@ventago-app/src/navigation/vertical/index.ts — 네비게이션 (DB 기반 동적 생성)
@ventago-app/src/pages/configuracion/ — 기존 설정 페이지 구조

네비게이션은 user.structure(DB)에서 동적 생성됨.
superadmin의 admin 앱은 하드코딩된 메뉴를 사용 (vertical/index.ts:55-66).
일반 admin은 DB 모듈 기반.
임포트 메뉴 추가 방법:
- superadmin: 하드코딩 배열에 추가
- admin: DB modules 테이블에 레코드 추가 또는 getAppChildren에서 조건부 추가
</context>

<tasks>

<task type="auto">
  <name>Task 1: 페이지 및 메인 뷰 생성</name>
  <files>
    ventago-app/src/pages/configuracion/importar/index.tsx,
    ventago-app/src/views/import/ImportView.tsx
  </files>
  <read_first>
    - ventago-app/src/pages/configuracion/productos/index.tsx (기존 설정 페이지 패턴)
    - ventago-app/src/navigation/vertical/index.ts
    - ventago-app/src/hooks/useAuth.ts
    - ventago-app/src/views/admin/stores/hooks/useBranch.ts
  </read_first>
  <action>
1. `ventago-app/src/pages/configuracion/importar/index.tsx` 생성:
   - ImportView 컴포넌트 렌더링
   - 기존 설정 페이지 패턴 따름

2. `ventago-app/src/views/import/ImportView.tsx` 생성:
   - MUI Stepper로 3단계 플로우: "Subir Archivo" → "Vista Previa" → "Resultado"
   - 상태 관리:
     ```typescript
     const [activeStep, setActiveStep] = useState(0)
     const [file, setFile] = useState<File | null>(null)
     const [parsedData, setParsedData] = useState<ImportPayload | null>(null)
     const [parseError, setParseError] = useState<string | null>(null)
     const [selectedBranchId, setSelectedBranchId] = useState<number | null>(null)
     const [importResult, setImportResult] = useState<ImportResult | null>(null)
     const [loading, setLoading] = useState(false)
     ```
   - Step 0: FileUploader + 지점 선택 드롭다운 (useBranch 훅 사용)
   - Step 1: ImportPreview (파싱된 데이터 요약)
   - Step 2: ImportResult (실행 결과)
   - "Importar" 버튼: parsedData + selectedBranchId 있을 때만 활성화
   - API 호출: `apiConnector.post('/import/migrate', { ...parsedData, targetBranchId: selectedBranchId })`

3. TypeScript 인터페이스 정의 (ImportView.tsx 상단 또는 별도 types.ts):
   ```typescript
   interface ImportPayload {
     categories?: Array<{ legacyId: number; name: string }>
     colors?: Array<{ legacyId: number; name: string }>
     sizes?: Array<{ name: string }>
     parents: Array<{ legacyId: number; sku: string; name: string; price: number; [key: string]: any }>
     variants: Array<{ sku: string; name: string; price: number; parentLegacyId: number; [key: string]: any }>
   }
   interface ImportResultSummary {
     categories: { created: number; existing: number }
     colors: { created: number; existing: number }
     sizes: { created: number; existing: number }
     parents: { created: number; skipped: number; errors: string[] }
     variants: { created: number; skipped: number; errors: string[] }
   }
   ```
  </action>
  <verify>
    <automated>cd ventago-app && npx next lint src/pages/configuracion/importar/index.tsx src/views/import/ImportView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - /configuracion/importar 페이지 접근 시 Stepper UI 표시
    - 지점 선택 드롭다운에 현재 매장의 지점 목록 표시
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FileUploader 컴포넌트</name>
  <files>ventago-app/src/views/import/components/FileUploader.tsx</files>
  <action>
1. 드래그앤드롭 영역 + "Seleccionar archivo" 버튼
2. MUI Box + input type="file" accept=".json"
3. 파일 선택/드롭 시:
   - FileReader로 텍스트 읽기
   - JSON.parse 시도
   - 성공: setParsedData + setParseError(null) + 자동으로 Step 1로 이동
   - 실패: setParseError("Archivo JSON invalido: {error.message}")
4. 필수 키 검증: parents 배열 존재 + variants 배열 존재
   - 없으면: setParseError("El archivo debe contener 'parents' y 'variants'")
5. 파일 정보 표시: 파일명, 크기, 파싱 상태
  </action>
  <acceptance_criteria>
    - .json 파일만 선택 가능
    - 드래그앤드롭 동작
    - 잘못된 JSON 시 에러 메시지 표시
    - 유효한 JSON 시 자동으로 미리보기 단계로 이동
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ImportPreview 컴포넌트</name>
  <files>ventago-app/src/views/import/components/ImportPreview.tsx</files>
  <action>
1. 데이터 요약 카드 (MUI Card + Grid):
   - Categorias: {N} items
   - Colores: {N} items
   - Talles: {N} items
   - Productos Madre: {N} items
   - Productos Hijo: {N} items
2. 샘플 테이블 (MUI Table):
   - 부모 상품 처음 5개: SKU | Nombre | Precio
   - 자식 상품 처음 5개: SKU | Nombre | Precio | Padre SKU | Color | Talle
3. "Importar" 버튼 (지점 선택 + 파싱 완료 시 활성화)
4. "Volver" 버튼 → Step 0으로 이동
  </action>
  <acceptance_criteria>
    - 요약 카드에 정확한 건수 표시
    - 샘플 테이블에 첫 5개 데이터 표시
    - 지점 미선택 시 "Importar" 버튼 비활성화
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: ImportResult 컴포넌트</name>
  <files>ventago-app/src/views/import/components/ImportResult.tsx</files>
  <action>
1. 성공 시:
   - MUI Alert severity="success" + 요약 테이블
   - | 항목 | Creados | Omitidos | Errores |
   - categories, colors, sizes, parents, variants 행
   - "Ver Productos" 버튼 → /productos로 이동
   - "Importar Otro" 버튼 → Step 0으로 리셋
2. 에러 시:
   - MUI Alert severity="error"
   - 에러 목록 (Accordion으로 확장 가능)
   - "Reintentar" 버튼
3. 실행 시간 표시 (summary.durationMs)
  </action>
  <acceptance_criteria>
    - 성공 응답 시 요약 테이블 정확히 표시
    - 에러가 있으면 에러 목록 확장 가능
    - "Ver Productos" 클릭 시 /productos로 이동
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: 네비게이션 메뉴 추가</name>
  <files>ventago-app/src/navigation/vertical/index.ts</files>
  <read_first>
    - ventago-app/src/navigation/vertical/index.ts
  </read_first>
  <action>
1. superadmin 하드코딩 메뉴 (line 56-66)에 추가:
   ```typescript
   { title: 'Importar Datos', icon: 'tabler:database-import', path: '/configuracion/importar' },
   ```
2. 일반 admin용: getAppChildren에서 admin 앱에 조건부 추가:
   ```typescript
   if (appSlug === 'admin') {
     children.push({ title: 'Generar Token', ... });
     children.push({ title: 'Importar Datos', icon: 'tabler:database-import', path: '/configuracion/importar' });
   }
   ```
  </action>
  <verify>
    <automated>grep -n "Importar Datos" ventago-app/src/navigation/vertical/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - superadmin 사이드바에 "Importar Datos" 메뉴 표시
    - admin 사이드바에 "Importar Datos" 메뉴 표시
    - vendedor/gerente에게는 미표시
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: UI 전체 플로우 검증</name>
  <files>none</files>
  <action>
    로컬 환경에서 전체 플로우 테스트:
    1. admin 계정 로그인 → 사이드바에 "Importar Datos" 메뉴 확인
    2. /configuracion/importar 접근 → Stepper UI 표시
    3. 테스트용 JSON 파일 업로드 → 미리보기 확인
    4. 지점 선택 → "Importar" 클릭 → 결과 리포트 확인
    5. /productos에서 임포트된 상품 확인
    6. 잘못된 JSON 파일 업로드 → 에러 메시지 확인
    7. vendedor 계정에서 메뉴 미표시 확인
  </action>
  <what-built>
    관리자 페이지에 JSON 파일 기반 데이터 임포트 UI가 추가되었습니다.
    3단계 플로우: 파일 업로드 + 지점 선택 → 데이터 미리보기 → 임포트 실행 + 결과 리포트.
    admin/superadmin만 접근 가능하며, 사이드바에 "Importar Datos" 메뉴가 표시됩니다.
  </what-built>
  <how-to-verify>
    1. `npm run dev` 로 로컬 실행
    2. admin 계정 로그인
    3. 사이드바 "Importar Datos" 클릭
    4. 테스트 JSON 파일 업로드 (extract-and-build-payload.sh로 생성)
    5. 미리보기에서 건수 확인 → "Importar" 클릭
    6. 결과 리포트에서 created/skipped/errors 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<success_criteria>
1. /configuracion/importar 페이지 정상 렌더링
2. JSON 파일 업로드 + 클라이언트 파싱 동작
3. 미리보기에서 정확한 건수 및 샘플 데이터 표시
4. "Importar" 실행 → 성공 리포트 표시
5. 에러 케이스 (잘못된 JSON, 빈 파일, 필수 키 누락) 처리
6. admin/superadmin만 메뉴 및 페이지 접근 가능
</success_criteria>
