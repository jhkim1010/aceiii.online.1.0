---
phase: 61-tienda-online-editor
plan: 15
type: execute
wave: 8
depends_on: ["61-09", "61-10", "61-11", "61-12", "61-13", "61-14"]
files_modified:
  - .planning/phases/61-tienda-online-editor/61-UAT.md
autonomous: false
requirements: [R8, R1, R2, R3, R4, R5, R6, R7, R9, R10, R11]
must_haves:
  truths:
    - "신규 마이그레이션 파일이 R9 의 CHECK 교체 SQL 1개뿐이고 테이블/컬럼 추가가 0건이다"
    - "변경 파일 전체에 new Pool(/new Client( 신규 호출이 0건이다"
    - "코드베이스에 'doc' 렌더 분기가 0건이다(스펙 강등 테스트 제외)"
    - "api-ventago shop-public 유닛 스위트가 green 이고 tienda-app 이 tsc/eslint 통과한다"
    - "확장 키 없는 기존 매장 공개 페이지가 회귀 없이 렌더된다"
  artifacts:
    - path: ".planning/phases/61-tienda-online-editor/61-UAT.md"
      provides: "R1~R11 자동 게이트 결과 + 브라우저 UAT 체크리스트 + gap 기록"
      contains: "R11"
  key_links:
    - from: "61-UAT.md"
      to: "61-VALIDATION.md Per-Task Verification Map"
      via: "각 Req 의 automated command 실행 결과 기록"
      pattern: "R8"
---

<objective>
Phase 61 전체의 자동 게이트(R8 무회귀·pool·ESLint·마이그레이션 1건·doc 0건)를 실행하고, VALIDATION.md 의 R1~R11 검증 계약을 하나씩 돌려 결과를 `61-UAT.md` 에 기록한다. 자동으로 못 잡는 항목(브라우저 시각/재생/세션/운영 DB 대조)은 체크리스트로 정리해 사용자 UAT 를 유도한다.

Purpose: 산발적으로 검증된 개별 플랜을 phase 게이트로 수렴. 회귀·pool·DDL 위반을 최종 차단.
Output: `61-UAT.md` + 자동 게이트 통과 확인.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-VALIDATION.md
@.planning/phases/61-tienda-online-editor/61-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 자동 게이트 실행 + 결과 수집</name>
  <files>.planning/phases/61-tienda-online-editor/61-UAT.md</files>
  <read_first>
    - .planning/phases/61-tienda-online-editor/61-VALIDATION.md (Per-Task Verification Map — R1~R11 automated command)
    - .planning/phases/61-tienda-online-editor/61-SPEC.md 「완료 기준」 + R8 acceptance
  </read_first>
  <action>
아래 명령을 순서대로 실행하고 각 출력을 `61-UAT.md` 에 기록한다. **하나라도 실패하면 UAT.md 에 gap 으로 명시**하고(어느 플랜의 어느 파일인지) 통과할 때까지 원인을 좁힌다.

**A. R8 무회귀·pool·DDL 게이트**
```bash
# 1) 신규 마이그레이션이 R9 CHECK 교체 1개뿐 (테이블/컬럼 추가 0)
git status --porcelain api-ventago/migrations/ | grep "^??" | wc -l          # 1 기대
ls api-ventago/migrations/2026-07-24-store-themes-macro-4.sql                  # 존재
grep -ciE "CREATE TABLE|ADD COLUMN|DROP COLUMN" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql   # 0

# 2) 신규 Pool/Client 0 (변경된 백엔드 파일 전체)
grep -rn "new Pool(\|new Client(" api-ventago/src/app/shop-public/         # store-theme-asset/catalog/constants/admin 등 0건

# 3) doc 잔여 0 (스펙 강등 테스트 제외)
grep -rn "'doc'" tienda-app/src/ api-ventago/src/app/shop-public/ | grep -v ".spec.ts"   # 0건

# 4) 신규 라이브러리 0
git diff main -- tienda-app/package.json api-ventago/package.json | wc -l    # 0

# 5) dangerouslySetInnerHTML 0
grep -rn "dangerouslySetInnerHTML" tienda-app/src/                            # 0건
```

**B. 유닛 + lint 게이트**
```bash
cd api-ventago && npx jest src/app/shop-public --silent                       # green
cd api-ventago && npx eslint src/app/shop-public/                             # 0
cd tienda-app && npx tsc --noEmit                                             # 0
cd tienda-app && npx eslint src/                                              # 0
```
**주의(CONTEXT LOCKED)**: `cd tienda-app && npx tsc --noEmit` 는 tienda-app 단독이라 가볍다. **api-ventago 전체 타입체크(`tsc -b` 전 워크스페이스)는 device VM OOM 이력이 있으므로 실행하지 않는다** — 최종 타입 게이트는 Jenkins 빌드. 이 사실을 UAT.md 에 명시.

**C. R1~R11 자동 검증 매핑** — VALIDATION.md 의 각 Req 의 automated 명령을 실행. 서버 기동이 필요한 smoke(curl)는 `./scripts/smoke-shop-theme.sh` 로 대체 실행하되, 서버 미기동이면 SKIP 으로 기록(브라우저 UAT 로 이관).

`61-UAT.md` 를 아래 구조로 작성:
```markdown
# Phase 61 — UAT

**Executed:** {date}
**Status:** {passed | passed_with_gaps | failed}

## 자동 게이트 (R8 + 유닛 + lint)
| Gate | Command | Result | Evidence |
|------|---------|--------|----------|
| 마이그레이션 1건 | git status ... migrations | ✅/❌ | {출력} |
| 신규 Pool 0 | grep new Pool | ✅ | {출력} |
| doc 잔여 0 | grep 'doc' | ✅ | {출력} |
| 신규 lib 0 | git diff package.json | ✅ | {출력} |
| shop-public 유닛 | npx jest | ✅ | {Tests: N passed} |
| tienda-app tsc | npx tsc --noEmit | ✅ | {출력} |
| ESLint (양쪽) | npx eslint | ✅ | {출력} |

## R1~R11 검증 결과
| Req | Behavior | Type | Result | Note |
| ... (VALIDATION.md 표를 채운다) ... |
```
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/shop-public --silent && cd ../tienda-app && npx tsc --noEmit && npx eslint src/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/61-tienda-online-editor/61-UAT.md` exit 0
    - `grep -c "자동 게이트" .planning/phases/61-tienda-online-editor/61-UAT.md` == 1
    - `grep -c "R11" .planning/phases/61-tienda-online-editor/61-UAT.md` >= 1
    - `git status --porcelain api-ventago/migrations/ | grep -c "2026-07-24-store-themes-macro-4.sql"` == 1
    - `ls api-ventago/migrations/ | grep -cE "2026-07-2[4-9]|2026-08" | head` — Phase 61 신규 파일이 1개인지 UAT.md 에 기록
    - `grep -rn "new Pool(\|new Client(" api-ventago/src/app/shop-public/ | grep -v "shop-readonly-db.service.ts" | wc -l` == 0 (기존 격리 pool 1개만 허용)
    - `grep -rn "'doc'" tienda-app/src/ api-ventago/src/app/shop-public/ | grep -v ".spec.ts" | wc -l` == 0
    - `cd api-ventago && npx jest src/app/shop-public --silent` exit 0
    - `cd tienda-app && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>모든 자동 게이트 결과가 61-UAT.md 에 기록되고 실패 항목이 gap 으로 명시된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 브라우저 UAT + 운영 5434 마이그레이션 확인</name>
  <read_first>
    - .planning/phases/61-tienda-online-editor/61-VALIDATION.md § "Manual-Only Verifications"
    - .planning/phases/61-tienda-online-editor/61-UAT.md (Task 1 자동 결과)
  </read_first>
  <what-built>
    Phase 61 이 tienda-app 스토어프런트/에디터 + api-ventago shop-public 을 확장했다. 자동 게이트(R8·유닛·lint·doc 0·마이그레이션 1건)는 Task 1 에서 통과했다. 아래는 자동으로 못 잡는 항목이다.
  </what-built>
  <how-to-verify>
    `./dev.sh` 로 로컬 기동 후(또는 tienda-app dev 서버) 확인:

    **무회귀 (최우선)**
    1. 확장 키 없는 기존 매장(예: storeId=9 ACE)의 공개 페이지를 열어 **변경 전과 시각적으로 동일**한지 확인(hero 문구/Destacados/AI strip 이 DEFAULT_CONTENT 로 재현). 깨지면 즉시 gap.

    **R3/R4 에디터→공개 왕복**
    2. `/{storeId}/panel/diseno?t=<edit-link 토큰>` 에서 아코디언 12그룹 확인, hero 를 benefits 아래로 이동 + newsletter 켬 → Guardar borrador → 미리보기 반영 → Publicar → 공개 페이지에 순서/토글 반영.
    3. 로고 png 업로드 → 헤더에 로고 이미지, 3MB/.exe 는 거부 문구.

    **R9 rails/masonry**
    4. 구조 카드에서 Rails 선택 → 미리보기 즉시 선반 레이아웃 → Publicar → 공개 홈이 가로 스크롤 선반. Masonry 도 동일. marquee/bento 회귀 없음.
    5. Rails → Marquee 되돌린 뒤 carousel 설정이 보존됐는지(값 복귀) 확인.
    6. **운영 5434 마이그레이션**: Plan 61-04 Task 2 에서 이미 적용·대조했다면 그 결과를 재확인. 미적용이면 여기서 적용(사용자 승인 필수 DDL).

    **R10 reels**
    7. reels 에 영상 2개(poster 포함) 업로드 → publish → 공개 홈에서 세로 카드, DevTools Network 에서 **초기 로드 시 영상 바이트 0**(poster 만) → 탭하면 재생, 다른 영상 탭 시 이전 정지. 21MB/.mov 거부.

    **R11 quiz**
    8. quiz 질문 3개 설정 → publish → 공개 홈 배너 → 3문항 → 추천 3개(MATCH 배지+이유). DevTools Network 에서 **요청 URL 이 /public/shop/*/products 뿐**(신규 엔드포인트 0). Ver catálogo completo 가 필터 적용 이동.

    **R7 마케팅**
    9. 팝업 켜고 publish → 첫 방문 1회 표시, 재방문 안 뜸(sessionStorage). seoTitle 설정 → 페이지 소스 `<title>` 확인. pixelId null → `<script id="meta-pixel">` 없음.

    **R5/R6**
    10. `productCard.discountBadge=false` → 배지 사라짐. `catalog.pageSize=12&sort=price_asc` → 12개 가격 오름차순.
    10b. **가격 필터(실구현)**: `filters.price=false` publish → 공개 목록에 가격 필터 UI 가 **없음**. `filters.price=true` publish → 가격 필터 UI 표시 → 구간(예: 10000~30000) 지정 시 목록이 그 구간으로 좁혀지고 요청 URL 에 `minPrice`/`maxPrice` 반영(DevTools Network).
    10c. **color/size 필터(no-op 예외)**: `filters.color`/`filters.size` 를 켜고 꺼도 공개몰에 색상/사이즈 필터 UI 가 **렌더되지 않음**(가짜 필터 없음). 토글 값은 draft/published JSONB 에 저장만 되는지 확인(에디터 재로드 시 값 유지). 이는 공개 API 에 variant 집계가 없어 확정된 예외(R5 variantDots 와 동일).

    결과를 61-UAT.md 브라우저 섹션에 ✅/⚠️/❌ 로 기록.
  </how-to-verify>
  <acceptance_criteria>
    - 무회귀(1) 확인: 기존 매장 공개 페이지 변경 전/후 동일
    - rails/masonry(4) 렌더 확인 + marquee/bento 회귀 없음
    - reels(7) 초기 영상 바이트 0 + 탭 재생
    - quiz(8) 네트워크 요청이 카탈로그 엔드포인트뿐
    - 가격 필터(10b): filters.price=false 시 UI 숨김, true+구간 시 목록 좁힘 + minPrice/maxPrice URL 반영
    - color/size 필터(10c): 필터 UI 미렌더(예외) + 토글 값 저장 확인
    - 운영 5434 CHECK 제약이 4값(로컬과 동일)
    - 61-UAT.md 브라우저 섹션 작성 완료
  </acceptance_criteria>
  <resume-signal>전 항목 확인 시 "uat passed" 또는 발견한 gap 을 나열해 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 전체 변경 표면 → 운영 배포 | 이 게이트가 회귀·pool 누수·미검증 DDL 을 마지막으로 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-73 | Elevation | 미검증 마이그레이션이 2개 이상 슬쩍 포함 | mitigate | `git status migrations` 로 신규 파일 1개 강제 확인 |
| T-61-74 | DoS | 새 Pool/Client 가 어딘가 추가됨 | mitigate | 전 shop-public grep(격리 pool 1개 제외) 0건 |
| T-61-75 | Tampering | doc 렌더 잔여로 도달 불가 코드 | mitigate | grep 0건 게이트 |
| T-61-76 | Repudiation | 운영 DDL 무기록 적용 | mitigate | 61-04 SUMMARY + 61-UAT.md 에 양쪽 제약 정의 기록 |
| T-61-77 | Tampering | 무회귀 미검증 배포로 기존 매장 파손 | mitigate | 브라우저 UAT 무회귀 항목을 blocking 체크포인트로 |
</threat_model>

<verification>
```bash
cd api-ventago && npx jest src/app/shop-public --silent
cd tienda-app && npx tsc --noEmit && npx eslint src/
git status --porcelain api-ventago/migrations/
grep -rn "new Pool(\|new Client(" api-ventago/src/app/shop-public/ | grep -v shop-readonly-db.service.ts
grep -rn "'doc'" tienda-app/src/ api-ventago/src/app/shop-public/ | grep -v ".spec.ts"
```
</verification>

<success_criteria>
- 자동 게이트 전부 통과(마이그레이션 1건 · Pool 0 · doc 0 · lib 0 · 유닛 green · tsc/eslint 0)
- R1~R11 검증 결과가 61-UAT.md 에 기록
- 브라우저 UAT 무회귀 + rails/masonry/reels/quiz 확인
- 운영 5434 CHECK 제약이 로컬과 동일한 4값
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-15-SUMMARY.md` 생성 + `61-UAT.md` 갱신
</output>
