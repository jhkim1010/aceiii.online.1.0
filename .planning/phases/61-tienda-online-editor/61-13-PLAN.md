---
phase: 61-tienda-online-editor
plan: 13
type: execute
wave: 7
depends_on: ["61-09", "61-12"]
files_modified:
  - tienda-app/src/components/MarketingPopup.tsx
  - tienda-app/src/pages/[storeId]/index.tsx
  - tienda-app/src/pages/[storeId]/panel/diseno.tsx
autonomous: true
requirements: [R7, R6, R8]
must_haves:
  truths:
    - "marketing.popup.enabled=true 로 publish 하면 첫 방문에 팝업이 1회 뜨고 같은 세션 재방문에는 안 뜬다"
    - "seoTitle 을 설정하면 페이지 소스의 <title> 이 그 값이다"
    - "pixelId 가 null 이면 pixel 스크립트 태그가 없고, 유효한 값이면 <script> 가 삽입된다"
    - "trust(결제/배송 로고·정책 링크·compra protegida)를 에디터에서 편집해 푸터에 반영할 수 있다"
  artifacts:
    - path: "tienda-app/src/components/MarketingPopup.tsx"
      provides: "세션 1회 웰컴 팝업(제목 + 쿠폰 코드)"
      contains: "sessionStorage"
    - path: "tienda-app/src/pages/[storeId]/index.tsx"
      provides: "SEO title/description <Head> 주입 + 조건부 pixel + 팝업 마운트"
      contains: "seoTitle"
    - path: "tienda-app/src/pages/[storeId]/panel/diseno.tsx"
      provides: "🛡 Confianza + 📈 Marketing & SEO 아코디언 그룹"
      contains: "Marketing & SEO"
  key_links:
    - from: "index.tsx <Head>"
      to: "content.marketing.seoTitle/seoDescription/pixelId"
      via: "getServerSideProps 로 받은 theme 에서 SSR 주입"
      pattern: "content.marketing.seoTitle"
    - from: "MarketingPopup.tsx"
      to: "sessionStorage popup_shown_{storeId}"
      via: "세션 1회 판정"
      pattern: "popup_shown_"
---

<objective>
마케팅 웰컴 팝업(세션 1회) + SEO title/description `<Head>` 주입 + 조건부 pixel 스크립트를 완성하고(R7), 신뢰 요소(결제/배송 로고·정책 링크·compra protegida) 에디터 그룹(R6 trust)을 붙인다. 쿠폰 코드 ↔ Campañas discounts 실검증은 범위 외 — 표시 전용 + TODO.

Output: MarketingPopup + index.tsx SEO/pixel/팝업 + 에디터 그룹 2개(🛡 Confianza, 📈 Marketing & SEO).
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-RESEARCH.md

<interfaces>
```typescript
// content.marketing: { popup: { enabled: boolean; title: string; coupon: string | null }; seoTitle: string | null; seoDescription: string | null; pixelId: string | null }
// content.trust: { paymentLogos: string[]; shippingLogos: string[]; protectedBadge: boolean; policyLinks: { label: string; href: string }[] }
// 백엔드 sanitize(Plan 61-01): pixelId 는 /^[A-Za-z0-9_-]{1,64}$/, coupon 은 /^[A-Za-z0-9_-]{0,32}$/, seoTitle 70자, seoDescription 160자
// next/script 는 Next 13.3.2 지원. 프로젝트에 next/script 사용례 0건 — 신규 패턴.
```
Footer 는 Plan 61-09 가 이미 `trust` 를 렌더한다 — 이 플랜은 trust **에디터**만 추가한다.
</interfaces>

**UI-SPEC 계약**
- 팝업: 중앙 오버레이 `background:rgba(0,0,0,0.4)`, 박스 밝은 배경(`var(--card)`), radius 14, 패딩 32, 닫기 `✕`(우상단, `aria-label="Cerrar"`), 쿠폰 코드 점선 pill(`border:1.5px dashed var(--gold); color:var(--gold)`), `zIndex:50`(WhatsApp 30 위). `sessionStorage` 키 `popup_shown_{storeId}`. 별도 액션 버튼 없이 `✕` 로만 닫음. 목업 예시 제목 `¡10% OFF en tu primera compra!`
- pixel: `pixelId` 있을 때만 `<Script strategy="afterInteractive">` 로 Meta Pixel 삽입. `pixelId` 는 백엔드가 영숫자/하이픈/언더스코어로 이미 걸렀으므로 템플릿 리터럴 삽입 안전.
- SEO: `seoTitle` 있으면 `<title>` override, 없으면 기존 기본값. `seoDescription` 동일.
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarketingPopup.tsx — 세션 1회 웰컴 팝업</name>
  <files>tienda-app/src/components/MarketingPopup.tsx</files>
  <read_first>
    - .planning/phases/61-tienda-online-editor/61-RESEARCH.md § "세션 1회 팝업 판정" (sessionStorage 코드)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 B — 마케팅 팝업"
    - tienda-app/src/context/ThemeContentContext.tsx (useThemeContent)
  </read_first>
  <action>
```tsx
export default function MarketingPopup({ storeId }: { storeId: number }) {
  const { marketing } = useThemeContent();
  const [show, setShow] = useState(false);

  useEffect(() => {
    if (!marketing.popup.enabled || !marketing.popup.title) return;
    const key = `popup_shown_${storeId}`;
    if (typeof window === 'undefined') return;
    if (sessionStorage.getItem(key)) return;
    setShow(true);
    sessionStorage.setItem(key, '1');
  }, [storeId, marketing.popup.enabled, marketing.popup.title]);

  if (!show) return null;
  // 오버레이 + 박스 + ✕(aria-label="Cerrar") + 제목 + (coupon 있으면) 점선 pill
}
```
- 오버레이 클릭 또는 `✕` 클릭 시 `setShow(false)`. 박스 내부 클릭은 `stopPropagation`.
- `sessionStorage` 접근은 반드시 `useEffect`(클라이언트) 안에서 — SSR 에서 `sessionStorage` 없음.
- 쿠폰: `marketing.popup.coupon` 있으면 `<div style={couponPill}>{coupon}</div>` — 별도 액션 버튼 없음.
- 파일 상단 한국어 주석: `// TODO(후속 Phase): 쿠폰 코드 ↔ Campañas discounts 실검증/발급 연동은 Phase 61 범위 외 — 현재는 표시 전용.`
- ESC 키로도 닫히게 `keydown` 리스너 추가(접근성) + cleanup.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/MarketingPopup.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "popup_shown_" tienda-app/src/components/MarketingPopup.tsx` >= 2 (set + get)
    - `grep -c 'aria-label="Cerrar"' tienda-app/src/components/MarketingPopup.tsx` == 1
    - `grep -c "typeof window === 'undefined'" tienda-app/src/components/MarketingPopup.tsx` == 1
    - `grep -c "TODO(후속 Phase): 쿠폰 코드" tienda-app/src/components/MarketingPopup.tsx` == 1
    - `grep -c "stopPropagation" tienda-app/src/components/MarketingPopup.tsx` == 1
    - `grep -c "dashed var(--gold)" tienda-app/src/components/MarketingPopup.tsx` == 1
    - `cd tienda-app && npx eslint src/components/MarketingPopup.tsx` exit 0
  </acceptance_criteria>
  <done>팝업이 세션당 1회 뜨고 SSR-safe 하며 쿠폰은 표시 전용이다.</done>
</task>

<task type="auto">
  <name>Task 2: index.tsx — SEO <Head> 주입 + 조건부 pixel + 팝업 마운트</name>
  <files>tienda-app/src/pages/[storeId]/index.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/index.tsx (Plan 61-12 결과 — <Head>/파비콘/Footer/WhatsAppFloat 위치)
    - .planning/phases/61-tienda-online-editor/61-RESEARCH.md § "getServerSideProps + next/head" + "조건부 3rd-party 스크립트 삽입"
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § SEO/pixel
  </read_first>
  <action>
1) **`<Head>` SEO override**:
```tsx
<Head>
  <title>{theme.content.marketing.seoTitle || 'CoolShop — Tienda online'}</title>
  <meta
    name="description"
    content={theme.content.marketing.seoDescription || 'CoolShop — indumentaria online con probador virtual con IA.'}
  />
  {theme.content.brand.faviconFile ? (<link rel="icon" href={minioImageUrl(theme.content.brand.faviconFile)} />) : null}
</Head>
```
기존 하드코딩 `<title>CoolShop — Tienda online</title>` 를 이 형태로 교체. **SSR 에서 주입돼야 크롤러가 본다** — `getServerSideProps` 가 이미 `theme` 을 SSR 로 넘기므로 추가 조회 불필요.

2) **조건부 pixel** — `import Script from 'next/script';` 추가. `<div wrapStyle>` 최하단(Footer 뒤)에:
```tsx
{theme.content.marketing.pixelId ? (
  <Script id="meta-pixel" strategy="afterInteractive">
    {`!function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,document,'script','https://connect.facebook.net/en_US/fbevents.js');fbq('init','${theme.content.marketing.pixelId}');fbq('track','PageView');`}
  </Script>
) : null}
```
   **`pixelId` 는 백엔드 `PIXEL_ID_RE` 로 이미 영숫자/하이픈/언더스코어만 남았으므로** 템플릿 리터럴 삽입이 안전하다 — 이 근거를 한국어 주석으로 남긴다.

3) **팝업 마운트** — `<WhatsAppFloat />` 옆에 `<MarketingPopup storeId={storeId} />` 추가.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "content.marketing.seoTitle" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "content.marketing.seoDescription" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c 'import Script from' "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c 'id="meta-pixel"' "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "content.marketing.pixelId ?" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "<MarketingPopup" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "PIXEL_ID_RE\|영숫자/하이픈" "tienda-app/src/pages/[storeId]/index.tsx"` >= 1 (안전 근거 주석)
    - `cd tienda-app && npx tsc --noEmit` exit 0 · `npx eslint src/` exit 0
  </acceptance_criteria>
  <done>SEO/pixel/팝업이 조건부로 주입되고, pixelId 없으면 스크립트 태그가 없다.</done>
</task>

<task type="auto">
  <name>Task 3: diseno.tsx — 🛡 Confianza + 📈 Marketing & SEO 그룹</name>
  <files>tienda-app/src/pages/[storeId]/panel/diseno.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/panel/diseno.tsx (Plan 61-12 결과 — 그룹 8·9 뒤)
    - tienda-app/src/components/panel/AssetUploadField.tsx (kind="payment"/"shipping")
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § 아코디언 그룹 11·12
  </read_first>
  <action>
그룹 9 뒤, 그룹 10(`💬 Contacto & redes`, Plan 61-08 이 이미 추가) 뒤에 2개 추가(접힘):

**그룹 11 `🛡 Confianza, pagos y envíos`** — `content.trust`:
- 결제 로고: `paymentLogos` 최대 8, 각 `AssetUploadField kind="payment" accept="image/png,image/jpeg,image/webp,image/svg+xml"` + `✕`. `+ Agregar logo de pago`
- 배송사 로고: `shippingLogos` 동일 패턴, `kind="shipping"`
- `protectedBadge` SwitchField — `Mostrar «Compra protegida»`
- 정책 링크: `policyLinks` 최대 6, 각 행 = label TextField(60) + href TextField(500, placeholder `https://... o /ruta`) + `✕`. `+ Agregar enlace`

**그룹 12 `📈 Marketing & SEO`** — `content.marketing`:
- 팝업: `popup.enabled` SwitchField(`Popup de bienvenida`) · `popup.title` TextField(120, placeholder `¡10% OFF en tu primera compra!`) · `popup.coupon` TextField(32, placeholder `BIENVENIDO10`, hint `El código se muestra pero todavía no se valida automáticamente.`)
- `seoTitle` TextField(70, hint `Aparece en la pestaña del navegador y en Google.`)
- `seoDescription` TextField(160, multiline)
- `pixelId` TextField(64, placeholder `1234567890`, hint `Meta Pixel ID — solo números, letras, guiones.`)
각 변경 시 `patchContent({ trust: {...} })` / `patchContent({ marketing: {...} })`.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/panel/diseno.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Confianza, pagos y envíos" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "Marketing & SEO" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c 'kind="payment"' "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c 'kind="shipping"' "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "El código se muestra pero todavía no se valida" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "patchContent({ trust" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 1
    - `grep -c "patchContent({ marketing" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 1
    - `cd tienda-app && npx eslint src/` exit 0
  </acceptance_criteria>
  <done>신뢰/마케팅 그룹이 추가되어 12그룹 아코디언이 완성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `marketing.pixelId` → `<script>` 템플릿 리터럴 | 유일하게 텍스트 이스케이프로 방어 불가한 스크립트 컨텍스트 |
| `marketing.seoTitle/Description` → `<title>`/`<meta>` | React 가 자동 이스케이프하는 속성/텍스트 컨텍스트 |
| `trust.policyLinks[].href` → `<a href>` (Footer) | Plan 61-09 렌더, 여기서는 편집만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-64 | Elevation | `pixelId` 스크립트 인젝션 | mitigate | 백엔드 `PIXEL_ID_RE = /^[A-Za-z0-9_-]{1,64}$/` 로 따옴표/괄호/세미콜론 구조적 차단(Plan 61-01). 프런트는 걸러진 값만 삽입 |
| T-61-65 | Tampering | `seoTitle/Description` XSS | mitigate | JSX `{value}` 속성/텍스트 — React 자동 이스케이프. `dangerouslySetInnerHTML` 미사용 |
| T-61-66 | Tampering | `popup.coupon` 표시값 조작 | mitigate | 백엔드 `/^[A-Za-z0-9_-]{0,32}$/` whitelist |
| T-61-67 | Information Disclosure | 팝업이 매 방문 노출(스팸) | mitigate | `sessionStorage` 세션 1회 판정 |
| T-61-68 | Tampering | 3rd-party pixel 이 임의 도메인 스크립트 로드 | accept | Meta Pixel 고정 도메인(`connect.facebook.net`)만 삽입. pixelId 는 계정 식별자일 뿐 URL 조립 불가 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "dangerouslySetInnerHTML" tienda-app/src/     # 0건
# 스모크: seoTitle 설정 + pixelId=null publish 후
curl -s "http://localhost:3060/9" | grep -c "<title>"                  # 1
curl -s "http://localhost:3060/9" | grep -c 'id="meta-pixel"'          # pixelId null 이면 0
```
</verification>

<success_criteria>
- 팝업 세션 1회, SEO title 반영, pixelId null 시 스크립트 없음
- trust/marketing 에디터 그룹으로 12그룹 아코디언 완성
- pixelId 인젝션 차단(백엔드 whitelist + 안전 삽입)
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-13-SUMMARY.md` 생성
</output>
