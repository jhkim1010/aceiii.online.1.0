---
phase: 61-tienda-online-editor
plan: 05
type: execute
wave: 3
depends_on: ["61-01", "61-02", "61-03", "61-04"]
files_modified:
  - tienda-app/src/types/shop.ts
  - tienda-app/src/lib/theme-preset.ts
  - tienda-app/src/services/shop-api.ts
  - tienda-app/src/context/ThemeContentContext.tsx
  - tienda-app/src/pages/[storeId]/index.tsx
  - tienda-app/src/pages/[storeId]/panel/diseno.tsx
autonomous: true
requirements: [R1, R2, R5, R6, R9, R8]
must_haves:
  truths:
    - "프런트 Macrostructure 타입이 4종이고 MACRO_OPTIONS 도 4개다"
    - "코드베이스 tienda-app/src 에 'doc' 문자열이 0건이다"
    - "listProducts 가 sort/showOutOfStock/minPrice/maxPrice 쿼리 파라미터를 전달한다"
    - "uploadThemeAsset(storeId, token, file, kind) 로 에셋을 업로드할 수 있다"
    - "useThemeContent() 가 어디서든 안전한 기본 content 를 반환한다(Provider 밖에서도 크래시 없음)"
  artifacts:
    - path: "tienda-app/src/types/shop.ts"
      provides: "StoreThemeContent/SectionConfig/MacroSettings/Macrostructure 4종/ShopProduct priceOrig·stock 미러"
      contains: "export interface StoreThemeContent"
    - path: "tienda-app/src/context/ThemeContentContext.tsx"
      provides: "테마 content 를 컴포넌트 트리에 내려주는 단일 진입점 + DEFAULT_CONTENT 폴백"
      contains: "useThemeContent"
    - path: "tienda-app/src/services/shop-api.ts"
      provides: "content 저장 · sort/showOutOfStock 조회 · uploadThemeAsset"
      contains: "uploadThemeAsset"
  key_links:
    - from: "tienda-app/src/types/shop.ts"
      to: "api-ventago/src/app/shop-public/store-theme.constants.ts"
      via: "의도적 타입 미러 (백엔드가 SSOT)"
      pattern: "StoreThemeContent"
    - from: "tienda-app/src/services/shop-api.ts::uploadThemeAsset"
      to: "POST /shop/:storeId/theme/asset"
      via: "FormData + Bearer 편집 토큰"
      pattern: "theme/asset"
---

<objective>
백엔드가 확정한 계약(content / macrostructure 4종 / priceOrig·stock / sort·showOutOfStock / asset 업로드)을 tienda-app 쪽에 미러링하고, 이후 모든 프런트 플랜이 쓸 단일 진입점(`useThemeContent()`)을 만든다. `Macrostructure` 에서 `'doc'` 이 사라지면 `index.tsx`/`diseno.tsx` 의 기존 `=== 'doc'` 비교가 TypeScript 에러(TS2367)가 되므로, **doc 렌더 분기 제거를 이 플랜에서 함께 처리**해 컴파일을 항상 통과 상태로 유지한다.

Purpose: 프런트 전 플랜의 타입/API 기반 확보 + R9 의 `doc` 잔여 코드 0건 달성.
Output: 확장된 타입·API 클라이언트·테마 컨텍스트 + doc 제거.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-SPEC.md
@.planning/phases/61-tienda-online-editor/61-PATTERNS.md
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md

<interfaces>
백엔드 확정 계약 (Plan 61-01/61-03/61-04 산출물 — 이 값을 그대로 미러):
```typescript
export type Macrostructure = 'marquee' | 'bento' | 'rails' | 'masonry';
export type CatalogSort = 'newest' | 'price_asc' | 'price_desc' | 'bestseller';
export interface StoreThemeContent {
  brand: { displayName: string; logoFile: string | null; faviconFile: string | null };
  announce: { enabled: boolean; text: string; href: string | null };
  sections: SectionConfig[];   // 7종: hero | benefits | carousel | duoBanners | newsletter | reels | quiz
  contact: { whatsapp: string | null; instagram: string | null; facebook: string | null; footerText: string | null };
  productCard: { discountBadge: boolean; installments: boolean; quickAdd: boolean; hoverSecondImage: boolean; lastUnitsBadge: boolean; variantDots: boolean };
  catalog: { defaultSort: CatalogSort; pageSize: number; showOutOfStock: boolean; filters: { size: boolean; color: boolean; price: boolean } };
  trust: { paymentLogos: string[]; shippingLogos: string[]; protectedBadge: boolean; policyLinks: { label: string; href: string }[] };
  marketing: { popup: { enabled: boolean; title: string; coupon: string | null }; seoTitle: string | null; seoDescription: string | null; pixelId: string | null };
  macroSettings: MacroSettings;   // Plan 61-04 산출물 (rails/masonry 만)
}
// ★ SectionConfig 7종 · QuizQuestion · ReelsSection item 형태는 store-theme.constants.ts(Plan 61-01) 정의를 그대로 복사할 것
export interface StoreThemeResponse { storeId; baseTheme; macrostructure; tokens; cssVars; enabled; content: StoreThemeContent }
export interface ShopProductDto { ...기존...; priceOrig: number | null; stock: number | null }
// POST /shop/:storeId/theme/asset?kind=logo|favicon|hero|banner|payment|shipping|reelVideo|reelPoster
//   multipart field 명 = 'file', 응답 = { fileName: string }
```

tienda-app 스타일/의존성 관례 (LOCKED — 절대 벗어나지 말 것):
- 의존성은 `next`/`react`/`react-dom` 3개 고정. **신규 npm 패키지 도입 금지.**
- MUI 없음, styled-components 없음. 파일 하단 `const s: Record<string, CSSProperties> = { ... }` 상수 + `var(--*)` 참조가 유일한 스타일 관례.
- API 호출은 `apiConnector` 가 아니라 `shop-api.ts` 의 `req()`/`fetch` 패턴.
- 이미지/영상 URL 조립은 반드시 `minioImageUrl()` 경유(직접 템플릿 문자열 금지).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types/shop.ts + lib/theme-preset.ts 미러 확장 (Macrostructure 4종 포함)</name>
  <files>tienda-app/src/types/shop.ts, tienda-app/src/lib/theme-preset.ts</files>
  <read_first>
    - tienda-app/src/types/shop.ts (전체 — 52행의 Macrostructure 3종)
    - tienda-app/src/lib/theme-preset.ts (전체 — 특히 107~111행 MACRO_OPTIONS, tokensToCssVars)
    - api-ventago/src/app/shop-public/store-theme.constants.ts (미러 원본. 값이 하나라도 다르면 미리보기와 발행 결과가 어긋난다)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 A — 구조 선택 카드" (4종 라벨/설명/적합 매장 카피)
  </read_first>
  <action>
**A. `tienda-app/src/types/shop.ts`**

1) `ShopProduct` 에 추가:
```typescript
  priceOrig: number | null;
  stock: number | null;
```
2) `Macrostructure` 를 `'marquee' | 'bento' | 'rails' | 'masonry'` 로 교체(`'doc'` 삭제).
3) `CatalogSort` 타입 추가: `export type CatalogSort = 'newest' | 'price_asc' | 'price_desc' | 'bestseller';`
4) `SectionConfig` / `MacroSettings` / `StoreThemeContent` 를 백엔드와 **필드명·옵셔널성까지 동일하게** 미러(위 `<interfaces>` 블록 그대로).
5) `StoreTheme` 인터페이스에 `content: StoreThemeContent;` 추가.
6) 파일 상단 주석에 한 줄 추가: `// ★ 백엔드 store-theme.constants.ts 의 의도적 미러 — 값이 어긋나면 미리보기≠발행결과.`

**B. `tienda-app/src/lib/theme-preset.ts`**

1) `MACRO_OPTIONS` 를 4종으로 교체. 라벨/설명/적합매장 카피는 UI-SPEC 「구조 선택 카드」 표의 스페인어 원문을 그대로 옮긴다:
```typescript
export interface MacroOption {
  id: Macrostructure;
  label: string;      // 카드 이름
  tag: 'exist' | 'new';  // YA EXISTE(초록) | NUEVO(gold)
  desc: string;       // 설명 1줄
  fit: string;        // 적합 매장 1줄
}

export const MACRO_OPTIONS: MacroOption[] = [
  { id: 'marquee', label: 'Marquee', tag: 'exist',
    desc: 'Hero grande arriba + grilla de productos abajo. El clásico.',
    fit: 'Ideal si tenés una campaña o una foto fuerte por temporada.' },
  { id: 'bento', label: 'Bento', tag: 'exist',
    desc: 'Mosaico de baldosas de distinto tamaño. Cada baldosa es una categoría o un destacado.',
    fit: 'Ideal si vendés varias categorías bien distintas y querés que se vean todas de una.' },
  { id: 'rails', label: 'Rails', tag: 'new',
    desc: 'Estantes horizontales tipo Netflix. Cada fila es una selección con scroll lateral.',
    fit: 'Ideal si tenés mucho SKU y clientes que vuelven seguido: se recorre rapidísimo en el celular.' },
  { id: 'masonry', label: 'Masonry', tag: 'new',
    desc: 'Grilla irregular tipo Pinterest: cada foto conserva su alto real.',
    fit: 'Ideal si tus fotos son verticales y de producción propia. Se navega mirando, no leyendo.' },
];
```
   기존 소비자(`diseno.tsx` 의 `MACRO_OPTIONS.map(m => ... m.label)`)는 `label` 필드를 계속 쓰므로 깨지지 않는다.

2) `tokensToCssVars()` 반환 객체에 `'--on-navy': '#ffffff',` 추가 — 백엔드와 동일 위치(`'--green'` 아래). 미러가 어긋나면 에디터 미리보기에서만 색이 깨진다.

3) **`GATED_BY_MACRO` 를 export 한다** — 구조별 비활성 섹션 규칙(LOCKED). 이 상수의 단일 소유지를 여기(theme-preset.ts)로 정해 `index.tsx`(Plan 61-09)와 `SectionGatingChips.tsx`(Plan 61-10)가 **둘 다 여기서 import** 하게 한다(중복 정의 금지 — 에디터 안내와 실제 렌더 게이팅이 갈라지면 안 됨):
```typescript
import type { Macrostructure, SectionType } from '@/types/shop';
// 구조별로 렌더에서 생략되는 섹션(값은 JSONB 에 보존, 렌더만 skip). marquee=전부 가능.
export const GATED_BY_MACRO: Record<Macrostructure, SectionType[]> = {
  marquee: [],
  bento: ['hero', 'duoBanners'],
  rails: ['carousel'],
  masonry: ['benefits', 'duoBanners'],
};
```

4) `DEFAULT_CONTENT` 를 export 한다 — 백엔드 `DEFAULT_CONTENT`(Plan 61-01 Task 1) 와 **문자 단위로 동일한 값**(sections 7개: hero/benefits/carousel/duoBanners/newsletter/reels/quiz, announce 기본 문구, productCard 6키, catalog pageSize 24, macroSettings rails 2선반 + masonry 4·2·24). 이 상수가 `ThemeContentContext` 의 폴백이 된다.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type Macrostructure = 'marquee' | 'bento' | 'rails' | 'masonry'" tienda-app/src/types/shop.ts` == 1
    - `grep -c "priceOrig: number | null" tienda-app/src/types/shop.ts` == 1
    - `grep -c "export interface StoreThemeContent" tienda-app/src/types/shop.ts` == 1
    - `grep -c "content: StoreThemeContent" tienda-app/src/types/shop.ts` == 1
    - `grep -c "id: 'rails'" tienda-app/src/lib/theme-preset.ts` == 1
    - `grep -c "id: 'masonry'" tienda-app/src/lib/theme-preset.ts` == 1
    - `grep -c -- "'--on-navy': '#ffffff'" tienda-app/src/lib/theme-preset.ts` == 1
    - `grep -c "export const DEFAULT_CONTENT" tienda-app/src/lib/theme-preset.ts` == 1
    - `grep -c "export const GATED_BY_MACRO" tienda-app/src/lib/theme-preset.ts` == 1
    - `grep -c "type: 'quiz'" tienda-app/src/lib/theme-preset.ts` == 1 (sections 7개 미러 확인)
    - `grep -c "discountBadge" tienda-app/src/lib/theme-preset.ts` == 1
  </acceptance_criteria>
  <done>프런트 타입/미러가 백엔드 계약과 필드 단위로 일치하고 MACRO_OPTIONS 가 4종이다.</done>
</task>

<task type="auto">
  <name>Task 2: shop-api.ts — content 저장 · sort/showOutOfStock 조회 · uploadThemeAsset</name>
  <files>tienda-app/src/services/shop-api.ts</files>
  <read_first>
    - tienda-app/src/services/shop-api.ts (전체 — req()/authHeaders()/listProducts()/SaveThemeBody/tryOnFromProduct 의 FormData analog)
    - api-ventago/src/app/shop-public/store-theme-asset.controller.ts (Plan 61-02 산출물 — multipart 필드명 'file', 쿼리 'kind')
    - api-ventago/src/app/shop-public/shop-catalog.controller.ts (Plan 61-03 산출물 — 쿼리 파라미터명)
  </read_first>
  <action>
1) `listProducts()` 파라미터 확장 — 기존 호출부(`{ pageSize: 24 }` 등)를 깨지 않도록 전부 optional:
```typescript
export function listProducts(
  storeId: number,
  params: {
    q?: string;
    globalCategoryId?: number;
    categoryId?: number;
    page?: number;
    pageSize?: number;
    sort?: CatalogSort;
    showOutOfStock?: boolean;
    minPrice?: number;
    maxPrice?: number;
  } = {},
): Promise<ShopListResult> {
```
   - `if (params.categoryId) qs.set('categoryId', String(params.categoryId));`
   - `if (params.sort) qs.set('sort', params.sort);`
   - `if (params.showOutOfStock === false) qs.set('showOutOfStock', 'false');` (true 는 기본값이라 안 보냄 — 캐시 키 파편화 방지)
   - `if (params.minPrice != null) qs.set('minPrice', String(params.minPrice));`
   - `if (params.maxPrice != null) qs.set('maxPrice', String(params.maxPrice));`

2) `SaveThemeBody` 에 `content: StoreThemeContent;` 추가. `saveThemeDraft()` 본문은 그대로(JSON.stringify(body)).

3) 신규 `uploadThemeAsset()` — analog 은 기존 `tryOnFromProduct()` 의 FormData 패턴:
```typescript
export type ThemeAssetKind =
  | 'logo' | 'favicon' | 'hero' | 'banner' | 'payment' | 'shipping'
  | 'reelVideo' | 'reelPoster';

// 테마 에셋 업로드 (편집 토큰 필요). 응답의 fileName 만 JSONB 에 저장한다.
export function uploadThemeAsset(
  storeId: number,
  token: string,
  file: File,
  kind: ThemeAssetKind,
): Promise<{ fileName: string }> {
  const fd = new FormData();
  fd.append('file', file);

  return req<{ fileName: string }>(
    `/shop/${storeId}/theme/asset?kind=${encodeURIComponent(kind)}`,
    { method: 'POST', headers: authHeaders(token), body: fd },
  );
}
```
   **주의**: `Content-Type` 헤더를 직접 설정하지 않는다 — FormData 는 브라우저가 boundary 를 포함해 자동 설정해야 한다(수동 설정 시 multipart 파싱 실패).

4) `minioImageUrl()` 은 그대로 재사용한다(영상 파일명도 동일 MinIO 경로). 별도 `minioVideoUrl` 을 만들지 않는다 — SSOT 유지.

5) `getThemeDraft()`/`getStoreTheme()` 반환 타입은 이미 `StoreTheme` 이므로 Task 1 의 `content` 필드 추가로 자동 반영된다. 단 **백엔드가 content 를 안 내려주는 구버전 API 와 마주쳤을 때 크래시하지 않도록**, 두 함수 뒤에 정규화 래퍼를 둔다:
```typescript
function withContentFallback(t: StoreTheme): StoreTheme {
  return { ...t, content: t.content ?? DEFAULT_CONTENT };
}
```
   `getStoreTheme`/`getThemeDraft`/`saveThemeDraft` 반환에 `.then(withContentFallback)` 적용.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit 2>&1 | head -30 && npx eslint src/services/shop-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function uploadThemeAsset" tienda-app/src/services/shop-api.ts` == 1
    - `grep -c "theme/asset?kind=" tienda-app/src/services/shop-api.ts` == 1
    - `grep -c "'Content-Type'" tienda-app/src/services/shop-api.ts` == 1 (saveThemeDraft 의 JSON 용 1건만. uploadThemeAsset 에는 없어야 함)
    - `grep -c "qs.set('sort'" tienda-app/src/services/shop-api.ts` == 1
    - `grep -c "showOutOfStock" tienda-app/src/services/shop-api.ts` >= 2
    - `grep -c "minPrice" tienda-app/src/services/shop-api.ts` >= 1
    - `grep -c "withContentFallback" tienda-app/src/services/shop-api.ts` >= 4
    - `grep -c "minioVideoUrl" tienda-app/src/services/shop-api.ts` == 0
    - `cd tienda-app && npx eslint src/services/shop-api.ts` exit 0
  </acceptance_criteria>
  <done>API 클라이언트가 content 저장/조회, 정렬 파라미터, 에셋 업로드를 모두 지원하고 구버전 응답에도 안전하다.</done>
</task>

<task type="auto">
  <name>Task 3: ThemeContentContext.tsx 신규 + index.tsx/diseno.tsx 의 doc 분기 제거</name>
  <files>tienda-app/src/context/ThemeContentContext.tsx, tienda-app/src/pages/[storeId]/index.tsx, tienda-app/src/pages/[storeId]/panel/diseno.tsx</files>
  <read_first>
    - tienda-app/src/context/ShopContext.tsx (기존 컨텍스트 관례 — Provider/useXxx 훅 형태를 그대로 따른다)
    - tienda-app/src/pages/[storeId]/index.tsx 126~136행 (gridStyle 의 doc 삼항 분기)
    - tienda-app/src/pages/[storeId]/panel/diseno.tsx 140~149행 (grid 의 doc 삼항 분기)
    - .planning/phases/61-tienda-online-editor/61-SPEC.md R9 「doc 렌더 경로 제거」
  </read_first>
  <action>
**A. `tienda-app/src/context/ThemeContentContext.tsx` 신규**

```tsx
import { createContext, useContext, useMemo } from 'react';
import type { ReactNode } from 'react';
import { DEFAULT_CONTENT } from '@/lib/theme-preset';
import type { StoreThemeContent } from '@/types/shop';

// 테마 content 를 트리 전체에 내려주는 단일 진입점.
// Provider 밖에서 호출돼도 DEFAULT_CONTENT 를 반환해 절대 크래시하지 않는다(공개몰은 안 깨지는 게 최우선).
const ThemeContentContext = createContext<StoreThemeContent>(DEFAULT_CONTENT);

export function ThemeContentProvider({
  content,
  children,
}: {
  content: StoreThemeContent | null | undefined;
  children: ReactNode;
}) {
  const value = useMemo(() => content ?? DEFAULT_CONTENT, [content]);

  return (
    <ThemeContentContext.Provider value={value}>
      {children}
    </ThemeContentContext.Provider>
  );
}

export function useThemeContent(): StoreThemeContent {
  return useContext(ThemeContentContext);
}
```
`value` 는 반드시 `useMemo` 로 감싼다(프로젝트 성능 규약: Context value 메모이제이션).

**B. `index.tsx` — doc 분기 제거 (렌더 결과 무회귀)**

`gridStyle` 을 아래로 교체한다. `doc` 분기만 삭제하고 marquee/bento 동작은 **완전히 동일**하게 남긴다:
```typescript
  // macrostructure(레이아웃)별 상품 그리드 밀도 — rails/masonry 는 별도 레이아웃 컴포넌트가 담당(Plan 61-07/61-10)
  const gridStyle: CSSProperties = {
    display: 'grid',
    gap: 18,
    gridTemplateColumns:
      theme.macrostructure === 'bento'
        ? 'repeat(auto-fill, minmax(240px, 1fr))'
        : 'repeat(auto-fill, minmax(200px, 1fr))',
  };
```
`defaultTheme()` 폴백 객체에 `content: DEFAULT_CONTENT` 를 추가한다(TS 필수 필드).
그 외 렌더 구조는 **이 플랜에서 건드리지 않는다** — sections 순회는 Plan 61-10 의 범위.

**C. `diseno.tsx` — doc 분기 제거**

`grid` 상수를 동일 방식으로 교체:
```typescript
  const grid: CSSProperties = {
    display: 'grid',
    gap: 18,
    gridTemplateColumns:
      macro === 'bento'
        ? 'repeat(auto-fill, minmax(200px, 1fr))'
        : 'repeat(auto-fill, minmax(180px, 1fr))',
  };
```
`DUMMY` 상품 3개에 `priceOrig: null, stock: null` 을 추가한다(Task 1 의 타입 확장으로 필수 필드가 됨).
아코디언 전환/구조 선택 UI 는 **이 플랜의 범위가 아니다**(Plan 61-08/61-11).
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/context/ThemeContentContext.tsx "src/pages/[storeId]/index.tsx" "src/pages/[storeId]/panel/diseno.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'doc'" tienda-app/src/` 결과 0건
    - `grep -c "export function useThemeContent" tienda-app/src/context/ThemeContentContext.tsx` == 1
    - `grep -c "useMemo" tienda-app/src/context/ThemeContentContext.tsx` >= 1
    - `grep -c "content: DEFAULT_CONTENT" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "priceOrig: null" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `cd tienda-app && npx tsc --noEmit` exit 0
    - `cd tienda-app && npx eslint src/` exit 0
    - `git diff "tienda-app/src/pages/[storeId]/index.tsx" | grep -c "^-.*minmax(240px"` == 1 이고 `grep -c "^+.*minmax(240px"` == 1 (bento 밀도 값 보존)
  </acceptance_criteria>
  <done>tienda-app 전체에서 'doc' 이 사라지고 TypeScript 가 통과하며, marquee/bento 그리드 값은 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 응답 → 프런트 타입 캐스팅 | `req<T>()` 는 런타임 검증 없이 `res.json() as T` — 필드 누락 시 `undefined` 접근 크래시 가능 |
| 브라우저 File 객체 → `POST /theme/asset` | 클라이언트 검증은 UX 보조일 뿐, 신뢰 경계는 서버(Plan 61-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-25 | DoS | 구버전/오류 응답에 `content` 누락 → 공개몰 전체 크래시 | mitigate | `withContentFallback()` + `useThemeContent()` 의 `DEFAULT_CONTENT` 폴백 이중 방어. 공개몰은 어떤 경우에도 렌더되어야 함 |
| T-61-26 | Tampering | 프런트에서 파일 확장자만 걸러 안심 | accept | 프런트 검증은 넣지 않거나 UX 용으로만. 실제 차단은 서버(T-61-08/09)에서 이미 강제 |
| T-61-27 | Information Disclosure | 편집 토큰이 URL 쿼리(`?t=`)로 노출 | accept | 기존 매직링크 설계 그대로(15분 TTL HMAC). 이 Phase 에서 변경하지 않음 |
| T-61-28 | Tampering | 프런트/백엔드 sanitize 값 불일치로 미리보기≠발행 | mitigate | `theme-preset.ts` 상단 미러 주석 + `DEFAULT_CONTENT` 값 동일성을 acceptance 로 고정. 최종 대조는 Plan 61-17 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "'doc'" tienda-app/src/ api-ventago/src/app/shop-public/ | grep -v ".spec.ts"   # 0건 (R9 acceptance)
grep -rn "new Pool(\|new Client(" tienda-app/src/                                        # 0건
git status --porcelain api-ventago/migrations/ | wc -l                                    # 1 (61-04 파일만)
```
</verification>

<success_criteria>
- `npx tsc --noEmit` 통과 (doc 제거로 인한 TS2367 없음)
- `grep -rn "'doc'" tienda-app/src` 0건
- MACRO_OPTIONS 4종 + `--on-navy` 미러
- `uploadThemeAsset` 존재, FormData 에 Content-Type 수동 지정 없음
- `useThemeContent()` 가 Provider 밖에서도 DEFAULT_CONTENT 반환
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-05-SUMMARY.md` 생성
</output>
