---
phase: 61-tienda-online-editor
plan: 09
type: execute
wave: 5
depends_on: ["61-06", "61-07"]
files_modified:
  - tienda-app/src/components/Header.tsx
  - tienda-app/src/components/Footer.tsx
  - tienda-app/src/components/WhatsAppFloat.tsx
  - tienda-app/src/pages/[storeId]/index.tsx
autonomous: true
requirements: [R4, R9, R8]
must_haves:
  truths:
    - "공개 홈이 content.sections 배열 순서대로 섹션을 렌더한다"
    - "확장 키가 없는 기존 매장의 공개 페이지가 현행과 시각적으로 동일하게 렌더된다"
    - "macrostructure=rails 면 RailsLayout 이, masonry 면 MasonryLayout 이 상품 영역을 대체한다"
    - "브랜드 로고가 있으면 헤더 워드마크 대신 로고 이미지가 나온다"
    - "contact.whatsapp 이 있으면 WhatsApp 플로팅 버튼이, 없으면 렌더되지 않는다"
    - "구조에 맞지 않는 섹션은 렌더되지 않지만 JSONB 값은 그대로 보존된다"
  artifacts:
    - path: "tienda-app/src/pages/[storeId]/index.tsx"
      provides: "sections 순회 렌더 + macrostructure 최상위 분기 + 파비콘 주입"
      contains: "SectionRenderer"
    - path: "tienda-app/src/components/Footer.tsx"
      provides: "SNS/정책/결제·배송 로고/compra protegida 푸터"
    - path: "tienda-app/src/components/WhatsAppFloat.tsx"
      provides: "WhatsApp 플로팅 버튼 (contact.whatsapp 있을 때만)"
      contains: "aria-label=\"Contactar por WhatsApp\""
  key_links:
    - from: "index.tsx"
      to: "components/sections/SectionRenderer.tsx"
      via: "content.sections 순회 렌더"
      pattern: "SectionRenderer"
    - from: "index.tsx"
      to: "components/macro/RailsLayout.tsx / MasonryLayout.tsx"
      via: "macrostructure 최상위 분기"
      pattern: "macrostructure === 'rails'"
    - from: "Header.tsx"
      to: "content.announce / content.brand"
      via: "useThemeContent()"
      pattern: "useThemeContent"
---

<objective>
공개 스토어프런트를 하드코딩 레이아웃에서 **데이터 기반 조립**으로 전환한다: `content.sections` 순회 렌더, macrostructure 최상위 분기(rails/masonry), 공지바·헤더 로고·파비콘·WhatsApp 플로팅 버튼·푸터 신설. 확장 키가 없는 기존 매장은 `DEFAULT_CONTENT` 덕분에 **현행과 동일하게** 렌더돼야 한다(무회귀가 완료 게이트).

Purpose: R4 의 핵심. 이 플랜이 끝나야 에디터 편집 결과가 실제 공개 페이지에 나타난다.
Output: 재조립된 `index.tsx` + 신규 Footer/WhatsAppFloat + 확장된 Header.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-SPEC.md

<interfaces>
```typescript
// Plan 61-06
export default function SectionRenderer({ storeId, section, initialItems }: { storeId: number; section: SectionConfig; initialItems?: ShopProduct[] })
// Plan 61-07
export default function RailsLayout({ storeId, heroSection }: { storeId: number; heroSection?: HeroSection })
export default function MasonryLayout({ storeId, initialItems, categories, activeCat, onCat }: {...})
// Plan 61-05
export function useThemeContent(): StoreThemeContent
export function ThemeContentProvider({ content, children }): JSX.Element
export function minioImageUrl(fileName: string): string
```

**Header.tsx 현행 (확장 대상)**
```tsx
<header style={s.header}>
  <div style={s.announce}>
    Envío gratis desde $50.000 · <b style={{ color:'var(--gold)' }}>3 cuotas sin interés</b> · probador virtual con IA
  </div>
  <div className="container" style={s.hdr}>
    <div style={s.logo}>Cool<span style={{ color:'var(--gold)' }}>Shop</span></div>
    <div style={s.search}>...</div>
    <button style={s.cart} onClick={openDrawer}>Carrito<b style={s.cartBadge}>{count}</b></button>
  </div>
  <nav style={s.navWrap}>...</nav>
</header>
```

**index.tsx 현행 렌더 순서**: `<Head>` → `<Header>` → `<main class="container">` → hero(하드코딩) + promo + secHead + 상품 그리드 + aistrip
</interfaces>

**UI-SPEC 표면 B 계약**
- 색은 CSS 변수만. `var(--navy)` 위 텍스트는 `var(--on-navy)`. 예외 하드코딩 2건만 허용: WhatsApp `#25d366`, cuotas 는 `var(--green)` 경유.
- 구조별 섹션 게이팅(LOCKED): `bento` → `hero`·`duoBanners` 비활성 / `rails` → `carousel` 비활성 / `masonry` → `benefits`·`duoBanners` 비활성 / `marquee` → 전부 가능. **비활성 = 렌더 생략일 뿐 JSONB 값은 건드리지 않는다.**
- 푸터: 배경 `var(--navy)`, 텍스트 밝은 톤(다크 배경 전용 `rgba(255,255,255,0.75)` 허용), 4열 그리드 → 모바일 2열, 로고 chip `background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.13); borderRadius:5; padding:'4px 8px'`, label 12px.
- WhatsApp 플로팅: `bottom:16px; right:16px`, 48×48px 원형, `#25d366`, `aria-label="Contactar por WhatsApp"`, z-index 는 콘텐츠보다 위·팝업 모달보다 아래.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header.tsx — 공지바 + 브랜드 로고 데이터 연결</name>
  <files>tienda-app/src/components/Header.tsx</files>
  <read_first>
    - tienda-app/src/components/Header.tsx (전체 — 특히 18~26행 하드코딩 공지바/워드마크, 65행 이후 스타일 오브젝트)
    - tienda-app/src/context/ThemeContentContext.tsx (Plan 61-05)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 B — 공지바 / 헤더 로고 / 푸터 / WhatsApp"
  </read_first>
  <action>
1) `const { announce, brand } = useThemeContent();` 추가. props 시그니처는 **변경하지 않는다**(기존 호출부 2곳: index.tsx, diseno.tsx 미리보기).

2) **공지바** — 하드코딩 블록을 완전 대체:
```tsx
{announce.enabled && announce.text ? (
  <div style={s.announce}>
    {announce.href ? (
      <a href={announce.href} style={{ color: 'inherit', textDecoration: 'none' }}>{announce.text}</a>
    ) : (
      announce.text
    )}
  </div>
) : null}
```
`enabled=false` 또는 빈 텍스트면 **JSX 자체를 렌더하지 않는다**(`display:none` 금지 — 빈 공간이 남으면 안 됨).
`s.announce` 스타일의 텍스트 색을 `var(--on-navy)` 로 지정한다(현재 값이 `#fff` 계열이면 교체).

3) **브랜드 로고**:
```tsx
<div style={s.logo}>
  {brand.logoFile ? (
    // eslint-disable-next-line @next/next/no-img-element
    <img src={minioImageUrl(brand.logoFile)} alt={brand.displayName || 'Logo'} style={s.logoImg} />
  ) : (
    brand.displayName || <>Cool<span style={{ color: 'var(--gold)' }}>Shop</span></>
  )}
</div>
```
`s.logoImg = { height: 32, objectFit: 'contain', display: 'block' }`.
`displayName` 이 비어 있고 로고도 없으면 **기존 `CoolShop` 워드마크를 그대로 유지** — 이것이 무회귀 경로다. 이 근거를 한국어 주석으로 남긴다.

4) 검색/카트/카테고리 nav 는 **변경하지 않는다**.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useThemeContent" tienda-app/src/components/Header.tsx` == 2
    - `grep -c "announce.enabled && announce.text" tienda-app/src/components/Header.tsx` == 1
    - `grep -c "brand.logoFile" tienda-app/src/components/Header.tsx` == 1
    - `grep -c "Envío gratis desde" tienda-app/src/components/Header.tsx` == 0 (하드코딩 공지 문구 제거 — DEFAULT_CONTENT 로 이동)
    - `grep -c "var(--on-navy)" tienda-app/src/components/Header.tsx` >= 1
    - `grep -c "CoolShop\|Cool<span" tienda-app/src/components/Header.tsx` >= 1 (무회귀 폴백 유지)
    - `cd tienda-app && npx eslint src/components/Header.tsx` exit 0
  </acceptance_criteria>
  <done>공지바/로고가 content 를 따르고, 데이터가 없으면 기존 모습 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: Footer.tsx + WhatsAppFloat.tsx 신규</name>
  <files>tienda-app/src/components/Footer.tsx, tienda-app/src/components/WhatsAppFloat.tsx</files>
  <read_first>
    - tienda-app/src/components/Header.tsx (스타일 오브젝트 관례 + `.container` 클래스 사용법)
    - tienda-app/src/styles/globals.css (`.container` 정의)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 B — 공지바 / 헤더 로고 / 푸터 / WhatsApp" (푸터 4열/chip 스타일, WhatsApp 48×48)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § Color 「목업 색상 → CSS 변수 매핑」 표 (`#17161c` → `var(--navy)`)
  </read_first>
  <action>
**A. `WhatsAppFloat.tsx`**
```tsx
export default function WhatsAppFloat() {
  const { contact } = useThemeContent();
  if (!contact.whatsapp) return null;

  const digits = contact.whatsapp.replace(/[^0-9]/g, '');
  if (!digits) return null;

  return (
    <a
      href={`https://wa.me/${digits}`}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Contactar por WhatsApp"
      style={s.fab}
    >
      <span aria-hidden="true">💬</span>
    </a>
  );
}
```
`s.fab = { position:'fixed', bottom:16, right:16, width:48, height:48, borderRadius:'50%', background:'#25d366', color:'#ffffff', display:'flex', alignItems:'center', justifyContent:'center', fontSize:24, textDecoration:'none', boxShadow:'0 4px 12px rgba(0,0,0,0.25)', zIndex:30 }`
`#25d366` 은 **UI-SPEC 이 명시적으로 허용한 2개 하드코딩 예외 중 하나**(WhatsApp 브랜드 고정색) — 이 근거를 한국어 주석으로 남긴다. `z-index:30` 은 Header 의 `zIndex:20` 보다 위, 마케팅 팝업(Plan 61-15 에서 `zIndex:50`)보다 아래.

**B. `Footer.tsx`**
```tsx
export default function Footer({ storeName }: { storeName?: string })
```
`const { contact, trust, brand } = useThemeContent();`

렌더 조건: `contact`/`trust` 가 전부 비어 있으면(`whatsapp/instagram/facebook/footerText` 전부 null **그리고** `paymentLogos/shippingLogos/policyLinks` 전부 빈 배열 **그리고** `protectedBadge === false`) → **`return null`**. 기존 매장 무회귀(현재 `<footer>` 자체가 없음)를 위해 필수.

렌더 구조(전체 `background:'var(--navy)'`, `color:'rgba(255,255,255,0.75)'`, `padding:'32px 0 24px'`, `marginTop:40`):
- `.container` 안에 4열 그리드(`display:'grid'`, `gridTemplateColumns:'repeat(auto-fit, minmax(180px, 1fr))'`, `gap:24`):
  1. **브랜드 열**: `brand.displayName || storeName` (heading 20px, `color:'var(--on-navy)'`) + `contact.footerText`(body 13px)
  2. **Redes 열**: `Instagram` / `Facebook` 링크 — `contact.instagram`/`contact.facebook` 이 있을 때만. 값이 `http` 로 시작하면 그대로, 아니면 `https://instagram.com/{handle}` / `https://facebook.com/{handle}` 로 조립. `target="_blank" rel="noopener noreferrer"`.
  3. **Políticas 열**: `trust.policyLinks.map(l => <a href={l.href}>{l.label}</a>)` — 배열이 비면 열 자체 생략
  4. **Pagos y envíos 열**: `trust.paymentLogos` + `trust.shippingLogos` 를 chip 으로. 각 chip 은 `<img src={minioImageUrl(f)} alt="" style={{ height:16 }} />` 를 감싼 `{ background:'rgba(255,255,255,0.08)', border:'1px solid rgba(255,255,255,0.13)', borderRadius:5, padding:'4px 8px', display:'inline-flex' }`. 배열이 비면 열 생략
- `trust.protectedBadge === true` 이면 상단 구분선(`borderTop:'1px solid rgba(255,255,255,0.1)'`, `marginTop:24`, `paddingTop:16`) 아래에 `🛡 Compra protegida` 배지 행(label 12px).

이모지는 `aria-hidden="true"`.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/Footer.tsx src/components/WhatsAppFloat.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-label="Contactar por WhatsApp"' tienda-app/src/components/WhatsAppFloat.tsx` == 1
    - `grep -c "#25d366" tienda-app/src/components/WhatsAppFloat.tsx` == 1
    - `grep -c "rel=\"noopener noreferrer\"" tienda-app/src/components/WhatsAppFloat.tsx` == 1
    - `grep -c "return null" tienda-app/src/components/Footer.tsx` >= 1
    - `grep -c "var(--navy)" tienda-app/src/components/Footer.tsx` == 1
    - `grep -c "Compra protegida" tienda-app/src/components/Footer.tsx` == 1
    - `grep -c "rel=\"noopener noreferrer\"" tienda-app/src/components/Footer.tsx` >= 1
    - `grep -c "#17161c" tienda-app/src/components/Footer.tsx` == 0 (목업 리터럴 미사용)
    - `cd tienda-app && npx eslint src/components/` exit 0
  </acceptance_criteria>
  <done>두 컴포넌트가 데이터 없을 때 null 을 반환하고, 외부 링크에 rel 보호가 붙는다.</done>
</task>

<task type="auto">
  <name>Task 3: index.tsx — sections 순회 + macrostructure 분기 + 게이팅 + 파비콘</name>
  <files>tienda-app/src/pages/[storeId]/index.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/index.tsx (전체 — Plan 61-05 이 doc 분기 제거 + content 폴백을 넣은 상태)
    - tienda-app/src/components/sections/SectionRenderer.tsx (Plan 61-06)
    - tienda-app/src/components/macro/RailsLayout.tsx / MasonryLayout.tsx (Plan 61-07)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 A — 구조별 섹션 게이팅" (게이팅 규칙표 — LOCKED)
    - tienda-online-rails-masonry-reels-mockup.html (레포 루트 — rails/masonry 최종 렌더 확인)
  </read_first>
  <action>
1) **Provider 래핑** — 최상위 `<div style={wrapStyle} data-macro={theme.macrostructure}>` 를 `<ThemeContentProvider content={theme.content}>` 로 감싼다.

2) **파비콘** — `<Head>` 안에:
```tsx
{theme.content.brand.faviconFile ? (
  <link rel="icon" href={minioImageUrl(theme.content.brand.faviconFile)} />
) : null}
```
`<title>`/`<meta description>` 은 **이 플랜에서 건드리지 않는다**(SEO 주입은 Plan 61-15 소유).

3) **구조별 섹션 게이팅** — 렌더 직전 필터. JSONB 는 절대 수정하지 않는다. **`GATED_BY_MACRO` 는 `@/lib/theme-preset` 에서 import 한다**(Plan 61-05 가 export 하는 단일 소유지 — 로컬 재정의 금지, 에디터 안내(Plan 61-10)와 규칙이 갈라지지 않게):
```typescript
import { GATED_BY_MACRO } from '@/lib/theme-preset';
// 구조에 맞지 않는 섹션은 렌더만 생략한다(값은 JSONB 에 그대로 보존 — 구조를 되돌리면 복귀).
const gated = GATED_BY_MACRO[theme.macrostructure] ?? [];
const visibleSections = theme.content.sections.filter((s) => !gated.includes(s.type));
```

4) **`<main>` 재조립** — 기존 하드코딩 hero/promo/secHead/그리드/aistrip 블록을 **전부 삭제**하고:
```tsx
<main className="container" style={{ paddingBottom: 40 }}>
  {error ? <p style={s.muted}>Error: {error}</p> : null}

  {theme.macrostructure === 'rails' ? (
    <>
      <RailsLayout storeId={storeId} heroSection={heroSection} />
      {visibleSections
        .filter((sec) => sec.type !== 'hero' && sec.type !== 'carousel')
        .map((sec, i) => (
          <SectionRenderer key={`${sec.type}-${i}`} storeId={storeId} section={sec} />
        ))}
    </>
  ) : theme.macrostructure === 'masonry' ? (
    <>
      {visibleSections
        .filter((sec) => sec.type !== 'carousel')
        .map((sec, i) => (
          <SectionRenderer key={`${sec.type}-${i}`} storeId={storeId} section={sec} />
        ))}
      <MasonryLayout
        storeId={storeId}
        initialItems={items}
        categories={categories}
        activeCat={activeCat}
        onCat={setActiveCat}
      />
    </>
  ) : (
    visibleSections.map((sec, i) => (
      <SectionRenderer
        key={`${sec.type}-${i}`}
        storeId={storeId}
        section={sec}
        initialItems={sec.type === 'carousel' ? items : undefined}
      />
    ))
  )}
</main>
<Footer storeName={theme.content.brand.displayName} />
<WhatsAppFloat />
```
`heroSection` = `visibleSections.find(s => s.type === 'hero') as HeroSection | undefined`.

**설계 근거(한국어 주석으로 남길 것)**: rails/masonry 는 그리드 밀도가 아니라 **렌더 구조 자체**가 다르므로 `gridStyle` 삼항 확장이 아니라 최상위 분기여야 한다. rails 는 hero 를 콤팩트 밴드로 흡수하고 carousel 을 선반이 대체하며, masonry 는 상품 영역 전체를 대체한다.

5) **기존 검색/필터 useEffect 유지** — `items` state 와 `listProducts` 재조회 로직은 그대로 두되, `pageSize: 50` 을 `theme.content.catalog.pageSize` 로 바꾸는 것은 **Plan 61-12 의 몫**이다. 이 플랜에서는 건드리지 않는다.

6) **`getServerSideProps`** — 변경 없음(`getStoreTheme` 이 이미 content 를 포함).

7) 삭제된 스타일 오브젝트 키(`hero`/`heroBig`/`heroTitle`/`heroP`/`promo`/`promoTitle`/`promoP`/`tag`/`aistrip`/`aiTitle`/`aiP`)를 `s` 상수에서 제거한다(`no-unused-vars` 방지). `secHead`/`secTitle`/`muted` 는 남긴다.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SectionRenderer" "tienda-app/src/pages/[storeId]/index.tsx"` >= 4
    - `grep -c "GATED_BY_MACRO" "tienda-app/src/pages/[storeId]/index.tsx"` == 2 (import 1 + 사용 1, 로컬 `const GATED_BY_MACRO` 정의는 0건)
    - `grep -c "macrostructure === 'rails'" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "macrostructure === 'masonry'" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "ThemeContentProvider" "tienda-app/src/pages/[storeId]/index.tsx"` == 2
    - `grep -c "<WhatsAppFloat />" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "<Footer" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "rel=\"icon\"" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "Nueva temporada otoño 2026\|Probador virtual con IA 👗" "tienda-app/src/pages/[storeId]/index.tsx"` == 0 (하드코딩 카피 제거 — DEFAULT_CONTENT 로 이동)
    - `grep -c "sections.*splice\|sections\[.*\] =" "tienda-app/src/pages/[storeId]/index.tsx"` == 0 (게이팅이 JSONB 를 mutate 하지 않음)
    - `cd tienda-app && npx tsc --noEmit` exit 0 · `npx eslint src/` exit 0
  </acceptance_criteria>
  <done>공개 홈이 sections 배열과 macrostructure 를 따라 조립되고, 게이팅은 렌더만 생략한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `published_tokens.content` → 익명 방문자 브라우저 | admin 입력 텍스트/URL/파일명이 그대로 렌더된다 |
| `contact.instagram/facebook` → 외부 링크 조립 | 값이 handle 일 수도, 전체 URL 일 수도 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-44 | Tampering | `announce.href` / `policyLinks[].href` → `<a href>` | mitigate | 백엔드 `sanitizeHref()` 가 `http(s)`/`/` 만 통과시킴. 프런트는 null 이면 `<a>` 자체를 만들지 않음 |
| T-61-45 | Tampering | `contact.instagram/facebook` 로 `javascript:` 주입 | mitigate | 값이 `http` 로 시작하지 **않으면** `https://instagram.com/{value}` 로 조립하므로 스킴 주입 불가. `http` 로 시작하면 그대로 쓰되 백엔드 `clampText` 100자 + 프런트에서 `https?://` 만 허용하도록 재확인 |
| T-61-46 | Tampering | 외부 링크 `target="_blank"` reverse tabnabbing | mitigate | 모든 외부 링크에 `rel="noopener noreferrer"` — acceptance grep 고정 |
| T-61-47 | Tampering | 파비콘/로고 파일명 경로 조작 | mitigate | `minioImageUrl()` 의 `encodeURIComponent` + 백엔드 `sanitizeFileName()` |
| T-61-48 | Tampering | 게이팅이 JSONB 를 파괴적으로 수정 | mitigate | 필터는 순수 `Array.filter` 로 새 배열만 생성. `splice`/인덱스 대입 0건 acceptance |
| T-61-49 | DoS | 섹션마다 카탈로그 조회 | mitigate | `carousel` 은 SSR `items` 재사용, rails 는 lazy load, masonry 는 페이지네이션 — 초기 요청 1회 유지 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "dangerouslySetInnerHTML" tienda-app/src/                    # 0건
grep -rEn "#[0-9a-fA-F]{3,6}" tienda-app/src/components/sections/ tienda-app/src/components/macro/   # 0건
grep -rn "#25d366" tienda-app/src/ | wc -l                            # 1 (WhatsAppFloat 만)
# 무회귀 스모크: 확장 키 없는 운영 매장 공개 HTML 을 변경 전/후 비교
SHOP_HOST=http://localhost:3060 STORE_ID=9 ./scripts/smoke-shop-theme.sh
```
</verification>

<success_criteria>
- 확장 키 없는 매장의 공개 페이지가 현행과 동일 렌더(hero 문구/Destacados/AI strip 이 DEFAULT_CONTENT 로 재현)
- `macrostructure=rails|masonry` 로 publish 시 해당 레이아웃으로 렌더, `marquee`/`bento` 회귀 0
- 로고/파비콘/공지바/WhatsApp/푸터가 content 를 따르고 데이터 없으면 미렌더
- 게이팅이 JSONB 를 수정하지 않음
- `npx tsc --noEmit` + `npx eslint src/` 통과
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-09-SUMMARY.md` 생성
</output>
