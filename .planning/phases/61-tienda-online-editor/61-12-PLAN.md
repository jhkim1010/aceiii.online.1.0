---
phase: 61-tienda-online-editor
plan: 12
type: execute
wave: 6
depends_on: ["61-09", "61-10"]
files_modified:
  - tienda-app/src/components/ProductCard.tsx
  - tienda-app/src/styles/globals.css
  - tienda-app/src/pages/[storeId]/index.tsx
  - tienda-app/src/pages/[storeId]/panel/diseno.tsx
autonomous: true
requirements: [R5, R6, R8]
must_haves:
  truths:
    - "productCard.discountBadge=false 로 publish 하면 공개 카드에서 할인 배지가 사라진다"
    - "hoverSecondImage=false 면 hover 시 두 번째 이미지로 바뀌지 않는다"
    - "lastUnitsBadge=true 이고 stock 이 1~2 인 상품에 ÚLTIMAS UNIDADES 배지가 뜬다"
    - "variantDots 는 토글해도 공개 뷰에 아무 점도 렌더하지 않는다(정직한 no-op)"
    - "catalog.pageSize=12·defaultSort=price_asc·filters.color=false 로 publish 하면 목록이 12개씩 가격 오름차순이고 색상 필터가 숨겨진다"
  artifacts:
    - path: "tienda-app/src/components/ProductCard.tsx"
      provides: "6개 옵션 조건부 렌더 (options prop)"
      contains: "ProductCardOptions"
    - path: "tienda-app/src/pages/[storeId]/index.tsx"
      provides: "catalog 토큰을 기본값으로 사용 + 필터 표시 토글 + pageSize/sort 전달"
      contains: "catalog.pageSize"
    - path: "tienda-app/src/pages/[storeId]/panel/diseno.tsx"
      provides: "🛍 Tarjeta de producto + 📚 Catálogo y filtros 아코디언 그룹"
      contains: "Tarjeta de producto"
  key_links:
    - from: "ProductCard.tsx"
      to: "content.productCard"
      via: "options prop (index/carousel 이 전달)"
      pattern: "options\\?"
    - from: "index.tsx"
      to: "shop-api.ts::listProducts"
      via: "catalog.defaultSort/pageSize/showOutOfStock 전달"
      pattern: "sort: theme.content.catalog"
---

<objective>
`ProductCard.tsx` 에 6개 표시 옵션(discountBadge / installments / quickAdd / hoverSecondImage / lastUnitsBadge / variantDots)을 붙이고, 공개 목록이 `catalog` 토큰(정렬/페이지/필터 표시)을 따르도록 `index.tsx` 를 배선한다. 에디터에 `🛍 Tarjeta de producto` · `📚 Catálogo y filtros` 그룹을 추가한다. `variantDots` 는 데이터 소스가 없으므로 **정직한 no-op**(가짜 점 금지).

Output: 옵션화된 ProductCard + catalog 배선된 index + 에디터 그룹 2개.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-RESEARCH.md

<interfaces>
```typescript
// types/shop.ts (Plan 61-05)
export interface ShopProduct { ...; priceOrig: number | null; stock: number | null }
// content.productCard: { discountBadge, installments, quickAdd, hoverSecondImage, lastUnitsBadge, variantDots: boolean }
// content.catalog: { defaultSort: CatalogSort; pageSize: number; showOutOfStock: boolean; filters: { size; color; price: boolean } }
// shop-api.ts (Plan 61-05): listProducts(storeId, { ..., sort, showOutOfStock, categoryId, pageSize })
// lib/format.ts: money(n), cuotas(n), placeholderGradient(id)
```
</interfaces>

**UI-SPEC 표면 B — ProductCard 6옵션 시각 계약 (4px 스냅)**
| 옵션 | 조건 | 시각 |
|---|---|---|
| discountBadge | `priceOrig != null && priceOrig > price` | 좌상단 pill `background:var(--gold); color:var(--navy)`, 12px, **800**, `4px 8px`, radius 5, 내용 `-{round((1-price/priceOrig)*100)}%` |
| installments | 항상 계산 가능 | OFF 시 `cuotas` 줄 자체 미렌더. ON 시 기존 스타일(`var(--green)`, 600) 유지 |
| quickAdd | 항상 | 32×32 원형, 이미지 우하단 오버레이, `background:var(--gold)`, 아이콘 `＋` 16px `var(--navy)`, `boxShadow:'0 3px 8px rgba(0,0,0,0.2)'`, 클릭 시 `add(product)`, `aria-label="Agregar al carrito"` |
| hoverSecondImage | `imageUrls?.[1]` 존재 시만 | img1/img2 절대위치 스택, `img2{opacity:0}`, 컨테이너 hover 시 `img2{opacity:1}`, `transition:opacity 250ms` |
| lastUnitsBadge | `stock != null && stock > 0 && stock < 3` | 우상단 pill `background:var(--navy); color:var(--gold)`, 12px, **800**, `4px 8px`, 내용 `ÚLTIMAS UNIDADES` |
| variantDots | 데이터 없음 | **no-op** — 아무것도 렌더 안 함. `// TODO(Phase 61 이후): variant 색상 집계 API 필요` |

상품카드 가격은 heading 20px(`var(--disp-weight)`)로 스냅(기존 `fontSize:18` → 20). hover 는 CSS 가 필요하므로 컨테이너에 `.pc-hover2` 클래스를 부여하고 globals.css 규칙을 쓰거나, `onMouseEnter/Leave` state 로 처리(라이브러리 금지). CSS-only 권장 — globals.css 에 1개 규칙 추가 허용.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductCard.tsx — 6개 옵션 조건부 렌더</name>
  <files>tienda-app/src/components/ProductCard.tsx</files>
  <read_first>
    - tienda-app/src/components/ProductCard.tsx (전체 — 현재 하드코딩 구조 + 하단 s 상수)
    - tienda-online-rails-masonry-reels-mockup.html (레포 루트 — 카드 배지/quickAdd/hover2 시각)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 B — ProductCard 6개 옵션 시각 계약"
    - tienda-app/src/lib/format.ts (money/cuotas/placeholderGradient)
  </read_first>
  <action>
1) props 확장:
```tsx
export interface ProductCardOptions {
  discountBadge: boolean; installments: boolean; quickAdd: boolean;
  hoverSecondImage: boolean; lastUnitsBadge: boolean; variantDots: boolean;
}
export default function ProductCard({ product, options }: { product: ShopProduct; options?: ProductCardOptions })
```
`options` 미지정 시 기존 동작을 유지하는 기본값을 함수 안에서 둔다: `const o = options ?? { discountBadge: true, installments: true, quickAdd: false, hoverSecondImage: true, lastUnitsBadge: false, variantDots: false };` (DEFAULT_CONTENT.productCard 와 동일 — 무회귀).

2) **이미지 영역**:
- `o.hoverSecondImage && product.imageUrls?.[1]` 이면 img1(기본) 위에 img2 절대배치. 컨테이너에 `className="pc-hover2"`.
- `o.discountBadge && product.priceOrig != null && product.priceOrig > product.price` → 좌상단 `<span>-{Math.round((1 - product.price / product.priceOrig) * 100)}%</span>` pill
- `o.lastUnitsBadge && product.stock != null && product.stock > 0 && product.stock < 3` → 우상단 `ÚLTIMAS UNIDADES` pill
- `o.quickAdd` → 이미지 우하단 32×32 `＋` 버튼, `onClick={(e) => { e.stopPropagation(); add(product); }}`, `aria-label="Agregar al carrito"`
- 배지 2개가 동시에 뜰 수 있으므로 discount=좌상단, lastUnits=우상단으로 분리(겹침 방지).

3) **바디**:
- 이름(기존)
- 가격 `<div style={s.price}>` — `fontSize:20`(heading 스냅), `fontWeight:'var(--disp-weight)'`(기존 800 하드코딩 → 테마 굵기로 통합, UI-SPEC 확정)
- `o.installments` 이면 `<div style={s.cuotas}>{cuotas(product.price)}</div>`, OFF 면 줄 자체 미렌더
- `o.variantDots` → **아무것도 렌더하지 않는다.** 그 자리에 주석: `{/* TODO(Phase 61 이후): variant 색상 집계 API 필요 — 데이터 없이 가짜 점 금지(no-op) */}`
- 기존 Agregar/Probar 버튼은 유지.

4) globals.css 에 hover2 규칙 1개 추가(인라인으로 `:hover` 불가):
```css
.pc-hover2 .pc-img2 { opacity: 0; transition: opacity 250ms ease; }
.pc-hover2:hover .pc-img2 { opacity: 1; }
```
   → `files_modified` 에 `tienda-app/src/styles/globals.css` 를 추가할 것. img2 에 `className="pc-img2"` 부여. **주의**: Plan 61-07 도 globals.css 를 편집한다. 61-07 은 Wave 4, 이 플랜은 Wave 6 이므로 순서상 안전(append only).

5) 색상은 CSS 변수만. `boxShadow` 의 `rgba(0,0,0,0.2)` 는 그림자라 허용.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/ProductCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ProductCardOptions" tienda-app/src/components/ProductCard.tsx` >= 2
    - `grep -c "priceOrig > product.price" tienda-app/src/components/ProductCard.tsx` == 1
    - `grep -c "ÚLTIMAS UNIDADES" tienda-app/src/components/ProductCard.tsx` == 1
    - `grep -c 'aria-label="Agregar al carrito"' tienda-app/src/components/ProductCard.tsx` == 1
    - `grep -c "TODO(Phase 61 이후): variant 색상 집계" tienda-app/src/components/ProductCard.tsx` == 1
    - `grep -c "pc-hover2" tienda-app/src/components/ProductCard.tsx` >= 1
    - `grep -c "pc-hover2:hover" tienda-app/src/styles/globals.css` == 1
    - `grep -c "fontSize: 20\|fontSize:20" tienda-app/src/components/ProductCard.tsx` >= 1
    - `cd tienda-app && npx eslint src/components/ProductCard.tsx` exit 0
  </acceptance_criteria>
  <done>6개 옵션이 조건부 렌더되고 variantDots 는 no-op + TODO 다.</done>
</task>

<task type="auto">
  <name>Task 2: index.tsx — catalog 토큰 배선 + ProductCard options 전달</name>
  <files>tienda-app/src/pages/[storeId]/index.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/index.tsx (Plan 61-09 결과 — sections 순회 + macro 분기 상태)
    - tienda-app/src/components/Header.tsx (필터 chip nav — filters 토글 대상)
    - tienda-app/src/components/sections/Carousel.tsx (ProductCard 를 렌더하는 곳 — options 전달 경로)
    - .planning/phases/61-tienda-online-editor/61-SPEC.md R6 acceptance
  </read_first>
  <action>
1) **catalog 기본값 사용** — 검색/필터 useEffect 의 `pageSize: 50` 을 `theme.content.catalog.pageSize` 로, 정렬을 `theme.content.catalog.defaultSort` 로, 품절을 `theme.content.catalog.showOutOfStock` 로 교체:
```typescript
const res = await listProducts(storeId, {
  q: q.trim() || undefined,
  globalCategoryId: activeCat ?? undefined,
  pageSize: theme.content.catalog.pageSize,
  sort: theme.content.catalog.defaultSort,
  showOutOfStock: theme.content.catalog.showOutOfStock,
});
```
`getServerSideProps` 의 초기 `listProducts(storeId, { pageSize: 24 })` 도 SSR 에서 theme 을 먼저 조회하므로 `pageSize: theme.content.catalog.pageSize`, `sort: theme.content.catalog.defaultSort`, `showOutOfStock` 를 넘긴다(theme 은 이미 그 위에서 조회됨).

2) **ProductCard options 전달** — 마퀴/벤토 경로의 `SectionRenderer`(carousel)와 masonry/rails 는 내부에서 ProductCard 를 렌더한다. `content.productCard` 를 각 렌더 지점까지 전달해야 한다. 가장 간단한 방법: **`ThemeContentProvider` 가 이미 트리에 content 를 주입하므로, `ProductCard` 가 직접 `useThemeContent()` 로 `productCard` 를 읽게 한다.** 이 편이 prop drilling 을 없앤다.
   → **Task 1 을 조정**: `ProductCard` 가 `options` prop 이 없으면 `useThemeContent().productCard` 를 폴백으로 쓰도록 한다. (`options ?? useThemeContent().productCard`). 단 `useThemeContent` 는 Provider 밖에서도 DEFAULT_CONTENT 를 반환하므로 안전. 이 결정을 Task 1 의 기본값 로직과 통일할 것 — **실행자는 Task 1 에서 `const o = options ?? useThemeContent().productCard;` 로 구현**하고, 에디터 미리보기/rails/masonry/carousel 어디서든 자동으로 옵션이 적용되게 한다.
   따라서 index.tsx 는 ProductCard 에 options 를 명시 전달할 필요가 없다(Provider 경유). 이 근거를 한국어 주석으로 남긴다.

3) **필터 표시 토글** — `Header` 의 카테고리 chip nav 는 유지하되, size/color/price 필터 UI 는 현재 코드에 **없다**(검색 + 카테고리 chip 만 존재). SPEC R6 의 `filters.size/color/price` 토글은 "있는 필터를 숨기는" 것인데 실제 size/color/price 필터 UI 자체가 미구현이다.
   → **정직한 처리**: size/color/price 필터 UI 를 이 Phase 에서 새로 만들지 않는다(공개 카탈로그에 variant/color 데이터가 없어 R5 variantDots 와 동일한 데이터 공백). 대신 `content.catalog.filters` 값은 저장·노출하되 프런트는 **현재 렌더할 필터가 없으므로 no-op** 로 둔다. 이 한계를 한국어 주석으로 명시: `// TODO(Phase 61 이후): size/color/price 필터 UI 는 공개 API 에 variant 집계가 없어 미구현. catalog.filters 토글은 저장되나 현재 렌더 대상 없음(가짜 필터 금지).`
   에디터(Task 3)의 필터 토글 옆에도 이 사실을 hint 로 노출한다.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "theme.content.catalog.pageSize" "tienda-app/src/pages/[storeId]/index.tsx"` >= 2 (SSR + client)
    - `grep -c "theme.content.catalog.defaultSort" "tienda-app/src/pages/[storeId]/index.tsx"` >= 2
    - `grep -c "theme.content.catalog.showOutOfStock" "tienda-app/src/pages/[storeId]/index.tsx"` >= 2
    - `grep -c "pageSize: 50\|pageSize: 24" "tienda-app/src/pages/[storeId]/index.tsx"` == 0 (하드코딩 제거)
    - `grep -c "TODO(Phase 61 이후): size/color/price 필터" "tienda-app/src/pages/[storeId]/index.tsx"` == 1
    - `grep -c "useThemeContent().productCard" tienda-app/src/components/ProductCard.tsx` == 1
    - `cd tienda-app && npx tsc --noEmit` exit 0 · `npx eslint src/` exit 0
  </acceptance_criteria>
  <done>공개 목록이 catalog 토큰(pageSize/sort/showOutOfStock)을 따르고 ProductCard 옵션이 Provider 로 전달된다.</done>
</task>

<task type="auto">
  <name>Task 3: diseno.tsx — 🛍 Tarjeta de producto + 📚 Catálogo y filtros 그룹</name>
  <files>tienda-app/src/pages/[storeId]/panel/diseno.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/panel/diseno.tsx (Plan 61-10 결과 — 아코디언 그룹 구조)
    - tienda-app/src/components/panel/PanelPrimitives.tsx (SwitchField/SelectField/NumberField/HintBanner)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § 아코디언 그룹 8·9 + variantDots hint 카피
  </read_first>
  <action>
그룹 7(`🎨 Colores y tipografía`) 뒤에 그룹 2개 추가(접힘 상태, `defaultOpen` 없음):

**그룹 8 `🛍 Tarjeta de producto`** — `content.productCard` 6개 SwitchField:
- `discountBadge` — `Mostrar % de descuento`
- `installments` — `Mostrar cuotas`
- `quickAdd` — `Botón de agregado rápido (＋)`
- `hoverSecondImage` — `Cambiar a 2ª foto al pasar el mouse`
- `lastUnitsBadge` — `Aviso «Últimas unidades»`
- `variantDots` — `Puntos de color de variantes`, **hint 필수**: `Sin datos de color de variantes aún — este interruptor no afecta la vista pública por ahora.`
각 토글 변경 시 `patchContent({ productCard: { ...content.productCard, [key]: v } })`.

**그룹 9 `📚 Catálogo y filtros`**:
- `defaultSort` SelectField(`Más nuevos`=newest / `Precio: menor a mayor`=price_asc / `Precio: mayor a menor`=price_desc / `Más vendidos`=bestseller)
- `pageSize` NumberField(min=12, max=48, step=4) — 라벨 `Productos por página`
- `showOutOfStock` SwitchField — `Mostrar productos sin stock`
- 필터 3개 SwitchField(`filters.size` `Filtro de talles` / `filters.color` `Filtro de colores` / `filters.price` `Filtro de precio`) + **hint 필수**: `Los filtros de talle/color/precio todavía no están disponibles en la vista pública — se guardan para cuando lo estén.`
각 변경 시 `patchContent({ catalog: { ...content.catalog, ... } })`.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/panel/diseno.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Tarjeta de producto" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "Catálogo y filtros" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "Sin datos de color de variantes aún" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "Los filtros de talle/color/precio todavía no están disponibles" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -c "patchContent({ productCard" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 1
    - `grep -c "patchContent({ catalog" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 1
    - `cd tienda-app && npx eslint src/` exit 0
  </acceptance_criteria>
  <done>상품카드/카탈로그 그룹이 추가되고 미구현 항목에 정직한 hint 가 붙는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `content.catalog.pageSize/defaultSort` → 카탈로그 쿼리 | admin 값이 공개 목록 요청 파라미터가 된다(백엔드가 최종 clamp) |
| `product.priceOrig/stock` → 배지 판정 | 공개 DTO 값(Plan 61-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-60 | DoS | `catalog.pageSize` 폭주 | mitigate | 백엔드 sanitize 48 clamp(61-01) + 컨트롤러 48 상한(61-03) + 에디터 NumberField max=48 |
| T-61-61 | Tampering | `defaultSort` 문자열 SQL 주입 | mitigate | 백엔드 `resolveOrderBy` 화이트리스트(61-03) — 프런트 값은 서버가 재검증 |
| T-61-62 | Information Disclosure | variantDots 가짜 데이터로 오정보 | mitigate | no-op + TODO 로 가짜 UI 원천 배제 |
| T-61-63 | Tampering | quickAdd 담기 클릭이 카드 링크와 충돌 | mitigate | `stopPropagation` 으로 분리 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rEn "#[0-9a-fA-F]{3,6}" tienda-app/src/components/ProductCard.tsx    # 0건(그림자 rgba 제외)
# 스모크: publish 후
curl -s "http://localhost:5002/api/public/shop/9/products?pageSize=12&sort=price_asc" | python3 -c "import sys,json;d=json.load(sys.stdin);p=[i['price'] for i in d['items']];print(len(d['items'])<=12, p==sorted(p))"
```
</verification>

<success_criteria>
- 6개 옵션 조건부 렌더, variantDots no-op
- `pageSize=12`/`price_asc`/`showOutOfStock` 이 공개 목록에 반영
- 미구현 필터/변형점에 정직한 hint
- 상품카드 가격이 heading 20px
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-12-SUMMARY.md` 생성
</output>
