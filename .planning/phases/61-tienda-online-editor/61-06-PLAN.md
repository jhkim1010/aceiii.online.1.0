---
phase: 61-tienda-online-editor
plan: 06
type: execute
wave: 4
depends_on: ["61-05"]
files_modified:
  - tienda-app/src/components/sections/Hero.tsx
  - tienda-app/src/components/sections/Benefits.tsx
  - tienda-app/src/components/sections/Carousel.tsx
  - tienda-app/src/components/sections/DuoBanners.tsx
  - tienda-app/src/components/sections/Newsletter.tsx
  - tienda-app/src/components/sections/SectionRenderer.tsx
autonomous: true
requirements: [R4, R8]
must_haves:
  truths:
    - "SectionRenderer 가 section.type 별로 올바른 컴포넌트를 렌더하고, enabled=false 인 섹션은 아무것도 렌더하지 않는다"
    - "hero.images 가 비어 있으면 그라데이션 배경만, 1장이면 정지 이미지, 2장 이상이면 캐러셀로 렌더된다"
    - "duoBanners 의 href 는 sanitize 를 통과한 값만 오므로 <a> 로 그대로 감싼다"
    - "어떤 섹션 컴포넌트도 dangerouslySetInnerHTML 을 쓰지 않는다"
  artifacts:
    - path: "tienda-app/src/components/sections/SectionRenderer.tsx"
      provides: "section.type → 컴포넌트 분기 (index.tsx 가 이 한 파일만 호출)"
      contains: "switch (section.type)"
    - path: "tienda-app/src/components/sections/Hero.tsx"
      provides: "hero 섹션 렌더 + 이미지 캐러셀"
    - path: "tienda-app/src/components/sections/Carousel.tsx"
      provides: "source 별 상품 목록 조회 + 그리드 렌더"
  key_links:
    - from: "SectionRenderer.tsx"
      to: "Hero/Benefits/Carousel/DuoBanners/Newsletter"
      via: "switch 분기 렌더"
      pattern: "case 'carousel'"
    - from: "Carousel.tsx"
      to: "shop-api.ts::listProducts"
      via: "source(newest|bestseller|category) → sort/categoryId 파라미터"
      pattern: "listProducts\\("
---

<objective>
`content.sections` 배열의 5개 기본 타입(hero / benefits / carousel / duoBanners / newsletter)을 렌더하는 컴포넌트와, `index.tsx` 가 호출할 단일 분기점 `SectionRenderer.tsx` 를 만든다. `reels`/`quiz` 는 후속 플랜(61-11/61-14)이 이 `switch` 에 case 를 추가한다.

Purpose: 스토어프런트를 하드코딩 레이아웃에서 데이터 기반 섹션 조립으로 전환하기 위한 부품 확보. `index.tsx` 통합은 Plan 61-09 의 몫이라 이 플랜은 파일 충돌 없이 병렬 실행된다.
Output: 신규 컴포넌트 6개.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-PATTERNS.md

<interfaces>
```typescript
// tienda-app/src/types/shop.ts (Plan 61-05)
export interface HeroSection { type: 'hero'; enabled: boolean; title: string; subtitle: string; cta: string; images: string[] }
export interface BenefitsSection { type: 'benefits'; enabled: boolean; items: { icon: string; text: string }[] }
export interface CarouselSection { type: 'carousel'; enabled: boolean; title: string; source: 'newest'|'bestseller'|'category'; categoryId: number | null }
export interface DuoBannersSection { type: 'duoBanners'; enabled: boolean; banners: { image: string|null; title: string; subtitle: string; href: string|null }[] }
export interface NewsletterSection { type: 'newsletter'; enabled: boolean; title: string }

// tienda-app/src/services/shop-api.ts
export function listProducts(storeId, params?: { q?; globalCategoryId?; categoryId?; page?; pageSize?; sort?; showOutOfStock? }): Promise<ShopListResult>
export function minioImageUrl(fileName: string): string
// tienda-app/src/context/ThemeContentContext.tsx
export function useThemeContent(): StoreThemeContent
```

**기존 index.tsx 의 스타일 원형** (이 값을 그대로 이식해 무회귀 렌더를 만든다):
```typescript
hero:      { display:'grid', gridTemplateColumns:'2fr 1fr', gap:16, margin:'22px 0' }
heroBig:   { borderRadius:'var(--radius)', padding:'48px 40px', color:'#fff',
             background:'linear-gradient(120deg, var(--hero-from), var(--hero-to))',
             display:'flex', flexDirection:'column', justifyContent:'center', minHeight:280 }
heroTitle: { fontSize:34, margin:'0 0 8px', lineHeight:1.1, fontFamily:'var(--font-display)', fontWeight:'var(--disp-weight)' }
heroP:     { margin:'0 0 20px', color:'#d8d6ea', maxWidth:380 }
promo:     { borderRadius:'var(--radius)', padding:28, background:'var(--promo-bg)', border:'1px solid var(--promo-line)', display:'flex', flexDirection:'column', justifyContent:'center' }
promoTitle:{ fontSize:20, margin:'14px 0 6px', fontFamily:'var(--font-display)', fontWeight:'var(--disp-weight)' }
promoP:    { margin:'0 0 16px', color:'var(--muted)', fontSize:14 }
secHead:   { display:'flex', alignItems:'baseline', justifyContent:'space-between', margin:'24px 0 16px' }
secTitle:  { fontSize:22, margin:0, fontFamily:'var(--font-display)', fontWeight:'var(--disp-weight)' }
aistrip:   { margin:'40px 0', borderRadius:'var(--radius)', padding:36, color:'#fff',
             background:'linear-gradient(120deg, var(--hero-from), var(--hero-to))' }
aiTitle:   { fontSize:26, margin:'0 0 8px', fontFamily:'var(--font-display)', fontWeight:'var(--disp-weight)' }
aiP:       { margin:0, color:'#d8d6ea', maxWidth:440 }
```
</interfaces>

**UI-SPEC 표면 B 계약 (반드시 준수)**
- 색은 **CSS 변수만** — `#fff` 하드코딩 금지, `var(--navy)` 배경 위 텍스트는 `var(--on-navy)`. 위 원형의 `color:'#fff'`/`'#d8d6ea'` 는 그라데이션(`--hero-from/to`) 위 텍스트이므로 `var(--on-navy)` 로 교체하고, 보조 텍스트는 `var(--on-navy)` + `opacity:.8` 로 표현한다.
- 타이포 4토큰: `label` 12px/400 · `body` 13px/400 · `heading` 20px/`var(--disp-weight)` · `display` 24–34px/`var(--disp-weight)`(Hero h1 전용).
  → 위 원형의 `secTitle: fontSize:22` 는 신규 컴포넌트에서 **20px** 으로, `aiTitle: 26` 도 **20px**(heading) 으로 스냅한다. `heroTitle: 34` 는 `display` 토큰 상단이므로 유지.
- spacing 4px 그리드: 신규 컴포넌트는 전부 스냅(예: `margin:'22px 0'` → `'24px 0'`, `padding:28` → `24`, `padding:36` → `32`, `gap:6` → `8`).
- 버튼은 기존 `.btn`/`.btn-gold`/`.btn-ghost` 전역 클래스 재사용 — 커스텀 폰트 크기/굵기 지정 금지.
- `dangerouslySetInnerHTML` 금지.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero.tsx + Benefits.tsx</name>
  <files>tienda-app/src/components/sections/Hero.tsx, tienda-app/src/components/sections/Benefits.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/index.tsx 156~204행 + 210~291행 (하드코딩 hero/aistrip 블록과 스타일 원형 — 이식 대상)
    - tienda-app/src/components/ProductCard.tsx (파일 하단 `const s: Record<string, CSSProperties>` 관례)
    - tienda-online-editor-mockup.html (레포 루트 — 승인 목업, 표면 A 이지만 hero 미리보기 톤 참고)
    - tienda-online-rails-masonry-reels-mockup.html (레포 루트 — 표면 B 시각 정본)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § Typography 표면 B 4토큰 표
  </read_first>
  <action>
**A. `Hero.tsx`**
```tsx
export default function Hero({ section }: { section: HeroSection }) 
```
- `images.length === 0` → 그라데이션 배경만(`linear-gradient(120deg, var(--hero-from), var(--hero-to))`) — **현행 무회귀 경로**
- `images.length === 1` → 그라데이션 위에 `<img src={minioImageUrl(images[0])}>` 를 `position:absolute; inset:0; objectFit:cover; opacity:.55` 로 깔고 텍스트를 위에 얹는다(가독성)
- `images.length >= 2` → 캐러셀. `useState(index)` + `useEffect` 의 `setInterval(5000)` 자동 순환. **cleanup 에서 `clearInterval` 필수**. 하단 중앙에 인디케이터 점(`8px` 원, 활성 `var(--gold)`, 비활성 `var(--on-navy)` + `opacity:.4`, `gap:8`). 신규 캐러셀 라이브러리 도입 금지.
- 텍스트: `<h1>` = `title`(display 토큰: `fontSize:34`, 모바일 `clamp(24px, 6vw, 34px)`, `fontFamily:'var(--font-display)'`, `fontWeight:'var(--disp-weight)'`, `color:'var(--on-navy)'`), `<p>` = `subtitle`(body 13px, `color:'var(--on-navy)'`, `opacity:.8`, `maxWidth:380`)
- `cta` 가 빈 문자열이 아니면 `<button className="btn btn-gold">{section.cta}</button>` 렌더, 빈 문자열이면 버튼 미렌더
- 컨테이너 스타일: `{ borderRadius:'var(--radius)', padding:'48px 40px', minHeight:280, position:'relative', overflow:'hidden', display:'flex', flexDirection:'column', justifyContent:'center', margin:'24px 0' }`
- `title`/`subtitle` 이 **둘 다** 빈 문자열이면 섹션 자체를 렌더하지 않는다(`return null`)

**B. `Benefits.tsx`**
```tsx
export default function Benefits({ section }: { section: BenefitsSection })
```
- `items.length === 0` → `return null`
- 컨테이너: `{ margin:'40px 0', borderRadius:'var(--radius)', padding:32, background:'linear-gradient(120deg, var(--hero-from), var(--hero-to))', color:'var(--on-navy)', display:'grid', gridTemplateColumns:'repeat(auto-fit, minmax(200px, 1fr))', gap:16 }`
- 각 item: `<div>` 안에 `<span aria-hidden="true">{item.icon}</span>`(fontSize 24) + `<span>{item.text}</span>`(body 13px). icon 이 빈 문자열이면 span 자체 생략.
- 이모지는 장식이므로 `aria-hidden="true"`, 의미는 `text` 가 전달.

두 파일 모두 하단에 `const s: Record<string, CSSProperties> = { ... }` 상수를 두고 인라인 객체 리터럴 남용을 피한다(기존 관례).
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/sections/Hero.tsx src/components/sections/Benefits.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clearInterval" tienda-app/src/components/sections/Hero.tsx` == 1
    - `grep -c "var(--on-navy)" tienda-app/src/components/sections/Hero.tsx` >= 2
    - `grep -c "#fff\|#ffffff\|#d8d6ea" tienda-app/src/components/sections/Hero.tsx tienda-app/src/components/sections/Benefits.tsx` == 0
    - `grep -c "minioImageUrl" tienda-app/src/components/sections/Hero.tsx` >= 1
    - `grep -c "aria-hidden" tienda-app/src/components/sections/Benefits.tsx` == 1
    - `grep -c "dangerouslySetInnerHTML" tienda-app/src/components/sections/` == 0
    - `grep -c "Record<string, CSSProperties>" tienda-app/src/components/sections/Hero.tsx` == 1
    - `cd tienda-app && npx eslint src/components/sections/` exit 0
  </acceptance_criteria>
  <done>Hero/Benefits 가 데이터 기반으로 렌더되고 빈 데이터에서 null 을 반환한다.</done>
</task>

<task type="auto">
  <name>Task 2: Carousel.tsx + DuoBanners.tsx + Newsletter.tsx</name>
  <files>tienda-app/src/components/sections/Carousel.tsx, tienda-app/src/components/sections/DuoBanners.tsx, tienda-app/src/components/sections/Newsletter.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/index.tsx 180~195행(secHead/그리드) + 236~263행(promo 블록 스타일 원형)
    - tienda-app/src/components/Header.tsx 27~35행 (`s.search` 입력 스타일 — Newsletter 입력의 partial analog)
    - tienda-app/src/services/shop-api.ts (listProducts 시그니처 — Plan 61-05 확장본)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § Color 「목업 색상 → CSS 변수 매핑」 표
  </read_first>
  <action>
**A. `Carousel.tsx`**
```tsx
export default function Carousel({ storeId, section, initialItems }: {
  storeId: number;
  section: CarouselSection;
  initialItems?: ShopProduct[];   // SSR 로 이미 받은 목록이 있으면 재조회 없이 사용
})
```
- `source` → `listProducts` 파라미터 매핑:
  - `'newest'` → `{ sort: 'newest', pageSize: 12 }`
  - `'bestseller'` → `{ sort: 'bestseller', pageSize: 12 }`
  - `'category'` → `{ categoryId: section.categoryId ?? undefined, pageSize: 12 }` (categoryId 가 null 이면 조회 자체를 하지 않고 `return null`)
- `initialItems` 가 있고 `source === 'newest'` 면 그것을 그대로 쓰고 fetch 하지 않는다(SSR 재사용, 요청 절약).
- 그 외에는 `useEffect` 안에서 1회 fetch. `let alive = true;` 가드 + cleanup(기존 `index.tsx` 의 페치 관례와 동일).
- 헤더: `<div style={s.secHead}><h2 style={s.secTitle}>{section.title || 'Destacados'}</h2></div>` — `secTitle` 은 heading 토큰 20px.
- 목록: `<section style={gridStyle}>{items.map(p => <ProductCard key={p.id} product={p} />)}</section>`. `gridStyle` 은 `{ display:'grid', gap:16, gridTemplateColumns:'repeat(auto-fill, minmax(200px, 1fr))' }`.
- 로딩 중에는 `<span style={s.muted}>Cargando...</span>`(기존 카피 재사용), 결과 0개면 `<p style={s.muted}>No hay productos.</p>`(기존 카피 재사용).

**B. `DuoBanners.tsx`**
```tsx
export default function DuoBanners({ section }: { section: DuoBannersSection })
```
- `banners.length === 0` → `return null`
- 2열 그리드(`gridTemplateColumns:'1fr 1fr'`, 모바일 `<640px` 1열), `gap:16`, `margin:'24px 0'`
- 각 배너: 컨테이너 `{ borderRadius:'var(--radius)', padding:24, background:'var(--promo-bg)', border:'1px solid var(--promo-line)', display:'flex', flexDirection:'column', justifyContent:'center', position:'relative', overflow:'hidden', minHeight:160 }`
  - **2번째 배너**(index 1)는 UI-SPEC 상 "세일" 강조 → 배경을 `linear-gradient(120deg, var(--hero-from), var(--gold))` 로 하고 텍스트를 `var(--on-navy)` 로 한다.
  - `image` 가 있으면 `<img src={minioImageUrl(image)}>` 를 `position:absolute; inset:0; objectFit:cover; opacity:.35` 로 깐다.
  - `title`(heading 20px) + `subtitle`(body 13px, `var(--muted)` 또는 2번째 배너면 `var(--on-navy)`+`opacity:.8`)
  - `href` 가 null 이 아니면 전체를 `<a href={href} style={{ textDecoration:'none', color:'inherit' }}>` 로 감싼다. **href 는 백엔드 sanitizeHref 를 통과한 값이므로 프런트에서 재검증하지 않는다**(SSOT 원칙) — 이 근거를 한국어 주석으로 남긴다.

**C. `Newsletter.tsx`**
```tsx
export default function Newsletter({ section }: { section: NewsletterSection })
```
- 컨테이너: `{ margin:'32px 0', borderRadius:'var(--radius)', padding:32, background:'var(--soft)', border:'1px solid var(--line)', display:'flex', flexDirection:'column', alignItems:'center', gap:16 }`
- `<h2>` = `section.title || 'Suscribite a novedades'`(heading 20px)
- 입력 + 버튼: `<input type="email" placeholder="Tu email" />` (Header 의 `s.searchInput` 스타일 관례 재사용: `border:'1px solid var(--line)'`, `borderRadius:'var(--radius)'`, `padding:'8px 12px'`, `background:'var(--card)'`, `color:'var(--ink)'`) + `<button className="btn btn-gold" type="submit">Suscribirme</button>`
- **제출 백엔드가 없다** — `onSubmit` 은 `e.preventDefault()` 후 로컬 state 로 `'¡Listo! Te vamos a escribir.'` 문구만 표시한다. 가짜 성공처럼 보이지 않도록 코드에 한국어 TODO 주석을 남긴다: `// TODO(후속 Phase): 뉴스레터 구독 저장 엔드포인트 없음 — 현재는 클라이언트 표시만. 실제 수집 연동 필요.`
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/sections/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "listProducts(" tienda-app/src/components/sections/Carousel.tsx` >= 1
    - `grep -c "sort: 'bestseller'" tienda-app/src/components/sections/Carousel.tsx` == 1
    - `grep -c "alive" tienda-app/src/components/sections/Carousel.tsx` >= 2 (페치 cleanup 가드)
    - `grep -c "No hay productos." tienda-app/src/components/sections/Carousel.tsx` == 1
    - `grep -c "sanitizeHref" tienda-app/src/components/sections/DuoBanners.tsx` == 1 (주석으로 근거 명시)
    - `grep -c "TODO(후속 Phase): 뉴스레터" tienda-app/src/components/sections/Newsletter.tsx` == 1
    - `grep -rc "#fff\|rgba(0,0,0" tienda-app/src/components/sections/DuoBanners.tsx` == 0
    - `cd tienda-app && npx eslint src/components/sections/` exit 0
  </acceptance_criteria>
  <done>세 컴포넌트가 데이터 기반 렌더 + 빈 상태 null + CSS 변수만 사용한다.</done>
</task>

<task type="auto">
  <name>Task 3: SectionRenderer.tsx — section.type 단일 분기점</name>
  <files>tienda-app/src/components/sections/SectionRenderer.tsx</files>
  <read_first>
    - tienda-app/src/types/shop.ts (SectionConfig 유니온 — Plan 61-05)
    - 이 플랜 Task 1/2 산출물 5개 컴포넌트의 props 시그니처
  </read_first>
  <action>
```tsx
export default function SectionRenderer({ storeId, section, initialItems }: {
  storeId: number;
  section: SectionConfig;
  initialItems?: ShopProduct[];
}) {
  // 표시 토글이 꺼진 섹션은 DOM 자체를 만들지 않는다(빈 공간 남기지 않음 — display:none 아님)
  if (!section.enabled) return null;

  switch (section.type) {
    case 'hero':        return <Hero section={section} />;
    case 'benefits':    return <Benefits section={section} />;
    case 'carousel':    return <Carousel storeId={storeId} section={section} initialItems={initialItems} />;
    case 'duoBanners':  return <DuoBanners section={section} />;
    case 'newsletter':  return <Newsletter section={section} />;
    // TODO(Plan 61-11): case 'reels' → <ReelsSection section={section} />
    // TODO(Plan 61-14): case 'quiz'  → <QuizSection storeId={storeId} section={section} />
    default:            return null;   // 알 수 없는 타입은 조용히 무시(백엔드 sanitize 가 이미 걸러내지만 방어)
  }
}
```
`SectionConfig` 유니온에 `reels`/`quiz` 가 이미 포함돼 있으므로 `switch` 는 `default` 로 안전하게 떨어진다. TypeScript exhaustiveness 검사를 강제하지 말 것(후속 플랜이 case 를 추가할 때까지 컴파일이 막히면 안 됨).

**구조별 섹션 게이팅은 여기서 하지 않는다** — 게이팅은 에디터(표면 A)의 안내 UI 이고(Plan 61-10), 실제 렌더 억제는 Plan 61-09 의 `index.tsx` 가 macrostructure 에 따라 필터링한다. 이 근거를 한국어 주석으로 남긴다.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/sections/SectionRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "switch (section.type)" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `grep -c "if (!section.enabled) return null" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `grep -c "case '" tienda-app/src/components/sections/SectionRenderer.tsx` == 5
    - `grep -c "TODO(Plan 61-11)" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `grep -c "TODO(Plan 61-14)" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `cd tienda-app && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>index.tsx 가 SectionRenderer 하나만 import 하면 5종 섹션을 모두 렌더할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `published_tokens` content → React 렌더 | admin 이 입력한 텍스트·URL·파일명이 익명 방문자 브라우저에서 렌더된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-29 | Tampering | 섹션 텍스트(제목/부제/benefit) | mitigate | JSX `{text}` 만 사용 — React 가 자동 이스케이프. `dangerouslySetInnerHTML` 을 어느 파일에도 쓰지 않는 것이 방어(acceptance grep 0건) |
| T-61-30 | Tampering | `duoBanners[].href` → `<a href>` | mitigate | 백엔드 `sanitizeHref()` 가 `javascript:`/`data:` 를 이미 null 로 강등. 프런트는 재검증하지 않고 null 이면 `<a>` 를 만들지 않는다 |
| T-61-31 | Tampering | `hero.images[]` → `<img src>` | mitigate | `minioImageUrl()` 이 `encodeURIComponent` 적용 + 백엔드 `sanitizeFileName()` 이 경로 구분자 거부 |
| T-61-32 | DoS | Hero 캐러셀 `setInterval` 누수 | mitigate | `useEffect` cleanup 에서 `clearInterval` — acceptance grep 고정 |
| T-61-33 | DoS | Carousel 이 섹션마다 무제한 fetch | mitigate | `pageSize: 12` 고정 + `initialItems` SSR 재사용 + 백엔드 60초 캐시 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "dangerouslySetInnerHTML" tienda-app/src/                    # 0건
grep -rEn "#[0-9a-fA-F]{3,6}" tienda-app/src/components/sections/     # 0건 (색은 CSS 변수만)
grep -rn "'doc'" tienda-app/src/                                       # 0건
```
</verification>

<success_criteria>
- 6개 신규 컴포넌트가 존재하고 `npx tsc --noEmit` 통과
- 색상 하드코딩 0건, `dangerouslySetInnerHTML` 0건
- 빈 데이터(빈 title/items/banners)에서 각 섹션이 null 반환
- SectionRenderer 가 enabled=false 를 렌더하지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-06-SUMMARY.md` 생성
</output>
