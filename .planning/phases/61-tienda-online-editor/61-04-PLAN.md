---
phase: 61-tienda-online-editor
plan: 04
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - api-ventago/migrations/2026-07-24-store-themes-macro-4.sql
  - api-ventago/src/app/shop-public/store-theme.constants.ts
  - api-ventago/src/app/shop-public/store-theme.constants.spec.ts
autonomous: false
requirements: [R9, R8]
must_haves:
  truths:
    - "로컬 5432 와 운영 5434 의 chk_store_theme_macro 정의가 동일하게 ('marquee','bento','rails','masonry') 4값이다"
    - "sanitizeMacrostructure('rails') === 'rails', sanitizeMacrostructure('masonry') === 'masonry'"
    - "sanitizeMacrostructure('doc') === 'marquee' 이고 sanitizeMacrostructure('foo') === 'marquee'"
    - "macroSettings 확장 키가 sanitizeContent 를 통해 clamp 되어 저장된다"
    - "shop_readonly role 이 sales/sale_items 를 SELECT 할 수 있다(bestseller 선반)"
  artifacts:
    - path: "api-ventago/migrations/2026-07-24-store-themes-macro-4.sql"
      provides: "CHECK 제약 4값 교체 + doc 방어적 UPDATE + shop_readonly GRANT — 이 Phase 유일한 신규 마이그레이션 파일"
      contains: "chk_store_theme_macro"
    - path: "api-ventago/src/app/shop-public/store-theme.constants.ts"
      provides: "Macrostructure 4종 + macroSettings sanitize"
      contains: "'rails', 'masonry'"
  key_links:
    - from: "store-theme.constants.ts::sanitizeMacrostructure"
      to: "store_themes.chk_store_theme_macro"
      via: "허용값 집합 일치 (코드가 DB 제약보다 넓으면 저장 시 23514 위반)"
      pattern: "'marquee', 'bento', 'rails', 'masonry'"
---

<objective>
`store_themes.macrostructure` 를 4종(`marquee | bento | rails | masonry`)으로 재편한다. **마이그레이션(CHECK 제약 교체)이 코드 변경보다 먼저 적용돼야 한다** — 순서가 뒤집히면 `rails` 발행 시 PostgreSQL 23514 제약 위반이 난다. 같은 마이그레이션 파일에 `bestseller` 선반이 필요로 하는 `shop_readonly` GRANT 도 포함한다(신규 마이그레이션 파일은 여전히 1개).

Purpose: 색/글꼴만 바뀌던 한계를 깨고 레이아웃 뼈대 자체를 선택 가능하게 만드는 A2 의 기반.
Output: 로컬 5432 + 운영 5434 에 적용·대조된 마이그레이션 + 4종 SSOT + macroSettings 가드레일.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-SPEC.md
@.planning/phases/61-tienda-online-editor/61-CONTEXT.md
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@CLAUDE.md

<interfaces>
현행 DDL (`api-ventago/migrations/2026-07-22-store-themes.sql:8-16`):
```sql
CREATE TABLE IF NOT EXISTS store_themes (
  store_id         INTEGER      PRIMARY KEY REFERENCES stores(id) ON DELETE CASCADE,
  base_theme       VARCHAR(40)  NOT NULL DEFAULT 'Studio',
  macrostructure   VARCHAR(40)  NOT NULL DEFAULT 'marquee',   -- marquee | bento | doc
  published_tokens JSONB        NOT NULL DEFAULT '{}'::jsonb,
  draft_tokens     JSONB,
  published_at     TIMESTAMPTZ,
  updated_at       TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
  CONSTRAINT chk_store_theme_macro
    CHECK (macrostructure IN ('marquee', 'bento', 'doc'))
);
```
실측(2026-07-23 조회): 운영 5434 = 9행 전부 `marquee`, 로컬 5432 = 4행 전부 `marquee`. **`doc` 사용 0건**(컬럼 값 + published_tokens/draft_tokens JSONB 내부 모두).

현행 코드 (`store-theme.constants.ts`):
```typescript
export type Macrostructure = 'marquee' | 'bento' | 'doc';
export const DEFAULT_MACROSTRUCTURE: Macrostructure = 'marquee';
export function sanitizeMacrostructure(raw: unknown): Macrostructure {
  return (['marquee', 'bento', 'doc'] as Macrostructure[]).includes(raw as Macrostructure)
    ? (raw as Macrostructure) : DEFAULT_MACROSTRUCTURE;
}
```

`shop_readonly` GRANT 현황 (`api-ventago/migrations/shop-mvp-readonly-role.sql:33-45`): products/categories/subcategories/products_subcategories/colors/sizes/seasons/origins/stores/prices/price_types + (별도 파일) global_categories, store_themes. **`sales`/`sale_items` 없음.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] 마이그레이션 SQL 작성</name>
  <files>api-ventago/migrations/2026-07-24-store-themes-macro-4.sql</files>
  <read_first>
    - api-ventago/migrations/2026-07-22-store-themes.sql (전체 — 헤더 주석/트랜잭션/DO 블록 관례)
    - api-ventago/migrations/2026-07-22-store-themes-enabled.sql (전체 — ALTER-only 마이그레이션 analog)
    - api-ventago/migrations/shop-mvp-readonly-role.sql 30~55행 (GRANT 루프 관례)
    - CLAUDE.md 「DB 마이그레이션 적용 규칙」 절
  </read_first>
  <action>
`api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` 를 아래 내용 그대로 생성한다. **이 Phase 의 유일한 신규 마이그레이션 파일이다** — 테이블/컬럼 추가는 단 하나도 넣지 않는다.

```sql
-- 2026-07-24 Phase 61 · store_themes.macrostructure 4종 재편 (rails/masonry 추가, doc 제거)
-- 목적: 레이아웃 뼈대를 marquee | bento | rails | masonry 로 확정.
--   'doc'(롱 도큐먼트)은 Lookbook 아키타입과 역할이 중복되어 제거(사용자 확정 2026-07-23).
--   실측: 운영 5434 9행 / 로컬 5432 4행 전부 'marquee' — 'doc' 사용 0건. 데이터 마이그레이션 불필요.
--   store_themes 는 매장당 1행(수십~수백 행)이라 CHECK 즉시 검증의 락 시간은 무시 가능.
-- 함께 포함: 공개몰 'Más vendidos'(bestseller) 선반 집계를 위한 shop_readonly SELECT 권한.
--   (신규 테이블/컬럼 0 — 권한 부여와 제약 교체뿐)
BEGIN;

-- 1) 방어적 정규화 — 실행 시점에 'doc' 행이 생겼을 경우 대비 (현재 기준 0행 영향)
UPDATE store_themes SET macrostructure = 'marquee' WHERE macrostructure = 'doc';

-- 2) CHECK 제약 교체 (동일 이름 유지)
ALTER TABLE store_themes DROP CONSTRAINT IF EXISTS chk_store_theme_macro;

ALTER TABLE store_themes ADD CONSTRAINT chk_store_theme_macro
  CHECK (macrostructure IN ('marquee', 'bento', 'rails', 'masonry'));

-- 3) bestseller 집계용 읽기 권한 (role 존재 시에만)
DO $$ BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'shop_readonly') THEN
    GRANT SELECT ON public.sales TO shop_readonly;
    GRANT SELECT ON public.sale_items TO shop_readonly;
  END IF;
END $$;

COMMIT;

-- ============================================================================
-- 적용:
--   로컬 5432: psql -p 5432 -d ventago -v ON_ERROR_STOP=1 --single-transaction \
--                -f api-ventago/migrations/2026-07-24-store-themes-macro-4.sql
--   운영 5434: ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -v ON_ERROR_STOP=1 --single-transaction" \
--                < api-ventago/migrations/2026-07-24-store-themes-macro-4.sql
-- 검증(양쪽 동일 출력이어야 함):
--   SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'chk_store_theme_macro';
--   → CHECK ((macrostructure)::text = ANY ((ARRAY['marquee'::character varying, 'bento'::character varying, 'rails'::character varying, 'masonry'::character varying])::text[]))
--   SELECT has_table_privilege('shop_readonly','public.sale_items','SELECT');  → t
-- 기존 테이블 ALTER 이므로 owner 이전 불필요.
-- ============================================================================
```

주의: `DROP CONSTRAINT IF EXISTS` 를 쓴다(제약이 이미 없는 환경에서도 멱등).
  </action>
  <verify>
    <automated>test -f api-ventago/migrations/2026-07-24-store-themes-macro-4.sql && grep -c "rails" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CHECK (macrostructure IN ('marquee', 'bento', 'rails', 'masonry'))" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` == 1
    - `grep -c "WHERE macrostructure = 'doc'" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` == 1
    - `grep -c "GRANT SELECT ON public.sale_items TO shop_readonly" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` == 1
    - `grep -ciE "CREATE TABLE|ADD COLUMN|DROP COLUMN" api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` == 0
    - `ls api-ventago/migrations/ | grep -c "2026-07-24"` == 1 (신규 파일 정확히 1개)
  </acceptance_criteria>
  <done>마이그레이션 파일이 존재하고 테이블/컬럼 추가 구문이 0건이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] 로컬 5432 + 운영 5434 적용 및 제약 정의 대조</name>
  <read_first>
    - api-ventago/migrations/2026-07-24-store-themes-macro-4.sql (Task 1 산출물 — 실행 전 SQL 전문 확인)
    - CLAUDE.md 「운영 서버 직접 접근 규칙」 + 「DB 마이그레이션 적용 규칙」
  </read_first>
  <what-built>
    Task 1 이 `api-ventago/migrations/2026-07-24-store-themes-macro-4.sql` 을 생성했다. 내용:
    (1) `UPDATE store_themes SET macrostructure='marquee' WHERE macrostructure='doc'` — 현재 기준 영향 0행
    (2) `chk_store_theme_macro` 를 4값(`marquee|bento|rails|masonry`)으로 교체
    (3) `shop_readonly` 에 `sales`/`sale_items` SELECT GRANT (role 존재 시에만)
    테이블/컬럼 추가 0건. 기존 테이블 ALTER 이므로 owner 이전 불필요.
  </what-built>
  <how-to-verify>
    이 DDL 은 운영 DB 를 변경하므로 **사용자 승인 없이 실행 금지**(CLAUDE.md 규칙).

    1. 위 SQL 전문을 사용자에게 보여주고 "예상 영향: 운영 9행 / 로컬 4행 전부 이미 marquee → UPDATE 0행, 제약 교체 1건, GRANT 2건" 을 함께 제시한다.
    2. 승인 후 **로컬 5432** 적용:
       ```
       psql -p 5432 -d ventago -v ON_ERROR_STOP=1 --single-transaction \
         -f api-ventago/migrations/2026-07-24-store-themes-macro-4.sql
       ```
    3. 승인 후 **운영 5434** 적용:
       ```
       ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -v ON_ERROR_STOP=1 --single-transaction" \
         < api-ventago/migrations/2026-07-24-store-themes-macro-4.sql
       ```
    4. **양쪽 대조** — 아래 두 쿼리를 5432 와 5434 에서 각각 실행해 출력이 문자 단위로 동일한지 확인:
       ```sql
       SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'chk_store_theme_macro';
       SELECT has_table_privilege('shop_readonly','public.sale_items','SELECT') AS si,
              has_table_privilege('shop_readonly','public.sales','SELECT')      AS s;
       ```
       기대: 제약 정의에 `rails`/`masonry` 포함 + `doc` 미포함, 권한 둘 다 `t`.
       (운영에 `shop_readonly` role 이 없으면 GRANT 는 skip 되고 `has_table_privilege` 조회 자체가 에러 — 이 경우 "role 없음"을 기록하고 bestseller 는 61-03 의 안전 강등으로 최신순 동작함을 확인한다.)
    5. 두 환경의 제약 정의를 SUMMARY 에 그대로 붙여넣는다.
  </how-to-verify>
  <acceptance_criteria>
    - 5432 `pg_get_constraintdef` 출력에 `'rails'` 와 `'masonry'` 포함, `'doc'` 미포함
    - 5434 `pg_get_constraintdef` 출력이 5432 와 동일
    - `SELECT count(*) FROM store_themes WHERE macrostructure NOT IN ('marquee','bento','rails','masonry')` == 0 (양쪽)
    - SUMMARY 에 양쪽 출력 원문이 기록됨
  </acceptance_criteria>
  <resume-signal>양쪽 적용·대조 완료 시 "applied" 를 입력하거나, 차이/오류가 있으면 출력 원문을 붙여넣어 주세요</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 백엔드 Macrostructure 4종 + macroSettings sanitize + 스펙 케이스</name>
  <files>api-ventago/src/app/shop-public/store-theme.constants.ts, api-ventago/src/app/shop-public/store-theme.constants.spec.ts</files>
  <read_first>
    - api-ventago/src/app/shop-public/store-theme.constants.ts (Plan 61-01 결과 — sanitizeContent/StoreThemeContent 위치 확인)
    - api-ventago/src/app/shop-public/store-theme.constants.spec.ts (Plan 61-01 결과 — describe 구조에 이어붙임)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 A — 구조별 설정 필드" (rails/masonry 필드 정의)
    - .planning/phases/61-tienda-online-editor/61-SPEC.md R9
  </read_first>
  <behavior>
    - `sanitizeMacrostructure('rails')` → `'rails'` / `('masonry')` → `'masonry'`
    - `sanitizeMacrostructure('doc')` → `'marquee'` / `('foo')` → `'marquee'` / `(undefined)` → `'marquee'`
    - `sanitizeContent({}).macroSettings.masonry.desktopCols` → `4`
    - `sanitizeContent({ macroSettings: { masonry: { desktopCols: 9 } } }).macroSettings.masonry.desktopCols` → `4` (3/4/5 whitelist 밖)
    - `sanitizeContent({ macroSettings: { masonry: { firstLoad: 999 } } }).macroSettings.masonry.firstLoad` → `48`
    - `sanitizeContent({ macroSettings: { rails: { shelves: Array(9) } } }).macroSettings.rails.shelves.length` → `6`
  </behavior>
  <action>
**A. `store-theme.constants.ts`**

1) 타입/함수 4종 교체 (`doc` 완전 제거):
```typescript
export type Macrostructure = 'marquee' | 'bento' | 'rails' | 'masonry';
```
```typescript
export function sanitizeMacrostructure(raw: unknown): Macrostructure {
  // 'doc' 은 2026-07-23 제거됨 — 알 수 없는 값과 동일하게 marquee 로 강등된다.
  return (
    ['marquee', 'bento', 'rails', 'masonry'] as Macrostructure[]
  ).includes(raw as Macrostructure)
    ? (raw as Macrostructure)
    : DEFAULT_MACROSTRUCTURE;
}
```

2) `macroSettings` 스키마를 `StoreThemeContent` 에 추가한다. **rails/masonry 만** 정의한다 — marquee/bento 는 이 Phase 에서 렌더러가 변하지 않으므로(SPEC: marquee/bento 회귀 금지) 설정 필드를 만들면 아무 효과 없는 가짜 UI 가 된다(SPEC 「가짜 UI 금지」).
```typescript
export interface RailShelf {
  title: string;
  source: 'newest' | 'bestseller' | 'category';
  categoryId: number | null;
  limit: number; // 4~20
}

export interface MacroSettings {
  rails: { shelves: RailShelf[]; showArrows: boolean; lazyRows: boolean };
  masonry: {
    desktopCols: 3 | 4 | 5;
    mobileCols: 1 | 2;
    firstLoad: number;      // 12~48, 4의 배수
    keepRatio: boolean;
    stickyFilter: boolean;
    showOverlayInfo: boolean;
  };
}
```
`StoreThemeContent` 에 `macroSettings: MacroSettings;` 추가.

3) `DEFAULT_CONTENT` 에 추가 (SPEC R9 「Novedades / Más vendidos / 카테고리별」 중 카테고리 선반은 admin 이 categoryId 를 골라야 하므로 기본 2개만 둔다):
```typescript
  macroSettings: {
    rails: {
      shelves: [
        { title: 'Novedades', source: 'newest', categoryId: null, limit: 10 },
        { title: 'Más vendidos', source: 'bestseller', categoryId: null, limit: 10 },
      ],
      showArrows: true,
      lazyRows: true,
    },
    masonry: {
      desktopCols: 4,
      mobileCols: 2,
      firstLoad: 24,
      keepRatio: true,
      stickyFilter: true,
      showOverlayInfo: false,
    },
  },
```

4) `sanitizeContent()` 안에 `macroSettings` 처리 추가:
   - `rails.shelves`: 배열 아니면 default 복제. 배열이면 `.slice(0, 6)`, 각 항목 `{ title: clampText(x.title, 40), source: ['newest','bestseller','category'].includes(x.source) ? x.source : 'newest', categoryId: (source==='category' && Number.isInteger(Number(x.categoryId))) ? Number(x.categoryId) : null, limit: clamp(Number(x.limit), 4, 20, 10) }`
   - `rails.showArrows` / `rails.lazyRows`: `!== false` (미지정 시 true 유지)
   - `masonry.desktopCols`: `[3,4,5].includes(n) ? n : 4`
   - `masonry.mobileCols`: `[1,2].includes(n) ? n : 2`
   - `masonry.firstLoad`: `clamp(Number(n), 12, 48, 24)` 후 `Math.round(v/4)*4` 로 4의 배수 스냅
   - `masonry.keepRatio` / `stickyFilter`: `!== false`, `showOverlayInfo`: `=== true`

**B. `store-theme.constants.spec.ts`** — 기존 파일에 `describe` 2개 추가:
```typescript
describe('sanitizeMacrostructure — 4종 재편(R9 acceptance)', () => { ... })
describe('sanitizeContent — macroSettings clamp(R9)', () => { ... })
```
위 `<behavior>` 6개 케이스를 전부 `it` 으로 작성한다. `'doc' → 'marquee'` 케이스는 반드시 포함(SPEC acceptance).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/shop-public/store-theme.constants --silent && npx eslint src/app/shop-public/store-theme.constants.ts src/app/shop-public/store-theme.constants.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type Macrostructure = 'marquee' | 'bento' | 'rails' | 'masonry'" api-ventago/src/app/shop-public/store-theme.constants.ts` == 1
    - `grep -c "'doc'" api-ventago/src/app/shop-public/store-theme.constants.ts` == 0
    - `grep -rn "'doc'" api-ventago/src/app/shop-public/` 결과에서 `.spec.ts`(강등 테스트) 외 파일 0건
    - `grep -c "macroSettings: MacroSettings" api-ventago/src/app/shop-public/store-theme.constants.ts` == 1
    - `grep -c "export interface RailShelf" api-ventago/src/app/shop-public/store-theme.constants.ts` == 1
    - `cd api-ventago && npx jest src/app/shop-public/store-theme.constants --silent` exit 0, 실패 0
    - `cd api-ventago && npx eslint src/app/shop-public/` exit 0
  </acceptance_criteria>
  <done>백엔드 SSOT 가 4종만 허용하고, macroSettings 가 clamp 되어 저장되며, doc 은 코드에서 사라졌다(강등 테스트 제외).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 에디터 → `macrostructure` 문자열 → DB CHECK | 코드 허용값이 DB 제약보다 넓으면 저장 실패(23514), 좁으면 기능 미노출 |
| 에디터 → `macroSettings` JSONB | rails 선반 개수/limit, masonry 열 수가 렌더 비용을 직접 좌우한다 |
| 운영 DB DDL 실행 | 되돌리기 비용이 큰 상태 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-19 | DoS | 코드/DB 허용값 불일치로 발행 500 | mitigate | Task 2 를 `[BLOCKING]` 체크포인트로 두어 마이그레이션이 Task 3(코드)보다 **먼저** 적용되도록 강제. 양쪽 `pg_get_constraintdef` 대조를 acceptance 로 고정 |
| T-61-20 | DoS | `rails.shelves` 무제한 → 선반마다 카탈로그 쿼리 | mitigate | 선반 최대 6개, 선반당 `limit` 4~20 clamp. 각 선반은 기존 60초 캐시된 카탈로그 엔드포인트 재사용 |
| T-61-21 | DoS | `masonry.firstLoad` 폭주 | mitigate | 12~48 clamp + 4의 배수 스냅. 컨트롤러 pageSize 48 상한이 2차 방어(61-03) |
| T-61-22 | Elevation | `shop_readonly` 권한 확대(sales/sale_items) | mitigate | `SELECT` 만 부여. role 에 `default_transaction_read_only=on` + `statement_timeout=5s` 가 이미 설정돼 있어 쓰기/장기쿼리 불가. 사용자 승인 체크포인트 경유 |
| T-61-23 | Repudiation | 운영 DDL 무기록 실행 | mitigate | SQL 파일을 git 커밋 + SUMMARY 에 양쪽 제약 정의 원문 기록 |
| T-61-24 | Tampering | `doc` 잔여 데이터로 제약 위반 | mitigate | 제약 재생성 **전에** 같은 트랜잭션에서 `UPDATE ... WHERE macrostructure='doc'` 방어 실행 |
</threat_model>

<verification>
```bash
# 신규 마이그레이션 파일이 정확히 1개인지 (R8 acceptance)
git status --porcelain api-ventago/migrations/ | grep -c "2026-07-24-store-themes-macro-4.sql"   # 1
git status --porcelain api-ventago/migrations/ | wc -l                                            # 1
# doc 잔여 코드 0건 (spec 의 강등 테스트 제외)
grep -rn "'doc'" api-ventago/src/app/shop-public/ | grep -v ".spec.ts"                            # 0건
cd api-ventago && npx jest src/app/shop-public --silent && npx eslint src/app/shop-public/
```
</verification>

<success_criteria>
- 로컬 5432 / 운영 5434 의 `chk_store_theme_macro` 정의가 동일한 4값
- `sanitizeMacrostructure` 가 rails/masonry 통과, doc/foo 는 marquee 강등
- `macroSettings` clamp 유닛테스트 통과
- `api-ventago/migrations/` 신규 파일 1개, 그 안에 CREATE TABLE/ADD COLUMN 0건
- 백엔드 `shop-public/` 에 `'doc'` 리터럴 0건(스펙 파일 제외)
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-04-SUMMARY.md` 생성 — 양쪽 DB 의 `pg_get_constraintdef` 출력 원문 포함 필수
</output>
