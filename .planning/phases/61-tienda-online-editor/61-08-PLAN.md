---
phase: 61-tienda-online-editor
plan: 08
type: execute
wave: 4
depends_on: ["61-05"]
files_modified:
  - tienda-app/src/components/panel/PanelPrimitives.tsx
  - tienda-app/src/components/panel/AssetUploadField.tsx
  - tienda-app/src/components/panel/SectionListEditor.tsx
  - tienda-app/src/pages/[storeId]/panel/diseno.tsx
autonomous: true
requirements: [R3, R2, R8]
must_haves:
  truths:
    - "에디터가 아코디언 그룹 구조로 렌더되고 헤더 클릭으로 펼침/접힘이 토글된다"
    - "브랜드 로고를 업로드하면 fileName 이 draft content 에 저장되고 미리보기 헤더에 반영된다"
    - "섹션 리스트에서 ▲▼ 로 순서를 바꾸고 👁 로 표시를 끄면 draft 저장 후 미리보기에 반영된다"
    - "에디터의 사용자 노출 카피가 전부 스페인어다"
    - "draft 저장 body 에 content 가 포함된다"
  artifacts:
    - path: "tienda-app/src/components/panel/SectionListEditor.tsx"
      provides: "섹션 순서 이동/표시 토글/타입별 인라인 편집"
      contains: "onMove"
    - path: "tienda-app/src/components/panel/AssetUploadField.tsx"
      provides: "파일 선택 → uploadThemeAsset → fileName 콜백 + 오류 카피"
      contains: "uploadThemeAsset"
    - path: "tienda-app/src/components/panel/PanelPrimitives.tsx"
      provides: "AccordionGroup / TextField / SwitchField / HintBanner / WarnBanner (표면 A 고정 팔레트)"
      contains: "export function AccordionGroup"
  key_links:
    - from: "diseno.tsx"
      to: "shop-api.ts::saveThemeDraft"
      via: "body 에 content 포함"
      pattern: "content,"
    - from: "SectionListEditor.tsx"
      to: "diseno.tsx content state"
      via: "onChange(sections) 콜백"
      pattern: "onChange"
---

<objective>
`diseno.tsx` 를 단일 패널에서 **아코디언 그룹 구조**로 전환하고, 브랜드(로고/파비콘/표시명) · 공지바 · 홈 섹션 리스트 · 연락처 편집 UI 를 붙인다. 표면 A(에디터 크롬)는 매장 테마와 무관한 **고정 Ventago 다크 네이비+골드 팔레트**를 쓴다. 사용자 노출 카피는 스페인어로 통일한다(현재 한국어는 개발 임시 카피이며 실사용자는 아르헨티나 매장주).

Purpose: 매장 admin 이 콘텐츠를 직접 편집할 수 있는 최초의 UI. 구조 선택(Plan 61-10)·상품카드/카탈로그/신뢰(Plan 61-13)·마케팅(Plan 61-15)은 이 셸 위에 그룹을 추가한다.
Output: 재사용 가능한 패널 프리미티브 + 섹션 리스트 편집기 + 아코디언화된 에디터.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-PATTERNS.md

<interfaces>
```typescript
// shop-api.ts (Plan 61-05)
export interface SaveThemeBody { baseTheme: string; macrostructure: Macrostructure; tokens: StoreThemeTokens; content: StoreThemeContent }
export function saveThemeDraft(storeId, token, body: SaveThemeBody): Promise<StoreTheme>
export function publishTheme(storeId, token): Promise<{ published: boolean }>
export function getThemeDraft(storeId, token): Promise<StoreTheme>
export function uploadThemeAsset(storeId, token, file: File, kind: ThemeAssetKind): Promise<{ fileName: string }>
export function minioImageUrl(fileName: string): string
// lib/theme-preset.ts
export const DEFAULT_CONTENT: StoreThemeContent
```

**diseno.tsx 기존 구조 (유지할 흐름)**
```typescript
const [baseTheme, setBaseTheme] = useState('Studio');
const [macro, setMacro] = useState<Macrostructure>('marquee');
const [tokens, setTokens] = useState<StoreThemeTokens>(DEFAULT_TOKENS);
const patch = useCallback((p: Partial<StoreThemeTokens>) => { setTokens(prev => ({ ...prev, ...p })); setState('idle'); }, []);
const onSave = ... saveThemeDraft(storeId, token, { baseTheme, macrostructure: macro, tokens });
const onPublish = ... saveThemeDraft(...) 후 publishTheme(...);
// 스타일: 파일 하단 `const ui: Record<string, CSSProperties>` — MUI 아님
```
기존 표면 A 팔레트(계속 사용): panel `#171a21`, border `#2a2f3a`, 입력 배경 `#1e222b`, 텍스트 `#e8eaed`, 보조 `#9aa2b1`, 강조 `#6ea8fe`
</interfaces>

**UI-SPEC 표면 A 계약 (반드시 준수)**
- 색: 배경 `#15152a`(navy2) / 패널 `#20203c` / hover `#262649` / topbar `#1a1a2e` / **accent `#f5a623`(gold)** 는 「Publicar 버튼 · 프리셋 선택 테두리 · 업로드 드롭존 hover · 토글 ON · NUEVO 태그 · 구조 카드 선택」에만.
  보조: cyan `#22d3ee`(`.hint` 배너: bg `#22d3ee12` / border `#22d3ee33` / text `#a5e9f5`), 초록 `#2e7d5b`(YA EXISTE·게이팅 사용가능), warn 배너는 gold 저채도(bg `#f5a62312` / border `#f5a62333` / text `#f7c471`).
  **기존 diseno.tsx 팔레트(`#171a21` 등)와 UI-SPEC 팔레트(`#15152a` 등)가 다르다 → 이 플랜에서 UI-SPEC 값으로 통일한다.**
- 타이포 4역할: Hint/Label 11px/400 · Body·Input·Button 13px(본문 400 / 버튼·탭·아코디언헤더 700) · Section Heading 13.5px/700 · Brand wordmark 15–16px/800(예외 1건).
- spacing: 신규 컴포넌트는 4px 그리드 스냅.
- 아코디언 인터랙션: 헤더 클릭 시 `open` 토글, `›` 아이콘 `transform:rotate(90deg)` + `transition .2s`, 본문 `display:none`↔`block`(높이 애니메이션 없음).
- **아이콘 라이브러리 도입 금지** — 이모지 또는 손수 작성 인라인 SVG 만.
</context>

<tasks>

<task type="auto">
  <name>Task 1: PanelPrimitives.tsx + AssetUploadField.tsx</name>
  <files>tienda-app/src/components/panel/PanelPrimitives.tsx, tienda-app/src/components/panel/AssetUploadField.tsx</files>
  <read_first>
    - tienda-online-editor-mockup.html (레포 루트 — **표면 A 아코디언 시각 정본**. `.acc`/`.acc-h`/`.chev`/`.fld`/`.sw`/`.upload` 클래스 실측값을 그대로 읽어 CSSProperties 로 옮길 것)
    - tienda-app/src/pages/[storeId]/panel/diseno.tsx 337~368행 (`ui` 스타일 오브젝트 관례)
    - tienda-app/src/services/shop-api.ts (uploadThemeAsset — Plan 61-05)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § Color/Typography 표면 A
  </read_first>
  <action>
**A. `PanelPrimitives.tsx`** — 표면 A 전용 재사용 부품. 전부 named export.

```tsx
export function AccordionGroup({ icon, title, defaultOpen, children }: {
  icon: string; title: string; defaultOpen?: boolean; children: ReactNode;
})
```
- `const [open, setOpen] = useState(defaultOpen === true);`
- 헤더 `<button type="button" onClick={() => setOpen(o => !o)}>`: `{ width:'100%', display:'flex', alignItems:'center', gap:8, background:'#20203c', border:'1px solid #32325a', borderRadius:8, padding:'12px', color:'#e8eaed', fontSize:13.5, fontWeight:700, cursor:'pointer', textAlign:'left' }`, hover 시 `#262649`
- `›` 셰브론: `<span style={{ marginLeft:'auto', transition:'transform .2s', transform: open ? 'rotate(90deg)' : 'none' }}>›</span>`
- 본문: `open ? <div style={{ padding:'12px', display:'flex', flexDirection:'column', gap:12 }}>{children}</div> : null`
- 접근성: 헤더 버튼에 `aria-expanded={open}`.

```tsx
export function TextField({ label, value, onChange, placeholder, maxLength = 200, multiline }: {...})
export function NumberField({ label, value, onChange, min, max, step }: {...})
export function SelectField({ label, value, onChange, options }: { options: { value: string; label: string }[] })
export function SwitchField({ label, checked, onChange, hint }: {...})
export function HintBanner({ children }: { children: ReactNode })
export function WarnBanner({ children }: { children: ReactNode })
```
- 라벨: `{ fontSize:11, color:'#9aa2b1', marginBottom:4, display:'block' }`
- 입력: `{ width:'100%', background:'#1a1a2e', color:'#e8eaed', border:'1px solid #32325a', borderRadius:8, padding:'8px 12px', fontSize:13 }`
- `SwitchField` 토글: 28×16px 트랙 + 12px 노브, ON 이면 트랙 `#f5a623`. `<input type="checkbox">` 를 시각적으로 숨기고(`opacity:0; position:absolute`) label 로 감싸 네이티브 키보드 접근성 유지 — **div onClick 금지**.
- `hint` 가 있으면 스위치 아래 11px `#9aa2b1` 텍스트.
- `HintBanner`: `{ background:'#22d3ee12', border:'1px solid #22d3ee33', borderRadius:8, padding:'8px 12px', color:'#a5e9f5', fontSize:11, lineHeight:1.5, marginTop:12 }`
- `WarnBanner`: `{ background:'#f5a62312', border:'1px solid #f5a62333', borderRadius:8, padding:'8px 12px', color:'#f7c471', fontSize:11, lineHeight:1.5, marginTop:12 }`
- `TextField` 는 `maxLength` 를 실제 `<input maxLength>` 로 강제한다(백엔드 clamp 와 동일 값 — 사용자가 잘림을 나중에 발견하지 않게).

**B. `AssetUploadField.tsx`**
```tsx
export default function AssetUploadField({ storeId, token, kind, value, onChange, label, accept }: {
  storeId: number; token: string; kind: ThemeAssetKind;
  value: string | null; onChange: (fileName: string | null) => void;
  label: string; accept: string;
})
```
- 드롭존 스타일: `{ border:'1px dashed #32325a', borderRadius:8, padding:16, textAlign:'center', cursor:'pointer', color:'#9aa2b1', fontSize:11 }`, hover 시 `borderColor:'#f5a623', color:'#f5a623'`
- 숨긴 `<input type="file" accept={accept}>` 를 `<label>` 로 감싸 클릭 유도(네이티브 접근성 유지)
- 선택 시: `setBusy(true)` → `uploadThemeAsset(storeId, token, file, kind)` → `onChange(res.fileName)` → `setBusy(false)`
- 실패 시 오류 문구를 **UI-SPEC 고정 카피**로 표시:
  - 이미지 kind: `El archivo supera 2MB o el formato no es válido (PNG/JPG/WEBP).`
  - `reelVideo`: `El video supera 20MB o el formato no es válido (MP4/WEBM).`
- `value` 가 있으면 미리보기: `reelVideo` 이면 파일명 텍스트만, 그 외에는 `<img src={minioImageUrl(value)} style={{ maxHeight:48, objectFit:'contain' }} />` + `Quitar` 텍스트 버튼(클릭 시 `onChange(null)`).
  **SVG 는 반드시 `<img>` 로만 렌더한다** — 인라인 삽입(`dangerouslySetInnerHTML`) 금지(SVG 내 스크립트 방어).
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/panel/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function AccordionGroup" tienda-app/src/components/panel/PanelPrimitives.tsx` == 1
    - `grep -c "aria-expanded" tienda-app/src/components/panel/PanelPrimitives.tsx` == 1
    - `grep -c "rotate(90deg)" tienda-app/src/components/panel/PanelPrimitives.tsx` == 1
    - `grep -c "#22d3ee12" tienda-app/src/components/panel/PanelPrimitives.tsx` == 1
    - `grep -c "#f5a62312" tienda-app/src/components/panel/PanelPrimitives.tsx` == 1
    - `grep -c "input type=\"checkbox\"\|type=\"checkbox\"" tienda-app/src/components/panel/PanelPrimitives.tsx` >= 1
    - `grep -c "uploadThemeAsset" tienda-app/src/components/panel/AssetUploadField.tsx` == 1
    - `grep -c "El archivo supera 2MB o el formato no es válido (PNG/JPG/WEBP)." tienda-app/src/components/panel/AssetUploadField.tsx` == 1
    - `grep -c "El video supera 20MB o el formato no es válido (MP4/WEBM)." tienda-app/src/components/panel/AssetUploadField.tsx` == 1
    - `grep -c "dangerouslySetInnerHTML" tienda-app/src/components/panel/` == 0
    - `cd tienda-app && npx eslint src/components/panel/` exit 0
  </acceptance_criteria>
  <done>아코디언/필드/배너/업로드 부품이 UI-SPEC 표면 A 팔레트로 존재하고 키보드 접근 가능하다.</done>
</task>

<task type="auto">
  <name>Task 2: SectionListEditor.tsx — 순서 이동 · 표시 토글 · 타입별 인라인 편집</name>
  <files>tienda-app/src/components/panel/SectionListEditor.tsx</files>
  <read_first>
    - tienda-online-editor-mockup.html (레포 루트 — 섹션 리스트 아이템 UI: 순서 번호 + 제목 + ▲▼ + 토글 스위치)
    - tienda-online-estructuras-editor-mockup.html (레포 루트 — 리스트 편집 공통 패턴: 행 `gap:8px`, ▲▼ 세로 쌍 `gap:4px`, `+ Agregar` dashed 버튼)
    - tienda-app/src/types/shop.ts (SectionConfig 7종 — Plan 61-05)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "리스트 편집 UI 공통 패턴" + § 아코디언 그룹 6번
  </read_first>
  <action>
```tsx
export default function SectionListEditor({ sections, onChange, storeId, token, disabledTypes }: {
  sections: SectionConfig[];
  onChange: (next: SectionConfig[]) => void;
  storeId: number;
  token: string;
  disabledTypes?: SectionType[];   // 현재 구조에서 비활성인 타입(Plan 61-10 이 전달). 값은 보존하고 표시만 흐리게.
})
```

**행 렌더** — 각 섹션마다:
- 좌측: 순서 번호(`{i + 1}`, 11px `#9aa2b1`) + 타입 라벨(스페인어 고정 매핑):
  `hero → 'Portada (Hero)'`, `benefits → 'Beneficios'`, `carousel → 'Carrusel de productos'`, `duoBanners → 'Banners dobles'`, `newsletter → 'Newsletter'`, `reels → 'Reels de video'`, `quiz → 'Quiz — asesor guiado'`
- 우측: ▲/▼ 세로 버튼 쌍(`gap:4px`) + 표시 토글(`SwitchField` 의 스위치만 재사용 또는 `👁`/`🚫` 버튼) + `›` 펼침 버튼
- 행 스타일: `{ display:'flex', alignItems:'center', gap:8, background:'#1a1a2e', border:'1px solid #32325a', borderRadius:8, padding:8 }`
- `disabledTypes` 에 포함된 타입: `{ opacity:.45 }` + 타입 라벨에 `textDecoration:'line-through'`. **토글/순서 이동은 계속 가능**(값 보존이 원칙이므로 조작 자체를 막지 않는다).

**순서 이동** — 배열 swap:
```typescript
const onMove = (i: number, dir: -1 | 1) => {
  const j = i + dir;
  if (j < 0 || j >= sections.length) return;
  const next = [...sections];
  [next[i], next[j]] = [next[j], next[i]];
  onChange(next);
};
```
첫 행의 ▲ 와 마지막 행의 ▼ 는 `disabled` + `opacity:.3`.

**표시 토글**: `onChange(sections.map((s, k) => (k === i ? { ...s, enabled: !s.enabled } : s)))`

**타입별 인라인 편집** (행을 펼쳤을 때만 렌더, `useState<number | null>(openIdx)`):
| type | 필드 |
|---|---|
| `hero` | `title` TextField(200) · `subtitle` TextField(200) · `cta` TextField(40) · 이미지 최대 5장 — `AssetUploadField kind="hero"` + 업로드된 파일명 리스트(각 항목에 `✕` 제거). 5장 도달 시 업로드 필드 비활성 |
| `benefits` | 항목 최대 6개, 각 행 = emoji TextField(8) + text TextField(200) + `✕`. `+ Agregar beneficio` dashed 버튼 |
| `carousel` | `title` TextField(80) · `source` SelectField(`Más nuevos`/`Más vendidos`/`Categoría`) · `source==='category'` 일 때만 categoryId SelectField(카테고리 목록은 props 로 받지 말고 `listCategories(storeId)` 를 컴포넌트 안에서 1회 조회, 실패 시 숫자 입력으로 폴백) |
| `duoBanners` | 최대 2개, 각 = `AssetUploadField kind="banner"` + title(200) + subtitle(200) + href TextField(500, placeholder `https://... o /ruta`) |
| `newsletter` | `title` TextField(120) |
| `reels` | **이 플랜에서는 편집 UI 없음** — `<HintBanner>Editor de Reels disponible próximamente.</HintBanner>` 대신 **아무것도 렌더하지 않는다**(가짜 UI 금지). Plan 61-11 이 채운다. 코드에 `// TODO(Plan 61-11): reels 편집 서브폼` 주석. |
| `quiz` | 동일 — Plan 61-14 가 채운다. `// TODO(Plan 61-14): quiz 편집 서브폼` |

**섹션 추가/삭제는 지원하지 않는다** — `sections` 는 7종 고정 슬롯이며 순서/표시만 바꾼다(SPEC 스키마: 타입별 1개). 이 근거를 파일 상단 한국어 주석으로 명시한다.

**빈 배열 엣지 케이스**: `sections.length === 0` 이면 UI-SPEC Empty state 카피 렌더 — 헤딩 `Todavía no hay secciones`, 본문 `Guardá el borrador para restaurar las secciones por defecto.`
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/panel/SectionListEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const onMove" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "Carrusel de productos" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "Quiz — asesor guiado" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "Todavía no hay secciones" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "TODO(Plan 61-11): reels 편집 서브폼" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "TODO(Plan 61-14): quiz 편집 서브폼" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "disabledTypes" tienda-app/src/components/panel/SectionListEditor.tsx` >= 2
    - `grep -c "line-through" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `cd tienda-app && npx eslint src/components/panel/` exit 0
  </acceptance_criteria>
  <done>섹션 순서/표시/인라인 편집이 동작하고, 미구현 타입에는 가짜 UI 를 만들지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: diseno.tsx 아코디언 전환 + content state + 스페인어 카피</name>
  <files>tienda-app/src/pages/[storeId]/panel/diseno.tsx</files>
  <read_first>
    - tienda-app/src/pages/[storeId]/panel/diseno.tsx (전체 — Plan 61-05 이 doc 분기를 이미 제거한 상태)
    - tienda-online-editor-mockup.html (레포 루트 — 아코디언 그룹 순서/라벨 시각 정본)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 A — 아코디언 그룹 순서" (12그룹) + § Copywriting Contract
    - tienda-app/src/context/ThemeContentContext.tsx (Plan 61-05 — 미리보기 래핑용)
  </read_first>
  <action>
**1) content state 추가**
```typescript
const [content, setContent] = useState<StoreThemeContent>(DEFAULT_CONTENT);
const patchContent = useCallback((p: Partial<StoreThemeContent>) => {
  setContent((prev) => ({ ...prev, ...p }));
  setState('idle');
}, []);
```
초안 로드 시 `setContent({ ...DEFAULT_CONTENT, ...t.content })`.
`onSave`/`onPublish` 의 `saveThemeDraft(...)` body 에 `content` 추가 + `useCallback` deps 배열에 `content` 추가.

**2) 아코디언 그룹** — 이 플랜은 아래 **5개 그룹**만 렌더한다. 나머지 7개는 후속 플랜(61-10: 1·2·3, 61-13: 8·9·11, 61-15: 12)이 추가한다. 미구현 그룹의 빈 껍데기를 만들지 않는다.

| # | 아이콘+제목 | 내용 | defaultOpen |
|---|---|---|---|
| 4 | `🏷 Identidad de marca` | `brand.displayName` TextField(60) · 로고 `AssetUploadField kind="logo" accept="image/png,image/jpeg,image/webp,image/svg+xml"` · 파비콘 `kind="favicon" accept="image/png,image/jpeg,image/webp"` | true |
| 5 | `📣 Barra de anuncio` | `announce.enabled` SwitchField · `announce.text` TextField(200) · `announce.href` TextField(500, placeholder `https://... o /ruta`) | false |
| 6 | `🧩 Secciones del inicio` | `<SectionListEditor sections={content.sections} onChange={...} storeId={storeId} token={token} />` | true |
| 7 | `🎨 Colores y tipografía` | **기존 섹션 1·2·3·4 의 내용을 그대로 이 그룹 안으로 이동**(프리셋 12종 / 포인트색 / 채도 / 배경톤 / 글꼴 / 제목굵기 / 모서리). `MACRO_OPTIONS` 세그먼트 버튼은 **여기서 제거**한다 — 구조 선택은 Plan 61-10 의 그룹 1 로 이동하며, 그 전까지 macrostructure 는 초안에서 읽은 값을 그대로 저장만 한다(선택 UI 부재). 이 과도기를 한국어 주석으로 명시: `// TODO(Plan 61-10): 구조 선택 UI 는 그룹 1(🧱 Estructura de la home)로 이동 예정. 그때까지 macro state 는 초안 값 유지.` | false |
| 10 | `💬 Contacto & redes` | `contact.whatsapp` TextField(20, placeholder `+54 9 11 ...`) · `instagram` TextField(100) · `facebook` TextField(100) · `footerText` TextField(200) | false |

**3) 카피 스페인어 통일** — 아래를 **정확히** 교체한다:
| 현재(한국어) | 교체(스페인어) |
|---|---|
| `<title>내 매장 홈페이지 꾸미기</title>` | `<title>Personalizá tu tienda</title>` |
| `🎨 내 매장 홈페이지 꾸미기` (brandH) | `🎨 Personalizá tu tienda` |
| `테마·색·글꼴·레이아웃을 고르면 오른쪽에 바로 반영됩니다.` | `Elegí colores, tipografías y secciones. Todo se ve al instante a la derecha.` |
| `초안 저장` / `저장 중…` | `Guardar borrador` / `Guardando…` |
| `발행하기` / `발행 중…` | `Publicar` / `Publicando…` |
| `초안이 저장되었습니다 (아직 공개에는 반영 안 됨)` | `Borrador guardado (todavía no está publicado).` |
| `발행 완료 — 공개 홈페이지에 반영되었습니다` | `¡Publicado! Ya está en vivo.` |
| `저장 실패: {e}` | `No se pudo guardar el borrador: {e}` |
| `발행 실패: {e}` | `No se pudo publicar: {e}` |
| `초안을 불러오지 못했습니다: {e}` | `No se pudo cargar el borrador: {e}` |
| `편집 링크가 필요합니다` / 안내문 | `Necesitás un link de edición` / `Generá el link desde el panel de administración con el botón «Editar diseño».` |
| 배경톤 버튼 `밝게`/`중간`/`어둡게` | `Claro` / `Medio` / `Oscuro` |
| 글꼴 select 옵션 4개 | `Editorial (serif)` / `Moderno (sans)` / `Técnico (mono)` / `Impacto (condensada)` |
| 라벨 `포인트 색` / `채도` / `배경 톤` / `제목 굵기` / `모서리 둥글기` | `Color de acento` / `Saturación` / `Tono de fondo` / `Grosor de títulos` / `Redondeo de esquinas` |
| 섹션 헤딩 `1 · 스타일 테마` 등 | 아코디언 그룹 제목으로 대체되므로 삭제 |
코드 주석은 계속 **한국어**(CLAUDE.md 규약).

**4) 미리보기(우측) 갱신**
- 미리보기 래퍼를 `<ThemeContentProvider content={content}>` 로 감싼다.
- 하드코딩된 `Nueva temporada` 히어로 대신 `content.sections` 를 `SectionRenderer` 로 순회 렌더한다 — **단 `carousel` 은 실제 API 를 치지 않도록** `initialItems={DUMMY}` 를 넘긴다(에디터 미리보기가 매 편집마다 카탈로그를 조회하면 안 됨). `SectionRenderer` 는 Plan 61-06 산출물.
- 미리보기 헤더는 기존 `<Header>` 를 그대로 두되, 로고/공지바 반영은 Plan 61-09 가 `Header.tsx` 를 확장한 뒤 자동으로 따라온다. 이 플랜에서 `Header.tsx` 를 수정하지 않는다(파일 소유권 분리).

**5) 팔레트 통일** — `ui` 스타일 오브젝트의 색을 UI-SPEC 표면 A 값으로 교체:
`app` 배경 `#15152a` · `panel` `#1a1a2e` + border `#32325a` · 입력/카드 `#20203c` · hover `#262649` · `btnPrimary`(=Publicar) `background:'#f5a623', color:'#1a1a2e'` · `btnGhost`(=Guardar borrador) `background:'#20203c', border:'1px solid #32325a'` · 프리셋 선택 outline `2px solid #f5a623`.
`stage` 배경은 `#15152a` 유지.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint "src/pages/[storeId]/panel/diseno.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AccordionGroup" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 6 (import 1 + 사용 5)
    - `grep -c "SectionListEditor" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 2
    - `grep -c "ThemeContentProvider" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 2
    - `grep -c "content," "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 2 (saveThemeDraft body + deps)
    - `grep -c "Publicar" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 1
    - `grep -c "Guardar borrador" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `grep -cE "초안 저장|발행하기|내 매장 홈페이지|밝게|어둡게" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 0 (한국어 UI 카피 잔여 0)
    - `grep -c "#f5a623" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` >= 2
    - `grep -c "TODO(Plan 61-10): 구조 선택 UI" "tienda-app/src/pages/[storeId]/panel/diseno.tsx"` == 1
    - `git diff tienda-app/src/components/Header.tsx | wc -l` == 0 (파일 소유권 분리 — Plan 61-09 소유)
    - `cd tienda-app && npx tsc --noEmit` exit 0 · `npx eslint src/` exit 0
  </acceptance_criteria>
  <done>에디터가 5개 아코디언 그룹으로 렌더되고, content 가 draft 에 저장되며, 사용자 노출 카피가 전부 스페인어다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 편집 토큰(`?t=`) → 에디터 API 호출 | 15분 TTL HMAC. 토큰 보유자만 draft/publish/asset 조작 가능 |
| admin 입력 → draft JSONB | 프런트 `maxLength` 는 UX 보조. 실제 강제는 백엔드 `sanitizeContent()` |
| 업로드 파일 → MinIO | 서버 검증(Plan 61-02)이 유일 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-39 | Elevation | 다른 매장 편집 시도 | mitigate | 모든 호출이 `StoreThemeEditGuard` 뒤 — 토큰 storeId ≠ 라우트 storeId 면 401. 프런트는 URL 의 storeId 를 그대로 쓰되 서버가 최종 판정 |
| T-61-40 | Tampering | SVG 로고 인라인 삽입으로 스크립트 실행 | mitigate | 미리보기/업로드 필드가 SVG 를 `<img src>` 로만 렌더. `dangerouslySetInnerHTML` 0건 acceptance |
| T-61-41 | Information Disclosure | 편집 토큰이 URL 에 노출 | accept | 기존 매직링크 설계(15분 TTL). 이 Phase 에서 변경하지 않음 |
| T-61-42 | Tampering | 프런트 maxLength 우회(DevTools) | accept | 백엔드 `clampText` 가 최종 강제. 프런트는 UX 힌트일 뿐 |
| T-61-43 | DoS | 에디터 미리보기가 편집마다 카탈로그 조회 | mitigate | 미리보기 `Carousel` 에 `initialItems={DUMMY}` 를 넘겨 네트워크 요청 0 |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "dangerouslySetInnerHTML" tienda-app/src/          # 0건
grep -rn "'doc'" tienda-app/src/                             # 0건
git diff tienda-app/package.json | wc -l                     # 0
# 브라우저: /{storeId}/panel/diseno?t=<token> 에서 아코디언 5개 확인, 로고 업로드 → 미리보기 반영
```
</verification>

<success_criteria>
- 아코디언 5개 그룹이 UI-SPEC 팔레트/타이포로 렌더
- 로고/파비콘 업로드 → `fileName` 이 content 에 저장 → draft 저장 성공
- 섹션 ▲▼/표시 토글이 미리보기에 즉시 반영
- 에디터 한국어 UI 카피 0건
- `Header.tsx` 무변경, 신규 의존성 0
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-08-SUMMARY.md` 생성
</output>
