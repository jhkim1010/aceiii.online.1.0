---
phase: 61-tienda-online-editor
plan: 11
type: execute
wave: 5
depends_on: ["61-06", "61-08"]
files_modified:
  - tienda-app/src/components/sections/ReelsSection.tsx
  - tienda-app/src/components/sections/SectionRenderer.tsx
  - tienda-app/src/components/panel/SectionListEditor.tsx
autonomous: true
requirements: [R10, R8]
must_haves:
  truths:
    - "reels 섹션이 세로 영상 카드 가로 스크롤로 렌더되고 초기 로드 시 영상 바이트를 요청하지 않는다(preload=none, poster 만)"
    - "영상을 탭하면 재생되고, 다른 영상을 탭하면 이전 영상이 멈춘다"
    - "어떤 video 태그에도 autoplay 속성이 없다"
    - "영상 0개인 reels 섹션은 렌더되지 않는다"
    - "에디터에서 영상+poster 를 올려 draft 에 저장할 수 있고, poster 없이는 저장되지 않는다"
  artifacts:
    - path: "tienda-app/src/components/sections/ReelsSection.tsx"
      provides: "세로 영상 카드 + 탭 재생 + 상품 오버레이"
      contains: "preload=\"none\""
    - path: "tienda-app/src/components/panel/SectionListEditor.tsx"
      provides: "reels 편집 서브폼(영상/poster 업로드 + 상품 연결)"
      contains: "reelVideo"
  key_links:
    - from: "SectionRenderer.tsx"
      to: "ReelsSection.tsx"
      via: "case 'reels'"
      pattern: "case 'reels'"
    - from: "SectionListEditor.tsx reels 서브폼"
      to: "shop-api.ts::uploadThemeAsset"
      via: "kind=reelVideo / reelPoster"
      pattern: "reelPoster"
---

<objective>
`reels` 섹션 타입을 완성한다: 세로 영상 카드 가로 스크롤 렌더(`ReelsSection.tsx`), `SectionRenderer` 의 `case 'reels'` 연결, `SectionListEditor` 의 reels 편집 서브폼. 영상은 `<video muted playsInline preload="none" poster>` 로 **autoplay 없이 탭 재생**한다. 코드베이스에 `<video>` 사용례가 0건이므로 RESEARCH 스케치와 승인 목업이 유일 근거.

Output: ReelsSection + SectionRenderer case + reels 편집 UI.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-tienda-online-editor/61-UI-SPEC.md
@.planning/phases/61-tienda-online-editor/61-RESEARCH.md

<interfaces>
```typescript
// types/shop.ts (Plan 61-05 미러) / store-theme.constants.ts (Plan 61-01)
export interface ReelsSection {
  type: 'reels'; enabled: boolean; title: string;
  items: { videoFile: string; posterFile: string; productId: number | null; durationLabel: string }[];
}
// shop-api.ts
export function uploadThemeAsset(storeId, token, file, kind: 'reelVideo'|'reelPoster'|...): Promise<{ fileName: string }>
export function minioImageUrl(fileName: string): string  // 영상 파일명도 동일 경로
export function listProducts(storeId, params): Promise<ShopListResult>  // 상품 연결 셀렉터용
```
백엔드 `sanitizeSection('reels')` 는 이미 `posterFile`/`videoFile` 둘 다 없으면 item 을 drop 한다(Plan 61-01).
</interfaces>

**UI-SPEC 표면 B reels 계약 (4px 스냅 완료)**
- 섹션 제목: `<h2>` heading 20px, 좌우 패딩 24px
- 카드 가로 스크롤: `display:flex; gap:16px; overflowX:auto; scrollSnapType:'x mandatory'`, 컨테이너 패딩 `4px 24px 20px`, 스크롤바 숨김(`.sf-rail` 재사용 가능)
- 카드: **192px**(데스크톱) / **152px**(모바일), `aspectRatio:'9/16'`, `borderRadius:12`, `overflow:hidden`, `scrollSnapAlign:'start'`, `cursor:pointer`
- `<video muted playsInline preload="none" poster={...}>` — **autoplay 금지**, `controls` 생략
- 재생 전 오버레이: 중앙 ▶ 32px, `rgba(255,255,255,0.85)`, `textShadow:'0 2px 10px rgba(0,0,0,0.5)'`. 탭하면 아이콘 숨김+재생
- (선택) 재생시간 배지: `durationLabel` 있을 때만 우상단 pill(`rgba(0,0,0,0.5)`, `var(--on-navy)`, 12px, radius 5, `4px 8px`)
- 상품 오버레이 pill(productId 있을 때만): `position:absolute; left:8; right:8; bottom:8; background:var(--card); borderRadius:9; padding:'8px 12px'; display:flex; gap:8`. 썸네일 32×40 + 이름(12px `var(--ink)`) + 가격(12px `var(--ink)`) + 우측 담기 버튼(`🛒`, `background:var(--navy)`, glyph `var(--on-navy)`, radius 6, padding 8, `aria-label="Agregar al carrito"`)
- productId 없는 item → 오버레이 pill 미렌더(가짜 상품 카드 금지)
- 영상 0개 → 섹션 전체 미렌더
- 탭 핸들러: 클릭한 video 외 모든 `<video>` `pause()`
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReelsSection.tsx — 영상 카드 + 탭 재생 + 상품 오버레이</name>
  <files>tienda-app/src/components/sections/ReelsSection.tsx</files>
  <read_first>
    - tienda-online-rails-masonry-reels-mockup.html (레포 루트 — **reels 렌더 시각 정본**, T3 탭)
    - .planning/phases/61-tienda-online-editor/61-RESEARCH.md § "reels 영상 렌더 패턴" (preload=none 근거 + iOS playsInline)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § "표면 B — reels 섹션"
    - tienda-app/src/context/ShopContext.tsx (add(product) — 담기 버튼 연결)
    - tienda-app/src/services/shop-api.ts (minioImageUrl, listProducts)
  </read_first>
  <action>
```tsx
export default function ReelsSection({ storeId, section }: { storeId: number; section: ReelsSection })
```
- `if (section.items.length === 0) return null;`
- 상품 오버레이용 데이터: `productId` 가 있는 item 들의 상품 정보를 얻어야 한다. 공개 API 에 "id 목록으로 상품 조회"가 없으므로, `listProducts(storeId, { pageSize: 48 })` 를 1회 조회해 `Map<id, ShopProduct>` 로 만들고 매칭한다(간단·요청 1회). 매칭 실패 시 오버레이는 이름/가격 없이 담기 버튼만 두거나 오버레이 자체 생략 — **가짜 데이터 렌더 금지**, 매칭 안 되면 pill 미렌더.
- 제목: `section.title` 이 있으면 `<h2>`(heading 20px, 패딩 좌우 24), 없으면 제목 생략.
- 카드 컨테이너: `<div className="sf-rail" style={{ padding:'4px 24px 20px' }}>` 재사용(Plan 61-07 의 globals.css 클래스). 단 `.sf-rail > *` 의 flex-basis 는 172px 이므로 reels 카드는 인라인으로 폭을 덮어쓴다: `style={{ flex:'0 0 192px', aspectRatio:'9/16', ... }}` (모바일 152 는 `.sf-rail` 미디어쿼리와 별개 — reels 는 인라인 `window.matchMedia` 없이 CSS 로 처리하려면 별도 클래스가 필요하므로, 폭은 `min(192px, 60vw)` 로 반응형 근사한다).
- 각 카드:
```tsx
<video
  ref={...}
  muted
  playsInline
  preload="none"
  poster={minioImageUrl(item.posterFile)}
  style={{ width:'100%', height:'100%', objectFit:'cover' }}
  onClick={handleTap}
>
  <source src={minioImageUrl(item.videoFile)} type={item.videoFile.endsWith('.webm') ? 'video/webm' : 'video/mp4'} />
</video>
```
  **`autoplay` 속성을 절대 넣지 않는다.** `controls` 도 생략.
- `handleTap(e)`:
```typescript
const v = e.currentTarget;
document.querySelectorAll('video').forEach((other) => { if (other !== v) other.pause(); });
if (v.paused) { v.play().catch(() => undefined); setPlayingId(id); } else { v.pause(); setPlayingId(null); }
```
  `.catch(() => undefined)` 로 iOS 재생 거부 시 조용히 무시.
- 재생 전 오버레이 ▶(state `playingId !== id` 일 때만): 중앙 절대배치, 32px, `rgba(255,255,255,0.85)`, `pointerEvents:'none'`.
- `durationLabel` 이 빈 문자열이 아니면 우상단 시간 배지.
- 상품 오버레이 pill(매칭된 product 있을 때만): 썸네일 `<img src={minioImageUrl(product.imageUrl)}>` + 이름 + 가격(`money()`) + 담기 버튼 `<button aria-label="Agregar al carrito" onClick={(ev) => { ev.stopPropagation(); add(product); }}>🛒</button>`. `stopPropagation` 으로 카드 탭(재생)과 분리.
- 파일 하단 `const s: Record<string, CSSProperties>`.
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/sections/ReelsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'preload="none"' tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -c "autoplay" tienda-app/src/components/sections/ReelsSection.tsx` == 0
    - `grep -c "playsInline" tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -c "querySelectorAll('video')" tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -c 'aria-label="Agregar al carrito"' tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -c "stopPropagation" tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -c "if (section.items.length === 0) return null" tienda-app/src/components/sections/ReelsSection.tsx` == 1
    - `grep -cE "#[0-9a-fA-F]{3,6}" tienda-app/src/components/sections/ReelsSection.tsx` <= 3 (rgba 오버레이 리터럴만 — 흰색/검정 그림자는 UI-SPEC 허용, hex 색은 0)
    - `cd tienda-app && npx eslint src/components/sections/ReelsSection.tsx` exit 0
  </acceptance_criteria>
  <done>영상이 preload=none + poster 로 렌더되고 탭 재생/단일 재생이 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: SectionRenderer 에 case 'reels' 추가</name>
  <files>tienda-app/src/components/sections/SectionRenderer.tsx</files>
  <read_first>
    - tienda-app/src/components/sections/SectionRenderer.tsx (Plan 61-06 — `// TODO(Plan 61-11): case 'reels'` 위치)
    - tienda-app/src/components/sections/ReelsSection.tsx (Task 1 산출물)
  </read_first>
  <action>
`SectionRenderer.tsx` 의 `switch` 에서 `// TODO(Plan 61-11): ...` 주석 줄을 실제 case 로 교체:
```tsx
    case 'reels':  return <ReelsSection storeId={storeId} section={section} />;
```
`import ReelsSection from './ReelsSection';` 추가. `quiz` TODO 는 그대로 남긴다(Plan 61-14 소유).
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/sections/SectionRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case 'reels':" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `grep -c "TODO(Plan 61-11)" tienda-app/src/components/sections/SectionRenderer.tsx` == 0 (해제됨)
    - `grep -c "TODO(Plan 61-14)" tienda-app/src/components/sections/SectionRenderer.tsx` == 1 (quiz 는 그대로)
    - `grep -c "import ReelsSection" tienda-app/src/components/sections/SectionRenderer.tsx` == 1
    - `cd tienda-app && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>공개 홈이 reels 섹션을 렌더할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 3: SectionListEditor reels 편집 서브폼</name>
  <files>tienda-app/src/components/panel/SectionListEditor.tsx</files>
  <read_first>
    - tienda-app/src/components/panel/SectionListEditor.tsx (Plan 61-08 — `// TODO(Plan 61-11): reels 편집 서브폼` 위치 + 타입별 인라인 편집 패턴)
    - tienda-app/src/components/panel/AssetUploadField.tsx (Plan 61-08 — kind 별 업로드)
    - .planning/phases/61-tienda-online-editor/61-UI-SPEC.md § reels 계약 (poster 필수)
  </read_first>
  <action>
`SectionListEditor.tsx` 의 `reels` 타입 인라인 편집 분기(현재 TODO 주석 자리)에 서브폼을 구현한다:
- `section.title` TextField(80) — 라벨 `Título de la sección`(placeholder `Así se usan 🧶`)
- **영상 목록**(최대 12): 각 item 행:
  - 영상 업로드 `<AssetUploadField kind="reelVideo" accept="video/mp4,video/webm" value={item.videoFile || null} onChange={fn}>`
  - poster 업로드 `<AssetUploadField kind="reelPoster" accept="image/png,image/jpeg,image/webp" value={item.posterFile || null} onChange={fn}>` — 라벨 옆에 `(obligatorio)` 표시
  - 상품 연결 SelectField(`listCategories` 아님 — 상품 셀렉터. `listProducts(storeId, { pageSize: 48 })` 1회 조회 후 `<select>`. 미선택 = null 허용)
  - `durationLabel` TextField(8, placeholder `0:24`, hint `Opcional — se muestra como etiqueta.`)
  - `✕` 삭제(확인 다이얼로그 없음)
- `+ Agregar reel` dashed 버튼, 12개 도달 시 비활성
- **저장 전 경고**: poster 없이 영상만 올린 item 이 있으면 그 행에 `<WarnBanner>Este reel necesita una imagen de portada (poster) para guardarse.</WarnBanner>` 를 표시한다 — 백엔드 sanitize 가 poster 없는 item 을 drop 하므로 사용자에게 미리 알린다.
- 새 item 기본값: `{ videoFile: '', posterFile: '', productId: null, durationLabel: '' }`
  </action>
  <verify>
    <automated>cd tienda-app && npx tsc --noEmit && npx eslint src/components/panel/SectionListEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO(Plan 61-11): reels 편집 서브폼" tienda-app/src/components/panel/SectionListEditor.tsx` == 0 (해제됨)
    - `grep -c 'kind="reelVideo"' tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c 'kind="reelPoster"' tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "+ Agregar reel" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "necesita una imagen de portada" tienda-app/src/components/panel/SectionListEditor.tsx` == 1
    - `grep -c "TODO(Plan 61-14): quiz 편집 서브폼" tienda-app/src/components/panel/SectionListEditor.tsx` == 1 (quiz 는 그대로)
    - `cd tienda-app && npx eslint src/components/panel/` exit 0
  </acceptance_criteria>
  <done>reels 영상/poster/상품을 편집·저장할 수 있고 poster 누락을 경고한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `reels.items[].videoFile/posterFile` → `<video>`/`<img>` src | admin 업로드 파일명이 공개 렌더된다 |
| 업로드 영상 → MinIO | 서버 검증(Plan 61-02)이 유일 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-55 | DoS | preload 로 대용량 영상 자동 다운로드 | mitigate | `preload="none"` — 초기 바이트 0, poster 만. acceptance grep 고정 |
| T-61-56 | DoS | MinIO 영상 용량 폭발 | mitigate | 서버 20MB/mp4·webm 강제(Plan 61-02) + poster 필수 sanitize(Plan 61-01) |
| T-61-57 | Tampering | 영상 파일명 경로 조작 | mitigate | `minioImageUrl()` encodeURIComponent + 백엔드 `sanitizeFileName()` |
| T-61-58 | Tampering | poster 없이 저장 시도 | mitigate | 백엔드 sanitize 가 item drop + 에디터 사전 경고 |
| T-61-59 | DoS | `<video>` 다수 동시 재생 | mitigate | 탭 핸들러가 다른 video 전부 `pause()` |
</threat_model>

<verification>
```bash
cd tienda-app && npx tsc --noEmit && npx eslint src/
grep -rn "autoplay" tienda-app/src/components/                  # 0건
grep -rn "dangerouslySetInnerHTML" tienda-app/src/              # 0건
# 스모크: reels publish 후 공개 HTML grep
curl -s "http://localhost:3060/9" | grep -c 'preload="none"'    # >= 1 (reels 켜진 경우)
```
</verification>

<success_criteria>
- reels 가 preload=none + poster 로 렌더되고 탭 재생/단일 재생
- video 에 autoplay 속성 0건
- 영상 0개면 섹션 미렌더, poster 없는 item 은 저장 안 됨(경고 표시)
- SectionRenderer 의 reels case 연결
</success_criteria>

<output>
완료 후 `.planning/phases/61-tienda-online-editor/61-11-SUMMARY.md` 생성
</output>
