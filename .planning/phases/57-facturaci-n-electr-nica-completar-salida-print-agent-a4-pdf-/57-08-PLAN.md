---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 08
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - ventago-app/src/views/facturacion/EmitidasPanel.tsx
  - ventago-app/src/views/facturacion/PartialInvoiceModal.tsx
autonomous: true
requirements: ["R4"]
must_haves:
  truths:
    - "El panel Emitidas muestra un botón 'PDF A4' por comprobante que llama GET /afip/vouchers/:id/pdf y descarga el PDF"
    - "El botón funciona para cualquier comprobante con CAE (incluido NC/ND) sin re-emitir (afip_number no cambia)"
    - "Tras F10 en POS, PartialInvoiceModal muestra un sub-estado de éxito con 'Descargar PDF A4' + 'Continuar'"
    - "Un fallo de descarga muestra Alert inline + toast (ambos, feedback_error_visibility)"
  artifacts:
    - path: "ventago-app/src/views/facturacion/EmitidasPanel.tsx"
      provides: "botón PDF A4 en la celda Acción"
      contains: "responseType: 'blob'"
    - path: "ventago-app/src/views/facturacion/PartialInvoiceModal.tsx"
      provides: "sub-estado post-CAE con descarga A4 + Continuar"
      contains: "Descargar PDF A4"
  key_links:
    - from: "EmitidasPanel.tsx botón PDF A4"
      to: "GET /afip/vouchers/:id/pdf"
      via: "apiConnector.get(path,{responseType:'blob'}) → Blob → anchor download"
      pattern: "vouchers/\\$\\{.*\\}/pdf"
    - from: "PartialInvoiceModal.tsx success sub-state"
      to: "GET /afip/vouchers/:voucherId/pdf"
      via: "Descargar PDF A4 button (mismo patrón blob)"
      pattern: "responseType"
---

<objective>
Añadir el botón "A4 PDF" on-demand (D-06 / R4) en dos touchpoints web: (1) fila del panel Emitidas (`EmitidasPanel.tsx`), botón "PDF A4" que llama `GET /afip/vouchers/:id/pdf` con `responseType:'blob'` y descarga el PDF (patrón blob de `CutTicketTab.tsx`); (2) POS tras F10 (`PartialInvoiceModal.tsx`) — un sub-estado de éxito post-CAE con botón "Descargar PDF A4" + "Continuar" (en vez de cerrar el modal automáticamente). Ambos sin re-emitir CAE (el `afip_number` no cambia). El endpoint ya existe y funciona; este plan es 100% wiring frontend.

Purpose: R4 — el A4 se puede emitir/descargar en cualquier momento tras la emisión, para cualquier comprobante con CAE.
Output: 2 touchpoints web con descarga A4 on-demand.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-UI-SPEC.md

<interfaces>
Patrón blob-download a COPIAR (CutTicketTab.tsx:97-123, ya probado en prod):
```tsx
const resp = await apiConnector.get(`/afip/vouchers/${voucherId}/pdf`, { responseType: 'blob' })
const blob = new Blob([(resp as any).data ?? resp], { type: 'application/pdf' })
const url = URL.createObjectURL(blob)
const a = document.createElement('a')
a.href = url
a.download = `factura-${tipo}-${String(puntoVenta).padStart(5,'0')}-${String(afipNumber).padStart(8,'0')}.pdf`
document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(url)
```
apiConnector.get soporta responseType:'blob' (api.service.ts:155-174). Estado de loading: patrón pdfLoading boolean (setPdfLoading(true)→fetch→finally false).

EmitidasPanel.tsx: celda Acción :66-80 (botones NC/ND). El botón PDF A4 va SIEMPRE visible (a diferencia de NC/ND que se ocultan si v.notaCredito). size="small" variant="outlined" color="primary". Copy: "PDF A4" (idle) / "Generando…" (loading).

PartialInvoiceModal.tsx: output radio :19,108-112; onConfirm success llama onIssued() inmediatamente :45; listener F10/Enter :56-67. Nuevo estado `issued` (voucherId/tipo/afipNumber); en success → setIssued(...) + mantener Dialog abierto con sub-vista (Chip via tipoBadgeColor + "Comprobante emitido — CAE obtenido" + "Descargar PDF A4" + "Continuar"→onIssued()). Guardar el listener F10 para no re-emitir cuando issued está seteado; Esc → Continuar.

Error copy (UI-SPEC): Alert inline "No se pudo generar el PDF. Reintentá o revisá la conexión." + toast.error "PDF A4 falló — reintentá desde Emitidas." (ambos). Colores vía tokens MUI (color="primary"/"error"), nunca hex. tipoBadgeColor de facturacion/tipoBadge.ts para el chip de letra.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Botón "PDF A4" en EmitidasPanel.tsx (descarga blob on-demand)</name>
  <files>ventago-app/src/views/facturacion/EmitidasPanel.tsx</files>
  <read_first>
    - ventago-app/src/views/facturacion/EmitidasPanel.tsx (celda Acción :66-80, columnas puntoVenta/afipNumber :61-62)
    - ventago-app/src/views/talleres/cut-ticket/CutTicketTab.tsx (handlePdf :97-123 — patrón blob)
    - ventago-app/src/services/api.service.ts (get responseType:'blob' :155-174)
    - 57-UI-SPEC.md sección "1. A4 PDF button — EmitidasPanel.tsx"
  </read_first>
  <action>
    Añadir un botón "PDF A4" (size="small" variant="outlined" color="primary") en la celda Acción de cada fila, SIEMPRE visible (independiente de v.notaCredito), en un Box/Stack con gap:1 junto a NC/ND. Implementar `handlePdf(v)` copiando el patrón blob de CutTicketTab: `apiConnector.get(`/afip/vouchers/${v.id}/pdf`, { responseType: 'blob' })` → Blob application/pdf → URL.createObjectURL → anchor download (filename `factura-${v.tipoComprobante}-${String(v.puntoVenta).padStart(5,'0')}-${String(v.afipNumber).padStart(8,'0')}.pdf`) → revokeObjectURL. Estado `pdfLoadingId` (id del voucher en descarga) para swap del label a "Generando…" y disable. En error: Alert inline (severity="error", copy UI-SPEC) + toast.error (copy UI-SPEC) — ambos. Sin toast de éxito (la descarga es la confirmación).
    ESLint warning=error: newline-before-return, lines-around-comment, no-unused-vars. `apiConnector.get` (no remove).
  </action>
  <verify>
    <automated>cd ventago-app && npx eslint src/views/facturacion/EmitidasPanel.tsx --max-warnings=0 && grep -q "responseType: 'blob'" src/views/facturacion/EmitidasPanel.tsx && grep -q "vouchers/" src/views/facturacion/EmitidasPanel.tsx && echo EMITIDAS_OK</automated>
  </verify>
  <done>Botón PDF A4 por fila descarga el PDF vía blob; siempre visible; error muestra Alert+toast; ESLint limpio.</done>
  <acceptance_criteria>
    - `cd ventago-app && npx eslint src/views/facturacion/EmitidasPanel.tsx --max-warnings=0` exit 0
    - `grep -q "responseType: 'blob'" ventago-app/src/views/facturacion/EmitidasPanel.tsx`
    - `grep -Eq "vouchers/\\$\\{.*\\}/pdf" ventago-app/src/views/facturacion/EmitidasPanel.tsx`
    - `grep -q "PDF A4" ventago-app/src/views/facturacion/EmitidasPanel.tsx`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sub-estado post-CAE con "Descargar PDF A4" en PartialInvoiceModal.tsx</name>
  <files>ventago-app/src/views/facturacion/PartialInvoiceModal.tsx</files>
  <read_first>
    - ventago-app/src/views/facturacion/PartialInvoiceModal.tsx (output radio :19,108-112, onConfirm/onIssued :45, listener F10/Enter :56-67)
    - ventago-app/src/views/facturacion/tipoBadge.ts (tipoBadgeColor)
    - EmitidasPanel.tsx (handlePdf de Task 1 — mismo patrón blob)
    - 57-UI-SPEC.md sección "2. Descargar PDF A4 — PartialInvoiceModal.tsx"
  </read_first>
  <action>
    Añadir estado `const [issued, setIssued] = useState<{voucherId:number; tipo:string; afipNumber:number} | null>(null)`. En el success path de onConfirm (donde hoy se llama `onIssued()` inmediatamente :45), en su lugar `setIssued({voucherId: res.voucherId, tipo, afipNumber})` y mantener el Dialog abierto mostrando una sub-vista de éxito (reemplaza el RadioGroup/preview): Chip de letra (via tipoBadgeColor) + Typography "Comprobante emitido — CAE obtenido" + botón "Descargar PDF A4" (mismo patrón blob que Task 1, GET /afip/vouchers/${issued.voucherId}/pdf, estados idle/"Generando…"/error Alert+toast) + botón "Continuar" que llama `onIssued()` (diferido a este punto).
    Guardar el listener F10/Enter (:56-67) para que NO re-dispare onConfirm cuando `issued != null` (evita re-emisión); mapear Esc en este sub-estado a "Continuar" (dismiss), no a "Omitir".
    Error de descarga en este sub-estado: mismo copy "No se pudo generar el PDF…" (NO confundir con error de emisión de CAE — el CAE ya se obtuvo). ESLint warning=error.
  </action>
  <verify>
    <automated>cd ventago-app && npx eslint src/views/facturacion/PartialInvoiceModal.tsx --max-warnings=0 && grep -q "Descargar PDF A4" src/views/facturacion/PartialInvoiceModal.tsx && grep -q "setIssued" src/views/facturacion/PartialInvoiceModal.tsx && echo POS_OK</automated>
  </verify>
  <done>Tras F10 exitoso, el modal muestra el sub-estado de éxito con descarga A4 + Continuar; el listener no re-emite con issued seteado; ESLint limpio.</done>
  <acceptance_criteria>
    - `cd ventago-app && npx eslint src/views/facturacion/PartialInvoiceModal.tsx --max-warnings=0` exit 0
    - `grep -q "Descargar PDF A4" ventago-app/src/views/facturacion/PartialInvoiceModal.tsx`
    - `grep -q "setIssued\|issued" ventago-app/src/views/facturacion/PartialInvoiceModal.tsx`
    - `grep -q "responseType" ventago-app/src/views/facturacion/PartialInvoiceModal.tsx`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → GET /afip/vouchers/:id/pdf | El cliente solicita el PDF por voucherId; el backend scopea por storeId del usuario autenticado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-18 | Information Disclosure / IDOR | Solicitar el PDF de un voucher de otra tienda cambiando el id en la URL | mitigate | La ruta GET /afip/vouchers/:id/pdf ya está @Auth + store-scoped server-side (afip.controller); el frontend no puede saltarse ese scope — sin cambios de backend en este plan |
| T-57-19 | Tampering | Que la descarga re-emita CAE | accept | El endpoint es read-only (regenera PDF desde el voucher existente, afip_number invariante) — SPEC explícito; no hay POST de emisión en este flujo |
</threat_model>

<verification>
- `cd ventago-app && npx eslint src/views/facturacion/EmitidasPanel.tsx src/views/facturacion/PartialInvoiceModal.tsx --max-warnings=0` limpio
- Manual (57-VALIDATION.md): Emitidas ▸ "PDF A4" descarga el PDF sin re-emitir (afip_number invariante); POS tras F10 muestra "Descargar PDF A4" + "Continuar"
</verification>

<success_criteria>
- A4 PDF descargable on-demand desde Emitidas y desde POS tras F10, para cualquier comprobante con CAE (R4)
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-08-SUMMARY.md`
</output>
