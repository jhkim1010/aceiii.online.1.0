---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - api-ventago/src/app/afip/afip-issuer.service.ts
  - api-ventago/src/app/afip/afip-issuer.service.spec.ts
autonomous: true
requirements: ["R6"]
must_haves:
  truths:
    - "resolvePvAndCoolUser(cuit, sucursal) consulta manager.coolsistema.com/api/data/header/cuit/{cuit}/{sucursal} y devuelve {point, coolUser}"
    - "Con caché in-memory por clave cuit:sucursal, TTL 60s (MemoryCacheService)"
    - "Fallback al puntoVenta local si: invoiceSucursal null, header null, branchs vacío, point no finito, o error de red — cada caso logueado"
    - "El CUIT se envía digits-only (sin guiones) al construir la URL"
    - "La llamada al manager es un try/catch aislado, sin retry acoplado a la emisión de CAE (anti doble-CAE)"
  artifacts:
    - path: "api-ventago/src/app/afip/afip-issuer.service.ts"
      provides: "resolvePvAndCoolUser(cuit, sucursal) con caché + fallback"
      exports: ["resolvePvAndCoolUser"]
    - path: "api-ventago/src/app/afip/afip-issuer.service.spec.ts"
      provides: "unit tests cache hit/miss/fallback/null/empty-branchs"
  key_links:
    - from: "afip-issuer.service.ts::resolvePvAndCoolUser"
      to: "manager.coolsistema.com/api/data/header"
      via: "axios GET (isolated try/catch, non-throwing)"
      pattern: "data/header/cuit"
    - from: "resolvePvAndCoolUser"
      to: "MemoryCacheService"
      via: "get/set clave afip-header:cuit:sucursal TTL 60000"
      pattern: "afip-header:"
---

<objective>
Implementar la resolución del punto de venta (PV) y `coolUser` vía el gateway `manager` (D-07 / R6): un método `resolvePvAndCoolUser(cuit, sucursal)` en `afip-issuer.service.ts` que consulta `GET https://manager.coolsistema.com/api/data/header/cuit/{cuit}/{sucursal}`, extrae `branchs[0].point` + `coolUser`, con caché in-memory (MemoryCacheService, TTL 60s, clave `cuit:sucursal`) y fallback al `puntoVenta` local con log. El endpoint YA fue verificado en vivo (57-RESEARCH.md D-07 Verified); este plan incluye una tarea de RE-VERIFICACIÓN en vivo antes de cablear (gate D-07). El wiring dentro de `issueForSale` es del Plan 06.

Purpose: "El punto de venta lo decide invoice" (intención del usuario). Sin esto, el provider siempre envía el `point` local.
Output: resolvePvAndCoolUser + spec; wiring lo consume Plan 06.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-RESEARCH.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
Endpoint VERIFICADO en vivo (57-RESEARCH.md, 2026-07-20):
`GET https://manager.coolsistema.com/api/data/header/cuit/{cuit}/{sucursal}`
→ 200 `{ cuit, coolUser, name, condition, branchs:[{branchId, point, alias, comercialAddress}], ... }`
→ 200 `null` (CUIT no registrado)
→ 200 `{ branchs: [] }` (CUIT registrado, sucursal sin branch)
Sin auth (GET público, CORS *). Host = manager (NO invoice.coolsistema.com, que da 404). CUIT digits-only (con guiones → null).

Extracción point (CoolSyncro rest-gateway-provider.js:179-183):
```javascript
let point = null;
const branchs = header && Array.isArray(header.branchs) ? header.branchs : [];
if (branchs[0] && Number.isFinite(Number(branchs[0].point))) point = Number(branchs[0].point);
```

Caché idiom Ventago (sizes.service.ts / vto.service.ts):
```typescript
import { MemoryCacheService } from 'src/common/cache/memory-cache.service';
// get<T>(key), set(key, value, ttlMs), del(key), delByPrefix(prefix)
const CACHE_TTL = 60_000;
const cacheKey = `afip-header:${cuit}:${sucursal}`;
```
MemoryCacheService en api-ventago/src/common/cache/memory-cache.service.ts (+ memory-cache.module.ts). Verificar que AfipModule importa el módulo de cache (o importarlo).

loadIssuer actual (afip-issuer.service.ts:13-25) y loadIssuerByBranch — puntos de integración (el wiring lo hace Plan 06). axios ya usado por rest-gateway.provider.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RE-VERIFICAR endpoint manager en vivo (gate D-07)</name>
  <files>(sin archivos — verificación en vivo, resultado al SUMMARY)</files>
  <read_first>
    - 57-RESEARCH.md sección "D-07 Verified — Gateway Manager Endpoint" (host/ruta/shape/edge cases)
    - 57-CONTEXT.md D-07 (warning: invoice.coolsistema.com dio 404; confirmar host manager)
  </read_first>
  <action>
    ANTES de cablear, re-verificar en vivo el endpoint (gate obligatorio del phase_ordering). Ejecutar un GET al host manager con un CUIT+sucursal de prueba y confirmar shape JSON `{coolUser, branchs:[{point}]}`. Si el endpoint responde 200 con esa shape → proceder con Task 2. Si responde 404 / HTML SPA / cambió → NO cablear la llamada; implementar resolvePvAndCoolUser devolviendo SIEMPRE el fallback local (point local) y documentar el bloqueo en el SUMMARY para decisión del usuario (comportamiento por defecto = envío de `point` local, per D-07). Registrar el resultado (host, ruta, status, shape) en el SUMMARY.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code} %{content_type}" "https://manager.coolsistema.com/api/data/header/cuit/30710419414/1" | tee /dev/stderr | grep -q "200 application/json" && echo MANAGER_LIVE_OK || echo MANAGER_FALLBACK_MODE</automated>
  </verify>
  <done>Resultado de la verificación registrado en el SUMMARY (host/ruta/status/shape). Si no es 200 JSON, Task 2 implementa modo fallback-only.</done>
  <acceptance_criteria>
    - El comando curl del verify imprime `MANAGER_LIVE_OK` o `MANAGER_FALLBACK_MODE`
    - El SUMMARY registra explícitamente cuál de los dos caminos se tomó
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolvePvAndCoolUser (caché 60s + fallback + defensivo) + spec</name>
  <files>api-ventago/src/app/afip/afip-issuer.service.ts, api-ventago/src/app/afip/afip-issuer.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/afip-issuer.service.ts (loadIssuer :13-25, constructor/DI)
    - api-ventago/src/app/afip/afip-issuer.service.spec.ts (patrón de test existente)
    - api-ventago/src/common/cache/memory-cache.service.ts (get/set/del)
    - api-ventago/src/app/afip/providers/rest-gateway.provider.ts (uso de axios + classifyTransportError, anti doble-CAE)
    - /Users/marcoskim/Trabajos_Programming/CoolSyncro/src/main/afip/rest-gateway-provider.js (getHeader :138-183)
    - 57-PATTERNS.md sección "afip-issuer.service.ts (MODIFIED — resolvePvAndCoolUser)"
  </read_first>
  <behavior>
    - resolvePvAndCoolUser(cuit, sucursal): si sucursal es null/undefined → devolver null (el caller usa puntoVenta local; skip HTTP, sin latencia).
    - cuit se normaliza digits-only antes de la URL.
    - cache hit (clave afip-header:cuit:sucursal) → devuelve el valor cacheado sin HTTP.
    - cache miss → GET manager; 200 con branchs[0].point finito y coolUser → {point, coolUser}, cachea 60s, devuelve.
    - header === null → null (fallback), log.
    - branchs === [] → null (fallback), log.
    - branchs[0].point no finito → null (fallback), log.
    - error de red/timeout → null (fallback), log. NUNCA lanza (non-throw). NUNCA reintenta acoplado al CAE POST.
  </behavior>
  <action>
    Inyectar `MemoryCacheService` en el constructor de `AfipIssuerService` (si AfipModule no importa MemoryCacheModule, importarlo). Implementar `async resolvePvAndCoolUser(cuit: string, sucursal: number | null): Promise<{point:number; coolUser:string} | null>` siguiendo el behavior. Normalizar `const c = String(cuit).replace(/\D/g,'')`. URL `https://manager.coolsistema.com/api/data/header/cuit/${c}/${Number(sucursal)}`. Usar axios con timeout (~4000ms). try/catch AISLADO (non-throw) — devolver null en cualquier fallo/edge, con `this.logger?.warn(...)` o console indicando la condición de fallback (invoiceSucursal null / header null / branchs vacío / point no finito / red). NO usar `pool.connect()` — es HTTP externo, no DB. Cachear solo resultados válidos (no cachear null-fallback para permitir reintento tras 60s de recuperación del gateway).
    Escribir spec `afip-issuer.service.spec.ts` con axios mockeado: casos hit/miss-ok/null/empty-branchs/non-finite-point/network-error → todos devuelven lo esperado; verificar que set() se llama con TTL 60000 solo en el caso ok; verificar que sucursal null hace skip del GET.
    ESLint/tsc: sin imports sin usar; línea en blanco antes de return/comentarios (aunque el gate real es tsc/jest en backend).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/afip-issuer.service --silent</automated>
  </verify>
  <done>resolvePvAndCoolUser implementado con caché 60s, digits-only, todos los fallbacks logueados y non-throw; spec verde cubriendo hit/miss/null/empty/non-finite/error/skip.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/afip-issuer.service --silent` exit 0
    - `grep -q "resolvePvAndCoolUser" api-ventago/src/app/afip/afip-issuer.service.ts`
    - `grep -q "afip-header:" api-ventago/src/app/afip/afip-issuer.service.ts`
    - `grep -Eq "replace\(/\\\\D/g" api-ventago/src/app/afip/afip-issuer.service.ts` (digits-only)
    - `grep -q "manager.coolsistema.com" api-ventago/src/app/afip/afip-issuer.service.ts`
    - El spec incluye un caso de branchs vacío y uno de network-error que devuelven null (fallback)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api-ventago → manager.coolsistema.com | Llamada HTTP saliente no autenticada (por diseño del gateway). El CUIT/sucursal provienen de afip_issuers (admin-gestionado), no de input de usuario final. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-08 | Repudiation / Tampering (doble-CAE) | Llamada al manager reintentando dentro del loop que envuelve el CAE POST | mitigate | resolvePvAndCoolUser es try/catch AISLADO, non-throw, sin retry acoplado a issueCae; un fallo → fallback local + log, jamás dispara un segundo intento de emisión |
| T-57-09 | Tampering (SSRF-adjacent) | Construcción de URL con cuit/sucursal sin sanitizar | mitigate | cuit → digits-only (replace /\D/g), sucursal → Number(); ambos originados en afip_issuers admin, no user input |
| T-57-10 | Information Disclosure | Cachear/cruzar branchs de distinta sucursal del mismo CUIT | mitigate | Clave de caché `${cuit}:${sucursal}` (no solo cuit) — verificado que distintas sucursales devuelven branchs distintos |
</threat_model>

<verification>
- `cd api-ventago && npx jest src/app/afip/afip-issuer.service --silent` verde
- curl de re-verificación del endpoint registrado en SUMMARY
- Manual (57-VALIDATION.md): con invoice_sucursal seteado, el point proviene de branchs[0].point; header caído → fallback local + log
</verification>

<success_criteria>
- resolvePvAndCoolUser resuelve PV/coolUser vía manager con caché y fallback robusto (R6)
- Endpoint re-verificado en vivo antes de cablear (gate D-07)
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-05-SUMMARY.md`
</output>
