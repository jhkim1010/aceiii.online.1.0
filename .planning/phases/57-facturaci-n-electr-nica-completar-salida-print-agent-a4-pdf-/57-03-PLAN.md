---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api-ventago/migrations/afip-issuer-invoice-type.sql
  - api-ventago/src/app/afip/models/afip-issuer.model.ts
  - api-ventago/src/app/afip/dto/upsert-issuer.dto.ts
  - ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx
autonomous: true
requirements: ["R5"]
must_haves:
  truths:
    - "afip_issuers.invoice_type existe en 5432 (local) y 5434 (prod), varchar(1) default 'A'"
    - "El DTO acepta invoiceType con validación @IsIn(['A','M']); el modelo lo persiste vía spread {...dto}"
    - "En ModalBranch el selector A/M aparece SOLO cuando ivaCondition === 'RI'"
    - "Para MONO/EXENTO el selector no se renderiza (no solo deshabilitado)"
  artifacts:
    - path: "api-ventago/migrations/afip-issuer-invoice-type.sql"
      provides: "ADD COLUMN invoice_type varchar(1) DEFAULT 'A'"
      contains: "invoice_type"
    - path: "api-ventago/src/app/afip/models/afip-issuer.model.ts"
      provides: "columna invoiceType"
      contains: "invoiceType"
    - path: "api-ventago/src/app/afip/dto/upsert-issuer.dto.ts"
      provides: "campo invoiceType @IsIn(['A','M'])"
      contains: "invoiceType"
    - path: "ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx"
      provides: "selector A/M RI-gated"
      contains: "invoiceType"
  key_links:
    - from: "ModalBranch.tsx"
      to: "PUT /afip/issuers/by-branch/:branchId"
      via: "onSubmit payload incluye invoiceType"
      pattern: "invoiceType"
    - from: "ModalBranch.tsx watch('ivaCondition')"
      to: "render condicional del selector"
      via: "watch === 'RI'"
      pattern: "watch\\('ivaCondition'\\)"
---

<objective>
Scaffolding D-08 de la selección Factura A/M por sucursal: (1) migración `afip-issuer-invoice-type.sql` (ADD COLUMN, aplicar 5432 + 5434); (2) columna `invoiceType` en el modelo Sequelize; (3) campo `invoiceType` en el DTO con `@IsIn(['A','M'])`; (4) selector A/M en `ModalBranch.tsx` visible SOLO para `ivaCondition === 'RI'`. El wiring runtime de `decideComprobante` (gate IVA=RI) es del Plan 06 — este plan solo deja persistencia + UI.

Purpose: Cada sucursal RI decide si emite A o solo M. Replica exacta del patrón `invoiceSucursal` aterrizado esta sesión (misma tabla, mismo triple migración+modelo+DTO).
Output: migración commiteada, modelo/DTO extendidos, selector RI-gated en ModalBranch.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-UI-SPEC.md

<interfaces>
Migración patrón exacto (copiar de api-ventago/migrations/afip-issuer-invoice-sucursal.sql):
`ALTER TABLE afip_issuers ADD COLUMN IF NOT EXISTS invoice_type varchar(1) DEFAULT 'A';` + COMMENT ON COLUMN.

Modelo (afip-issuer.model.ts:31-33, patrón invoiceSucursal):
`@Column({ type: DataType.STRING(1), allowNull: false, defaultValue: 'A' }) invoiceType: string;`

DTO (upsert-issuer.dto.ts, patrón ivaCondition @IsIn):
`@IsOptional() @IsIn(['A', 'M']) invoiceType?: string;`

ModalBranch (patrón ivaCondition select :220-233; invoiceSucursal :206; reset :64-74; onSubmit destructure+PUT :89-113). El archivo hoy NO usa watch() — añadirlo al destructure de useForm. Ya importa Controller/useForm.
Copy exacto UI-SPEC: label "Tipo de factura habilitada", opciones "Puede emitir Factura A" (A, default) / "Solo Factura M" (M); helper "Solo aplica a receptores con CUIT (venta a Responsable Inscripto). Con 'Solo Factura M', esas ventas emiten Factura M en lugar de A."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración invoice_type + modelo + DTO (dual-apply 5432/5434)</name>
  <files>api-ventago/migrations/afip-issuer-invoice-type.sql, api-ventago/src/app/afip/models/afip-issuer.model.ts, api-ventago/src/app/afip/dto/upsert-issuer.dto.ts</files>
  <read_first>
    - api-ventago/migrations/afip-issuer-invoice-sucursal.sql (patrón exacto a copiar)
    - api-ventago/src/app/afip/models/afip-issuer.model.ts (:31-33 invoiceSucursal)
    - api-ventago/src/app/afip/dto/upsert-issuer.dto.ts (:16-29 invoiceSucursal + ivaCondition @IsIn)
    - CLAUDE.md sección "DB 마이그레이션 적용 규칙" (5432 + 5434 simultáneo)
  </read_first>
  <action>
    Crear `api-ventago/migrations/afip-issuer-invoice-type.sql` con el ADD COLUMN IF NOT EXISTS invoice_type varchar(1) DEFAULT 'A' + COMMENT. Owner/sequence transfer NO necesario (solo ADD COLUMN, la tabla mantiene owner).
    Añadir la columna `invoiceType` al modelo `afip-issuer.model.ts` (patrón invoiceSucursal, DataType.STRING(1), allowNull:false, defaultValue:'A'). Importar DataType si aún no está.
    Añadir `invoiceType?: string` al DTO `upsert-issuer.dto.ts` con `@IsOptional()` + `@IsIn(['A','M'])`.
    APLICACIÓN DUAL (obligatoria, CLAUDE.md): (a) prod 5434 vía SSH — `ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -v ON_ERROR_STOP=1 --single-transaction -f -" < api-ventago/migrations/afip-issuer-invoice-type.sql`; (b) local 5432 — el sandbox NO alcanza el Mac local, ENTREGAR al usuario el comando `psql -p 5432 -d ventago -f api-ventago/migrations/afip-issuer-invoice-type.sql` en el SUMMARY. No cerrar como "aplicado en local" sin confirmación del usuario.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "invoice_type" migrations/afip-issuer-invoice-type.sql && grep -q "invoiceType" src/app/afip/models/afip-issuer.model.ts && grep -q "invoiceType" src/app/afip/dto/upsert-issuer.dto.ts && (npx tsc --noEmit -p . 2>&1 | grep -E "afip-issuer.model|upsert-issuer" || echo TSC_OK)</automated>
  </verify>
  <done>Migración creada con ADD COLUMN default 'A'; modelo y DTO extendidos; tsc sin errores; comando 5432 entregado al usuario y 5434 aplicado vía SSH (o registrado como pendiente de confirmación).</done>
  <acceptance_criteria>
    - `grep -q "invoice_type varchar(1) DEFAULT 'A'" api-ventago/migrations/afip-issuer-invoice-type.sql`
    - `grep -q "invoiceType" api-ventago/src/app/afip/models/afip-issuer.model.ts`
    - `grep -Eq "@IsIn\(\['A', ?'M'\]\)" api-ventago/src/app/afip/dto/upsert-issuer.dto.ts`
    - El SUMMARY documenta el comando de aplicación 5432 para el usuario Y el estado de 5434
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Selector A/M RI-gated en ModalBranch.tsx (D-08 UI)</name>
  <files>ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx</files>
  <read_first>
    - ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx (useForm :40, reset :64-74, onSubmit :89-113, ivaCondition select :220-233, invoiceSucursal :206)
    - 57-UI-SPEC.md sección "3. A/M selector — ModalBranch.tsx" (copy exacto, gate, Controller)
    - 57-PATTERNS.md sección ModalBranch.tsx (RI-gating, watch)
  </read_first>
  <action>
    Añadir `watch` al destructure de `useForm()` (hoy no está). Añadir `invoiceType` a `reset({...})` (:64-74) leyendo `issuer.invoiceType ?? 'A'`, y a defaults/schema si aplica (default 'A'). En `onSubmit`, añadir `invoiceType` al destructure de `data` y al payload del `PUT /afip/issuers/by-branch/:branchId` (mismo spread que invoiceSucursal :106-107).
    Renderizar el selector SOLO cuando `watch('ivaCondition') === 'RI'`, directamente debajo del select "Condición IVA" (:220-233). Usar `Controller` (react-hook-form) + `RadioGroup` con 2 opciones (copy exacto UI-SPEC): value 'A' = "Puede emitir Factura A" (default), value 'M' = "Solo Factura M". Label del grupo: "Tipo de factura habilitada". Helper: "Solo aplica a receptores con CUIT (venta a Responsable Inscripto). Con 'Solo Factura M', esas ventas emiten Factura M en lugar de A." Colores vía tokens MUI (color="primary") — NUNCA hex (UI-SPEC).
    ESLint warning=error: línea en blanco antes de `return` y antes de comentarios `//`; sin imports sin usar. Usar `apiConnector.put`.
  </action>
  <verify>
    <automated>cd ventago-app && npx eslint src/views/admin/stores/details/components/ModalBranch.tsx --max-warnings=0 && grep -q "watch('ivaCondition')" src/views/admin/stores/details/components/ModalBranch.tsx && grep -q "invoiceType" src/views/admin/stores/details/components/ModalBranch.tsx && echo MODALBRANCH_OK</automated>
  </verify>
  <done>El selector A/M se renderiza solo para RI, envía invoiceType en el PUT, y ESLint pasa con max-warnings=0.</done>
  <acceptance_criteria>
    - `cd ventago-app && npx eslint src/views/admin/stores/details/components/ModalBranch.tsx --max-warnings=0` exit 0
    - `grep -q "watch('ivaCondition') === 'RI'" ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx`
    - `grep -q "Solo Factura M" ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx`
    - `grep -q "Puede emitir Factura A" ventago-app/src/views/admin/stores/details/components/ModalBranch.tsx`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend form → PUT /afip/issuers/by-branch | El cliente envía `invoiceType`; el backend debe validar (@IsIn) y scopear por storeId. La visibilidad RI es UX — la regla real se re-valida server-side (Plan 06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Tampering / Input Validation | Cliente enviando invoiceType arbitrario (ej. 'X') o para sucursal no-RI | mitigate | DTO `@IsIn(['A','M'])` rechaza valores fuera de A/M; el gate real IVA=RI se aplica en decideComprobante (Plan 06), nunca se confía en el gate visual del cliente |
| T-57-07 | Information Disclosure / IDOR | Actualizar issuer de otra tienda vía by-branch | mitigate | La ruta PUT /afip/issuers/by-branch ya scopea por `{id, storeId: user.storeId}` (patrón existente afip.controller :104-117) — sin cambios; verificar que el nuevo campo no bypassa ese where |
</threat_model>

<verification>
- `cd ventago-app && npx eslint src/views/admin/stores/details/components/ModalBranch.tsx --max-warnings=0` limpio
- `cd api-ventago && npx tsc --noEmit -p .` sin errores nuevos
- Migración presente en `migrations/` y aplicada a 5434; comando 5432 entregado al usuario
- Manual (57-VALIDATION.md): ModalBranch con RI muestra el selector; con MONO/EXENTO no aparece
</verification>

<success_criteria>
- Columna invoice_type persistida (5432+5434), modelo/DTO extendidos
- Selector A/M RI-gated funcional en ModalBranch (R5 UI/persistencia)
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-03-SUMMARY.md`
</output>
