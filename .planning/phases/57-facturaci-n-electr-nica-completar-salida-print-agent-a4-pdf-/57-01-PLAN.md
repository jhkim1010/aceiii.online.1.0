---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - api-ventago/src/app/afip/build-factura.ts
  - api-ventago/src/app/afip/code-maps.ts
  - api-ventago/src/app/afip/build-factura.spec.ts
  - api-ventago/src/app/afip/code-maps.spec.ts
autonomous: true
requirements: ["R1", "R3", "R5"]
must_haves:
  truths:
    - "buildFactura devuelve el objeto factura D-02 con letra/cod/emisor/receptor/items/neto/iva21/total/ivaDiscrim/cae/caeVto/qrUrl"
    - "Las líneas de items se reconstruyen con applyPartial (idénticas al preview F10), Σsubtotal === voucher.impTotal"
    - "Para letra A/M ivaDiscrim=true con neto+iva21 discriminados; para B ivaDiscrim=false"
    - "decideComprobante('RI',{configInvoiceType:'M',docNro:11dígitos}) devuelve 'M'; con configInvoiceType 'A' o undefined devuelve 'A'"
  artifacts:
    - path: "api-ventago/src/app/afip/build-factura.ts"
      provides: "Fuente única buildFactura(voucher, sale, issuer) → D-02 shape (D-04)"
      exports: ["buildFactura", "Factura"]
    - path: "api-ventago/src/app/afip/code-maps.ts"
      provides: "condIvaLabel display map + decideComprobante A/M (ya existente)"
      contains: "condIvaLabel"
    - path: "api-ventago/src/app/afip/build-factura.spec.ts"
      provides: "Unit tests D-04/D-05 line reconstruction + receptor A/M vs B"
  key_links:
    - from: "build-factura.ts"
      to: "partial-invoice.ts::applyPartial"
      via: "import + call para escalar líneas"
      pattern: "applyPartial\\("
    - from: "build-factura.ts"
      to: "code-maps.ts::letraOf + computeNetoIva"
      via: "import canónico (nunca duplicar)"
      pattern: "letraOf|computeNetoIva"
---

<objective>
Crear la fuente única `buildFactura(voucher, sale, issuer)` (D-04) que produce el objeto `factura` estructurado D-02, consumido tanto por el thermal (`fiscal-formatter`) como por el A4 (`a4-generator`). Cero lógica de comprobante duplicada. Añadir el mapa de etiquetas `condIvaLabel` a `code-maps.ts` (Open Question 3) y cubrir con unit tests el A/M gate de `decideComprobante`.

Purpose: Sin este objeto centralizado, thermal y A4 re-implementarían las reglas AFIP por separado (drift garantizado). Es la base de W1 (Plan 02) y W2 (Plan 04).
Output: `build-factura.ts` (nuevo, puro), `code-maps.ts` (mapa etiquetas), specs.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
<!-- Contratos que el executor usa directamente — no explorar el codebase. -->

Payload D-02 (destino de buildFactura), verbatim de CONTEXT.md D-02:
```typescript
export interface FacturaItem { cant: number; desc: string; pUnit: number; subtotal: number; }
export interface Factura {
  letra: 'A' | 'B' | 'M';
  cod: string;                 // '01','06','51'... padStart(2,'0')
  number: string;              // PV(5) - número(8), p.ej. "00010-00000123"
  emisor: { razonSocial: string; cuit: string; domicilio: string; condIva: string; iibb: string; inicioAct: string };
  receptor:                    // A/M = identidad completa; B = Consumidor Final
    | { tipo: 'identificado'; razonSocial: string; cuit: string; condIva: string; domicilio: string }
    | { tipo: 'consumidorFinal'; doc?: string };
  items: FacturaItem[];
  neto: number;
  iva21: number;
  total: number;
  ivaDiscrim: boolean;         // true solo para A/M
  cae: string;
  caeVto: string;
  qrUrl: string;
}
```

Helpers canónicos existentes a reusar (NO reimplementar) — de code-maps.ts:
```typescript
export function letraOf(cbteTipo: number): string;   // 1,2,3→'A'; 51,52,53→'M'; 11,12,13→'C'; 19,20,21→'E'; else 'B'
export function computeNetoIva({ tpago, ivaBase }: { tpago: number; ivaBase: number }): { neto: number; impuesto: number }; // BigInt exact
export function decideComprobante(ivaCondition: string, { docNro, resiva, configInvoiceType }): string; // ya tiene rama A/M en :147-174
```

De partial-invoice.ts (D-05, reusar exactamente):
```typescript
import { applyPartial } from './partial-invoice';
const { lines, impTotal } = applyPartial(items, Number(sale.totalAmount), voucher.invoicePct);
```

Patrón existente para líneas + receptor (afip-voucher.service.ts:178-201, buildPartialItems/resolveReceptor):
```typescript
private buildPartialItems(sale: Sale): PartialLineInput[] {
  return (sale.items || []).map((si) => ({
    cantidad: Number(si.quantity),
    precioUnitario: Number(si.price),
    subtotal: Number(si.subtotal),
    descripcion: si.product?.name || si.customName || 'Ítem',
  }));
}
```
Modelos: GlobalClient/Clients tienen fullname, document, address, resIva (verificado). storeClient.globalClient tiene prioridad sobre client legacy (Phase 25 read-precedence).

QR: reusar qr-builder.ts (buildQrUrl) del backend para el campo qrUrl.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: condIvaLabel map + buildFactura (fuente única D-04)</name>
  <files>api-ventago/src/app/afip/code-maps.ts, api-ventago/src/app/afip/build-factura.ts</files>
  <read_first>
    - api-ventago/src/app/afip/code-maps.ts (decideComprobante :147-174, letraOf :222-240, computeNetoIva :193-219, COND_IVA_RECEPTOR export)
    - api-ventago/src/app/afip/afip-voucher.service.ts (buildPartialItems :178-201, resolveReceptor, loadSaleWithReceptor :155-176)
    - api-ventago/src/app/afip/partial-invoice.ts (applyPartial signature)
    - api-ventago/src/app/afip/qr-builder.ts (buildQrUrl signature)
    - 57-PATTERNS.md sección "build-factura.ts (NEW)" (excerpts verbatim)
    - 57-RESEARCH.md Open Question 3 (condIva display-label map)
  </read_first>
  <behavior>
    - buildFactura(voucher, sale, issuer) devuelve el objeto Factura D-02 completo.
    - items = applyPartial(buildPartialItems(sale), Number(sale.totalAmount), voucher.invoicePct).lines mapeadas a {cant,desc,pUnit,subtotal}.
    - letra = letraOf(voucher.tipoComprobante); cod = String(numTipo/tipoComprobante).padStart(2,'0').
    - ivaDiscrim = (letra === 'A' || letra === 'M'). Si ivaDiscrim → {neto,impuesto}=computeNetoIva({tpago: total, ivaBase: 1.21}); si no, neto=total, iva21=0.
    - receptor: si letra A/M → {tipo:'identificado', razonSocial, cuit, condIva, domicilio} desde storeClient.globalClient (prioridad) o client legacy; si B → {tipo:'consumidorFinal', doc?}.
    - condIva del receptor usa condIvaLabel (nuevo map) — NUNCA un código crudo.
    - qrUrl = buildQrUrl(...) del qr-builder existente.
    - assert de sanidad: impTotal reconstruido === Number(voucher.impTotal).
  </behavior>
  <action>
    En `code-maps.ts` añadir `export const condIvaLabel: Record<string, string>` (o keyed por COND_IVA_RECEPTOR) que mapea RI→'Responsable Inscripto', MONO→'Responsable Monotributo', EXENTO→'Exento', CF→'Consumidor Final'. Respetar ESLint: línea en blanco antes de comentarios `//` y antes de `return`.

    Crear `build-factura.ts` con `export interface Factura`/`FacturaItem` (shape del bloque interfaces) y `export function buildFactura(voucher, sale, issuer): Factura`. Función PURA (recibe `sale` ya eager-loaded con items+product+storeClient.globalClient+client; NO consulta DB — la carga la hace Plan 04). Usar `applyPartial` (D-05), `letraOf`/`computeNetoIva` canónicos (NO duplicar — prohibido copiar la privada `letra()` de afip-output.service.ts:93). Receptor A/M vs B por letra (patrón thermal-generator.js receptorLines de PATTERNS). `emisor` desde issuer.{razonSocial,cuit,domicilio,ivaCondition→condIvaLabel,ingresosBrutos,inicioActividad}. `qrUrl` desde qr-builder. `number` = `${String(voucher.puntoVenta).padStart(5,'0')}-${String(voucher.afipNumber).padStart(8,'0')}`. Normalizar cuit del emisor con `String(cuit).replace(/\D/g,'')` para el QR. Añadir un `if (Math.round(impTotalReconstruido*100) !== Math.round(Number(voucher.impTotal)*100)) { /* log warning, no throw */ }` como sanity check (D-05 coherencia con imp_total).
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p . 2>&1 | grep -E "build-factura|code-maps" || echo "TSC OK (afip)"</automated>
  </verify>
  <done>build-factura.ts existe y exporta buildFactura+Factura; code-maps.ts exporta condIvaLabel; tsc sin errores en los archivos tocados; buildFactura no importa la clase Sequelize para consultar (pura).</done>
  <acceptance_criteria>
    - `grep -q "export function buildFactura" api-ventago/src/app/afip/build-factura.ts`
    - `grep -q "condIvaLabel" api-ventago/src/app/afip/code-maps.ts`
    - `grep -Eq "applyPartial|letraOf|computeNetoIva" api-ventago/src/app/afip/build-factura.ts` (reusa canónicos)
    - `! grep -q "preuni/1.21\|/ 1.21" api-ventago/src/app/afip/build-factura.ts` (no re-deriva IVA a mano; usa computeNetoIva)
    - `grep -q "ivaDiscrim" api-ventago/src/app/afip/build-factura.ts`
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: specs — build-factura + decideComprobante A/M gate (R5 unit)</name>
  <files>api-ventago/src/app/afip/build-factura.spec.ts, api-ventago/src/app/afip/code-maps.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/code-maps.spec.ts (patrón de test existente)
    - api-ventago/src/app/afip/partial-invoice.spec.ts (fixtures de items)
    - build-factura.ts (Task 1)
  </read_first>
  <behavior>
    - build-factura.spec: Factura A con ≥2 items → items.length===2, ivaDiscrim===true, neto+iva21===total (±1 cent), receptor.tipo==='identificado'. Factura B → ivaDiscrim===false, receptor.tipo==='consumidorFinal'. Σsubtotal===impTotal (D-05).
    - code-maps.spec: decideComprobante('RI',{configInvoiceType:'M',docNro:'20111111112'})==='M'; ('RI',{configInvoiceType:'A',docNro:'20111111112'})==='A'; ('RI',{docNro:'20111111112'})==='A' (undefined→A); ('MONO',{configInvoiceType:'M',docNro:'20111111112'})==='C' (gate ignorado, no RI).
  </behavior>
  <action>
    Crear `build-factura.spec.ts` con fixtures mock de Sale (items con product.name, storeClient.globalClient con fullname/document/address/resIva) y AfipVoucher/AfipIssuer mock. Aserciones sobre la shape D-02 (ver behavior). Extender `code-maps.spec.ts` con un describe('decideComprobante A/M gate') cubriendo los 4 casos del behavior. Respetar ESLint (líneas en blanco). No usar DB real — objetos planos.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/build-factura src/app/afip/code-maps --silent</automated>
  </verify>
  <done>Ambos spec verdes; cubren D-05 (Σsubtotal===impTotal), ivaDiscrim por letra, receptor A/M vs B, y los 4 casos del A/M gate.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/build-factura src/app/afip/code-maps --silent` exit 0
    - `grep -q "consumidorFinal" api-ventago/src/app/afip/build-factura.spec.ts`
    - `grep -Eq "'M'|=== 'M'" api-ventago/src/app/afip/code-maps.spec.ts`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch → buildFactura | buildFactura recibe un `sale` ya cargado; NO consulta DB. El scoping por storeId es responsabilidad del cargador (Plan 04) — buildFactura no debe abrir una vía de bypass consultando por su cuenta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | buildFactura leyendo Sale de otra tienda | mitigate | buildFactura NO consulta DB (función pura); el `sale` llega ya scopeado `{storeId,id}` desde Plan 04 — documentar en comentario que el caller debe pasar un sale store-scoped |
| T-57-02 | Tampering | Duplicar reglas AFIP (letra/IVA) creando drift | mitigate | Importar `letraOf`/`computeNetoIva`/`decideComprobante` canónicos; test que verifica que NO existe división `/1.21` manual en build-factura.ts |
</threat_model>

<verification>
- `cd api-ventago && npx jest src/app/afip/build-factura src/app/afip/code-maps --silent` verde
- `cd api-ventago && npx tsc --noEmit -p .` sin errores nuevos en afip
</verification>

<success_criteria>
- buildFactura devuelve la shape D-02 completa reusando applyPartial + helpers canónicos
- condIvaLabel disponible para el bloque receptor
- A/M gate de decideComprobante cubierto por unit tests (R5)
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-01-SUMMARY.md`
</output>
