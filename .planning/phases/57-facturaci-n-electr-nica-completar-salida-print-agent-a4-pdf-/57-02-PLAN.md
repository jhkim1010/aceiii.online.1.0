---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - print-agent/src/fiscal-formatter.js
  - print-agent/main.js
autonomous: true
requirements: ["R1", "R2"]
must_haves:
  truths:
    - "El ticket ESC/POS de una Factura A imprime letra 'A' + COD, ítems, IVA 21% discriminado, CAE, Vto CAE y QR escaneable"
    - "El ticket de una Factura B no muestra columna IVA y muestra 'Consumidor Final'"
    - "El QR se renderiza como imagen escaneable (QRCode.toDataURL), no como texto de URL"
    - "Los tickets de control no-AFIP existentes siguen imprimiendo sin regresión"
  artifacts:
    - path: "print-agent/src/fiscal-formatter.js"
      provides: "formatFiscalHtml(factura) async que consume la shape D-02 y renderiza el comprobante"
      contains: "QRCode.toDataURL"
    - path: "print-agent/main.js"
      provides: "print_invoice handler que ramifica en payload.factura"
      contains: "payload.factura"
  key_links:
    - from: "print-agent/main.js print_invoice handler"
      to: "fiscal-formatter.js::formatFiscalHtml"
      via: "if (payload.factura) then formatFiscalHtml + renderHtmlToPng + printImage"
      pattern: "formatFiscalHtml\\(payload.factura"
    - from: "fiscal-formatter.js"
      to: "qrcode (QRCode.toDataURL)"
      via: "render QR imagen desde factura.qrUrl"
      pattern: "QRCode.toDataURL"
---

<objective>
Reescribir `print-agent/src/fiscal-formatter.js` para consumir la shape `factura` D-02 (del backend, Plan 01) y renderizar el comprobante ESC/POS completo: letra chip + `COD. NN`, N° comprobante, fecha, bloque emisor, bloque receptor (A/M identidad completa vs B Consumidor Final), tabla de ítems, para A/M subtotal neto + IVA 21% discriminado, TOTAL, `CAE N°`, `Vencimiento CAE`, QR RG 4892 como imagen escaneable, y leyenda "Comprobante autorizado AFIP/ARCA". Cablear `main.js` para que el handler `print_invoice` ramifique en la presencia de `payload.factura` (WIRING GAP #1: hoy siempre llama `printTicket`/`formatInvoiceHtml`, nunca el path fiscal).

Purpose: El formatter es código muerto hoy — sin el branch en main.js, cualquier campo fiscal se descarta silenciosamente (Pitfall 1). D-01: QR generado en el agente desde qrUrl; D-02: el agente solo renderiza, nunca decide reglas AFIP; D-03: QR imagen reemplaza el texto de URL actual.
Output: fiscal-formatter.js reescrito (async), main.js con branch fiscal.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
El agente SOLO renderiza esta shape D-02 (decidida en backend). No re-deriva letra/IVA.

payload.factura (D-02) que llega por socket 'print_invoice':
`{ letra:'A'|'B'|'M', cod:'NN', number:'00010-00000123', emisor:{razonSocial,cuit,domicilio,condIva,iibb,inicioAct}, receptor: {tipo:'identificado',razonSocial,cuit,condIva,domicilio} | {tipo:'consumidorFinal',doc?}, items:[{cant,desc,pUnit,subtotal}], neto, iva21, total, ivaDiscrim:boolean, cae, caeVto, qrUrl }`

Patrón QR-imagen a COPIAR verbatim (print-agent/src/qr-formatter.js:7,29-36 — YA funciona, Phase 38):
`const QRCode = require('qrcode');` (qrcode@^1.5.4 YA instalado — NO instalar)
`const qrDataUri = await QRCode.toDataURL(String(qrUrl || ''), { margin: 1, width: 360 });` luego `<img class="qr" src="${qrDataUri}" />`

Handler hermano YA correcto a replicar (main.js:996-1023 print_fiscal): html = formatFiscalHtml(payload); png = await renderHtmlToPng(html, 576, 10000, broadcastLog); await printImage(png, printerCfg, broadcastLog); emit print_ack.

Handler print_invoice actual (main.js:929-993) — llama printTicket(payload,...) → formatInvoiceHtml (control ticket plano). NO tocar el path no-fiscal. imports ya presentes: formatFiscalHtml (:9), renderHtmlToPng (:12), printImage (:14).

Layout de referencia (paridad): CoolSyncro/src/main/pdf/thermal-generator.js (letra box :212-219, receptorLines :83-105, IVA discrim :249-259, leyenda :276).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir fiscal-formatter.js (async, consume factura D-02, QR imagen)</name>
  <files>print-agent/src/fiscal-formatter.js</files>
  <read_first>
    - print-agent/src/fiscal-formatter.js (estado actual: QR-como-texto :73-77, CSS injection :80-113, module.exports :133)
    - print-agent/src/qr-formatter.js (patrón QRCode.toDataURL :7,29-36)
    - print-agent/src/formatter.js (formatInvoiceHtml — base HTML que se extiende)
    - /Users/marcoskim/Trabajos_Programming/CoolSyncro/src/main/pdf/thermal-generator.js (orden de campos)
    - 57-PATTERNS.md sección "fiscal-formatter.js (REWRITTEN)"
  </read_first>
  <action>
    Convertir `formatFiscalHtml` a `async function formatFiscalHtml(factura)` (async porque QRCode.toDataURL es async — mismo patrón que qr-formatter.js). Requerir `const QRCode = require('qrcode')` (ya instalado). Añadir helper local `escapeHtml(s)`. Renderizar en este orden (paridad thermal-generator.js), consumiendo SOLO campos de `factura` (NUNCA re-derivar letra/IVA):
    1. Caja de letra (borde, letra 20pt bold centrada) + `COD. ${factura.cod}` debajo.
    2. N° comprobante `factura.number`, fecha.
    3. EMISOR: razonSocial, `CUIT: cuit`, domicilio, `Cond. IVA: condIva`, `IIBB: iibb`, `Inicio act.: inicioAct`.
    4. RECEPTOR: si `factura.receptor.tipo==='identificado'` → Cliente/CUIT/Cond.IVA/Domicilio; si `'consumidorFinal'` → "Consumidor Final" (+ Doc opcional).
    5. Tabla de ítems: por cada `factura.items[]` → cant, desc (clamp 2 líneas con CSS existente), pUnit, subtotal.
    6. Si `factura.ivaDiscrim` (A/M): filas "Subtotal" = `factura.neto` e "IVA 21%" = `factura.iva21` antes del TOTAL. Si no (B): omitir.
    7. `TOTAL: ${factura.total}` (bloque destacado).
    8. `CAE N°: ${factura.cae}` + `Vencimiento CAE: ${factura.caeVto}`.
    9. QR imagen: `const qrDataUri = await QRCode.toDataURL(String(factura.qrUrl||''), { margin:1, width:360 });` → `<img class="qr" src="${qrDataUri}" />`. ELIMINAR el bloque "Verificar en AFIP: <texto url>" (:73-77).
    10. Leyenda: "Comprobante autorizado — AFIP/ARCA".
    Escapar TODOS los campos de texto con `escapeHtml` antes de interpolar (T-57-04). `qrUrl` va SOLO dentro de `QRCode.toDataURL`, nunca como innerHTML. Mantener el patrón de inyección CSS (`html.replace('</body>', ...)`). Actualizar `module.exports = { formatFiscalHtml }`.
  </action>
  <verify>
    <automated>cd print-agent && node -e "const {formatFiscalHtml}=require('./src/fiscal-formatter');const f={letra:'A',cod:'01',number:'00010-00000123',emisor:{razonSocial:'ACME',cuit:'30111111118',domicilio:'C1',condIva:'Responsable Inscripto',iibb:'123',inicioAct:'01/01/2020'},receptor:{tipo:'identificado',razonSocial:'Cli',cuit:'20111111112',condIva:'Responsable Inscripto',domicilio:'Dir'},items:[{cant:2,desc:'Prod',pUnit:100,subtotal:200}],neto:165.29,iva21:34.71,total:200,ivaDiscrim:true,cae:'75123456789012',caeVto:'2026-08-01',qrUrl:'https://www.afip.gob.ar/fe/qr/?p=eyJ2ZXIiOjF9'};Promise.resolve(formatFiscalHtml(f)).then(h=>{if(!/<img/.test(h))throw Error('no QR');if(!/IVA 21/.test(h))throw Error('no IVA');if(!/30111111118/.test(h))throw Error('no emisor');console.log('FISCAL_A_OK');});"</automated>
  </verify>
  <done>formatFiscalHtml es async, consume la shape D-02, renderiza QR como img escaneable, discrimina IVA solo para A/M, escapa campos de texto. El smoke node imprime FISCAL_A_OK.</done>
  <acceptance_criteria>
    - `grep -q "QRCode.toDataURL" print-agent/src/fiscal-formatter.js`
    - `grep -Eq "async function formatFiscalHtml|formatFiscalHtml = async" print-agent/src/fiscal-formatter.js`
    - `! grep -q "Verificar en AFIP" print-agent/src/fiscal-formatter.js`
    - `grep -qi "ivaDiscrim" print-agent/src/fiscal-formatter.js`
    - El smoke node del verify imprime FISCAL_A_OK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: main.js — print_invoice ramifica en payload.factura (WIRING GAP #1)</name>
  <files>print-agent/main.js</files>
  <read_first>
    - print-agent/main.js (print_invoice handler :929-993, print_fiscal hermano :996-1023, imports :8-14, gates printControl/printFiscal)
    - 57-PATTERNS.md sección "main.js (MODIFIED)" y "Wiring Gaps #1"
  </read_first>
  <action>
    En `wsConnection.on('print_invoice', async (payload) => {...})` (:929), añadir un branch: si `payload.factura` presente → path fiscal en lugar de `printTicket`. Insertar DESPUÉS de obtener `printerCfg` y ANTES del `await printTicket(payload, ...)` (:970), dentro del try/catch existente:
    `if (payload.factura) { if (!store.get('printFiscal')) { broadcastLog('print_invoice(fiscal) ignorado — printFiscal=false'); return; } const html = await formatFiscalHtml(payload.factura); const png = await renderHtmlToPng(html, 576, 10000, broadcastLog); await printImage(png, printerCfg, broadcastLog); wsConnection.emit('print_ack', { invoiceId: payload?.invoiceId, status: 'ok', ts: Date.now() }); broadcastLog('print_invoice(fiscal) OK'); return; }`
    El path no-fiscal (sin `payload.factura`) queda EXACTAMENTE igual (control ticket, sin regresión — constraint SPEC). `formatFiscalHtml` ya importado (:9). Gate `printFiscal` para el path fiscal (coherente con el handler print_fiscal hermano).
  </action>
  <verify>
    <automated>cd print-agent && node --check main.js && grep -q "formatFiscalHtml(payload.factura" main.js && echo MAIN_BRANCH_OK</automated>
  </verify>
  <done>El handler print_invoice ramifica en payload.factura y llama formatFiscalHtml; node --check pasa; el path no-fiscal intacto.</done>
  <acceptance_criteria>
    - `cd print-agent && node --check main.js` exit 0
    - `grep -q "payload.factura" print-agent/main.js`
    - `grep -q "formatFiscalHtml(payload.factura" print-agent/main.js`
    - `grep -q "printTicket(payload" print-agent/main.js` (path no-fiscal preservado)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend socket → print-agent | El payload `factura` cruza desde el backend por Socket.io; el print-agent lo renderiza dentro de un `BrowserWindow` (Electron) → los campos de texto entran a un DOM HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering | print-agent re-derivando letra/IVA en vez de usar factura | mitigate | fiscal-formatter consume SOLO `factura.*`; nunca importa/reimplementa decideComprobante ni computeNetoIva (D-02) |
| T-57-04 | Elevation / Injection | Campos de texto (razonSocial/domicilio/desc) interpolados en HTML del render Electron | mitigate | `escapeHtml()` sobre todo campo de texto antes de interpolar; `qrUrl` SOLO dentro de `QRCode.toDataURL`, jamás como innerHTML/href |
| T-57-05 | Denial of Service | Regresión de tickets de control no-AFIP | accept/mitigate | El branch fiscal solo actúa si `payload.factura` existe; el path `printTicket` queda intacto — test grep confirma que `printTicket(payload` persiste |
</threat_model>

<verification>
- `cd print-agent && node --check main.js` sin errores de sintaxis
- Smoke node de Task 1 imprime FISCAL_A_OK
- `grep -q "printTicket(payload" print-agent/main.js` (no regresión control ticket)
- Manual (homologación, ver 57-VALIDATION.md): emitir Factura A y B, escanear QR del ticket → decodifica a afip.gob.ar/fe/qr con codAut=CAE; A muestra IVA 21%, B no
</verification>

<success_criteria>
- fiscal-formatter renderiza el comprobante D-02 completo con QR escaneable (R1/R2)
- main.js dirige los payloads fiscales al path correcto sin romper control tickets
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-02-SUMMARY.md`
</output>
