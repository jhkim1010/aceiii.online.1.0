---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 06
type: execute
wave: 3
depends_on: ["01", "03", "04", "05"]
files_modified:
  - api-ventago/src/app/afip/afip-voucher.service.ts
  - api-ventago/src/app/afip/afip.controller.ts
  - api-ventago/src/app/afip/afip-voucher.service.spec.ts
  - api-ventago/src/app/afip/afip.controller.spec.ts
autonomous: true
requirements: ["R5", "R6"]
must_haves:
  truths:
    - "AfipController.issue() llama outputService.dispatch() tras una emisión exitosa (WIRING GAP #2)"
    - "issueForSale() devuelve branchId (ya lo deriva de sale.user.branchId) para que el controller despache sin query extra"
    - "decideComprobante recibe configInvoiceType = (issuer.ivaCondition==='RI' ? issuer.invoiceType : undefined) — gate D-08"
    - "issueForSale resuelve PV/coolUser vía resolvePvAndCoolUser cuando invoiceSucursal está seteado, con fallback local"
    - "Una sucursal RI 'Solo Factura M' a receptor CUIT produce CbteTipo 51 (M); 'Puede emitir A' produce CbteTipo 1 (A)"
  artifacts:
    - path: "api-ventago/src/app/afip/afip.controller.ts"
      provides: "issue() despacha output tras emitir"
      contains: "outputService.dispatch"
    - path: "api-ventago/src/app/afip/afip-voucher.service.ts"
      provides: "gate A/M + resolvePvAndCoolUser + return branchId"
      contains: "resolvePvAndCoolUser"
  key_links:
    - from: "afip.controller.ts::issue"
      to: "outputService.dispatch"
      via: "tras issueForSale ok, dispatch({storeId, voucherId, output, branchId})"
      pattern: "outputService.dispatch"
    - from: "afip-voucher.service.ts::issueForSale"
      to: "code-maps.ts::decideComprobante"
      via: "configInvoiceType gated by ivaCondition==='RI'"
      pattern: "configInvoiceType"
    - from: "afip-voucher.service.ts::issueForSale"
      to: "afip-issuer.service.ts::resolvePvAndCoolUser"
      via: "resolver PV cuando invoiceSucursal seteado, fallback local"
      pattern: "resolvePvAndCoolUser"
---

<objective>
Cablear el path de emisión: (1) WIRING GAP #2 — `AfipController.issue()` llama `outputService.dispatch()` tras una emisión exitosa (hoy solo devuelve `dto.output` sin actuar); (2) `issueForSale()` devuelve `branchId` (ya lo deriva internamente en :84); (3) gate A/M D-08 — pasar `configInvoiceType = issuer.ivaCondition==='RI' ? issuer.invoiceType : undefined` a `decideComprobante`; (4) integrar `resolvePvAndCoolUser` (Plan 05) en `issueForSale` cuando `invoiceSucursal` esté seteado, con fallback al PV local. Sin doble-CAE.

Purpose: Sin el dispatch en issue(), ningún formatter mejorado (Plan 02/04) es alcanzable desde el flujo "Facturar" (Pitfall 2). El gate A/M y la resolución de PV materializan R5/R6 en runtime.
Output: emisión que decide letra (A/M gated), resuelve PV (manager/fallback) y despacha la salida.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-RESEARCH.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
issue() actual (afip.controller.ts:177-186) — gap (output nunca despachado):
```typescript
async issue(@Body() dto: IssueVoucherDto, @GetUser() user: Users) {
  const result = await this.voucherService.issueForSale({ storeId: this.requireStoreId(user), saleId: dto.saleId, puntoVenta: dto.puntoVenta, invoicePct: dto.invoicePct });
  return { ...result, output: dto.output };   // ← BUG
}
```
Shape dispatch a copiar (reprint :220-238): `this.outputService.dispatch({ storeId, voucherId: id, output, branchId })`.

issueForSale (afip-voucher.service.ts:63-90...): deriva `const branchId = sale.user?.branchId ?? null` (:84), usa loadIssuerByBranch, decideComprobante. Return type actual: {ok, reason?, cae?, voucherId?, qrUrl?, ambiguous?} — AÑADIR branchId?.

decideComprobante (code-maps.ts:147-174): param `configInvoiceType` ya soportado; rama 'M' en :166. D-08: pasar `issuer.ivaCondition === 'RI' ? issuer.invoiceType : undefined`.

resolvePvAndCoolUser (Plan 05, afip-issuer.service.ts): `async resolvePvAndCoolUser(cuit, sucursal): Promise<{point,coolUser}|null>`. Llamar solo si issuer.invoiceSucursal seteado; si devuelve null → usar puntoVenta local. NO reintentar acoplado a issueCae (T-57-08).

CbteTipo: A=1, M=51. DocType CUIT=80.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: issueForSale — gate A/M + resolvePvAndCoolUser + return branchId</name>
  <files>api-ventago/src/app/afip/afip-voucher.service.ts, api-ventago/src/app/afip/afip-voucher.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/afip-voucher.service.ts (issueForSale :63-..., branchId :84, loadIssuerByBranch, decideComprobante call, provider.issueCae)
    - api-ventago/src/app/afip/afip-issuer.service.ts (resolvePvAndCoolUser — Plan 05)
    - api-ventago/src/app/afip/code-maps.ts (decideComprobante :147-174)
    - api-ventago/src/app/afip/providers/rest-gateway.provider.ts (clasificación ambiguous/retryable/fatal — no romper)
    - 57-PATTERNS.md secciones code-maps.ts (D-08 gate) + afip-issuer.service.ts (D-07)
  </read_first>
  <behavior>
    - Emisión a receptor CUIT (doc 11 dígitos) con issuer RI + invoiceType 'M' → CbteTipo 51 (letra M).
    - Mismo caso con invoiceType 'A' (o undefined) → CbteTipo 1 (letra A).
    - issuer MONO + invoiceType 'M' → gate ignorado (decideComprobante devuelve 'C' por MONO).
    - Con invoiceSucursal seteado y manager OK → el point usado proviene de resolvePvAndCoolUser (branchs[0].point).
    - Con invoiceSucursal null o manager fallback → usa puntoVenta local; una sola emisión (sin doble-CAE).
    - issueForSale devuelve branchId en el resultado ok.
  </behavior>
  <action>
    En `issueForSale`: (a) pasar a `decideComprobante` el arg `configInvoiceType: issuer.ivaCondition === 'RI' ? issuer.invoiceType : undefined` (gate D-08). (b) ANTES de armar el voucherRequest, si `issuer.invoiceSucursal != null`, llamar `const resolved = await this.issuerService.resolvePvAndCoolUser(issuer.cuit, issuer.invoiceSucursal)`; si `resolved?.point` finito → usar ese `point` (y `resolved.coolUser` para el provider); si null → usar `puntoVenta` local y log del fallback. Envolver la resolución en su propio try/catch aislado (non-throw) — NUNCA reintentar la emisión de CAE por un fallo del header (T-57-08). (c) Añadir `branchId` al return type y devolver el `branchId` ya derivado (:84).
    No alterar la clasificación ambiguous/retryable/fatal del provider. tsc/jest gate.
    Extender `afip-voucher.service.spec.ts` con los casos del behavior (mock issuerService.resolvePvAndCoolUser + provider). Verificar CbteTipo 51 vs 1 y que branchId se retorna.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/afip-voucher.service --silent && grep -q "resolvePvAndCoolUser" src/app/afip/afip-voucher.service.ts && grep -q "configInvoiceType" src/app/afip/afip-voucher.service.ts</automated>
  </verify>
  <done>issueForSale aplica el gate A/M (RI-only), resuelve PV vía manager con fallback aislado, y devuelve branchId; spec verde cubriendo M vs A y fallback.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/afip-voucher.service --silent` exit 0
    - `grep -q "resolvePvAndCoolUser" api-ventago/src/app/afip/afip-voucher.service.ts`
    - `grep -Eq "ivaCondition === 'RI'.*invoiceType|invoiceType.*ivaCondition === 'RI'" api-ventago/src/app/afip/afip-voucher.service.ts` (gate)
    - `grep -q "branchId" api-ventago/src/app/afip/afip-voucher.service.ts` (return)
    - El spec verifica CbteTipo 51 (M) para RI+'M' y 1 (A) para RI+'A'
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AfipController.issue() → outputService.dispatch (WIRING GAP #2) + spec</name>
  <files>api-ventago/src/app/afip/afip.controller.ts, api-ventago/src/app/afip/afip.controller.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/afip.controller.ts (issue :177-186, reprint dispatch shape :220-238, requireStoreId)
    - api-ventago/src/app/afip/afip-output.service.ts (dispatch signature — Plan 04)
    - 57-PATTERNS.md sección afip.controller.ts (MODIFIED) + Wiring Gaps #2
    - 57-RESEARCH.md Wave 0 gap (afip.controller.spec.ts nuevo)
  </read_first>
  <behavior>
    - issue() con emisión exitosa (result.ok) y branchId presente → llama outputService.dispatch({storeId, voucherId: result.voucherId, output: dto.output, branchId: result.branchId}) exactamente una vez.
    - Si result.ok es false → NO despacha (no hay comprobante).
    - El dispatch NO re-emite CAE (solo produce salida) — el afip_number no cambia.
  </behavior>
  <action>
    En `issue()`, tras `const result = await this.voucherService.issueForSale(...)`: si `result.ok && result.voucherId` → `await this.outputService.dispatch({ storeId: this.requireStoreId(user), voucherId: result.voucherId, output: dto.output, branchId: result.branchId })` (envolver en try/catch para que un fallo de impresión no rompa la respuesta de emisión — la salida es fire-and-forget respecto al CAE ya obtenido). Devolver `{ ...result, output: dto.output }`. Inyectar `outputService` si no está ya en el controller (reprint ya lo usa → ya inyectado).
    Crear `afip.controller.spec.ts` (nuevo — Wave 0 gap): mock voucherService + outputService; verificar que issue() con result.ok llama dispatch una vez con los args correctos, y que con result.ok=false NO llama dispatch.
    tsc/jest gate.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/afip.controller --silent && grep -q "outputService.dispatch" src/app/afip/afip.controller.ts</automated>
  </verify>
  <done>issue() despacha la salida tras emitir (fire-and-forget respecto al CAE); afip.controller.spec.ts nuevo verde cubriendo ok/no-ok.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/afip.controller --silent` exit 0
    - `grep -q "outputService.dispatch" api-ventago/src/app/afip/afip.controller.ts` en el método issue
    - El spec verifica dispatch llamado 1 vez en ok y 0 veces en no-ok
    - `ls api-ventago/src/app/afip/afip.controller.spec.ts` existe
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POS/Facturar → issue() → provider CAE | El disparo de emisión cruza al gateway AFIP; una sola emisión debe producir un solo CAE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-13 | Repudiation / Tampering (doble-CAE) | dispatch tras issue re-disparando emisión, o resolvePvAndCoolUser reintentando el CAE POST | mitigate | dispatch SOLO produce salida (thermal/pdf), nunca re-emite; la resolución de PV es try/catch aislado non-throw; el dispatch va en try/catch para no propagar fallos de impresión a la emisión |
| T-57-14 | Information Disclosure / IDOR | dispatch con voucherId/branchId de otra tienda | mitigate | dispatch recibe storeId=requireStoreId(user); getVoucher/branch ya scopean por storeId (Plan 04); el controller nunca confía en un storeId del body |
| T-57-15 | Tampering | Cliente forzando invoiceType='M' vía body para emitir M sin ser RI | mitigate | El gate A/M se recomputa server-side desde issuer.ivaCondition (DB), nunca desde el body; MONO/EXENTO ignoran invoiceType |
</threat_model>

<verification>
- `cd api-ventago && npx jest src/app/afip/afip-voucher.service src/app/afip/afip.controller --silent` verde
- `cd api-ventago && npx tsc --noEmit -p .` sin errores nuevos
- Manual (57-VALIDATION.md): sucursal RI 'Solo Factura M' → CbteTipo 51; 'Puede emitir A' → 1; invoice_sucursal seteado → point desde manager, header caído → fallback local + log
</verification>

<success_criteria>
- La emisión decide letra con gate A/M (R5), resuelve PV vía manager/fallback (R6) y despacha la salida mejorada (habilita R1/R3 end-to-end)
- Sin doble-CAE
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-06-SUMMARY.md`
</output>
