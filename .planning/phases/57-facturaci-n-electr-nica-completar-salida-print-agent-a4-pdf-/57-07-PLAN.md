---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 07
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - api-ventago/src/app/afip/nota-credito.service.ts
  - api-ventago/src/app/afip/nota-debito.service.ts
  - api-ventago/src/app/afip/nota-credito.service.spec.ts
  - api-ventago/src/app/afip/nota-debito.service.spec.ts
autonomous: true
requirements: ["R7"]
must_haves:
  truths:
    - "NotaCreditoService.emit() llama outputService.dispatch() tras crear el AfipVoucher (WIRING GAP #3 — nuevo, no había salida antes)"
    - "NotaDebitoService.emit() llama outputService.dispatch() del mismo modo"
    - "La NC/ND reusa la salida mejorada (thermal+A4) con letra derivada (NCA/NCB/NCM, NDA/NDB/NDM) e ítems/IVA/QR"
    - "El dispatch de NC/ND no re-emite CAE ni introduce doble emisión"
  artifacts:
    - path: "api-ventago/src/app/afip/nota-credito.service.ts"
      provides: "emit() despacha salida tras crear el voucher NC"
      contains: "outputService.dispatch"
    - path: "api-ventago/src/app/afip/nota-debito.service.ts"
      provides: "emit() despacha salida tras crear el voucher ND"
      contains: "outputService.dispatch"
  key_links:
    - from: "nota-credito.service.ts::emit"
      to: "afip-output.service.ts::dispatch"
      via: "tras voucherModel.create, dispatch({voucherId: nc.id, output, branchId})"
      pattern: "outputService.dispatch"
    - from: "nota-debito.service.ts::emit"
      to: "afip-output.service.ts::dispatch"
      via: "tras voucherModel.create, dispatch({voucherId: nd.id, output, branchId})"
      pattern: "outputService.dispatch"
---

<objective>
Cerrar el WIRING GAP #3: `NotaCreditoService.emit()` y `NotaDebitoService.emit()` hoy solo crean el `AfipVoucher` y devuelven `{cae, qrUrl}` — NO despachan salida alguna (R7 "sin regresión" no tiene nada que regresar; es cableado nuevo). Añadir la llamada `outputService.dispatch({storeId, voucherId: nc.id, output, branchId})` tras crear el voucher, para que NC/ND reusen la misma salida mejorada (thermal + A4) de Plan 04, con letra derivada (NCA/NCB/NCM, NDA/NDB/NDM) e ítems/IVA/QR (buildFactura ya deriva la letra vía letraOf desde tipoComprobante).

Purpose: R7 — la NC sobre una Factura A debe imprimir un ticket con letra "NC A", CAE propio, QR e ítems; el A4 de esa NC lista los ítems. Sin este cableado, NC/ND siguen sin salida.
Output: emit() de NC y ND despachan la salida mejorada.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-RESEARCH.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
Estado actual (verificado): nota-credito.service.ts y nota-debito.service.ts tienen 0 referencias a dispatch/outputService — solo `voucherModel.create({...notaCredito:true})` y return {cae, qrUrl}.

dispatch signature (afip-output.service.ts, Plan 04): `dispatch({ storeId, voucherId, output, branchId, targetSocketId? })`. buildFactura ya deriva la letra NC/ND vía `letraOf(voucher.tipoComprobante)` (CbteTipo NCA=3→'A' contexto NC, etc.) — el A4/thermal muestran la letra correcta desde el voucher.

CbteTipo NC: NCA=3, NCB=8, NCM=53. ND: NDA=2, NDB=7, NDM=52. La referencia al comprobante original (asoc) ya se guarda en el voucher NC/ND.

branchId: derivar igual que issueForSale (sale.user?.branchId) — cargar/propagar en emit(). Si el emit no tiene el branchId a mano, obtenerlo del sale asociado (mismo patrón store-scoped).

Decisión (A2 RESEARCH / discreción planner): auto-dispatch thermal en emit() success (espeja el fix de issue()). El output por defecto para NC/ND = 'thermal' (o el que el caller pase); el botón A4 on-demand (Plan 08) también sirve para NC/ND vía GET /afip/vouchers/:id/pdf.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotaCreditoService.emit() → dispatch (R7)</name>
  <files>api-ventago/src/app/afip/nota-credito.service.ts, api-ventago/src/app/afip/nota-credito.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/nota-credito.service.ts (emit completo, voucherModel.create, return)
    - api-ventago/src/app/afip/afip-output.service.ts (dispatch signature — Plan 04)
    - api-ventago/src/app/afip/afip-voucher.service.ts (derivación branchId sale.user.branchId — patrón)
    - api-ventago/src/app/afip/nota-credito.service.spec.ts (spec existente)
    - 57-PATTERNS.md sección nota-credito.service.ts + Wiring Gaps #3
  </read_first>
  <behavior>
    - emit() exitoso (voucher NC creado) → llama outputService.dispatch({storeId, voucherId: nc.id, output, branchId}) una vez.
    - Un fallo del dispatch (impresión) NO revierte la NC ya emitida (fire-and-forget respecto al CAE).
    - El voucherId despachado es el de la NC (nc.id), no el de la factura original.
  </behavior>
  <action>
    Inyectar `AfipOutputService` en `NotaCreditoService` (constructor). Tras crear el voucher NC (`const nc = await this.voucherModel.create(...)`), derivar `branchId` (del sale asociado: sale.user?.branchId — cargarlo si no está disponible, store-scoped) y llamar dentro de un try/catch (non-throw): `await this.outputService.dispatch({ storeId, voucherId: nc.id, output: params.output ?? 'thermal', branchId })`. Mantener el return {cae, qrUrl} existente. NO re-emitir CAE. Evitar dependencia circular (AfipOutputService ↔ NotaCredito): si hay ciclo, usar forwardRef o mover el dispatch al controller de NC (documentar la elección).
    Extender el spec: mock outputService.dispatch; verificar llamada 1 vez con voucherId=nc.id; verificar que un throw de dispatch no rompe el return de emit().
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/nota-credito.service --silent && grep -q "outputService.dispatch" src/app/afip/nota-credito.service.ts</automated>
  </verify>
  <done>emit() de NC despacha la salida mejorada con voucherId=nc.id; spec verde; sin doble-CAE ni ciclo de DI.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/nota-credito.service --silent` exit 0
    - `grep -q "outputService.dispatch" api-ventago/src/app/afip/nota-credito.service.ts`
    - `grep -q "nc.id\|voucherId:" api-ventago/src/app/afip/nota-credito.service.ts`
    - El spec verifica dispatch con voucherId de la NC y que un fallo de dispatch no propaga
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotaDebitoService.emit() → dispatch (R7)</name>
  <files>api-ventago/src/app/afip/nota-debito.service.ts, api-ventago/src/app/afip/nota-debito.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/nota-debito.service.ts (emit completo — sibling casi idéntico a NC)
    - api-ventago/src/app/afip/nota-credito.service.ts (la solución de Task 1 — replicar shape)
    - api-ventago/src/app/afip/nota-debito.service.spec.ts (spec existente)
  </read_first>
  <behavior>
    - Igual que Task 1 pero para ND: emit() exitoso → dispatch({voucherId: nd.id, ...}) una vez, non-throw respecto al CAE.
  </behavior>
  <action>
    Replicar exactamente la solución de Task 1 en `NotaDebitoService.emit()` (archivo hermano casi idéntico): inyectar AfipOutputService, dispatch tras crear el voucher ND con `voucherId: nd.id, output: params.output ?? 'thermal', branchId`, try/catch non-throw, misma decisión de forwardRef/ubicación que Task 1. Extender el spec de ND con las mismas aserciones.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/nota-debito.service --silent && grep -q "outputService.dispatch" src/app/afip/nota-debito.service.ts</automated>
  </verify>
  <done>emit() de ND despacha la salida mejorada con voucherId=nd.id; spec verde.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/nota-debito.service --silent` exit 0
    - `grep -q "outputService.dispatch" api-ventago/src/app/afip/nota-debito.service.ts`
    - El spec verifica dispatch con voucherId de la ND
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NC/ND emit → dispatch → provider | Cada emisión de NC/ND produce un CAE propio; el dispatch solo genera salida, no re-emite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-16 | Repudiation / Tampering (doble-CAE) | dispatch de NC/ND re-disparando emisión | mitigate | dispatch SOLO produce salida (thermal/pdf) con el voucherId ya creado; try/catch non-throw; nunca vuelve a llamar al provider |
| T-57-17 | Information Disclosure / IDOR | dispatch con voucherId/branchId cruzando tienda | mitigate | storeId de emit() (store del voucher original), branchId derivado del sale store-scoped; dispatch re-scopea por storeId (Plan 04) |
</threat_model>

<verification>
- `cd api-ventago && npx jest src/app/afip/nota-credito.service src/app/afip/nota-debito.service --silent` verde
- `cd api-ventago && npx tsc --noEmit -p .` sin errores nuevos
- Manual (57-VALIDATION.md): emitir NC sobre Factura A → ticket con letra "NC A", CAE propio, QR e ítems; A4 de esa NC lista los ítems
</verification>

<success_criteria>
- NC y ND reusan la salida mejorada (R7) sin doble-CAE ni ciclo de DI
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-07-SUMMARY.md`
</output>
