---
phase: 57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api-ventago/src/app/afip/afip-output.service.ts
  - api-ventago/src/app/afip/afip-query.service.ts
  - api-ventago/src/app/afip/pdf/a4-generator.ts
  - api-ventago/src/app/afip/pdf/a4-generator.spec.ts
  - api-ventago/src/app/afip/afip-output.service.spec.ts
autonomous: true
requirements: ["R1", "R3"]
must_haves:
  truths:
    - "dispatch() carga Sale+SaleItem+Product (store-scoped) y llama buildFactura para producir el objeto factura"
    - "El thermal emit incluye el campo factura (D-02) en el payload de print_invoice"
    - "El A4 PDF lista los ítems reales (lines != []) con P.Unit, subtotal, y para A/M discrimina IVA 21% (Neto+IVA+TOTAL)"
    - "El A4 de una Factura B muestra precios con IVA incluido sin columna IVA"
    - "El duplicado privado letra() de afip-output.service se elimina y usa letraOf canónico"
  artifacts:
    - path: "api-ventago/src/app/afip/afip-output.service.ts"
      provides: "dispatch() carga Sale, llama buildFactura, pasa factura a thermal y a A4"
      contains: "buildFactura"
    - path: "api-ventago/src/app/afip/afip-query.service.ts"
      provides: "getSaleForVoucher(storeId, saleId) con includes"
      contains: "getSaleForVoucher"
    - path: "api-ventago/src/app/afip/pdf/a4-generator.ts"
      provides: "A4 con letra chip, COD, receptor, ítems reales, IVA discrim gate"
  key_links:
    - from: "afip-output.service.ts::dispatch"
      to: "build-factura.ts::buildFactura"
      via: "carga Sale store-scoped luego buildFactura(voucher, sale, issuer)"
      pattern: "buildFactura\\("
    - from: "dispatch (output=pdf)"
      to: "a4-generator.ts::generateA4Pdf"
      via: "pasa factura.items reales en vez de lines: []"
      pattern: "generateA4Pdf"
    - from: "dispatch (output=thermal)"
      to: "printService.emitPrintInvoice"
      via: "payload incluye { factura }"
      pattern: "emitPrintInvoice"
---

<objective>
Cerrar los WIRING GAPS #3 y #4: en `afip-output.service.ts::dispatch()`, cargar `Sale`+`SaleItem`+`Product` (store-scoped) para `voucher.saleId` vía un nuevo helper `getSaleForVoucher` en `afip-query.service.ts`, llamar `buildFactura(voucher, sale, issuer)` (Plan 01), pasar el objeto `factura` (D-02) al payload thermal (`emitPrintInvoice`) y las líneas reales al A4 (reemplaza el `lines: []` hardcodeado en :82). Reescribir `a4-generator.ts` para renderizar letra chip + COD, bloque receptor, ítems reales y discriminación de IVA 21% SOLO para A/M (paridad CoolSyncro generator.js). Eliminar el `letra()` privado duplicado (:93) usando `letraOf` canónico.

Purpose: `lines: []` no es un bug local de a4-generator — es un join faltante (Pitfall 4). Sin esto, ni el A4 ni el thermal reciben ítems reales.
Output: dispatch cableado a buildFactura, A4 con ítems+IVA, query helper store-scoped.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-SPEC.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-CONTEXT.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-RESEARCH.md
@.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-PATTERNS.md

<interfaces>
De Plan 01 (build-factura.ts):
`export function buildFactura(voucher: AfipVoucher, sale: Sale, issuer: AfipIssuer): Factura`  (Factura = shape D-02, ver 57-01-PLAN interfaces)

Gap hardcodeado a arreglar (afip-output.service.ts:77-87):
```typescript
if (input.output === 'pdf') {
  const pdf = await generateA4Pdf({ issuer, voucher, tipoLetra: this.letra(voucher.tipoComprobante), lines: [], qrUrl });  // ← lines:[] BUG + letra() duplicado
  return { ok: true, pdf };
}
```

Thermal emit actual (afip-output.service.ts:67-72) — añadir factura al objeto:
```typescript
this.printService.emitPrintInvoice(input.branchId, { cae: voucher.cae, caeVto: voucher.caeVto, qrUrl, voucher, issuer /* + factura */ }, input.targetSocketId);
```

Guard cross-tenant existente a MANTENER (afip-output.service.ts:58-65): branchModel.findOne where {id: branchId, storeId}. La nueva carga de Sale DEBE scopear `{storeId, id: saleId}` (nunca findByPk).

Patrón de carga (afip-voucher.service.ts:155-176 loadSaleWithReceptor) a replicar en getSaleForVoucher:
includes SaleItem→Product(id,name), Users(id,branchId) required:false, StoreClient→GlobalClient required:false, Clients required:false.

A4 types ya correctos (a4-generator.ts:5-34): A4Line{cantidad,precioUnitario,subtotal,descripcion}, A4Input{issuer,voucher,tipoLetra,lines,qrUrl}. Reusar computeNetoIva/letraOf de code-maps.ts. Layout paridad CoolSyncro/src/main/pdf/generator.js (letra box, COD, receptor split, IVA solo A/M).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getSaleForVoucher + dispatch carga Sale y llama buildFactura (thermal + A4)</name>
  <files>api-ventago/src/app/afip/afip-query.service.ts, api-ventago/src/app/afip/afip-output.service.ts, api-ventago/src/app/afip/afip-output.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/afip-query.service.ts (getVoucher — bare findOne, sin include)
    - api-ventago/src/app/afip/afip-output.service.ts (dispatch completo, letra() privado :93-103, thermal emit :67-72, pdf branch :77-87, guard :58-65)
    - api-ventago/src/app/afip/afip-voucher.service.ts (loadSaleWithReceptor :155-176 — patrón includes)
    - api-ventago/src/app/afip/build-factura.ts (Plan 01, buildFactura)
    - api-ventago/src/app/afip/afip-output.service.spec.ts (specs dispatch :42-77)
    - 57-PATTERNS.md secciones afip-output.service.ts + Wiring Gaps #4
  </read_first>
  <action>
    En `afip-query.service.ts` añadir `async getSaleForVoucher(storeId: number, saleId: number): Promise<Sale | null>` que hace `saleModel.findOne({ where: {storeId, id: saleId}, include: [...] })` con los includes del patrón loadSaleWithReceptor (SaleItem→Product, Users, StoreClient→GlobalClient, Clients). Inyectar los modelos necesarios si aún no están en el módulo.
    En `afip-output.service.ts::dispatch()`: tras el guard de branch, cargar `const sale = await this.queryService.getSaleForVoucher(input.storeId, voucher.saleId)`; si !sale → return {ok:false, reason}. Construir `const factura = buildFactura(voucher, sale, issuer)`. 
    - output === 'thermal': añadir `factura` al objeto de `emitPrintInvoice` (payload print_invoice).
    - output === 'pdf': llamar `generateA4Pdf({ issuer, voucher, tipoLetra: letraOf(voucher.tipoComprobante), lines: factura.items.map(...→A4Line), qrUrl })` — reemplazar `lines: []`.
    ELIMINAR el método privado `letra()` (:93-103) e importar `letraOf` de code-maps. Mantener el guard cross-tenant; la nueva carga de Sale usa `{storeId, id}`.
    Actualizar `afip-output.service.spec.ts`: los mocks de dispatch deben stubbear getSaleForVoucher + buildFactura; añadir aserción de que el payload thermal incluye `factura` y que el A4 recibe `lines.length > 0`.
    tsc/jest son el gate (backend).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/afip-output.service --silent && grep -q "getSaleForVoucher" src/app/afip/afip-query.service.ts && grep -q "buildFactura" src/app/afip/afip-output.service.ts && (grep -q "private letra(" src/app/afip/afip-output.service.ts && echo "WARN_letra_still_present" || echo "letra_removed_OK")</automated>
  </verify>
  <done>getSaleForVoucher store-scoped existe; dispatch carga Sale, llama buildFactura, pasa factura al thermal y lines reales al A4; letra() privado eliminado; specs verdes.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/afip-output.service --silent` exit 0
    - `grep -q "getSaleForVoucher" api-ventago/src/app/afip/afip-query.service.ts`
    - `grep -q "buildFactura" api-ventago/src/app/afip/afip-output.service.ts`
    - `! grep -q "lines: \[\]" api-ventago/src/app/afip/afip-output.service.ts`
    - `! grep -q "private letra(" api-ventago/src/app/afip/afip-output.service.ts`
    - `grep -Eq "where: ?\{ ?storeId" api-ventago/src/app/afip/afip-query.service.ts` (Sale store-scoped)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a4-generator.ts — letra chip + COD + receptor + ítems reales + IVA discrim gate</name>
  <files>api-ventago/src/app/afip/pdf/a4-generator.ts, api-ventago/src/app/afip/pdf/a4-generator.spec.ts</files>
  <read_first>
    - api-ventago/src/app/afip/pdf/a4-generator.ts (estructura actual :39-96, types :5-34)
    - api-ventago/src/app/afip/pdf/a4-generator.spec.ts (spec existente)
    - api-ventago/src/app/afip/code-maps.ts (computeNetoIva, letraOf)
    - /Users/marcoskim/Trabajos_Programming/CoolSyncro/src/main/pdf/generator.js (A4 pdfkit: letra chip + COD + receptor + IVA table)
    - 57-PATTERNS.md sección "pdf/a4-generator.ts (MODIFIED)"
  </read_first>
  <behavior>
    - Factura A con ≥2 líneas: el PDF lista las 2 líneas (cantidad/desc/pUnit/subtotal), muestra caja de letra "A" + "COD. 01", bloque receptor identificado, filas Neto Gravado + IVA 21% + TOTAL coherentes con impTotal.
    - Factura B: precios con IVA incluido, SIN columna/filas de IVA discriminado, receptor "Consumidor Final".
    - lines vacío ya no ocurre (viene de dispatch) — pero el generador debe soportar lines.length>0.
  </behavior>
  <action>
    Reescribir el cuerpo de `generateA4Pdf` (mantener firma A4Input) para añadir: (1) caja de letra (rect + letra bold 20pt centrada) en vez del texto plano "FACTURA {tipoLetra}"; (2) línea `COD. NN`; (3) bloque receptor (hoy ausente) — recibir datos de receptor vía A4Input (extender A4Input con `receptor?` o pasar desde factura); si letra A/M → identidad completa, si B → "Consumidor Final"; (4) tabla de ítems iterando `input.lines`; (5) gate de discriminación IVA: SOLO si letra ∈ {A,M} renderizar Subtotal(neto)+IVA 21%(impuesto) usando `computeNetoIva({tpago: total, ivaBase: 1.21})`; para B omitir. Reusar `letraOf`/`computeNetoIva` canónicos (NO re-derivar). QR ya se renderiza (QRCode.toDataURL width 200).
    Extender `a4-generator.spec.ts`: caso Factura A con 2 líneas → PDF buffer no trivial, aserción de que se invocó con lines.length===2 y que el branch A/M discrimina; caso B → sin discriminación. Usar mocks de PDFDocument o aserciones sobre las llamadas.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/afip/pdf/a4-generator --silent</automated>
  </verify>
  <done>a4-generator renderiza letra chip/COD/receptor/ítems reales, discrimina IVA solo A/M; spec verde con casos A (2 líneas) y B.</done>
  <acceptance_criteria>
    - `cd api-ventago && npx jest src/app/afip/pdf/a4-generator --silent` exit 0
    - `grep -Eq "computeNetoIva|letraOf" api-ventago/src/app/afip/pdf/a4-generator.ts`
    - `grep -Eq "'A'|'M'" api-ventago/src/app/afip/pdf/a4-generator.ts` (gate discriminación)
    - El spec cubre un caso A con ≥2 líneas y un caso B sin discriminación
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch → Sale/SaleItem load | Nueva consulta por voucher.saleId; debe permanecer dentro del store del voucher. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-11 | Information Disclosure / IDOR | getSaleForVoucher cargando Sale de otra tienda vía saleId | mitigate | `where: {storeId, id: saleId}` — nunca findByPk; storeId proviene de getVoucher(storeId,id) ya scopeado; test grep confirma el where |
| T-57-12 | Tampering | A4 re-derivando IVA/letra con matemática propia | mitigate | Reusar computeNetoIva/letraOf canónicos; sin división /1.21 manual |
</threat_model>

<verification>
- `cd api-ventago && npx jest src/app/afip/afip-output.service src/app/afip/pdf/a4-generator --silent` verde
- `cd api-ventago && npx tsc --noEmit -p .` sin errores nuevos
- Manual: GET /afip/vouchers/:id/pdf de Factura A con ≥2 ítems lista los ítems y discrimina IVA 21%
</verification>

<success_criteria>
- dispatch produce el objeto factura y lo entrega a thermal (D-02) y A4 (ítems reales)
- A4 con paridad CoolSyncro: letra chip, COD, receptor, ítems, IVA discrim solo A/M (R3)
</success_criteria>

<output>
Crear `.planning/phases/57-facturaci-n-electr-nica-completar-salida-print-agent-a4-pdf-/57-04-SUMMARY.md`
</output>
