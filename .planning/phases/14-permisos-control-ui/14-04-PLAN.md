---
phase: 14-permisos-control-ui
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - ventago-app/src/views/users/components/UserPermissionsDrawer.tsx
  - ventago-app/src/pages/401.tsx
  - ventago-app/src/navigation/vertical/index.ts
  - ventago-app/src/layouts/components/acl/CanViewNavLink.tsx
  - ventago-app/src/layouts/components/acl/CanViewNavGroup.tsx
  - ventago-app/src/@core/components/auth/AclGuard.tsx
  - ventago-app/src/configs/acl.ts
  - ventago-app/src/pages/productos/index.tsx
  - ventago-app/src/pages/nueva-venta/index.tsx
  - ventago-app/src/pages/ventas/index.tsx
  - ventago-app/src/pages/ventas/detalle/[id].tsx
  - ventago-app/src/pages/gastos/index.tsx
  - ventago-app/src/pages/caja/index.tsx
  - ventago-app/src/pages/caja/detalle/[id].tsx
  - ventago-app/src/pages/caja-fuerte/index.tsx
  - ventago-app/src/pages/control-de-caja/index.tsx
  - ventago-app/src/pages/control-de-caja/detalle/[id].tsx
  - ventago-app/src/pages/sucursales/index.tsx
  - ventago-app/src/pages/sucursales/[id]/impresora.tsx
  - ventago-app/src/pages/usuarios/index.tsx
  - ventago-app/src/pages/talleres/vendors/index.tsx
  - ventago-app/src/pages/talleres/pedidos/index.tsx
  - ventago-app/src/pages/talleres/dashboard/index.tsx
  - ventago-app/src/pages/dashboards/index.tsx
  - ventago-app/src/pages/dashboards/ventas/index.tsx
  - ventago-app/src/pages/dashboards/producto/index.tsx
  - ventago-app/src/pages/dashboards/stock/index.tsx
  - ventago-app/src/pages/dashboards/fabrica/index.tsx
  - ventago-app/src/pages/dashboards/talleres/index.tsx
  - ventago-app/src/pages/dashboards/admin/index.tsx
  - ventago-app/src/pages/reportes/index.tsx
  - ventago-app/src/pages/reportes-v2/[[...slug]].tsx
  - ventago-app/src/pages/precios/index.tsx
  - ventago-app/src/pages/configuracion/productos/index.tsx
  - ventago-app/src/pages/configuracion/ventas/index.tsx
  - ventago-app/src/pages/admin/tiendas/index.tsx
  - ventago-app/src/pages/admin/registros/index.tsx
  - ventago-app/src/pages/admin/auditoria/index.tsx
  - ventago-app/src/pages/admin/suscripcion/index.tsx
  - ventago-app/src/pages/admin/permisos/index.tsx
  - ventago-app/src/pages/admin/generar-token.tsx
  - ventago-app/src/pages/admin/soporte-remoto.tsx
  - ventago-app/src/pages/admin/ventas/index.tsx
  - ventago-app/src/pages/herramientas/print-agent/index.tsx
  - ventago-app/src/pages/perfil/index.tsx
  - ventago-app/src/pages/cliente-vista/index.tsx
  - ventago-app/src/pages/codigo-vista/index.tsx
  - ventago-app/src/pages/reportes/alertas/index.tsx
  - ventago-app/src/pages/reportes/breve-venta/index.tsx
  - ventago-app/src/pages/reportes/cheque-estado/index.tsx
  - ventago-app/src/pages/reportes/clientes-credito/index.tsx
  - ventago-app/src/pages/reportes/corregido/index.tsx
  - ventago-app/src/pages/reportes/dashboards/index.tsx
  - ventago-app/src/pages/reportes/facturacion/index.tsx
  - ventago-app/src/pages/reportes/fallados/index.tsx
  - ventago-app/src/pages/reportes/gastos/index.tsx
  - ventago-app/src/pages/reportes/ingreso/index.tsx
  - ventago-app/src/pages/reportes/items/index.tsx
  - ventago-app/src/pages/reportes/movidos/index.tsx
  - ventago-app/src/pages/reportes/reservado/index.tsx
  - ventago-app/src/pages/reportes/stocks/index.tsx
  - ventago-app/src/pages/reportes/vendedor/index.tsx
  - ventago-app/src/pages/reportes/ventas/index.tsx
autonomous: false
requirements: [PERM-04, PERM-05, PERM-06, PERM-07]

must_haves:
  truths:
    - "UserPermissionsDrawer에서 각 Function에 C/R/U/D 칩이 표시되고 role 기본값과의 차이가 amber 보더로 표시됨"
    - "유저 권한 리셋 시 ConfirmDialog 확인 후 POST /user-functions/reset/:userId 호출"
    - "권한 없는 페이지 URL 직접 접근 시 NotAuthorized 컴포넌트 inline 렌더 (per D-04)"
    - "권한 없는 사이드바 메뉴가 네비게이션에서 숨겨짐 (per D-04)"
    - "CanViewNavLink가 CASL ability.can(action, subject)로 메뉴 표시/숨김 결정"
    - "모든 인증 페이지에 올바른 .acl = { action, subject } 설정 적용 (per D-02 system-wide)"
  artifacts:
    - path: "ventago-app/src/pages/401.tsx"
      provides: "UnauthorizedPage (401 접근 거부 페이지)"
      contains: "Sin Autorización"
    - path: "ventago-app/src/views/users/components/UserPermissionsDrawer.tsx"
      provides: "CRUD 액션 기반 유저 권한 편집 (override 표시)"
  key_links:
    - from: "UserPermissionsDrawer.tsx"
      to: "PUT /user-functions/actions/:userId"
      via: "apiConnector.put"
      pattern: "user-functions/actions"
    - from: "CanViewNavLink.tsx"
      to: "acl.ts"
      via: "AbilityContext -> ability.can(action, subject)"
      pattern: "ability.can"
    - from: "AclGuard.tsx"
      to: "acl.ts"
      via: "buildAbilityFor(roles, subject, permissions) -> ability.can(action, subject)"
      pattern: "ability.can.*aclAbilities"
    - from: "navigation/vertical/index.ts"
      to: "CanViewNavLink.tsx"
      via: "navLink.action + navLink.subject properties"
      pattern: "action.*read"
---

<objective>
UserPermissionsDrawer CRUD 확장 (override 표시 + 리셋) + 401 페이지 + 네비게이션 권한 숨김 + 모든 인증 페이지에 granular aclAbilities 설정 (per D-02 system-wide).

Purpose: 유저별 권한 오버라이드 편집 완성 (per D-05), 권한 없는 접근 차단 UX (per D-04), manage:all 패턴을 모든 페이지에서 granular 체크로 교체 (per D-02).
Output: UserPermissionsDrawer CRUD 모드, 401.tsx 페이지, 네비게이션 메뉴 권한 기반 숨김, 전체 페이지 aclAbilities 설정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-permisos-control-ui/14-CONTEXT.md
@.planning/phases/14-permisos-control-ui/14-RESEARCH.md
@.planning/phases/14-permisos-control-ui/14-UI-SPEC.md
@.planning/phases/14-permisos-control-ui/14-03-SUMMARY.md

<interfaces>
<!-- CrudActionRow 컴포넌트 (Plan 03에서 생성) -->
```typescript
interface CrudActionRowProps {
  functionId: number
  actions: Record<'create' | 'read' | 'update' | 'delete', boolean>
  roleActions?: Record<'create' | 'read' | 'update' | 'delete', boolean>
  onChange: (functionId: number, action: 'create'|'read'|'update'|'delete', value: boolean) => void
  variant?: 'role' | 'user-override'
  disabled?: boolean
}
export default CrudActionRow
```

<!-- PUT /user-functions/actions/:targetUserId body (Plan 02에서 구현) -->
```typescript
{ data: Array<{ functionId: number; actions: Array<{ action: string; allowed: boolean }> }> }
```

<!-- POST /user-functions/reset/:targetUserId -- 유저 override 전부 삭제 (Plan 02에서 구현) -->

<!-- ConfirmDialog 컴포넌트 -->
```typescript
// src/components/forms/ConfirmDialog.tsx
// props: open, onClose, onConfirm, title, content, confirmText, cancelText
```

<!-- AclGuard.tsx 현재 동작 (이미 NotAuthorized inline 렌더 패턴 사용 중) -->
```typescript
// line 17: import NotAuthorized from 'src/pages/401'
// line 54: ability = buildAbilityFor(auth.user.roles, aclAbilities.subject)
// line 72: ability.can(aclAbilities.action, aclAbilities.subject)
// line 81-85: 권한 없으면 <BlankLayout><NotAuthorized /></BlankLayout> 렌더
```

<!-- CanViewNavLink 현재 로직 -->
```typescript
// line 20: const ability = useContext(AbilityContext)
// line 25: return ability && ability.can(navLink?.action, navLink?.subject) ? <>{children}</> : null
```

<!-- 네비게이션은 user.structure에서 동적 생성 (navigation/vertical/index.ts) -->
<!-- nav item에 action/subject 프로퍼티를 추가하면 CanViewNavLink가 체크 -->
<!-- 현재 대부분의 nav items에 이 필드가 없음 -> 추가 필요 -->

<!-- defaultACLObj (현재) -->
```typescript
export const defaultACLObj: ACLObj = { action: 'manage', subject: 'all' }
```

<!-- _app.tsx -->
```typescript
const aclAbilities = Component.acl ?? defaultACLObj
// 모든 페이지가 .acl 미설정이면 defaultACLObj { action: 'manage', subject: 'all' } 사용
// D-02 요구: 모든 페이지에 명시적 .acl = { action: 'read', subject: '{functionSlug}' } 설정
```

<!-- UI-SPEC 401 페이지 사양 -->
```
- Icon: tabler:shield-off, 64px, color error.main
- Heading: h4 "Sin Autorización"
- Body: body1 "No tenés permiso para acceder a esta sección."
- CTA: contained primary "Volver al inicio" -> navigates to /
- Container: role="alert"
- File: src/pages/401.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserPermissionsDrawer CRUD 확장 + override 표시 + 리셋</name>
  <files>
    ventago-app/src/views/users/components/UserPermissionsDrawer.tsx
  </files>
  <read_first>
    ventago-app/src/views/users/components/UserPermissionsDrawer.tsx,
    ventago-app/src/views/users/roles/components/CrudActionRow.tsx,
    ventago-app/src/views/users/roles/hooks/useRoleFunctions.ts,
    ventago-app/src/components/forms/ConfirmDialog.tsx
  </read_first>
  <action>
1. **UserPermissionsDrawer에 CRUD action 관리 추가:**
   - state 추가:
     ```typescript
     // role 기본 action 맵 (비교용)
     const [roleActionMap, setRoleActionMap] = useState<Map<number, Record<string, boolean>>>(new Map())
     // 유저 override action 맵 (편집용)
     const [userActionMap, setUserActionMap] = useState<Map<number, Record<string, boolean>>>(new Map())
     // 리셋 확인 다이얼로그
     const [resetDialogOpen, setResetDialogOpen] = useState(false)
     ```

   - 데이터 로딩:
     a. useRoleFunctions 훅으로 해당 유저의 role의 function-action 데이터 로드 -> `roleActionMap` 초기화
     b. GET /user-functions (userId) 응답에서 `userFunctionActions` 포함 -> `userActionMap` 초기화
     c. 유저의 effective 권한 = role baseline + user override 병합

   - Function 행에 CrudActionRow 사용:
     ```tsx
     <CrudActionRow
       functionId={fn.id}
       actions={{
         create: getEffectiveAction(fn.id, 'create'),
         read: getEffectiveAction(fn.id, 'read'),
         update: getEffectiveAction(fn.id, 'update'),
         delete: getEffectiveAction(fn.id, 'delete'),
       }}
       roleActions={roleActionMap.get(fn.id) ? {
         create: roleActionMap.get(fn.id)!.create ?? false,
         read: roleActionMap.get(fn.id)!.read ?? false,
         update: roleActionMap.get(fn.id)!.update ?? false,
         delete: roleActionMap.get(fn.id)!.delete ?? false,
       } : undefined}
       onChange={handleUserActionToggle}
       variant='user-override'
     />
     ```

   - `getEffectiveAction(fnId, action)`: userActionMap에 override 있으면 그 값, 없으면 roleActionMap 값 사용

   - `handleUserActionToggle`: userActionMap에 override 저장 (role 기본값과 다를 때만)

2. **Guardar 핸들러:**
   ```typescript
   const handleSubmit = async () => {
     setSaving(true)
     try {
       const data = Array.from(userActionMap.entries())
         .map(([functionId, actions]) => ({
           functionId,
           actions: Object.entries(actions).map(([action, allowed]) => ({ action, allowed }))
         }))

       await apiConnector.put(`/user-functions/actions/${userId}`, { data })
       toast.success('Permisos actualizados correctamente')
       onClose()
     } catch (err: any) {
       toast.error('Error al guardar permisos. Intentá nuevamente.')
     } finally {
       setSaving(false)
     }
   }
   ```

3. **리셋 기능 (UI-SPEC 사양):**
   - Drawer footer에 리셋 아이콘 버튼 추가 (`tabler:refresh`)
   - 클릭 시 `ConfirmDialog` 열기:
     - heading: "Restablecer Permisos"
     - body: "Queres restablecer todos los permisos de este usuario a los valores del rol? Esta accion no se puede deshacer."
     - confirm: "Si, restablecer" (color="error")
     - cancel: "No, conservar permisos"
   - 확인 시: `apiConnector.post(`/user-functions/reset/${userId}`)` 호출 -> 성공 시 drawer 닫기 + toast

4. **리셋 아이콘 버튼 tooltip:** "Restablecer permisos del rol"

ESLint: `return` 전 빈 줄, `//` 주석 전 빈 줄, 세미콜론 없음, single quote.
  </action>
  <verify>
    <automated>cd ventago-app && npx next lint --file src/views/users/components/UserPermissionsDrawer.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `UserPermissionsDrawer.tsx`에 `import CrudActionRow` 포함
    - `UserPermissionsDrawer.tsx`에 `variant='user-override'` 포함
    - `UserPermissionsDrawer.tsx`에 `roleActionMap` state 포함
    - `UserPermissionsDrawer.tsx`에 `user-functions/actions` URL 포함
    - `UserPermissionsDrawer.tsx`에 `user-functions/reset` URL 포함
    - `UserPermissionsDrawer.tsx`에 `ConfirmDialog` import 및 사용 포함
    - `UserPermissionsDrawer.tsx`에 `'Restablecer Permisos'` 문자열 포함
    - ESLint 에러 없음
  </acceptance_criteria>
  <done>UserPermissionsDrawer가 CRUD action 편집, role 기본값 대비 override 표시 (amber 보더), 리셋 기능 동작</done>
</task>

<task type="auto">
  <name>Task 2: 401 페이지 + AclGuard 수정 + 네비게이션 권한 숨김 + 모든 페이지 aclAbilities 설정 (per D-02 system-wide)</name>
  <files>
    ventago-app/src/pages/401.tsx,
    ventago-app/src/@core/components/auth/AclGuard.tsx,
    ventago-app/src/configs/acl.ts,
    ventago-app/src/navigation/vertical/index.ts,
    ventago-app/src/layouts/components/acl/CanViewNavLink.tsx,
    ventago-app/src/layouts/components/acl/CanViewNavGroup.tsx,
    ventago-app/src/pages/productos/index.tsx,
    ventago-app/src/pages/nueva-venta/index.tsx,
    ventago-app/src/pages/ventas/index.tsx,
    ventago-app/src/pages/ventas/detalle/[id].tsx,
    ventago-app/src/pages/gastos/index.tsx,
    ventago-app/src/pages/caja/index.tsx,
    ventago-app/src/pages/caja/detalle/[id].tsx,
    ventago-app/src/pages/caja-fuerte/index.tsx,
    ventago-app/src/pages/control-de-caja/index.tsx,
    ventago-app/src/pages/control-de-caja/detalle/[id].tsx,
    ventago-app/src/pages/sucursales/index.tsx,
    ventago-app/src/pages/sucursales/[id]/impresora.tsx,
    ventago-app/src/pages/usuarios/index.tsx,
    ventago-app/src/pages/talleres/vendors/index.tsx,
    ventago-app/src/pages/talleres/pedidos/index.tsx,
    ventago-app/src/pages/talleres/dashboard/index.tsx,
    ventago-app/src/pages/dashboards/index.tsx,
    ventago-app/src/pages/dashboards/ventas/index.tsx,
    ventago-app/src/pages/dashboards/producto/index.tsx,
    ventago-app/src/pages/dashboards/stock/index.tsx,
    ventago-app/src/pages/dashboards/fabrica/index.tsx,
    ventago-app/src/pages/dashboards/talleres/index.tsx,
    ventago-app/src/pages/dashboards/admin/index.tsx,
    ventago-app/src/pages/reportes/index.tsx,
    ventago-app/src/pages/reportes-v2/[[...slug]].tsx,
    ventago-app/src/pages/precios/index.tsx,
    ventago-app/src/pages/configuracion/productos/index.tsx,
    ventago-app/src/pages/configuracion/ventas/index.tsx,
    ventago-app/src/pages/admin/tiendas/index.tsx,
    ventago-app/src/pages/admin/registros/index.tsx,
    ventago-app/src/pages/admin/auditoria/index.tsx,
    ventago-app/src/pages/admin/suscripcion/index.tsx,
    ventago-app/src/pages/admin/permisos/index.tsx,
    ventago-app/src/pages/admin/generar-token.tsx,
    ventago-app/src/pages/admin/soporte-remoto.tsx,
    ventago-app/src/pages/admin/ventas/index.tsx,
    ventago-app/src/pages/herramientas/print-agent/index.tsx,
    ventago-app/src/pages/perfil/index.tsx,
    ventago-app/src/pages/cliente-vista/index.tsx,
    ventago-app/src/pages/codigo-vista/index.tsx,
    ventago-app/src/pages/reportes/alertas/index.tsx,
    ventago-app/src/pages/reportes/breve-venta/index.tsx,
    ventago-app/src/pages/reportes/cheque-estado/index.tsx,
    ventago-app/src/pages/reportes/clientes-credito/index.tsx,
    ventago-app/src/pages/reportes/corregido/index.tsx,
    ventago-app/src/pages/reportes/dashboards/index.tsx,
    ventago-app/src/pages/reportes/facturacion/index.tsx,
    ventago-app/src/pages/reportes/fallados/index.tsx,
    ventago-app/src/pages/reportes/gastos/index.tsx,
    ventago-app/src/pages/reportes/ingreso/index.tsx,
    ventago-app/src/pages/reportes/items/index.tsx,
    ventago-app/src/pages/reportes/movidos/index.tsx,
    ventago-app/src/pages/reportes/reservado/index.tsx,
    ventago-app/src/pages/reportes/stocks/index.tsx,
    ventago-app/src/pages/reportes/vendedor/index.tsx,
    ventago-app/src/pages/reportes/ventas/index.tsx
  </files>
  <read_first>
    ventago-app/src/@core/components/auth/AclGuard.tsx,
    ventago-app/src/configs/acl.ts,
    ventago-app/src/navigation/vertical/index.ts,
    ventago-app/src/layouts/components/acl/CanViewNavLink.tsx,
    ventago-app/src/layouts/components/acl/CanViewNavGroup.tsx,
    ventago-app/src/pages/404.tsx,
    ventago-app/src/pages/productos/index.tsx,
    ventago-app/src/pages/nueva-venta/index.tsx,
    ventago-app/src/pages/gastos/index.tsx,
    ventago-app/src/pages/caja/index.tsx,
    ventago-app/src/pages/usuarios/index.tsx,
    ventago-app/src/pages/sucursales/index.tsx,
    ventago-app/src/pages/talleres/vendors/index.tsx,
    ventago-app/src/pages/dashboards/index.tsx,
    ventago-app/src/pages/precios/index.tsx,
    ventago-app/src/pages/configuracion/productos/index.tsx,
    ventago-app/src/pages/admin/tiendas/index.tsx,
    ventago-app/src/pages/reportes/index.tsx,
    ventago-app/src/pages/reportes-v2/[[...slug]].tsx,
    ventago-app/src/pages/perfil/index.tsx
  </read_first>
  <action>
1. **신규 파일: `ventago-app/src/pages/401.tsx`** (이미 존재할 수 있음 -- 기존 AclGuard.tsx가 import하고 있음. 없으면 생성, 있으면 UI-SPEC 사양으로 업데이트)
   - UI-SPEC 사양:
     ```tsx
     import { Box, Button, Typography } from '@mui/material'
     import { useRouter } from 'next/router'
     import Icon from 'src/@core/components/icon'
     import BlankLayout from 'src/@core/layouts/BlankLayout'

     const UnauthorizedPage = () => {
       const router = useRouter()

       return (
         <Box
           role='alert'
           sx={{
             display: 'flex',
             flexDirection: 'column',
             alignItems: 'center',
             justifyContent: 'center',
             minHeight: '100vh',
             textAlign: 'center',
             p: 4
           }}
         >
           <Icon icon='tabler:shield-off' fontSize={64} color='error' />

           <Typography variant='h4' sx={{ mt: 4 }}>
             Sin Autorización
           </Typography>

           <Typography variant='body1' sx={{ mt: 2, color: 'text.secondary' }}>
             No tenés permiso para acceder a esta sección.
           </Typography>

           <Button
             variant='contained'
             color='primary'
             sx={{ mt: 4 }}
             onClick={() => router.push('/')}
           >
             Volver al inicio
           </Button>
         </Box>
       )
     }

     UnauthorizedPage.getLayout = (page: any) => <BlankLayout>{page}</BlankLayout>

     export default UnauthorizedPage
     ```

2. **AclGuard.tsx 수정 -- 기존 inline NotAuthorized 렌더링 패턴 유지 (이미 올바르게 동작 중):**
   - 현재 AclGuard.tsx 이미 `import NotAuthorized from 'src/pages/401'` + 권한 없으면 `<BlankLayout><NotAuthorized /></BlankLayout>` 렌더
   - 변경점: `buildAbilityFor` 호출에 3번째 인자 `auth.user.permissions` 전달 (Plan 03에서 이미 수행 -- 확인만):
     ```typescript
     ability = buildAbilityFor(auth.user.roles, aclAbilities.subject, auth.user.permissions)
     ```
   - `ability.can(aclAbilities.action, aclAbilities.subject)` 체크가 granular permissions 기반으로 동작하는지 확인
   - **리다이렉트 불필요 -- inline <NotAuthorized /> 렌더가 기존 패턴이며 올바른 접근**

3. **defaultACLObj 변경:**
   - `ventago-app/src/configs/acl.ts`에서 `defaultACLObj`를 안전한 기본값으로 변경:
     ```typescript
     export const defaultACLObj: ACLObj = {
       action: 'read',
       subject: 'all'  // 'all'에 대한 read는 인증된 모든 유저에게 허용 (홈, 프로필 등)
     }
     ```
   - 이렇게 하면 `.acl` 미설정 페이지도 최소한 `read:all` 체크를 수행

4. **모든 인증 페이지에 `.acl` 프로퍼티 설정 (per D-02 system-wide):**

   각 페이지 파일의 default export 컴포넌트에 `.acl` 프로퍼티 추가. 패턴:
   ```typescript
   // 파일 하단, export default 이후
   ComponentName.acl = { action: 'read', subject: 'function-slug' }
   ```

   **전체 매핑 (DB functions.slug와 일치해야 함 -- 실행 전 DB에서 slug 목록 확인):**

   | 페이지 파일 | .acl subject | 비고 |
   |---|---|---|
   | `productos/index.tsx` | `'productos'` | 상품 관리 |
   | `nueva-venta/index.tsx` | `'ventas'` | POS 판매 |
   | `ventas/index.tsx` | `'ventas'` | 판매 내역 |
   | `ventas/detalle/[id].tsx` | `'ventas'` | 판매 상세 |
   | `gastos/index.tsx` | `'gastos'` | 비용 관리 |
   | `caja/index.tsx` | `'caja'` | 금전함 |
   | `caja/detalle/[id].tsx` | `'caja'` | 금전함 상세 |
   | `caja-fuerte/index.tsx` | `'caja-fuerte'` | 금고 |
   | `control-de-caja/index.tsx` | `'control-de-caja'` | 금전함 통제 |
   | `control-de-caja/detalle/[id].tsx` | `'control-de-caja'` | 금전함 통제 상세 |
   | `sucursales/index.tsx` | `'sucursales'` | 지점 관리 |
   | `sucursales/[id]/impresora.tsx` | `'sucursales'` | 지점 프린터 |
   | `usuarios/index.tsx` | `'usuarios'` | 사용자 관리 |
   | `talleres/vendors/index.tsx` | `'talleres'` | 외주 업체 |
   | `talleres/pedidos/index.tsx` | `'talleres'` | 외주 발주 |
   | `talleres/dashboard/index.tsx` | `'talleres'` | 외주 대시보드 |
   | `dashboards/index.tsx` | `'dashboards'` | 대시보드 메인 |
   | `dashboards/ventas/index.tsx` | `'dashboards'` | 판매 대시보드 |
   | `dashboards/producto/index.tsx` | `'dashboards'` | 상품 대시보드 |
   | `dashboards/stock/index.tsx` | `'dashboards'` | 재고 대시보드 |
   | `dashboards/fabrica/index.tsx` | `'dashboards'` | 공장 대시보드 |
   | `dashboards/talleres/index.tsx` | `'dashboards'` | 외주 대시보드 |
   | `dashboards/admin/index.tsx` | `'admin-dashboard'` | 관리자 대시보드 |
   | `reportes/index.tsx` | `'reportes'` | 보고서 메인 |
   | `reportes-v2/[[...slug]].tsx` | `'reportes'` | 보고서 v2 셸 |
   | `reportes/alertas/index.tsx` | `'reportes'` | 알림 보고서 |
   | `reportes/breve-venta/index.tsx` | `'reportes'` | 간이 판매 보고서 |
   | `reportes/cheque-estado/index.tsx` | `'reportes'` | 수표 상태 보고서 |
   | `reportes/clientes-credito/index.tsx` | `'reportes'` | 외상 고객 보고서 |
   | `reportes/corregido/index.tsx` | `'reportes'` | 수정 보고서 |
   | `reportes/dashboards/index.tsx` | `'reportes'` | 보고서 대시보드 |
   | `reportes/facturacion/index.tsx` | `'reportes'` | 청구 보고서 |
   | `reportes/fallados/index.tsx` | `'reportes'` | 실패 보고서 |
   | `reportes/gastos/index.tsx` | `'reportes'` | 비용 보고서 |
   | `reportes/ingreso/index.tsx` | `'reportes'` | 수입 보고서 |
   | `reportes/items/index.tsx` | `'reportes'` | 항목 보고서 |
   | `reportes/movidos/index.tsx` | `'reportes'` | 이동 보고서 |
   | `reportes/reservado/index.tsx` | `'reportes'` | 예약 보고서 |
   | `reportes/stocks/index.tsx` | `'reportes'` | 재고 보고서 |
   | `reportes/vendedor/index.tsx` | `'reportes'` | 판매원 보고서 |
   | `reportes/ventas/index.tsx` | `'reportes'` | 판매 보고서 |
   | `precios/index.tsx` | `'precios'` | 가격 관리 |
   | `configuracion/productos/index.tsx` | `'configuracion'` | 상품 설정 |
   | `configuracion/ventas/index.tsx` | `'configuracion'` | 판매 설정 |
   | `admin/tiendas/index.tsx` | `'admin-tiendas'` | 매장 관리 |
   | `admin/registros/index.tsx` | `'admin-registros'` | 관리자 등록 |
   | `admin/auditoria/index.tsx` | `'admin-auditoria'` | 감사 |
   | `admin/suscripcion/index.tsx` | `'admin-suscripcion'` | 구독 관리 |
   | `admin/permisos/index.tsx` | `'admin-permisos'` | 권한 관리 |
   | `admin/generar-token.tsx` | `'admin-generar-token'` | 토큰 생성 |
   | `admin/soporte-remoto.tsx` | `'admin-soporte-remoto'` | 원격 지원 |
   | `admin/ventas/index.tsx` | `'admin-ventas'` | 관리자 판매 |
   | `herramientas/print-agent/index.tsx` | `'herramientas'` | 도구/다운로드 |
   | `perfil/index.tsx` | -- | 프로필은 `.acl` 미설정 (모든 인증 유저 접근 가능, defaultACLObj로 fallback) |
   | `cliente-vista/index.tsx` | `'ventas'` | 고객 뷰 (판매 관련) |
   | `codigo-vista/index.tsx` | `'ventas'` | 코드 뷰 (판매 관련) |

   **주의사항:**
   - `.acl` subject는 DB `functions.slug` 값과 정확히 일치해야 함
   - 실행 전 반드시 DB에서 `SELECT slug FROM functions` 조회하여 slug 목록 확인
   - DB에 없는 slug을 사용하면 FunctionPermissionGuard가 `fn` 못 찾고 통과시킴 (안전하지만 의도와 다름)
   - admin 하위 페이지의 slug이 DB에 없으면 DB에 해당 Function 레코드도 추가 필요
   - `perfil/index.tsx`는 모든 인증 유저가 접근 가능해야 하므로 `.acl` 미설정 (defaultACLObj fallback)
   - 모든 `.acl`의 action은 `'read'` (페이지 접근 = 읽기 권한)

   **적용 방법 (각 페이지 파일에 동일 패턴):**
   ```typescript
   // 파일 하단 (export default 이후)
   // 또는 .getLayout이 있으면 그 아래에 추가
   PageComponent.acl = {
     action: 'read',
     subject: 'function-slug-here'
   }
   ```

5. **네비게이션 메뉴에 action/subject 추가 (per D-04):**
   - `navigation/vertical/index.ts`에서 동적으로 생성되는 nav items에 `action: 'read'` + `subject: module.slug 또는 functions.slug` 추가
   - `getAppChildren()` 함수의 children 매핑에서:
     ```typescript
     const children = app.modules
       .filter(mod => !mod.isAuxiliary && !(mod.url && hiddenModuleUrls.has(mod.url)))
       .map(mod => ({
         title: (mod.url && moduleNameOverrides[mod.url]) || mod.name,
         icon: mod.icon,
         path: mod.url,
         action: 'read',
         subject: mod.slug || mod.name.toLowerCase().replace(/\s+/g, '-'),
       }))
     ```
   - superadmin 하드코딩 메뉴에도 동일하게 subject 추가
   - `getAuxiliaryItems()`에도 subject 추가

6. **CanViewNavLink / CanViewNavGroup 안전 가드:**
   - `CanViewNavLink.tsx`에서 action/subject가 undefined이면 기본적으로 보이도록:
     ```typescript
     if (navLink && navLink.auth === false) {
       return <>{children}</>
     }

     // action/subject 미설정이면 기본 표시 (기존 메뉴 호환)
     if (!navLink?.action || !navLink?.subject) {
       return <>{children}</>
     }

     return ability && ability.can(navLink.action, navLink.subject) ? <>{children}</> : null
     ```
   - `CanViewNavGroup.tsx`에도 동일한 가드 적용 (navGroup에 action/subject가 있으면 체크, 없으면 통과)

ESLint: `return` 전 빈 줄, `//` 주석 전 빈 줄, 세미콜론 없음, single quote.
  </action>
  <verify>
    <automated>cd ventago-app && npx next lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ventago-app/src/pages/401.tsx` 파일 존재하고 `'Sin Autorización'` + `'tabler:shield-off'` + `role='alert'` + `'Volver al inicio'` 포함
    - `AclGuard.tsx`에서 `buildAbilityFor(auth.user.roles, aclAbilities.subject, auth.user.permissions)` 호출 (3번째 인자 permissions)
    - `AclGuard.tsx`에서 `ability.can(aclAbilities.action, aclAbilities.subject)` 체크 후 실패 시 `<NotAuthorized />` inline 렌더
    - `acl.ts`의 `defaultACLObj`가 `action: 'read', subject: 'all'` 로 변경됨
    - `navigation/vertical/index.ts`의 getAppChildren()에서 각 module item에 `action: 'read'` + `subject` 추가
    - `CanViewNavLink.tsx`에서 action/subject undefined이면 `<>{children}</>` 반환 (기존 메뉴 호환)
    - 다음 페이지 파일에 `.acl = { action: 'read', subject: '...' }` 설정됨:
      - `productos/index.tsx` (subject: 'productos')
      - `nueva-venta/index.tsx` (subject: 'ventas')
      - `ventas/index.tsx` (subject: 'ventas')
      - `gastos/index.tsx` (subject: 'gastos')
      - `caja/index.tsx` (subject: 'caja')
      - `caja-fuerte/index.tsx` (subject: 'caja-fuerte')
      - `control-de-caja/index.tsx` (subject: 'control-de-caja')
      - `sucursales/index.tsx` (subject: 'sucursales')
      - `usuarios/index.tsx` (subject: 'usuarios')
      - `talleres/vendors/index.tsx` (subject: 'talleres')
      - `talleres/pedidos/index.tsx` (subject: 'talleres')
      - `dashboards/index.tsx` (subject: 'dashboards')
      - `reportes/index.tsx` (subject: 'reportes')
      - `reportes-v2/[[...slug]].tsx` (subject: 'reportes')
      - `precios/index.tsx` (subject: 'precios')
      - `configuracion/productos/index.tsx` (subject: 'configuracion')
      - `configuracion/ventas/index.tsx` (subject: 'configuracion')
      - `admin/tiendas/index.tsx` (subject: 'admin-tiendas')
      - `admin/permisos/index.tsx` (subject: 'admin-permisos')
      - 기타 모든 admin, reportes, dashboards 하위 페이지
    - `perfil/index.tsx`에는 `.acl` 미설정 (모든 인증 유저 접근 가능)
    - ESLint 에러 없음
  </acceptance_criteria>
  <done>401 페이지 표시, AclGuard가 permissions 기반 ability.can() 체크 후 NotAuthorized inline 렌더, 모든 인증 페이지에 granular aclAbilities 설정, 네비게이션 메뉴 권한 기반 숨김</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 기능 검증 -- CRUD 권한 관리 + 메뉴 숨김 + 401 페이지 + FunctionPermissionGuard 동작</name>
  <files>ventago-app/src/pages/401.tsx</files>
  <action>
Phase 14 전체 기능 수동 검증. Task 1, Task 2의 자동 구현 완료 후 사용자가 직접 확인.
  </action>
  <what-built>Phase 14 전체: DB 스키마(RoleFunctionAction/UserFunctionAction) + 백엔드 Guard(FunctionPermissionGuard) + /me permissions 맵 + 프론트 CASL granular 체크 + 모든 페이지 aclAbilities + 네비게이션 숨김 + UserPermissionsDrawer CRUD</what-built>
  <how-to-verify>
아래 단계를 순서대로 테스트:

1. **서버 실행:** `npm run dev` 로 백엔드 + 프론트엔드 실행

2. **superadmin 로그인 -> 역할 권한 편집:**
   - Usuarios -> 역할 카드에서 편집 아이콘 클릭
   - RolePermissionsDrawer 열림 -> 각 Function에 C/R/U/D 4개 칩 확인
   - vendedor 역할에서 상품(productos) function의 create/update/delete 비활성화 (read만 유지) 후 저장
   - toast 메시지 "Permisos actualizados correctamente" 확인

3. **vendedor 계정 로그인 -> 읽기 전용 확인:**
   - 사이드바에 상품 메뉴 보임 (read 권한 있으므로)
   - 상품 페이지 접근 가능
   - 생성/수정/삭제 버튼이 숨겨졌거나 비활성화됨 (향후 UI 레벨에서 추가 구현 가능)

4. **vendedor 역할에서 비용(gastos) function 전체 비활성화:**
   - superadmin으로 다시 로그인 -> vendedor 역할에서 gastos 4개 action 모두 해제 후 저장
   - vendedor로 로그인 -> 사이드바에서 비용 메뉴 숨김 확인
   - 브라우저에서 `/gastos` URL 직접 입력 -> 401 "Sin Autorización" 페이지 표시 확인
   - "Volver al inicio" 버튼 클릭 -> 홈으로 이동 확인

5. **FunctionPermissionGuard 백엔드 검증 (curl):**
   - vendedor의 JWT 토큰으로 보호된 API 엔드포인트 호출 시 403 반환 확인
   - superadmin의 JWT 토큰으로 동일 엔드포인트 호출 시 정상 200 반환 확인

6. **유저별 override -> UserPermissionsDrawer:**
   - Usuarios -> 특정 vendedor 유저 선택 -> 권한 편집 Drawer 열기
   - CrudActionRow에 C/R/U/D 칩 표시 + role 기본값과 다른 칩에 amber 보더 확인
   - override 변경 후 저장 -> toast 확인
   - "Restablecer" 아이콘 클릭 -> ConfirmDialog -> "Si, restablecer" 클릭 -> role 기본값 복원 확인
  </how-to-verify>
  <verify>수동 검증 -- 위 6단계 모두 통과</verify>
  <done>Phase 14 전체 기능이 정상 동작 확인됨</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser URL -> AclGuard | 권한 없는 페이지 URL 직접 접근 차단 |
| navigation render -> ability check | 메뉴 숨김으로 비인가 경로 노출 방지 |
| client -> API (FunctionGuard) | 백엔드 Guard가 실제 권한 검증 (프론트 우회 불가) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Elevation of Privilege | URL 직접 접근 | mitigate | AclGuard가 ability.can 체크 후 NotAuthorized inline 렌더 |
| T-14-10 | Information Disclosure | 사이드바 메뉴 | mitigate | CanViewNavLink가 ability 체크 실패 시 null 반환 (DOM에서 제거, display:none 아님) |
| T-14-11 | Elevation of Privilege | UserPermissionsDrawer | mitigate | 백엔드 enforceScope로 gerente/admin 범위 제한 (Plan 02에서 구현) |
</threat_model>

<verification>
- 401.tsx 페이지가 정상 렌더
- 권한 없는 메뉴가 사이드바에서 제거됨
- 모든 인증 페이지에 .acl 프로퍼티 설정됨 (perfil 제외)
- vendedor가 read-only function 페이지에 접근 가능하되 전체 비활성화 function은 401 표시
- UserPermissionsDrawer에서 override 표시 + 리셋 동작
- FunctionPermissionGuard가 권한 없는 유저에게 403 반환하고 superadmin은 통과
</verification>

<success_criteria>
- UserPermissionsDrawer CRUD action 편집 + role baseline 대비 override amber 표시
- 리셋 기능 ConfirmDialog 확인 후 role 기본값 복원
- 401 페이지가 권한 없는 URL 접근 시 inline 표시 (AclGuard 내 NotAuthorized 렌더)
- 사이드바 메뉴에서 권한 없는 항목 숨김
- 모든 인증 페이지에 granular aclAbilities 설정 완료 (D-02 system-wide 충족)
- FunctionPermissionGuard가 403 반환 + superadmin bypass 동작 확인 (Task 3 검증)
</success_criteria>

<output>
After completion, create `.planning/phases/14-permisos-control-ui/14-04-SUMMARY.md`
</output>
