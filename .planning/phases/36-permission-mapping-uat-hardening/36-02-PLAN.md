# Phase 36 Plan 02: 운영 PG10 적용 RUNBOOK 작성

**Created:** 2026-05-23
**Phase:** 36 (권한매핑보강+UAT감업)
**Requirements covered:** R2 (SPEC.md)
**Estimated effort:** 90분 (RUNBOOK 작성 + 사용자 검토 대기)

## Goal

`.planning/phases/35-activity-ledger/35-RUNBOOK-PROD.md` 작성 — Phase 35 운영 PG10 적용 절차 step-by-step. 사용자가 SSH alias `jhkim-server` 환경에서 실행 가능한 명령 라인 + 예상 결과 + 실패 시 롤백 절차 포함.

## Tasks

### Task 1: RUNBOOK 5 sections 초안 작성

**File:** `.planning/phases/35-activity-ledger/35-RUNBOOK-PROD.md`

**Outline:**

```markdown
# Phase 35 운영 적용 RUNBOOK (PG10)

**Target:** 운영 서버 srv803182 (62.72.7.245) / PostgreSQL 10 / ventago DB
**Pre-conditions:** 사용자 0명 시간대 (UTC 시간 확인) / 운영자 SSH 접근 가능
**Estimated downtime:** 0분 (모든 DDL Phase 35 시점 완료, 본 RUNBOOK 은 마이그레이션 INSERT + Docker 재배포만)

## Section 0: 사전 점검 (10분)

### 0.1 운영 DB 백업
ssh jhkim-server "sudo -u postgres pg_dump ventago -Fc -f /tmp/ventago-pre-phase35-$(date +%Y%m%d-%H%M%S).dump"
ssh jhkim-server "ls -lh /tmp/ventago-pre-phase35-*.dump | tail -1"

### 0.2 운영 서비스 헬스 체크
ssh jhkim-server "curl -s -o /dev/null -w '%{http_code}\n' https://newapi.coolsistema.com/api"
ssh jhkim-server "docker ps --filter name=api_ventago --format '{{.Status}}'"

### 0.3 활성 세션 0명 확인
ssh jhkim-server "sudo -u postgres psql -d ventago -c 'SELECT COUNT(*) FROM active_sessions;'"
# 예상: 0 (또는 본 운영자 자신만 1)

### 0.4 사용자 승인
"운영 적용 시작합니다. 진행해도 됩니까?" — 사용자 confirm 후 다음 단계.

## Section 1: 마이그레이션 SQL 적용 순서 (5분)

### 1.1 Phase 35 base schema
ssh jhkim-server "sudo -u postgres psql -d ventago -v ON_ERROR_STOP=1" < api-ventago/migrations/phase35-activity-ledger.sql

### 1.2 stock.movement permission 등록
ssh jhkim-server "sudo -u postgres psql -d ventago -v ON_ERROR_STOP=1" < api-ventago/migrations/phase35-stock-movement-permission.sql

### 1.3 Phase 36 role_function_actions 보강 (Plan 01 산출물)
ssh jhkim-server "sudo -u postgres psql -d ventago -v ON_ERROR_STOP=1" < api-ventago/migrations/phase36-stock-movement-actions-backfill.sql

### 1.4 마이그레이션 검증
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT COUNT(*) FROM information_schema.columns WHERE table_name='sales' AND column_name IN ('activity_type','origin_branch_id','target_branch_id');
\""
# 예상: 3 (3 신규 컬럼 모두 존재)

ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT COUNT(*) FROM role_function_actions rfa JOIN role_functions rf ON rf.id=rfa.role_function_id WHERE rf.function_id=149;
\""
# 예상: 48 (12 role_functions × 4 actions)

## Section 2: Backfill 실행 (15분)

### 2.1 dry-run
./api-ventago/migrations/phase35-backfill-dry-run.sh prod

### 2.2 dry-run 결과 검증
# (출력) backfilled_rows = movido + fallado 그룹 수
# (출력) failure_rows = 0 또는 < 5% (실패는 backfill_failures 에 기록)

### 2.3 사용자 승인 후 실제 commit
ssh jhkim-server "sudo -u postgres psql -d ventago -v ON_ERROR_STOP=1" < api-ventago/migrations/phase35-backfill-movidos-to-sales.sql

### 2.4 backfill 결과 검증
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT activity_type, COUNT(*) FROM sales GROUP BY activity_type;
\""
# (예상) sale: 기존 동일, movido: 신규 추가, fallado: 신규 추가

ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT COUNT(*) FROM backfill_failures;
\""
# (예상) 0 또는 < 5%

### 2.5 Phase 36.1 REG-2 데이터 복원 (필요 시)
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  UPDATE sales SET daily_number = 0 WHERE activity_type IN ('movido','fallado') AND daily_number != 0;
\""

## Section 3: Hotfix 코드 배포 (20분)

### 3.1 api-ventago Docker 빌드 + push (CI 자동)
# Jenkins 또는 GitHub Actions 가 api-ventago commit 감지 후 빌드
# 운영 적용 commit:
#   - de8d0ae: 5건 Phase 35 hotfix
#   - f3ade81: Phase 36.1 REG-1 + REG-2 hotfix

### 3.2 ventago-app Docker 빌드 + push (CI 자동)
# 운영 적용 commit:
#   - 0151df5: tab title + multi-branch admin fallback
#   - d215bbb: VentaVista toolbar Movidos/Fallados 체크박스

### 3.3 운영 컨테이너 재배포
ssh jhkim-server "cd /opt/coolsistema/api-ventago && docker compose pull && docker compose up -d"
ssh jhkim-server "cd /opt/coolsistema/ventago-app && docker compose pull && docker compose up -d"

### 3.4 헬스 체크
ssh jhkim-server "curl -s -o /dev/null -w '%{http_code}\n' https://newapi.coolsistema.com/api"
ssh jhkim-server "curl -s -o /dev/null -w '%{http_code}\n' https://app.coolsistema.com"

## Section 4: 회귀 검증 (10분)

### 4.1 ventaVista 활동 분류
- /ventas 진입 → Resumen 테이블 노출 (8 컬럼: SUCURSAL · VENTAS · PRENDAS · DESC · MOV+ · MOV− · FAL · NETO)
- 행 클릭 → URL ?originBranchId=X + chip [지점명 ✕]
- 셀 클릭 → 2 chip
- toolbar 의 Movidos/Fallados 체크박스 토글 → activityType filter sync

### 4.2 Stock Cockpit MOV+/MOV−/FAL + OFFSET
- /reportes/stocks 진입 → PanelB ItemTable 의 MOV+ / MOV− / FAL 컬럼 노출
- 임의 product 의 OFFSET = 0 확인 (movido/fallado 흡수 안 됨)
- STOCK 등식 검증: INGRESO − VENTA + MOV+ − MOV− − FAL + OFFSET = STOCK

### 4.3 권한 매트릭스
- /configuracion/permisos 탭 1 → "stock.movement" 행의 모든 5종 role 에 ✓ 표시

### 4.4 cURL POST /stocks/movement
TOKEN=...  # 운영 vendedor JWT
curl -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"type":"movido","originBranchId":1,"targetBranchId":2,"items":[{"productId":N,"quantity":1}]}' \
  https://newapi.coolsistema.com/api/stocks/movement
# 예상: HTTP 200 + {"saleId": NNN, "success": true}

## Section 5: 롤백 절차 (긴급 시)

### 5.1 Docker 이전 태그 복구
ssh jhkim-server "cd /opt/coolsistema/api-ventago && docker compose pull <PREV_TAG> && docker compose up -d"
ssh jhkim-server "cd /opt/coolsistema/ventago-app && docker compose pull <PREV_TAG> && docker compose up -d"

### 5.2 마이그레이션 ROLLBACK (DDL 컬럼은 유지, 데이터만)
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  -- backfilled sales 제거 (notes prefix 매칭)
  DELETE FROM sale_items WHERE sale_id IN (SELECT id FROM sales WHERE notes LIKE '[Backfill Phase 35]%');
  DELETE FROM sales WHERE notes LIKE '[Backfill Phase 35]%';
  -- role_function_actions 보강 분 제거 (function_id=149)
  -- ⚠ 주의: Phase 35 시점 기존 4 행도 함께 제거됨 → 보존 필요 시 별도 처리
  DELETE FROM role_function_actions WHERE role_function_id IN (SELECT id FROM role_functions WHERE function_id=149);
\""

### 5.3 pg_dump 복원 (최후 수단)
ssh jhkim-server "sudo -u postgres pg_restore -d ventago_new < /tmp/ventago-pre-phase35-*.dump"
# 별도 DB 로 복원 후 swap (운영 다운타임 발생 — 최후 수단)

---

**Sign-off:** 사용자 검증 완료 시 .planning/STATE.md 의 Phase 35 status → complete.
```

**Acceptance:**
- 파일 존재 + 5 sections + 각 명령 라인이 jhkim-server SSH alias + PG10 syntax 사용
- 각 section 별 estimated time + 실패 시 다음 액션 명시

### Task 2: 사용자 검토 + 승인

**Procedure:**
- 사용자 (junghokim10@gmail.com) 에게 RUNBOOK 파일 위치 알림
- 사용자가 1회 review + "OK 진행" 또는 "수정 요청" 회신
- 수정 요청 시 반영 후 재검토

**Acceptance:** 사용자 승인 메시지 수신 + 본 plan 의 commit message 에 사용자 ID 명시.

## Out of Scope

- 실제 운영 SSH 실행 → 본 plan 범위 외. RUNBOOK 작성 + 승인까지만.

## Dependencies

- Plan 01 (`phase36-stock-movement-actions-backfill.sql`) 가 RUNBOOK Section 1.3 에 참조됨

## Commit Strategy

```
docs(36-02): add 35-RUNBOOK-PROD.md — Phase 35 운영 적용 절차 (5 sections)
```

파일: `.planning/phases/35-activity-ledger/35-RUNBOOK-PROD.md`
