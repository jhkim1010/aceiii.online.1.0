---
phase: 84-produccion-cantidad-ledger-y-flujo-unico
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - api-ventago/migrations/2026-08-XX-phase84-settlements-store-id.sql
  - api-ventago/migrations/2026-08-XX-phase84-lote-route-steps.sql
  - api-ventago/src/app/subcon/subcon-settlements/
autonomous: true
requirements: [R6]

must_haves:
  truths:
    - "정산이 매장 경계를 넘지 않는다"
    - "다음 공정이 로트별로 결정된다"
---

<objective>
격리 구멍과 공정 순서 모호성을 봉합한다. **지금이 유일한 기회다** — `talleres_settlements`
가 0건일 때 컬럼을 넣는 것과, 데이터가 쌓인 뒤 넣는 것은 난이도가 다르다.
</objective>

<constraints>
- ★ **전역 `UNIQUE(store_id, order)` 를 넣지 마라** (CODEX). 모든 제품이 같은 공정을 거치지
  않으므로 전역 순서가 유일해야 한다는 업무 규칙이 없다. 로트별 route 가 맞다.
- store 6 의 order 중복(planchero 5 · costurero 5)은 **데이터로** 정리한다(costurero 는 비활성).
</constraints>

<tasks>
1. `talleres_settlements.store_id` 추가 + FK + 매장 범위 인덱스 + **모든 조회에 tenant 조건**.
   기존 `LEFT JOIN` 으로 전 매장 노출되던 경로(G6)를 함께 막는다.
2. `talleres_lote_route_steps(lot_id, sequence_no, stage_id, vendor_id, status)` +
   `UNIQUE(lot_id, sequence_no)`. 기존 `routing_path` jsonb 에서 이관.
3. 최종 공정 판정(84-01)이 이 테이블을 단일 출처로 쓰게 전환.
</tasks>

<verification>
- 타 매장 정산이 조회되지 않는다(테넌트 테스트)
- 로트마다 다음 공정이 유일하게 결정된다
</verification>

<success-criteria>
- 정산에 데이터를 넣기 전에 경계가 서 있다
</success-criteria>
