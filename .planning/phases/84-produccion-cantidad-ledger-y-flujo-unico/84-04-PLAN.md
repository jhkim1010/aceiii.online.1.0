---
phase: 84-produccion-cantidad-ledger-y-flujo-unico
plan: 04
type: execute
wave: 4
depends_on: [84-03]
files_modified:
  - api-ventago/src/app/subcon/
  - ventago-app/src/views/talleres/
autonomous: false
requirements: [R4]

must_haves:
  truths:
    - "자동으로 추측해서 고치지 않는다 — 사람이 확인하고 게시한다"
---

<objective>
운영에 이미 쌓인 모순을 **목록으로 만든다.** 고치지는 않는다.
</objective>

<constraints>
- ★ **자동 복구 금지** (CODEX). 로트 10 은 사람이 실제 사이즈/색상 실적과 입고 지점을 확인한 뒤
  게시한다. 로트 8 의 100장은 **사유가 입력되기 전까지 손대지 않는다.**
- 추정 안분으로 재고를 만들지 않는다 — 부족분이 특정 사이즈에 몰리면 판매 재고가 허위가 된다.
</constraints>

<tasks>
1. 예외 조회: ① 최종공정 완료 + inventory PENDING ② 전 공정 완료 + 로트 미종료
   ③ envío 없이 available 감소 ④ 계획/발송/수령/불량/재고 합계 불일치 ⑤ 자재 잔액 있고 movement 0.
2. 각 항목에 **그 자리에서 푸는 행동**을 붙인다(격자 입력·지점 선택·사유 입력).
3. 로트 10 · 로트 8 을 사용자와 함께 실제로 정리한다.
</tasks>

<verification>
- 로트 10 이 실제 격자로 재고에 들어가고 `stocks`에 `type='production'` **첫 행**이 생긴다
- 로트 8 의 100장에 사유가 붙는다(또는 붙기 전까지 미해결로 남는다)
</verification>

<success-criteria>
- 생산품이 판매 가능 재고에 도달하는 경로가 **운영에서 한 번 돈다**
</success-criteria>
