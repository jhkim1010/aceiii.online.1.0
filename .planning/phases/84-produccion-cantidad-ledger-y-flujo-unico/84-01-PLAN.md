---
phase: 84-produccion-cantidad-ledger-y-flujo-unico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/src/views/talleres/components/PendingInventoryBanner.tsx
  - ventago-app/src/views/talleres/tabs/LotesTab.tsx
  - ventago-app/src/views/talleres/drawers/LoteDetailDrawer.tsx
autonomous: true
requirements: [R1]
status: DONE (2026-08-18, app `e215fc8`)

must_haves:
  truths:
    - "배너에서 한 번 눌러 실제로 입고할 수 있다"
    - "배너가 불가능한 행동을 시키지 않는다"
---

<objective>
막힌 수령을 **푸는 길**을 만든다.

★ **초안 정정 (2026-08-18 코드 대조).** 이 plan 의 원안은 "최종 수령에 격자·지점 필수화" 였다.
**그건 이미 전부 구현돼 배포돼 있다.** 착수 전 확인하지 않았으면 있는 것을 다시 만들 뻔했다:

| 원안이 만들려던 것 | 실제 상태 |
|---|---|
| 최종 공정 판정 | `routing-position.ts` 의 `postsToStock()` — 있음 |
| 색×사이즈 격자 입력 | `getVariantGrid()` + `RecepcionFormDialog` — 있음 |
| 격자·지점 필수 | `postToStock()` 이 없으면 PENDING 으로 남김 — 있음 |
| 막힌 이유 노출 | `findPendingInventory()` + `PendingInventoryBanner` — 있음 |
| 로트 완료 판정 | `evaluateLoteCompletion()` — 있음 |
| 입고 실행 | `ingresarAStock()` + `IngresoStockDialog` — 있음 |

**그런데도 로트 10 은 34일째 PENDING 이었다.** 이유가 이 plan 의 진짜 범위다.
</objective>

<constraints>
- ★ **있는 것을 다시 만들지 않는다.** 예방 장치는 완비돼 있다.
- ★ 배너는 **이미 존재하는 수령**을 가리킨다. "수령을 등록하라" 는 지시는
  `envio.pendingQuantity=0` 이라 **실행 불가능**하다(`Cantidad excede pendiente` 로 거부).
</constraints>

<tasks>
1. 배너 칩 → `?tab=lotes&lote=<id>&action=ingreso` 딥링크.
2. `LotesTab` 이 그 로트의 드로어 + 입고 다이얼로그를 연다.
3. 플래그는 한 번 쓰고 URL 에서 제거 — 안 지우면 닫아도 다시 열려 갇힌다.
4. 문구 교정: "무엇이 빠졌나" → "여기서 입고하라 · 아직 팔 수 없다".
</tasks>

<verification>
- 배너 칩 클릭 → 로트 10 의 입고 다이얼로그가 열린다
- 다이얼로그를 닫으면 다시 열리지 않는다
</verification>

<success-criteria>
- 배너에 뜬 건을 **화면 안에서** 끝낼 수 있다
</success-criteria>

<notes>
★ 이 wave 에서 배운 것: **"기능이 없다" 와 "기능에 닿을 수 없다" 는 다르다.**
운영 증상(재고 0건)만 보고 기능 부재로 진단했는데, 실제로는 전 경로가 구현돼 있고
**마지막 한 번의 클릭으로 가는 길만 없었다.** 다음 wave 들도 착수 전 코드 대조 필수.
</notes>
