---
phase: 84-produccion-cantidad-ledger-y-flujo-unico
plan: 05
type: execute
wave: 5
depends_on: [84-04]
files_modified:
  - api-ventago/migrations/2026-08-XX-phase84-material-opening-balance.sql
  - api-ventago/src/app/production/material-movements/
autonomous: true
requirements: [R5]

must_haves:
  truths:
    - "자재 잔액은 movement 에서만 움직인다"
    - "과거 이력을 추측해서 만들지 않았다"
---

<objective>
자재 재고에 **원장의 권위**를 준다. 지금은 잔액 있고 이력 0건인 자재가 7건이라
두 숫자가 애초에 무관하다.
</objective>

<constraints>
- ★ **과거 movement 를 추측해 생성하지 않는다** (CODEX). 전환 시점의 감사 가능한
  `OPENING_BALANCE` 이벤트 하나로 봉한다: 수량·측정시각·사유·입력자·근거.
- ★ **완제품과 같은 테이블로 합치지 않는다.** 같아야 하는 것은 테이블이 아니라 **원리**다 —
  movement 가 진실, 잔액은 projection, 직접 수정 금지.
- ★ **이중단위를 지금 켜지 않는다.** 16개 자재 전부 `purchase_unit` NULL 이다. 안 쓰는 기능을
  UI 에 노출하면 개념만 흐려진다. 기준 단위 하나로 원장을 안정화한 뒤 실제 사례가 생기면 켠다.
</constraints>

<tasks>
1. `OPENING_BALANCE` movement 유형 추가 + 7건 봉합(사람 확인 후).
2. `current_stock` 을 movement 와 **같은 트랜잭션**에서만 갱신되는 projection 으로.
   직접 UPDATE 경로 차단.
3. 대조 뷰 — movement 합 ≠ current_stock 인 자재 0행.
</tasks>

<verification>
- 잔액 있고 이력 없는 자재 0건
- 자재 잔액을 API 로 직접 못 바꾼다
</verification>

<success-criteria>
- "재고" 라는 말이 시스템 안에서 **한 가지 뜻**이 된다
</success-criteria>
