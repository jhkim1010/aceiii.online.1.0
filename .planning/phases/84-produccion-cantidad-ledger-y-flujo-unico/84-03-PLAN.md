---
phase: 84-produccion-cantidad-ledger-y-flujo-unico
plan: 03
type: execute
wave: 3
depends_on: [84-02]
files_modified:
  - api-ventago/migrations/2026-08-XX-phase84-lote-quantity-events.sql
  - api-ventago/src/app/subcon/lotes/
  - api-ventago/src/app/subcon/qc-items/qc-item.service.ts
autonomous: true
requirements: [R3]
status: DONE (2026-08-18, api `d9e2408` #729)

must_haves:
  truths:
    - "available_quantity 를 직접 쓰는 경로가 없다"
    - "수량이 바뀐 모든 사건에 이전값·변화량·사유·사용자·시각이 남는다"
---

<objective>
**로트 8 의 100장이 다시 사라지지 않게** 만든다. 지금 `available_quantity` 는 여러 경로가
직접 고치는 저장값이라 증발을 설명할 방법이 없다.
</objective>

<constraints>
- ★ 한 주에 완전한 새 원장을 세우지 않는다(CODEX). **최소 장치**는 감사 기록 + 직접 쓰기 차단이다.
- 이벤트 유형: `SEND` `RECEIVE` `REJECT` `REWORK` `SCRAP` `STOCK_POST` `ADJUST`.
- ★ `ADJUST` 는 **사유 필수**. 사유 없는 조정을 허용하면 원장이 다시 거짓말한다.
- 기존 값을 소급 재구성하지 않는다 — 84-04 의 복구 큐가 사람 손으로 처리한다.
</constraints>

<tasks>
1. `talleres_lote_quantity_events` 신설: `lote_id, event_type, quantity, before_qty, after_qty,
   reason_code, envio_id, recepcion_id, user_id, occurred_at`.
2. 수량을 바꾸는 **모든** 코드 경로가 이 테이블에 쓰도록 통과시킨다(헬퍼는 `transaction` 을
   선택이 아닌 **필수 인자**로 받는다 — Phase 64 규약).
3. `available_quantity` 직접 UPDATE 전수 차단. 남기더라도 이벤트에서 계산·검증되는 projection.
   ★ QC `REWORK` 가 불량 수량을 이중 계상하던 기존 결함(D6)을 여기서 함께 잡는다.
4. 대조 뷰 `v_lote_quantity_drift` — 보존식이 안 맞는 로트 0행이어야 한다.
</constraints>

<verification>
- 발송 없이 available 을 바꾸는 호출이 코드에 없다(grep + 변이 테스트)
- 로트 8 이 `v_lote_quantity_drift` 에 뜬다(설명될 때까지)
</verification>

<success-criteria>
- 수량 변화에 **설명 없는 것이 0건**이 되는 길이 열린다
</success-criteria>
