# Phase 27 — Wave 1 PLAN: DB 마이그레이션 + 모델

**목표**: `online_orders`, `online_order_items`, `online_returns` 테이블 생성 + Sequelize 모델 + 모듈 등록.

## 변경 대상

### 신규 파일

- `api-ventago/migrations/phase27-online-orders.sql` — DDL (CREATE TABLE × 3)
- `api-ventago/src/app/online-orders/online-order.model.ts`
- `api-ventago/src/app/online-orders/online-order-item.model.ts`
- `api-ventago/src/app/online-orders/online-return.model.ts`
- `api-ventago/src/app/online-orders/online-orders.module.ts` (Sequelize 모델 등록 only — 컨트롤러/서비스는 Wave 2)

### 수정

- `api-ventago/src/app.module.ts` — `OnlineOrdersModule` import + imports 배열 추가

## 수용 기준

- 마이그레이션 SQL 은 PG10/PG15 모두 호환 (BIGSERIAL, CHECK constraint 사용)
- Sequelize `underscored: true` 자동 매핑 활용 — DB 컬럼 snake_case
- 모든 테이블 `store_id` 격리, FK `ON DELETE CASCADE` (적절한 곳)
- 모델 import 후 `npm run build` 가 api-ventago 에서 통과
