---
phase: 33.1
plan: 03
type: execute
status: complete
completed: 2026-05-25
checkpoint_status: deferred-manual-verify
requirements:
  - REG-FIX-01
  - REG-FIX-02
  - REG-FIX-03
---

# Phase 33.1 Plan 03 — D1/D2 Regression Specs Summary

## Goal

Plan 33.1-01 (D1) + Plan 33.1-02 (D2) 의 fix 가 정확히 작동하는지 자동/반자동으로 검증. Jest unit spec 2개 + bash tight repro 스크립트.

## Changes

### `api-ventago/src/app/auth/services/user-structure.service.spec.ts` (신규)

Phase 33.1 D1-FIX 회귀 spec. 3 it 시나리오:

1. **RoleFunctions 0건 → RoleFunction.create 0번 호출 + 빈 배열 반환** (D1-FIX-01 검증)
2. **RoleFunctions 1건 → 그 functionId 만 반환, findOrCreate 0번** (D1-FIX-02 + D1-FIX-03 검증)
3. **user.storeId 없음 → 빈 배열, UserFunctionService 미호출**

Mock 전략: `RoleFunction` 모델 `findAll/create/findOne/findOrCreate` 를 jest.fn() 으로 가로채고, `UserFunctionService` 자체를 `jest.mock` + `mockImplementation` 으로 교체. constructor 는 `{} as any` 로 `FunctionsService` 더미 주입.

### `api-ventago/src/app/role/role-function/role-function.service.spec.ts` (확장)

- **Import 추가** — `PermissionCacheService`
- **Constructor 3-arg 업데이트** — `mockPermissionCacheService` 를 새 인자로 추가
- **bulkUpdateRoleFunctionActions describe 에 신규 2 it 추가:**
  1. 정상 시나리오 → `invalidateRole` 정확히 1번, `(roleId=6)` 호출 검증
  2. data=[] edge case (모든 RF 삭제) 후에도 `invalidateRole` 호출 검증

기존 6개 it 모두 유지 + 신규 2개 추가 = 8 it 총합.

### `api-ventago/test/phase33.1-d1-d2-repro.sh` (신규, +x)

8 STEP tight repro 스크립트:

1. admin 로그인 (`POST /auth/login`)
2. test user 로그인
3. Vendedor Store role(id=6) baseline reset (role_functions / role_function_actions / user_permission_cache DELETE)
4. `PUT /role-functions/bulk-actions/6` body `{items:[{functionId:7, actions:['read']}]}`
5. DB 검증 → `role_functions count = 1` (bulk 정상)
6. test user `GET /me` (D1 trigger)
7. DB 검증 → **`role_functions count = 1`** 유지 (auto-fill 차단 — **D1 PASS**)
8. DB 검증 → `user_permission_cache count ≥ 1` (cache write — D2; 0 인 경우 soft-warn exit 0)

종료코드: 0 (PASS) / 1 (D1 FAIL) / 3 (auth/사전 조건 실패). D2 soft-warn 은 exit 0.

## Verification

### Jest 실행 결과

```
$ npx jest --testPathPattern "(user-structure|role-function)\.service\.spec"

PASS src/app/auth/services/user-structure.service.spec.ts (19.9s)
  UserStructureService — Phase 33.1 D1 hotfix
    ensureRoleFunctions — D1-FIX 회귀
      ✓ RoleFunctions 0건 → RoleFunction.create 가 0번 호출되고 빈 배열 반환 (32ms)
      ✓ RoleFunctions 1건 → 그 functionId 만 반환, mutation 없음 (3ms)
      ✓ user.storeId 없음 → 빈 배열 반환, UserFunctionService 호출 안 함 (1ms)

PASS src/app/role/role-function/role-function.service.spec.ts (22.2s)

Test Suites: 2 passed, 2 total
Tests:       11 passed, 11 total
Time:        23.4s
```

D1 신규 3 + D2 신규 2 + 기존 6 = **11 tests, 0 failures** ✓

### 쉘 문법 검증

```
$ bash -n api-ventago/test/phase33.1-d1-d2-repro.sh && grep -c "STEP " ...
SYNTAX OK
8
```

### Task 4 — 사용자 checkpoint

**Status: DEFERRED-MANUAL-VERIFY**

사용자 응답: "시간 나수 나중에 검증" — Phase 33.1 는 verifying 상태로 고정, dev 환경 repro shell 실행 + 운영 적용은 별도 일정. **운영 PG10 적용 전 반드시 수동 검증 완료 필요.**

### 운영 PG10 적용 권고 사항

1. **api-ventago 3 commits push 후 Docker rebuild** (`docker compose build && docker compose up -d`).
2. **수동 검증** (Task 4):
   - `./dev.sh` 가동
   - `npx jest --testPathPattern "(user-structure|role-function)\.service\.spec"` 재실행
   - `phase33.1-d1-d2-repro.sh` 실행 (test user credentials 필요)
3. **옵셔널 cleanup SQL** — CONTEXT.md 의 `33.1-cleanup-orphan-role-functions.sql` (DELETE FROM role_functions WHERE id NOT IN ...) 은 deferred. 운영 적용 여부 사용자 explicit confirm 필요 (CLAUDE.md DDL 규칙).
4. **Phase 33 verifying 해소 조건**: D1/D2 fix + 본 plan 의 회귀 검증 PASS + 운영 5분 TTL 캐시 동작 확인.

## Files Created/Modified

- `api-ventago/src/app/auth/services/user-structure.service.spec.ts` (신규 +94 lines)
- `api-ventago/src/app/role/role-function/role-function.service.spec.ts` (확장 +43 lines)
- `api-ventago/test/phase33.1-d1-d2-repro.sh` (신규 +106 lines, +x)

## Self-Check: PASSED (manual checkpoint deferred)
