---
phase: 33.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-ventago/src/app/auth/services/user-structure.service.ts
  - api-ventago/src/app/auth/services/user-registration.service.ts
autonomous: true
requirements:
  - D1-FIX-01
  - D1-FIX-02
  - D1-FIX-03
  - D1-FIX-04

must_haves:
  truths:
    - "GET /me 호출이 role_functions 테이블에 어떤 INSERT 도 트리거하지 않는다 (read-only)"
    - "ensureRoleFunctions 는 RoleFunction.findAll 결과의 functionId 배열만 반환한다"
    - "user-structure.service.ts 에 backfillRoleFunctions 메서드가 더 이상 존재하지 않는다"
    - "user-registration.service.ts 에 ensureRoleFunctions 메서드가 더 이상 존재하지 않는다"
    - "Audit 의 tight repro (bulk-actions fn=7 ['read'] → /me) 후 role_functions row 수가 1 그대로 유지된다"
  artifacts:
    - path: "api-ventago/src/app/auth/services/user-structure.service.ts"
      provides: "read-only ensureRoleFunctions (backfillRoleFunctions 삭제됨)"
      contains: "async ensureRoleFunctions(user: Users, roleId: number)"
      must_not_contain: "RoleFunction.create(|backfillRoleFunctions"
    - path: "api-ventago/src/app/auth/services/user-registration.service.ts"
      provides: "user 등록 로직 (ensureRoleFunctions 삭제됨)"
      must_not_contain: "ensureRoleFunctions"
  key_links:
    - from: "user-structure.service.ts:361 (getUserPermissions 내부)"
      to: "ensureRoleFunctions (read-only)"
      via: "this.ensureRoleFunctions(user, roleId)"
      pattern: "await this.ensureRoleFunctions"
---

<objective>
Phase 33 D1 결함 fix: `user-structure.service.ts::ensureRoleFunctions` 의 두 auto-fill side-effect 경로(Path A 0-row auto-create + Path B backfillRoleFunctions 호출)를 모두 제거하여 `/me` 호출을 read-only 로 복원한다. 더불어 `backfillRoleFunctions` 메서드 자체와 `user-registration.service.ts::ensureRoleFunctions` 의 dead-code 도 함께 삭제하여 동일 결함의 재발생을 차단한다.

Purpose: 권한 매트릭스 UI 의 신뢰성 회복. 사용자의 명시적 bulk-actions 토글과 DB 상태가 분리되는 audit 발견 결함(1 row → /me → 11 row)을 근본 차단.
Output: 2 파일 수정 (1 메서드 refactor, 2 메서드 삭제). dead code 0 라인.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md
@.planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md
@CLAUDE.md
@api-ventago/src/app/auth/services/user-structure.service.ts
@api-ventago/src/app/auth/services/user-registration.service.ts

<interfaces>
<!-- 단일 호출처: user-structure.service.ts:361 (getUserPermissions 내부) -->
<!-- Signature 유지 — 반환 타입은 number[] (functionId 배열) -->

```typescript
// user-structure.service.ts:361 — 유일한 caller
let allowedFunctionIds: number[] = [];
if (roleId) {
  allowedFunctionIds = await this.ensureRoleFunctions(user, roleId);
}

// UserFunctionService.getEffectiveFunctions 는 (userId, roleFunctionIds, storeId) 시그니처
// — role-based functionIds 위에 user-level override(allow/deny) 를 합성해 최종 effective set 반환
```

```typescript
// 삭제 대상 — user-structure.service.ts:83-110 (28 lines)
async backfillRoleFunctions(roleId: number, storeId: number): Promise<void> { ... }

// 삭제 대상 — user-registration.service.ts:139-163 (25 lines)
async ensureRoleFunctions(adminRole: Role, storeId: number): Promise<void> { ... }
```

`grep -rn "backfillRoleFunctions\|\.ensureRoleFunctions(" api-ventago/src/` 확인 결과:
- `backfillRoleFunctions`: user-structure.service.ts 의 정의 1건 + 내부 호출 1건 (line 182) — 외부 호출 0건
- `ensureRoleFunctions`: user-structure.service.ts 의 정의 + 내부 caller (line 361) / user-registration.service.ts 의 정의 — 두 정의는 서로 다른 클래스의 메서드 (호출 충돌 없음)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: user-structure.service.ts::ensureRoleFunctions read-only 로 refactor + backfillRoleFunctions 삭제</name>
  <files>api-ventago/src/app/auth/services/user-structure.service.ts</files>
  <read_first>
    - api-ventago/src/app/auth/services/user-structure.service.ts (전체 — 현재 결함 상태 + line 361 caller 확인)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (D1 LOCKED decisions)
    - .planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md (tight repro + 결함 원인)
  </read_first>
  <action>
    **편집 1 — backfillRoleFunctions 메서드 전체 삭제 (line 82-111, 30 라인):**

    삭제 대상 (정확히 다음 블록 — 주석 line 82 포함):
    ```typescript
      // 역할에 누락된 함수를 자동 backfill (새 앱/모듈 추가 시 기존 역할에도 함수 등록)
      async backfillRoleFunctions(roleId: number, storeId: number): Promise<void> {
        const FunctionsModel = (await import('../../functions/functions.model'))
          .Functions;
        const allFunctions = await FunctionsModel.findAll();
        const existingRoleFunctions = await RoleFunction.findAll({
          where: { roleId, storeId },
        });
        const existingFunctionIds = new Set(
          existingRoleFunctions.map((rf: any) => rf.functionId),
        );

        let addedCount = 0;

        for (const fn of allFunctions) {
          if (!existingFunctionIds.has(fn.id)) {
            await RoleFunction.findOrCreate({
              where: { roleId, functionId: fn.id, storeId },
              defaults: { roleId, functionId: fn.id, storeId },
            });
            addedCount++;
          }
        }

        if (addedCount > 0) {
          console.log(
            `[AUTH DEBUG] Backfilled ${addedCount} missing RoleFunctions for roleId=${roleId}, storeId=${storeId}`,
          );
        }
      }
    ```

    → 메서드 전체를 제거하고 그 자리에 빈 라인 1개만 남긴다 (ESLint `lines-around-comment` 호환).

    **편집 2 — ensureRoleFunctions 본문 전체 교체 (line 113-225, 113 라인):**

    현재 line 113 부터 시작하는 `async ensureRoleFunctions(user: Users, roleId: number): Promise<number[]> { ... }` 메서드 본문 전체를 다음으로 교체 (시그니처 그대로 유지, body 만 read-only 로 단순화):

    ```typescript
      // [Phase 33.1 D1-FIX] read-only — RoleFunction 자동 생성/backfill 제거. 빈 결과는 빈 배열 반환.
      async ensureRoleFunctions(user: Users, roleId: number): Promise<number[]> {
        const roleFunctions = await RoleFunction.findAll({
          where: { roleId, storeId: user.storeId },
          include: ['function'],
        });

        console.log(
          `[AUTH DEBUG] RoleFunctions found: ${roleFunctions.length} for roleId=${roleId}, storeId=${user.storeId}`,
        );

        const roleFunctionIds = roleFunctions.map((rf: any) => rf.functionId);

        if (!user.storeId) {
          return [];
        }

        const userFunctionService = new UserFunctionService(UserFunction);
        const allowedFunctionIds =
          await userFunctionService.getEffectiveFunctions(
            user.id,
            roleFunctionIds,
            user.storeId,
          );

        console.log(
          `[AUTH DEBUG] Effective FunctionIds: ${allowedFunctionIds.length}`,
        );

        return allowedFunctionIds;
      }
    ```

    근거 (D1-FIX-01 + D1-FIX-02 + D1-FIX-03 per 33.1-CONTEXT.md):
    - Path A (line 122-179 의 `if (roleFunctions.length === 0)` 분기 — RoleFunction.create 루프) 완전 제거
    - Path B (line 180-209 의 `else` 분기 — `backfillRoleFunctions(roleId, user.storeId!)` 호출) 완전 제거
    - `backfillRoleFunctions` 자체 메서드도 삭제 (위 편집 1) — dead code 차단
    - 반환 동작은 그대로: `roleFunctions` 가 0건이면 빈 배열 → `/me` 의 permissions = `{}` → admin 이 명시적 토글 필요
    - 단일 호출처 line 361 (현재 → 새 line number 는 ~150 부근으로 shift) 의 signature/return 타입 호환 유지

    **ESLint 규칙 준수 (CLAUDE.md):**
    - `lines-around-comment`: 주석 `// [Phase 33.1 D1-FIX]` 위에 빈 줄
    - `newline-before-return`: `return [];` 와 `return allowedFunctionIds;` 위에 빈 줄 (위 코드에 이미 반영)
    - `no-unused-vars`: import 들 (`UserFunctionService`, `UserFunction`) 은 그대로 사용됨 — 변경 없음

    **검증 후 line 361 caller 확인:** 두 편집 후 파일 내 `await this.ensureRoleFunctions(user, roleId)` 호출이 그대로 존재해야 함 (caller 는 손대지 않음).
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && grep -c "RoleFunction.create(" api-ventago/src/app/auth/services/user-structure.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RoleFunction.create(" api-ventago/src/app/auth/services/user-structure.service.ts` returns `0` (D1-FIX-01 + D1-FIX-02)
    - `grep -c "backfillRoleFunctions" api-ventago/src/app/auth/services/user-structure.service.ts` returns `0` (D1-FIX-03 — 메서드 정의 + 내부 호출 모두 제거)
    - `grep -c "async ensureRoleFunctions" api-ventago/src/app/auth/services/user-structure.service.ts` returns `1` (메서드 정의는 유지)
    - `grep "await this.ensureRoleFunctions" api-ventago/src/app/auth/services/user-structure.service.ts` 결과에 `allowedFunctionIds = await this.ensureRoleFunctions(user, roleId)` 한 줄 포함 (caller 손상 없음)
    - `grep -E "findOrCreate|RoleFunction\.findOne" api-ventago/src/app/auth/services/user-structure.service.ts` returns `0` (Path A 의 `findOne` + Path A 의 `findOrCreate` 모두 제거 확인 — 단 `Role.findOne` 등 다른 모델 호출은 grep 결과에 나올 수 있음, 그 경우 RoleFunction 만 0건인지 시각 확인)
    - `cd api-ventago && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "user-structure.service.ts"` 결과 0건 (타입 오류 없음)
    - `cd api-ventago && npx eslint src/app/auth/services/user-structure.service.ts` exit code 0 (또는 새로운 에러 없음 — 기존 console.log warning 은 무관)
  </acceptance_criteria>
  <done>
    ensureRoleFunctions 가 read-only (mutation 없음). backfillRoleFunctions 메서드 삭제됨. 단일 caller (line 361 부근) 정상 동작 (signature 동일). TypeScript 빌드 통과. ESLint 신규 위반 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: user-registration.service.ts::ensureRoleFunctions 메서드 삭제 (dead code)</name>
  <files>api-ventago/src/app/auth/services/user-registration.service.ts</files>
  <read_first>
    - api-ventago/src/app/auth/services/user-registration.service.ts (전체 — 클래스 닫힘 위치 + 다른 메서드 영향 없음 확인)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (D1-DEAD-CODE LOCKED decision)
  </read_first>
  <action>
    **편집 — ensureRoleFunctions 메서드 전체 삭제 (line 139-163, 25 라인):**

    삭제 대상 (정확히 다음 블록):
    ```typescript
      async ensureRoleFunctions(adminRole: Role, storeId: number): Promise<void> {
        const existingRoleFunctions = await RoleFunction.findAll({
          where: { roleId: adminRole.id, storeId },
        });

        if (existingRoleFunctions.length === 0) {
          const FunctionsModel = (await import('../../functions/functions.model'))
            .Functions;
          const allFunctions = await FunctionsModel.findAll();
          if (allFunctions.length > 0) {
            for (const fn of allFunctions) {
              const existing = await RoleFunction.findOne({
                where: { roleId: adminRole.id, functionId: fn.id, storeId },
              });
              if (!existing) {
                await RoleFunction.create({
                  roleId: adminRole.id,
                  functionId: fn.id,
                  storeId,
                });
              }
            }
          }
        }
      }
    ```

    → 메서드 블록 전체를 제거하고 닫는 `}` (class 종료) 만 남긴다.

    **삭제 후 처리:**
    1. 만약 `RoleFunction` import 가 이 파일에서 더 이상 사용되지 않으면 (grep 으로 확인), import 라인도 함께 제거 (ESLint `no-unused-vars`).
       - 확인 방법: `grep -n "RoleFunction" api-ventago/src/app/auth/services/user-registration.service.ts` — 결과가 import 라인 한 줄만 남으면 그 import 도 제거.
    2. 만약 `Role` import (NestJS Role 모델) 가 이 파일의 다른 메서드에서 사용되면 (line 139 의 `adminRole: Role` 외) 유지, 아니면 import 제거 — grep 으로 확인.

    근거 (D1-FIX-04 per 33.1-CONTEXT.md):
    - `grep -rn "\.ensureRoleFunctions(" api-ventago/src/` 결과 user-registration.service.ts 메서드의 호출처는 0건 (dead code)
    - 동일 auto-fill 패턴(RoleFunction.create loop) 이 잠재 미래 호출 시 D1 결함 재발 위험 → 안전 차원에서 제거
    - user-structure.service.ts 의 `ensureRoleFunctions` 와는 별개 클래스의 메서드 (이름만 동일) — Task 1 작업과 독립

    **ESLint 규칙 준수:**
    - 메서드 삭제 후 클래스 종료 `}` 위에 빈 줄 1개 (`newline-before-return` 영향 없음, but `lines-around-comment` 가 트리거되지 않도록 정리)
    - import 정리 시 unused import 0건 보장
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && grep -c "ensureRoleFunctions" api-ventago/src/app/auth/services/user-registration.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ensureRoleFunctions" api-ventago/src/app/auth/services/user-registration.service.ts` returns `0` (D1-FIX-04 — 메서드 완전 삭제)
    - `grep -c "RoleFunction.create" api-ventago/src/app/auth/services/user-registration.service.ts` returns `0` (auto-fill 패턴 잔존 0)
    - `grep -rn "\.ensureRoleFunctions(" api-ventago/src/` 결과에 user-registration.service.ts 가 포함되지 않음 (호출처도 없으므로 dead code 완전 제거)
    - `cd api-ventago && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "user-registration.service.ts"` 결과 0건
    - `cd api-ventago && npx eslint src/app/auth/services/user-registration.service.ts` exit code 0 (또는 신규 위반 0)
    - 파일이 여전히 NestJS `@Injectable()` 클래스로 valid (다른 메서드들 정상 — `grep "@Injectable\|export class" api-ventago/src/app/auth/services/user-registration.service.ts` 결과 변화 없음)
  </acceptance_criteria>
  <done>
    user-registration.service.ts 의 ensureRoleFunctions 메서드 완전 삭제. unused import 정리 (있을 경우). TypeScript 빌드 통과. ESLint 신규 위반 없음. 다른 메서드(createAdminRoleForStore 등) 동작 영향 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API (`/me`) | 인증된 사용자가 자기 권한 조회. 본 hotfix 로 read-only 강화 |
| Internal service → DB (RoleFunction table) | 본 hotfix 이전: `/me` 가 silent DDL-like mutation 수행. 이후: read-only 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-01 | Tampering | `user-structure.service.ts::ensureRoleFunctions` | mitigate | Path A + Path B 의 RoleFunction.create 호출 모두 삭제 → `/me` 가 권한 매트릭스 silent mutation 불가 |
| T-33.1-02 | Tampering | `user-registration.service.ts::ensureRoleFunctions` (dead code) | mitigate | 메서드 자체 삭제 → 미래 우발 호출로 인한 동일 결함 재발생 차단 |
| T-33.1-03 | Repudiation | RoleFunction 자동 생성 로그 (`[AUTH DEBUG] Created ${createdCount} RoleFunctions`) | accept | console.log 제거 (Path A/B 와 함께 삭제) — 자동 mutation 자체가 없어지므로 audit log 필요성 소멸 |
| T-33.1-04 | Information Disclosure | `[AUTH DEBUG]` console.log 의 functionId 노출 | accept | hotfix scope 아님 (CONTEXT.md Claude's Discretion: 로깅 정리는 별도 phase) |
| T-33.1-05 | Denial of Service | `/me` 의 합법 read-only 응답 | accept | 본 hotfix 는 mutation 만 제거 — read 성능은 동일 (오히려 향상) |
</threat_model>

<verification>
**전체 plan 완료 후 회귀 확인:**

1. TypeScript 컴파일: `cd api-ventago && npx tsc --noEmit -p tsconfig.json` exit 0
2. ESLint 두 파일 통과: `cd api-ventago && npx eslint src/app/auth/services/user-structure.service.ts src/app/auth/services/user-registration.service.ts`
3. Dead code 0건: `grep -rn "backfillRoleFunctions" api-ventago/src/` → user-structure.service.ts 0 hits, 다른 파일 0 hits
4. user-registration.service.ts::ensureRoleFunctions 0건: `grep -rn "ensureRoleFunctions" api-ventago/src/app/auth/services/user-registration.service.ts` exit 1 (no match)
5. 단일 caller 보존: `grep "await this.ensureRoleFunctions" api-ventago/src/app/auth/services/user-structure.service.ts` 결과 1 line
6. 회귀 검증 (Plan 33.1-03 에서 수행): bulk-actions fn=7 ['read'] → /me → DB count(*) FROM role_functions = 1 유지 ✅
</verification>

<success_criteria>
- D1-FIX-01: user-structure.service.ts 의 Path A (0-row auto-create RoleFunction.create 루프) 코드 제거 — grep `RoleFunction.create` returns 0
- D1-FIX-02: user-structure.service.ts 의 Path B (else branch 의 backfillRoleFunctions 호출) 코드 제거 — grep `backfillRoleFunctions` returns 0
- D1-FIX-03: user-structure.service.ts 의 backfillRoleFunctions 메서드 정의 삭제 — grep `async backfillRoleFunctions` returns 0
- D1-FIX-04: user-registration.service.ts 의 ensureRoleFunctions 메서드 정의 삭제 — grep `ensureRoleFunctions` returns 0
- TypeScript 빌드 + ESLint 통과 (CLAUDE.md 규칙 준수)
- 단일 caller (user-structure.service.ts:~361) signature/return 타입 호환 유지
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-01-SUMMARY.md` documenting:
- Lines removed (approximate count per file)
- Final ensureRoleFunctions body (read-only)
- Verification of zero callers for deleted methods
</output>
