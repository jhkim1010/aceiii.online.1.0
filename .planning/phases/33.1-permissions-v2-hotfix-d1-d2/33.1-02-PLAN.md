---
phase: 33.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api-ventago/src/app/permissions/permission-cache.service.ts
  - api-ventago/src/app/role/role-function/role-function.service.ts
  - api-ventago/src/app/role/role-function/role-function.module.ts
autonomous: true
requirements:
  - D2-FIX-01
  - D2-FIX-02

must_haves:
  truths:
    - "PermissionCacheService 에 invalidateRole(roleId: number): Promise<number> 메서드가 존재한다"
    - "RoleFunctionService.bulkUpdateRoleFunctionActions 의 마지막 동작이 permissionCacheService.invalidateRole(roleId) 호출이다"
    - "bulk-actions 후 영향 user 의 user_permission_cache row 가 삭제되어 다음 /me 호출 시 재계산된다"
    - "RoleFunctionModule 의 imports 에 PermissionsModule (PermissionCacheService 를 export 하는 모듈) 이 포함된다"
  artifacts:
    - path: "api-ventago/src/app/permissions/permission-cache.service.ts"
      provides: "invalidateRole 신규 메서드"
      contains: "async invalidateRole(roleId: number)"
    - path: "api-ventago/src/app/role/role-function/role-function.service.ts"
      provides: "bulkUpdateRoleFunctionActions 끝에 invalidateRole 호출"
      contains: "this.permissionCacheService.invalidateRole(roleId)"
    - path: "api-ventago/src/app/role/role-function/role-function.module.ts"
      provides: "DI 와이어링 (PermissionsModule import)"
      contains: "PermissionsModule"
  key_links:
    - from: "RoleFunctionService.bulkUpdateRoleFunctionActions (line 73-103)"
      to: "PermissionCacheService.invalidateRole(roleId)"
      via: "constructor DI 주입"
      pattern: "this\\.permissionCacheService\\.invalidateRole"
    - from: "PermissionCacheService.invalidateRole"
      to: "user_permission_cache DELETE WHERE user_id IN (SELECT user_id FROM user_roles WHERE role_id = :roleId)"
      via: "Sequelize raw query"
      pattern: "DELETE FROM user_permission_cache"
---

<objective>
Phase 33 D2 결함 fix: `RoleFunctionService.bulkUpdateRoleFunctionActions` 끝에 `user_permission_cache` invalidate 호출 누락. 신규 `PermissionCacheService.invalidateRole(roleId)` 메서드를 추가하고 (analog: `invalidateStore`) `bulkUpdateRoleFunctionActions` 마지막에 호출하여 Phase 33 spec 의 "5분 TTL 캐시" 가 실제 invalidate 되도록 한다.

Purpose: Phase 33 spec 의 "function-permission.guard.ts (104ms slow query) 캐시 기반 교체" 미달성 결함 해소. `/me` 매 호출 재계산으로 인한 PG pool 부담 제거.
Output: 1 신규 메서드 (permission-cache.service.ts), 1 DI 주입 + 1 호출 추가 (role-function.service.ts), 1 module imports 변경 (role-function.module.ts).
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md
@.planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md
@CLAUDE.md
@api-ventago/src/app/permissions/permission-cache.service.ts
@api-ventago/src/app/permissions/permissions.module.ts
@api-ventago/src/app/role/role-function/role-function.service.ts
@api-ventago/src/app/role/role-function/role-function.module.ts

<interfaces>
<!-- PermissionCacheService 현재 시그니처 (permission-cache.service.ts:25-) -->
```typescript
@Injectable()
export class PermissionCacheService {
  private readonly logger = new Logger('PermissionCacheService');

  constructor(
    @InjectModel(UserPermissionCache)
    private readonly cacheModel: typeof UserPermissionCache,
  ) {}

  async get(userId: number, branchId: number): Promise<PermissionMap | null>;
  async set(userId: number, branchId: number, permissions: PermissionMap): Promise<void>;
  async invalidateUser(userId: number): Promise<number>;        // 기존
  async invalidateBranch(branchId: number): Promise<number>;    // 기존
  async invalidateStore(storeId: number): Promise<number>;      // 기존 — analog 패턴
  async purgeExpired(olderThanMs?: number): Promise<number>;
}
```

<!-- invalidateStore 의 raw-query 패턴 (permission-cache.service.ts:134-157) — invalidateRole 신규 추가 시 동일 구조 -->
```typescript
async invalidateStore(storeId: number): Promise<number> {
  try {
    const sequelize = this.cacheModel.sequelize;
    if (!sequelize) {
      throw new Error('Sequelize instance unavailable');
    }

    const [, meta] = await sequelize.query(
      `DELETE FROM user_permission_cache
       WHERE branch_id IN (SELECT id FROM branches WHERE store_id = :storeId)`,
      { replacements: { storeId } },
    );

    const deleted = (meta as { rowCount?: number })?.rowCount ?? 0;
    this.logger.debug(`invalidateStore(${storeId}) — ${deleted} rows 삭제`);

    return deleted;
  } catch (error) {
    this.logger.error(`invalidateStore(${storeId}) 실패:`, error);

    return 0;
  }
}
```

<!-- DB 스키마 확인 (db-schema-tables.md) -->
- `user_roles` columns: id, user_id, role_id, created_at, updated_at ✓
- `user_permission_cache` columns: user_id, branch_id, permissions, computed_at (Phase 33 신규)

<!-- PermissionsModule 이미 PermissionCacheService 를 export 함 (permissions.module.ts:51-59) ✓ -->
```typescript
exports: [
  SequelizeModule,
  PermissionCacheService,  // ← 이미 export 됨, 별도 변경 불필요
  ...
],
```

<!-- RoleFunctionModule 현재 imports (role-function.module.ts) — PermissionsModule 추가 필요 -->
```typescript
@Module({
  imports: [
    SequelizeModule.forFeature([RoleFunction, RoleFunctionAction, Role, Functions]),
    // PermissionsModule 추가 필요
  ],
  providers: [RoleFunctionService],
  controllers: [RoleFunctionController],
  exports: [SequelizeModule],
})
export class RoleFunctionModule {}
```

<!-- RoleFunctionService 현재 constructor (role-function.service.ts:11-19) — PermissionCacheService 주입 필요 -->
```typescript
@Injectable()
export class RoleFunctionService extends CrudService<RoleFunction> {
  constructor(
    @InjectModel(RoleFunction)
    private readonly roleFunctionModel: typeof RoleFunction,
    @InjectModel(RoleFunctionAction)
    private readonly roleFunctionActionModel: typeof RoleFunctionAction,
    // private readonly permissionCacheService: PermissionCacheService,  ← 추가
  ) {
    super(roleFunctionModel);
  }
}
```

<!-- bulkUpdateRoleFunctionActions 현재 본문 (role-function.service.ts:73-103) — 마지막에 invalidateRole 추가 -->
```typescript
async bulkUpdateRoleFunctionActions(
  roleId: number,
  storeId: number,
  data: Array<{ functionId: number; actions: string[] }>,
) {
  const existingRFs = await this.roleFunctionModel.findAll({ where: { roleId, storeId } });
  const existingMap = new Map(existingRFs.map((rf) => [rf.functionId, rf]));

  for (const item of data) {
    let rf = existingMap.get(item.functionId);
    if (!rf) {
      rf = await this.createWithActions(roleId, storeId, item.functionId);
    }
    await this.updateActionsForRoleFunction(rf.id, item.actions);
  }

  const dataFnIds = new Set(data.map((d) => d.functionId));
  for (const [fnId, rf] of existingMap) {
    if (!dataFnIds.has(fnId)) {
      await rf.destroy();
    }
  }
  // ← 여기에 await this.permissionCacheService.invalidateRole(roleId); 추가
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PermissionCacheService.invalidateRole 메서드 신규 추가</name>
  <files>api-ventago/src/app/permissions/permission-cache.service.ts</files>
  <read_first>
    - api-ventago/src/app/permissions/permission-cache.service.ts (전체 — invalidateStore 패턴 line 130-157, Logger 사용법, raw query 패턴)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (D2 LOCKED decision — exact code block 제공됨)
    - .planning/intel/db-schema-tables.md (user_roles 스키마 확인)
  </read_first>
  <action>
    **편집 — `invalidateStore` 메서드 바로 다음에 `invalidateRole` 메서드 신규 추가:**

    삽입 위치: `invalidateStore` 의 닫는 `}` 직후 (현재 line 157 다음) + `purgeExpired` 의 주석 (`/** 만료된 캐시 일괄 정리 ... */`) 바로 직전.

    삽입할 코드 (CONTEXT.md D2 decision 의 코드와 동일, ESLint 규칙 준수 형식):

    ```typescript

      /**
       * 특정 역할의 모든 사용자 캐시 무효화
       * (role_functions / role_function_actions 변경 시 호출)
       *
       * user_permission_cache 는 user_id 기준 — user_roles JOIN 으로 영향 user 식별.
       * branch 단위 추가 필터링 없음 → 같은 role 의 모든 branch 캐시도 함께 invalidate (의도된 동작).
       */
      async invalidateRole(roleId: number): Promise<number> {
        try {
          const sequelize = this.cacheModel.sequelize;
          if (!sequelize) {
            throw new Error('Sequelize instance unavailable');
          }

          const [, meta] = await sequelize.query(
            `DELETE FROM user_permission_cache
             WHERE user_id IN (SELECT user_id FROM user_roles WHERE role_id = :roleId)`,
            { replacements: { roleId } },
          );

          const deleted = (meta as { rowCount?: number })?.rowCount ?? 0;
          this.logger.debug(`invalidateRole(${roleId}) — ${deleted} rows 삭제`);

          return deleted;
        } catch (error) {
          this.logger.error(`invalidateRole(${roleId}) 실패:`, error);

          return 0;
        }
      }
    ```

    **근거 (D2-FIX-01 per 33.1-CONTEXT.md):**
    - analog = `invalidateStore` (line 134-157) — 동일한 try/catch + sequelize.query + meta.rowCount 추출 + this.logger 패턴
    - SQL: `DELETE FROM user_permission_cache WHERE user_id IN (SELECT user_id FROM user_roles WHERE role_id = :roleId)` — Sequelize replacements 로 SQL injection 방지
    - 반환 타입 `Promise<number>` — 다른 invalidate* 메서드와 일관
    - logger 메시지: `invalidateRole(${roleId}) — ${deleted} rows 삭제` (한글 OK — CLAUDE.md 컨벤션)
    - **PG pool 영향 (CLAUDE.md 규약):** Sequelize 모델 메서드 + raw query — pool.connect() 직접 사용 없음. invalidateStore 와 동일 패턴이므로 pool 부담 동등.

    **왜 invalidateRole 인가 (CONTEXT.md):**
    - `invalidateUser(userId)`: role 단위 변경에 부적합 (한 role 에 N user → loop 필요, N+1 쿼리)
    - `invalidateStore(storeId)`: 너무 광범위 (다른 role 의 cache 도 함께 날아감 — 무관한 user 까지 cache miss)
    - `invalidateRole(roleId)`: role_functions 변경의 정확한 scope, user_roles JOIN 으로 정확한 영향 user 집합 식별

    **ESLint 규칙 (CLAUDE.md):**
    - 주석 `/** ... */` 위에 빈 줄 (`lines-around-comment`) — `}` 다음 빈 줄 1개로 처리
    - `return deleted;` 와 `return 0;` 위에 빈 줄 (`newline-before-return`) — 위 코드에 이미 반영
    - 새 import 필요 없음 (UserPermissionCache 모델은 기존 cacheModel 사용, Op import 도 raw query 이므로 불필요)
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && grep -c "async invalidateRole" api-ventago/src/app/permissions/permission-cache.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async invalidateRole" api-ventago/src/app/permissions/permission-cache.service.ts` returns `1` (D2-FIX-01)
    - `grep -A 10 "async invalidateRole" api-ventago/src/app/permissions/permission-cache.service.ts | grep "DELETE FROM user_permission_cache"` returns the SQL line
    - `grep -A 12 "async invalidateRole" api-ventago/src/app/permissions/permission-cache.service.ts | grep "FROM user_roles WHERE role_id = :roleId"` returns the JOIN clause
    - `grep "invalidateRole(\${roleId})" api-ventago/src/app/permissions/permission-cache.service.ts` returns the logger.debug line (검증: roleId 가 메시지에 포함)
    - 기존 메서드 모두 보존: `grep -c "async invalidate" api-ventago/src/app/permissions/permission-cache.service.ts` returns `4` (invalidateUser + invalidateBranch + invalidateStore + invalidateRole)
    - `cd api-ventago && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "permission-cache.service.ts"` returns 0 (타입 OK)
    - `cd api-ventago && npx eslint src/app/permissions/permission-cache.service.ts` exit code 0 (신규 위반 0)
  </acceptance_criteria>
  <done>
    invalidateRole 메서드가 PermissionCacheService 에 추가되어 raw query 로 user_roles JOIN 으로 영향 user 의 cache row 를 삭제한다. 반환 타입 + 에러 처리 + logger 가 invalidateStore analog 와 동일. TypeScript + ESLint 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: RoleFunctionService 에 PermissionCacheService DI 주입 + bulkUpdateRoleFunctionActions 끝에 invalidateRole 호출 + RoleFunctionModule 에 PermissionsModule import 추가</name>
  <files>api-ventago/src/app/role/role-function/role-function.service.ts, api-ventago/src/app/role/role-function/role-function.module.ts</files>
  <read_first>
    - api-ventago/src/app/role/role-function/role-function.service.ts (전체 — constructor + bulkUpdateRoleFunctionActions line 73-103 확인)
    - api-ventago/src/app/role/role-function/role-function.module.ts (전체 — imports/providers 구조)
    - api-ventago/src/app/permissions/permissions.module.ts (PermissionCacheService 가 exports 에 이미 포함됨을 확인 — line 51-59)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (D2 LOCKED — DI 주입 + invalidateRole 호출)
    - api-ventago/src/app/permissions/permissions.service.ts (line 256 — invalidateUser 호출 site analog: transaction 후 cache invalidate 순서)
  </read_first>
  <action>
    **편집 1 — role-function.module.ts: imports 에 PermissionsModule 추가:**

    현재 imports 배열:
    ```typescript
    imports: [
      SequelizeModule.forFeature([
        RoleFunction,
        RoleFunctionAction,
        Role,
        Functions,
      ]),
    ],
    ```

    다음으로 변경:
    ```typescript
    imports: [
      SequelizeModule.forFeature([
        RoleFunction,
        RoleFunctionAction,
        Role,
        Functions,
      ]),
      PermissionsModule,
    ],
    ```

    상단 import 추가:
    ```typescript
    import { PermissionsModule } from 'src/app/permissions/permissions.module';
    ```

    **편집 2 — role-function.service.ts: PermissionCacheService DI 주입 + bulkUpdateRoleFunctionActions 끝에 invalidateRole 호출:**

    **2-a) 상단 import 추가 (다른 import 들 다음에):**
    ```typescript
    import { PermissionCacheService } from 'src/app/permissions/permission-cache.service';
    ```

    **2-b) Constructor 에 새 의존성 주입 (line 12-19):**

    현재:
    ```typescript
    constructor(
      @InjectModel(RoleFunction)
      private readonly roleFunctionModel: typeof RoleFunction,
      @InjectModel(RoleFunctionAction)
      private readonly roleFunctionActionModel: typeof RoleFunctionAction,
    ) {
      super(roleFunctionModel);
    }
    ```

    다음으로 변경:
    ```typescript
    constructor(
      @InjectModel(RoleFunction)
      private readonly roleFunctionModel: typeof RoleFunction,
      @InjectModel(RoleFunctionAction)
      private readonly roleFunctionActionModel: typeof RoleFunctionAction,
      private readonly permissionCacheService: PermissionCacheService,
    ) {
      super(roleFunctionModel);
    }
    ```

    **2-c) `bulkUpdateRoleFunctionActions` 메서드의 마지막 `for` 루프 직후에 invalidateRole 호출 추가:**

    현재 method 끝부분 (line 95-103):
    ```typescript
        // data에 없는 기존 function은 삭제 (cascade로 action도 삭제)
        const dataFnIds = new Set(data.map((d) => d.functionId));

        for (const [fnId, rf] of existingMap) {
          if (!dataFnIds.has(fnId)) {
            await rf.destroy();
          }
        }
      }
    ```

    다음으로 변경 (`}` 직전에 invalidateRole 호출 + 주석):
    ```typescript
        // data에 없는 기존 function은 삭제 (cascade로 action도 삭제)
        const dataFnIds = new Set(data.map((d) => d.functionId));

        for (const [fnId, rf] of existingMap) {
          if (!dataFnIds.has(fnId)) {
            await rf.destroy();
          }
        }

        // [Phase 33.1 D2-FIX] role_functions / role_function_actions 변경 후 영향 user 의 cache invalidate
        await this.permissionCacheService.invalidateRole(roleId);
      }
    ```

    **근거 (D2-FIX-02 per 33.1-CONTEXT.md):**
    - `permissions.service.ts:256` analog: transaction 종료 후 cache invalidate (mutation 완료 후 무효화, 일관성 보장)
    - `roleId` 는 이미 메서드 파라미터로 받음 (기존 시그니처 변경 없음)
    - `await` 사용 — 캐시 invalidate 실패 시 호출자에게 전파 (`invalidateRole` 자체가 try/catch 로 0 반환하므로 실제로는 throw 안 함, 그러나 future-proof)

    **Circular Dependency 검증:**
    - PermissionsModule → AuditLogModule (imports), 자체 모델 (SequelizeModule.forFeature)
    - PermissionCacheService 는 다른 도메인 서비스에 의존하지 않음 (UserPermissionCache 모델만 의존)
    - RoleFunctionModule → PermissionsModule 단방향 import — circular 없음
    - 만약 NestJS 가 빌드 시 circular 경고를 보내면 (예상되지 않음) `forwardRef(() => PermissionsModule)` 사용

    **ESLint 규칙 (CLAUDE.md):**
    - 주석 `// [Phase 33.1 D2-FIX] ...` 위에 빈 줄 (`lines-around-comment`) — 위 코드에 이미 반영
    - `return` 키워드 없음 (void 반환) — `newline-before-return` 영향 없음
    - import 정렬: NestJS/Sequelize import 들 다음에 alias path import
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && grep "permissionCacheService.invalidateRole" api-ventago/src/app/role/role-function/role-function.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "this.permissionCacheService.invalidateRole(roleId)" api-ventago/src/app/role/role-function/role-function.service.ts` returns the call line (D2-FIX-02)
    - `grep -A 30 "bulkUpdateRoleFunctionActions" api-ventago/src/app/role/role-function/role-function.service.ts | grep "invalidateRole"` returns a match (호출이 메서드 내부에 위치)
    - `grep "PermissionCacheService" api-ventago/src/app/role/role-function/role-function.service.ts` returns ≥ 2 lines (import + constructor parameter type)
    - `grep "private readonly permissionCacheService: PermissionCacheService" api-ventago/src/app/role/role-function/role-function.service.ts` returns the DI line
    - `grep "PermissionsModule" api-ventago/src/app/role/role-function/role-function.module.ts` returns ≥ 2 lines (import + imports array entry)
    - `cd api-ventago && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "role-function\.(service|module)\.ts"` returns 0 (타입 OK)
    - `cd api-ventago && npx eslint src/app/role/role-function/role-function.service.ts src/app/role/role-function/role-function.module.ts` exit code 0 (신규 위반 0)
    - `cd api-ventago && npm run build 2>&1 | tail -5` 결과에 error 0 (NestJS DI 검증 — circular dep 경고 없음)
  </acceptance_criteria>
  <done>
    PermissionCacheService 가 RoleFunctionService 에 DI 로 주입됨. bulkUpdateRoleFunctionActions 마지막 라인에서 invalidateRole(roleId) 호출. RoleFunctionModule 의 imports 에 PermissionsModule 추가. TypeScript + ESLint + NestJS build (circular 없음) 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → API (PUT /role-functions/bulk-actions/:roleId) | 인증된 admin 의 권한 변경 요청 |
| RoleFunctionService → user_permission_cache | mutation 후 cache 정합성 유지 책임 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-06 | Tampering | `RoleFunctionService.bulkUpdateRoleFunctionActions` | mitigate | invalidateRole 호출 추가 → DB 변경 직후 cache 무효화 → 다음 /me 호출 시 실제 권한 재계산 |
| T-33.1-07 | Information Disclosure | `invalidateRole` SQL — user_roles 조회 | accept | Sequelize replacements 로 SQL injection 차단 (`{ replacements: { roleId } }`) — invalidateStore 와 동일 패턴 |
| T-33.1-08 | Denial of Service | RoleFunctionModule → PermissionsModule circular dep | mitigate | 단방향 import 만 사용 (PermissionsModule 은 RoleFunctionModule 을 import 하지 않음). build 시 검증. circular 발생 시 forwardRef fallback 명시 |
| T-33.1-09 | Elevation of Privilege | bulk-actions 후 cache 미갱신으로 stale 권한 사용 | mitigate | invalidateRole 호출로 영향 user 의 모든 branch cache 삭제 — 다음 요청 시 최신 권한 반영 |
| T-33.1-10 | Denial of Service | invalidateRole 의 DELETE 쿼리 부하 | accept | role 당 영향 user 는 일반적으로 ≤ 100명, user_permission_cache 인덱스 (user_id, branch_id) 활용 — 100ms 이하 예상 |
</threat_model>

<verification>
**전체 plan 완료 후 회귀 확인:**

1. TypeScript 컴파일: `cd api-ventago && npx tsc --noEmit -p tsconfig.json` exit 0
2. ESLint 3 파일 통과
3. NestJS DI 검증: `cd api-ventago && npm run build` → circular dependency 경고 0건
4. 회귀 검증 (Plan 33.1-03 에서 수행):
   - bulk-actions → DB 직후 cache invalidate 호출 확인 (Jest spy)
   - tight repro: bulk-actions fn=7 ['read'] → test user /me → user_permission_cache row ≥ 1 (cache write 됨, 단 PermissionResolverService 가 cache 를 write 하는지는 별도 검증)
5. PG pool 영향: invalidateRole 의 DELETE 쿼리는 1 connection 사용, 즉시 release (Sequelize 모델 메서드, CLAUDE.md 규약 준수)
</verification>

<success_criteria>
- D2-FIX-01: PermissionCacheService 에 `invalidateRole(roleId: number): Promise<number>` 메서드 신규 추가 — `user_roles` JOIN 으로 영향 user 식별 + raw DELETE
- D2-FIX-02: RoleFunctionService.bulkUpdateRoleFunctionActions 마지막에 `await this.permissionCacheService.invalidateRole(roleId)` 호출 추가 + DI 주입 + RoleFunctionModule.imports 에 PermissionsModule 추가
- TypeScript 빌드 + ESLint + NestJS DI 검증 통과 (circular dep 0)
- analog 패턴 일관성: invalidateStore (line 134-157) 와 동일한 try/catch + sequelize.query + meta.rowCount + logger 패턴
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-02-SUMMARY.md` documenting:
- invalidateRole 메서드 본문 (final)
- DI wiring diff (constructor + module)
- circular dep 검증 결과 (build 로그 발췌)
</output>
