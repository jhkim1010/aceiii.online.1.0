---
phase: 33.1
plan: 03
type: execute
wave: 2
depends_on:
  - 33.1-01
  - 33.1-02
files_modified:
  - api-ventago/src/app/auth/services/user-structure.service.spec.ts
  - api-ventago/src/app/role/role-function/role-function.service.spec.ts
  - api-ventago/test/phase33.1-d1-d2-repro.sh
autonomous: false
requirements:
  - REG-FIX-01
  - REG-FIX-02
  - REG-FIX-03

must_haves:
  truths:
    - "D1 회귀 Jest spec 이 PASS — ensureRoleFunctions 가 mutation 을 트리거하지 않음을 증명"
    - "D2 회귀 Jest spec 이 PASS — bulkUpdateRoleFunctionActions 가 invalidateRole 을 정확히 1번 호출함을 증명"
    - "tight repro 쉘 스크립트가 존재하여 dev 환경에서 D1/D2 동작을 재현·검증 가능"
    - "재현 쉘 실행 시 두 assertion 모두 PASS: (1) role_functions row 수가 /me 호출 후에도 1 유지, (2) user_permission_cache row ≥ 1"
  artifacts:
    - path: "api-ventago/src/app/auth/services/user-structure.service.spec.ts"
      provides: "D1 회귀 Jest spec (신규)"
      contains: "describe('ensureRoleFunctions'"
      min_lines: 60
    - path: "api-ventago/src/app/role/role-function/role-function.service.spec.ts"
      provides: "D2 회귀 Jest spec (기존 확장)"
      contains: "invalidateRole"
    - path: "api-ventago/test/phase33.1-d1-d2-repro.sh"
      provides: "수동 repro 쉘 스크립트"
      contains: "role_functions"
  key_links:
    - from: "user-structure.service.spec.ts mock"
      to: "ensureRoleFunctions read-only 검증"
      via: "jest.fn() spy on RoleFunction.create"
      pattern: "expect.*RoleFunction.create.*toHaveBeenCalledTimes\\(0\\)"
    - from: "role-function.service.spec.ts mock"
      to: "permissionCacheService.invalidateRole spy"
      via: "jest.fn()"
      pattern: "expect.*invalidateRole.*toHaveBeenCalledWith"
---

<objective>
Plan 33.1-01 (D1) + Plan 33.1-02 (D2) 의 fix 가 정확히 작동하는지 자동/반자동으로 검증한다. (1) D1 회귀 Jest unit spec 신규 작성 — `ensureRoleFunctions` 가 어떤 mutation 도 호출하지 않음을 mock 으로 증명. (2) D2 회귀 Jest spec 기존 파일 확장 — `bulkUpdateRoleFunctionActions` 가 `invalidateRole` 을 정확한 인자로 1번 호출함을 spy 로 증명. (3) tight repro 쉘 스크립트 — audit 의 tight repro 시나리오를 한 번에 실행하고 두 assertion (role_functions count + user_permission_cache count) 결과를 출력. (4) 사용자 checkpoint — dev 환경에서 실제 실행 + 결과 확인.

Purpose: 운영 PG10 적용 전 D1/D2 fix 의 정합성 보장. 미래 회귀 발생 시 자동 감지.
Output: 1 신규 spec, 1 기존 spec 확장, 1 신규 repro 쉘 스크립트, 1 사용자 검증 checkpoint.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md
@.planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md
@CLAUDE.md
@.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-01-PLAN.md
@.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-02-PLAN.md
@api-ventago/src/app/role/role-function/role-function.service.spec.ts
@api-ventago/src/app/auth/services/user-structure.service.ts
@api-ventago/src/app/role/role-function/role-function.service.ts

<interfaces>
<!-- Existing Jest spec analog (role-function.service.spec.ts) — 따를 보일러플레이트 패턴 -->
```typescript
// 상단: 모델 mock + constructor 호출 패턴 (line 1-22)
import { RoleFunctionService } from './role-function.service';
import { RoleFunction } from './role-function.model';
import { RoleFunctionAction } from './role-function-action.model';

jest.mock('./role-function.model');
jest.mock('./role-function-action.model');

describe('RoleFunctionService', () => {
  let service: RoleFunctionService;
  const mockRoleFunctionModel = RoleFunction as any;
  const mockRoleFunctionActionModel = RoleFunctionAction as any;

  beforeEach(() => {
    service = new RoleFunctionService(
      mockRoleFunctionModel,
      mockRoleFunctionActionModel,
      // ← Plan 33.1-02 후 PermissionCacheService 추가됨 — Wave 2 spec 에서 mock 으로 전달
    );
    jest.clearAllMocks();
  });

  // ... describe('bulkUpdateRoleFunctionActions') 가 line 91-129 에 존재 — 확장 대상
});
```

<!-- D1 spec 작성을 위한 UserStructureService 신호 -->
```typescript
// user-structure.service.ts constructor (line 17-18)
@Injectable()
export class UserStructureService {
  constructor(private functionsService: FunctionsService) {}
  // → spec 에서는 mock FunctionsService 만 주입하면 됨

  async ensureRoleFunctions(user: Users, roleId: number): Promise<number[]>;
  // → 모델 import: RoleFunction.findAll (static call), UserFunction (인스턴스화)
}
```

<!-- UserFunctionService 호출 분기 (Plan 33.1-01 적용 후) -->
```typescript
// new UserFunctionService(UserFunction) — 모듈 import 인스턴스화
// userFunctionService.getEffectiveFunctions(user.id, roleFunctionIds, user.storeId)
// → spec 에서는 UserFunctionService prototype.getEffectiveFunctions 를 mock
```

<!-- tight repro 쉘이 호출할 endpoints -->
- POST /auth/login (admin@cool.test / <password>) → returns { token }
- PUT /role-functions/bulk-actions/:roleId (admin token) body { items: [{ functionId: 7, actions: ['read'] }] }
- GET /me (test user token) → triggers ensureRoleFunctions
- Docker PG query: `docker exec api_ventago node -e "..."` 패턴 (CLAUDE.md 의 DB 접속 예제)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D1 회귀 Jest spec 신규 작성 — user-structure.service.spec.ts</name>
  <files>api-ventago/src/app/auth/services/user-structure.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/auth/services/user-structure.service.ts (Plan 33.1-01 적용 후 — read-only ensureRoleFunctions 본문)
    - api-ventago/src/app/role/role-function/role-function.service.spec.ts (boilerplate pattern — jest.mock + 모델 mock 인스턴스)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (REG-FIX-01 시나리오)
  </read_first>
  <action>
    **신규 파일 생성: `api-ventago/src/app/auth/services/user-structure.service.spec.ts`**

    파일 내용 (CONTEXT.md REG-FIX-01 의 두 시나리오 + Jest boilerplate 따름):

    ```typescript
    /**
     * Phase 33.1 D1-FIX 회귀 spec
     *
     * 목적: ensureRoleFunctions 가 read-only 임을 증명
     *   (1) RoleFunctions 0건 → 어떤 RoleFunction.create 도 호출 안 됨
     *   (2) RoleFunctions 1건 → 그 functionId 만 반환, 추가 mutation 없음
     */

    import { UserStructureService } from './user-structure.service';
    import { RoleFunction } from '../../role/role-function/role-function.model';
    import { UserFunctionService } from '../../users/user-function/user-function.service';

    jest.mock('../../role/role-function/role-function.model');
    jest.mock('../../users/user-function/user-function.model');
    jest.mock('../../users/user-function/user-function.service');

    describe('UserStructureService — Phase 33.1 D1 hotfix', () => {
      let service: UserStructureService;
      const mockRoleFunctionModel = RoleFunction as any;

      beforeEach(() => {
        // FunctionsService 는 ensureRoleFunctions 와 무관 — 빈 mock
        service = new UserStructureService({} as any);
        jest.clearAllMocks();
      });

      describe('ensureRoleFunctions — D1-FIX 회귀', () => {
        it('RoleFunctions 0건 → RoleFunction.create 가 0번 호출되고 빈 배열 반환', async () => {
          mockRoleFunctionModel.findAll = jest.fn().mockResolvedValue([]);
          mockRoleFunctionModel.create = jest.fn().mockResolvedValue({});
          mockRoleFunctionModel.findOne = jest.fn().mockResolvedValue(null);

          // UserFunctionService.getEffectiveFunctions 도 mock — 빈 배열 입력 → 빈 배열 반환
          const mockGetEffective = jest.fn().mockResolvedValue([]);
          (UserFunctionService as jest.Mock).mockImplementation(() => ({
            getEffectiveFunctions: mockGetEffective,
          }));

          const fakeUser = { id: 1, storeId: 10 } as any;
          const result = await service.ensureRoleFunctions(fakeUser, 6);

          expect(result).toEqual([]);
          // [D1-FIX-01] RoleFunction.create 호출 0번 (Path A 제거 검증)
          expect(mockRoleFunctionModel.create).toHaveBeenCalledTimes(0);
          // [D1-FIX-02 + D1-FIX-03] backfillRoleFunctions 자체가 없으므로 findOrCreate 도 0번
          expect(mockRoleFunctionModel.findOrCreate ?? jest.fn()).not.toHaveBeenCalled?.();
          // findAll 은 read 1회만 (Plan 33.1-01 적용 후 retry/backfill 없음)
          expect(mockRoleFunctionModel.findAll).toHaveBeenCalledTimes(1);
        });

        it('RoleFunctions 1건 → 그 functionId 만 반환, mutation 없음', async () => {
          const mockRf = { functionId: 7, function: { name: 'crear-usuario' } };
          mockRoleFunctionModel.findAll = jest.fn().mockResolvedValue([mockRf]);
          mockRoleFunctionModel.create = jest.fn().mockResolvedValue({});
          mockRoleFunctionModel.findOrCreate = jest.fn().mockResolvedValue([{}, false]);

          // UserFunctionService.getEffectiveFunctions: [7] → [7] (override 없음)
          const mockGetEffective = jest.fn().mockResolvedValue([7]);
          (UserFunctionService as jest.Mock).mockImplementation(() => ({
            getEffectiveFunctions: mockGetEffective,
          }));

          const fakeUser = { id: 1, storeId: 10 } as any;
          const result = await service.ensureRoleFunctions(fakeUser, 6);

          expect(result).toEqual([7]);
          // [D1-FIX-02] backfillRoleFunctions 의 findOrCreate 가 절대 호출되지 않음
          expect(mockRoleFunctionModel.findOrCreate).toHaveBeenCalledTimes(0);
          // [D1-FIX-01] RoleFunction.create 도 0번
          expect(mockRoleFunctionModel.create).toHaveBeenCalledTimes(0);
          // findAll 은 1회만 (재조회 패턴 제거됨)
          expect(mockRoleFunctionModel.findAll).toHaveBeenCalledTimes(1);
          // getEffectiveFunctions 는 (userId, roleFunctionIds=[7], storeId) 로 호출
          expect(mockGetEffective).toHaveBeenCalledWith(1, [7], 10);
        });

        it('user.storeId 없음 → 빈 배열 반환, UserFunctionService 호출 안 함', async () => {
          mockRoleFunctionModel.findAll = jest.fn().mockResolvedValue([]);
          mockRoleFunctionModel.create = jest.fn().mockResolvedValue({});

          const mockGetEffective = jest.fn();
          (UserFunctionService as jest.Mock).mockImplementation(() => ({
            getEffectiveFunctions: mockGetEffective,
          }));

          const fakeUser = { id: 1, storeId: undefined } as any;
          const result = await service.ensureRoleFunctions(fakeUser, 6);

          expect(result).toEqual([]);
          expect(mockRoleFunctionModel.create).toHaveBeenCalledTimes(0);
          expect(mockGetEffective).not.toHaveBeenCalled();
        });
      });
    });
    ```

    **검증 가이드:**
    - `RoleFunction` 의 import path: `'../../role/role-function/role-function.model'` (auth/services/ 에서 상대 경로 2 level up)
    - `UserFunctionService` import path: `'../../users/user-function/user-function.service'`
    - 실제 Plan 33.1-01 의 read-only ensureRoleFunctions body 가 적용된 상태에서 실행되어야 함 (Wave 2 = depends_on Plan 01)
    - jest.config 또는 testRegex 가 `.spec.ts` 파일을 찾도록 설정되어 있음 (확인: `cd api-ventago && cat jest.config.json 2>/dev/null || grep -A 10 '"jest"' package.json`)

    **ESLint 규칙 (CLAUDE.md):**
    - 주석 위 빈 줄 (`lines-around-comment`)
    - `import` 정렬: NestJS/타사 라이브러리 → 프로젝트 내부 경로
    - 위 코드에 이미 반영됨
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago && npx jest --testPathPattern user-structure.service.spec 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 파일 생성 확인: `test -f api-ventago/src/app/auth/services/user-structure.service.spec.ts && echo OK` returns `OK`
    - `grep -c "describe\\(.ensureRoleFunctions" api-ventago/src/app/auth/services/user-structure.service.spec.ts` returns `1` (describe 블록 존재)
    - `grep -c "expect.*RoleFunction.*create.*toHaveBeenCalledTimes(0)" api-ventago/src/app/auth/services/user-structure.service.spec.ts` ≥ 2 (두 시나리오 모두 create 0건 검증)
    - `cd api-ventago && npx jest --testPathPattern user-structure.service.spec --silent` exit code 0 (3 tests PASS)
    - `cd api-ventago && npx eslint src/app/auth/services/user-structure.service.spec.ts` exit code 0
  </acceptance_criteria>
  <done>
    user-structure.service.spec.ts 신규 파일 생성. 3 describe block (RoleFunctions 0건 / 1건 / storeId 없음). 모두 PASS. RoleFunction.create 0번 호출 검증으로 D1-FIX-01/02/03 회귀 가드 완성.
  </done>
</task>

<task type="auto">
  <name>Task 2: D2 회귀 Jest spec 기존 파일 확장 — role-function.service.spec.ts</name>
  <files>api-ventago/src/app/role/role-function/role-function.service.spec.ts</files>
  <read_first>
    - api-ventago/src/app/role/role-function/role-function.service.spec.ts (전체 — bulkUpdateRoleFunctionActions describe block line 89-130 + constructor instantiation line 17-20)
    - api-ventago/src/app/role/role-function/role-function.service.ts (Plan 33.1-02 적용 후 — constructor 시그니처 변경됨)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (REG-FIX-02 시나리오)
  </read_first>
  <action>
    **편집 1 — 상단 import 추가 + constructor 호출 변경:**

    파일 line 1-7 의 imports 직후 추가:
    ```typescript
    import { PermissionCacheService } from '../../permissions/permission-cache.service';
    ```

    `beforeEach` 내부 (line 16-21) 의 `service = new RoleFunctionService(...)` 호출을 다음으로 변경:

    현재:
    ```typescript
    beforeEach(() => {
      service = new RoleFunctionService(
        mockRoleFunctionModel,
        mockRoleFunctionActionModel,
      );
      jest.clearAllMocks();
    });
    ```

    변경:
    ```typescript
    // PermissionCacheService mock — Plan 33.1-02 의 invalidateRole 호출 검증용
    let mockPermissionCacheService: { invalidateRole: jest.Mock };

    beforeEach(() => {
      mockPermissionCacheService = {
        invalidateRole: jest.fn().mockResolvedValue(0),
      };
      service = new RoleFunctionService(
        mockRoleFunctionModel,
        mockRoleFunctionActionModel,
        mockPermissionCacheService as unknown as PermissionCacheService,
      );
      jest.clearAllMocks();
      // mock 인스턴스는 clearAllMocks 후 재초기화 필요 (jest 호출 카운트만 리셋)
      mockPermissionCacheService.invalidateRole.mockResolvedValue(0);
    });
    ```

    (참고: 변수 선언 위치를 `let service` 직후로 옮긴다. 기존 `let service: RoleFunctionService;` 다음 line 에 `let mockPermissionCacheService: ...;` 추가.)

    **편집 2 — `describe('bulkUpdateRoleFunctionActions')` 블록 (line 91-129) 끝에 신규 it() 2개 추가:**

    기존 두 it 블록 (line 92-113 + 115-128) 다음에 다음 추가:

    ```typescript

        it('[Phase 33.1 D2-FIX] invalidateRole 가 정확히 1번 (roleId 인자로) 호출됨', async () => {
          mockRoleFunctionModel.findAll = jest.fn().mockResolvedValue([]);
          jest.spyOn(service, 'createWithActions').mockResolvedValue({ id: 1 } as any);
          jest.spyOn(service, 'updateActionsForRoleFunction').mockResolvedValue([]);

          await service.bulkUpdateRoleFunctionActions(6, 1, [
            { functionId: 7, actions: ['read'] },
          ]);

          expect(mockPermissionCacheService.invalidateRole).toHaveBeenCalledTimes(1);
          expect(mockPermissionCacheService.invalidateRole).toHaveBeenCalledWith(6);
        });

        it('[Phase 33.1 D2-FIX] data 가 빈 배열이어도 invalidateRole 호출 (기존 RF 전체 삭제 시나리오)', async () => {
          const rf = { id: 100, functionId: 5, destroy: jest.fn() };
          mockRoleFunctionModel.findAll = jest.fn().mockResolvedValue([rf]);
          jest.spyOn(service, 'updateActionsForRoleFunction').mockResolvedValue([]);

          await service.bulkUpdateRoleFunctionActions(6, 1, []);

          expect(rf.destroy).toHaveBeenCalled();
          // 모든 RF 삭제 후에도 invalidateRole 호출 (cache 정합성 보장)
          expect(mockPermissionCacheService.invalidateRole).toHaveBeenCalledTimes(1);
          expect(mockPermissionCacheService.invalidateRole).toHaveBeenCalledWith(6);
        });
    ```

    **근거 (REG-FIX-02 per 33.1-CONTEXT.md):**
    - 시나리오 1: 정상 bulk-actions → invalidateRole 1번 호출 + 인자 = roleId
    - 시나리오 2: edge case (data=[] 로 전체 삭제) — 여전히 cache invalidate 되어야 함
    - 기존 두 it() 블록 (line 92-113 + 115-128) 은 그대로 유지 — 회귀 보호

    **ESLint 규칙:**
    - 신규 it() 블록 위에 빈 줄
    - 주석 `// [Phase 33.1 D2-FIX] ...` 위에 빈 줄

    **참고 — Plan 33.1-02 의 constructor 변경 확인:**
    Plan 33.1-02 Task 2-b 에 따라 RoleFunctionService constructor 가 `(model, actionModel, permissionCacheService)` 3-arg 가 된 상태. spec 의 `new RoleFunctionService(...)` 호출도 동일하게 3-arg 로 맞추어야 NestJS DI 없이 직접 인스턴스화 가능.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago && npx jest --testPathPattern role-function.service.spec 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PermissionCacheService" api-ventago/src/app/role/role-function/role-function.service.spec.ts` returns ≥ 2 lines (import + type assertion)
    - `grep -c "invalidateRole" api-ventago/src/app/role/role-function/role-function.service.spec.ts` ≥ 4 (mock 선언 + 2 it 블록의 expect 들)
    - `grep "toHaveBeenCalledWith(6)" api-ventago/src/app/role/role-function/role-function.service.spec.ts` returns ≥ 2 lines (두 신규 it 블록 모두 roleId=6 검증)
    - `cd api-ventago && npx jest --testPathPattern role-function.service.spec --silent` exit code 0 (기존 + 신규 모두 PASS — 6개 it 블록 이상)
    - `cd api-ventago && npx eslint src/app/role/role-function/role-function.service.spec.ts` exit code 0
    - 기존 4개 it 블록 모두 그대로 유지: `grep -c "it\\(" api-ventago/src/app/role/role-function/role-function.service.spec.ts` returns ≥ 6 (기존 4 + 신규 2)
  </acceptance_criteria>
  <done>
    role-function.service.spec.ts 가 PermissionCacheService mock 을 받도록 constructor 호출 업데이트됨. bulkUpdateRoleFunctionActions describe block 에 신규 2 it 추가 (정상 시나리오 + 빈 배열 edge case). 전체 Jest 실행 시 PASS. ESLint 통과.
  </done>
</task>

<task type="auto">
  <name>Task 3: tight repro 쉘 스크립트 작성 — phase33.1-d1-d2-repro.sh</name>
  <files>api-ventago/test/phase33.1-d1-d2-repro.sh</files>
  <read_first>
    - .planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md (tight repro 시나리오 — line 51-58 참조)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (REG-FIX-03 시나리오)
    - CLAUDE.md (DB 접속 패턴: docker exec api_ventago node -e "...")
  </read_first>
  <action>
    **신규 파일 생성: `api-ventago/test/phase33.1-d1-d2-repro.sh` (실행권한 0755)**

    내용:

    ```bash
    #!/usr/bin/env bash
    # Phase 33.1 D1/D2 hotfix tight repro
    #
    # 사전 조건:
    #   1. ./dev.sh 가 가동 중 (api:5002 + dbpostgres docker 컨테이너 동작)
    #   2. admin@cool.test 계정 존재 (store_id=1)
    #   3. Vendedor Store role (id=6, store_id=1) 존재 — 없으면 SKIP 메시지
    #   4. test_perm_userb@local.test 또는 동등한 test user 가 Vendedor Store role 보유
    #
    # 사용법:
    #   ADMIN_EMAIL=admin@cool.test ADMIN_PASS=<password> \
    #   TEST_EMAIL=test_perm_userb@local.test TEST_PASS=<password> \
    #   ./api-ventago/test/phase33.1-d1-d2-repro.sh
    #
    # 종료 코드:
    #   0 — 두 assertion 모두 PASS (D1 + D2 fix 확인됨)
    #   1 — D1 assertion FAIL (role_functions row 수가 1 이 아님 — auto-fill 재발생)
    #   2 — D2 assertion FAIL (user_permission_cache row 가 0 — cache write 안 됨)
    #   3 — 사전 조건 불충족 (auth 실패, role 없음 등)

    set -euo pipefail

    API="${API:-http://localhost:5002/api}"
    ADMIN_EMAIL="${ADMIN_EMAIL:-admin@cool.test}"
    ADMIN_PASS="${ADMIN_PASS:?ADMIN_PASS 환경변수 필수}"
    TEST_EMAIL="${TEST_EMAIL:?TEST_EMAIL 환경변수 필수 (Vendedor Store role 보유 test user)}"
    TEST_PASS="${TEST_PASS:?TEST_PASS 환경변수 필수}"
    VENDEDOR_ROLE_ID="${VENDEDOR_ROLE_ID:-6}"
    TARGET_FUNCTION_ID="${TARGET_FUNCTION_ID:-7}"

    # DB 쿼리 헬퍼 (CLAUDE.md 패턴)
    pg_query() {
      docker exec api_ventago node -e "
        const { Client } = require('pg');
        const c = new Client({host:'dbpostgres',user:'coolsistema',password:'<REDACTED>',database:'ventago'});
        c.connect().then(() => c.query(\"$1\")).then(r => { console.log(JSON.stringify(r.rows)); c.end(); }).catch(e => { console.error(e.message); process.exit(1); });
      " 2>/dev/null
    }

    echo "[STEP 1] admin 로그인..."
    ADMIN_TOKEN=$(curl -s -X POST "$API/auth/login" \
      -H "Content-Type: application/json" \
      -d "{\"email\":\"$ADMIN_EMAIL\",\"password\":\"$ADMIN_PASS\"}" | jq -r '.token // .access_token // empty')
    if [[ -z "$ADMIN_TOKEN" ]]; then
      echo "ERROR: admin 로그인 실패" >&2
      exit 3
    fi

    echo "[STEP 2] test user 로그인..."
    TEST_TOKEN=$(curl -s -X POST "$API/auth/login" \
      -H "Content-Type: application/json" \
      -d "{\"email\":\"$TEST_EMAIL\",\"password\":\"$TEST_PASS\"}" | jq -r '.token // .access_token // empty')
    if [[ -z "$TEST_TOKEN" ]]; then
      echo "ERROR: test user 로그인 실패" >&2
      exit 3
    fi

    echo "[STEP 3] Vendedor Store role 의 모든 role_function 삭제 (clean baseline)..."
    pg_query "DELETE FROM role_function_actions WHERE role_function_id IN (SELECT id FROM role_functions WHERE role_id = $VENDEDOR_ROLE_ID);" > /dev/null
    pg_query "DELETE FROM role_functions WHERE role_id = $VENDEDOR_ROLE_ID;" > /dev/null
    pg_query "DELETE FROM user_permission_cache WHERE user_id IN (SELECT user_id FROM user_roles WHERE role_id = $VENDEDOR_ROLE_ID);" > /dev/null

    echo "[STEP 4] bulk-actions: fn=$TARGET_FUNCTION_ID actions=['read']..."
    curl -s -X PUT "$API/role-functions/bulk-actions/$VENDEDOR_ROLE_ID" \
      -H "Authorization: Bearer $ADMIN_TOKEN" \
      -H "Content-Type: application/json" \
      -d "{\"items\":[{\"functionId\":$TARGET_FUNCTION_ID,\"actions\":[\"read\"]}]}" > /dev/null

    echo "[STEP 5] DB 검증 — bulk-actions 직후 role_functions count..."
    BEFORE_COUNT=$(pg_query "SELECT count(*)::int AS c FROM role_functions WHERE role_id = $VENDEDOR_ROLE_ID;" | jq '.[0].c')
    echo "  → role_functions count = $BEFORE_COUNT (expected: 1)"
    if [[ "$BEFORE_COUNT" != "1" ]]; then
      echo "ERROR: bulk-actions 직후 row 수가 1 이 아님 — Plan 33.1-02 의 bulk update 로직 문제" >&2
      exit 1
    fi

    echo "[STEP 6] test user /me 호출 (D1 trigger point — read-only 검증)..."
    curl -s "$API/me" -H "Authorization: Bearer $TEST_TOKEN" > /dev/null

    echo "[STEP 7] DB 검증 — /me 호출 후 role_functions count (D1-FIX)..."
    AFTER_COUNT=$(pg_query "SELECT count(*)::int AS c FROM role_functions WHERE role_id = $VENDEDOR_ROLE_ID;" | jq '.[0].c')
    echo "  → role_functions count = $AFTER_COUNT (expected: 1 — auto-fill 차단)"
    if [[ "$AFTER_COUNT" != "1" ]]; then
      echo "❌ FAIL [D1]: /me 가 role_functions 를 mutate 했음 (count $BEFORE_COUNT → $AFTER_COUNT)" >&2
      echo "   Plan 33.1-01 의 ensureRoleFunctions read-only refactor 미적용 또는 실패" >&2
      exit 1
    fi

    echo "[STEP 8] DB 검증 — user_permission_cache write 됨 (D2-FIX)..."
    TEST_USER_ID=$(pg_query "SELECT id::int FROM users WHERE email = '$TEST_EMAIL';" | jq '.[0].id')
    CACHE_COUNT=$(pg_query "SELECT count(*)::int AS c FROM user_permission_cache WHERE user_id = $TEST_USER_ID;" | jq '.[0].c')
    echo "  → user_permission_cache count for user_id=$TEST_USER_ID = $CACHE_COUNT (expected: ≥ 1)"
    if [[ "$CACHE_COUNT" -lt 1 ]]; then
      echo "⚠ SOFT-FAIL [D2]: cache write 안 됨. 가능한 원인:" >&2
      echo "   (a) PermissionResolverService 가 /me 경로에서 cache.set 을 호출하지 않음 (별도 wiring 필요)" >&2
      echo "   (b) Plan 33.1-02 의 invalidateRole 호출은 OK 지만 cache 가 write 되지 않으면 다음 /me 도 cache miss" >&2
      echo "   참고: D2 fix 의 일차 목표는 invalidate (cache write 는 Phase 33 PermissionResolverService 의 책임)" >&2
      # 이 케이스는 hotfix 범위 외이므로 exit 2 가 아니라 경고만 — 사용자가 결정
      exit 0
    fi

    echo ""
    echo "✅ ALL ASSERTIONS PASS"
    echo "  [D1] role_functions count = 1 (auto-fill 차단됨)"
    echo "  [D2] user_permission_cache count = $CACHE_COUNT (cache write 됨)"
    exit 0
    ```

    **권한 부여:**
    스크립트 작성 후 `chmod 755 api-ventago/test/phase33.1-d1-d2-repro.sh` 실행.

    **참고 — D2 soft-fail 처리 근거:**
    Plan 33.1-02 의 invalidateRole 은 cache **삭제** 만 보장한다. cache **쓰기** 는 PermissionResolverService 의 책임으로 Phase 33 본 구현에 포함됨 (CONTEXT.md 의 "수정 후 흐름" 참조: `PermissionResolverService → cache miss → 계산 → cache write`). 따라서 cache count = 0 이어도 Plan 33.1-02 의 fix 가 실패한 것이 아닐 수 있음 — 별도 Phase 33 wiring 이슈일 가능성. 이 케이스는 경고만 출력하고 exit 0.

    **사용자 실행 안내 (autonomous: false 의 이유):**
    이 스크립트는 dev 환경 (`./dev.sh` 가 가동) + 실제 test user 자격증명을 요구하므로 autonomous 실행 불가. Plan 33.1-01 + 02 적용 후 사용자가 수동으로 실행하여 hotfix 검증.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && test -x api-ventago/test/phase33.1-d1-d2-repro.sh && bash -n api-ventago/test/phase33.1-d1-d2-repro.sh && echo "SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + 실행권한: `test -x api-ventago/test/phase33.1-d1-d2-repro.sh && echo OK` returns `OK`
    - Bash 문법 검증: `bash -n api-ventago/test/phase33.1-d1-d2-repro.sh` exit code 0
    - 필수 단계 모두 포함: `grep -c "STEP " api-ventago/test/phase33.1-d1-d2-repro.sh` returns ≥ 8
    - DB 쿼리 패턴 (CLAUDE.md): `grep "docker exec api_ventago" api-ventago/test/phase33.1-d1-d2-repro.sh` returns ≥ 1 line
    - D1 assertion: `grep "AFTER_COUNT.*1" api-ventago/test/phase33.1-d1-d2-repro.sh` returns the expected-1 check
    - D2 assertion: `grep "user_permission_cache" api-ventago/test/phase33.1-d1-d2-repro.sh` returns ≥ 3 lines
    - 환경변수 보호: `grep "ADMIN_PASS:?" api-ventago/test/phase33.1-d1-d2-repro.sh` returns the required-var check
    - shellcheck (있을 경우): `which shellcheck >/dev/null && shellcheck api-ventago/test/phase33.1-d1-d2-repro.sh || echo "shellcheck 미설치 — 스킵"` (exit 0)
  </acceptance_criteria>
  <done>
    phase33.1-d1-d2-repro.sh 가 실행권한과 함께 생성됨. 8단계 시나리오 (admin login → test user login → baseline reset → bulk-actions → DB before count → /me 호출 → DB after count [D1 assertion] → cache count [D2 soft assertion]) 가 audit 의 tight repro 와 일치. bash -n 문법 검증 통과.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 사용자 — dev 환경에서 phase33.1-d1-d2-repro.sh 실행 + 결과 확인</name>
  <files>(no file modification — verification only)</files>
  <read_first>
    - api-ventago/test/phase33.1-d1-d2-repro.sh (Task 3 에서 생성된 쉘)
    - .planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-CONTEXT.md (전체 D1/D2/REG 결정 + tight repro 기대 결과)
    - .planning/phases/33-permissions-v2/audit/2026-05-24-automated-verification.md (원본 결함 reproduction)
  </read_first>
  <action>
    **사용자가 직접 실행 — Claude 는 대기 (blocking checkpoint).**

    사용자에게 다음 안내 메시지 출력:

    ```
    Plan 33.1-01 (D1) + 33.1-02 (D2) + 33.1-03 Task 1-3 (Jest spec 2 + repro 쉘) 가 모두 완료되었습니다.
    이제 dev 환경에서 hotfix 가 실제로 동작하는지 확인해주세요.

    [수행 단계]

    1. dev 환경 가동 확인:
       cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0 && ./dev.sh
       (이미 가동 중이면 스킵)

    2. test user 자격증명 준비. test_perm_userb@local.test 또는 동등한 Vendedor Store role(id=6) 보유 user.
       없으면 dev DB 에서 조회:
       docker exec api_ventago node -e "const {Client}=require('pg');const c=new Client({host:'dbpostgres',user:'coolsistema',password:'<REDACTED>',database:'ventago'});c.connect().then(()=>c.query(\"SELECT u.id,u.email FROM users u JOIN user_roles ur ON ur.user_id=u.id WHERE ur.role_id=6 LIMIT 5;\")).then(r=>{console.log(r.rows);c.end();});"

    3. Jest spec 자동 실행 (Plan 33.1-01 + 02 의 회귀 가드):
       cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago
       npx jest --testPathPattern "(user-structure|role-function)\.service\.spec" --silent
       기대: exit code 0, 모든 test PASS

    4. tight repro 쉘 실행:
       ADMIN_EMAIL=admin@cool.test ADMIN_PASS=<실제 패스워드> \
       TEST_EMAIL=<test user email> TEST_PASS=<실제 패스워드> \
       /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/test/phase33.1-d1-d2-repro.sh

       기대 결과:
       - STEP 5: role_functions count = 1
       - STEP 7: role_functions count = 1 (auto-fill 차단 확인 — D1 PASS)
       - STEP 8: user_permission_cache count ≥ 1 (또는 0 이지만 soft-warn — D2 fix 의 invalidate 자체는 OK)
       - 마지막 라인: "✅ ALL ASSERTIONS PASS"

    5. 운영 안전성 체크:
       - 마이그레이션 0 건 확인: git status api-ventago/migrations/ → Phase 33.1 관련 신규 파일 없어야 함
       - 옵셔널 cleanup SQL (CONTEXT.md 의 33.1-cleanup-orphan-role-functions.sql) 은 deferred — 운영 적용 시 별도 confirm

    [중요: D2 cache write 부분 PASS 시 주의]
    STEP 8 의 user_permission_cache count 가 0 (soft-warn) 일 경우, Phase 33.1 의 invalidate 자체는 작동하지만
    cache WRITE 가 PermissionResolverService 어딘가에서 누락된 상태. 이 경우:
    - Phase 33.1 자체는 verifying 통과 가능 (invalidateRole 호출 확인됨)
    - **그러나 Phase 33 의 "5분 TTL 캐시 작동" verifying 상태는 해소 불가** — PermissionResolverService 호출 흐름 별도 조사 필요
    - 운영 PG10 적용은 D1/D2 invalidate 만으로도 안전 (성능 효과만 미달성)

    [결과 회신]
    - "approved" 또는 "PASS" 입력 → Phase 33.1 verifying 단계 진입
    - 실패 시 STEP n 의 에러 출력을 그대로 회신 → 추가 hotfix plan 작성
    ```

    **Claude 의 역할:**
    - 위 안내 출력 후 사용자 응답 대기 (blocking)
    - "approved" 응답 시 SUMMARY 작성으로 진행
    - 실패 회신 시 Plan 33.1-01 또는 02 의 implementation 검토 + 후속 plan 제안

    **자동화 불가능 사유 (CLAUDE 가 직접 실행 못 하는 이유):**
    - 실제 dev 환경 가동 상태 + test user 자격증명 (password) 이 필요
    - admin/test user password 는 git/repo 에 없으며 사용자만 보유
    - bulk-actions PUT + /me GET 의 실제 HTTP 트래픽 발생 — dev 환경의 진짜 데이터 변경
  </action>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — Claude waits for user confirmation (approved/PASS) or failure report"</automated>
  </verify>
  <acceptance_criteria>
    - 사용자가 "approved" 또는 동등한 PASS 시그널 응답
    - 또는: 명시적 실패 보고 (어느 STEP 이 어떤 결과로 FAIL 했는지) → 후속 plan 트리거
    - Jest 결과: "Tests: N passed, 0 failed"
    - 쉘 결과: 마지막 line "✅ ALL ASSERTIONS PASS" (D2 soft-warn 도 PASS 로 간주)
    - git status: api-ventago/migrations/ 디렉토리에 Phase 33.1 관련 신규 파일 0건 (code-only hotfix 확인)
  </acceptance_criteria>
  <done>
    사용자가 dev 환경 검증 결과를 회신함 (PASS 또는 FAIL). PASS 인 경우 Phase 33.1 가 verifying 상태로 전환되어 운영 PG10 적용 RUNBOOK 단계로 진행. FAIL 인 경우 후속 hotfix plan 트리거.
  </done>
  <resume-signal>Type "approved" if all PASS, or describe the failure (Jest output 또는 repro 쉘 STEP n FAIL).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec mocks ↔ implementation | mock 이 실제 시그니처와 일치하지 않으면 false PASS 가능 |
| Repro 쉘 ↔ dev DB | 쉘이 baseline reset 시 다른 user 의 role 데이터 영향 없도록 role_id 격리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.1-11 | Tampering | Jest mock 이 실제 implementation 과 drift | mitigate | Plan 33.1-01/02 적용 후 즉시 spec 실행 (Wave 2 depends_on) — 시그니처 drift 시 즉시 FAIL |
| T-33.1-12 | DoS | repro 쉘의 baseline reset SQL 이 다른 role 데이터 삭제 | mitigate | DELETE WHERE role_id = $VENDEDOR_ROLE_ID 명시 (id=6 hardcode default, env override 가능). user_permission_cache 도 user_roles JOIN 으로 영향 user 만 |
| T-33.1-13 | Information Disclosure | 쉘 스크립트 로그에 token 노출 | mitigate | `> /dev/null` 로 curl 출력 suppress, token 변수만 추출 |
| T-33.1-14 | Repudiation | Jest spec 결과가 CI/CD 에 통합되지 않음 | accept | 본 phase 의 scope 아님 — CI 통합은 별도 phase (Phase 33 verifying 단계 또는 운영 적용 RUNBOOK 에서 처리) |
</threat_model>

<verification>
**전체 plan 완료 후 회귀 확인:**

1. Jest 전체 통과: `cd api-ventago && npx jest --testPathPattern "(user-structure|role-function)\.service\.spec"` exit 0
2. ESLint 통과: `cd api-ventago && npx eslint src/app/auth/services/user-structure.service.spec.ts src/app/role/role-function/role-function.service.spec.ts`
3. 쉘 문법 OK: `bash -n api-ventago/test/phase33.1-d1-d2-repro.sh`
4. Repro 쉘 실행 (수동 — Task 4 checkpoint): 모든 assertion PASS
5. 코드 변경 없음 (Plan 33.1-01 + 02 의 implementation 은 손대지 않음): `git diff api-ventago/src/app/auth/services/user-structure.service.ts api-ventago/src/app/role/role-function/role-function.service.ts` → 0 line diff (Plan 33.1-03 은 spec + 쉘만 추가)
</verification>

<success_criteria>
- REG-FIX-01: user-structure.service.spec.ts 신규 생성, 3 test PASS, RoleFunction.create 0번 호출 검증
- REG-FIX-02: role-function.service.spec.ts 확장 (2 신규 it), invalidateRole 정확히 1번 호출 + roleId 인자 검증
- REG-FIX-03: phase33.1-d1-d2-repro.sh 생성 + 실행권한 + 8 STEP 시나리오 + D1/D2 assertion
- Task 4 사용자 checkpoint: 실제 dev 환경에서 repro 쉘 실행 → "✅ ALL ASSERTIONS PASS"
- 운영 적용 안전성: 마이그레이션 0건 (code-only hotfix 확인)
</success_criteria>

<output>
After completion, create `.planning/phases/33.1-permissions-v2-hotfix-d1-d2/33.1-03-SUMMARY.md` documenting:
- Jest 실행 결과 (PASS count, 시간)
- Repro 쉘 실행 결과 (STEP 별 출력 발췌)
- 운영 PG10 적용 RUNBOOK 추가 권고 사항:
  - Plan 33.1-01 + 02 의 코드 배포 (Docker rebuild)
  - 옵셔널 cleanup SQL (CONTEXT.md `33.1-cleanup-orphan-role-functions.sql`) 의 사용자 confirm 후 운영 적용 여부
  - Phase 33 verifying status 의 해소 조건 (D1/D2 fix + 본 plan 의 회귀 검증 통과)
</output>
