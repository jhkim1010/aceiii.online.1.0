---
phase: 08-reportajes-ux
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ventago-app/src/views/reports/vendedor/VendedorReportBody.tsx
  - ventago-app/src/views/reports/vendedor/VendedorReport.tsx
  - ventago-app/src/views/reports/gastos/GastoReportBody.tsx
  - ventago-app/src/views/reports/gastos/GastoReport.tsx
  - ventago-app/src/views/reports/breve-venta/BreveVentaReportBody.tsx
  - ventago-app/src/views/reports/breve-venta/BreveVentaReport.tsx
  - ventago-app/src/views/reports/fallados/FalladosReportBody.tsx
  - ventago-app/src/views/reports/fallados/FalladosReport.tsx
  - ventago-app/src/views/reports/corregido/CorregidoReportBody.tsx
  - ventago-app/src/views/reports/corregido/CorregidoReport.tsx
  - ventago-app/src/views/reports/facturacion/FacturacionReportBody.tsx
  - ventago-app/src/views/reports/facturacion/FacturacionReport.tsx
  - ventago-app/src/views/reports/ingreso/IngresoReportBody.tsx
  - ventago-app/src/views/reports/ingreso/IngresoReport.tsx
  - ventago-app/src/views/reports/movidos/MovidosReportBody.tsx
  - ventago-app/src/views/reports/movidos/MovidosReport.tsx
  - ventago-app/src/views/reports/reservado/ReservadoReportBody.tsx
  - ventago-app/src/views/reports/reservado/ReservadoReport.tsx
  - ventago-app/src/views/reports/cheque-estado/ChequeEstadoReportBody.tsx
  - ventago-app/src/views/reports/cheque-estado/ChequeEstadoReport.tsx
  - ventago-app/src/views/reports/alertas/AlertasReportBody.tsx
  - ventago-app/src/views/reports/alertas/AlertasReport.tsx
  - ventago-app/src/views/reports/clientes-credito/ClientesCreditoReportBody.tsx
  - ventago-app/src/views/reports/clientes-credito/ClientesCreditoReport.tsx
  - ventago-app/src/views/reports/sales/SalesReportBody.tsx
  - ventago-app/src/views/reports/sales/SalesReport.tsx
  - ventago-app/src/views/reports/products/ProductReportBody.tsx
  - ventago-app/src/views/reports/products/ProductReport.tsx
  - ventago-app/src/views/reports/stocks/StockReportBody.tsx
  - ventago-app/src/views/reports/stocks/StockReport.tsx
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - "15개 Phase 6 view가 XxxReportBody (inner) + XxxReport (thin wrapper) 구조로 분리됨"
    - "XxxReportBody는 `{ params, setParams }` props를 받고 훅에 주입"
    - "XxxReport는 `useState(xxxDefaultParams)` + `<XxxReportBody />` 렌더링만 수행"
    - "pages/reportes/* 라우트는 수정되지 않음 — CONTEXT.md 원칙 준수"
    - "ReportesHub.tsx 미수정 — Phase 6 hub는 동결"
    - "모든 기존 /reportes/* 페이지가 zero regression으로 동작 (헬퍼 버튼, drilldown, 날짜 선택 포함)"
  artifacts:
    - path: "ventago-app/src/views/reports/vendedor/VendedorReportBody.tsx"
      provides: "Embeddable body component with (params, setParams) props"
  key_links:
    - from: "VendedorReport (wrapper)"
      to: "VendedorReportBody"
      via: "params useState owned by wrapper"
      pattern: "useState.*vendedorDefaultParams"
---

<objective>
15개 Phase 6 view 컴포넌트를 **Body 추출 패턴**으로 분리한다. `XxxReport.tsx` (기존 default export)를 `useState(xxxDefaultParams)` + `<XxxReportBody params={params} setParams={setParams} />` 만 담는 thin wrapper로 전환하고, 실제 JSX/훅 호출은 새 `XxxReportBody.tsx` 로 이전한다.

이 구조는 Phase 8 셸(08-03)의 `ReportsPreviewPanel`이 동일 Body 컴포넌트를 lazy import해서 preview에 embed할 수 있게 한다.

**핵심 원칙 (CONTEXT.md locked):**
- `pages/reportes/*` 라우트 파일 수정 금지
- `ReportesHub.tsx` 수정 금지
- 기존 default export 이름 유지 (`export default VendedorReport`) — page import 경로 불변
- Variant A 3개는 helper 함수(setThisMonth/setToday/clear)를 Wrapper로 이전 — Body는 pure controlled

Purpose: 셸이 재사용할 embeddable Body를 확보하면서 Phase 6 페이지 회귀 0.
Output: 15 new XxxReportBody.tsx files + 15 updated XxxReport.tsx wrappers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reportajes-ux/08-CONTEXT.md
@.planning/phases/08-reportajes-ux/08-RESEARCH.md
@.planning/phases/08-reportajes-ux/08-01-SUMMARY.md
@ventago-app/src/views/reports/vendedor/VendedorReport.tsx
@ventago-app/src/views/reports/sales/SalesReport.tsx

<interfaces>
<!-- Body extraction template — apply to 15 reports. -->

```typescript
// views/reports/vendedor/VendedorReportBody.tsx  (NEW)
import { useEffect } from 'react'
import { Grid, Card } from '@mui/material'
import useVendedorReport from './hooks/useVendedorReport'
import VendedorReportTable from './components/VendedorReportTable'
// ... 기존 VendedorReport.tsx에서 JSX 그대로 이전

type Props = { params: any; setParams: (p: any) => void }

const VendedorReportBody = ({ params, setParams }: Props) => {
  const { data, getData, loading, downloadExcel } = useVendedorReport(params)

  useEffect(() => {
    getData()
  }, [params])

  return (
    <Grid container spacing={4}>
      {/* 기존 VendedorReport.tsx JSX 그대로 복사 */}
    </Grid>
  )
}

export default VendedorReportBody
```

```typescript
// views/reports/vendedor/VendedorReport.tsx  (UPDATED — thin wrapper)
import { useState } from 'react'
import VendedorReportBody from './VendedorReportBody'
import { vendedorDefaultParams } from './hooks/useVendedorReport'

const VendedorReport = () => {
  const [params, setParams] = useState<any>(vendedorDefaultParams)

  return <VendedorReportBody params={params} setParams={setParams} />
}

export default VendedorReport
```

**Variant A (sales/products/stocks) — wrapper가 helper 함수 소유:**
```typescript
const SalesReport = () => {
  const [params, setParams] = useState<any>(salesDefaultParams)

  const setThisMonth = () => setParams((p: any) => ({
    ...p,
    startDate: DateTime.now().startOf('month').toFormat('yyyy-MM-dd'),
    endDate: DateTime.now().toFormat('yyyy-MM-dd')
  }))
  const setToday = () => { /* ... */ }
  const clear = () => setParams(salesDefaultParams())

  return (
    <SalesReportBody
      params={params}
      setParams={setParams}
      setThisMonth={setThisMonth}
      setToday={setToday}
      clear={clear}
    />
  )
}
```
SalesReportBody는 위 helper props도 수신하여 기존 버튼에 전달. Phase 8 셸은 helper 없이 Body를 embed 가능 (helper props optional).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract Body from 10 Variant B/C reports (vendedor, gastos, breve-venta, fallados, corregido, facturacion, ingreso, movidos, reservado, cheque-estado, alertas, clientes-credito)</name>
  <files>
    ventago-app/src/views/reports/vendedor/VendedorReportBody.tsx,
    ventago-app/src/views/reports/vendedor/VendedorReport.tsx,
    ventago-app/src/views/reports/gastos/GastoReportBody.tsx,
    ventago-app/src/views/reports/gastos/GastoReport.tsx,
    ventago-app/src/views/reports/breve-venta/BreveVentaReportBody.tsx,
    ventago-app/src/views/reports/breve-venta/BreveVentaReport.tsx,
    ventago-app/src/views/reports/fallados/FalladosReportBody.tsx,
    ventago-app/src/views/reports/fallados/FalladosReport.tsx,
    ventago-app/src/views/reports/corregido/CorregidoReportBody.tsx,
    ventago-app/src/views/reports/corregido/CorregidoReport.tsx,
    ventago-app/src/views/reports/facturacion/FacturacionReportBody.tsx,
    ventago-app/src/views/reports/facturacion/FacturacionReport.tsx,
    ventago-app/src/views/reports/ingreso/IngresoReportBody.tsx,
    ventago-app/src/views/reports/ingreso/IngresoReport.tsx,
    ventago-app/src/views/reports/movidos/MovidosReportBody.tsx,
    ventago-app/src/views/reports/movidos/MovidosReport.tsx,
    ventago-app/src/views/reports/reservado/ReservadoReportBody.tsx,
    ventago-app/src/views/reports/reservado/ReservadoReport.tsx,
    ventago-app/src/views/reports/cheque-estado/ChequeEstadoReportBody.tsx,
    ventago-app/src/views/reports/cheque-estado/ChequeEstadoReport.tsx,
    ventago-app/src/views/reports/alertas/AlertasReportBody.tsx,
    ventago-app/src/views/reports/alertas/AlertasReport.tsx,
    ventago-app/src/views/reports/clientes-credito/ClientesCreditoReportBody.tsx,
    ventago-app/src/views/reports/clientes-credito/ClientesCreditoReport.tsx
  </files>
  <action>
    12개 Variant B/C 보고서 각각에 대해:

    1. **새 `XxxReportBody.tsx` 생성**:
       - 기존 `XxxReport.tsx`의 imports, 훅 호출, JSX return을 그대로 이전
       - Props 타입: `type Props = { params: any; setParams: (p: any) => void }`
       - 훅 호출을 controlled mode로: `const { data, getData, loading, downloadExcel } = useXxxReport(params)`
       - `useEffect(() => { getData() }, [params])` 유지
       - 기존 자식 컴포넌트 (`<XxxReportTable>`, `<RangeDate>`, `<ProductFilterInput>`)에 `params`/`setParams` 그대로 전달
       - default export

    2. **`XxxReport.tsx` 를 thin wrapper로 교체**:
       ```typescript
       import { useState } from 'react'
       import XxxReportBody from './XxxReportBody'
       import { xxxDefaultParams } from './hooks/useXxxReport'

       const XxxReport = () => {
         const [params, setParams] = useState<any>(xxxDefaultParams)
         return <XxxReportBody params={params} setParams={setParams} />
       }
       export default XxxReport
       ```
       - default export 이름 유지 (기존 `pages/reportes/vendedor/index.tsx` 등 import 경로 불변)

    3. **ESLint**: return 위 빈 줄, 주석 위 빈 줄, 한국어 주석 ("// Phase 8: Body 분리 — 셸에서 embed 가능")

    4. **Variant C** (alertas, clientes-credito): 동일 패턴이나 `RangeDate` JSX 없음. paramsSchema에 date range 없으므로 Body는 filter input만 포함.

    5. **검증**: 각 페이지를 개발 서버에서 로드해 기존과 동일 동작 확인 (Wave 2 task 3에서 일괄).

    Reference canonical: vendedor 먼저 구현 후 패턴 검증, 나머지 11개 동일 복제.
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit</automated>
  </verify>
  <done>
    - 12개 XxxReportBody.tsx 파일 생성
    - 12개 XxxReport.tsx 가 thin wrapper로 교체
    - `npx tsc --noEmit` 에러 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract Body from 3 Variant A reports (sales/products/stocks) with helper props</name>
  <files>
    ventago-app/src/views/reports/sales/SalesReportBody.tsx,
    ventago-app/src/views/reports/sales/SalesReport.tsx,
    ventago-app/src/views/reports/products/ProductReportBody.tsx,
    ventago-app/src/views/reports/products/ProductReport.tsx,
    ventago-app/src/views/reports/stocks/StockReportBody.tsx,
    ventago-app/src/views/reports/stocks/StockReport.tsx
  </files>
  <action>
    Variant A 3개 보고서에 대해 Body 추출. Variant B/C와 같지만 **helper 함수 3개 + drilldown**을 wrapper가 소유하고 Body에 props로 전달:

    1. **`SalesReportBody.tsx` (products/stocks 동일 패턴)** 생성:
       ```typescript
       type Props = {
         params: any
         setParams: (p: any) => void
         setThisMonth?: () => void
         setToday?: () => void
         clear?: () => void
       }

       const SalesReportBody = ({ params, setParams, setThisMonth, setToday, clear }: Props) => {
         const { sales, getSales, loading, downloadExcel, getSaleItemsSummary } = useSalesReport(params)

         useEffect(() => { getSales() }, [params])

         return (
           <Grid container spacing={4}>
             {/* 기존 JSX 그대로, 헬퍼 버튼 onClick은 props 사용 */}
             {setThisMonth && <Button onClick={setThisMonth}>Este mes</Button>}
             {/* drilldown modal은 getSaleItemsSummary(overrideParams) 호출 */}
           </Grid>
         )
       }
       ```

    2. **`SalesReport.tsx` wrapper**:
       ```typescript
       const SalesReport = () => {
         const [params, setParams] = useState<any>(salesDefaultParams)

         const setThisMonth = useCallback(() => {
           setParams({
             ...params,
             startDate: DateTime.now().startOf('month').toFormat('yyyy-MM-dd'),
             endDate: DateTime.now().toFormat('yyyy-MM-dd')
           })
         }, [params])

         const setToday = useCallback(() => { /* ... */ }, [params])
         const clear = useCallback(() => setParams(salesDefaultParams()), [])

         return (
           <SalesReportBody
             params={params}
             setParams={setParams}
             setThisMonth={setThisMonth}
             setToday={setToday}
             clear={clear}
           />
         )
       }
       ```

    3. **drilldown (`getSaleItemsSummary` 등)**: Wave 1에서 override params 인자 수용하도록 이미 수정됨. Body에서 현재 `params` 또는 행별 특정 params로 호출 가능.

    4. **Phase 8 셸이 Body embed 시** helper props 생략 — 헬퍼 버튼은 조건부 렌더링 (`{setThisMonth && ...}`)으로 셸 preview에서는 표시 안 됨 (MVP 결정). 셸은 자체 Topbar에 "오늘/이번달" 액션을 별도 제공.

    5. **ESLint 준수**, 한국어 주석.
  </action>
  <verify>
    <automated>cd ventago-app && npm run build 2>&1 | tail -60</automated>
  </verify>
  <done>
    - 3개 XxxReportBody.tsx + 3개 wrapper 교체
    - `npm run build` 그린
    - 수동 확인: `/reportes/ventas`, `/reportes/items`, `/reportes/stocks` 에서 helper 버튼, drilldown modal 정상 동작
    - 수동 확인: 12개 Variant B/C 페이지 sample 4개 (`/reportes/vendedor`, `/reportes/gastos`, `/reportes/alertas`, `/reportes/clientes-credito`) 정상 동작
  </done>
</task>

</tasks>

<verification>
- `cd ventago-app && npm run build` → exit 0
- `ls ventago-app/src/views/reports/*/[A-Z]*Body.tsx | wc -l` → 15
- Manual click-through: 15개 `/reportes/*` 페이지 전부 로드 + 날짜 변경 + Excel 다운로드 1회씩
</verification>

<success_criteria>
- [ ] 15 XxxReportBody.tsx 파일 존재
- [ ] 15 XxxReport.tsx 가 useState + <Body /> 렌더링만 포함 (< 30 lines each)
- [ ] `pages/reportes/*` 와 `ReportesHub.tsx` 미수정 (git diff 확인)
- [ ] `npm run build` 그린
- [ ] 수동 회귀 테스트 15페이지 통과
</success_criteria>

<output>
After completion, create `.planning/phases/08-reportajes-ux/08-02-SUMMARY.md` listing:
- 15 Body 파일 경로 + props 시그니처
- Variant A 3개의 helper props 시그니처
- Wave 3에서 registry에 등록할 lazy import 경로 테이블
</output>
