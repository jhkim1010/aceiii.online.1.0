---
phase: 08-reportajes-ux
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - ventago-app/src/views/reports-v2/registry.ts
  - ventago-app/src/views/reports-v2/ReportsSidebar.tsx
  - ventago-app/src/views/reports-v2/ReportsTopbar.tsx
  - ventago-app/src/views/reports-v2/ReportsPreviewPanel.tsx
  - ventago-app/src/views/reports-v2/hooks/useFavorites.ts
autonomous: false
requirements: [UX-04]

must_haves:
  truths:
    - "16개 보고서 entry 전부 bodyComponent lazy import로 embed됨 (MVP 3개 + 신규 13개)"
    - "16개 보고서 모두 /reportes-v2 셸 내에서 params 변경/Excel 다운로드/데이터 조회 동작"
    - "검색창이 fuzzy/substring 필터링으로 title+description 대상으로 동작"
    - "localStorage 기반 favorites (⭐ 버튼 토글 → localStorage user_report_favorites_{userId})"
    - "Topbar의 Excel 버튼이 현재 embedded Body의 downloadExcel을 호출"
    - "최근 실행 5개 slug가 사이드바 상단에 'Recientes' 섹션으로 표시"
    - "기존 /reportes/* 페이지 zero regression"
  artifacts:
    - path: "ventago-app/src/views/reports-v2/hooks/useFavorites.ts"
      provides: "localStorage-backed favorites hook"
    - path: "ventago-app/src/views/reports-v2/registry.ts"
      provides: "16 entries with bodyComponent populated"
  key_links:
    - from: "registry.ts"
      to: "all 15 XxxReportBody.tsx (lazy)"
      via: "React.lazy dynamic import"
      pattern: "lazy\\(\\(\\) => import"
---

<objective>
Wave 3에서 구축한 셸 MVP를 완성형으로 확장한다. 13개 보고서에 bodyComponent lazy import를 추가하고, favorites/recents/search 필터 폴리싱을 수행한다.

**범위:**
- Registry 13개 entry에 bodyComponent 배선
- ReportsSidebar: 검색 필터 title+description 대상 확장, "Recientes" 섹션 (recentSlugs 기준 상위 5개)
- ReportsTopbar: Excel/PDF 버튼이 Body의 downloadExcel을 호출 (ref 또는 Redux action pattern)
- localStorage favorites 훅 + ⭐ 버튼 토글
- 성능 회귀 재점검 (React DevTools Profiler)

**Out of scope (deferred):**
- user_report_favorites DB 테이블 (Phase 8 후속 plan으로)
- PDF 다운로드 구현 (현재 Phase 6 훅에 없음 — 버튼만 노출, 누르면 alert)
- Alertas/Cheque Estado 백엔드 Wave 4 의존 (Phase 6에서 완료됨)

Purpose: 16개 보고서 전부 셸에서 사용 가능한 최종 상태.
Output: 완전 기능 `/reportes-v2` shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reportajes-ux/08-CONTEXT.md
@.planning/phases/08-reportajes-ux/08-RESEARCH.md
@.planning/phases/08-reportajes-ux/08-03-SUMMARY.md
@ventago-app/src/views/reports-v2/registry.ts
@ventago-app/src/views/reports-v2/ReportsSidebar.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Embed remaining 13 reports + Topbar Excel/PDF wire + favorites hook</name>
  <files>
    ventago-app/src/views/reports-v2/registry.ts,
    ventago-app/src/views/reports-v2/hooks/useFavorites.ts,
    ventago-app/src/views/reports-v2/ReportsPreviewPanel.tsx,
    ventago-app/src/views/reports-v2/ReportsTopbar.tsx
  </files>
  <action>
    1. **`registry.ts` 업데이트**: 13개 entry의 `bodyComponent`를 `lazy(() => import('src/views/reports/{slug}/XxxReportBody'))` 로 설정. 최종 16개 전부 bodyComponent 보유 (clientes-credito는 중복 카테고리지만 단일 entry이므로 1회만 lazy).

       대상 13 slug + path:
       - ventas → sales/SalesReportBody
       - items → products/ProductReportBody
       - clientes-credito → clientes-credito/ClientesCreditoReportBody
       - reservado → reservado/ReservadoReportBody
       - alertas → alertas/AlertasReportBody
       - facturacion → facturacion/FacturacionReportBody
       - cheque-estado → cheque-estado/ChequeEstadoReportBody
       - stocks → stocks/StockReportBody
       - corregido → corregido/CorregidoReportBody
       - movidos → movidos/MovidosReportBody
       - fallados → fallados/FalladosReportBody
       - ingreso → ingreso/IngresoReportBody
       - fallados (이미 나열)
       (총 13 — 기 embed된 vendedor/gastos/breve-venta 제외)

    2. **`hooks/useFavorites.ts`** (신규):
       - localStorage key: `ventago_report_favorites_{userId}` (useAuth로 userId 획득)
       - Return `{ favorites: string[], toggle(slug), isFavorite(slug) }`
       - SSR 안전: `typeof window !== 'undefined'` 체크

    3. **`ReportsTopbar.tsx`** 업데이트:
       - ⭐ 버튼: `useFavorites` 훅 연결, 토글 동작
       - Excel 버튼: selected slug의 Body가 소유한 downloadExcel 호출. **Pattern**: Redux에 `triggerExcelFor: string | null` 상태 추가, Topbar가 dispatch → PreviewPanel의 Body wrapper가 useEffect로 감지해 실행 후 clear. (또는 간단히 Body가 Topbar에 대한 ref 등록 — Redux 방식이 Pages Router에서 깔끔)
       - PDF 버튼: `alert('PDF export — próximamente')` placeholder
       - ▶ Ejecutar: Redux `refreshCounter++` dispatch → Body의 useEffect가 refreshCounter 의존성에 반응

    4. **`ReportsPreviewPanel.tsx`** 업데이트: Topbar 트리거(excel/refresh) 감지 로직 추가. Body wrapper로 감싸서 params 외에 `refreshCounter`, `excelTrigger` prop 전달하거나 Redux selector를 Body 호출 전에 읽어 effect 실행.

    5. **ESLint + 한국어 주석**.

    6. **수동 테스트**: 13개 신규 embed 전부 로드, params 변경, Excel 다운로드 동작.
  </action>
  <verify>
    <automated>cd ventago-app && npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 16개 entry 전부 bodyComponent 존재
    - useFavorites 훅 동작 (localStorage persist)
    - Topbar Excel/⭐/Ejecutar 배선 완료
    - `npm run build` 그린
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sidebar search polish + Recientes section + perf re-check</name>
  <files>
    ventago-app/src/views/reports-v2/ReportsSidebar.tsx
  </files>
  <action>
    1. **검색 필터 확장**: 기존 title 매치에 description도 포함 (`title + description`을 lower-case로 합친 뒤 substring match). 16개 항목이므로 debounce 불필요 (RESEARCH Don't Hand-Roll).

    2. **"Recientes" 섹션** (사이드바 최상단, 카테고리 그룹 위):
       - Redux `recentSlugs` 상위 5개 선택
       - 섹션 제목 "Recientes" + 항목 렌더링
       - 빈 경우 섹션 숨김
       - `useMemo`로 registry lookup 캐싱

    3. **"Favoritos" 섹션** (Recientes 아래, 카테고리 위):
       - `useFavorites().favorites` 기반
       - 빈 경우 숨김

    4. **Perf 재점검**:
       - `React.memo(ReportsSidebar)` 확인
       - SearchInput 서브컴포넌트 분리 (local useState + onBlur 또는 onChange dispatch)
       - 카테고리 그룹 리스트 `useMemo` 의존성: `[searchQuery, favorites, recentSlugs]`
       - darkTheme 상수화 (모듈 스코프)

    5. **ESLint, 한국어 주석**.
  </action>
  <verify>
    <automated>cd ventago-app && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 검색창이 title+description 매치
    - Recientes + Favoritos 섹션 표시
    - React DevTools Profiler에서 보고서 전환 시 Sidebar 리렌더 ≤ 1 (selectedSlug 하이라이트만)
    - `npm run build` 그린
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify full 16-report shell + regression check</name>
  <what-built>
    Phase 8 셸 최종 상태: 16개 보고서 전부 embed, favorites/recents/search 동작, Topbar Excel/Ejecutar 배선, localStorage 즐겨찾기.
  </what-built>
  <how-to-verify>
    1. `cd ventago-app && npm run dev`
    2. http://localhost:3000/reportes-v2 접근
    3. 16개 보고서 전부 클릭해보면서 Body embed 동작 확인 (variant A 3개의 helper 버튼은 Body에서 숨김 상태일 수 있음 — Topbar에 대체 버튼 없으면 정상)
    4. 각 보고서에서 params 변경 + Ejecutar 클릭 → 데이터 갱신 확인
    5. Topbar Excel 버튼 → 선택된 보고서의 Excel 다운로드 실행
    6. ⭐ 버튼 토글 → localStorage 저장, 사이드바 "Favoritos" 섹션에 나타남, 새로고침 후에도 유지
    7. 보고서 3~4개 전환 → 사이드바 "Recientes" 섹션 갱신
    8. 검색창 "stock"/"credito"/"fabric" 등 입력 → title+description 필터 확인
    9. React DevTools Profiler: 보고서 클릭 시 Sidebar 렌더 횟수 확인 (최소화됐는지)
    10. 기존 /reportes/vendedor, /reportes/ventas, /reportes/alertas, /reportes/clientes-credito 재방문 → zero regression
    11. UI 토글 OFF 상태에서 /reportes-v2 여전히 접근 가능 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exit 0
- 16 lazy imports in registry.ts
- 모든 success_criteria 체크
- 사용자 승인
</verification>

<success_criteria>
- [ ] 16개 entry에 bodyComponent 존재
- [ ] 16개 보고서 전부 셸 내 embed 동작
- [ ] localStorage favorites + Recientes 섹션 동작
- [ ] Topbar Excel/Ejecutar 배선
- [ ] Sidebar 리렌더 최적화 유지
- [ ] /reportes/* 기존 페이지 zero regression
- [ ] Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/08-reportajes-ux/08-04-SUMMARY.md` listing:
- 16 embedded reports + lazy import paths
- Topbar 배선 패턴 (Redux refreshCounter / excelTrigger)
- Phase 8 최종 상태 + 후속 plan 후보 (DB 즐겨찾기, PDF, Alertas 전용 preview 커스터마이즈)
</output>
