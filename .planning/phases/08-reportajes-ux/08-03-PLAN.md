---
phase: 08-reportajes-ux
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - ventago-app/src/store/apps/reports-v2/index.ts
  - ventago-app/src/store/index.ts
  - ventago-app/src/views/reports-v2/registry.ts
  - ventago-app/src/views/reports-v2/ReportsShell.tsx
  - ventago-app/src/views/reports-v2/ReportsSidebar.tsx
  - ventago-app/src/views/reports-v2/ReportsTopbar.tsx
  - ventago-app/src/views/reports-v2/ReportsParamsPanel.tsx
  - ventago-app/src/views/reports-v2/ReportsPreviewPanel.tsx
  - ventago-app/src/pages/reportes-v2/[[...slug]].tsx
autonomous: false
requirements: [UX-04]

must_haves:
  truths:
    - "`/reportes-v2` 라우트 진입 시 16-entry 사이드바 + 우측 params/preview 표시"
    - "사이드바 보고서 클릭 시 shallow routing으로 URL만 갱신, 우측 content만 재렌더"
    - "검색창 입력 시 보고서 목록 필터 (16개 → filter(title))"
    - "MVP 3개 보고서 (vendedor, gastos, breve-venta) Body가 preview panel에 lazy load되어 정상 동작"
    - "나머지 13개 entry는 'Próximamente — ver versión clásica' placeholder + legacyHref 링크 표시"
    - "UI 토글과 무관하게 `/reportes-v2` 접근 가능 (토글 독립)"
    - "사이드바 리렌더 Phase 1 교훈 적용 — React.memo + useMemo, 검색 input은 local state로 부모 리렌더 차단"
    - "`reportsV2Slice`가 store에 등록되고 selectedSlug/paramsBySlug/recentSlugs 관리"
  artifacts:
    - path: "ventago-app/src/pages/reportes-v2/[[...slug]].tsx"
      provides: "Catch-all route for /reportes-v2 and /reportes-v2/:slug"
    - path: "ventago-app/src/views/reports-v2/registry.ts"
      provides: "16-report static registry with lazy bodyComponent"
      contains: "REPORTS_REGISTRY"
    - path: "ventago-app/src/store/apps/reports-v2/index.ts"
      provides: "Redux slice (selectedSlug, paramsBySlug, recentSlugs, searchQuery)"
  key_links:
    - from: "ReportsSidebar"
      to: "router.push('/reportes-v2/:slug', { shallow: true })"
      via: "handleSelect callback"
      pattern: "shallow:\\s*true"
    - from: "ReportsPreviewPanel"
      to: "registry[slug].bodyComponent (lazy)"
      via: "React.lazy + Suspense"
      pattern: "Suspense"
    - from: "store/index.ts"
      to: "reports-v2/index.ts reducer"
      via: "configureStore reducer registration"
      pattern: "reportsV2"
---

<objective>
Phase 8 셸 MVP 구축. `/reportes-v2` 라우트에 좌측 다크 사이드바 + 우측 params/preview 레이아웃을 구현한다. 16-entry registry, reportsV2Slice, 5개 셸 컴포넌트, catch-all 라우트를 생성하고, MVP 3개 보고서(vendedor, gastos, breve-venta)를 embed 상태로 동작시킨다.

나머지 13개 entry는 사이드바에는 표시되나 preview는 "준비 중 — 기존 버전 보기" placeholder와 `legacyHref` 링크를 제공한다 (Wave 4에서 전부 embed).

**CONTEXT.md locked decisions:**
- D-02 `/reportes-v2` 토글 독립 라우트
- D-05 Redux slice 위치: `src/store/apps/reports-v2/index.ts`
- D-06 단일 catch-all `[[...slug]].tsx`
- D-07 Creditos/Clientes Crédito 중복: registry에 `categories: ReportCategory[]` (배열) 허용 → 동일 entry가 Ventas와 Clientes 두 카테고리에 표시

Purpose: 셸 골격을 완성해 Wave 4에서 나머지 13개 embed만 추가하면 되게 함.
Output: MVP 동작 가능한 /reportes-v2 진입점.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reportajes-ux/08-CONTEXT.md
@.planning/phases/08-reportajes-ux/08-RESEARCH.md
@.planning/phases/08-reportajes-ux/08-02-SUMMARY.md
@ventago-app/src/store/index.ts
@ventago-app/src/store/apps/user/index.ts
@ventago-app/src/@core/layouts/VerticalLayout.tsx
@ventago-app/src/views/reports/hub/ReportesHub.tsx

<interfaces>
<!-- Registry shape -->

```typescript
// views/reports-v2/registry.ts
import { ComponentType, lazy } from 'react'

export type ReportCategory = 'ventas' | 'finanzas' | 'inventario' | 'clientes'

export interface ReportParamsSchema {
  hasDateRange: boolean
  hasFilter: boolean
  hasIsParent?: boolean
}

export interface ReportEntry {
  slug: string
  title: string
  categories: ReportCategory[]   // array — enables Creditos in both Ventas + Clientes
  icon: string                   // iconify name
  description: string
  paramsSchema: ReportParamsSchema
  defaultParams: () => Record<string, any>
  bodyComponent?: ComponentType<{ params: any; setParams: (p: any) => void }>
  legacyHref: string
}
```

<!-- Redux slice -->
```typescript
// store/apps/reports-v2/index.ts
interface ReportsV2State {
  selectedSlug: string | null
  paramsBySlug: Record<string, any>
  recentSlugs: string[]
  searchQuery: string
}
```

<!-- Shell layout -->
```typescript
// views/reports-v2/ReportsShell.tsx
<Box sx={{ display: 'grid', gridTemplateColumns: '300px 1fr', height: 'calc(100vh - 64px)', minHeight: 0 }}>
  <ReportsSidebar />
  <Box sx={{ display: 'flex', flexDirection: 'column', minHeight: 0, overflow: 'auto' }}>
    <ReportsTopbar />
    <ReportsParamsPanel />
    <ReportsPreviewPanel />
  </Box>
</Box>
```

<!-- Catch-all route -->
```typescript
// pages/reportes-v2/[[...slug]].tsx
// NO getServerSideProps / getInitialProps — shallow routing requires pure client
```

16-report categories (Ventas 7 / Finanzas 3 / Inventario 5 / Clientes 1, with clientes-credito in both Ventas+Clientes):
Ventas: ventas, items, vendedor, clientes-credito, breve-venta, reservado, alertas
Finanzas: facturacion, gastos, cheque-estado
Inventario: stocks, corregido, movidos, fallados, ingreso
Clientes: clientes-credito (duplicate categories array)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Registry + Redux slice + store wiring</name>
  <files>
    ventago-app/src/views/reports-v2/registry.ts,
    ventago-app/src/store/apps/reports-v2/index.ts,
    ventago-app/src/store/index.ts
  </files>
  <action>
    1. **`registry.ts` 생성**:
       - 위 interface 정의 export (ReportCategory, ReportParamsSchema, ReportEntry)
       - `REPORTS_REGISTRY: ReportEntry[]` 에 16개 entry (그 중 clientes-credito는 `categories: ['ventas', 'clientes']` 로 단일 entry 중복 카테고리)
       - 각 entry의 `defaultParams`는 Wave 1에서 만든 `xxxDefaultParams` 함수를 re-export 또는 직접 import
       - MVP 3개 (vendedor, gastos, breve-venta)만 `bodyComponent: lazy(() => import('src/views/reports/{slug}/XxxReportBody'))` 설정
       - 나머지 13개는 `bodyComponent: undefined` (Wave 4에서 채움)
       - `legacyHref`: `/reportes/{slug}` (RESEARCH.md 16-Report Mapping 테이블 참조)
       - 아이콘은 iconify 이름 (ReportesHub.tsx 참고해서 재사용: `mdi:sale-outline`, `mdi:account-cash`, `mdi:package-variant`, etc.)
       - paramsSchema: 15 변종 (Variant A 3개는 `hasIsParent: true`, Variant C 2개는 `hasDateRange: false`)

    2. **`store/apps/reports-v2/index.ts`** — `createSlice`:
       - initialState: `{ selectedSlug: null, paramsBySlug: {}, recentSlugs: [], searchQuery: '' }`
       - reducers:
         - `selectReport(state, action: PayloadAction<string>)` — set selectedSlug + push to recentSlugs (max 5, dedup)
         - `setParamsForSlug(state, action: PayloadAction<{ slug: string; params: any }>)`
         - `setSearchQuery(state, action: PayloadAction<string>)`
       - default export reducer, named export actions

    3. **`store/index.ts`** — `configureStore` reducer에 `reportsV2: reportsV2Reducer` 추가. user 등 기존 reducer는 그대로 유지.

    4. **한국어 주석**, ESLint 준수 (newline-before-return 특히 reducer 함수들).
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit</automated>
  </verify>
  <done>
    - REPORTS_REGISTRY에 16개 entry (clientes-credito는 categories=['ventas','clientes'])
    - MVP 3개 entry에 bodyComponent lazy import 설정
    - reportsV2 reducer가 store에 등록됨
    - tsc 통과
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Shell components (Shell/Sidebar/Topbar/ParamsPanel/PreviewPanel) + catch-all route</name>
  <files>
    ventago-app/src/views/reports-v2/ReportsShell.tsx,
    ventago-app/src/views/reports-v2/ReportsSidebar.tsx,
    ventago-app/src/views/reports-v2/ReportsTopbar.tsx,
    ventago-app/src/views/reports-v2/ReportsParamsPanel.tsx,
    ventago-app/src/views/reports-v2/ReportsPreviewPanel.tsx,
    ventago-app/src/pages/reportes-v2/[[...slug]].tsx
  </files>
  <action>
    1. **`ReportsShell.tsx`** (`{ slug: string }` prop):
       - Grid 300px/1fr, height `calc(100vh - 64px)`, minHeight 0
       - useEffect: slug 변경 시 `dispatch(selectReport(slug))`
       - 렌더 `<ReportsSidebar selectedSlug={slug} />` + 오른쪽 column `<Topbar /><ParamsPanel /><PreviewPanel />`

    2. **`ReportsSidebar.tsx`** — `React.memo` 필수:
       - Props: `{ selectedSlug: string }`
       - MUI nested `ThemeProvider` (dark mode) 또는 `bgcolor: 'grey.900', color: 'grey.100'`
       - 상단: `<SearchInput />` 자체 컴포넌트 (local useState, onChange로 Redux `setSearchQuery` dispatch) — 이 분리로 부모 리렌더 차단
       - 카테고리 그룹 4개: Ventas/Finanzas/Inventario/Clientes&Control
       - `useMemo`로 registry 필터링: `REPORTS_REGISTRY.filter(r => r.title.toLowerCase().includes(query) && r.categories.includes(cat))`
       - 각 항목 클릭 시 `handleSelect(slug)` — `router.push('/reportes-v2/' + slug, undefined, { shallow: true })` + `dispatch(selectReport(slug))`
       - active 항목 하이라이트 (selectedSlug 기준)
       - **Phase 1 교훈**: `React.memo`, `useMemo` on category groups, darkTheme은 모듈 레벨 상수 또는 `useMemo([])`

    3. **`ReportsTopbar.tsx`**:
       - breadcrumb `Reportes > {category title} > {report title}`
       - 우측 액션: ⭐ (favorite — localStorage), PDF, Excel, ▶ Ejecutar
       - Ejecutar 클릭 → selected slug의 훅 `getData()` 재실행 트리거 (Redux action `refreshCounter++` 또는 ref 콜백)
       - Excel 클릭 → preview Body의 downloadExcel 위임 (Topbar에서 Body로 ref 전달 또는 단순히 Body 내부에 Excel 버튼 유지하고 Topbar Excel은 Wave 4에서 배선)

    4. **`ReportsParamsPanel.tsx`**:
       - 현재 selected registry entry의 `paramsSchema` 기반 동적 폼
       - `hasDateRange` → `<RangeDate>` (기존 컴포넌트 재사용)
       - `hasFilter` → `<ProductFilterInput>` 또는 단순 `<TextField>` (Phase 6 컴포넌트 재사용)
       - `hasIsParent` → Checkbox
       - params 상태는 Redux `paramsBySlug[slug]` 에서 읽고 변경 시 `setParamsForSlug` dispatch
       - 초기값: `registry[slug].defaultParams()` (paramsBySlug에 없을 때)

    5. **`ReportsPreviewPanel.tsx`**:
       - selected entry의 `bodyComponent` 확인
       - 있으면: `<Suspense fallback={<Skeleton />}><BodyComponent params={params} setParams={setParams} /></Suspense>`
       - 없으면: "Próximamente — ver versión clásica" placeholder + `<Button href={entry.legacyHref}>` 링크
       - params/setParams는 Redux 경유

    6. **`pages/reportes-v2/[[...slug]].tsx`**:
       - **getInitialProps / getServerSideProps 정의 금지** (shallow routing 깨짐 — RESEARCH Pitfall 5)
       - `const router = useRouter(); const slug = (router.query.slug?.[0]) ?? 'vendedor'` (default MVP)
       - `return <ReportsShell slug={slug} />`
       - UserLayout wrapping을 위해 `Page.getLayout` 패턴 (기존 페이지 참고)

    7. **ESLint 철저** — 15개 newline-before-return 위반 예상. 한국어 주석으로 각 section 설명.

    8. **빌드 + 수동 확인**: `/reportes-v2` 접근 → 사이드바 16개, vendedor 기본 선택, Body embed, 다른 보고서 클릭 시 shallow routing (DevTools Network 탭에서 page request 0), gastos/breve-venta 전환 확인.
  </action>
  <verify>
    <automated>cd ventago-app && npm run build 2>&1 | tail -60</automated>
  </verify>
  <done>
    - `/reportes-v2` 접근 시 16-entry 사이드바 + Shell 표시
    - MVP 3개 (vendedor/gastos/breve-venta) Body가 preview에 embed 동작
    - 나머지 13개는 placeholder + legacyHref 링크
    - 사이드바 클릭 shallow routing 동작 (Network 탭 full reload 없음)
    - `npm run build` 그린
    - 기존 `/reportes/*` 페이지 회귀 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify MVP shell (3 embedded reports + shallow routing + sidebar render perf)</name>
  <what-built>
    Phase 8 셸 MVP: /reportes-v2 라우트, 16-entry 다크 사이드바, params panel, 3개 embedded preview (vendedor/gastos/breve-venta), 13개 placeholder.
  </what-built>
  <how-to-verify>
    1. 개발 서버 실행: `cd ventago-app && npm run dev`
    2. 브라우저에서 http://localhost:3000/reportes-v2 접근
    3. 사이드바에 16개 보고서가 4개 카테고리로 표시되는지 확인 (Ventas 7, Finanzas 3, Inventario 5, Clientes 1 — 단 clientes-credito는 Ventas와 Clientes 양쪽 표시)
    4. 검색창에 "stock" 입력 → 관련 보고서만 필터
    5. vendedor 클릭 → URL이 /reportes-v2/vendedor로 변경, Body 렌더링, DevTools Network 탭에서 full page request 없음 (shallow 확인)
    6. gastos, breve-venta 순서대로 클릭 → 각 Body가 정상 embed되고 날짜 변경/Excel 다운로드 동작
    7. facturacion 등 placeholder 13개 클릭 → "Próximamente" + "Ver versión clásica" 링크 표시
    8. React DevTools Profiler로 다른 보고서 클릭 시 Sidebar가 리렌더되지 않는지 확인 (selectedSlug highlight만 갱신되면 OK, 전체 목록 리렌더 시 최적화 부족)
    9. UI 토글 OFF 상태에서도 /reportes-v2 접근 가능 확인 (토글 독립)
    10. 기존 /reportes/vendedor, /reportes/ventas, /reportes/alertas 접근 → 회귀 없음 확인
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exit 0
- Shell 5 components + registry + slice + route 파일 모두 존재
- 사용자 승인 획득
</verification>

<success_criteria>
- [ ] REPORTS_REGISTRY 16 entries with correct categories (clientes-credito dual)
- [ ] reportsV2 slice registered in store
- [ ] /reportes-v2 route operational
- [ ] MVP 3 reports embedded and functional
- [ ] Shallow routing verified
- [ ] Sidebar render optimization verified (React DevTools)
- [ ] Zero regression on /reportes/*
- [ ] Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/08-reportajes-ux/08-03-SUMMARY.md` listing:
- Registry 16 entry 테이블 (slug, categories, paramsSchema, MVP embed 상태)
- Shell component 트리 + prop flow
- Wave 4에 남은 작업: 13개 entry에 bodyComponent 추가 + search debounce + favorites + Topbar Excel wire
</output>
