---
phase: 64-transaction-safety-concurrency-integrity
plan: 10
type: execute
wave: 10
depends_on: ["64-01", "64-02", "64-03", "64-04", "64-05", "64-06", "64-07", "64-08", "64-09"]
files_modified:
  - .planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md
  - .planning/phases/64-transaction-safety-concurrency-integrity/64-UAT.md
  - .planning/intel/db-schema-tables.md
  - .planning/intel/db-schema-fks.md
  - CLAUDE.md
autonomous: false
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12]
must_haves:
  truths:
    - "R1~R12 전 항목의 Acceptance 증거가 64-VALIDATION.md 에 기록됐다"
    - "마이그레이션 3종이 로컬 5432 와 운영 5434 양쪽에 적용되고 스키마가 대조됐다"
    - "grep 게이트 3종이 0건이다"
    - "브라우저 UAT 로 소매 판매/취소/보류/생산/재고 무회귀가 확인됐다"
    - "CLAUDE.md 에 쓰기 경로 3규약이 추가됐다"
  artifacts:
    - path: ".planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md"
      provides: "전 요구사항 검증 증거 모음"
      contains: "R12"
  key_links:
    - from: "64-VALIDATION.md"
      to: "각 Wave 의 SUMMARY"
      via: "Acceptance 근거 링크 — 주장 아닌 출력 원문"
      pattern: "SUMMARY"
---

<objective>
Phase 64 를 닫는다. 전 요구사항의 Acceptance 를 실제 출력으로 증명하고, 마이그레이션을 양쪽 DB 에 정합하게 반영하고, 이번에 세운 규약을 CLAUDE.md 에 남긴다.

Purpose: 검증되지 않은 "완료" 방지. Phase 64 는 돈·재고 경로를 건드렸으므로 증거 없는 종결은 허용되지 않는다.
Output: 64-VALIDATION.md + 64-UAT.md + intel 재생성 + CLAUDE.md 규약 + 배포 후 관측 항목.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-transaction-safety-concurrency-integrity/64-SPEC.md
@.planning/phases/64-transaction-safety-concurrency-integrity/64-CONTEXT.md
@CLAUDE.md

<interfaces>
Phase 64 마이그레이션 3종(각 Plan 산출물):
1. `api-ventago/migrations/2026-07-27-phase64-sale-idempotency-keys.sql` (64-01) — 신규 테이블, owner 이전 필요
2. `api-ventago/migrations/2026-07-27-phase64-outbox-lease.sql` (64-05) — ALTER + in-flight 회수
3. `api-ventago/migrations/2026-07-27-phase64-outbox-lease-index.sql` (64-05) — CONCURRENTLY, `--single-transaction` 금지

intel 재생성: `./.planning/intel/db-schema.regen.sh` (로컬 PG18 ventago → `db-schema-tables.md` / `db-schema-fks.md`)

배포: push → Jenkins webhook 자동 빌드(`api-new-coolsistema` / `front-coolsistema`). ★`.env` 는 컨테이너 생성 시에만 주입되므로 새 환경변수 추가 시 `--force-recreate` 필요 — 본 Phase 는 신규 필수 env 없음(`SHOP_DB_*` 는 선택).

관측 재사용: `app/diagnostics/*`(느린 쿼리·pool·outbox), `database.module.ts:184`–`:201`(pool 80% 경고).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] 마이그레이션 3종 최종 적용 + 스키마 대조</name>
  <read_first>
    - 64-01-SUMMARY.md / 64-05-SUMMARY.md (각 Plan 에서 이미 적용했는지 확인)
    - CLAUDE.md 「DB 마이그레이션 적용 규칙」
  </read_first>
  <what-built>
    Phase 64 의 DDL 은 3개 파일이다 — 멱등키 테이블(신규), outbox lease 컬럼(ALTER + 1회성 회수), outbox 인덱스(CONCURRENTLY).
  </what-built>
  <how-to-verify>
    각 Plan 의 체크포인트에서 이미 적용했다면 **여기서는 최종 대조만** 한다. 미적용분이 있으면 승인 후 적용한다.

    로컬 5432 · 운영 5434 양쪽에서:
    ```sql
    -- 1) 멱등키 테이블
    SELECT column_name, data_type, is_nullable
      FROM information_schema.columns
     WHERE table_name = 'sale_idempotency_keys' ORDER BY ordinal_position;
    SELECT tableowner FROM pg_tables WHERE tablename = 'sale_idempotency_keys';

    -- 2) outbox lease 컬럼
    SELECT column_name, data_type
      FROM information_schema.columns
     WHERE table_name = 'sync_outbox' AND column_name IN ('locked_by','lease_expires_at');

    -- 3) 인덱스
    SELECT indexname FROM pg_indexes
     WHERE tablename = 'sync_outbox' AND indexname LIKE 'idx_sync_outbox_%';

    -- 4) 잔여 processing 0 확인
    SELECT status, count(*) FROM sync_outbox GROUP BY status;
    ```
    두 환경 출력이 **문자 단위로 동일**해야 한다(owner 는 로컬에 role 없으면 예외 — 사실을 기록).
  </how-to-verify>
  <acceptance_criteria>
    - 3종 전부 양쪽 적용 확인
    - `sale_idempotency_keys` owner == coolsistema (운영), 시퀀스 owner 동일
    - `sync_outbox` 의 두 컬럼 + 인덱스 2종 양쪽 존재
    - `processing` 잔여 0
    - 출력 원문이 64-VALIDATION.md 에 첨부됨
  </acceptance_criteria>
  <resume-signal>양쪽 대조 출력을 붙여넣어 주세요</resume-signal>
</task>

<task type="auto">
  <name>Task 2: grep 게이트 + 자동 검증 실행</name>
  <files>.planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md</files>
  <read_first>
    - 64-SPEC.md 「완료 기준」
    - 64-01 ~ 64-09 의 각 SUMMARY
  </read_first>
  <action>
아래를 실행하고 **출력 원문**을 `64-VALIDATION.md` 에 기록한다. 요약이 아니라 명령 + 결과를 그대로 남긴다.

```bash
# ── 게이트 1: stocks 대상 productId 참조 0건 ──
grep -rn "stockModel\.\(findOne\|findAll\|create\|update\)" -A3 \
  api-ventago/src/app/production api-ventago/src/app/stocks | grep -c "productId"      # 0

# ── 게이트 2: 원장 UPDATE/DELETE 경로 0건 ──
grep -rn "\.destroy()" api-ventago/src/app/stocks/stocks.service.ts                     # 0건
grep -rn "stock\.update(" api-ventago/src/app/stocks/stocks.service.ts                  # 0건

# ── 게이트 3: 트랜잭션 안 외부 I/O 0건 ──
# 판매 트랜잭션 블록 안에 http/axios/fetch/socket emit 이 없는지
awk '/const sale = await this.sequelize.transaction/,/^    \}\);/' \
  api-ventago/src/app/sales/sales-create.service.ts | grep -ciE "axios|fetch\(|http|emit\(" # 0

# ── 빌드/테스트 ──
cd api-ventago && npx tsc --noEmit -p tsconfig.json
npx jest --silent
npx eslint src/
npm run test:concurrency          # 사용자 Mac 에서 (실 DB 필요)
cd ../ventago-app && npx tsc --noEmit && npx eslint src/
```
⚠ `timeout` 명령을 앞에 붙이지 않는다 — 이 Mac 에는 없어서 exit 127 로 아무것도 실행되지 않는다.

`64-VALIDATION.md` 구조:
```markdown
# Phase 64 Validation

**Date:** <실행일>
**Environment:** 로컬 PG18 5432 / 운영 PG18 5434

## 마이그레이션 대조
<Task 1 출력 원문>

## grep 게이트
<명령 + 출력>

## 빌드/테스트
<tsc/jest/eslint/test:concurrency 결과>

## 요구사항별 Acceptance
| R | 요구사항 | 증거 | 상태 |
|---|---------|------|------|
| R1 | 판매 멱등 + 커밋 후 500 제거 | 64-01-SUMMARY, sale-idempotency.conc-spec | PASS/FAIL |
| ... | ... | ... | ... |
| R12 | 공개몰 pool 예산 | 기동 로그 라인, 부하 결과 | PASS/FAIL |
```
FAIL 이 하나라도 있으면 해당 Wave Plan 으로 되돌아간다 — **부분 통과로 Phase 를 닫지 않는다.**
  </action>
  <verify>
    <automated>test -f .planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md && grep -c "R12" .planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 게이트 3종 전부 0건
    - tsc/jest/eslint (api + app) exit 0
    - `test:concurrency` 전 케이스 통과
    - R1~R12 표가 전부 채워지고 FAIL 0
  </acceptance_criteria>
  <done>자동 검증 증거가 문서화됐다.</done>
</task>

<task type="auto">
  <name>Task 3: intel 재생성</name>
  <files>.planning/intel/db-schema-tables.md, .planning/intel/db-schema-fks.md</files>
  <read_first>
    - .planning/intel/db-schema.regen.sh (실행 방법)
  </read_first>
  <action>
스키마가 바뀌었으므로(신규 테이블 1 + 컬럼 2 + 인덱스 2) reference 를 갱신한다:
```bash
./.planning/intel/db-schema.regen.sh
```
이 샌드박스에서 로컬 PG 에 닿지 않으면 사용자 Mac 실행으로 넘기고, 결과 diff 를 커밋한다.
갱신 후 `sale_idempotency_keys` 와 `sync_outbox` 의 신규 컬럼이 파일에 나타나는지 확인한다.
  </action>
  <verify>
    <automated>grep -c "sale_idempotency_keys" .planning/intel/db-schema-tables.md</automated>
  </verify>
  <acceptance_criteria>
    - `db-schema-tables.md` 에 `sale_idempotency_keys` 존재
    - `sync_outbox` 섹션에 `locked_by` / `lease_expires_at` 존재
    - `db-schema-fks.md` 에 신규 FK(store_id, sale_id) 반영
  </acceptance_criteria>
  <done>스키마 reference 가 현행과 일치한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 브라우저 UAT (소매 무회귀 중심)</name>
  <files>.planning/phases/64-transaction-safety-concurrency-integrity/64-UAT.md</files>
  <what-built>
    Phase 64 전 Wave 가 판매·취소·보류·생산·재고·outbox·오프라인 경로를 건드렸다. 자동 테스트는 통과했고, 이제 실제 화면에서 소매(의류) 업무가 그대로인지 확인한다.
  </what-built>
  <how-to-verify>
    로컬 dev(`./dev.sh`)에서 아래 17항목을 수행하고 결과를 `64-UAT.md` 에 기록한다(64-SPEC.md 회귀 표 RG-1~RG-17 과 1:1).

    | # | 시나리오 | 확인 |
    |---|---------|------|
    | RG-1 | POS 일반 판매(현금/카드/분할) | 정상 완료, 영수증 출력 |
    | RG-2 | 멱등키 미전송 경로(구버전 시뮬) | 정상 판매 |
    | RG-3 | APK/에이전트 요청 | 정상 |
    | RG-4 | 프로모션 포함 취소 | 재고 1회 복원, 로그 존재 |
    | RG-5 | MP 결제 판매 취소 | 환불 실패해도 취소 유지 |
    | RG-6 | 보류 생성→수정→확정 | 아이템 보존, 예약 재고 합 0 |
    | RG-7 | WP 웹훅 보류 판매 | 무회귀 |
    | RG-8 | BOM 생산 완료 | 원자재 -, 완제품 + (신규 동작) |
    | RG-9 | outbox on/off | 두 경로 무회귀 |
    | RG-10 | 오프라인 배치 부분 실패 | 나머지 정상 처리 |
    | RG-11 | generic 상품 판매 | 정상 |
    | RG-12 | 레거시 데이터 판매 | 매장 검증 통과 |
    | RG-13 | 재고 허용 매장 판매 | 음수 재고 허용(현행) |
    | RG-14 | 재고 비허용 매장 판매 | 초과 판매 차단 |
    | RG-15 | 오늘 판매 번호 | 연속 증가 |
    | RG-16 | 식당/배달/온라인 경로 | 무회귀 |
    | RG-17 | pool 사용률 | 경고 없음, 응답 지연 현행 이하 |

    각 항목에 PASS/FAIL + 근거(스크린샷 또는 쿼리 출력)를 남긴다.
  </how-to-verify>
  <acceptance_criteria>
    - 17항목 전부 PASS (FAIL 시 해당 Wave 로 회귀)
    - RG-13/RG-14 가 설정 두 갈래를 모두 덮음
    - 64-UAT.md 에 항목별 근거 기록
  </acceptance_criteria>
  <resume-signal>UAT 결과표를 붙여넣어 주세요. FAIL 항목이 있으면 재현 절차도 함께 주세요</resume-signal>
</task>

<task type="auto">
  <name>Task 5: CLAUDE.md 규약 추가 + 배포 후 관측 항목 정의</name>
  <files>CLAUDE.md, .planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md 「주의 사항」 / 「백엔드 규약」 절 (삽입 위치)
  </read_first>
  <action>
**A. CLAUDE.md 규약 3줄 추가** (「백엔드 규약」 하위):
```markdown
### 쓰기 경로 규약 (Phase 64)
- **단일 트랜잭션 원칙**: 하나의 업무 동작(판매·취소·보류·생산 완료)이 만드는 모든 행은 하나의 트랜잭션에서 커밋한다. 여러 모델을 순차 호출하면서 `transaction` 인자를 빠뜨리면 그 문장만 별도 커넥션에서 커밋돼 부분 저장이 된다.
- **`stocks` 는 append-only 원장**: 행을 UPDATE/DELETE 하지 않는다. 잘못된 이동은 반대 부호 보정 행으로 상쇄하고 `products.stock` 캐시를 같은 트랜잭션에서 맞춘다. 조회/기록은 항상 `product_branch_id` 기준 (`product_id` 컬럼은 존재하지 않는다).
- **트랜잭션 안 외부 I/O 금지**: HTTP·프린터·소켓 호출은 커밋 후에 한다. 반드시 일어나야 하는 후속 작업은 같은 트랜잭션에서 `sync_outbox` 에 INSERT 하고 워커가 집행한다.
```

**B. 배포 후 관측 항목** — `64-VALIDATION.md` 말미에 추가:
```markdown
## 배포 후 관측 (2주)
| 지표 | 조회 | 기대 |
|---|---|---|
| 판매 500율 | API 로그 `POST /sales` 5xx 비율 | 배포 전 이하 |
| 멱등 재생 건수 | `SELECT count(*) FROM sale_idempotency_keys WHERE status='completed'` 대비 재요청 로그 | 재시도가 판매를 만들지 않음 |
| outbox failed 증가율 | `SELECT status, count(*) FROM sync_outbox GROUP BY status` | 급증 없음 |
| outbox 정체 | `SELECT count(*) FROM sync_outbox WHERE status='processing' AND lease_expires_at < NOW()` | 지속 0 |
| 음수 재고(비허용 매장) | `SELECT p.id FROM products p JOIN store_configs c ON c.store_id=p.store_id WHERE c.allow_sale_without_stock=false AND p.stock<0` | 0건 |
| 번호 중복 | `GROUP BY store_id, sale_day_local, daily_number HAVING count(*)>1` | 0행 |
| pool 경고 | API 로그 `pool` 80% 경고 | 배포 전 이하 |
```

**C. 배포** — `push-both.sh` 또는 개별 push 로 3 repo 반영. Jenkins webhook 이 자동 빌드한다. 신규 필수 env 없음(`SHOP_DB_*` 는 선택) → `--force-recreate` 불필요. 이 사실을 SUMMARY 에 명시한다.

**D. ROADMAP/STATE 갱신** — Phase 64 진행표 행을 `10/10 Complete` 로, STATE 의 현재 위치를 갱신한다.
  </action>
  <verify>
    <automated>grep -c "쓰기 경로 규약" CLAUDE.md && grep -c "배포 후 관측" .planning/phases/64-transaction-safety-concurrency-integrity/64-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md 에 규약 3줄 존재
    - 64-VALIDATION.md 에 관측 표 7행 존재
    - ROADMAP 진행표/STATE 갱신
  </acceptance_criteria>
  <done>규약이 남고 관측 항목이 정의됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영 DB 최종 적용 | 되돌리기 비용이 큰 상태 변경 |
| "완료" 선언 | 증거 없는 종결은 운영 장애로 되돌아온다 |
| 배포 | 여러 Wave 가 동시에 운영에 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-43 | Repudiation | 검증 없이 Phase 종결 | mitigate | R1~R12 표에 증거 링크 강제, FAIL 0 이어야 종결 |
| T-64-44 | DoS | 한쪽 DB 만 적용돼 배포 후 500 | mitigate | Task 1 양쪽 대조를 blocking 체크포인트로 |
| T-64-45 | DoS | 다수 Wave 동시 배포로 원인 격리 곤란 | mitigate | 배포 후 관측 표 + Wave 별 커밋 분리(되돌리기 단위 확보) |
| T-64-46 | Tampering | intel 문서와 실제 스키마 괴리 | mitigate | regen 스크립트 실행 + 커밋 |
</threat_model>

<verification>
```bash
# 게이트 3종 (0 이어야 함)
grep -rn "stockModel\.\(findOne\|findAll\|create\|update\)" -A3 api-ventago/src/app/production api-ventago/src/app/stocks | grep -c "productId"
grep -rn "\.destroy()\|stock\.update(" api-ventago/src/app/stocks/stocks.service.ts | wc -l
awk '/const sale = await this.sequelize.transaction/,/^    \}\);/' api-ventago/src/app/sales/sales-create.service.ts | grep -ciE "axios|fetch\(|http|emit\("
# 빌드/테스트 (timeout 명령 사용 금지 — 이 Mac 에 없음)
cd api-ventago && npx tsc --noEmit -p tsconfig.json && npx jest --silent && npx eslint src/
cd ../ventago-app && npx tsc --noEmit && npx eslint src/
```
</verification>

<success_criteria>
- 마이그레이션 3종 양쪽 적용·대조 완료
- grep 게이트 3종 0건
- api/app 빌드·유닛·동시성 테스트 전부 통과
- 브라우저 UAT 17항목 PASS
- CLAUDE.md 규약 + 배포 후 관측 표 기록
- ROADMAP/STATE 갱신
</success_criteria>

<output>
완료 후 `.planning/phases/64-transaction-safety-concurrency-integrity/64-10-SUMMARY.md` 생성 — 64-VALIDATION.md / 64-UAT.md 링크와 최종 판정 포함
</output>
