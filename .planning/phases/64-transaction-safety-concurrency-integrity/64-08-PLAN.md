---
phase: 64-transaction-safety-concurrency-integrity
plan: 08
type: execute
wave: 8
depends_on: []
files_modified:
  - api-ventago/src/app/sales/sales-create.service.ts
  - api-ventago/src/app/sales/sales-stock-decrement.spec.ts
  - api-ventago/src/app/sales/sales-daily-number.spec.ts
  - api-ventago/src/app/shop-public/shop-readonly-db.service.ts
  - api-ventago/src/database/database.module.ts
  - api-ventago/src/app/diagnostics/diagnostics.service.ts
autonomous: false
requirements: [R10, R11, R12]
must_haves:
  truths:
    - "allowSaleWithoutStock=false 매장은 재고 초과 판매가 실제로 차단된다(동시 판매 포함)"
    - "allowSaleWithoutStock=true 매장은 현행 그대로 — 음수 재고 허용, 락/검사 추가 없음"
    - "백데이트 판매의 sale_day_local 과 daily_number 가 대상 영업일 기준으로 채번된다"
    - "기동 로그에 총 커넥션 예산 산식이 출력된다"
    - "공개몰 pool 이 메인 DB 로 폴백하면 경고 + 상한 축소가 일어난다"
  artifacts:
    - path: "api-ventago/src/app/sales/sales-create.service.ts"
      provides: "설정 분기 재고 차감 + 대상 영업일 채번"
      contains: "allowSaleWithoutStock"
  key_links:
    - from: "store_configs.allow_sale_without_stock"
      to: "applyStockDecrements 분기"
      via: "설정이 비허용일 때만 조건부 UPDATE 로 실제 차단"
      pattern: "stock >= "
---

<objective>
세 가지 잔여 결함을 한 Plan 으로 정리한다.

**R10 (재고 동시성)**: `processSaleItems:981` 이 `product.stock <= 0` 을 락 없이 읽고, 실제 차감은 `:467` 에서 일어난다(TOCTOU). **단, 이 방어는 매장 설정에 따라 달라야 한다** — `allowSaleWithoutStock=true` 매장은 음수 재고가 의도된 동작이므로 여기에 차단을 걸면 정상 업무가 막히는 회귀다.

**R11 (백데이트 채번)**: `now = dtoSaleDate ?? new Date()`(`:258`)로 과거 날짜 판매가 가능한데, `reserveDailyNumber`(`:610`)는 락 키(`:618`)와 `saleDayLocal`(`:630`) 둘 다 `NOW()` 기준이라 백데이트 판매가 오늘 번호를 소비한다.

**R12 (pool 예산)**: 공개몰 격리 pool(`shop-readonly-db.service.ts:34`, 워커당 15)이 메인 예산(워커당 20) 밖에서 잡히고, `SHOP_DB_*` 미설정 시 같은 DB 로 폴백해 격리가 이름뿐이 된다.

Purpose: 결함 10·11·12 제거.
Output: 설정 분기 원자 차감 + 대상 영업일 채번 + pool 예산 가시화.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-transaction-safety-concurrency-integrity/64-SPEC.md
@.planning/phases/64-transaction-safety-concurrency-integrity/64-RESEARCH.md
@CLAUDE.md

<interfaces>
**R10 관련** (`api-ventago/src/app/sales/sales-create.service.ts`):
- `:215`–`:229` Store + StoreConfig 단일 JOIN (timezone, allowSaleWithoutStock, AFIP 게이트) — **신규 쿼리 없이 설정 재사용 가능**
- `:234`–`:235` `const allowSaleWithoutStock = store?.storeConfig?.allowSaleWithoutStock ?? true` (기본 허용)
- `:981`–`:986` 재고 검사 + `Sin stock disponible` 400
- `:467`–`:480` `applyStockDecrements(...)` 호출 (트랜잭션 끝)
- `:897`–`:950` `applyStockDecrements` — Phase 63 B-0c 주석: productId 오름차순 정렬, 상대 감소(`stock = stock - N`)
- 권위 소스 규약: `store_configs`(v2) 단일, `stores` 폴백 제거됨

**R11 관련**:
- `:256`–`:259` `const now = dtoSaleDate ? new Date(dtoSaleDate) : new Date()`
- `:610`–`:663` `reserveDailyNumber(storeId, tz, t)` — `:618` `pg_advisory_xact_lock($1, to_char((NOW() AT TIME ZONE $2),'YYYYMMDD')::int)`, `:630` `SELECT to_char((NOW() AT TIME ZONE $1)::date,'YYYY-MM-DD')`
- `:641`–`:650` `saleModel.findOne({ where: { storeId, activityType:'sale', saleDayLocal }, order: dailyNumber DESC })`
- UNIQUE 인덱스: `migrations/2026-07-27-phase63-sale-day-local-2-index.sql`
- `:697`–`:728` nullifySale 의 역분개 채번도 같은 함수 사용

**R12 관련**:
- `api-ventago/src/app/shop-public/shop-readonly-db.service.ts:12`–`:13` (주석: 메인 pool `min=10/max=80` ← **현행과 불일치**), `:23`–`:40` `new Pool({ ..., max: Number(process.env.SHOP_DB_POOL_MAX || 15) })`, `:47`–`:58` 기동 시 `SELECT 1` + 로그
- `api-ventago/src/database/database.module.ts:52`–`:58` `pool: { min: 2, max: 20, idle: 10000, acquire: 15000, evict: 1000 }`, `:184`–`:201` 사용률 80% 경고
- 주석 상 운영값: pgbouncer `ventago pool_size=50`, PG `max_connections=200`, 워커 4
- `api-ventago/src/app/diagnostics/diagnostics.service.ts` — pool/outbox 조회 노출 지점(재사용)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 재고 차감 설정 분기 (허용=현행 / 비허용=원자 차단)</name>
  <files>api-ventago/src/app/sales/sales-create.service.ts, api-ventago/src/app/sales/sales-stock-decrement.spec.ts</files>
  <read_first>
    - api-ventago/src/app/sales/sales-create.service.ts 210~240행, 953~1000행, 897~950행
    - .planning/phases/64-transaction-safety-concurrency-integrity/64-SPEC.md R10 (분기 표)
    - .planning/phases/64-transaction-safety-concurrency-integrity/64-RESEARCH.md § 7
  </read_first>
  <behavior>
    - 비허용 매장 · 재고 1 · 동시 2판매 → 1건 성공 + 1건 400, `products.stock == 0`
    - 비허용 매장 · 재고 충분 → 정상 판매, 지연 증가 없음
    - **허용 매장 · 재고 1 · 동시 2판매 → 2건 모두 성공, `products.stock == -1`** (현행 유지, 회귀 금지)
    - 허용 매장 → 조건부 UPDATE 경로를 타지 않는다(추가 락 0)
    - generic 상품 → 어느 설정에서도 차감/검사 대상 아님
    - 부모 변형 상품도 같은 규칙 + productId 오름차순 유지
  </behavior>
  <action>
**A. 설정을 차감 함수까지 전달** — `applyStockDecrements(decrements, t, allowSaleWithoutStock)`.
설정값은 이미 `:234` 에서 로드돼 있다. **신규 쿼리 0.**

**B. 분기 구현**:
```typescript
// [Phase 64 W8/R10] 재고 초과 판매 방어는 매장 설정에 따라 동작이 갈린다.
//
//   allowSaleWithoutStock = true  (허용, 기본) → 현행 그대로. 음수 재고는 의도된 동작이다.
//                                                 여기에 차단을 걸면 정상 업무가 막힌다(회귀).
//   allowSaleWithoutStock = false (비허용)     → 검사와 차감을 한 문장으로 합쳐 실제로 막는다.
//                                                 종전에는 검사(processSaleItems)와 차감이 떨어져 있어
//                                                 동시 판매가 둘 다 통과했다(TOCTOU).
//
// 락 순서는 두 경로 모두 productId 오름차순 유지 (Phase 63 B-0c — 교착 방지).
for (const d of sorted) {
  if (allowSaleWithoutStock) {
    // 현행 경로: 상대 감소만. 음수 허용.
    await this.productModel.decrement('stock', { by: d.quantity, where: { id: d.productId }, transaction: t });
  } else {
    // 비허용 경로: 조건부 UPDATE — 영향 행 0 이면 재고 부족이다.
    const [, meta] = await this.sequelize.query(
      `UPDATE products SET stock = stock - $1 WHERE id = $2 AND stock >= $1`,
      { bind: [d.quantity, d.productId], transaction: t },
    );
    if (((meta as any)?.rowCount ?? 0) === 0) {
      throw new BadRequestException(
        `Sin stock disponible para el producto ${d.productId}. La venta de productos sin stock está deshabilitada para esta tienda.`,
      );
    }
  }

  // 부모 변형 상품 잔액도 동일 규칙으로 (부모는 집계 캐시라 비허용 매장에서도 조건 없이 감소시킬지 여부를
  // 결정해 주석으로 남긴다 — 기본: 부모는 조건 없이 감소, 판단 근거는 자식 재고가 이미 검증됐다는 점)
}
```

**C. `processSaleItems`(`:981`)의 사전 검사 유지** — 비허용 매장에서 **빠른 실패 + 상품명 포함 메시지**(UX)를 위해 남긴다. 다만 이것이 방어의 근거가 아님을 주석에 명시한다:
```typescript
// [Phase 64 W8/R10] 이 검사는 UX 용 조기 실패일 뿐이다(상품명을 보여주려고 남긴다).
// 실제 동시성 방어는 커밋 직전 applyStockDecrements 의 조건부 UPDATE 가 담당한다.
```

**D. 에러 메시지 호환** — 프런트가 `Sin stock disponible` 문자열을 매칭할 수 있으므로 접두 문구를 유지한다.

**E. 스펙** — `<behavior>` 6개 케이스 전부. 특히 **허용 매장 2건 성공** 케이스를 명시적으로 둔다(이 Plan 이 만들 수 있는 가장 큰 회귀).
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p tsconfig.json && npx jest src/app/sales --silent && npx eslint src/app/sales/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stock = stock - \$1 WHERE id = \$2 AND stock >= \$1" api-ventago/src/app/sales/sales-create.service.ts` == 1
    - `grep -c "allowSaleWithoutStock" api-ventago/src/app/sales/sales-create.service.ts` >= 3 (로드/전달/분기)
    - 허용 매장 회귀 케이스가 스펙에 존재 (`grep -c "음수" api-ventago/src/app/sales/sales-stock-decrement.spec.ts` >= 1)
    - 정렬 규칙 주석/코드 유지
    - tsc/jest/eslint exit 0
  </acceptance_criteria>
  <done>설정이 "막겠다"면 실제로 막히고, "허용"이면 현행 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 백데이트 영업일 채번</name>
  <files>api-ventago/src/app/sales/sales-create.service.ts, api-ventago/src/app/sales/sales-daily-number.spec.ts</files>
  <read_first>
    - api-ventago/src/app/sales/sales-create.service.ts 250~270행, 593~665행, 690~730행
    - api-ventago/migrations/2026-07-27-phase63-sale-day-local-2-index.sql (UNIQUE 정의)
    - .planning/phases/64-transaction-safety-concurrency-integrity/64-RESEARCH.md § 8
  </read_first>
  <behavior>
    - `dtoSaleDate` 없음 → 현행과 동일(오늘 영업일, 오늘 번호)
    - 3일 전 날짜 → `sale_day_local` = 그 날짜, `daily_number` = 그 날 최대+1
    - 백데이트 판매가 오늘 번호를 소비하지 않는다
    - 같은 과거 날짜 동시 2건 → 번호 중복 0 (advisory lock 키도 그 날짜)
    - `sale_date` 와 `sale_day_local` 이 같은 날을 가리킨다
  </behavior>
  <action>
**A. 시그니처 확장**:
```typescript
private async reserveDailyNumber(
  storeId: number,
  tz: string,
  t: Transaction,
  targetDate?: Date,        // [Phase 64 W8/R11] 백데이트 판매의 대상 영업일 (미지정 = 오늘)
): Promise<{ dailyNumber: number; saleDayLocal: string }>
```

**B. 날짜 계산을 한 곳으로**:
```typescript
// [Phase 64 W8/R11] 락 키와 sale_day_local 이 반드시 같은 날짜를 봐야 한다.
// 종전에는 둘 다 NOW() 라, Alt+F2 로 과거 날짜를 등록하면 오늘 번호를 소비하고
// sale_date(과거) 와 sale_day_local(오늘) 이 어긋났다.
// Phase 63 F-14 가 타임존에 대해 세운 원칙(하나의 값을 조회·인덱스가 공유)을 날짜 선택으로 확장한다.
const [dayRow] = await this.sequelize.query<{ day: string }>(
  targetDate
    ? `SELECT to_char(($1::timestamptz AT TIME ZONE $2)::date, 'YYYY-MM-DD') AS day`
    : `SELECT to_char((NOW() AT TIME ZONE $1)::date, 'YYYY-MM-DD') AS day`,
  { bind: targetDate ? [targetDate, tz] : [tz], transaction: t, type: QueryTypes.SELECT },
);
const saleDayLocal = dayRow.day;

await this.sequelize.query(
  `SELECT pg_advisory_xact_lock($1::int, replace($2, '-', '')::int)`,
  { bind: [storeId, saleDayLocal], transaction: t },
);
```
락을 **날짜 계산 이후**에 잡는다(같은 값을 쓰기 위해). 기존 조회(`:641`–`:650`)는 `saleDayLocal` 을 그대로 사용하므로 변경 없음.

**C. 호출부** — 판매 생성(`:370`)은 `now`(= `dtoSaleDate ?? new Date()`)를 넘긴다. `dtoSaleDate` 가 없으면 `undefined` 를 넘겨 현행 경로 유지.

**D. nullifySale 의 역분개 채번**(`:698`) — **취소일 기준 유지**(현행 동작). 회계 정책상 취소는 취소일에 잡히는 것이 맞다. 이 결정을 주석 + SUMMARY 에 명시한다.

**E. 스펙** — `<behavior>` 5개 케이스. 날짜 문자열 생성 SQL 은 모킹으로 반환값을 고정하고, **락 키와 saleDayLocal 이 같은 날짜에서 파생되는지**를 단언한다.
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p tsconfig.json && npx jest src/app/sales --silent && npx eslint src/app/sales/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "targetDate" api-ventago/src/app/sales/sales-create.service.ts` >= 3
    - `grep -c "pg_advisory_xact_lock(\$1::int, replace(\$2, '-', '')::int)"` == 1
    - 락 호출이 날짜 계산 **이후**에 위치 (수동 확인)
    - 5개 스펙 통과, tsc/jest/eslint exit 0
  </acceptance_criteria>
  <done>백데이트 판매가 그 날짜의 번호를 받는다.</done>
</task>

<task type="auto">
  <name>Task 3: 커넥션 예산 가시화 + 공개몰 폴백 방어</name>
  <files>api-ventago/src/app/shop-public/shop-readonly-db.service.ts, api-ventago/src/database/database.module.ts, api-ventago/src/app/diagnostics/diagnostics.service.ts</files>
  <read_first>
    - api-ventago/src/app/shop-public/shop-readonly-db.service.ts (전문 95행)
    - api-ventago/src/database/database.module.ts 40~70행, 140~205행
    - api-ventago/src/app/diagnostics/diagnostics.service.ts (pool 조회 노출 형태)
    - .planning/phases/64-transaction-safety-concurrency-integrity/64-RESEARCH.md § 9
  </read_first>
  <action>
**A. 폴백 감지 + 상한 축소** (`shop-readonly-db.service.ts` 생성자):
```typescript
// [Phase 64 W8/R12] SHOP_DB_HOST 가 없으면 이 pool 은 메인과 같은 인스턴스를 가리킨다.
// 그러면 "격리"는 이름뿐이고 커넥션 예산만 두 배가 된다 — 공개 트래픽이 POS 를 굶길 수 있다.
// 이 경우 경고를 남기고 상한을 축소한다(운영이 SHOP_DB_* 를 채우면 원래 상한으로 돌아온다).
const isolated = !!process.env.SHOP_DB_HOST;
const requestedMax = Number(process.env.SHOP_DB_POOL_MAX || 15);
const effectiveMax = isolated ? requestedMax : Math.min(requestedMax, 5);
if (!isolated) {
  this.logger.warn(
    `공개몰 pool 이 메인 DB 인스턴스로 폴백됨 — 격리 미적용. 상한 ${requestedMax} → ${effectiveMax} 로 축소. SHOP_DB_HOST 설정 권장.`,
  );
}
```

**B. 예산 산식 기동 로그** (`database.module.ts` 부팅 로그 `:147` 부근에 추가):
```typescript
// [Phase 64 W8/R12] 총 커넥션 예산을 한 줄로 보이게 한다.
// 부하 시 실질 병목은 앱 pool 이 아니라 pgbouncer 서버 슬롯 큐잉이다(Phase 63 실측) —
// 그래서 앱 max 를 올리는 대응은 금물이고, 예산을 눈으로 확인할 수 있어야 한다.
const workers = Number(process.env.WEB_CONCURRENCY || process.env.PM2_INSTANCES || 1);
const shopMax = process.env.SHOP_DB_HOST ? Number(process.env.SHOP_DB_POOL_MAX || 15) : Math.min(Number(process.env.SHOP_DB_POOL_MAX || 15), 5);
this.logger.log(
  `커넥션 예산: 워커 ${workers} × (메인 ${mainMax} + 공개몰 ${shopMax}) = ${workers * (mainMax + shopMax)} 클라이언트 ` +
  `| pgbouncer pool_size=${process.env.PGBOUNCER_POOL_SIZE ?? '50(추정)'} | 공개몰 격리=${!!process.env.SHOP_DB_HOST}`,
);
```
워커 수 환경변수명은 실제 배포 설정(pm2)에 맞춰 확인 후 사용한다 — 추정값이면 로그에 `(추정)` 표기.

**C. 진단 노출** — `diagnostics.service.ts` 의 pool 조회 응답에 위 산식 값(메인 max, 공개몰 max, 워커 수, 격리 여부)을 추가한다. 신규 엔드포인트는 만들지 않는다.

**D. 오래된 주석 교정** — `shop-readonly-db.service.ts:12`–`:13` 의 "메인 pool min=10/max=80" 서술을 현행(`min=2/max=20`, 2026-07-25 변경)으로 고친다.

**E. 문서** — 산식과 현재 실측값을 `64-VALIDATION.md`(W10 산출물)에 기록할 수 있도록 SUMMARY 에 표로 남긴다.
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p tsconfig.json && npx eslint src/app/shop-public/shop-readonly-db.service.ts src/database/database.module.ts src/app/diagnostics/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "커넥션 예산" api-ventago/src/database/database.module.ts` == 1
    - `grep -c "SHOP_DB_HOST" api-ventago/src/app/shop-public/shop-readonly-db.service.ts` >= 2
    - `grep -c "max=80" api-ventago/src/app/shop-public/shop-readonly-db.service.ts` == 0 (주석 교정 완료)
    - 진단 응답에 pool 예산 필드 추가됨
    - tsc/eslint exit 0
  </acceptance_criteria>
  <done>예산이 로그·진단에서 보이고 폴백이 방어된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 설정 분기 실동작 UAT (허용/비허용 양쪽)</name>
  <what-built>
    재고 차감이 `allowSaleWithoutStock` 설정에 따라 분기한다. 비허용 매장은 조건부 UPDATE 로 실제 차단, 허용 매장은 현행 그대로 음수 허용. 백데이트 판매는 대상 영업일 번호를 받는다. 기동 로그에 커넥션 예산이 출력된다.
  </what-built>
  <how-to-verify>
    1. **허용 매장(회귀 확인이 더 중요)**: Preferencias 에서 "재고 없이 판매 허용" ON → 재고 0 상품 판매 → **성공**해야 한다. `SELECT stock FROM products WHERE id=...` 음수 확인.
    2. **비허용 매장**: 같은 토글 OFF → 재고 0 상품 판매 시도 → 400 `Sin stock disponible`. 재고 1 상품 판매 → 성공, `stock=0`.
    3. **백데이트**: Alt+F2(또는 해당 UI)로 3일 전 날짜 판매 생성 후:
       ```sql
       SELECT id, sale_date, sale_day_local, daily_number
         FROM sales WHERE store_id = ? ORDER BY id DESC LIMIT 5;
       ```
       → `sale_day_local` 이 3일 전, `daily_number` 가 그 날 연속값인지 확인. 오늘 번호가 건너뛰지 않았는지도 확인.
    4. **기동 로그**: API 재기동 후 `커넥션 예산:` 라인 확인.
    5. 토글 전환 후 반영 지연(캐시 TTL) 을 실측해 SUMMARY 에 기록.
  </how-to-verify>
  <acceptance_criteria>
    - 허용 매장 판매 성공 + 음수 재고 (회귀 없음)
    - 비허용 매장 재고 초과 판매 400
    - 백데이트 판매의 `sale_day_local` == 대상 날짜, 오늘 번호 미소비
    - 기동 로그에 예산 라인 존재
    - 설정 반영 지연 값이 기록됨
  </acceptance_criteria>
  <resume-signal>UAT 결과(쿼리 출력 + 로그 라인)를 붙여넣어 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 매장 설정 토글 | 판매 차단 여부를 결정 — 잘못 해석하면 정상 판매가 막히거나 초과 판매가 통과 |
| `dtoSaleDate` | 클라이언트가 주는 날짜가 채번 키가 된다 |
| 공개 익명 트래픽 | POS 와 같은 DB 자원을 두고 경합 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-34 | DoS | 허용 매장에 차단이 걸려 판매 정지 | mitigate | 설정 분기 + 허용 매장 회귀 케이스를 스펙/UAT 양쪽에 고정 |
| T-64-35 | Tampering | 비허용 매장 초과 판매 통과 | mitigate | 검사·차감 단일 문(조건부 UPDATE) |
| T-64-36 | Tampering | 임의 과거 날짜로 번호 체계 교란 | mitigate | 대상 날짜 기준 advisory lock + UNIQUE 인덱스가 2차 방어. 백데이트 권한은 기존 UI 권한을 따름 |
| T-64-37 | DoS | 공개몰 트래픽이 POS 커넥션 잠식 | mitigate | 폴백 시 상한 축소 + 예산 로그 + 진단 노출 |
| T-64-38 | DoS | 교착(비허용 경로의 추가 UPDATE) | mitigate | productId 오름차순 정렬 유지, `lock_timeout=3s` |
</threat_model>

<verification>
```bash
grep -c "AND stock >= " api-ventago/src/app/sales/sales-create.service.ts        # 1
grep -c "targetDate" api-ventago/src/app/sales/sales-create.service.ts           # >= 3
grep -c "커넥션 예산" api-ventago/src/database/database.module.ts                 # 1
grep -c "max=80" api-ventago/src/app/shop-public/shop-readonly-db.service.ts     # 0
cd api-ventago && npx jest src/app/sales --silent && npx eslint src/app/sales/ src/app/shop-public/ src/database/
```
</verification>

<success_criteria>
- 비허용 매장: 동시 판매에서도 재고 초과 불가
- 허용 매장: 현행 그대로 (음수 허용, 지연 변화 없음) — **이 회귀 없음이 최우선**
- 백데이트 판매가 대상 영업일 번호를 받고 오늘 번호를 소비하지 않음
- 기동 로그·진단에 커넥션 예산 노출, 폴백 시 경고 + 상한 축소
- tsc/jest/eslint 통과
</success_criteria>

<output>
완료 후 `.planning/phases/64-transaction-safety-concurrency-integrity/64-08-SUMMARY.md` 생성 — UAT 쿼리 출력 + 기동 로그 예산 라인 + 설정 반영 지연 실측값 포함
</output>
