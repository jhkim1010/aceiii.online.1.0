---
phase: 70-stock-cache-retirement-and-backlog-cleanup
plan: 07
type: verify
wave: 3
depends_on: [70-01, 70-02, 70-03, 70-04, 70-05, 70-06]
files_modified:
  - .planning/phases/70-stock-cache-retirement-and-backlog-cleanup/70-UAT.md
autonomous: false
requirements: [UAT]

must_haves:
  truths:
    - "불변식 2개가 배포 후에도 0행이다"
    - "Trello 카드들이 실제 화면에서 재현되지 않는다"
    - "해결된 카드가 Hechos Semanales 로 이동됐다"
  artifacts:
    - path: ".planning/phases/70-stock-cache-retirement-and-backlog-cleanup/70-UAT.md"
      provides: "항목별 통과/실패 기록"
---

<objective>
Phase 70 전체를 운영에서 검증하고 Trello 카드를 정리한다.

Purpose: 코드가 머지됐다는 것과 증상이 사라졌다는 것은 다른 사실이다. 이번 트리아지에서 `fix/trello-6a635c22` 가 커밋만 되고 main 에 없어 카드가 살아 있던 전례가 있다.
Output: UAT 기록 + 정리된 Trello 보드.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/70-stock-cache-retirement-and-backlog-cleanup/70-CONTEXT.md
</context>

<tasks>

### T1 — 불변식 (운영 5434 + 로컬 5432)
```sql
SELECT count(*) FROM v_stock_balance_drift;  -- 0
SELECT count(*) FROM v_stock_tenant_leak;    -- 0
```
0이 아니면 **어느 Plan 이후 깨졌는지**를 `stock_balances.updated_at` 으로 좁힌다.

### T2 — 배포 확인
Jenkins `api-new-coolsistema` / `front-coolsistema` 최신 빌드가 SUCCESS 이고 **컨테이너가 재생성**됐는가. 각 커밋이 `origin/main` 의 조상인지 `git merge-base --is-ancestor` 로 확인.

### T3 — 화면 검증 (Trello 카드별)

| 카드 | 확인 항목 |
|---|---|
| [Articulos](https://trello.com/c/fXUDii66) | Códigos madres 목록에 수정/삭제 액션이 보이고 동작하는가. 권한 없는 역할에는 안 보이는가 |
| [Pasar a pdf](https://trello.com/c/30zWO5C8) | 브라우저 확대 **90% / 100% / 110% / 125%** 각각에서 PDF 버튼이 보이는가. 눌렀을 때 실제 PDF 가 받아지는가 |
| [Cargar varios productos](https://trello.com/c/diACgk5B) | 확정된 정책대로 동작하는가 |
| [Eliminar un ingreso](https://trello.com/c/LNBmJ2ZI) | 입고 삭제를 **연속 5회** 눌러도 재고가 추가로 깎이지 않는가 (멱등). 첫 클릭에 목록에서 사라지는가 |
| [Sucursal](https://trello.com/c/uyBUKfBM) | 지점 전환이 유지되는가. 로그아웃→재로그인 후에도 유지되는가 |
| [Codigo Vista](https://trello.com/c/zTHHD941) | 코드 칼럼이 보이고 hover 로 전체 코드가 뜨는가 |

### T4 — 회귀 확인 (수정하지 않은 것)
- **견적·온라인 티켓에 고객명이 찍히는가** — `ProductList.tsx:1765` 는 `60d7c83`(front #528)에서 이미 고쳐졌다. 이번 Phase 에서 손대지 않았으므로 **회귀만 확인**한다
- 재고 없는 상품 판매가 여전히 되는가 (`allowSaleWithoutStock=true` 매장). 차단되면 **회귀**다
- Stock Vistas 4개 탭의 숫자가 서로 맞물리는가 (변형 합 = 마드레, 지점 합 = 통합)

### T5 — Trello 정리
통과한 카드를 **Hechos Semanales 로 이동**한다. 아카이브·삭제 금지.
- `triage-state.json` 에 `hechosPending: true` 로 표시하거나 Chrome 에서 직접 이동
- 보드 읽기는 `trello-triage` 스킬의 Chrome `.json` 익스포트 경로 사용

### T6 — 기록
`70-UAT.md` 에 항목별 통과/실패와 근거(스크린샷 경로·SQL 결과)를 남긴다. 실패 항목은 후속 Plan 으로 등록한다.
</tasks>

<constraints>
- ❌ Trello 카드 아카이브·삭제 금지 — Hechos Semanales 이동만
- ❌ 실패 항목을 "통과" 로 기록 금지. 애매하면 애매하다고 쓴다
- ⚠️ 재현 정보 대기 중인 카드([APK Vendedor](https://trello.com/c/sW1EH87H), [Registro de tienda](https://trello.com/c/0p0yNa7x))는 이 Phase 범위 밖 — 건드리지 않는다
</constraints>
