---
phase: 70-stock-cache-retirement-and-backlog-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/src/views/products/list/components/BasicDataCard.tsx
  - ventago-app/src/views/products/list/ProductsView.tsx
autonomous: false
requirements: [TRELLO-diACgk5B]

must_haves:
  truths:
    - "연속 등록 시 이전 상품의 입력이 남아 잘못된 상품이 생성되지 않는다"
    - "'입력값 유지' 를 의도한 기존 동작(같은 지점 연속 입고)은 깨지지 않는다"
  artifacts:
    - path: "ventago-app/src/views/products/list/components/BasicDataCard.tsx"
      provides: "카테고리 변경 시 폼 초기화 트리거"
---

<objective>
Trello [Cargar varios productos](https://trello.com/c/diACgk5B) — *"Al haber cargado mas de un producto, si se quiere agregar otro se mantiene. si se selecciona otra categoria, aun se quedan el articulo cargado. Solo si se selecciona serial, si se borran"*.

**원인은 확정됐다.** 폼 초기화 `handleStartNewProduct`(`ProductsView.tsx:319-341`)를 호출하는 트리거가 세 곳뿐이다 — **Serial 클릭**(`BasicDataCard.tsx:681-704`), **서브카테고리 onFocus**(`:558-621`), **공급자 onFocus**. **Categoría select 에는 연결돼 있지 않다**(`:536-556`, `onChange` 만 있고 리셋 없음). 신고 내용과 정확히 일치한다.

**★ 그런데 이건 의도된 동작일 수도 있다.** `ProductsView.tsx:951-964` 에 주석이 있다:

> "사용자 요청 — agregar 후 같은 지점에서 입력값 유지"

즉 저장 후 값을 남기는 건 **누군가 요청해서 넣은 기능**이다. 그래서 이 Plan 은 `autonomous: false` 다 — 무엇을 "고침" 으로 볼지 먼저 정해야 한다.

Purpose: 정책을 확정한 뒤 그 정책대로만 고친다.
Output: 연속 등록 흐름이 사용자 기대와 일치.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-stock-cache-retirement-and-backlog-cleanup/70-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

### T0 — 정책 확정 (착수 전 사용자 확인, 필수)

세 안 중 하나를 고른다:

| 안 | 동작 | 장점 | 단점 |
|---|---|---|---|
| **A** 카테고리 변경 시에만 리셋 | 서브카테고리·공급자·Serial 과 동일하게 카테고리에도 트리거 연결 | 최소 변경. 기존 "값 유지" 의도 보존 | 같은 카테고리 안에서 연속 등록하면 여전히 값이 남는다 |
| **B** 저장 성공 후 항상 리셋 | `doSubmit` 성공 시 `resetAll()` | 신고 내용에 가장 부합 | `:951-964` 의 "값 유지" 요청과 정면 충돌 |
| **C** 저장 후 "계속 입력 / 새로 입력" 선택 | 스낵바에 액션 버튼 | 둘 다 만족 | UI 추가, 클릭 1회 증가 |

**권장 A** — 신고 문장이 *"si se selecciona otra categoria, aun se quedan"* 이라 카테고리 변경을 "다른 상품 시작" 신호로 보는 게 사용자 기대에 맞고, 기존 의도도 안 깨진다.

정책이 정해지기 전에는 코드를 수정하지 않는다.

### T1 — 구현 (선택된 안만)
안 A 기준:
- `BasicDataCard.tsx:536-556` 카테고리 `CustomTextField` 의 `onChange` 에서 **값이 실제로 달라졌을 때만** `userEditedSku.current = false; onStartNewProduct()` 호출
- 서브카테고리·공급자의 기존 패턴과 **동일하게** 맞춘다 (새 패턴 발명 금지)
- 같은 값 재선택 시 리셋되지 않도록 이전 값과 비교

### T2 — 부수 효과 확인
`handleStartNewProduct` 가 무엇을 지우는지 정확히 읽고, 카테고리 변경 때 지워지면 곤란한 값(선택된 지점 등)이 없는지 확인한다. 있으면 그 값은 보존한다.

### T3 — 검증
- `npx eslint <수정 파일>` 오류 0
- 수동 확인: 상품 등록 → 카테고리 변경 → 이름/SKU/variants 가 비는가 → 같은 카테고리 유지 시에는 기존 동작대로인가
</tasks>

<constraints>
- ❌ T0 정책 확정 전 코드 수정 금지
- ❌ `ProductsView.tsx:951-964` 의 "값 유지" 동작을 안 B 선택 없이 변경 금지
- ⚠️ ESLint: `return` 위 빈 줄, `//` 주석 위 빈 줄, 미사용 import 금지
</constraints>
