---
phase: 16-control-de-talleres
plan: "03"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - ventago-app/src/views/talleres/tabs/TalleresTab.tsx
  - ventago-app/src/views/talleres/drawers/VendorExpandedRow.tsx
  - ventago-app/src/views/talleres/tabs/LotesTab.tsx
  - ventago-app/src/views/talleres/drawers/LoteDetailDrawer.tsx
  - ventago-app/src/views/talleres/tabs/EnviosTab.tsx
  - ventago-app/src/views/talleres/TalleresMainView.tsx
autonomous: true
requirements:
  - TALLERES-05
  - TALLERES-06
  - TALLERES-07

must_haves:
  truths:
    - "Talleres 탭에서 행 클릭 시 확장되어 상세 통계(보유량, 채무, 이행률, 평점, 용량) 표시"
    - "Talleres 탭에서 상태(활성/비활성) + 담당 공정별 필터 동작"
    - "Lotes 탭에서 행 클릭 시 420px 우측 드로어 오픈 — 수량 분포 + 공정 진행도 + 타임라인"
    - "Envios 탭에서 상태/업체/공정별 필터 + 지연 행 빨간 배경 표시"
  artifacts:
    - path: "ventago-app/src/views/talleres/tabs/TalleresTab.tsx"
      provides: "MUI Table with Collapse expandable rows"
      exports: ["default"]
    - path: "ventago-app/src/views/talleres/drawers/LoteDetailDrawer.tsx"
      provides: "420px right drawer with qty distribution + etapa flow + timeline"
      exports: ["default"]
    - path: "ventago-app/src/views/talleres/tabs/EnviosTab.tsx"
      provides: "Envios table with multi-filter"
      exports: ["default"]
  key_links:
    - from: "ventago-app/src/views/talleres/tabs/TalleresTab.tsx"
      to: "/talleres/vendors/all"
      via: "apiConnector.get"
      pattern: "apiConnector\\.get.*talleres/vendors"
    - from: "ventago-app/src/views/talleres/tabs/LotesTab.tsx"
      to: "/talleres/lotes/all"
      via: "apiConnector.get"
      pattern: "apiConnector\\.get.*talleres/lotes"
---

<objective>
Wave 2 탭 구현: Talleres(확장 행) + Lotes(420px 드로어) + Envios(필터 테이블)

Purpose: D-14에 따른 Wave 2 우선순위 구현. 업체별 상세 확인, 로트 상세 조회, 발송 관리 3개 핵심 탭 완성
Output: TalleresTab + LotesTab + EnviosTab + VendorExpandedRow + LoteDetailDrawer 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-control-de-talleres/16-CONTEXT.md
@.planning/phases/16-control-de-talleres/16-RESEARCH.md
@.planning/phases/16-control-de-talleres/talleres-mockup.html
@.planning/phases/16-control-de-talleres/16-01-SUMMARY.md

@ventago-app/src/views/talleres/components/constants.ts
@ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx
@ventago-app/src/views/talleres/TalleresMainView.tsx
@ventago-app/src/views/talleres/vendors/talleres_VendorsListView.tsx
@ventago-app/src/views/talleres/lotes/talleres_LotesListView.tsx
@ventago-app/src/views/talleres/envios/talleres_EnviosListView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TalleresTab (MUI Table + Collapse 확장 행) + VendorExpandedRow (D-07, D-08)</name>
  <files>
    ventago-app/src/views/talleres/tabs/TalleresTab.tsx,
    ventago-app/src/views/talleres/drawers/VendorExpandedRow.tsx
  </files>
  <read_first>
    ventago-app/src/views/talleres/vendors/talleres_VendorsListView.tsx,
    ventago-app/src/views/talleres/vendors/components/talleres_VendorDetailPanel.tsx,
    ventago-app/src/views/talleres/components/constants.ts,
    .planning/phases/16-control-de-talleres/talleres-mockup.html
  </read_first>
  <action>
**1. `drawers/VendorExpandedRow.tsx` 생성:**

Props:
```typescript
interface VendorExpandedRowProps {
  vendor: {
    id: number
    name: string
    phone?: string
    isActive: boolean
    etapas?: Array<{ name: string }>
    pendingQuantity?: number
    totalDebt?: number
    completionRate?: number
    rating?: number
    capacityUsed?: number
  }
}
```

구현 (목업 PAGE 3 vendor-card 내부 통계 → 확장 행으로 변환, D-07):
- Box `sx={{ py: 2, px: 3, bgcolor: '#fafafa' }}`
- 2줄 레이아웃:
  - 상단: 5개 통계 카드 (flex row, gap 3)
    - Pendientes: `vendor.pendingQuantity`, 색 `#7C4DFF`
    - Deuda: `$${(vendor.totalDebt || 0).toLocaleString()}`, 색 `#F44336`
    - Cumplimiento: `${vendor.completionRate || 0}%`, 색 = >=90 green, >=80 orange, else red
    - Rating: `vendor.rating` (별 아이콘), 색 `#FF9800`
    - 각 통계: Box `textAlign: 'center'` — 값 (fontSize 16, fontWeight 700) + 레이블 (fontSize 10, text.secondary)
  - 하단: 용량(Capacidad) 프로그레스 바
    - Box: flex row, gap 2, alignItems center
    - Typography "Capacidad:" (fontSize 12)
    - Box (progress track): `flex: 1, height: 4, bgcolor: '#f0f0f0', borderRadius: 2`
    - Box (progress fill): `width: ${vendor.capacityUsed || 0}%`, height 100%, borderRadius 2, bgcolor = >=80 orange, else green
    - Typography `${vendor.capacityUsed || 0}%` (fontSize 10, text.secondary)

**2. `tabs/TalleresTab.tsx` 생성:**

State:
```typescript
const [vendors, setVendors] = useState<any[]>([])
const [expandedId, setExpandedId] = useState<number | null>(null)
const [loading, setLoading] = useState(true)
const [statusFilter, setStatusFilter] = useState<string>('all')
const [etapaFilter, setEtapaFilter] = useState<string>('all')
const [searchQuery, setSearchQuery] = useState('')
const [etapas, setEtapas] = useState<any[]>([])
```

데이터 로드:
- `apiConnector.get('/talleres/vendors/all')` → vendors
- `apiConnector.get('/talleres/etapas/all')` → etapas (필터 옵션용)
- `Promise.all()` 병렬 호출

필터 (D-08):
```typescript
const filteredVendors = vendors.filter(v => {
  if (statusFilter !== 'all') {
    const isActive = statusFilter === 'active'

    if (v.isActive !== isActive) return false
  }
  if (etapaFilter !== 'all') {
    const hasEtapa = v.vendorEtapas?.some((ve: any) => ve.etapaId === Number(etapaFilter))

    if (!hasEtapa) return false
  }
  if (searchQuery) {
    if (!v.name.toLowerCase().includes(searchQuery.toLowerCase())) return false
  }

  return true
})
```

레이아웃 (목업 PAGE 3 — D-07에 따라 카드 대신 테이블+확장 행):
- Header: flex space-between
  - 타이틀 "Talleres" (22px, bold)
  - "+ Nuevo Taller" 버튼 (기존 VendorFormDrawer 연결 또는 placeholder)
- Filter Bar: flex row, gap 10px, mb 20px
  - TextField: placeholder "Buscar taller...", width 250px, size small
  - Select: "Todos los estados" / "Activos" / "Inactivos" — value={statusFilter}, onChange → setStatusFilter
  - Select: "Todas las etapas" + etapas.map(e => option) — value={etapaFilter}, onChange → setEtapaFilter
- MUI `<Table>` (NOT DataGrid — DataGrid는 Collapse 지원 불가, Research Pitfall 1 참조):
  - TableHead: Taller, Telefono, Etapas, Pendientes, Deuda, Cumplimiento, Rating, Estado
  - TableBody: `filteredVendors.map(vendor => (` 
    - `<React.Fragment key={vendor.id}>`
    - 기본 TableRow: `hover, onClick={() => setExpandedId(prev => prev === vendor.id ? null : vendor.id), sx={{ cursor: 'pointer' }}`
      - 이름 셀: Avatar (이니셜 2글자, 컬러 순환) + 이름
      - 전화번호, 담당 etapas (comma join), pendingQuantity, totalDebt ($ 포맷), completionRate%, rating (별), isActive Chip
    - 확장 TableRow: `<TableCell colSpan={8} sx={{ py: 0 }}>`
      - `<Collapse in={expandedId === vendor.id} timeout='auto' unmountOnExit>`
      - `<VendorExpandedRow vendor={vendor} />`
    - `</React.Fragment>`

**주의:** 하나의 행만 동시 확장 (expandedId가 single number). 여러 행 동시 확장 아님.

**ESLint:** return 위 빈줄, 주석 위 빈줄, 미사용 import 금지, semi: false
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx next lint --file src/views/talleres/tabs/TalleresTab.tsx --file src/views/talleres/drawers/VendorExpandedRow.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TalleresTab.tsx contains `<Table` (NOT DataGrid or FullTable)
    - TalleresTab.tsx contains `<Collapse` and `unmountOnExit`
    - TalleresTab.tsx contains `expandedId` state (single number, not Set or array)
    - TalleresTab.tsx contains `statusFilter` and `etapaFilter` state variables
    - TalleresTab.tsx contains `apiConnector.get('/talleres/vendors/all')`
    - VendorExpandedRow.tsx contains `pendingQuantity` and `totalDebt` and `completionRate` and `rating`
    - VendorExpandedRow.tsx contains `capacityUsed` with progress bar (`height: 4`)
    - ESLint passes
  </acceptance_criteria>
  <done>Talleres 탭에서 vendor 목록이 MUI Table로 표시되고, 행 클릭 시 Collapse로 확장되어 통계(보유량, 채무, 이행률, 평점, 용량) 표시. 상태/공정 필터 동작.</done>
</task>

<task type="auto">
  <name>Task 2: LotesTab + LoteDetailDrawer + EnviosTab + TalleresMainView 탭 연결 (D-09, D-10, D-14)</name>
  <files>
    ventago-app/src/views/talleres/tabs/LotesTab.tsx,
    ventago-app/src/views/talleres/drawers/LoteDetailDrawer.tsx,
    ventago-app/src/views/talleres/tabs/EnviosTab.tsx,
    ventago-app/src/views/talleres/TalleresMainView.tsx
  </files>
  <read_first>
    ventago-app/src/views/talleres/lotes/talleres_LotesListView.tsx,
    ventago-app/src/views/talleres/envios/talleres_EnviosListView.tsx,
    ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx,
    ventago-app/src/views/talleres/components/constants.ts,
    ventago-app/src/views/talleres/TalleresMainView.tsx,
    .planning/phases/16-control-de-talleres/talleres-mockup.html
  </read_first>
  <action>
**1. `drawers/LoteDetailDrawer.tsx` 생성 (D-09):**

Props:
```typescript
interface LoteDetailDrawerProps {
  lote: any
  etapas: any[]
  envios: any[]
  onClose: () => void
}
```

구현 (목업 DRAWER 섹션, width 420px):
- Drawer header: "LOT-{loteNumber}" (16px, bold) + close button (X)
- Body:
  - 상단 info: 제품명 (좌) + Total 수량 (우, 22px bold)
  - **수량 분포 카드 3개** (flex row, gap 12px, mb 20px):
    - Disponible: 배경 `#ede7f6`, 값 색 `#7C4DFF` — `lote.availableQuantity` (계산: totalQuantity - envios PENDING/PARTIAL의 pendingQuantity 합계)
    - En Taller: 배경 `#fff3e0`, 값 색 `#FF9800` — envios PENDING/PARTIAL의 pendingQuantity 합계
    - Completado: 배경 `#e8f5e9`, 값 색 `#4CAF50` — envios COMPLETED의 deliveredQuantity 합계 또는 recepciones 합계
    - 각: flex:1, padding 10px, borderRadius 8px, textAlign center, 값 18px bold + 레이블 10px text.secondary
  - **공정 진행도** (D-06 재사용):
    - 제목: "Progreso por Etapa" (13px, bold, mb 12px)
    - `<EtapaFlowVisual etapas={etapas} envios={envios.filter(e => e.loteId === lote.id)} />`
  - **타임라인** (목업 `.timeline` 섹션):
    - 제목: "Historial de Movimientos" (13px, bold, mb 12px)
    - 좌측 2px 세로 선 (#e8e8e8)
    - 각 movement 항목:
      - 12px dot: done=green, active=primary, pending=grey border
      - title: "Envio → {vendor} ({etapa})" 또는 "Recepcion ← {vendor} ({etapa})"
      - desc: "날짜 · 수량 uds · Vence 날짜"
    - movement 데이터: lote의 envios를 createdAt DESC 정렬
  - **액션 버튼** (2개, flex row, gap 8px):
    - "Nuevo Envio" (primary) + "Cerrar Lote" (outline)
    - 이 버튼들은 placeholder (click 시 기존 모달/로직 연결은 후속 작업)

**2. `tabs/LotesTab.tsx` 생성 (D-10):**

State:
```typescript
const [lotes, setLotes] = useState<any[]>([])
const [etapas, setEtapas] = useState<any[]>([])
const [envios, setEnvios] = useState<any[]>([])
const [selectedLote, setSelectedLote] = useState<any | null>(null)
const [drawerOpen, setDrawerOpen] = useState(false)
const [loading, setLoading] = useState(true)
const [statusFilter, setStatusFilter] = useState<string>('all')
const [searchQuery, setSearchQuery] = useState('')
```

데이터 로드:
- `Promise.all([ apiConnector.get('/talleres/lotes/all'), apiConnector.get('/talleres/etapas/all'), apiConnector.get('/talleres/envios/all') ])`

행 클릭 핸들러 (D-10):
```typescript
const handleRowClick = (lote: any) => {
  setSelectedLote(lote)
  setDrawerOpen(true)
}
```

레이아웃 (목업 PAGE 4):
- Header: flex space-between — "Lotes de Produccion" + "+ Nuevo Lote" 버튼
- Filter bar: 검색 input + 상태 Select (Todos/Abierto/En Proceso/Completado/Cerrado)
- Card > Table (목업 data-table):
  - 컬럼: Lote, Producto, Total, Disponible, En Taller, Progreso (progress bar), Estado (Chip), Acciones
  - 각 행: `cursor: 'pointer', onClick: () => handleRowClick(lote)`
  - "En Taller" 값: primary 색상, bold
  - Progreso: Box progress bar (`height: 6`, 배경 `#f0f0f0`, fill = completedPercentage%, green)
    - completedPercentage = (각 로트의 COMPLETED envio deliveredQuantity 합계 / totalQuantity * 100)
  - Estado: STATUS_CONFIG에서 Chip 렌더링
  - Acciones: "Enviar" 버튼 (OPEN/IN_PROGRESS일 때) 또는 "Cerrar" 버튼 (COMPLETED일 때)
- MUI Drawer (anchor='right'):
  ```tsx
  <Drawer
    anchor='right'
    open={drawerOpen}
    onClose={() => setDrawerOpen(false)}
    sx={{ '& .MuiDrawer-paper': { width: { xs: '100%', md: 420 } } }}
  >
    {selectedLote && (
      <LoteDetailDrawer
        lote={selectedLote}
        etapas={etapas}
        envios={envios}
        onClose={() => setDrawerOpen(false)}
      />
    )}
  </Drawer>
  ```

**3. `tabs/EnviosTab.tsx` 생성:**

State:
```typescript
const [envios, setEnvios] = useState<any[]>([])
const [loading, setLoading] = useState(true)
const [statusFilter, setStatusFilter] = useState<string>('all')
const [vendorFilter, setVendorFilter] = useState<string>('all')
const [etapaFilter, setEtapaFilter] = useState<string>('all')
const [searchQuery, setSearchQuery] = useState('')
const [vendors, setVendors] = useState<any[]>([])
const [etapas, setEtapas] = useState<any[]>([])
```

데이터 로드:
- `Promise.all([ apiConnector.get('/talleres/envios/all'), apiConnector.get('/talleres/vendors/all'), apiConnector.get('/talleres/etapas/all') ])`

필터링:
```typescript
const filteredEnvios = envios.filter(e => {
  if (statusFilter !== 'all' && e.status !== statusFilter) return false
  if (vendorFilter !== 'all' && e.vendorId !== Number(vendorFilter)) return false
  if (etapaFilter !== 'all' && e.etapaId !== Number(etapaFilter)) return false
  if (searchQuery && !e.lote?.loteNumber?.toLowerCase().includes(searchQuery.toLowerCase())) return false

  return true
})
```

레이아웃 (목업 PAGE 5):
- Header: "Envios y Recepciones" + "+ Nuevo Envio" 버튼
- Filter bar: 검색 + 상태 Select (Todos/Pendiente/Parcial/Completado) + 업체 Select + 공정 Select
- Table 컬럼: Fecha, Lote, Taller, Etapa, Enviado, Pendiente, Vencimiento, Estado, Acciones
- **지연 행 강조**: `isOverdue = e.dueDate && new Date(e.dueDate) < new Date() && ['PENDING', 'PARTIAL'].includes(e.status)`
  - `sx={{ bgcolor: isOverdue ? '#fbe9e7' : 'inherit' }}`
  - Vencimiento 셀: 지연이면 빨간색 + 일수 표시 `(${날짜} (-${daysOverdue}d))`
  - 상태 Chip: 지연이면 "Atrasado" (overdue, red)
- Acciones: Pendiente/Parcial → "Recibir" 버튼, Completed → "Recibido" 텍스트

**4. `TalleresMainView.tsx` 업데이트:**
- TalleresTab, LotesTab, EnviosTab import 추가
- 기존 placeholder를 실제 컴포넌트로 교체:
  - `activeTab === 'talleres' && <TalleresTab />`
  - `activeTab === 'lotes' && <LotesTab />`
  - `activeTab === 'envios' && <EnviosTab />`

**ESLint:** return 위 빈줄, 주석 위 빈줄, 미사용 import 금지, semi: false, 싱글 쿼트
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx next lint --file src/views/talleres/tabs/TalleresTab.tsx --file src/views/talleres/tabs/LotesTab.tsx --file src/views/talleres/tabs/EnviosTab.tsx --file src/views/talleres/drawers/LoteDetailDrawer.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TalleresTab.tsx contains `<Table` and `<Collapse` and `expandedId`
    - LotesTab.tsx contains `<Drawer` and `anchor='right'` and `width:` with `420`
    - LotesTab.tsx contains `handleRowClick` and `setDrawerOpen(true)`
    - LoteDetailDrawer.tsx contains `EtapaFlowVisual` import and rendering
    - LoteDetailDrawer.tsx contains `timeline` section with dot styling
    - LoteDetailDrawer.tsx contains 3 quantity distribution cards (Disponible, En Taller, Completado)
    - EnviosTab.tsx contains `statusFilter` and `vendorFilter` and `etapaFilter`
    - EnviosTab.tsx contains `bgcolor:` with `#fbe9e7` for overdue row highlighting
    - TalleresMainView.tsx imports TalleresTab, LotesTab, EnviosTab
    - ESLint passes
  </acceptance_criteria>
  <done>Talleres 탭(확장 행), Lotes 탭(420px 드로어 with 수량 분포 + etapa flow + 타임라인), Envios 탭(필터 + 지연 강조) 모두 구현 완료. TalleresMainView에 연결됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 프론트엔드 전용 — 기존 API 사용, storeId 필터 이미 적용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | I (Information Disclosure) | LotesTab/EnviosTab | accept | 기존 API에 storeId 필터 내장 |
</threat_model>

<verification>
1. `cd ventago-app && npx next lint` — ESLint 통과
2. /talleres?tab=talleres — 업체 테이블, 행 클릭 시 확장 (통계 표시)
3. /talleres?tab=lotes — 로트 테이블, 행 클릭 시 420px 드로어 (수량 분포 + etapa flow + 타임라인)
4. /talleres?tab=envios — 발송 테이블, 필터 3개 동작, 지연 행 빨간 배경
</verification>

<success_criteria>
- Talleres: MUI Table + Collapse 확장 행, 상태/공정 필터
- Lotes: 420px Drawer (수량 분포 + EtapaFlowVisual + 타임라인)
- Envios: 상태/업체/공정 필터, 지연 행 빨간 배경
- 3개 탭 모두 TalleresMainView에 연결
- ESLint 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/16-control-de-talleres/16-03-SUMMARY.md`
</output>
