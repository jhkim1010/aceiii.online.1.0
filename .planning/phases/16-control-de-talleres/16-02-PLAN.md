---
phase: 16-control-de-talleres
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx
  - ventago-app/src/views/talleres/components/PipelineKanban.tsx
  - ventago-app/src/views/talleres/tabs/PipelineTab.tsx
autonomous: true
requirements:
  - TALLERES-04

must_haves:
  truths:
    - "Pipeline 탭에서 공정 단계별 컬럼에 현재 진행 중인 envio 카드 표시"
    - "선택된 로트의 etapa-flow 원형 노드가 상단에 시각화됨"
    - "Pipeline은 읽기 전용 — 드래그&드롭 없음"
  artifacts:
    - path: "ventago-app/src/views/talleres/components/PipelineKanban.tsx"
      provides: "CSS flexbox Kanban board (read-only)"
      exports: ["default"]
    - path: "ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx"
      provides: "Circular node flow visualization"
      exports: ["default"]
    - path: "ventago-app/src/views/talleres/tabs/PipelineTab.tsx"
      provides: "Pipeline tab with etapa flow + kanban"
      exports: ["default"]
  key_links:
    - from: "ventago-app/src/views/talleres/tabs/PipelineTab.tsx"
      to: "/talleres/etapas/all"
      via: "apiConnector.get"
      pattern: "apiConnector\\.get.*talleres/etapas"
    - from: "ventago-app/src/views/talleres/tabs/PipelineTab.tsx"
      to: "/talleres/envios/all"
      via: "apiConnector.get"
      pattern: "apiConnector\\.get.*talleres/envios"
---

<objective>
Pipeline 탭 구현 — 읽기 전용 Kanban 시각화 + 로트별 공정 진행도 (etapa-flow)

Purpose: 공정 단계별 현재 진행 현황을 한눈에 파악하고, 개별 로트의 공정 진행도를 원형 노드 플로우로 시각화
Output: PipelineKanban + EtapaFlowVisual 컴포넌트 + PipelineTab
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-control-de-talleres/16-CONTEXT.md
@.planning/phases/16-control-de-talleres/16-RESEARCH.md
@.planning/phases/16-control-de-talleres/talleres-mockup.html

@ventago-app/src/views/talleres/components/constants.ts
@api-ventago/src/app/subcon/etapas/etapa.model.ts
@api-ventago/src/app/subcon/envios/envio.model.ts
@api-ventago/src/app/subcon/lotes/lote.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: EtapaFlowVisual + PipelineKanban 공유 컴포넌트 (D-04, D-05, D-06)</name>
  <files>
    ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx,
    ventago-app/src/views/talleres/components/PipelineKanban.tsx
  </files>
  <read_first>
    ventago-app/src/views/talleres/components/constants.ts,
    .planning/phases/16-control-de-talleres/talleres-mockup.html,
    api-ventago/src/app/subcon/etapas/etapa.model.ts,
    api-ventago/src/app/subcon/envios/envio.model.ts
  </read_first>
  <action>
**1. `components/EtapaFlowVisual.tsx` 생성:**

인터페이스:
```typescript
interface EtapaFlowProps {
  etapas: Array<{ id: number; name: string; order: number }>
  envios: Array<{ etapaId: number; pendingQuantity: number; status: string }>
  totalQuantity?: number
}
```

구현 (목업 `.etapa-flow` + `.etapa-circle` 섹션 정확히 반영):
- `Box sx={{ display: 'flex', alignItems: 'center', overflowX: 'auto', py: 1.5 }}`
- etapas를 order 순 정렬 후 map:
  - 해당 etapa에 매핑된 envios를 filter → `inTaller = envios에서 pendingQuantity 합계`
  - `isDone = envios가 COMPLETED 상태이고 inTaller === 0`
  - `isActive = inTaller > 0`
  - 원형 노드: Box `width: 48, height: 48, borderRadius: '50%'`
    - done: `border: '3px solid #4CAF50'`, `bgcolor: '#4CAF50'`, `color: '#fff'`, 내부에 '✓' (fontSize 18)
    - active: `border: '3px solid #7C4DFF'`, `bgcolor: '#ede7f6'`, `color: '#7C4DFF'`, 내부에 `inTaller` 숫자 (fontSize 14 if <1000, 12 if >=1000)
    - inactive: `border: '3px solid #e0e0e0'`, `bgcolor: '#fff'`, `color: '#9e9e9e'`, 내부 빈 문자열 또는 0
  - 노드 아래: Typography `fontSize: 11, mt: 0.75, fontWeight: 500, color: 'text.secondary', textAlign: 'center'` — etapa.name
  - 노드 사이 화살표: Typography `fontSize: 18, color: '#e0e0e0', mx: 0.5, mb: 2.25` — '→'
  - 마지막 etapa 뒤에는 화살표 없음

**2. `components/PipelineKanban.tsx` 생성:**

인터페이스:
```typescript
interface PipelineStage {
  etapaId: number
  etapaName: string
  color: string
  items: Array<{
    envioId: number
    loteNumber: string
    vendorName: string
    quantity: number
    dueDate: string | null
    isOverdue: boolean
    status: string
  }>
}

interface PipelineKanbanProps {
  stages: PipelineStage[]
}
```

구현 (목업 `.pipeline` 섹션):
- 최상위 Box: `display: 'flex', gap: 2, overflowX: 'auto', pb: 1`
- 각 stage → Box:
  - `minWidth: 200, flex: 1, borderRadius: '10px', border: '1px solid #e0e0e0', bgcolor: 'background.paper', boxShadow: '0 2px 8px rgba(0,0,0,0.06)'`
  - `borderTop: '3px solid ${stageColor}'` — stageColor 결정:
    - 모든 items가 COMPLETED → `#4CAF50` (green/done)
    - items 중 isOverdue 있음 → `#FF9800` (orange/warning)
    - items 있음 (active) → `#7C4DFF` (primary)
    - items 없음 → `#e0e0e0` (grey)
  - Stage header: padding 14px 16px, borderBottom 1px #f0f0f0, flex justify-content space-between
    - h4: fontSize 13, fontWeight 600 — etapaName
    - count badge: fontSize 11, padding '2px 8px', borderRadius 10px, fontWeight 600 — `${items.length} lotes`
      - badge color: 해당 stage borderTop 색상의 light 버전 (배경) + dark 버전 (텍스트)
  - Items: 각 item → Box padding 10px 16px, borderBottom 1px #f8f8f8, flex justify space-between
    - 좌: loteNumber + vendorName (12px, bold) + subtitle: `${quantity} uds · ${dueDate 표시}` (11px, text2)
      - isOverdue이면 subtitle 앞에 Warning sign 추가, dueDate를 빨간색으로
    - 우: quantity (12px, bold, 색상 = isOverdue ? red : primary)
  - items 없으면: "Sin lotes en esta etapa" (12px, text2, 가운데 정렬, padding 20px)
- 읽기 전용: 드래그&드롭 이벤트 핸들러 없음 (D-04)

**ESLint:** return 위 빈줄, 주석 위 빈줄, 미사용 import 금지, semi: false, 싱글 쿼트
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx next lint --file src/views/talleres/components/EtapaFlowVisual.tsx --file src/views/talleres/components/PipelineKanban.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EtapaFlowVisual.tsx contains `borderRadius: '50%'` and `width: 48` and `height: 48`
    - EtapaFlowVisual.tsx contains `#4CAF50` (done) and `#7C4DFF` (active) and `#e0e0e0` (inactive)
    - EtapaFlowVisual.tsx contains arrow character `→`
    - PipelineKanban.tsx contains `display: 'flex'` and `overflowX: 'auto'` and `minWidth: 200`
    - PipelineKanban.tsx contains `borderTop: ` with stage color
    - PipelineKanban.tsx does NOT contain `onDrag` or `draggable` or `react-beautiful-dnd`
    - ESLint passes without errors
  </acceptance_criteria>
  <done>EtapaFlowVisual renders circular node flow (done/active/inactive states) and PipelineKanban renders CSS flexbox Kanban columns with stage items — both read-only, no drag&drop</done>
</task>

<task type="auto">
  <name>Task 2: PipelineTab 조립 + TalleresMainView 탭 연결 (D-06)</name>
  <files>
    ventago-app/src/views/talleres/tabs/PipelineTab.tsx,
    ventago-app/src/views/talleres/TalleresMainView.tsx
  </files>
  <read_first>
    ventago-app/src/views/talleres/components/EtapaFlowVisual.tsx,
    ventago-app/src/views/talleres/components/PipelineKanban.tsx,
    ventago-app/src/views/talleres/components/constants.ts,
    ventago-app/src/views/talleres/TalleresMainView.tsx,
    api-ventago/src/app/subcon/etapas/etapa.controller.ts,
    api-ventago/src/app/subcon/envios/envio.controller.ts,
    api-ventago/src/app/subcon/lotes/lote.controller.ts
  </read_first>
  <action>
**1. `tabs/PipelineTab.tsx` 생성:**

State:
```typescript
const [etapas, setEtapas] = useState<any[]>([])
const [envios, setEnvios] = useState<any[]>([])
const [lotes, setLotes] = useState<any[]>([])
const [selectedLoteId, setSelectedLoteId] = useState<number | null>(null)
const [loading, setLoading] = useState(true)
```

데이터 로드 (useEffect, 기존 API 2개 사용 — D-12 기존 API 변경 없음):
- `apiConnector.get('/talleres/etapas/all')` → etapas (공정 단계 목록)
- `apiConnector.get('/talleres/envios/all')` → envios (전체 envio 목록)
- `apiConnector.get('/talleres/lotes/all')` → lotes (로트 목록)
- 3개 `Promise.all()`로 병렬 호출
- 로드 완료 후 첫 번째 IN_PROGRESS 로트를 `selectedLoteId`로 기본 선택

데이터 가공:
- `selectedLote = lotes.find(l => l.id === selectedLoteId)`
- `selectedLoteEnvios = envios.filter(e => e.loteId === selectedLoteId)`
- Pipeline stages 구성:
  ```typescript
  const stages = etapas.map(etapa => ({
    etapaId: etapa.id,
    etapaName: etapa.name,
    color: TALLER_COLORS의 순환 값,
    items: envios
      .filter(e => e.etapaId === etapa.id && ['PENDING', 'PARTIAL'].includes(e.status))
      .map(e => ({
        envioId: e.id,
        loteNumber: e.lote?.loteNumber || `LOT-${e.loteId}`,
        vendorName: e.vendor?.name || 'N/A',
        quantity: e.pendingQuantity,
        dueDate: e.dueDate,
        isOverdue: e.dueDate && new Date(e.dueDate) < new Date(),
        status: e.status,
      }))
  }))
  ```

레이아웃 (목업 PAGE 2):
- 페이지 타이틀: "Pipeline de Produccion" (22px, bold)
- **상단 Card: 선택된 로트 Etapa Flow (D-06)**
  - Card header: `Flujo de Etapas — ${selectedLote?.loteNumber} · ${selectedLote?.product?.name} (${selectedLote?.totalQuantity} uds)`
  - 상태 Chip (OPEN/IN_PROGRESS)
  - Card body (flex center): `<EtapaFlowVisual etapas={etapas} envios={selectedLoteEnvios} />`
  - 로트 선택: Card header 좌측에 Select/드롭다운으로 IN_PROGRESS + OPEN 로트 목록 표시, 선택 시 selectedLoteId 업데이트
- **하단: Pipeline Kanban**
  - `<PipelineKanban stages={stages} />`
- loading 시: `<Box display='flex' justifyContent='center' py={8}><CircularProgress /></Box>`

**2. `TalleresMainView.tsx` 업데이트:**
- PipelineTab import 추가
- 기존 placeholder `activeTab === 'pipeline'` 조건을 `<PipelineTab />`으로 교체

**ESLint:** return 위 빈줄, 주석 위 빈줄, 미사용 import 금지
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx next lint --file src/views/talleres/tabs/PipelineTab.tsx --file src/views/talleres/TalleresMainView.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PipelineTab.tsx contains `apiConnector.get('/talleres/etapas/all')` and `apiConnector.get('/talleres/envios/all')`
    - PipelineTab.tsx contains `Promise.all` for parallel API calls
    - PipelineTab.tsx contains `selectedLoteId` state and `setSelectedLoteId`
    - PipelineTab.tsx contains `<EtapaFlowVisual` and `<PipelineKanban`
    - PipelineTab.tsx contains `filter(e => e.etapaId` for grouping envios by etapa
    - TalleresMainView.tsx contains `import` of PipelineTab
    - TalleresMainView.tsx contains `activeTab === 'pipeline'` with PipelineTab component (not placeholder)
    - ESLint passes
  </acceptance_criteria>
  <done>Pipeline 탭에서 상단에 선택 로트의 etapa-flow 원형 노드, 하단에 전체 Kanban 시각화 — 기존 API 3개(etapas/envios/lotes)만 사용, 새 API 불필요</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 이 플랜은 프론트엔드 전용 — 기존 API를 그대로 사용하며 새 endpoint 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | I (Information Disclosure) | PipelineTab.tsx | accept | 기존 API가 이미 storeId 필터링 적용됨 — 추가 조치 불필요 |
</threat_model>

<verification>
1. `cd ventago-app && npx next lint` — ESLint 통과
2. /talleres?tab=pipeline 접근 시 상단에 로트 선택 + etapa-flow, 하단에 Kanban 표시
3. 로트 선택 변경 시 상단 etapa-flow가 해당 로트의 공정 진행도로 업데이트
4. Kanban 컬럼은 드래그 불가 (읽기 전용)
</verification>

<success_criteria>
- Pipeline 탭에 etapa-flow 원형 노드 + Kanban 컬럼 렌더링
- 로트 선택 시 etapa-flow 업데이트
- 지연 envio에 빨간색 표시 + 경고 아이콘
- ESLint 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/16-control-de-talleres/16-02-SUMMARY.md`
</output>
