---
phase: 25-clientes-globales-compartidos-entre-tiendas-historial-aislad
plan: 13
type: execute
wave: 6
depends_on:
  - 25-06
  - 25-11
files_modified:
  - ventago-app/src/views/cliente-vista/ClienteVistaView.tsx
autonomous: true
requirements:
  - REQ-25-10
  - D4-01

must_haves:
  truths:
    - ClienteVistaView CardHeader action slot contains a new outlined "Importación masiva" button BEFORE the existing "Nuevo Cliente" button
    - Button is wrapped in WithFunctionAccess functionSlug="manage-clientes-import" so vendedor without the permission does not see it
    - Clicking the button navigates to /clientes-globales/carga-masiva via next/router
    - Button uses variant="outlined" + color="primary" + mdi:file-upload-outline icon (matches UI-SPEC S1)
    - i18n key cliente_import_button resolves correctly in both es and ko
  artifacts:
    - path: ventago-app/src/views/cliente-vista/ClienteVistaView.tsx
      provides: new outlined "Importación masiva" button in CardHeader action slot
      contains: "manage-clientes-import"
  key_links:
    - from: ClienteVistaView CardHeader action
      to: /clientes-globales/carga-masiva page
      via: router.push
      pattern: "router.push('/clientes-globales/carga-masiva')"
---

<objective>
Wave 6 Plan 13 — Add the "Importación masiva" button to ClienteVistaView top bar per UI-SPEC.md S1. This is the sole user entry point into the bulk import flow (D4-01 — no separate sidebar menu item). Button is CASL-gated so users without `manage-clientes-import` do not see it.

Purpose: Closes REQ-25-10 ("ClienteView 에 Importación masiva 버튼 + 모달") frontend entry point. Small, self-contained change; the modal workflow lives at the existing `/clientes-globales/carga-masiva` page (built in Plans 11-12).

Output: Single-file modification to ClienteVistaView.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-UI-SPEC.md
@.planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-PATTERNS.md
@.planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-CONTEXT.md
@ventago-app/src/views/cliente-vista/ClienteVistaView.tsx
@ventago-app/src/configs/withFunctionAccess.tsx

<interfaces>
From PATTERNS.md Excerpt F — ClienteVistaView.tsx:309-361 (existing CardHeader action slot):
```tsx
action={
  <Box display="flex" gap={2} alignItems="center" flexWrap="wrap">
    <Box display="flex" gap={0.5}>
      {/* existing status filter chips */}
    </Box>
    <TextField size="small" placeholder="Buscar cliente..." />
    <WithFunctionAccess functionSlug="crear-cliente">
      <Button
        variant="contained"
        startIcon={<Icon icon="mdi:plus" />}
        onClick={openCreateDialog}
      >
        Nuevo Cliente
      </Button>
    </WithFunctionAccess>
  </Box>
}
```

Target insertion (UI-SPEC S1) — immediately BEFORE the existing "Nuevo Cliente" button:
```tsx
<WithFunctionAccess functionSlug="manage-clientes-import">
  <Button
    variant="outlined"
    color="primary"
    startIcon={<Icon icon="mdi:file-upload-outline" />}
    onClick={() => router.push('/clientes-globales/carga-masiva')}
  >
    {t('cliente_import_button', 'Importación masiva')}
  </Button>
</WithFunctionAccess>
```

Rationale (UI-SPEC):
- `variant="outlined"` not `contained` — "Nuevo Cliente" stays the sole contained primary CTA (avoid two competing primaries)
- Gate by `manage-clientes-import` — slug was seeded in Plan 06; admin/superadmin can see it, vendedor cannot
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert "Importación masiva" button into ClienteVistaView CardHeader action slot</name>
  <files>ventago-app/src/views/cliente-vista/ClienteVistaView.tsx</files>
  <read_first>
    - ventago-app/src/views/cliente-vista/ClienteVistaView.tsx (entire file — confirm CardHeader location, existing "Nuevo Cliente" button, useRouter availability, t() import)
    - ventago-app/src/configs/withFunctionAccess.tsx (confirm API — functionSlug prop)
    - .planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-UI-SPEC.md (S1 component contract)
    - .planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-PATTERNS.md (Excerpt F)
    - ventago-app/public/locales/es.json (confirm cliente_import_button key present from Plan 11)
  </read_first>
  <behavior>
    - Test 1 (manual): Log in as admin — button is visible in ClienteVistaView top bar
    - Test 2 (manual): Log in as vendedor (no manage-clientes-import permission) — button is NOT visible
    - Test 3 (manual): Click the button — navigates to /clientes-globales/carga-masiva
    - Test 4 (automated): ESLint + build green
  </behavior>
  <action>
Edit `ventago-app/src/views/cliente-vista/ClienteVistaView.tsx`:

1. Verify imports at top include:
```typescript
import { useRouter } from 'next/router'   // should already exist in the file; if not, add
```

2. Inside the component, verify or add:
```typescript
const router = useRouter()
```

3. Locate the CardHeader `action` prop (around line 309 per PATTERNS.md). Find the `<WithFunctionAccess functionSlug="crear-cliente">` block wrapping the "Nuevo Cliente" button.

4. Immediately BEFORE that `<WithFunctionAccess functionSlug="crear-cliente">`, insert:

```tsx
<WithFunctionAccess functionSlug='manage-clientes-import'>
  <Button
    variant='outlined'
    color='primary'
    startIcon={<Icon icon='mdi:file-upload-outline' />}
    onClick={() => router.push('/clientes-globales/carga-masiva')}
  >
    {t('cliente_import_button', 'Importación masiva')}
  </Button>
</WithFunctionAccess>
```

(Match single-quote or double-quote convention of the existing file.)

5. ESLint compliance:
- Ensure blank line before the new `<WithFunctionAccess>` if preceded by another element
- No unused imports
- newline-before-return in any new blocks

Run:
```bash
cd ventago-app && npm run lint 2>&1 | grep -iE "ClienteVistaView|error" | tail -20
cd ventago-app && npm run build 2>&1 | tee /tmp/build.log && ! grep -qE "error TS[0-9]+|Module not found" /tmp/build.log
```
  </action>
  <verify>
    <automated>
      grep -q "manage-clientes-import" ventago-app/src/views/cliente-vista/ClienteVistaView.tsx &&
      grep -q "mdi:file-upload-outline" ventago-app/src/views/cliente-vista/ClienteVistaView.tsx &&
      grep -q "cliente_import_button" ventago-app/src/views/cliente-vista/ClienteVistaView.tsx &&
      grep -q "/clientes-globales/carga-masiva" ventago-app/src/views/cliente-vista/ClienteVistaView.tsx &&
      grep -q "variant='outlined'\|variant=\"outlined\"" ventago-app/src/views/cliente-vista/ClienteVistaView.tsx &&
      cd ventago-app && npm run build 2>&1 | tee /tmp/build.log && ! grep -qE "error TS[0-9]+|Module not found" /tmp/build.log
    </automated>
  </verify>
  <acceptance_criteria>
    - New WithFunctionAccess wrapper with functionSlug="manage-clientes-import" present
    - Inner Button has variant="outlined" + color="primary" + mdi:file-upload-outline icon
    - onClick calls router.push('/clientes-globales/carga-masiva')
    - Button label comes from t('cliente_import_button', 'Importación masiva')
    - Insertion position is BEFORE the "Nuevo Cliente" WithFunctionAccess block
    - No unused imports; ESLint clean
    - ventago-app npm run build green
  </acceptance_criteria>
  <done>Button present, gated, navigates correctly, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Button visibility (frontend CASL gate) | Cosmetic only — actual backend authorization is Plan 10 @FunctionGuard('manage-clientes-import', 'create') |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Elevation of Privilege (UI bypass) | WithFunctionAccess gate | mitigate | Even if a user manually visits `/clientes-globales/carga-masiva` without this button, the upload endpoint remains gated by backend @FunctionGuard; frontend wrapper is UX-only |
</threat_model>

<verification>
- Manual smoke with admin: button visible, navigates correctly
- Manual smoke with vendedor: button not visible
- Build green
</verification>

<success_criteria>
1. "Importación masiva" button inserted in ClienteVistaView CardHeader action slot
2. Button wrapped in WithFunctionAccess with correct slug
3. Navigation uses next/router push to the existing carga-masiva page
4. Button styling matches UI-SPEC S1 (outlined, primary, mdi:file-upload-outline icon)
5. i18n key used with ES fallback
6. ESLint + build green
</success_criteria>

<output>
After completion, create `.planning/phases/25-clientes-globales-compartidos-entre-tiendas-historial-aislad/25-13-SUMMARY.md` with:
- File modified
- Line number / section where insertion happened
- Description of button appearance for admin vs vendedor
</output>
