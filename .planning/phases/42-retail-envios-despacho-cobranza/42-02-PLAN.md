---
phase: 42-retail-envios-despacho-cobranza
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api-ventago/migrations/42-02-online-orders-cols.sql
  - api-ventago/src/app/online-orders/online-order.model.ts
  - api-ventago/src/app/online-orders/dto/ship-online-order.dto.ts
  - api-ventago/src/app/online-orders/dto/cobro-online-order.dto.ts
  - api-ventago/src/app/online-orders/online-orders.service.ts
  - api-ventago/src/app/online-orders/online-orders.module.ts
autonomous: true
requirements: [RD-2, RD-3, RD-4, RD-6, RD-7]
must_haves:
  truths:
    - "Shipping an order selects a transporte + tracking, sets dispatchedAt, and mirrors transporte.name into shippingCarrier"
    - "Shipping with a positive saldo (total − received) records the shortfall intent in order.metadata.shipSaldo (NOT a sale_credit ledger row — that is accrued at DELIVER once a mirror saleId exists, plan 42-03)"
    - "Shipping a fully-paid order records NO shipSaldo (saldo 0 → metadata.shipSaldo absent/0)"
    - "Shipping with saldo>0 requires an identified credit-eligible client (assertCreditEligible); anonymous orders are blocked at SHIP before any state change"
    - "A cobro (partial/split) can be registered in any status, pays down 외상 via FIFO payment_in, and posts a caja movement"
    - "Cancel(after payment) offers Devolver (caja reverse) vs Pasar a favor (favor_in + favorBalance)"
    - "Listo p/ despacho is derived from preparedAt set + dispatchedAt null (no new enum value)"
  artifacts:
    - path: "api-ventago/src/app/online-orders/online-order.model.ts"
      provides: "preparedAt, dispatchedAt, transporteId columns"
      contains: "transporteId"
    - path: "api-ventago/src/app/online-orders/online-orders.service.ts"
      provides: "ship completion gate (shipSaldo + anonymous block), prepareOrder preparedAt, registerCobro, cancel favor branch"
      contains: "registerCobro"
    - path: "api-ventago/migrations/42-02-online-orders-cols.sql"
      provides: "prepared_at, dispatched_at, transporte_id columns + FK + index"
      contains: "ADD COLUMN IF NOT EXISTS transporte_id"
  key_links:
    - from: "online-orders.service.shipOrder"
      to: "creditValidationService.assertCreditEligible"
      via: "anonymous/limit block before recording shipSaldo (sale_credit row deferred to deliver — 42-03)"
      pattern: "assertCreditEligible"
    - from: "online-orders.service.registerCobro"
      to: "creditPaymentService.registerPayment"
      via: "FIFO payment_in (its OWN tx — NOT nested)"
      pattern: "registerPayment"
    - from: "online-orders.service.registerCobro"
      to: "boxOperationService.addOperation"
      via: "caja movement on open cashRegister"
      pattern: "addOperation"
---

<objective>
Augment the OnlineOrder backbone for retail-delivery control: add transporteId + stage timestamps, extend shipOrder with the completion gate (saldo>0 → record shortfall intent in metadata.shipSaldo + block anonymous via assertCreditEligible), add a cobro (partial/split) operation that pays down 외상 (FIFO payment_in + caja movement), and add the cancel→Devolver/Favor fork. All credit/caja accounting goes through existing services (no hand-roll).

Purpose: RD-3 (ship + tracking + completion gate), RD-4 (외상 accrual — intent recorded at SHIP, ledger row accrued at DELIVER in plan 42-03), RD-7 (cobro down-payment), RD-6 (cancel favor). The "발송 ≠ 종료" invariant: the collection axis (외상) is DECIDED at SHIP (saldo recorded + eligibility enforced), while the actual sale_credit ledger row and revenue/stock recognition land at DELIVER (handled in plan 42-03), because the mirror saleId — required by the sale_credit DB CHECK — only exists after createMirror at deliver.
Output: migration 42-02, model columns, ship/cobro/cancel-favor DTOs + service methods, module wiring for credit/box services.

★ HIGHEST-RISK CONSTRAINT: This plan decides the credit axis at SHIP (records shipSaldo + enforces eligibility) but MUST NOT alter the deliver→mirror revenue path or write a sale_credit ledger row (mirrorSaleId is null at ship; the sale_credit accrual + deliver realignment + its regression gate are plan 42-03). Keep shipOrder additive — do not force paymentStatus, do not call appendMovement here.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md

<interfaces>
runStatusTx signature (online-orders.service.ts:801-808):
```typescript
private async runStatusTx(
  storeId: number, id: number,
  fromStatuses: OnlineOrderStatus[], toStatus: OnlineOrderStatus,
  sideEffect?: (order: OnlineOrder, t: Transaction) => Promise<void>,
  isolationLevel?: Transaction.ISOLATION_LEVELS,
): Promise<OnlineOrder>
```
OnlineOrderStatus: pending|confirmed|preparing|shipped|delivered|cancelled|returned
OnlineOrderPaymentStatus: pending|paid|refunded|failed

CreditLedgerService.appendMovement (credit-ledger.service.ts:91) — REQUIRES external transaction injected (throws BadRequest if missing, L109); locks store_clients FOR UPDATE. sale_credit/favor_apply REQUIRE saleId (DB CHECK). bucket: credito|favor.
  NOTE for this plan: appendMovement(sale_credit) is NOT called at ship — mirrorSaleId is null until deliver. The sale_credit row is accrued in plan 42-03 (deliver) keyed off metadata.shipSaldo recorded here. favor_in (cancel) DOES call appendMovement here because the mirror already exists for a committed order being cancelled.

CreditPaymentService.registerPayment (credit-payment.service.ts:70) — opens its OWN SERIALIZABLE tx (do NOT nest):
```typescript
registerPayment({ storeId, storeClientId, paymentKind:'credit_payment', totalAmount, paymentMethodId, optionId?, receiptNo, branchId, userId, note })
```

CreditValidationService.assertCreditEligible (credit-validation.service.ts:42-116) — requires store_client + globalClient.document(DNI/CUIT); throws "CRÉDITO requiere DNI/CUIT" for anonymous.

BoxOperationService.addOperation (box-operation.service.ts:16-32):
```typescript
addOperation({ cashRegisterId, userId, terminalId, amount, type, executionType, description }, t?)
```

OnlineOrder existing timestamps: confirmedAt/shippedAt/deliveredAt/cancelledAt (L131-141). metadata JSONB (defaultValue {}).
online_orders DB cols verified (.planning/intel/db-schema-tables.md:992-1029): has client_id, total, payment_method_id, mirror_sale_id, branch_id, metadata jsonb; LACKS prepared_at, dispatched_at, transporte_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 42-02 + OnlineOrder model columns + ship DTO</name>
  <read_first>
    - .planning/intel/db-schema-tables.md:992-1029 (online_orders current columns — confirm prepared_at/dispatched_at/transporte_id absent, confirmed_at/shipped_at/delivered_at present)
    - api-ventago/src/app/online-orders/online-order.model.ts:14-16,53-177 (column block + boot-hang note: transporteId must be plain INTEGER, NO @ForeignKey)
    - api-ventago/src/app/online-orders/dto/ship-online-order.dto.ts (current carrier/trackingCode/shippingLabelUrl fields)
    - api-ventago/migrations/40-01-repartidores.sql (ALTER ADD COLUMN IF NOT EXISTS idiom)
  </read_first>
  <files>api-ventago/migrations/42-02-online-orders-cols.sql, api-ventago/src/app/online-orders/online-order.model.ts, api-ventago/src/app/online-orders/dto/ship-online-order.dto.ts</files>
  <action>
    Create `42-02-online-orders-cols.sql` (runs AFTER 42-01 — FK references transportes):
    ```sql
    BEGIN;
    ALTER TABLE online_orders ADD COLUMN IF NOT EXISTS prepared_at TIMESTAMP WITH TIME ZONE NULL;
    ALTER TABLE online_orders ADD COLUMN IF NOT EXISTS dispatched_at TIMESTAMP WITH TIME ZONE NULL;
    ALTER TABLE online_orders ADD COLUMN IF NOT EXISTS transporte_id INTEGER NULL
      REFERENCES transportes(id) ON DELETE SET NULL;
    CREATE INDEX IF NOT EXISTS idx_online_orders_transporte ON online_orders (transporte_id);
    COMMIT;
    ```
    All nullable → 기존 행 영향 0 (RD-12).
    In online-order.model.ts, after the existing timestamp block, add (matching existing decorator style — `underscored:true` maps camelCase→snake_case automatically):
    ```typescript
    @Column({ type: DataType.DATE, allowNull: true })
    preparedAt: Date | null;
    @Column({ type: DataType.DATE, allowNull: true })
    dispatchedAt: Date | null;
    // transporteId — NO @ForeignKey (boot-hang guard, see L14-16/157-160). DB FK in migration only.
    @Column({ type: DataType.INTEGER, allowNull: true })
    transporteId: number | null;
    ```
    In ship-online-order.dto.ts add `transporteId: @IsInt @IsPositive` (required for retail ship) while keeping the existing `carrier`/`trackingCode`/`shippingLabelUrl` (carrier becomes optional fallback since transporte.name now mirrors). Keep backward compat: existing online ship without transporteId must still typecheck — make transporteId `@IsOptional @IsInt` so legacy callers don't break (RD-12), validity enforced in service for retail use_envios path.
  </action>
  <verify>
    <automated>test -f api-ventago/migrations/42-02-online-orders-cols.sql && grep -q "ADD COLUMN IF NOT EXISTS transporte_id" api-ventago/migrations/42-02-online-orders-cols.sql && grep -q "transporteId" api-ventago/src/app/online-orders/online-order.model.ts && grep -q "transporteId" api-ventago/src/app/online-orders/dto/ship-online-order.dto.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ADD COLUMN IF NOT EXISTS prepared_at" api-ventago/migrations/42-02-online-orders-cols.sql` matches
    - `grep "ADD COLUMN IF NOT EXISTS dispatched_at" api-ventago/migrations/42-02-online-orders-cols.sql` matches
    - `grep "REFERENCES transportes(id) ON DELETE SET NULL" api-ventago/migrations/42-02-online-orders-cols.sql` matches
    - `grep "preparedAt" api-ventago/src/app/online-orders/online-order.model.ts` matches; `grep "@ForeignKey" online-order.model.ts` count unchanged (transporteId has NO @ForeignKey)
    - `grep "transporteId" api-ventago/src/app/online-orders/dto/ship-online-order.dto.ts` matches with @IsOptional
  </acceptance_criteria>
  <done>Migration + model columns + ship DTO transporteId added; transporteId plain INTEGER (no boot-hang).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prepareOrder timestamp + shipOrder transporte + completion gate (shipSaldo + anonymous block) (RD-3/RD-4)</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.service.ts:292-328 (prepareOrder, shipOrder current side-effects)
    - api-ventago/src/app/online-orders/online-orders.service.ts:801-858 (runStatusTx — external tx given to sideEffect)
    - api-ventago/src/app/credit/services/credit-validation.service.ts:42-116 (assertCreditEligible: document required, blocks anonymous)
    - api-ventago/src/app/transportes/transportes.service.ts (findScoped — to resolve transporte.name for D-05 mirror)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md (Pitfall 1/2 lines 301-312; Open Question 1 RESOLVED lines 400-404 — sale_credit accrued at DELIVER, ship only records metadata.shipSaldo)
  </read_first>
  <behavior>
    - Test: prepareOrder sets preparedAt (drives "Listo" derivation, D-03)
    - Test: shipOrder sets transporteId, shippingCarrier=transporte.name, trackingCode, dispatchedAt
    - Test: shipOrder with saldo=0 (fully received) → order.metadata.shipSaldo is NOT set (or 0); assertCreditEligible NOT called
    - Test: shipOrder with saldo>0 and identified credit-eligible client → order.metadata.shipSaldo = saldo recorded; assertCreditEligible called once; NO appendMovement / NO sale_credit row written at ship (deferred to deliver — plan 42-03)
    - Test: shipOrder with saldo>0 and anonymous client (clientId null / no document) → assertCreditEligible throws BadRequestException; order NOT shipped (no state change committed)
    - Test: shipOrder eligibility check + shipSaldo write run inside the same runStatusTx transaction (assertCreditEligible receives t)
  </behavior>
  <files>api-ventago/src/app/online-orders/online-orders.service.ts</files>
  <action>
    1) In `prepareOrder` (L292-301) add a side-effect setting `order.preparedAt = new Date()` (currently no side-effect). This makes "Listo p/ despacho" derivable as preparedAt!=null && dispatchedAt==null (D-03 — NO new enum value, avoids CHECK desync per Pitfall 5).
    2) Extend `shipOrder` (L303-328): resolve transporte via TransportesService.findScoped(dto.transporteId, storeId); inside the existing side-effect set:
       ```
       order.transporteId = dto.transporteId;
       order.shippingCarrier = transporte.name;   // D-05 backward-compat mirror
       order.trackingCode = dto.trackingCode;
       order.dispatchedAt = new Date();
       ```
       Then the completion gate (D-06 — RESOLVED per RESEARCH Open Q1): compute `saldo = Number(order.total) − receivedSoFar`. receivedSoFar = sum of payments recorded so far for this order (read from order.metadata.received or existing payment tracking — confirm the canonical source while reading; if none yet, saldo = total when paymentStatus != 'paid', else 0).
       - If saldo <= 0: record nothing for credit (fully paid → no 외상). Proceed to ship.
       - If saldo > 0 (intent to ship with a shortfall = 외상):
         a) require identified client: if `order.clientId == null` throw BadRequestException('외상 발송은 식별된 고객(DNI/CUIT) 필요') (Pitfall 2 — anonymous cannot take credit).
         b) resolve storeClientId from order.clientId and call `await this.creditValidationService.assertCreditEligible({ storeId, storeClientId, amount: saldo, transaction: t })` (limit/document/credit_status check — fails fast for anonymous/over-limit BEFORE shipping).
         c) record the shortfall INTENT only: `order.metadata = { ...order.metadata, shipSaldo: saldo, shipSaldoStoreClientId: storeClientId, shipSaldoUserId: userId };` — do NOT call appendMovement and do NOT write a sale_credit row here. The mirror saleId required by the sale_credit DB CHECK does not exist until deliver; plan 42-03 reads metadata.shipSaldo and accrues the single sale_credit movement (saleId=mirrorSale.id) inside the deliver tx (external tx). (This is the RESOLVED Pitfall-1 seam — Research Open Q1.)
       Pass `userId` into shipOrder signature (add param; controller updates in Task 4/plan wiring).
    3) DO NOT touch deliverOrder paymentStatus and DO NOT call creditLedgerService.appendMovement(sale_credit) here (both belong to plan 42-03). shipOrder must remain inside its runStatusTx (SERIALIZABLE) — assertCreditEligible receives the injected `t` (Pattern 2). Emit gateway is plan 42-04.
    ESLint: newline-before-return, lines-around-comment.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders.service 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "order.preparedAt = new Date()" api-ventago/src/app/online-orders/online-orders.service.ts` matches
    - `grep "order.dispatchedAt = new Date()" online-orders.service.ts` matches
    - `grep "order.shippingCarrier = transporte.name" online-orders.service.ts` matches
    - `grep "shipSaldo" online-orders.service.ts` matches (intent recorded in metadata at ship)
    - `grep "assertCreditEligible" online-orders.service.ts` matches (anonymous/limit block at ship)
    - shipOrder body does NOT call appendMovement (the sale_credit row is plan 42-03) — verify by code read / `grep "appendMovement" online-orders.service.ts` only appears in the cancel(favor) path of Task 3, not in shipOrder
    - shipOrder throws BadRequestException when clientId null and saldo>0 (covered by jest test)
    - `cd api-ventago && npx jest online-orders.service` exits 0 (RD-3/RD-4 ship-side tests pass)
  </acceptance_criteria>
  <done>prepareOrder sets preparedAt; shipOrder mirrors transporte + records shipSaldo intent and blocks anonymous on saldo>0 (no ledger write at ship); deliver/appendMovement untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: registerCobro (FIFO payment_in + caja) + cancel Devolver/Favor fork (RD-7/RD-6)</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.service.ts:378-428 (cancelOrder wasCommitted branch + nullifyMirror)
    - api-ventago/src/app/credit/services/credit-payment.service.ts:70-138 (registerPayment OWN SERIALIZABLE tx, FIFO)
    - api-ventago/src/app/box-operation/box-operation.service.ts:16-32 (addOperation signature)
    - api-ventago/src/app/rider-settlement/rider-settlement.service.ts:150-228 (closingTime=null cashRegister lookup + addOperation mapping — Phase 40 caja pattern)
    - api-ventago/src/app/credit/services/credit-ledger.service.ts:91 (appendMovement favor_in)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md (Pitfall 3/4, lines 313-322)
  </read_first>
  <behavior>
    - Test: registerCobro(amount, paymentMethodId) with 외상 outstanding → registerPayment called paymentKind 'credit_payment', FIFO payment_in, balance down
    - Test: registerCobro posts box addOperation on open cashRegister (closingTime null)
    - Test: registerCobro when no open caja → throws BadRequestException ('Abrí la caja antes') (Pitfall 4 — strict, Phase 40 parity)
    - Test: registerCobro is NOT wrapped in runStatusTx (registerPayment opens its own tx — Pitfall 3)
    - Test: cancelOrder with action 'devolver' → caja reverse movement (negative addOperation), existing nullifyMirror path preserved
    - Test: cancelOrder with action 'favor' → appendMovement favor_in, StoreClient.favorBalance up, nullifyMirror still called
  </behavior>
  <files>api-ventago/src/app/online-orders/online-orders.service.ts, api-ventago/src/app/online-orders/dto/cobro-online-order.dto.ts</files>
  <action>
    1) Create `dto/cobro-online-order.dto.ts`: CobroOnlineOrderDto with `payments: PaymentLineDto[]` where PaymentLineDto = { paymentMethodId: @IsInt, amount: @IsNumber @IsPositive, optionId?, bankName?, chequeNumber? } (split support: array; Efectivo/Transferencia/Cheque/Tarjeta/QR). @ValidateNested @Type. Add `receiptNo?`.
    2) Add `async registerCobro(storeId, orderId, dto, userId)`:
       - Load order (storeId scoped, findById). Resolve storeClientId from order.clientId (require identified client for 외상 down-payment; new payments without 외상 can still record).
       - Resolve open cashRegister (closingTime=null) for order.branchId via the rider-settlement pattern; if none → throw BadRequestException('Abrí la caja antes de registrar el cobro') (Pitfall 4).
       - For the portion paying down 외상: call `this.creditPaymentService.registerPayment({ storeId, storeClientId, paymentKind:'credit_payment', totalAmount: cobroSum, paymentMethodId, optionId, receiptNo, branchId: order.branchId, userId, note:'Cobro envío #'+order.orderNumber })`. This opens its OWN tx — call it standalone, NOT inside any open tx (Pitfall 3).
       - For each payment line post `this.boxOperationService.addOperation({ cashRegisterId, userId, terminalId, amount, type:'income', executionType:'cobro', description:'Cobro envío #'+order.orderNumber })`.
       - Append a timeline note to order.metadata.timeline_notes (Open Q3 RESOLVED — JSONB array, no new table) and save order outside any nested credit tx.
       - Return updated order.
    3) Extend `cancelOrder` (L378-428): add a `dto.refundAction: 'devolver'|'favor'` param. In the wasCommitted branch, KEEP existing reverseSale + nullifyMirror (RD-12 — do not break). Then:
       - 'devolver': post a negative caja movement (addOperation type 'expense'/'refund', amount = paid) on open cashRegister.
       - 'favor': `appendMovement({ ..., movementType:'favor_in', amount: paid, saleId: order.mirrorSaleId, transaction: t, note:'Favor por cancelación envío #'+order.orderNumber })` inside the existing SERIALIZABLE tx → StoreClient.favorBalance up. favor_in uses bucket=favor. (mirrorSaleId IS present here because we are cancelling a committed/delivered order.)
       - When prevStatus is non-committed (pending/confirmed/preparing) keep the existing releaseHold path; refundAction irrelevant (no payment). Make refundAction optional.
    ESLint compliant.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders.service 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -f api-ventago/src/app/online-orders/dto/cobro-online-order.dto.ts`
    - `grep "async registerCobro" api-ventago/src/app/online-orders/online-orders.service.ts` matches
    - `grep "registerPayment" online-orders.service.ts` matches with `paymentKind: 'credit_payment'`
    - `grep "addOperation" online-orders.service.ts` matches
    - `grep "Abrí la caja" online-orders.service.ts` matches (Pitfall 4 guard)
    - `grep "movementType: 'favor_in'" online-orders.service.ts` matches; `grep "nullifyMirror" online-orders.service.ts` still present (RD-12 preserved)
    - registerCobro body does NOT call this.sequelize.transaction wrapping registerPayment (Pitfall 3) — verify by code read
    - `cd api-ventago && npx jest online-orders.service` exits 0
  </acceptance_criteria>
  <done>registerCobro (split, FIFO, caja, open-caja guard) + cancel Devolver/Favor fork; existing cancel path preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Wire controller routes + module providers (credit/box/transportes)</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.controller.ts (existing ship/deliver/cancel routes — @Auth, @GetUser, storeId)
    - api-ventago/src/app/online-orders/online-orders.module.ts (imports/providers — what's already injected)
    - api-ventago/src/app/repartidores/repartidores.controller.ts (route shape reference)
  </read_first>
  <files>api-ventago/src/app/online-orders/online-orders.controller.ts, api-ventago/src/app/online-orders/online-orders.module.ts</files>
  <action>
    Controller: add routes (all @Auth(), storeId + userId from @GetUser, IDOR-safe):
    - `POST /online-orders/:id/cobro` → service.registerCobro(user.storeId, +id, dto, user.id)
    - extend `PATCH/PUT :id/cancel` (or existing cancel route) to accept CancelOnlineOrderDto { refundAction?, payments? }
    - ensure ship route passes user.id into shipOrder (signature changed in Task 2).
    Module: import the modules exporting CreditLedgerService, CreditPaymentService, CreditValidationService, BoxOperationService, and TransportesModule (exports TransportesService) so they can be injected into OnlineOrdersService. Add to providers/imports as appropriate. Confirm no circular module import (use forwardRef only if NestJS boot errors). Verify boot via tsc.
    ESLint: no-unused-vars on new imports.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "registerCobro" src/app/online-orders/online-orders.controller.ts && grep -q "TransportesModule\|TransportesService" src/app/online-orders/online-orders.module.ts && npx tsc --noEmit 2>&1 | grep -i "online-orders\|cobro\|transporte" | head -10; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cobro" api-ventago/src/app/online-orders/online-orders.controller.ts` matches a route handler calling registerCobro
    - `grep "@GetUser" online-orders.controller.ts` used for storeId+userId on new routes; no storeId from request body
    - `grep "CreditPaymentService\|CreditLedgerService\|BoxOperationService" api-ventago/src/app/online-orders/online-orders.module.ts` matches (modules/providers wired)
    - `cd api-ventago && npx tsc --noEmit` no NEW errors in online-orders
  </acceptance_criteria>
  <done>Cobro/cancel routes exposed; credit/box/transportes services injectable into OnlineOrdersService; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → online-orders ship/cobro/cancel API | Authenticated; storeId/userId server-derived; money + credit cross here |
| service → CreditLedger/StoreClient | Append-only ledger + FOR UPDATE balance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Tampering | ship shortfall intent (shipSaldo) | mitigate | ship records intent only inside SERIALIZABLE tx; the actual append-only sale_credit row (saleId/venta link required) is written at deliver (plan 42-03); never UPDATE/DELETE ledger row |
| T-42-05 | Elevation/Tampering | 외상 limit bypass | mitigate | assertCreditEligible (document + credit_status + limit race lock) enforced at SHIP before recording shipSaldo |
| T-42-06 | Tampering | cobro amount negative/over | mitigate | DTO @IsPositive per line; registerPayment validates totalAmount>0 |
| T-42-07 | Info Disclosure | cross-store order ship/cobro | mitigate | every service method storeId-scoped WHERE; findById(storeId,id) |
| T-42-08 | Repudiation | caja reconciliation mismatch | mitigate | each cobro/refund → addOperation on open cashRegister (closingTime null); strict open-caja guard |
| T-42-09 | Tampering | anonymous order credit | mitigate | shipOrder blocks saldo>0 when clientId null / no document (assertCreditEligible) before any state change (Pitfall 2) |
</threat_model>

<verification>
- `cd api-ventago && npx jest online-orders.service` green (RD-3/4/6/7 behaviors)
- ship records metadata.shipSaldo + enforces assertCreditEligible; NO appendMovement(sale_credit) at ship (that is plan 42-03 deliver)
- registerPayment NOT nested in another tx (Pitfall 3); appendMovement(favor_in) always receives a tx (Pitfall 2)
- cancel existing nullifyMirror path intact (RD-12)
- migration 42-02 nullable cols (no impact on existing rows)
- NOTE: deliver paymentStatus realignment + sale_credit accrual (reading metadata.shipSaldo) + its regression gate are plan 42-03 (do not implement here)
</verification>

<success_criteria>
OnlineOrder augmented with transporteId + stage timestamps; ship applies transporte + completion gate (saldo>0 → record shipSaldo intent + assertCreditEligible block, anonymous rejected; fully-paid → no intent recorded); cobro pays down 외상 (FIFO + caja, open-caja guard); cancel offers Devolver/Favor without breaking existing cancel. All accounting via existing credit/box services. The sale_credit ledger row itself is accrued at deliver (plan 42-03).
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-02-SUMMARY.md`. Record that ship sets metadata.shipSaldo (intent) and that the sale_credit ledger row is accrued at deliver (plan 42-03) keyed off shipSaldo — this is the resolved Pitfall-1 seam, not an open decision.
</output>
