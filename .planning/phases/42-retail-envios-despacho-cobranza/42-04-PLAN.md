---
phase: 42-retail-envios-despacho-cobranza
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - api-ventago/src/app/online-orders/online-orders-board.gateway.ts
  - api-ventago/src/app/online-orders/online-orders.service.ts
  - api-ventago/src/app/online-orders/online-orders.controller.ts
  - api-ventago/src/app/online-orders/online-orders.module.ts
  - api-ventago/src/app/online-orders/dto/nota-online-order.dto.ts
  - api-ventago/src/app/online-orders/online-orders-board.gateway.spec.ts
autonomous: true
requirements: [RD-2, RD-7, RD-9]
must_haves:
  truths:
    - "A new /envios Socket.io gateway authenticates the handshake JWT and disconnects on failure"
    - "join is authorized only when the branch belongs to the user's store (branch room IDOR guard)"
    - "Every despacho transition (prepare/ship/deliver/cancel) and cobro emits envio_updated with the changed card AFTER commit"
    - "A board endpoint returns despacho cards scoped to store+branch for the kanban"
    - "GET /online-orders/cuentas-por-cobrar returns saldo>0 orders + per-client StoreClient.balance, storeId-scoped (backend data source for the Cuentas por cobrar tab)"
    - "GET /online-orders/:id is auth-scoped (JWT + storeId WHERE) and returns the merged timeline event set (status timestamps + SalePaymentMethod + CreditLedger + metadata.timeline_notes)"
    - "POST /online-orders/:id/nota appends a note to metadata.timeline_notes, storeId-scoped"
  artifacts:
    - path: "api-ventago/src/app/online-orders/online-orders-board.gateway.ts"
      provides: "OnlineOrdersBoardGateway namespace /envios, emitEnvioUpdated"
      contains: "namespace: '/envios'"
    - path: "api-ventago/src/app/online-orders/online-orders.controller.ts"
      provides: "GET /online-orders/board/:branchId, GET /online-orders/cuentas-por-cobrar, GET /online-orders/:id (auth-scoped merged timeline), POST /online-orders/:id/nota"
      contains: "cuentas-por-cobrar"
  key_links:
    - from: "online-orders.service transitions"
      to: "boardGateway.emitEnvioUpdated"
      via: "after t.commit (post-commit)"
      pattern: "emitEnvioUpdated"
    - from: "gateway join"
      to: "branch store ownership check"
      via: "branchModel lookup by user's storeId"
      pattern: "branch:"
    - from: "online-orders.controller GET :id"
      to: "service.getOrderTimeline (storeId-scoped)"
      via: "merge status timestamps + payments + credit events + notes"
      pattern: "getOrderTimeline"
    - from: "online-orders.controller GET cuentas-por-cobrar"
      to: "service.getCuentasPorCobrar (storeId-scoped)"
      via: "saldo>0 orders + StoreClient.balance"
      pattern: "getCuentasPorCobrar"
---

<objective>
Add real-time push + the read-side backend for the Despacho control center: a NEW `/envios` Socket.io gateway (domain separation from `/print-agent` and `/restaurant` per Research Open Q2 RESOLVED) cloned from the Phase 40 RestaurantDeliveryGateway, the board fetch endpoint, post-commit `envio_updated` emits on every transition/cobro, AND the three read/write routes the frontend tabs/timeline depend on: cuentas-por-cobrar (RD-7 data source), the auth-scoped merged-timeline detail (GET :id), and the Nota-append route.

Purpose: RD-9 (Socket.io push, no polling, card-level payload, pool 절약), RD-2 (board card data for the kanban), RD-7 (cuentas-por-cobrar backend data source — owned here, NOT a parenthetical in a frontend plan). Domain-separated gateway keeps retail-delivery cards isolated from restaurant cards. The merged-timeline GET :id and Nota route are the concrete backend routes plan 42-07 consumes (no "confirm"/"falls back" hedging downstream). GET :id is auth-scoped to address backend memory note I-1 (prior unauthenticated-PII concern on GET /online-orders/:id).
Output: OnlineOrdersBoardGateway + board endpoint + emit wiring + cuentas-por-cobrar + merged-timeline GET :id (auth-scoped) + nota route + gateway spec.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md

<interfaces>
Analog: api-ventago/src/app/restaurant-delivery/restaurant-delivery.gateway.ts (99 lines):
- L20 @WebSocketGateway({ namespace: '/restaurant', cors }) → change to '/envios'
- L32-58 handleConnection: handshake.auth.token → jwtService.verify(token, {secret: process.env.JWT_SECRET_KEY}) → disconnect on fail; store client.data.storeId
- L62-89 @SubscribeMessage('join'): branchModel.findOne({ where:{ id:branchId, storeId: client.data.storeId }}) → if missing disconnect/ignore (IDOR); client.join(`branch:${branchId}`)
- L92 emitDeliveryUpdated(branchId, card) → emitEnvioUpdated, event 'envio_updated'
DI: JwtModule + @InjectModel(Branch) (see restaurant-delivery.module.ts wiring).

emit-after-commit pattern: restaurant-delivery.service.ts:180 — call gateway emit AFTER the returned promise resolves (runStatusTx returns post-commit at L845), never inside the tx.

Card shape (toCard): id, orderNumber, channel, clientName, address, total, paymentStatus, saldo, transporte name + tracking (if shipped), columnKey. Derive "listo" from preparedAt!=null && dispatchedAt==null.

Read-side sources:
- CreditLedger rows for an order's storeClient (movement_type sale_credit/payment_in/favor_in) → credit events (credit-ledger.model.ts).
- SalePaymentMethod rows on the mirror sale → payment events.
- order.metadata.timeline_notes (JSONB array) → Nota events (Research Open Q3 RESOLVED).
- StoreClient.balance → per-client outstanding (store-clients.model.ts).
- Existing GET /online-orders/:id (if present): confirm @Auth + storeId scope; if missing/unscoped, ADD the guard (memory I-1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone OnlineOrdersBoardGateway (/envios) with JWT + branch room IDOR guard</name>
  <read_first>
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.gateway.ts (full — handshake JWT L32-58, join IDOR L62-89, emit L92)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.module.ts (DI: JwtModule, Branch model injection)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.service.spec.ts:62 (gateway mock pattern for spec)
  </read_first>
  <behavior>
    - Test: handleConnection with invalid/missing token → socket.disconnect called
    - Test: handleConnection with valid token → stores storeId on client.data
    - Test: join with branch belonging to user's store → client.join('branch:'+branchId)
    - Test: join with branch of another store → no join (IDOR guard), nothing leaked
    - Test: emitEnvioUpdated(branchId, card) → server.to('branch:'+branchId).emit('envio_updated', card)
  </behavior>
  <files>api-ventago/src/app/online-orders/online-orders-board.gateway.ts, api-ventago/src/app/online-orders/online-orders-board.gateway.spec.ts</files>
  <action>
    Clone restaurant-delivery.gateway.ts into online-orders-board.gateway.ts near-verbatim. Change namespace to '/envios', class to OnlineOrdersBoardGateway, event to 'envio_updated', method to emitEnvioUpdated(branchId, card). Keep: handshake.auth.token JWT verify with process.env.JWT_SECRET_KEY (disconnect on fail), client.data.storeId storage, @SubscribeMessage('join') with branch-belongs-to-store IDOR guard, client.join(`branch:${branchId}`). DI: JwtService + @InjectModel(Branch). Clone the spec covering the 5 behaviors from the restaurant-delivery gateway/service spec mock pattern. ESLint compliant.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "namespace: '/envios'" src/app/online-orders/online-orders-board.gateway.ts && npx jest online-orders-board.gateway 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "namespace: '/envios'" api-ventago/src/app/online-orders/online-orders-board.gateway.ts` matches
    - `grep "envio_updated" online-orders-board.gateway.ts` matches
    - `grep "branch:" online-orders-board.gateway.ts` matches (room join)
    - `grep "verify" online-orders-board.gateway.ts` matches (handshake JWT auth)
    - branch-of-other-store join test asserts NO join (IDOR)
    - `cd api-ventago && npx jest online-orders-board.gateway` exits 0
  </acceptance_criteria>
  <done>/envios gateway with JWT handshake + branch room IDOR guard + emitEnvioUpdated; spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Board endpoint + toCard helper + post-commit emits</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.service.ts:273-428 (prepare/ship/deliver/cancel) + :801-858 (runStatusTx returns post-commit)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.service.ts:166-186 (emit-after-commit + toCard shape)
    - api-ventago/src/app/online-orders/online-orders.controller.ts (route style, @Auth @GetUser)
    - ventago-app/src/views/restaurante/DeliveryBoard.tsx (card fields the board consumes — align toCard)
  </read_first>
  <files>api-ventago/src/app/online-orders/online-orders.service.ts, api-ventago/src/app/online-orders/online-orders.controller.ts</files>
  <action>
    1) Add `private toCard(order): EnvioCard` building { id, orderNumber, channel, clientName, address, total, paymentStatus, saldo, transporteName (shippingCarrier), trackingCode, columnKey } where columnKey derives: pending/confirmed→nuevo; preparing && preparedAt==null→preparando; preparedAt!=null && dispatchedAt==null→listo; shipped→en_transito; delivered→entregado (D-03 derivation). saldo derives from total − received (use metadata.shipSaldo when set, else total−received).
    2) After each transition method's runStatusTx returns (post-commit), call `this.boardGateway.emitEnvioUpdated(order.branchId, this.toCard(order))`. Apply to confirmOrder, prepareOrder, shipOrder, deliverOrder, cancelOrder, AND registerCobro (after cobro completes). NEVER emit inside the tx (Pattern 4). Inject OnlineOrdersBoardGateway into the service constructor.
    3) Add `GET /online-orders/board/:branchId` controller route (@Auth, storeId from @GetUser): service.getBoard(storeId, branchId) returns EnvioCard[] for active despacho statuses (exclude cancelled/returned) scoped to store+branch. pageSize≤50 if paginated.
    ESLint: no-unused-vars on gateway import.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "emitEnvioUpdated" src/app/online-orders/online-orders.service.ts && grep -q "toCard" src/app/online-orders/online-orders.service.ts && grep -q "board" src/app/online-orders/online-orders.controller.ts && npx jest online-orders.service 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "private toCard" api-ventago/src/app/online-orders/online-orders.service.ts` matches
    - `grep "emitEnvioUpdated" online-orders.service.ts` matches in transition methods (post-commit)
    - `grep "columnKey" online-orders.service.ts` matches with listo derivation (preparedAt && !dispatchedAt)
    - `grep "board/:branchId\|board" api-ventago/src/app/online-orders/online-orders.controller.ts` matches GET route
    - `cd api-ventago && npx jest online-orders.service` exits 0
  </acceptance_criteria>
  <done>toCard column derivation + post-commit emits on all transitions+cobro + board endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Read-side routes — cuentas-por-cobrar (RD-7) + auth-scoped merged-timeline GET :id (I-1) + Nota-append</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.controller.ts (existing :id detail route — confirm @Auth + storeId scope; if absent, this is the I-1 fix)
    - api-ventago/src/app/online-orders/online-orders.service.ts (findById storeId-scoped pattern; metadata access)
    - api-ventago/src/app/credit/credit-ledger.model.ts (CreditLedger movement rows for an order's storeClient — sale_credit/payment_in/favor_in)
    - api-ventago/src/app/shared/store-clients/store-clients.model.ts (StoreClient.balance — per-client outstanding)
    - api-ventago/src/app/sales (SalePaymentMethod model — payment rows on the mirror sale)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:412-416 (Open Q3 RESOLVED — metadata.timeline_notes JSONB; routes owned in backend plan)
  </read_first>
  <files>api-ventago/src/app/online-orders/online-orders.controller.ts, api-ventago/src/app/online-orders/online-orders.service.ts, api-ventago/src/app/online-orders/dto/nota-online-order.dto.ts</files>
  <action>
    1) `GET /online-orders/cuentas-por-cobrar` (@Auth, storeId from @GetUser) → `service.getCuentasPorCobrar(storeId)`:
       - Returns saldo>0 orders only, storeId-scoped WHERE. Each row: { orderId, orderNumber, clientId, clientName, total, recibido, saldo, branchId }.
       - Per-client outstanding: include StoreClient.balance for the row's client (join store_clients on clientId, storeId-scoped). Also return an aggregate `totalSaldo`.
       - saldo source-of-truth: prefer order.metadata.shipSaldo when set; otherwise total − received. Do NOT compute credit balances by hand beyond display — StoreClient.balance is the authoritative per-client figure (credit module owns it).
       - pageSize≤50 if paginated. This is the backend data source for the Cuentas por cobrar tab (RD-7) — owned HERE.
    2) `GET /online-orders/:id` (@Auth, storeId from @GetUser) → `service.getOrderTimeline(storeId, id)`:
       - FIRST ensure auth-scoping: load via findById(storeId, id) with storeId in the WHERE (404 if not in caller's store). If a legacy unscoped/unauthenticated :id route exists, REPLACE it with this guarded handler (addresses backend memory I-1 — prior unauthenticated-PII concern). Confirm @Auth() decorator present on the route.
       - Return the order detail + a merged, chronologically-mergeable timeline event payload: status timestamps (createdAt/orderedAt, preparedAt, dispatchedAt, deliveredAt, cancelledAt), payment events (SalePaymentMethod rows on the mirror sale), credit events (CreditLedger sale_credit/payment_in/favor_in for the order's storeClient + this order/mirror), and metadata.timeline_notes. Shape it so the frontend (42-07) can render directly — e.g. `{ order, events: [{ type, at, label, amount?, method? }], saldo }`. (Frontend sorts/renders; backend supplies the event set.)
    3) `POST /online-orders/:id/nota` (@Auth, storeId from @GetUser) → `service.appendNota(storeId, id, dto, userId)`:
       - DTO `nota-online-order.dto.ts`: NotaOnlineOrderDto { text: @IsString @IsNotEmpty @MaxLength(1000) }.
       - Load order storeId-scoped, push `{ text, at: new Date().toISOString(), userId }` onto order.metadata.timeline_notes (init to [] if absent — JSONB array, no new table per Open Q3). Save. Emit envio_updated post-save (board card unaffected but timeline refresh consumers may listen) — optional, keep if cheap.
    ESLint: newline-before-return, lines-around-comment, no-unused-vars.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "cuentas-por-cobrar" src/app/online-orders/online-orders.controller.ts && grep -q "getCuentasPorCobrar" src/app/online-orders/online-orders.service.ts && grep -q "getOrderTimeline" src/app/online-orders/online-orders.service.ts && grep -q "nota" src/app/online-orders/online-orders.controller.ts && test -f src/app/online-orders/dto/nota-online-order.dto.ts && npx tsc --noEmit 2>&1 | grep -i "online-orders\|cuentas\|nota" | head -10; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cuentas-por-cobrar" api-ventago/src/app/online-orders/online-orders.controller.ts` matches a @Auth GET route calling getCuentasPorCobrar
    - `grep "getCuentasPorCobrar" online-orders.service.ts` matches; returns saldo>0 + StoreClient.balance, storeId-scoped (verify by code read)
    - `grep "getOrderTimeline" online-orders.service.ts` matches; GET :id handler is @Auth and findById is storeId-scoped (I-1 addressed) — verify @Auth present on the :id route
    - `grep "timeline_notes" online-orders.service.ts` matches (Nota append + timeline merge)
    - `test -f api-ventago/src/app/online-orders/dto/nota-online-order.dto.ts` and `grep "nota" online-orders.controller.ts` matches POST route
    - `cd api-ventago && npx tsc --noEmit` no NEW errors in online-orders
  </acceptance_criteria>
  <done>cuentas-por-cobrar (RD-7 data source), auth-scoped merged-timeline GET :id (I-1 fix), and Nota-append routes owned here; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Register gateway provider + JwtModule/Branch DI in online-orders.module</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.module.ts (current imports/providers — credit/box added in 42-02)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.module.ts (JwtModule + SequelizeModule.forFeature([Branch]) wiring for gateway)
  </read_first>
  <files>api-ventago/src/app/online-orders/online-orders.module.ts</files>
  <action>
    Add OnlineOrdersBoardGateway to providers (and exports if any other module needs emit). Import JwtModule (register or import the shared one — mirror restaurant-delivery.module) and SequelizeModule.forFeature([Branch]) so @InjectModel(Branch) resolves in the gateway. Confirm app boots (tsc + DI). Watch for circular deps — gateway depends only on JwtService + Branch model, service depends on gateway (one-directional, no forwardRef needed). Also ensure CreditLedger/StoreClient/SalePaymentMethod models needed by getCuentasPorCobrar/getOrderTimeline (Task 3) are available via the credit/store-clients/sales modules already imported in 42-02 Task 4 (add forFeature if a model is read directly).
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "OnlineOrdersBoardGateway" src/app/online-orders/online-orders.module.ts && grep -q "JwtModule\|Branch" src/app/online-orders/online-orders.module.ts && npx tsc --noEmit 2>&1 | grep -i "online-orders\|gateway\|branch" | head -10; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "OnlineOrdersBoardGateway" api-ventago/src/app/online-orders/online-orders.module.ts` matches in providers
    - `grep "JwtModule" online-orders.module.ts` matches; `grep "Branch" online-orders.module.ts` matches (forFeature)
    - `cd api-ventago && npx tsc --noEmit` no NEW errors in online-orders
  </acceptance_criteria>
  <done>Gateway provider + JwtModule + Branch model wired; models for read-side routes available; tsc clean, no circular dep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket client → /envios gateway | Untrusted handshake; JWT verified; branch room subscription |
| client → GET :id / cuentas-por-cobrar / nota | Authenticated; storeId server-derived; PII (client name/address) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Spoofing | gateway handshake | mitigate | JWT verify(JWT_SECRET_KEY) on connect; disconnect on fail |
| T-42-14 | Info Disclosure | branch room join | mitigate | join only if branch.storeId == client.data.storeId (IDOR guard); cards never broadcast cross-store |
| T-42-15 | Info Disclosure | board endpoint | mitigate | getBoard storeId-scoped WHERE + branchId param validated against store |
| T-42-18 | Info Disclosure | GET /online-orders/:id PII | mitigate | @Auth + findById(storeId,id) storeId-scoped (addresses memory I-1 prior unauthenticated-PII concern); 404 if cross-store |
| T-42-19 | Info Disclosure | cuentas-por-cobrar 외상 data | mitigate | getCuentasPorCobrar storeId-scoped WHERE; StoreClient join storeId-scoped |
| T-42-25 | Tampering | nota append | mitigate | @Auth + storeId-scoped order load; DTO @MaxLength; append-only to JSONB array |
</threat_model>

<verification>
- `cd api-ventago && npx jest online-orders-board.gateway online-orders.service` green
- /envios namespace, envio_updated event, branch room IDOR guard present
- emits are post-commit only (never inside tx)
- cuentas-por-cobrar returns saldo>0 + StoreClient.balance (RD-7 data source owned here)
- GET :id is @Auth + storeId-scoped (I-1) and returns merged timeline event set
- POST :id/nota appends to metadata.timeline_notes
- module boots (tsc clean, no circular dep)
</verification>

<success_criteria>
RD-9/RD-2/RD-7: /envios gateway authenticates + scopes by branch room, board endpoint serves kanban cards, every transition+cobro pushes the changed card post-commit (no polling), cuentas-por-cobrar serves the 외상 tab data (saldo>0 + per-client balance), GET :id serves the auth-scoped merged timeline (I-1 closed), and the Nota route appends to metadata.timeline_notes — all concrete routes plans 42-05/07/08 consume.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-04-SUMMARY.md`. List the exact routes added (board/:branchId, cuentas-por-cobrar, GET :id merged-timeline, POST :id/nota) so the frontend plans reference them as existing.
</output>
