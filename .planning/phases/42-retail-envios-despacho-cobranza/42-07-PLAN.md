---
phase: 42-retail-envios-despacho-cobranza
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - ventago-app/src/views/ventas-online/components/EnvioTimeline.tsx
  - ventago-app/src/views/ventas-online/components/CobroModal.tsx
  - ventago-app/src/views/ventas-online/components/NuevoEnvioModal.tsx
  - ventago-app/src/views/ventas-online/DespachoBoard.tsx
autonomous: false
requirements: [RD-5, RD-6, RD-11]
must_haves:
  truths:
    - "Selecting a card shows a timeline: header (order/client/channel/address + red Saldo pendiente if any), chronological events (Pedido creado → Pago → Preparación → Nota → Despachado → Entregado → Pendiente de cobro)"
    - "Timeline has 🎫 Ticket / 🧾 Recibo (print-agent) and 📝 Nota (internal note → timeline) actions"
    - "Registrar cobro opens a partial/split modal (Efectivo/Transferencia/Cheque/Tarjeta/QR), posts cobro, decrements saldo"
    - "Cancelar pedido (after payment) offers Devolver dinero vs Pasar a favor"
    - "+Nuevo envío console creates an order (channel/client autocomplete/address/item picker)"
  artifacts:
    - path: "ventago-app/src/views/ventas-online/components/EnvioTimeline.tsx"
      provides: "master-detail timeline panel + action buttons"
      contains: "Recibo"
    - path: "ventago-app/src/views/ventas-online/components/CobroModal.tsx"
      provides: "split/partial cobro modal (props-injected, not useSaleProducts)"
      contains: "saldoPendiente"
    - path: "ventago-app/src/views/ventas-online/components/NuevoEnvioModal.tsx"
      provides: "new order console"
      contains: "NuevoEnvioModal"
  key_links:
    - from: "CobroModal"
      to: "/online-orders/:id/cobro"
      via: "apiConnector.post"
      pattern: "/cobro"
    - from: "EnvioTimeline"
      to: "CobroModal + cancel fork"
      via: "Registrar cobro / Cancelar pedido buttons"
      pattern: "Registrar cobro"
---

<objective>
Build the timeline detail panel (right ~25%), the delivery-specific CobroModal (split/partial — NOT a direct PaymentSummaryModal reuse, since that's POS-coupled), and the +Nuevo envío console. Wire them into DespachoBoard.

Purpose: RD-5 (cobro partial/split → saldo decrement + caja), RD-6 (cancel Devolver/Favor), RD-11 (Ticket/Recibo print-agent). The timeline merges order timestamps + payment events chronologically (D-12 / spec §5.3).
Output: EnvioTimeline, CobroModal, NuevoEnvioModal, DespachoBoard wiring.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md

<interfaces>
Analogs:
- ventago-app/src/views/homes/components/ProductList/components/PaymentSummaryModal.tsx (~L57 useSaleProducts coupling — NOT directly reusable; reuse split UI layout, payment-method row, "Aplicar saldo a favor" chip, credito slug handling; inject deps via props)
- ventago-app/src/views/restaurante/components/NuevoPedidoModal/OrderModal (Phase 40 nuevo pedido — channel/client/address/item picker) for NuevoEnvioModal
- Phase 32 stocks-historial-drawer (right-panel master-detail pattern; locate via glob) for EnvioTimeline layout
- restaurant-delivery.service.ts:166 (emitPrintTemp fire-and-forget) — Ticket/Recibo print path

Backend (plans 42-02/04): POST /online-orders/:id/cobro (CobroOnlineOrderDto { payments:[{paymentMethodId,amount,optionId?,bankName?,chequeNumber?}], receiptNo? }); cancel route with { refundAction: 'devolver'|'favor' }; ship route; GET /online-orders/:id (order detail for timeline). Print: POST /print/qr or print endpoint for ticket/recibo (confirm route while reading print.service).
PaymentMethod list: existing store payment methods (Efectivo/Transferencia/Cheque/Tarjeta/QR).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CobroModal (split/partial, props-injected)</name>
  <read_first>
    - ventago-app/src/views/homes/components/ProductList/components/PaymentSummaryModal.tsx (split UI layout, payment-method rows, "Aplicar saldo a favor" chip, credito slug handling, getSaldoPendiente — but note useSaleProducts coupling ~L57)
    - ventago-app/src/hooks/api (payment-methods hook if present) for method list
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:368-377 (D-09 reuse-vs-wrapper conclusion)
  </read_first>
  <files>ventago-app/src/views/ventas-online/components/CobroModal.tsx</files>
  <action>
    Build a delivery-specific CobroModal. Props (no useSaleProducts): `{ open, onClose, orderId, saldoPendiente, paymentMethods, favorBalance, onCobroDone }`.
    - Render split rows: add multiple payment lines, each { paymentMethod (Efectivo/Transferencia/Cheque(banco+número)/Tarjeta/QR), amount }. Sum shown vs saldoPendiente; allow partial (remaining stays 외상).
    - "Aplicar saldo a favor" chip if favorBalance>0 (reuse pattern).
    - On submit: `apiConnector.post('/online-orders/'+orderId+'/cobro', { payments, receiptNo })` then onCobroDone() (which mutates board + timeline). Double error surface (inline Alert + toast.error).
    - Reuse the visual split layout/rows from PaymentSummaryModal but DO NOT import useSaleProducts; everything comes via props.
    ESLint compliant. Navy+gold theme.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "saldoPendiente" src/views/ventas-online/components/CobroModal.tsx && grep -q "/cobro" src/views/ventas-online/components/CobroModal.tsx && ! grep -q "useSaleProducts" src/views/ventas-online/components/CobroModal.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "saldoPendiente" ventago-app/src/views/ventas-online/components/CobroModal.tsx` matches (prop)
    - `grep "useSaleProducts" CobroModal.tsx` returns nothing (decoupled per D-09)
    - `grep "apiConnector.post" CobroModal.tsx` with `/cobro` matches
    - payment methods include Cheque with banco/número fields (split support)
    - `grep "Alert" CobroModal.tsx` and `grep "toast" CobroModal.tsx` both match
  </acceptance_criteria>
  <done>CobroModal: split/partial, props-injected (no POS coupling), posts cobro, double error surface.</done>
</task>

<task type="auto">
  <name>Task 2: EnvioTimeline panel (merged events + Ticket/Recibo/Nota + cancel fork)</name>
  <read_first>
    - Phase 32 stocks-historial-drawer view (right-panel master-detail layout — glob `ventago-app/src/views/**/Stocks*Drawer*` or stocks-historial)
    - ventago-app/src/views/ventas-online/DespachoBoard.tsx (selectedCardId / panel slot from plan 42-06)
    - api-ventago/src/app/online-orders/online-orders.controller.ts (GET :id detail route; cancel route)
    - api-ventago/src/app/print/print.service.ts (emitPrintTemp ticket/recibo route)
  </read_first>
  <files>ventago-app/src/views/ventas-online/components/EnvioTimeline.tsx</files>
  <action>
    Build EnvioTimeline(props: { orderId, onChanged }):
    - Fetch order detail (GET /online-orders/:id) — include status timestamps (orderedAt=createdAt, preparedAt, dispatchedAt, deliveredAt), payments (SalePaymentMethod), credit events (CreditLedger), metadata.timeline_notes.
    - Header: order#, client, channel, address; red `Saldo pendiente $X` badge if saldo>0.
    - Action buttons: 🎫 Ticket (POST print ticket/packing-slip via print endpoint), 🧾 Recibo (POST print recibo), 📝 Nota (input → append to metadata.timeline_notes via a PATCH/note endpoint or the cobro/note route — confirm; falls back to a notes endpoint). All print = fire-and-forget (no-op if no agent — existing behavior, not a regression).
    - Timeline (chronological merge): Pedido creado(createdAt) → Pago $X(수단, from payments) → Preparación completada(preparedAt) → Nota(s) → Despachado(dispatchedAt, transporte, ⚠ saldo if any) → Entregado(deliveredAt) → Pendiente de cobro (if saldo>0). Sort all events by timestamp.
    - "Registrar cobro" button → opens CobroModal (pass saldoPendiente, paymentMethods, favorBalance). On done → onChanged() to refresh.
    - "Cancelar pedido" (when paid) → confirm dialog with two choices: 💸 Devolver dinero / 🎟️ Pasar a favor → POST cancel with refundAction. On done → onChanged().
    ESLint compliant. Navy+gold.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "Recibo" src/views/ventas-online/components/EnvioTimeline.tsx && grep -q "Registrar cobro" src/views/ventas-online/components/EnvioTimeline.tsx && grep -q "Pasar a favor\|favor" src/views/ventas-online/components/EnvioTimeline.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Saldo pendiente" ventago-app/src/views/ventas-online/components/EnvioTimeline.tsx` matches (red header badge)
    - `grep "Ticket" EnvioTimeline.tsx`, `grep "Recibo" EnvioTimeline.tsx`, `grep "Nota" EnvioTimeline.tsx` all match (action buttons)
    - `grep "Registrar cobro" EnvioTimeline.tsx` matches (opens CobroModal)
    - `grep "Devolver\|Pasar a favor" EnvioTimeline.tsx` matches (cancel fork)
    - timeline merges createdAt/preparedAt/dispatchedAt/deliveredAt + payment events
  </acceptance_criteria>
  <done>EnvioTimeline: merged chronological events, Ticket/Recibo/Nota, cobro trigger, cancel Devolver/Favor fork.</done>
</task>

<task type="auto">
  <name>Task 3: NuevoEnvioModal + wire timeline+modals into DespachoBoard</name>
  <read_first>
    - ventago-app/src/views/restaurante/components (Phase 40 NuevoPedidoModal/OrderModal — channel/client autocomplete/address/item picker)
    - ventago-app/src/views/ventas-online/DespachoBoard.tsx (panel slot + selectedCardId from plan 42-06)
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (if "+Nuevo envío" button placement belongs at page level)
  </read_first>
  <files>ventago-app/src/views/ventas-online/components/NuevoEnvioModal.tsx, ventago-app/src/views/ventas-online/DespachoBoard.tsx</files>
  <action>
    1) NuevoEnvioModal: clone Phase 40 Nuevo pedido console — channel select (Web/WhatsApp/Teléfono/MercadoLibre/Instagram), client autocomplete (existing clients hook) + address, item picker (reuse existing product picker), optional initial cobro. On submit: POST /online-orders (existing create route) → order enters board (socket emit handles appearance). Document the nueva-venta "envío 필요" auto-inflow connection point (note where an order created elsewhere lands on the board — the board already shows all active online orders, so creation via any path appears).
    2) Wire into DespachoBoard: replace the plan-42-06 placeholder panel with `<EnvioTimeline orderId={selectedCardId} onChanged={() => mutate()} />`. Add a "+Nuevo envío" button opening NuevoEnvioModal. Ensure card-click selection drives the timeline.
    ESLint compliant.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "NuevoEnvioModal" src/views/ventas-online/components/NuevoEnvioModal.tsx && grep -q "EnvioTimeline" src/views/ventas-online/DespachoBoard.tsx && grep -q "Nuevo envío" src/views/ventas-online/DespachoBoard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ventago-app/src/views/ventas-online/components/NuevoEnvioModal.tsx`
    - `grep "EnvioTimeline" ventago-app/src/views/ventas-online/DespachoBoard.tsx` matches (placeholder replaced)
    - `grep "Nuevo envío" DespachoBoard.tsx` matches (button)
    - NuevoEnvioModal POSTs to /online-orders create route
  </acceptance_criteria>
  <done>NuevoEnvioModal console + DespachoBoard wired to real EnvioTimeline + Nuevo envío button.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser UAT — cobro/cancel/timeline/print flows</name>
  <what-built>EnvioTimeline detail panel + split CobroModal + cancel Devolver/Favor fork + NuevoEnvioModal, wired into DespachoBoard.</what-built>
  <files>ventago-app/src/views/ventas-online/components/EnvioTimeline.tsx, ventago-app/src/views/ventas-online/components/CobroModal.tsx, ventago-app/src/views/ventas-online/components/NuevoEnvioModal.tsx, ventago-app/src/views/ventas-online/DespachoBoard.tsx</files>
  <action>Human verification AFTER the automated component build. Run the dev stack and walk the cobro/cancel/timeline/print flows below; this checkpoint confirms money flows (saldo decrement, favor, caja reconciliation) that require live data.</action>
  <how-to-verify>
    1) `./dev.sh`, use_envios store. Open Ventas Online → Despacho. Click a card → timeline panel shows header + chronological events.
    2) Ship a partial-paid order with saldo>0 → card shows red Saldo. Click → Registrar cobro → split (e.g. Efectivo + Cheque) partial → confirm saldo decrements, timeline gets Pago event, card updates in real time.
    3) Pay the rest → saldo 0 → order leaves Cuentas por cobrar (verify in plan 42-08 tab).
    4) Cancel a paid order → choose Pasar a favor → verify favorBalance increases (client). Then test Devolver dinero on another → caja reverse.
    5) 🎫 Ticket / 🧾 Recibo → if print-agent installed, output prints; otherwise no-op (no error).
    6) +Nuevo envío → create an order → appears on board.
    7) control-de-caja: confirm cobro caja movements reconcile.
  </how-to-verify>
  <verify>
    <automated>cd ventago-app && npx next lint --file src/views/ventas-online/components/CobroModal.tsx --file src/views/ventas-online/components/EnvioTimeline.tsx --file src/views/ventas-online/components/NuevoEnvioModal.tsx 2>&1 | tail -10; echo "lint-done"</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues (cobro not decrementing / favor not applied / caja mismatch).</resume-signal>
  <done>Human confirms: split/partial cobro decrements saldo + posts caja movement, cancel→favor raises favorBalance, cancel→devolver reverses caja, Ticket/Recibo print (or no-op), +Nuevo envío creates a board order, control-de-caja reconciles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cobro/cancel → API | Money operations; amounts validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-20 | Tampering | cobro amount | mitigate | server DTO @IsPositive + registerPayment validation; client UI is convenience only |
| T-42-21 | Tampering | cancel refund/favor | mitigate | server decides accounting (appendMovement favor_in / caja reverse); client only chooses action |
| T-42-22 | Info Disclosure | order detail fetch | mitigate | GET /online-orders/:id storeId-scoped (note backend memory I-1: ensure auth on detail route) |
</threat_model>

<verification>
- `cd ventago-app && npx next lint` clean on touched files
- CobroModal props-injected (no useSaleProducts), posts /cobro
- EnvioTimeline merged events + Ticket/Recibo/Nota + cancel fork
- NuevoEnvioModal creates orders; DespachoBoard wired
- Human UAT: cobro decrements + favor applied + caja reconciles
</verification>

<success_criteria>
RD-5/RD-6/RD-11: timeline-driven cobro (split/partial → saldo down + caja), cancel Devolver/Favor, Ticket/Recibo print, +Nuevo envío console — all wired into the master-detail board.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-07-SUMMARY.md`
</output>
