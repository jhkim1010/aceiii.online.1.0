---
phase: 42-retail-envios-despacho-cobranza
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - ventago-app/src/views/ventas-online/VentasOnlineView.tsx
  - ventago-app/src/views/ventas-online/DespachoBoard.tsx
autonomous: false
requirements: [RD-2, RD-9, RD-12]
must_haves:
  truths:
    - "For use_envios=true stores, Ventas Online shows 3 tabs: Despacho / Cuentas por cobrar / Historial"
    - "For use_envios=false stores, the legacy Ventas Online tabs keep working (no regression)"
    - "Despacho board shows kanban columns (Nuevo/Preparando/Listo p/ despacho/En tránsito/Entregado) with per-column count badges and vertically stacked cards"
    - "Board updates in real time via /envios socket envio_updated (no polling) and merges cards by id"
    - "Master-detail layout: board ~75% left, timeline panel ~25% right, selecting a card shows its detail"
  artifacts:
    - path: "ventago-app/src/views/ventas-online/DespachoBoard.tsx"
      provides: "kanban + socket subscription + master-detail shell"
      contains: "/envios"
    - path: "ventago-app/src/views/ventas-online/VentasOnlineView.tsx"
      provides: "3-tab upgrade gated by useEnvios"
      contains: "Despacho"
  key_links:
    - from: "DespachoBoard"
      to: "/envios socket"
      via: "io(WS_HOST, auth token).on('envio_updated')"
      pattern: "envio_updated"
    - from: "DespachoBoard"
      to: "useDespachoBoard hook"
      via: "card data + mutate merge"
      pattern: "useDespachoBoard"
---

<objective>
Upgrade the Ventas Online page to the 3-tab control center (gated by useEnvios so legacy stores are untouched) and build the Despacho kanban board with real-time /envios socket push and the master-detail (~75/25) shell.

Purpose: RD-2 (kanban columns + counts + cards), RD-9 (socket push, no polling), RD-12 (use_envios=false keeps legacy tabs — the #1 retail-regression checkpoint). The timeline panel content + cobro modal come in plan 42-07; this plan builds the board + selection shell.
Output: 3-tab VentasOnlineView, DespachoBoard with kanban + socket + master-detail.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md

<interfaces>
Analogs:
- ventago-app/src/views/ventas-online/VentasOnlineView.tsx (Tabs block L196-205, tab state L126, content switch L248-250 — current Pedidos/Envíos/Devoluciones)
- ventago-app/src/views/restaurante/DeliveryBoard.tsx (WS host L33-37, COLUMNS L46-53, mutateRef L136-141, socket effect L144-174, grouping L177-186, master-detail layout)

Hooks (plan 42-05): useDespachoBoard(branchId) → { cards: EnvioCard[], mutate }. useStoreConfig() → { useEnvios }. envioLabels: statusLabel/canalLabel/statusColor/paymentStatus colors.
WS host: dev 'http://localhost:5002/envios', prod 'https://newapi.coolsistema.com/envios'.
EnvioCard.columnKey ∈ nuevo|preparando|listo|en_transito|entregado.
BranchContext: selectedBranchId (use, do not re-fetch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-tab VentasOnlineView gated by useEnvios (legacy preserved)</name>
  <read_first>
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (full — current tabs, tab state, content switch, existing ReturnsTable/Envíos/Pedidos tabs)
    - ventago-app/src/context/StoreConfigContext.tsx (useEnvios getter from plan 42-05)
  </read_first>
  <files>ventago-app/src/views/ventas-online/VentasOnlineView.tsx</files>
  <action>
    Branch on `const { useEnvios } = useStoreConfig();`:
    - When useEnvios is TRUE: render the new 3 tabs — `Despacho` / `Cuentas por cobrar` / `Historial`. Content switch: despacho→<DespachoBoard/> (this plan), cobrar→<CuentasPorCobrarTab/> (plan 42-08), historial→<HistorialTab/> (plan 42-08). Use next/dynamic ssr:false for the three views (code-split, 300ms target). Import placeholders for CuentasPorCobrarTab/HistorialTab so it typechecks now — they are created in plan 42-08; use lazy dynamic imports so missing-at-build is avoided only if they exist. SAFER: create minimal stub components in plan 42-08; here, dynamic-import them and accept they land in 42-08 (this plan depends on 42-05 only, so guard the tab content to render DespachoBoard fully and the other two tabs as a lightweight 'Cargando…' until 42-08 lands). Document the stub approach.
    - When useEnvios is FALSE: render the EXISTING tabs (Pedidos/Envíos/Devoluciones) UNCHANGED — RD-12 regression gate. Do not delete or alter legacy tab code paths.
    ESLint: newline-before-return, lines-around-comment, no-unused-vars.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "useEnvios" src/views/ventas-online/VentasOnlineView.tsx && grep -q "Despacho" src/views/ventas-online/VentasOnlineView.tsx && grep -q "Cuentas por cobrar" src/views/ventas-online/VentasOnlineView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useEnvios" ventago-app/src/views/ventas-online/VentasOnlineView.tsx` matches (gate branch)
    - `grep "Despacho" VentasOnlineView.tsx`, `grep "Cuentas por cobrar" VentasOnlineView.tsx`, `grep "Historial" VentasOnlineView.tsx` all match (3 tabs)
    - legacy tab labels (Pedidos/Envíos/Devoluciones) still present in the useEnvios=false branch (RD-12)
    - `grep "next/dynamic\|dynamic(" VentasOnlineView.tsx` matches (code-split)
  </acceptance_criteria>
  <done>3-tab upgrade for use_envios stores; legacy tabs intact for others.</done>
</task>

<task type="auto">
  <name>Task 2: DespachoBoard kanban + /envios socket + master-detail shell</name>
  <read_first>
    - ventago-app/src/views/restaurante/DeliveryBoard.tsx (full — WS host L33-37, COLUMNS L46-53, mutateRef L136-141, socket effect L144-174, grouping L177-186, master-detail render)
    - ventago-app/src/hooks/api/useDespachoBoard.ts (EnvioCard, mutate — plan 42-05)
    - ventago-app/src/configs/envioLabels.ts (statusLabel/canalLabel/statusColor/payment colors — plan 42-05)
    - .claude/skills/sketch-findings-ace-online/SKILL.md (card/board theme)
  </read_first>
  <files>ventago-app/src/views/ventas-online/DespachoBoard.tsx</files>
  <action>
    Clone DeliveryBoard.tsx structure, retail-mapped:
    - WS_HOST: dev 'http://localhost:5002/envios', prod 'https://newapi.coolsistema.com/envios'.
    - COLUMNS = [{key:'nuevo',label:'Nuevo'},{key:'preparando',label:'Preparando'},{key:'listo',label:'Listo p/ despacho'},{key:'en_transito',label:'En tránsito'},{key:'entregado',label:'Entregado'}].
    - Data: useDespachoBoard(selectedBranchId from BranchContext). Group cards by columnKey; header per column shows count badge; cards stack vertically.
    - Card content (spec §5.2): orderNumber, channel badge (canalLabel), clientName, address, total, paymentStatus badge (Pagado/Parcial/Sin pagar), if shipped: transporte chip + trackingCode, if saldo>0: red `Saldo $X`.
    - Socket: `io(WS_HOST, { transports:['websocket'], auth:{ token: localStorage.getItem('accessToken') } })`; on 'connect' emit 'join' {branchId}; on 'envio_updated' merge the card into SWR cache via mutateRef functional updater (replace by id, no full refetch — D-11). Cleanup: socket.off('envio_updated'); socket.disconnect(). Keep mutateRef pattern so the effect doesn't reconnect on every cards change.
    - Master-detail: left ~75% board, right ~25% timeline panel slot. Card click sets selectedCardId (state owned here). Render <EnvioTimeline orderId={selected}/> placeholder area (component lands in plan 42-07; render a lightweight panel container now so layout is correct, conditionally render EnvioTimeline if available). Document the 42-07 dependency.
    - Despachar action on `listo` cards: open transporte dropdown (activeOnly via useTransportes) + tracking input → POST ship; if saldo>0 show the "外상으로 발송" completion-gate warning before confirming (wiring to backend ship route). The cobro modal trigger lives in the timeline (42-07); here wire the ship action + gate warning.
    ESLint compliant. next/dynamic ssr:false where the parent imports this.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "/envios" src/views/ventas-online/DespachoBoard.tsx && grep -q "envio_updated" src/views/ventas-online/DespachoBoard.tsx && grep -q "Listo p/ despacho" src/views/ventas-online/DespachoBoard.tsx && ! grep -q "refreshInterval\|setInterval" src/views/ventas-online/DespachoBoard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/envios" ventago-app/src/views/ventas-online/DespachoBoard.tsx` matches (WS host)
    - `grep "envio_updated" DespachoBoard.tsx` matches (socket event + cleanup off)
    - `grep "socket.emit('join'" DespachoBoard.tsx` matches
    - `grep "setInterval\|refreshInterval" DespachoBoard.tsx` returns nothing (no polling, D-11)
    - 5 columns present (nuevo/preparando/listo/en_transito/entregado)
    - `grep "Saldo" DespachoBoard.tsx` matches (red saldo badge)
    - `grep "外상\|completion\|Saldo pendiente\|外상으로\|enviar con saldo\|despachar con saldo" DespachoBoard.tsx` — completion-gate warning text present (any Spanish/UX phrasing)
  </acceptance_criteria>
  <done>DespachoBoard: 5-column kanban, count badges, socket push merge, master-detail shell, ship action + completion gate warning.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — board real-time + legacy no-regression</name>
  <what-built>3-tab Ventas Online (use_envios gated) + Despacho kanban with /envios socket push + master-detail.</what-built>
  <files>ventago-app/src/views/ventas-online/VentasOnlineView.tsx, ventago-app/src/views/ventas-online/DespachoBoard.tsx</files>
  <action>Human verification AFTER the automated board build. Run the dev stack and walk the steps below; this checkpoint confirms real-time socket behavior and legacy no-regression that automated tests cannot cover.</action>
  <how-to-verify>
    1) `./dev.sh` (or npm run dev:api + dev:app). Enable use_envios on a test store (Configuración → Transporte card visible; add a transporte).
    2) Open Ventas Online → confirm 3 tabs (Despacho/Cuentas por cobrar/Historial). Confirm kanban with 5 columns + count badges.
    3) Create/advance an order via the existing flow; confirm the card appears/moves in real time WITHOUT page refresh (socket push).
    4) Ship a `listo` card → transporte dropdown + tracking → confirm En tránsito move. With a partial-paid order, confirm the "外상으로 발송" warning shows before shipping.
    5) Switch to a use_envios=FALSE store → confirm Ventas Online shows the legacy tabs (Pedidos/Envíos/Devoluciones) working normally (RD-12 — no regression).
  </how-to-verify>
  <verify>
    <automated>cd ventago-app && npx next lint --file src/views/ventas-online/DespachoBoard.tsx --file src/views/ventas-online/VentasOnlineView.tsx 2>&1 | tail -10; echo "lint-done"</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues (board not updating / legacy tabs broken / gate missing).</resume-signal>
  <done>Human confirms: 3 tabs render for use_envios store, kanban updates in real time without refresh, ship + completion gate works, and use_envios=false store shows untouched legacy tabs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser socket → /envios | Token-authenticated handshake; branch room scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-18 | Info Disclosure | socket auth token | mitigate | accessToken passed in handshake auth; backend verifies + branch-room IDOR (plan 42-04) |
| T-42-19 | Repudiation | legacy regression | mitigate | useEnvios=false renders untouched legacy tabs; explicit UAT step 5 |
</threat_model>

<verification>
- `cd ventago-app && npx next lint` clean on touched files
- 3-tab gated by useEnvios; legacy tabs intact when false
- Board: 5 columns, socket push (no polling), master-detail shell
- Human UAT: real-time update + legacy no-regression confirmed
</verification>

<success_criteria>
RD-2/RD-9/RD-12: real-time Despacho kanban (5 columns, counts, card-level socket merge, ship + completion gate) within a 3-tab upgrade that leaves legacy stores untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-06-SUMMARY.md`
</output>
