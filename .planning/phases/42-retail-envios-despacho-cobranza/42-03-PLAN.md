---
phase: 42-retail-envios-despacho-cobranza
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - api-ventago/src/app/online-orders/online-order-sales-mirror.service.ts
  - api-ventago/src/app/online-orders/online-orders.service.ts
  - api-ventago/src/app/online-orders/online-orders.service.spec.ts
autonomous: true
requirements: [RD-1, RD-4, RD-10, RD-12]
must_haves:
  truths:
    - "migrations 42-01, 42-02, 42-03 are applied to the local PG18 dev DB (schema columns exist)"
    - "Delivering a fully-paid order still produces mirror PAID + revenue/stock recognition exactly as before (regression preserved)"
    - "Delivering an order with outstanding saldo does NOT force paymentStatus=PAID — the shortfall remains tracked as 외상 and surfaces in Cuentas por cobrar"
    - "Delivering an order that was shipped with a shortfall (order.metadata.shipSaldo>0) accrues exactly ONE CreditLedger sale_credit (saleId=mirror.id, bucket=credito) inside the deliver tx, raising StoreClient.balance"
    - "sale mirror keeps source=online + activityType=sale + idempotency (online_order_id UNIQUE)"
    - "sale_payment_methods on the mirror reflects 실수령액 (actually received), not the full total"
  artifacts:
    - path: "api-ventago/src/app/online-orders/online-order-sales-mirror.service.ts"
      provides: "createMirror with conditional payment row (실수령액) + preserved invariants"
      contains: "SaleSource.ONLINE"
    - path: "api-ventago/src/app/online-orders/online-orders.service.ts"
      provides: "deliver conditional paymentStatus + sale_credit accrual from metadata.shipSaldo (saleId=mirror.id)"
      contains: "shipSaldo"
    - path: "api-ventago/src/app/online-orders/online-orders.service.spec.ts"
      provides: "regression gate tests: fully-paid deliver = PAID; shortfall deliver != forced PAID + sale_credit accrued"
      contains: "deliverOrder"
  key_links:
    - from: "deliverOrder"
      to: "paymentStatus assignment"
      via: "conditional (saldo 0 → PAID, saldo>0 → keep)"
      pattern: "OnlineOrderPaymentStatus"
    - from: "deliverOrder"
      to: "creditLedgerService.appendMovement"
      via: "sale_credit from metadata.shipSaldo, saleId=mirror.id, external tx t (after createMirror)"
      pattern: "appendMovement"
    - from: "createMirror"
      to: "SalePaymentMethod.create"
      via: "amount = 실수령액 (not full total)"
      pattern: "SalePaymentMethod"
---

<objective>
Realign the deliver→mirror payment attribution (Pitfall 1 — the single highest integration + regression risk) so the "발송 ≠ 종료, 부족분 외상" model works WITHOUT breaking existing retail online-orders, AND accrue the sale_credit ledger row at deliver (the resolved Pitfall-1 / Research Open Q1 seam): the mirror saleId — required by the sale_credit DB CHECK — only exists after createMirror, so the shortfall recorded at SHIP (metadata.shipSaldo, plan 42-02) becomes a single sale_credit movement here. Apply all three migrations to the local PG18 dev DB (BLOCKING — types/build pass without live schema, creating false-positive verification). Add the regression gate proving fully-paid deliveries still mirror PAID.

Purpose: RD-10 (delivery sale → revenue/stock auto-reflect, source=online), RD-4 (외상 accrual — the sale_credit ledger row is written HERE at deliver, keyed off shipSaldo recorded at ship in plan 42-02), RD-12 (existing online-orders deliver/cancel zero regression — #1 retail-regression risk per user memory), RD-1 (live schema for transportes/use_envios).
Output: applied migrations, conditional paymentStatus at deliver, sale_credit accrual from metadata.shipSaldo, 실수령액 payment row in mirror, regression spec.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md

<interfaces>
deliverOrder (online-orders.service.ts:336-363) currently:
```typescript
await this.stockService.commitSale(order, t);
const mirrorSale = await this.mirrorService.createMirror(order, t);
order.mirrorSaleId = mirrorSale.id;
order.stockReleasedAt = new Date();
order.deliveredAt = new Date();
order.paymentStatus = OnlineOrderPaymentStatus.PAID;   // ← L355 UNCONDITIONAL — the bug
```
createMirror (online-order-sales-mirror.service.ts:52-159):
- L113 status: SaleStatus.PAID
- L116 source: SaleSource.ONLINE  ← INVARIANT, keep
- L119 activityType: SaleActivityType.SALE  ← INVARIANT, keep
- L57-68 idempotency via online_order_id UNIQUE  ← INVARIANT, keep
- L78-89 dailyNumber  ← INVARIANT, keep
- L148-156: SalePaymentMethod.create amount = totalAmount (full) ← CHANGE to 실수령액

CreditLedgerService.appendMovement (credit-ledger.service.ts:91) — REQUIRES external tx injected (throws BadRequest if missing); sale_credit REQUIRES saleId (DB CHECK); locks store_clients FOR UPDATE; bucket=credito:
```typescript
appendMovement({ storeId, storeClientId, movementType:'sale_credit', amount, saleId, branchId, userId, dueDate?, note, transaction })
```
NOTE: CreditLedgerService is already injectable into OnlineOrdersService (module wired in plan 42-02 Task 4). order.metadata.shipSaldo / shipSaldoStoreClientId / shipSaldoUserId are set at SHIP (plan 42-02 Task 2).

Local dev DB (CLAUDE.md project_env): PG18 host (NOT docker), DB `ventago`, no docker. Apply via host psql.
Regression checklist source: 42-RESEARCH.md lines 485-499.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migrations 42-01, 42-02, 42-03 to local PG18 dev DB</name>
  <read_first>
    - api-ventago/migrations/42-01-transportes.sql (plan 42-01)
    - api-ventago/migrations/42-02-online-orders-cols.sql (plan 42-02)
    - api-ventago/migrations/42-03-store-config-use-envios.sql (plan 42-01)
    - CLAUDE.md (로컬 dev = PG18 호스트, docker 아님 — local apply path)
  </read_first>
  <files>api-ventago/migrations/42-01-transportes.sql, api-ventago/migrations/42-02-online-orders-cols.sql, api-ventago/migrations/42-03-store-config-use-envios.sql</files>
  <action>
    [BLOCKING gate] Apply the three idempotent migrations IN ORDER (42-02 FK references transportes from 42-01) to the local PG18 `ventago` DB via Bash psql:
    ```bash
    psql -d ventago -f api-ventago/migrations/42-01-transportes.sql
    psql -d ventago -f api-ventago/migrations/42-02-online-orders-cols.sql
    psql -d ventago -f api-ventago/migrations/42-03-store-config-use-envios.sql
    ```
    This is automatable (local host psql per CLAUDE.md). If psql returns an auth/connection error, the dynamic auth gate applies — surface the exact error and the project's documented local connection string, then retry. This is BLOCKING because subsequent jest/integration verification reads the live schema — without it, tsc/build pass but runtime SELECTs of the new columns would fail (false-positive verification). Do NOT apply to production PG10 (user-confirmed deploy only).
  </action>
  <verify>
    <automated>psql -d ventago -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='online_orders' AND column_name IN ('prepared_at','dispatched_at','transporte_id');" 2>/dev/null | grep -q "^3$" && psql -d ventago -tAc "SELECT to_regclass('public.transportes');" 2>/dev/null | grep -q transportes && psql -d ventago -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='store_configs' AND column_name='use_envios';" 2>/dev/null | grep -q "^1$" && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `psql -d ventago -c "\d transportes"` shows the table with columns store_id, name, is_active
    - information_schema query returns prepared_at, dispatched_at, transporte_id for online_orders (count=3)
    - information_schema query returns use_envios for store_configs (count=1)
    - re-running each migration is a no-op (IF NOT EXISTS idempotency)
  </acceptance_criteria>
  <done>Three migrations applied to local PG18 ventago DB; schema verification queries pass (transportes table + 3 online_orders cols + use_envios).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional paymentStatus + sale_credit accrual from shipSaldo + 실수령액 mirror row (Pitfall 1, RD-4 deliver side)</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.service.ts:336-363 (deliverOrder — L355 unconditional PAID)
    - api-ventago/src/app/online-orders/online-order-sales-mirror.service.ts:52-159 (createMirror — L113 status, L116 source, L119 activityType, L148-156 full-amount payment row)
    - api-ventago/src/app/credit/services/credit-ledger.service.ts:91-149 (appendMovement: requires tx, sale_credit needs saleId, FOR UPDATE)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-02-SUMMARY.md (confirms ship set metadata.shipSaldo — intent; this plan accrues the ledger row)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:301-305 (Pitfall 1) + lines 400-404 (Open Q1 RESOLVED — accrue sale_credit at deliver keyed off shipSaldo)
  </read_first>
  <behavior>
    - Test: deliverOrder on a fully-paid order (no metadata.shipSaldo / saldo 0) → order.paymentStatus = PAID, mirror SaleStatus.PAID, SalePaymentMethod amount = total (UNCHANGED — regression gate RD-12); appendMovement NOT called
    - Test: deliverOrder on an order with metadata.shipSaldo>0 → paymentStatus NOT forced to PAID (stays current value), mirror created with source=online + activityType=sale, SalePaymentMethod amount = 실수령액 (received), and exactly ONE appendMovement('sale_credit', amount=shipSaldo, saleId=mirror.id, transaction=t) is called AFTER createMirror inside the same SERIALIZABLE deliver tx → StoreClient.balance rises
    - Test: sale_credit accrual is idempotent w.r.t. mirror idempotency — a second deliver call (same online_order_id) does NOT duplicate the mirror NOR the sale_credit row
    - Test: createMirror keeps source=SaleSource.ONLINE and activityType=SaleActivityType.SALE (invariants)
  </behavior>
  <files>api-ventago/src/app/online-orders/online-orders.service.ts, api-ventago/src/app/online-orders/online-order-sales-mirror.service.ts</files>
  <action>
    1) deliverOrder (L355): replace `order.paymentStatus = OnlineOrderPaymentStatus.PAID;` with conditional. Read the shortfall recorded at ship: `const shipSaldo = Number(order.metadata?.shipSaldo ?? 0);`
       ```typescript
       order.paymentStatus = shipSaldo <= 0
         ? OnlineOrderPaymentStatus.PAID
         : order.paymentStatus;   // keep (pending/partial) — do NOT force PAID
       ```
    2) sale_credit accrual (RD-4 deliver side — the resolved seam): AFTER `const mirrorSale = await this.mirrorService.createMirror(order, t);` (so mirror.id exists), if `shipSaldo > 0`:
       ```typescript
       await this.creditLedgerService.appendMovement({
         storeId,
         storeClientId: order.metadata.shipSaldoStoreClientId,   // recorded at ship
         movementType: 'sale_credit',                            // bucket=credito, +
         amount: shipSaldo,
         saleId: mirrorSale.id,                                  // now exists (DB CHECK satisfied)
         branchId: order.branchId,
         userId: order.metadata.shipSaldoUserId ?? userId,
         note: 'Envío #' + order.orderNumber + ' despachado con saldo',
         transaction: t,                                         // SAME deliver SERIALIZABLE tx
       });
       ```
       Guard against double-accrual: only accrue when the mirror was newly created this call (createMirror is idempotent via UNIQUE online_order_id — if it returned an existing mirror, do NOT re-append; detect via the idempotency signal createMirror exposes, or guard with order.mirrorSaleId == null before the call). Document the idempotency guard chosen.
    3) createMirror (L148-156): the SalePaymentMethod row amount must reflect 실수령액 (received = total − shipSaldo), NOT full totalAmount, when shipSaldo>0. For fully-paid (no shipSaldo) keep amount=totalAmount (regression). Preserve UNCHANGED: SaleSource.ONLINE (L116), SaleActivityType.SALE (L119), idempotency UNIQUE online_order_id (L57-68), dailyNumber (L78-89), SaleStatus row. Add a param or read order.metadata to know 실수령액 — pick the minimal-surface approach and keep mirror signature backward compatible for any other caller.
    4) Anti-pattern guard: NEVER UPDATE/DELETE a CreditLedger row; corrections only via reverse movement.
    ESLint compliant. Update online-orders.service.spec.ts with the regression + shortfall + sale_credit + idempotency tests.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "OnlineOrderPaymentStatus.PAID;" api-ventago/src/app/online-orders/online-orders.service.ts` — the unconditional `order.paymentStatus = OnlineOrderPaymentStatus.PAID;` assignment is gone; conditional form present (`shipSaldo <= 0 ?`)
    - `grep "shipSaldo" api-ventago/src/app/online-orders/online-orders.service.ts` matches (read from metadata at deliver)
    - `grep "appendMovement" online-orders.service.ts` matches with `movementType: 'sale_credit'` and `saleId: mirrorSale.id`
    - `grep "SaleSource.ONLINE" api-ventago/src/app/online-orders/online-order-sales-mirror.service.ts` still matches (invariant)
    - `grep "SaleActivityType.SALE" online-order-sales-mirror.service.ts` still matches (invariant)
    - regression test "fully-paid deliver → PAID + full payment row + no appendMovement" passes
    - shortfall test "shipSaldo>0 deliver → not forced PAID + 실수령액 payment row + ONE sale_credit (saleId=mirror.id)" passes
    - idempotency test "second deliver does not duplicate mirror nor sale_credit" passes
    - `cd api-ventago && npx jest online-orders` exits 0
  </acceptance_criteria>
  <done>Deliver no longer force-PAIDs shortfall orders; accrues exactly one sale_credit (saleId=mirror.id) from metadata.shipSaldo inside the deliver tx; mirror payment row = 실수령액; fully-paid path unchanged; invariants preserved; no double-accrual.</done>
</task>

<task type="auto">
  <name>Task 3: Full online-orders + credit + box regression suite (RD-12 gate)</name>
  <read_first>
    - api-ventago/src/app/online-orders/online-orders.service.spec.ts (existing + new tests)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:485-499 (소매 무회귀 점검 checklist)
  </read_first>
  <files>api-ventago/src/app/online-orders/online-orders.service.spec.ts</files>
  <action>
    Ensure the spec covers the RD-12 regression checklist (research lines 494-499):
    - 완납 online 주문 deliver → mirror PAID + 매출 반영 (after the conditional change), appendMovement NOT called
    - shortfall deliver → exactly one sale_credit accrued (RD-4 deliver side), balance up
    - 기존 online 취소(환불) → nullifyMirror 정상 (favor branch does not break default path)
    - mirror activity_type='sale' / source='online' invariant
    - no double-counting in revenue (single mirror per order — idempotency) AND no double sale_credit
    Run the wider suite to catch cross-module breakage: online-orders + credit + box-operation.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders credit box-operation 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd api-ventago && npx jest online-orders credit box-operation` exits 0 (no regressions across the three modules)
    - spec file contains a test asserting fully-paid deliver keeps mirror PAID (no appendMovement)
    - spec file contains a test asserting shortfall deliver accrues one sale_credit
    - spec file contains a test asserting cancel(refund/devolver) path still calls nullifyMirror
  </acceptance_criteria>
  <done>Regression suite green across online-orders/credit/box; RD-12 invariants + RD-4 deliver accrual asserted by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deliver → sale mirror + credit ledger | Revenue/stock recognition + sale_credit accrual; double-count + force-PAID + double-accrual risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Tampering | mirror payment attribution | mitigate | conditional paymentStatus; 실수령액 payment row; shortfall stays 외상 (no silent PAID) |
| T-42-11 | Tampering | revenue double-count + double sale_credit | mitigate | idempotency UNIQUE online_order_id preserved; sale_credit accrued only when mirror newly created (guard on mirrorSaleId/idempotency signal) |
| T-42-12 | Repudiation | retail online-orders regression | mitigate | explicit RD-12 regression spec; fully-paid deliver invariant test; cancel nullifyMirror test |
</threat_model>

<verification>
- Migrations applied to local PG18 (schema queries pass) — BLOCKING gate satisfied before tests
- `cd api-ventago && npx jest online-orders credit box-operation` green
- Unconditional paymentStatus=PAID removed; invariants (source/activityType/idempotency) intact
- Fully-paid deliver regression preserved; shortfall deliver no longer force-PAID; shortfall deliver accrues one sale_credit (saleId=mirror.id) from metadata.shipSaldo
</verification>

<success_criteria>
RD-4/RD-10/RD-12: deliver recognizes revenue/stock as before for fully-paid orders, accrues exactly one sale_credit (from metadata.shipSaldo, saleId=mirror.id) for shortfall orders so they surface in Cuentas por cobrar (no force-PAID); mirror invariants (source=online, activityType=sale, idempotency) unchanged; all three migrations live on dev DB; regression suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-03-SUMMARY.md`
</output>
