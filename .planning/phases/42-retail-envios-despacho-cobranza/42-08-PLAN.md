---
phase: 42-retail-envios-despacho-cobranza
plan: 08
type: execute
wave: 8
depends_on: [05, 07]
files_modified:
  - ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx
  - ventago-app/src/views/ventas-online/HistorialTab.tsx
  - ventago-app/src/hooks/api/useEnviosHistory.ts
autonomous: true
requirements: [RD-7, RD-8]
must_haves:
  truths:
    - "Cuentas por cobrar lists only saldo>0 orders (red), aggregated, with per-client balance (StoreClient.balance)"
    - "Registrar cobro from Cuentas por cobrar pays down 외상; when saldo hits 0 the order leaves the tab"
    - "Historial lists closed orders (DELIVERED fully-paid + CANCELLED) with a date-range filter"
    - "Fully-paid deliveries never appear in Cuentas por cobrar (clean close)"
  artifacts:
    - path: "ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx"
      provides: "saldo>0 control + per-client balance + cobro"
      contains: "saldo"
    - path: "ventago-app/src/views/ventas-online/HistorialTab.tsx"
      provides: "closed-order history with date range"
      contains: "Historial"
    - path: "ventago-app/src/hooks/api/useEnviosHistory.ts"
      provides: "dedicated retail envios history SWR hook (does NOT mutate Phase-40 useDeliveryHistory)"
      contains: "fromDate"
  key_links:
    - from: "CuentasPorCobrarTab"
      to: "useCuentasPorCobrar hook + CobroModal"
      via: "row → Registrar cobro"
      pattern: "Registrar cobro"
    - from: "HistorialTab"
      to: "useEnviosHistory"
      via: "date-range fetch"
      pattern: "fromDate"
---

<objective>
Build the Cuentas por cobrar tab (외상 control: saldo>0 only, per-client balance, cobro) and the Historial tab (closed orders with date range). This is the third leg of the 3-tab control center, plus the final phase verification.

Purpose: RD-7 (cuentas por cobrar control + cobro), RD-8 (historial). The cuentas/historial data layer consumes the shared hooks from 42-05 and a NEW dedicated useEnviosHistory hook (NOT a mutation of the Phase-40 useDeliveryHistory — shared-path regression guard, user memory 식당/소매 회귀 금지) and the CobroModal contract.
Output: CuentasPorCobrarTab, HistorialTab, useEnviosHistory hook.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md

<interfaces>
Analogs:
- ventago-app/src/views/ventas-online/VentasOnlineView.tsx ReturnsTable (L433-483 — table/empty/loading structure)
- ventago-app/src/views/restaurante/components/RiderSettlementView.tsx (per-client/per-entity balance control UI)
- ventago-app/src/views/restaurante/DeliveryBoard.tsx Historial view (L86-108 — fromDate/toDate/toExclusive + useDeliveryHistory)
- ventago-app/src/hooks/api/useDeliveryHistory.ts (date-range SWR PATTERN to clone — do NOT edit/extend this Phase-40 file; create a sibling)

Hooks (plan 42-05): useCuentasPorCobrar() → { rows, totalSaldo, mutate } (bound to GET /online-orders/cuentas-por-cobrar, owned by plan 42-04 Task 3). CobroModal (plan 42-07) props { open, onClose, orderId, saldoPendiente, paymentMethods, favorBalance, onCobroDone }.
Backend: GET /online-orders/cuentas-por-cobrar (saldo>0, per-client balance) — EXISTS (plan 42-04 Task 3). Historial: GET /online-orders with status filter + date range (confirm/extend existing list route while reading the controller; if a date+status filtered list route is missing, note it — but the existing online-orders list route already supports status/date filtering in most cases).
Plan dependency: 42-08 depends_on [05, 07] — CobroModal (42-07) is guaranteed merged before this plan runs (wave 8), so import it directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useEnviosHistory hook + CuentasPorCobrarTab</name>
  <read_first>
    - ventago-app/src/hooks/api/useDeliveryHistory.ts (date-range SWR shape — clone the PATTERN into a NEW sibling file; do NOT edit this Phase-40 file)
    - ventago-app/src/hooks/api/useCuentasPorCobrar.ts (plan 42-05 — rows/totalSaldo)
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (ReturnsTable L433-483 table/empty/loading)
    - ventago-app/src/views/restaurante/components/RiderSettlementView.tsx (per-client balance UI)
    - ventago-app/src/views/ventas-online/components/CobroModal.tsx (plan 42-07 — modal props)
  </read_first>
  <files>ventago-app/src/hooks/api/useEnviosHistory.ts, ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx</files>
  <action>
    1) Create a NEW dedicated `useEnviosHistory.ts` (clone the useDeliveryHistory PATTERN — do NOT mutate the Phase-40 file, INFO #6 shared-path regression guard). Keyed to the online-orders closed list with `{ fromDate, toDate }` params (toExclusive pattern). Use the existing online-orders list endpoint with date+status filter; if that endpoint lacks date+status filtering, note the backend route to add in the SUMMARY (do not silently assume). 5min dedup.
    2) CuentasPorCobrarTab: consume useCuentasPorCobrar() (bound to the existing 42-04 cuentas-por-cobrar route). Render only saldo>0 rows in RED (completed orders never appear — RD-7 / spec §5.5). Columns: order#, client, total, recibido, Saldo (red), per-client balance (StoreClient.balance from row.balance). Show totalSaldo aggregate header. Each row "Registrar cobro" → open CobroModal (saldoPendiente = row.saldo). onCobroDone → mutate() so a paid-to-zero row disappears. Empty/loading states from ReturnsTable pattern. pageSize≤50.
    ESLint: newline-before-return, lines-around-comment, no-unused-vars.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "saldo" src/views/ventas-online/CuentasPorCobrarTab.tsx && grep -q "Registrar cobro" src/views/ventas-online/CuentasPorCobrarTab.tsx && grep -q "fromDate\|toDate" src/hooks/api/useEnviosHistory.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ventago-app/src/hooks/api/useEnviosHistory.ts` (NEW sibling — `useDeliveryHistory.ts` NOT in files_modified, no Phase-40 mutation)
    - `grep "fromDate\|toDate" ventago-app/src/hooks/api/useEnviosHistory.ts` matches
    - `grep "saldo" ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx` matches (saldo>0 rows)
    - `grep "Registrar cobro" CuentasPorCobrarTab.tsx` matches (opens CobroModal)
    - `grep "balance" CuentasPorCobrarTab.tsx` matches (per-client StoreClient.balance)
    - `grep "totalSaldo\|total" CuentasPorCobrarTab.tsx` matches (aggregate)
  </acceptance_criteria>
  <done>Dedicated useEnviosHistory sibling (no Phase-40 mutation); Cuentas por cobrar: saldo>0 red rows + per-client balance + aggregate + cobro→0 removes row.</done>
</task>

<task type="auto">
  <name>Task 2: HistorialTab (closed orders + date range)</name>
  <read_first>
    - ventago-app/src/views/restaurante/DeliveryBoard.tsx:86-108 (Historial view fromDate/toDate/toExclusive pattern)
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (ReturnsTable table/empty/loading)
    - ventago-app/src/hooks/api/useEnviosHistory.ts (Task 1 — the dedicated retail history hook)
    - ventago-app/src/configs/envioLabels.ts (plan 42-05 — status/payment labels)
  </read_first>
  <files>ventago-app/src/views/ventas-online/HistorialTab.tsx</files>
  <action>
    HistorialTab: date-range filter (fromDate/toDate, toExclusive) consuming useEnviosHistory + table of CLOSED orders = DELIVERED-and-fully-paid + CANCELLED (RD-8). Columns: order#, client, channel, total, final paymentStatus, transporte, dispatched/delivered dates, outcome (Entregado/Cancelado). Use envioLabels for badges. Clicking a row may open the read-only timeline (optional — reuse EnvioTimeline if available). Empty/loading states. pageSize≤50, no polling.
    ESLint compliant.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "Historial" src/views/ventas-online/HistorialTab.tsx && grep -q "fromDate\|toDate" src/views/ventas-online/HistorialTab.tsx && grep -q "Cancelado\|Entregado\|delivered\|cancelled" src/views/ventas-online/HistorialTab.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fromDate\|toDate" ventago-app/src/views/ventas-online/HistorialTab.tsx` matches (date range)
    - `grep "useEnviosHistory" HistorialTab.tsx` matches (consumes the dedicated hook, not useDeliveryHistory)
    - `grep "Entregado\|Cancelado\|delivered\|cancelled" HistorialTab.tsx` matches (closed states)
    - `grep "pageSize" HistorialTab.tsx` with value ≤50, or default ≤50
    - `grep "setInterval\|refreshInterval" HistorialTab.tsx` returns nothing
  </acceptance_criteria>
  <done>Historial: date-range filtered closed-order table (delivered-paid + cancelled) via useEnviosHistory.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide verification + source-coverage check (post-UAT gate)</name>
  <read_first>
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:485-499 (소매 무회귀 checklist)
    - all 42-0X-SUMMARY.md files produced so far (INCLUDING 42-06-SUMMARY and 42-07-SUMMARY — the board + cobro/cancel browser UAT checkpoints must be resolved before claiming phase completion)
  </read_first>
  <files>ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx</files>
  <action>
    Run the cross-cutting verification (no new feature code — confirmation only). This task is the phase-completion gate and runs LAST: plan 42-08 depends_on [05, 07], so the blocking human-verify checkpoints in 42-06 (board UAT) and 42-07 (cobro/cancel/caja UAT) have already resolved before this runs — do NOT claim "full suite green + RD coverage" ahead of those UATs.
    1) Confirm 42-06 + 42-07 browser UAT checkpoints are marked approved in their SUMMARYs. If either is unresolved, STOP and surface it (do not claim phase completion).
    2) Backend full suite: `cd api-ventago && npx jest online-orders transportes credit box-operation` — all green.
    3) Frontend lint on all Phase 42 touched files: `cd ventago-app && npx next lint` — no errors (ESLint blocks build).
    4) Confirm RD-1..RD-12 coverage: each requirement maps to a plan (RD-1→01/03/05, RD-2→02/04/05/06, RD-3→02, RD-4→02(ship intent)/03(deliver accrual), RD-5→02/07, RD-6→02/07, RD-7→02/04(backend)/05(hook)/08(tab), RD-8→08, RD-9→04/06, RD-10→03, RD-11→07, RD-12→03/06).
    5) RD-12 regression spot-check (manual, documented in SUMMARY): use_envios=false store shows legacy Ventas Online tabs; fully-paid online deliver still mirrors PAID + appears in reports; no double-count; shortfall deliver accrues exactly one sale_credit.
    Record results in 42-08-SUMMARY.md. If any gap found, surface it (do not silently pass).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders transportes credit box-operation 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 42-06-SUMMARY + 42-07-SUMMARY confirm their blocking browser-UAT checkpoints are approved (verified before claiming completion)
    - `cd api-ventago && npx jest online-orders transportes credit box-operation` exits 0
    - `cd ventago-app && npx next lint` reports no errors on Phase 42 files
    - 42-08-SUMMARY.md documents RD-1..RD-12 coverage + RD-12 regression spot-check result
  </acceptance_criteria>
  <done>Board + cobro UAT confirmed resolved; full backend suite + frontend lint green; RD coverage + retail no-regression documented (completion not claimed ahead of UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cuentas/historial fetch | Authenticated; storeId server-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-23 | Info Disclosure | cuentas/historial endpoints | mitigate | storeId-scoped WHERE on list/cuentas routes (cuentas owned + scoped in 42-04); client never sends storeId |
| T-42-24 | Tampering | cobro from cuentas tab | mitigate | same server-validated cobro path as timeline (registerPayment + caja) |
</threat_model>

<verification>
- Cuentas por cobrar: saldo>0 only, per-client balance, cobro→0 removes row
- Historial: date-range closed orders via dedicated useEnviosHistory (no Phase-40 mutation)
- 42-06 + 42-07 browser UATs resolved BEFORE phase-completion claim
- Full backend jest suite green; frontend lint clean
- RD-1..RD-12 coverage documented; RD-12 retail no-regression confirmed
</verification>

<success_criteria>
RD-7/RD-8: 외상 control tab (saldo>0 red, per-client balance, cobro to close) + closed-order Historial with date range, completing the 3-tab center. Phase-wide suite green and source coverage verified — only after the 42-06/42-07 UAT checkpoints resolve.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-08-SUMMARY.md`
</output>
