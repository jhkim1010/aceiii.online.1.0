---
phase: 42-retail-envios-despacho-cobranza
plan: 08
type: execute
wave: 7
depends_on: [05]
files_modified:
  - ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx
  - ventago-app/src/views/ventas-online/HistorialTab.tsx
  - ventago-app/src/hooks/api/useDeliveryHistory.ts
autonomous: true
requirements: [RD-7, RD-8]
must_haves:
  truths:
    - "Cuentas por cobrar lists only saldo>0 orders (red), aggregated, with per-client balance (StoreClient.balance)"
    - "Registrar cobro from Cuentas por cobrar pays down 외상; when saldo hits 0 the order leaves the tab"
    - "Historial lists closed orders (DELIVERED fully-paid + CANCELLED) with a date-range filter"
    - "Fully-paid deliveries never appear in Cuentas por cobrar (clean close)"
  artifacts:
    - path: "ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx"
      provides: "saldo>0 control + per-client balance + cobro"
      contains: "saldo"
    - path: "ventago-app/src/views/ventas-online/HistorialTab.tsx"
      provides: "closed-order history with date range"
      contains: "Historial"
  key_links:
    - from: "CuentasPorCobrarTab"
      to: "useCuentasPorCobrar hook + CobroModal"
      via: "row → Registrar cobro"
      pattern: "Registrar cobro"
    - from: "HistorialTab"
      to: "history endpoint"
      via: "date-range fetch"
      pattern: "fromDate"
---

<objective>
Build the Cuentas por cobrar tab (외상 control: saldo>0 only, per-client balance, cobro) and the Historial tab (closed orders with date range). This is the third leg of the 3-tab control center, plus the final phase verification.

Purpose: RD-7 (cuentas por cobrar control + cobro), RD-8 (historial). This plan is independent of the board internals (42-06/07) — it consumes the shared hooks from 42-05 and the CobroModal contract — so it runs in parallel with 42-06.
Output: CuentasPorCobrarTab, HistorialTab, useDeliveryHistory hook.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md

<interfaces>
Analogs:
- ventago-app/src/views/ventas-online/VentasOnlineView.tsx ReturnsTable (L433-483 — table/empty/loading structure)
- ventago-app/src/views/restaurante/components/RiderSettlementView.tsx (per-client/per-entity balance control UI)
- ventago-app/src/views/restaurante/DeliveryBoard.tsx Historial view (L86-108 — fromDate/toDate/toExclusive + useDeliveryHistory)
- ventago-app/src/hooks/api/useDeliveryHistory.ts (date-range SWR pattern)

Hooks (plan 42-05): useCuentasPorCobrar() → { rows, totalSaldo, mutate }. CobroModal (plan 42-07) props { open, onClose, orderId, saldoPendiente, paymentMethods, favorBalance, onCobroDone }.
NOTE: CobroModal is created in plan 42-07. This plan (42-08) can import it; if 42-07 hasn't merged, render a "Registrar cobro" button that navigates to the Despacho timeline OR include a minimal inline cobro trigger. Prefer importing CobroModal (both are wave-7 frontend; execution order may interleave — guard the import).
Backend: GET /online-orders/cuentas-por-cobrar (saldo>0, per-client balance) — added in 42-05 task 2 or reuse board with filter. Historial: GET /online-orders with status filter + date range (confirm/extend existing list route).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useDeliveryHistory (retail) hook + CuentasPorCobrarTab</name>
  <read_first>
    - ventago-app/src/hooks/api/useDeliveryHistory.ts (date-range SWR shape — clone or generalize for online-orders)
    - ventago-app/src/hooks/api/useCuentasPorCobrar.ts (plan 42-05 — rows/totalSaldo)
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (ReturnsTable L433-483 table/empty/loading)
    - ventago-app/src/views/restaurante/components/RiderSettlementView.tsx (per-client balance UI)
    - ventago-app/src/views/ventas-online/components/CobroModal.tsx (plan 42-07 — modal props)
  </read_first>
  <files>ventago-app/src/hooks/api/useDeliveryHistory.ts, ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx</files>
  <action>
    1) Add a retail history hook (extend useDeliveryHistory or add a sibling fetch) keyed to online-orders closed list with `{ fromDate, toDate }` params (toExclusive pattern). If a generic online-orders list endpoint with date+status filter exists, use it; otherwise note the backend route to add. 5min dedup.
    2) CuentasPorCobrarTab: consume useCuentasPorCobrar(). Render only saldo>0 rows in RED (completed orders never appear — RD-7 / spec §5.5). Columns: order#, client, total, recibido, Saldo (red), per-client balance (StoreClient.balance). Show totalSaldo aggregate header. Each row "Registrar cobro" → open CobroModal (saldoPendiente = row.saldo). onCobroDone → mutate() so a paid-to-zero row disappears. Empty/loading states from ReturnsTable pattern. pageSize≤50.
    ESLint: newline-before-return, lines-around-comment, no-unused-vars.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "saldo" src/views/ventas-online/CuentasPorCobrarTab.tsx && grep -q "Registrar cobro" src/views/ventas-online/CuentasPorCobrarTab.tsx && grep -q "fromDate\|toDate" src/hooks/api/useDeliveryHistory.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "saldo" ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx` matches (saldo>0 rows)
    - `grep "Registrar cobro" CuentasPorCobrarTab.tsx` matches (opens CobroModal)
    - `grep "balance" CuentasPorCobrarTab.tsx` matches (per-client StoreClient.balance)
    - `grep "totalSaldo\|total" CuentasPorCobrarTab.tsx` matches (aggregate)
    - `grep "fromDate\|toDate" ventago-app/src/hooks/api/useDeliveryHistory.ts` matches
  </acceptance_criteria>
  <done>Cuentas por cobrar: saldo>0 red rows + per-client balance + aggregate + cobro→0 removes row.</done>
</task>

<task type="auto">
  <name>Task 2: HistorialTab (closed orders + date range)</name>
  <read_first>
    - ventago-app/src/views/restaurante/DeliveryBoard.tsx:86-108 (Historial view fromDate/toDate/toExclusive + useDeliveryHistory)
    - ventago-app/src/views/ventas-online/VentasOnlineView.tsx (ReturnsTable table/empty/loading)
    - ventago-app/src/configs/envioLabels.ts (plan 42-05 — status/payment labels)
  </read_first>
  <files>ventago-app/src/views/ventas-online/HistorialTab.tsx</files>
  <action>
    HistorialTab: date-range filter (fromDate/toDate, toExclusive) + table of CLOSED orders = DELIVERED-and-fully-paid + CANCELLED (RD-8). Columns: order#, client, channel, total, final paymentStatus, transporte, dispatched/delivered dates, outcome (Entregado/Cancelado). Use envioLabels for badges. Clicking a row may open the read-only timeline (optional — reuse EnvioTimeline if available). Empty/loading states. pageSize≤50, no polling.
    ESLint compliant.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "Historial" src/views/ventas-online/HistorialTab.tsx && grep -q "fromDate\|toDate" src/views/ventas-online/HistorialTab.tsx && grep -q "Cancelado\|Entregado\|delivered\|cancelled" src/views/ventas-online/HistorialTab.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fromDate\|toDate" ventago-app/src/views/ventas-online/HistorialTab.tsx` matches (date range)
    - `grep "Entregado\|Cancelado\|delivered\|cancelled" HistorialTab.tsx` matches (closed states)
    - `grep "pageSize" HistorialTab.tsx` with value ≤50, or default ≤50
    - `grep "setInterval\|refreshInterval" HistorialTab.tsx` returns nothing
  </acceptance_criteria>
  <done>Historial: date-range filtered closed-order table (delivered-paid + cancelled).</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide verification + source-coverage check</name>
  <read_first>
    - .planning/phases/42-retail-envios-despacho-cobranza/42-RESEARCH.md:485-499 (소매 무회귀 checklist)
    - all 42-0X-SUMMARY.md files produced so far
  </read_first>
  <files>ventago-app/src/views/ventas-online/CuentasPorCobrarTab.tsx</files>
  <action>
    Run the cross-cutting verification (no new feature code — confirmation only):
    1) Backend full suite: `cd api-ventago && npx jest online-orders transportes credit box-operation` — all green.
    2) Frontend lint on all Phase 42 touched files: `cd ventago-app && npx next lint` — no errors (ESLint blocks build).
    3) Confirm RD-1..RD-12 coverage: each requirement maps to a plan (RD-1→01/03/05, RD-2→02/04/05/06, RD-3/4→02, RD-5→02/07, RD-6→02/07, RD-7→02/05/08, RD-8→08, RD-9→04/06, RD-10→03, RD-11→07, RD-12→03/06).
    4) RD-12 regression spot-check (manual, documented in SUMMARY): use_envios=false store shows legacy Ventas Online tabs; fully-paid online deliver still mirrors PAID + appears in reports; no double-count.
    Record results in 42-08-SUMMARY.md. If any gap found, surface it (do not silently pass).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest online-orders transportes credit box-operation 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd api-ventago && npx jest online-orders transportes credit box-operation` exits 0
    - `cd ventago-app && npx next lint` reports no errors on Phase 42 files
    - 42-08-SUMMARY.md documents RD-1..RD-12 coverage + RD-12 regression spot-check result
  </acceptance_criteria>
  <done>Full backend suite + frontend lint green; RD coverage + retail no-regression documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cuentas/historial fetch | Authenticated; storeId server-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-23 | Info Disclosure | cuentas/historial endpoints | mitigate | storeId-scoped WHERE on list/cuentas routes; client never sends storeId |
| T-42-24 | Tampering | cobro from cuentas tab | mitigate | same server-validated cobro path as timeline (registerPayment + caja) |
</threat_model>

<verification>
- Cuentas por cobrar: saldo>0 only, per-client balance, cobro→0 removes row
- Historial: date-range closed orders
- Full backend jest suite green; frontend lint clean
- RD-1..RD-12 coverage documented; RD-12 retail no-regression confirmed
</verification>

<success_criteria>
RD-7/RD-8: 외상 control tab (saldo>0 red, per-client balance, cobro to close) + closed-order Historial with date range, completing the 3-tab center. Phase-wide suite green and source coverage verified.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-08-SUMMARY.md`
</output>
