---
phase: 42-retail-envios-despacho-cobranza
plan: 05
type: execute
wave: 5
depends_on: [01, 04]
files_modified:
  - ventago-app/src/context/StoreConfigContext.tsx
  - ventago-app/src/hooks/api/useTransportes.ts
  - ventago-app/src/hooks/api/useDespachoBoard.ts
  - ventago-app/src/hooks/api/useCuentasPorCobrar.ts
  - ventago-app/src/configs/envioLabels.ts
  - ventago-app/src/views/configuracion/transporte/TransporteCard.tsx
autonomous: true
requirements: [RD-1, RD-2, RD-7]
must_haves:
  truths:
    - "StoreConfig context exposes useEnvios (default false) fetched from the backend"
    - "The Transporte config card is shown only when useEnvios is true and supports add/edit/activo-toggle with inline error surfacing"
    - "SWR hooks exist for transportes (5min dedup), despacho board (branchId-gated, no polling), and cuentas por cobrar"
    - "Status/channel label + color maps exist for the retail despacho machine"
  artifacts:
    - path: "ventago-app/src/hooks/api/useTransportes.ts"
      provides: "useTransportes SWR hook"
      contains: "dedupingInterval: 300000"
    - path: "ventago-app/src/hooks/api/useDespachoBoard.ts"
      provides: "useDespachoBoard(branchId) null-key gated"
      contains: "useDespachoBoard"
    - path: "ventago-app/src/views/configuracion/transporte/TransporteCard.tsx"
      provides: "Transporte CRUD card gated by useEnvios"
      contains: "useEnvios"
    - path: "ventago-app/src/configs/envioLabels.ts"
      provides: "channel + status label/color maps"
      contains: "statusLabel"
  key_links:
    - from: "TransporteCard"
      to: "/transportes API"
      via: "apiConnector get/post/put (remove for none — soft toggle)"
      pattern: "apiConnector"
    - from: "StoreConfigContext"
      to: "store config fetch"
      via: "useEnvios mapped from response"
      pattern: "useEnvios"
---

<objective>
Build the frontend foundation: extend StoreConfig context with the `useEnvios` flag, add the gated Transporte config card (Repartidores card clone), and create the SWR hooks + label configs that the 3-tab board (plans 42-06/07/08) consume. Doing the data layer first means the board plans receive ready contracts (interface-first ordering).

Purpose: RD-1 (Transporte card UI + use_envios gate), RD-2 (board hook), RD-7 (cuentas por cobrar hook). 5min SWR dedup + no-polling board per CLAUDE.md.
Output: useEnvios context, useTransportes/useDespachoBoard/useCuentasPorCobrar hooks, envioLabels config, TransporteCard.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-retail-envios-despacho-cobranza/42-PATTERNS.md
@.planning/phases/42-retail-envios-despacho-cobranza/42-CONTEXT.md

<interfaces>
Analogs:
- ventago-app/src/context/StoreConfigContext.tsx (L18-80 state/default/fetch mapping — useRestaurantMode) → add useEnvios (default false)
- ventago-app/src/hooks/api/useRepartidores.ts (full) → useTransportes
- ventago-app/src/hooks/api/useDeliveryBoard.ts (full, branchId null-key gate, EnvioCard interface) → useDespachoBoard
- ventago-app/src/configs/deliveryLabels.ts (full — canalLabel/statusLabel/statusColor) → envioLabels
- ventago-app/src/views/configuracion/restaurante/RepartidoresCard.tsx (full, 236 lines — gate L42, double error L45-48, apiConnector.put toggle L79, theme NAVY_BG/GOLD L21-22, Switch gold L214-217) → TransporteCard (drop phone)
- ventago-app/src/views/restaurante/RestauranteConfigView.tsx (card placement reference)

apiConnector: get/post/put/remove (NOT .delete).
Backend endpoints (ALL exist — owned by earlier plans, consume as-is, do NOT add/branch):
- GET /online-orders/board/:branchId (plan 42-04 Task 2)
- GET /online-orders/cuentas-por-cobrar → { rows:[{ orderId, orderNumber, clientId, clientName, total, recibido, saldo, branchId, balance }], totalSaldo } (plan 42-04 Task 3 — RD-7 data source, storeId-scoped)
- GET/POST /transportes, PUT /transportes/:id (plan 42-01)
EnvioCard fields (from toCard, plan 42-04): id, orderNumber, channel, clientName, address, total, paymentStatus, saldo, transporteName, trackingCode, columnKey.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useEnvios in StoreConfigContext + envioLabels config</name>
  <read_first>
    - ventago-app/src/context/StoreConfigContext.tsx:18-80 (state shape, default values, fetch→state mapping for useRestaurantMode)
    - ventago-app/src/configs/deliveryLabels.ts (full — canalLabel/statusLabel/statusColor structure)
  </read_first>
  <files>ventago-app/src/context/StoreConfigContext.tsx, ventago-app/src/configs/envioLabels.ts</files>
  <action>
    1) StoreConfigContext: add `useEnvios: boolean` to the context type + state (default false) + map it from the config fetch response (mirror useRestaurantMode mapping exactly). Keep Provider value memoized with useMemo (CLAUDE.md). Add useEnvios to the dependency array.
    2) envioLabels.ts: clone deliveryLabels shape. canalLabel for channels per spec §5.2: Web/WhatsApp/Teléfono/MercadoLibre/Instagram (map OnlineOrderChannel enum: webshop→'Web', whatsapp→'WhatsApp', other→'Teléfono', mercadolibre→'MercadoLibre', instagram→'Instagram'). statusLabel for columns: nuevo→'Nuevo', preparando→'Preparando', listo→'Listo p/ despacho', en_transito→'En tránsito', entregado→'Entregado'. statusColor map (navy/gold theme). paymentStatus labels: Pagado/Parcial/Sin pagar with colors (saldo>0 red).
    ESLint: newline-before-return, lines-around-comment.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "useEnvios" src/context/StoreConfigContext.tsx && grep -q "statusLabel" src/configs/envioLabels.ts && grep -q "Listo p/ despacho" src/configs/envioLabels.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useEnvios" ventago-app/src/context/StoreConfigContext.tsx` matches in type + state + fetch mapping
    - `grep "useMemo" ventago-app/src/context/StoreConfigContext.tsx` still present (value memoized)
    - `grep "statusLabel\|canalLabel\|statusColor" ventago-app/src/configs/envioLabels.ts` matches
    - `grep "Listo p/ despacho" ventago-app/src/configs/envioLabels.ts` matches
  </acceptance_criteria>
  <done>useEnvios in StoreConfig context (default false, fetched); envioLabels with retail channel/status/payment maps.</done>
</task>

<task type="auto">
  <name>Task 2: SWR hooks — useTransportes, useDespachoBoard, useCuentasPorCobrar</name>
  <read_first>
    - ventago-app/src/hooks/api/useRepartidores.ts (full — useApi, dedupingInterval, return shape, mutate)
    - ventago-app/src/hooks/api/useDeliveryBoard.ts (full — branchId null-key gate, no polling)
    - ventago-app/src/hooks/api/useDeliveryHistory.ts (date-range fetch shape — for cuentas reference)
    - .planning/phases/42-retail-envios-despacho-cobranza/42-04-SUMMARY.md (confirms GET /online-orders/cuentas-por-cobrar exists + its response shape)
  </read_first>
  <files>ventago-app/src/hooks/api/useTransportes.ts, ventago-app/src/hooks/api/useDespachoBoard.ts, ventago-app/src/hooks/api/useCuentasPorCobrar.ts</files>
  <action>
    1) useTransportes.ts (clone useRepartidores): `TransporteRow { id, name, isActive }`; useApi('/transportes', { dedupingInterval: 300000 }); return { transportes: data ?? [], error, isLoading, mutate }.
    2) useDespachoBoard.ts (clone useDeliveryBoard): export `EnvioCard` interface matching backend toCard (id, orderNumber, channel, clientName, address, total, paymentStatus, saldo, transporteName, trackingCode, columnKey). useApi(branchId ? `/online-orders/board/${branchId}` : null) — null key skips fetch when no branch. NO polling (refreshInterval omitted) — socket push only (D-11). return { cards: data ?? [], error, isLoading, mutate }.
    3) useCuentasPorCobrar.ts: consume the EXISTING backend route `GET /online-orders/cuentas-por-cobrar` (owned by plan 42-04 Task 3 — do NOT add or branch the endpoint here). useApi('/online-orders/cuentas-por-cobrar', { dedupingInterval: 300000 }). Map the response { rows, totalSaldo } where each row = { orderId, orderNumber, clientId, clientName, total, recibido, saldo, branchId, balance }. Return { rows: data?.rows ?? [], totalSaldo: data?.totalSaldo ?? 0, isLoading, mutate }. 5min dedup.
    ESLint compliant. apiConnector via useApi hook (default export pattern per project).
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "dedupingInterval: 300000" src/hooks/api/useTransportes.ts && grep -q "export interface EnvioCard" src/hooks/api/useDespachoBoard.ts && grep -q "branchId ?" src/hooks/api/useDespachoBoard.ts && grep -q "cuentas-por-cobrar" src/hooks/api/useCuentasPorCobrar.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "dedupingInterval: 300000" ventago-app/src/hooks/api/useTransportes.ts` matches (5min dedup)
    - `grep "export interface EnvioCard" ventago-app/src/hooks/api/useDespachoBoard.ts` matches with columnKey + saldo fields
    - `grep "branchId ?" ventago-app/src/hooks/api/useDespachoBoard.ts` matches (null-key gate)
    - `grep "refreshInterval" ventago-app/src/hooks/api/useDespachoBoard.ts` returns nothing (no polling, D-11)
    - `grep "cuentas-por-cobrar" ventago-app/src/hooks/api/useCuentasPorCobrar.ts` matches (consumes the existing 42-04 route; no endpoint added here)
    - `grep "totalSaldo" ventago-app/src/hooks/api/useCuentasPorCobrar.ts` matches
  </acceptance_criteria>
  <done>Three SWR hooks: transportes (5min), board (branchId-gated no-polling), cuentas por cobrar (consumes existing 42-04 endpoint).</done>
</task>

<task type="auto">
  <name>Task 3: TransporteCard config card (useEnvios gated, soft toggle)</name>
  <read_first>
    - ventago-app/src/views/configuracion/restaurante/RepartidoresCard.tsx (full 236 lines — gate L42, inline Alert + toast.error L45-48, apiConnector.put toggle L79, theme L21-22, Switch L214-217)
    - ventago-app/src/views/restaurante/RestauranteConfigView.tsx (where the card is placed/rendered)
    - .claude/skills/sketch-findings-ace-online/SKILL.md (dark-navy + gold theme, MUI mapping)
  </read_first>
  <files>ventago-app/src/views/configuracion/transporte/TransporteCard.tsx</files>
  <action>
    Clone RepartidoresCard.tsx → TransporteCard.tsx, drop the `phone` field entirely. Key requirements:
    - Gate: `const { useEnvios } = useStoreConfig(); if (!useEnvios) return null;` (replace useRestaurantMode).
    - List transportes (useTransportes hook), inline add (name only), edit name, activo Switch toggle via `apiConnector.put('/transportes/'+id, { isActive: !r.isActive })` then mutate. NO hard delete (soft toggle preserves history — D-04).
    - Double error surface: inline MUI `Alert` + `toast.error` (feedback_error_visibility memory).
    - Theme: NAVY_BG '#0f0f1e', GOLD '#f5a623', gold Switch styling (sketch-findings skill).
    - Register/render the card in the configuración view alongside other config cards (mirror RestauranteConfigView placement; gate ensures it hides for non-envios stores).
    - Code-split if it's a heavy view: wrap with next/dynamic ssr:false at the parent if following the project pattern.
    ESLint: newline-before-return, lines-around-comment, no-unused-vars. apiConnector.remove not used (soft toggle only).
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "useEnvios" src/views/configuracion/transporte/TransporteCard.tsx && grep -q "if (!useEnvios) return null" src/views/configuracion/transporte/TransporteCard.tsx && ! grep -q "phone" src/views/configuracion/transporte/TransporteCard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "if (!useEnvios) return null" ventago-app/src/views/configuracion/transporte/TransporteCard.tsx` matches (gate)
    - `grep "phone" ventago-app/src/views/configuracion/transporte/TransporteCard.tsx` returns nothing (phone dropped)
    - `grep "apiConnector.put" TransporteCard.tsx` matches (toggle); `grep "apiConnector.remove\|apiConnector.delete" TransporteCard.tsx` returns nothing (soft toggle)
    - `grep "Alert" TransporteCard.tsx` and `grep "toast.error" TransporteCard.tsx` both match (double error surface)
    - card rendered in a configuración view file
  </acceptance_criteria>
  <done>TransporteCard: useEnvios-gated, add/edit/activo-toggle, soft toggle, double error surface, navy+gold theme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /transportes API | Authenticated; storeId server-derived; UI cannot escalate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-16 | Info Disclosure | TransporteCard visibility | mitigate | useEnvios gate hides card for non-envios stores; backend storeId scope is the real guard |
| T-42-17 | Tampering | client-supplied storeId | mitigate | hooks never send storeId; backend derives from JWT (@GetUser) |
</threat_model>

<verification>
- useEnvios in StoreConfig context (default false, memoized value)
- hooks: 5min dedup (transportes/cuentas), no-polling board, branchId null-key gate
- useCuentasPorCobrar consumes the existing 42-04 cuentas-por-cobrar route (no endpoint added in frontend)
- TransporteCard gated, soft toggle, no phone, double error surface
- `cd ventago-app && npx next lint` clean on touched files (ESLint blocks build)
</verification>

<success_criteria>
RD-1/RD-2/RD-7 data layer: useEnvios gate available app-wide, Transporte card manageable when enabled, SWR contracts (EnvioCard, transportes, cuentas) ready for the board/timeline/cobro plans — cuentas hook bound to the concrete 42-04 backend route.
</success_criteria>

<output>
After completion, create `.planning/phases/42-retail-envios-despacho-cobranza/42-05-SUMMARY.md`
</output>
