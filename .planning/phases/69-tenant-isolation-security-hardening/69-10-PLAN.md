---
phase: 69-tenant-isolation-security-hardening
plan: 10
type: execute
wave: 6
depends_on: [69-09]
files_modified:
  - .planning/phases/69-tenant-isolation-security-hardening/69-DEPLOY-RUNBOOK.md
  - .planning/phases/69-tenant-isolation-security-hardening/69-UAT.md
autonomous: false
requirements: [R1, R2, R3, R4, R5]

must_haves:
  truths:
    - "배포 순서(app → api)와 env 변경이 런북으로 문서화된다"
    - "운영 배포 후 5개 결함이 실제로 막혔음을 사람이 확인한다"
    - "실시간 소켓·POS 판매·프린터·벤더 앱의 무회귀가 확인된다"
    - "회귀 시 즉시 하향할 env 롤백 절차가 검증된다"
  artifacts:
    - path: ".planning/phases/69-tenant-isolation-security-hardening/69-DEPLOY-RUNBOOK.md"
      provides: "배포 순서 · env · 롤백 절차"
    - path: ".planning/phases/69-tenant-isolation-security-hardening/69-UAT.md"
      provides: "브라우저/앱 UAT 결과"
  key_links:
    - from: "69-DEPLOY-RUNBOOK.md"
      to: "Jenkins 빌드 확인"
      via: "push → 자동 빌드 → 컨테이너 재생성"
      pattern: "Jenkins"
---

<objective>
Phase 69 의 5개 교정을 운영에 반영하고, **결함이 실제로 막혔는지**와 **아무것도 깨지지 않았는지**를
사람이 확인한다. 이 Phase 는 신규 기능 0 이므로 UAT 의 절반은 "무회귀 확인"이다.

특히 위험한 두 지점:
1. **소켓 인증 도입** — ventago-app 이 api-ventago 보다 늦게 배포되면 유예 10초 뒤 끊겨
   팀 채팅·MP 승인 자동 판매·프린터 상태가 조용히 멈춘다 → **app 먼저 배포**
2. **벤더 토큰 무효화** — 69-05 배포 즉시 기존 벤더 세션이 전부 만료된다 → 재로그인 안내 필요

Purpose: 배포가 곧 사고가 되지 않게 순서와 롤백을 문서로 고정하고, 결함 봉쇄를 실증한다.
Output: 배포 런북 + UAT 결과 문서.

**DDL 없음** — Phase 69 전체에서 마이그레이션 대상 SQL 은 **0건**이다.
변경되는 것은 코드와 `.env` 키뿐이다 (`.env` 는 컨테이너 생성 시에만 주입되므로 `--force-recreate` 필요).
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-tenant-isolation-security-hardening/69-CONTEXT.md
@CLAUDE.md

<interfaces>
배포 경로 (CLAUDE.md 「상시 규칙: push 와 마이그레이션은 Claude 가 직접」):
- push → Jenkins webhook 자동 빌드 (`front-coolsistema` / `api-new-coolsistema`) → docker compose build && up
- 빌드 실패 시 `/var/lib/jenkins/jobs/<job>/builds/<n>/log` 확인 → 수정 → 재push
- "push 완료" = 빌드 성공 + 운영 컨테이너 재생성 확인까지
- `.env` 키 추가 시 `docker compose up -d --force-recreate` 필수

env 키 (69-07 / 69-08 결과에 따름):
- `TENANT_GUARD_MODE` (기존, 기본 enforce)
- `TENANT_DERIVED_MODE` (69-07 에서 승격 여부 결정)
- `TENANT_CONTEXT_FAIL_MODE` (69-08 신규, 기본 enforce)

운영 매장: CART(3), coolsistema(6), genius(8), ACE(9)
운영 로그: `ssh jhkim-server "docker logs api_ventago --since 1h 2>&1 | grep -E 'tenant fail-closed|derived observe|AUTH FAIL'"`

리포지토리 구성: `api-ventago`, `ventago-app` 은 루트 저장소 안의 별도 git 저장소(gitlink)다.
`talleres-vendor-app` 은 루트 저장소의 일반 디렉터리다. `mobile-sales-app` 만 .gitmodules 등록 서브모듈이다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 배포 런북 작성 + 운영 반영</name>
  <read_first>
    - .planning/phases/69-tenant-isolation-security-hardening/69-01-SUMMARY.md ~ 69-09-SUMMARY.md (전 플랜 결과)
    - CLAUDE.md (「상시 규칙: push 와 마이그레이션은 Claude 가 직접」, 「DB 마이그레이션 적용 규칙」)
    - api-ventago/.env.example (69-07 이 갱신)
  </read_first>
  <action>
`.planning/phases/69-tenant-isolation-security-hardening/69-DEPLOY-RUNBOOK.md` 를 작성한다:

- `## 0. 사전 확인` — `npm run test:tenant` / `npx jest` / `cd ventago-app && npx tsc --noEmit` 전부 초록,
  69-04·69-07 승인 기록 존재
- `## 1. DDL` — **없음**. 로컬 5432 / 운영 5434 적용 대상 0건임을 명시 (오해 방지)
- `## 2. 배포 순서` (순서가 안전의 전부):
  1) `ventago-app` push → Jenkins `front-coolsistema` 빌드 성공 확인 → 컨테이너 재생성 확인
  2) `talleres-vendor-app` (Flutter) 빌드/배포 — 벤더 앱 사용자에게 재로그인 안내
  3) 운영 `.env` 에 `TENANT_DERIVED_MODE` / `TENANT_CONTEXT_FAIL_MODE` 기록 (69-07/69-08 결정값)
  4) `api-ventago` push → Jenkins `api-new-coolsistema` 빌드 성공 확인
  5) `docker compose up -d --force-recreate` (env 반영)
  6) 부팅 로그에서 `installTenantGuard` 요약(mode/guarded/derivedMode/derived) 확인
- `## 3. 배포 후 즉시 확인(5분)` — 운영 로그에서 `AUTH FAIL`, `tenant fail-closed`, `derived observe` 급증 여부
- `## 4. 롤백` — 코드 revert 없이 가능한 순서:
  ① `TENANT_CONTEXT_FAIL_MODE=observe` ② `TENANT_DERIVED_MODE=observe` ③ 그래도 안 되면 이전 이미지로 롤백.
  각 단계에 명령과 확인 방법을 적는다
- `## 5. 사용자 통지` — 벤더 재로그인 안내 문안(스페인어), 69-04 (B) 버킷 매장 통지 대상

작성 후 런북 순서대로 **실제 배포를 수행**한다. 각 단계의 Jenkins 빌드 번호와 결과를 런북에 기록한다.
빌드 실패 시 로그를 읽고 수정 후 재push 한다 (CLAUDE.md 규약 — "push 완료"는 빌드 성공까지).
  </action>
  <verify>
    <automated>ssh jhkim-server "docker ps --format '{{.Names}} {{.Status}}' | grep -E 'api_ventago|front'"</automated>
  </verify>
  <acceptance_criteria>
    - `69-DEPLOY-RUNBOOK.md` 의 `## 1. DDL` 절이 "마이그레이션 대상 0건" 을 명시한다
    - 런북에 ventago-app 이 api-ventago **앞**에 오는 순서가 적혀 있고, 그 이유(소켓 유예 창)가 함께 적혀 있다
    - 각 배포 단계에 실제 Jenkins 빌드 번호와 성공 여부가 기록되어 있다
    - `ssh jhkim-server "docker ps"` 에서 `api_ventago` 컨테이너가 재생성 시각 이후 Up 상태다
    - 부팅 로그의 `derivedMode` 값이 69-07 결정값과 일치함이 런북에 기록되어 있다
  </acceptance_criteria>
  <done>운영에 반영되고 롤백 경로가 문서화된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 운영 UAT — 결함 봉쇄 5건 + 무회귀 6건</name>
  <read_first>
    - .planning/phases/69-tenant-isolation-security-hardening/69-DEPLOY-RUNBOOK.md (Task 1 결과)
    - .planning/phases/69-tenant-isolation-security-hardening/69-CONTEXT.md (Success Criteria 절)
  </read_first>
  <what-built>
R1 소켓 핸드셰이크 인증 + room 소유권 · R2 correct-today 소유권 검증 · R3 벤더 토큰 단일 매장 scope ·
R4 파생 스코프 확장/승격 · R5 TenantContext fail-closed. 전부 운영 배포 완료 상태.
  </what-built>
  <how-to-verify>
**A. 결함 봉쇄 확인 (막혀야 정상)**

A1. (R1) 브라우저 콘솔에서 토큰 없이 접속 시도:
```
const s = io('https://newapi.coolsistema.com/realtime', { transports:['websocket'] });
s.on('connect', () => s.emit('register_branch', { branchId: 1 }));
s.on('disconnect', r => console.log('DISCONNECTED', r));
```
→ 기대: 10초 내 `DISCONNECTED`, 그 사이 어떤 이벤트도 수신되지 않음

A2. (R1) 로그인 상태에서 **타 매장** branchId 로 `register_branch` emit
→ 기대: `join_error` 수신, 해당 지점 `agent_status_changed` 미수신

A3. (R2) 매장 A 계정으로 `PUT /products/{자기 상품}/correct-today` 에 매장 B 의 `branchId` 를 넣어 호출
→ 기대: 403. 이후 매장 B 의 Stocks 보고서에 새 조정 행이 **없음** (화면에서 확인)

A4. (R3) 벤더 앱: 매장 X PIN 으로 로그인 → 매장 목록에 매장 X 만 보임.
    (69-04 (B) 버킷 벤더가 있으면 그 계정으로 확인)
→ 기대: 타 매장 envíos/settlements 접근 불가

A5. (R5) 운영 로그에 `[tenant fail-closed]` 가 정상 사용 중 **발생하지 않음**
`ssh jhkim-server "docker logs api_ventago --since 1h 2>&1 | grep 'tenant fail-closed' | wc -l"`
→ 기대: 0 (1 이상이면 어느 라우트인지 확인 후 보고)

**B. 무회귀 확인 (동작해야 정상)**

B1. 팀 채팅 — 두 계정 간 메시지 송수신 실시간 도착
B2. POS 판매 1건 완료 — 감열 영수증 출력됨 (print-agent 온라인 표시)
B3. MP QR 결제 1건 — 승인 후 자동 판매 생성(터미널 room push 수신)
B4. 재고 화면 — 상품 목록·변형 재고 매트릭스가 비어 있지 않음 (R4 enforce JOIN 회귀 확인)
B5. 가격 화면 — PRECIO 목록 정상 표시 (Price 파생 모델)
B6. zebra-agent 라벨 출력 1건 — QR/바코드 정상

각 항목을 `69-UAT.md` 에 PASS/FAIL + 증거(스크린샷 경로 또는 로그 라인)로 기록한다.
FAIL 이 나오면 런북 `## 4. 롤백` 을 따르고 그 사실을 기록한다.
  </how-to-verify>
  <acceptance_criteria>
    - `69-UAT.md` 에 A1~A5, B1~B6 총 11개 항목이 각각 PASS/FAIL 로 기록되어 있다
    - A3 항목에 "매장 B Stocks 행 증가 0" 이 확인 방법과 함께 기록되어 있다
    - A5 항목에 `grep 'tenant fail-closed' | wc -l` 실제 출력값이 적혀 있다
    - FAIL 항목이 있으면 롤백 수행 여부와 후속 조치가 기록되어 있다
    - B1~B6 중 미수행 항목이 있으면 사유가 명시되어 있다 (공란 금지)
  </acceptance_criteria>
  <action>
아래 `how-to-verify` 의 A1~A5(결함 봉쇄) · B1~B6(무회귀) 11개 항목을 운영 환경에서 순서대로 수행하고,
각 결과를 `.planning/phases/69-tenant-isolation-security-hardening/69-UAT.md` 에 PASS/FAIL + 증거와 함께 기록한다.
FAIL 이 나오면 런북 `## 4. 롤백` 절차를 즉시 수행하고 그 사실도 기록한다.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/69-tenant-isolation-security-hardening/69-UAT.md</automated>
  </verify>
  <done>11개 항목 전부가 PASS/FAIL 로 판정되고 증거가 69-UAT.md 에 남는다.</done>
  <resume-signal>모든 항목 확인 후 "approved" 또는 실패 항목 번호와 증상을 알려주세요</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ROADMAP/STATE 갱신 + Phase 종결 정리</name>
  <read_first>
    - .planning/ROADMAP.md (`### Phase 69` 엔트리)
    - .planning/STATE.md (Current Position / Roadmap Evolution)
    - .planning/phases/69-tenant-isolation-security-hardening/69-UAT.md (Task 2 결과)
  </read_first>
  <action>
1. `ROADMAP.md` 의 `### Phase 69` 엔트리 `**Plans:**` 줄을 실제 결과로 갱신한다 —
   10 plans, 각 plan 파일명과 한 줄 목적, 체크박스 상태.
   `Waves:` 줄도 실제 wave 구성(1~6)으로 맞춘다.
2. `STATE.md` 의 `Current Position` 과 `stopped_at` 을 Phase 69 결과로 갱신하고,
   `Roadmap Evolution` 의 Phase 69 항목에 결과 요약(결함 5건 봉쇄 / DDL 0 / 회귀 스위트 추가)을 덧붙인다.
3. Phase Success Criteria 8개 각각에 대해 충족 여부를 `69-UAT.md` 하단 `## Success Criteria 대조` 표로 정리한다.
   미충족 항목이 있으면 후속 작업(별도 Phase 후보)으로 명시한다 — 조용히 넘기지 않는다.
4. 남은 미결(69-06 의 3단계 부모 체인 모델, vendor 멀티스토어 identity 재설계 등)을
   `## 후속 Phase 후보` 로 한 곳에 모은다.
5. `## 후속 Phase 후보` 에 **구버전 print-agent CLI 잔존 모니터링**을 1줄 추가한다.
   69-02 가 CLI 를 handshake 인증으로 올리므로 `register_api_key` 10초 유예 폴백은
   전 CLI 갱신 후 제거 대상이다. 잔존 비율을 확인할 방법(에이전트 버전 로그 또는
   `branch_agents.last_seen_at` 기준 구버전 접속 카운트)과 제거 조건을 적어 둔다 —
   지금 지우면 미갱신 매장이 조용히 끊긴다.
  </action>
  <verify>
    <automated>grep -c "69-0[1-9]-PLAN.md\|69-10-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `ROADMAP.md` 의 Phase 69 엔트리에 10개 plan 파일명이 전부 나열되어 있다
    - `STATE.md` 의 `stopped_at` 이 Phase 69 결과를 반영한다
    - `69-UAT.md` 에 Success Criteria 8개 전부에 대한 충족/미충족 판정이 있다
    - 미충족 항목마다 후속 계획이 1줄 이상 적혀 있다
  </acceptance_criteria>
  <done>Phase 69 의 결과와 잔여가 문서에 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 순서 → 실시간 기능 가용성 | api 가 app 보다 먼저 나가면 소켓 소비자가 끊긴다 |
| 운영 `.env` → 격리 강도 | 키 누락 시 코드 기본값으로 동작 (69-07/69-08 결정과 어긋날 수 있음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-39 | Denial of Service | app/api 배포 순서 역전 | mitigate | 런북에 순서 고정 + 이유 명시, UAT B1~B3 로 실시간 경로 확인 |
| T-69-40 | Denial of Service | 벤더 토큰 일괄 무효화 | mitigate | 사전 통지 문안 + UAT A4 에서 재로그인 확인 |
| T-69-41 | Elevation of Privilege | `.env` 키 누락으로 격리 강도 하락 | mitigate | 부팅 로그의 mode 값을 런북에 기록·대조 |
| T-69-42 | Repudiation | UAT 항목 공란 방치 | mitigate | 미수행 사유 명시를 acceptance 로 강제 |
</threat_model>

<verification>
- 운영 컨테이너 `api_ventago` / 프런트 컨테이너가 배포 시각 이후 Up
- `69-UAT.md` 11개 항목 전부 판정 완료
- 운영 로그 1시간 창에서 `tenant fail-closed` 0건
- DDL 0건 — 로컬 5432 / 운영 5434 스키마 무변경 (런북에 명시)
</verification>

<success_criteria>
- Phase 69 Success Criteria 8개에 대한 충족 여부가 증거와 함께 판정됨
- 결함 5건이 운영에서 실제로 막힘이 확인됨
- 실시간·POS·프린터·벤더 앱 무회귀 확인
- 롤백 경로가 문서화되고 필요 시 즉시 사용 가능
</success_criteria>

<output>
완료 후 `.planning/phases/69-tenant-isolation-security-hardening/69-10-SUMMARY.md` 를 작성한다.
SUMMARY 에 Jenkins 빌드 번호, UAT PASS/FAIL 집계, 미충족 Success Criteria 와 후속 계획을 기록한다.
</output>
