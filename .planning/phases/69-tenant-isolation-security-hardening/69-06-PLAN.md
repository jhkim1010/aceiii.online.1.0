---
phase: 69-tenant-isolation-security-hardening
plan: 06
type: execute
wave: 2
depends_on: [69-03]
files_modified:
  - api-ventago/src/common/tenant/tenant-scope.registry.ts
  - api-ventago/src/common/tenant/tenant-hooks.ts
  - api-ventago/src/common/tenant/tenant-hooks.spec.ts
  - api-ventago/src/common/tenant/tenant-derived.spec.ts
  - .planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md
autonomous: true
requirements: [R4]

must_haves:
  truths:
    - "store_id 컬럼이 없는 모델 전수(64개)가 파생대상/면제로 분류되어 문서화된다"
    - "ProductBranch 는 Product 와 Branch 양쪽 소유권을 동시에 검증한다"
    - "파생 규칙이 다중 부모를 지원한다"
    - "enforce 모드에서 주입되는 include 는 required:true 이고 attributes 를 늘리지 않는다"
  artifacts:
    - path: "api-ventago/src/common/tenant/tenant-scope.registry.ts"
      provides: "확장된 DERIVED_SCOPE (다중 부모 지원)"
      contains: "DERIVED_SCOPE"
    - path: ".planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md"
      provides: "store_id 미보유 모델 64개 전수 분류"
    - path: "api-ventago/src/common/tenant/tenant-derived.spec.ts"
      provides: "파생 스코프 주입 단위 테스트"
  key_links:
    - from: "tenant-hooks.ts injectDerived"
      to: "다중 부모 규칙 배열"
      via: "규칙마다 applyDerivedInclude 반복"
      pattern: "for \\(const rule"
    - from: "tenant-scope.registry.ts DERIVED_SCOPE.ProductBranch"
      to: "Product + Branch"
      via: "규칙 2개"
      pattern: "parentModel: 'Branch'"
---

<objective>
Phase 68 은 파생 스코프(부모 경유 격리)를 만들었지만 등록 모델이 `ProductBranch`/`Stocks`/`Price` **3개뿐**이고,
`ProductBranch` 규칙은 `product` 한쪽만 본다. 레지스트리에 없는 나머지 `store_id` 미보유 관계 모델은
관측 로그조차 남지 않는다 — 즉 **주 모델로 조회하는 순간 4중 방어 전부의 사각지대**다(R2 가 그 실증 사례).

Purpose: (1) 사각지대의 실제 크기를 전수 조사해 문서로 고정하고, (2) 규칙 엔진이 **다중 부모**를 지원하게 만들어
`ProductBranch` 를 Product·Branch 양쪽으로 검증한다.
Output: 분류 문서 + 확장된 레지스트리 + 다중 부모 지원 훅 + 단위 테스트.

**이 플랜은 `TENANT_DERIVED_MODE` 기본값을 바꾸지 않는다** — 여전히 `observe` 다.
enforce 승격은 69-07 에서 사용자 승인을 거쳐 수행한다(회귀 위험 때문에 분리).

**의존 이유:** `correct-today` 호출부가 스스로 소유권을 검증하게 된 뒤(69-03)에야
파생 enforce 가 안전하게 얹힌다. 파일은 겹치지 않지만 순서는 지켜야 한다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-tenant-isolation-security-hardening/69-CONTEXT.md
@CLAUDE.md

<interfaces>
현재 계약 (`tenant-scope.registry.ts` L78-127):
```
export interface DerivedScopeRule { as: string; parentModel: string; through?: DerivedScopeRule; }
const DERIVED_SCOPE: Record<string, DerivedScopeRule> = {
  ProductBranch: { as: 'product', parentModel: 'Product' },
  Stocks: { as: 'productBranch', parentModel: 'ProductBranch', through: { as: 'product', parentModel: 'Product' } },
  Price:  { as: 'product', parentModel: 'Product' },
};
export function resolveDerivedScope(modelName: string): DerivedScopeRule | null
export type DerivedMode = 'off' | 'observe' | 'enforce';
export function resolveDerivedMode(): DerivedMode   // 기본 'observe'
```
소비처 (`tenant-hooks.ts`): `installDerivedForModel` L248-292, `applyDerivedInclude` L203-242,
`buildDerivedInclude` L175-196, `callerHint` L143-159, `logThrottled` L50-57.

키는 **modelName** 이다 (tableName 은 Stocks/Prices 처럼 자동 복수형이라 흔들린다).

이미 알려진 함정(주석에 명시됨): 기존 include 를 재사용할 때 `required` 를 true 로 올리지 않으면
조건이 ON 절로 내려가 필터가 무력화된다 (Sager 사고).

조사 시작점 — `store_id` 컬럼이 없는 모델 64개는 다음 명령으로 재현 가능:
`cd api-ventago && grep -rl "@Table" src | while read -r f; do grep -q "storeId" "$f" || echo "$f"; done`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: store_id 미보유 모델 전수 분류 감사</name>
  <read_first>
    - api-ventago/src/common/tenant/tenant-scope.registry.ts (L57-127, DERIVED_SCOPE 배경 주석)
    - .planning/intel/db-schema-fks.md (FK 그래프 — 부모 판정 근거)
    - api-ventago/src/app/products/branch/products-branch.model.ts (association alias 예시: product / branch)
  </read_first>
  <action>
`store_id` 컬럼이 없는 모델 전체를 열거하고 3버킷으로 분류한다.
**모델 파일을 통째로 읽지 말고** 관계 선언만 뽑아 컨텍스트를 아낀다:
`grep -n "@Table\|@ForeignKey\|@BelongsTo\|declare .*Id\|Id: number" <file>`

버킷 정의:
- **D (derived)** — 매장에 속한 부모가 1~2단계 안에 있고, 주 모델로 조회될 수 있는 것 → 파생 규칙 대상
- **G (global)** — 매장 개념이 없는 참조표(예: `province`, `nation`, `functions`, `modules`, `apps`) → 면제
- **X (no-tenant)** — 워커/시스템/온보딩 전용이라 요청 경로에서 안 도는 것 → 면제

최소 다음 모델은 **반드시 D 로 분류하고 부모 association 이름까지 확정**한다(association alias 는 모델 파일에서 확인):
`SaleItem`(sale) · `SalePaymentMethod`(sale) · `SaleDiscount`(sale) · `SaleRecharge`(sale) ·
`OnlineOrderItem`(onlineOrder) · `OnlineReturn`(onlineOrder) · `BoxOperation`(box) ·
`CajaFuerteOperation`(cajaFuerte) · `MpMovement`/`MpRefund`/`MpTransfer`/`MpRefundAttempt`(mpAccount) ·
`QrPrintLog`(branch) · `BranchAgent`(branch) · `BranchPrinterConfig`(branch) · `BranchPriceTypeDisabled`(branch) ·
`ProductsCategories`(product) · `ProductDiscount`/`SubcategoryDiscount`/`PaymentMethodsDiscount`/`DiscountReason`(discount) ·
`UserRole`(user) · `UserBranch`(user) · `UserPermissionCache`(user) · `RoleFunctionAction`(roleFunction) ·
`UserFunctionAction`(userFunction) · `MobileSession`(user) · `CampaignRecipient`(campaign) ·
`ExpenseCheque`(cheque 또는 expense — FK 확인 후 확정) · `RevendedorCategories`(revendedor) ·
`BomItem`(bom) · `ProductionResult`(workOrder 또는 bom — FK 확인 후 확정) ·
subcon 계열 `VendorEtapa`/`SubconDefect`/`SubconMaterialIssue`/`SubconSettlement`/`SubconPayment`/`EnvioMaterial`/`SubconDelivery`
(subcon 은 `api-ventago/migrations/2026-07-30-tenant-talleres-triggers.sql` 로 쓰기 트리거가 이미 있으니 **읽기 경로만** 보강 대상임을 명시)

`.planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md` 를 작성한다. 구성:
- `## 집계` — 전체 N, D/G/X 각 개수
- `## D — 파생 대상` 표: `modelName | tableName | 부모 association | 부모 modelName | 부모가 store_id 보유? | 비고`
  (부모도 store_id 가 없으면 `through` 2단계로 표기)
- `## G — 전 매장 공용` 표: `modelName | 사유`
- `## X — 요청 경로 밖` 표: `modelName | 사유`
- `## 미결` — FK 그래프만으로 부모를 확정하지 못한 모델과 그 이유

각 분류에 **근거 파일:라인** 을 남긴다. "아마도"라는 추정으로 분류하지 말고 미결로 둔다.
  </action>
  <verify>
    <automated>test -f .planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md && grep -c "|" .planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 문서의 `## 집계` 에서 D+G+X+미결 합계가 `grep -rl "@Table" src | while read -r f; do grep -q "storeId" "$f" || echo "$f"; done | wc -l` 결과와 일치한다
    - `## D` 표에 위 목록의 모델이 전부 등장하고 각 행에 부모 association 이름이 채워져 있다
    - 모든 D 행에 근거 `파일:라인` 이 있다
    - "아마도 / 추정 / 아마" 같은 표현이 D 표에 0회 (미결 절에만 허용)
  </acceptance_criteria>
  <done>사각지대의 실제 크기가 숫자와 목록으로 확정된다.</done>
</task>

<task type="auto">
  <name>Task 2: 다중 부모 지원 + ProductBranch 양쪽 소유권 + 레지스트리 확장</name>
  <read_first>
    - api-ventago/src/common/tenant/tenant-scope.registry.ts (전체)
    - api-ventago/src/common/tenant/tenant-hooks.ts (L137-292 파생 스코프 구역 전체)
    - .planning/phases/69-tenant-isolation-security-hardening/69-DERIVED-MODEL-AUDIT.md (Task 1 산출물)
  </read_first>
  <action>
1. **다중 부모 지원**: `DERIVED_SCOPE` 의 값 타입을 `DerivedScopeRule` 단건에서 `DerivedScopeRule[]` 로 바꾸고
   `resolveDerivedScope(modelName): DerivedScopeRule[] | null` 로 시그니처를 변경한다.
   `tenant-hooks.ts` 의 `injectDerived` 는 규칙 배열을 순회하며 `applyDerivedInclude(options, rule, stores)` 를 각각 호출한다.
   observe 로그도 규칙별이 아니라 모델 단위로 1회만 남긴다(로그 폭주 방지 — `logThrottled` 키 유지).

2. **ProductBranch 양쪽 검증**:
   `ProductBranch: [{ as: 'product', parentModel: 'Product' }, { as: 'branch', parentModel: 'Branch' }]`
   (association alias 는 `products-branch.model.ts` 의 `@BelongsTo(() => Product) product` / `@BelongsTo(() => Branch) branch` 로 확정됨)

3. **Stocks 2단계 유지**: `[{ as:'productBranch', parentModel:'ProductBranch', through:{ as:'product', parentModel:'Product' } }]`.
   `through` 체인 안에서도 다중 부모가 필요하면 `through` 를 배열로 확장하지 말고 **1차 부모만** 둔다(복잡도 억제, 미결로 문서화).

4. **레지스트리 확장**: Task 1 의 D 버킷을 `DERIVED_SCOPE` 에 추가한다.
   부모가 `store_id` 를 가진 모델이면 1단계 규칙, 아니면 `through` 로 2단계까지만 표현한다.
   3단계 이상이 필요한 모델은 추가하지 말고 감사 문서 `## 미결` 로 남긴다.
   각 항목 위에 한 줄 한국어 주석으로 부모 근거를 남긴다.

5. **불변식 보존**: `buildDerivedInclude` 는 계속 `attributes: []` 를 쓰고 `required: true` 를 강제한다.
   기존 include 재사용 시 `existing.required = true` 승격 로직도 유지한다.

6. 기본 모드는 손대지 않는다 — `resolveDerivedMode()` 의 기본값 `'observe'` 를 **이 플랜에서 바꾸지 않는다.**
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `resolveDerivedScope` 의 반환 타입이 배열이다 (`grep -n "DerivedScopeRule\[\]" tenant-scope.registry.ts` ≥ 1건)
    - `DERIVED_SCOPE.ProductBranch` 에 `parentModel: 'Product'` 와 `parentModel: 'Branch'` 가 모두 있다
    - `DERIVED_SCOPE` 항목 수가 Task 1 의 D 버킷 수(미결 제외)와 일치한다
    - `tenant-hooks.ts` 의 `injectDerived` 안에 규칙 배열 순회(`for (const rule ...)`)가 있다
    - `resolveDerivedMode` 기본값이 여전히 `'observe'` 다 (`grep -n "?? 'observe'" tenant-scope.registry.ts` == 1건)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>ProductBranch 가 Product·Branch 양쪽으로 좁혀지고, 사각지대 모델이 최소 observe 로그에는 잡힌다.</done>
</task>

<task type="auto">
  <name>Task 3: 파생 스코프 주입 단위 테스트</name>
  <read_first>
    - api-ventago/src/common/tenant/tenant-hooks.spec.ts (기존 모델 스텁 + 훅 fire 패턴 전체)
    - api-ventago/src/common/tenant/tenant-hooks.ts (Task 2 결과)
  </read_first>
  <action>
`api-ventago/src/common/tenant/tenant-derived.spec.ts` 를 신규 작성한다.
기존 `tenant-hooks.spec.ts` 의 스텁 방식(`makeModel` + `addHook` 캡처 + `fire(event, options)`)을 재사용하되,
`storeId` 속성이 **없는** 모델 스텁(`makeDerivedModel(name)`)을 만든다.

고정할 케이스:
1. `TENANT_DERIVED_MODE=enforce` + `ProductBranch` beforeFind →
   `options.include` 에 `association:'product'` 와 `association:'branch'` 가 **둘 다** 있고 각각 `required === true`,
   `attributes` 가 `[]`, `where` 에 storeId 조건이 있다
2. 같은 조건에서 호출부가 이미 `include: [{ association:'product', required:false }]` 를 넘긴 경우 →
   중복 push 없이 기존 항목이 `required: true` 로 승격되고 where 가 병합된다 (Sequelize "already aliased" 회피)
3. `TENANT_DERIVED_MODE=observe` → `options.include` 가 변경되지 않는다 (undefined 유지)
4. `TENANT_DERIVED_MODE=off` → 훅 자체가 설치되지 않는다 (`hasHook('beforeFind') === false`)
5. `Stocks` 2단계 → `productBranch` include 안에 `product` include 가 중첩되고 안쪽에 storeId where 가 있다
6. 컨텍스트 미해석(`TenantContext` 없음) → 주입 없음 (크론/워커 무영향)
7. `options.tenantGuardBypass === true` → 주입 없음

`resolveDerivedMode` 는 env 를 읽으므로 각 테스트에서 `process.env.TENANT_DERIVED_MODE` 를 설정/복원한다.

`tenant-hooks.spec.ts` 는 기존 destroy 규약 테스트를 유지한 채,
`resolveDerivedScope` 시그니처 변경(배열)에 따른 컴파일 오류만 고친다.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/common/tenant</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/common/tenant` exit 0, 신규 스펙 7개 이상 통과
    - 케이스 1이 `product` 와 `branch` 두 include 의 `required === true` 를 **각각** 단언한다
    - 케이스 2가 `options.include.length` 가 늘어나지 않음을 단언한다
    - 기존 `tenant-hooks.spec.ts` 의 destroy 규약 테스트가 그대로 통과한다
  </acceptance_criteria>
  <done>파생 주입 규약이 테스트로 고정되어 69-07 의 enforce 승격이 안전해진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 코드 → ORM | `store_id` 없는 모델을 주 모델로 조회하면 격리 4중 방어 전부를 우회 |
| 훅 주입 include → SQL JOIN 종류 | `required:false` 면 조건이 ON 절로 내려가 필터가 무력화됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-22 | Information Disclosure | 미등록 파생 모델 조회 | mitigate | 전수 감사 후 D 버킷 전부 `DERIVED_SCOPE` 등록(최소 observe 관측 확보) |
| T-69-23 | Information Disclosure | `ProductBranch` 한쪽 부모만 검증 | mitigate | Product·Branch 두 규칙 동시 적용 |
| T-69-24 | Tampering | 기존 include 재사용 시 required 미승격 | mitigate | `existing.required = true` 강제 + 케이스 2 테스트로 고정 |
| T-69-25 | Denial of Service | JOIN 추가로 인한 느린 쿼리 | accept | 이 플랜은 여전히 observe. 성능 영향 측정은 69-07 승격 시 수행 |
| T-69-26 | Information Disclosure | 3단계 이상 부모 체인 모델 | accept | 미결로 문서화. 별도 Phase 후보 (범위 과대 방지) |
</threat_model>

<verification>
- `cd api-ventago && npx tsc --noEmit -p tsconfig.json` exit 0
- `cd api-ventago && npx jest src/common/tenant` exit 0
- `69-DERIVED-MODEL-AUDIT.md` 의 D+G+X+미결 합계 == 실제 store_id 미보유 모델 수
- `TENANT_DERIVED_MODE` 기본값 무변경(`observe`) 확인
- DDL 없음 — **마이그레이션 대상 0건**
</verification>

<success_criteria>
- 파생 규칙 엔진이 다중 부모를 지원
- ProductBranch 가 Product·Branch 양쪽 소유권 검증
- 사각지대 모델 전수 분류 문서 존재
- 기본 모드는 여전히 observe (승격은 69-07)
</success_criteria>

<output>
완료 후 `.planning/phases/69-tenant-isolation-security-hardening/69-06-SUMMARY.md` 를 작성한다.
SUMMARY 에 D/G/X/미결 개수, 새로 등록한 모델 목록, 69-07 이 관측해야 할 observe 로그 키 형태를 기록한다.
</output>
