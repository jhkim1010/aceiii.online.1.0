---
phase: 69-tenant-isolation-security-hardening
plan: 09
type: execute
wave: 5
depends_on: [69-01, 69-02, 69-03, 69-05, 69-06, 69-07, 69-08]
files_modified:
  - api-ventago/test/tenant/jest-tenant.json
  - api-ventago/test/tenant/cross-tenant.tenant-spec.ts
  - api-ventago/test/tenant/helpers/fixtures.ts
  - api-ventago/test/tenant/README.md
  - api-ventago/package.json
autonomous: true
requirements: [R1, R2, R3, R4, R5]

must_haves:
  truths:
    - "한 번의 명령(npm run test:tenant)으로 R1~R5 교차매장 시나리오가 전부 검증된다"
    - "이 스위트는 Phase 69 이전 코드에서 실패한다 (증거가 문서에 기록된다)"
    - "Phase 64 동시성 스위트 8종이 여전히 통과한다"
  artifacts:
    - path: "api-ventago/test/tenant/cross-tenant.tenant-spec.ts"
      provides: "R1~R5 교차매장 회귀 스위트"
    - path: "api-ventago/test/tenant/README.md"
      provides: "실행법 + 구코드 실패 증거"
    - path: "api-ventago/package.json"
      provides: "test:tenant 스크립트"
      contains: "test:tenant"
  key_links:
    - from: "package.json test:tenant"
      to: "test/tenant/jest-tenant.json"
      via: "jest --config"
      pattern: "jest-tenant"
---

<objective>
개별 플랜(69-01/03/05/06/08)이 각자 스펙을 남겼지만, **경계 전체를 한 번에 확인하는 관문**이 없다.
이 플랜은 R1~R5 의 핵심 시나리오를 하나의 스위트로 묶어 "이 명령이 초록이면 Phase 69 경계가 살아 있다" 는
단일 판정 기준을 만든다.

Purpose: 회귀 보호는 부수물이 아니라 산출물이다. 다음에 누군가 소켓 인증을 되돌리거나
`correct-today` 검증을 지우면 CI 에서 즉시 드러나야 한다.
Output: `npm run test:tenant` 로 실행되는 교차매장 회귀 스위트 + 구코드 실패 증거 문서.

**설계 원칙:** 이 스위트는 실 DB 를 요구하지 않는다(단위/모듈 레벨 mock). 실 DB 가 필요한 동시성 검증은
이미 Phase 64 `npm run test:concurrency` 가 담당하므로 중복하지 않고 **통과 여부만 확인**한다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-tenant-isolation-security-hardening/69-CONTEXT.md
@CLAUDE.md

<interfaces>
기존 jest 구성 (`api-ventago/package.json`):
- 기본: `rootDir: "src"`, `testRegex: ".*\\.spec\\.ts$"`, `moduleNameMapper: { "^src/(.*)$": "<rootDir>/$1" }`
- 별도 프로젝트 선례: `"test:concurrency": "jest --config ./test/concurrency/jest-concurrency.json --runInBand"`
  → `test/concurrency/jest-concurrency.json` 은 `rootDir: ".."`, `testRegex: "concurrency/.*\\.conc-spec\\.ts$"`
  **같은 구조를 그대로 복제**한다 (`tenant/.*\\.tenant-spec\\.ts$`). 확장자를 분리해야 기본 `npm test` 와 겹치지 않는다.

검증 대상 (선행 플랜 산출물):
- R1 `src/common/socket/websocket.gateway.ts` — `handleConnection` / `register_user|terminal|branch|api_key`
- R2 `src/app/products/productStock.service.ts` — `correctTodayStocks(..., scope)`
- R3 `src/app/vendor-portal/vendor-auth/vendor-auth.service.ts` + `vendor-jwt.strategy.ts`
- R4 `src/common/tenant/tenant-scope.registry.ts` + `tenant-hooks.ts`
- R5 `src/app/auth/guards/jwt-global.guard.ts`

Phase 64 스위트: `npm run test:concurrency` — `CONCURRENCY_DATABASE_URL` 필요
(`test/concurrency/helpers/db.ts` 가 운영 DSN 을 차단한다). 8종 불변식.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tenant 회귀 스위트 러너 구성 + 공용 픽스처</name>
  <read_first>
    - api-ventago/test/concurrency/jest-concurrency.json (복제할 구성)
    - api-ventago/package.json (scripts 절 + jest 절)
    - api-ventago/test/concurrency/helpers/db.ts (helpers 배치 관례)
  </read_first>
  <action>
1. `api-ventago/test/tenant/jest-tenant.json` 을 만든다:
   `{ moduleFileExtensions: ["js","json","ts"], rootDir: "..", testEnvironment: "node",
     testRegex: "tenant/.*\\.tenant-spec\\.ts$", transform: { "^.+\\.(t|j)s$": "ts-jest" },
     moduleNameMapper: { "^src/(.*)$": "<rootDir>/src/$1" }, testTimeout: 30000 }`
   `moduleNameMapper` 는 소스가 `src/...` 절대 import 를 쓰므로 반드시 넣는다.
2. `package.json` scripts 에 `"test:tenant": "jest --config ./test/tenant/jest-tenant.json"` 를 추가한다.
   기존 스크립트는 건드리지 않는다.
3. `api-ventago/test/tenant/helpers/fixtures.ts` 를 만든다. 두 매장(`STORE_A = 6`, `STORE_B = 9`)을 상수로 두고
   재사용 가능한 스텁 팩토리를 export 한다:
   - `makeSocketStub(handshakeAuth?)` — `{ id, handshake:{ auth, address, headers }, data:{}, join, leave, emit, disconnect }` (전부 jest.fn)
   - `makeJwtStub(payload)` — `{ verifyAsync: jest.fn().mockResolvedValue(payload), sign: jest.fn(() => 'tok') }`
   - `makeModelStub(rows)` — `{ findOne, findAll, findByPk, create }` (jest.fn, where 조건으로 필터링하는 간단 구현)
   - `makeExecutionContext(user, headers?)` — `switchToHttp().getRequest()` 만 흉내
   각 팩토리에 한국어 주석으로 용도를 남긴다.
  </action>
  <verify>
    <automated>cd api-ventago && npm run test:tenant -- --listTests</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:tenant -- --listTests` 가 exit 0 이고 `test/tenant` 경로를 출력한다
    - `npm test -- --listTests` 결과에 `.tenant-spec.ts` 파일이 **포함되지 않는다** (기본 스위트와 분리 확인)
    - `test/tenant/helpers/fixtures.ts` 가 `STORE_A`, `STORE_B`, `makeSocketStub`, `makeJwtStub`, `makeModelStub`, `makeExecutionContext` 를 export 한다
    - `package.json` 의 기존 스크립트 개수가 줄지 않았다
  </acceptance_criteria>
  <done>교차매장 스위트를 담을 독립 러너가 생긴다.</done>
</task>

<task type="auto">
  <name>Task 2: R1~R5 교차매장 시나리오 스위트 작성</name>
  <read_first>
    - api-ventago/test/tenant/helpers/fixtures.ts (Task 1 결과)
    - api-ventago/src/common/socket/websocket.gateway.ts
    - api-ventago/src/app/products/productStock.service.ts
    - api-ventago/src/app/vendor-portal/vendor-auth/vendor-auth.service.ts
    - api-ventago/src/app/vendor-portal/vendor-auth/vendor-jwt.strategy.ts
    - api-ventago/src/common/tenant/tenant-hooks.ts
    - api-ventago/src/app/auth/guards/jwt-global.guard.ts
  </read_first>
  <action>
`api-ventago/test/tenant/cross-tenant.tenant-spec.ts` 를 작성한다. 5개 describe 로 나눈다.
**모든 케이스는 매장 A(6) 사용자가 매장 B(9) 자원을 노리는 형태**로 쓴다.

`describe('R1 — 무인증/교차매장 소켓 room 가입')`
- 자격증명 없는 소켓: `register_user` / `register_terminal` / `register_branch` 호출 후 `join` 총 0회
- 유예 초과 시 `disconnect(true)` 1회
- 매장 A 토큰으로 매장 B terminal join → `join` 0회, `emit('join_error')` 1회
- 매장 A 토큰으로 매장 B branch join → `join` 0회
- branch_agents 미존재 apiKey → `disconnect(true)` 1회

`describe('R2 — correct-today 교차매장 원장 쓰기')`
- 매장 B `branchId` → `ForbiddenException` + `Stocks.create` 0회
- 매장 B `variantId` → `ForbiddenException` + `Stocks.create` 0회
- 다른 부모의 자식 `variantId` → `ForbiddenException` + `Stocks.create` 0회
- 정상 요청 → 보정 행 생성 (무회귀 확인)

`describe('R3 — 벤더 토큰 매장 scope')`
- 매장 B PIN 만 일치 → 토큰 `storeId === 9`, payload 에 `vendorIds` 없음
- 매장 A vendorId 가 토큰/응답에 미포함
- 구 토큰(`vendorIds` 만) → strategy 401
- 토큰 storeId ≠ 쿼리 storeId → `ForbiddenException`

`describe('R4 — 파생 스코프 enforce')`
- `TENANT_DERIVED_MODE=enforce` + 매장 A 컨텍스트에서 `ProductBranch` beforeFind →
  `product`·`branch` include 둘 다 `required:true` + storeId where
- `Stocks` 2단계 체인 주입 확인
- 컨텍스트 미해석(크론) → 주입 없음

`describe('R5 — TenantContext fail-closed')`
- 컨텍스트 미확정 인증 요청 → `ForbiddenException`
- `@Public` 경로 → 통과 (무회귀)
- superadmin → 통과 (무회귀)

각 describe 상단에 한국어 주석으로 원 결함(CR-01/CR-02/CR-03/WR-01/WR-02)과 증거 라인을 인용한다.
개별 플랜의 스펙과 어셈블리가 겹쳐도 무방하다 — 이 스위트의 목적은 **단일 관문**이다.
  </action>
  <verify>
    <automated>cd api-ventago && npm run test:tenant</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:tenant` exit 0 이고 5개 describe · 총 18개 이상 테스트가 통과한다
    - R1/R2 describe 에 `toHaveBeenCalledTimes(0)` 단언이 각각 3개 이상 있다
    - R5 describe 에 `@Public` 과 superadmin 무회귀 케이스가 있다
    - 스위트가 실 DB 연결을 요구하지 않는다 (`CONCURRENCY_DATABASE_URL` 미설정 상태에서 통과)
  </acceptance_criteria>
  <done>Phase 69 경계 전체가 명령 하나로 판정된다.</done>
</task>

<task type="auto">
  <name>Task 3: 구코드 실패 증거 + Phase 64 동시성 스위트 무회귀 확인</name>
  <read_first>
    - api-ventago/test/tenant/cross-tenant.tenant-spec.ts (Task 2 결과)
    - api-ventago/test/concurrency/jest-concurrency.json
    - api-ventago/test/concurrency/helpers/db.ts (운영 DSN 차단 로직 — 실행 전 반드시 확인)
  </read_first>
  <action>
1. **구코드 실패 증거 수집**: Phase 69 이전 커밋(`api-ventago@81474ab` 또는 69 작업 시작 직전 커밋)을
   `git worktree add ../ventago-pre69 <sha>` 로 별도 워크트리에 꺼내고, 이 스위트 파일 4개를 그쪽에 복사해
   `npm run test:tenant` 를 실행한다. **실패 개수와 실패한 테스트 이름을 기록**한 뒤 워크트리를 제거한다
   (`git worktree remove`). 기존 작업 트리를 절대 checkout 하지 않는다.
   ※ 스위트가 참조하는 심볼이 구코드에 없어 컴파일 자체가 깨질 수 있다 — 그 경우 "컴파일 실패 = 구코드에 해당 방어가 존재하지 않음"
   을 증거로 기록하고, 최소한 R2/R5 처럼 시그니처가 유지되는 케이스의 런타임 실패는 별도로 확인한다.
2. **Phase 64 동시성 스위트 실행**: `CONCURRENCY_DATABASE_URL` 를 로컬 테스트 DB(`ventago_test`)로 지정하고
   `npm run test:concurrency` 를 돌린다. 8종 불변식이 전부 통과해야 한다.
   테스트 DB 가 없어 실행 불가면 **실패로 처리하지 말고** "미실행 + 사유 + 69-10 에서 재시도" 로 기록한다.
   운영 DSN 은 절대 쓰지 않는다 (helpers/db.ts 가 차단하지만 이중으로 확인).
3. `api-ventago/test/tenant/README.md` 작성:
   - `## 실행` — `npm run test:tenant`
   - `## 무엇을 지키는가` — R1~R5 각 1줄
   - `## 구코드 실패 증거` — 1번 결과(대상 커밋 sha, 실패 개수, 실패 테스트 이름 목록)
   - `## Phase 64 동시성` — 2번 결과
   - `## 추가 시 규칙` — 새 교차매장 결함을 고칠 때 여기에 케이스를 추가한다는 규약
  </action>
  <verify>
    <automated>cd api-ventago && npm run test:tenant && test -f test/tenant/README.md && grep -c "구코드 실패 증거" test/tenant/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test/tenant/README.md` 의 `## 구코드 실패 증거` 에 대상 커밋 sha 와 실패 테스트 이름이 3개 이상 나열되어 있다
    - `## Phase 64 동시성` 에 "8/8 통과" 또는 "미실행 + 사유" 가 명시되어 있다
    - `git worktree list` 에 임시 워크트리가 남아 있지 않다
    - 실행한 명령 어디에도 운영 DSN(62.72.7.245 / newapi.coolsistema.com)이 없다
  </acceptance_criteria>
  <done>스위트가 실제로 결함을 잡는다는 증거가 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 실행 환경 → DB | 동시성 스위트가 실 DB 를 요구 — 운영 DSN 오지정 시 운영 데이터 훼손 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-36 | Tampering | `npm run test:concurrency` DSN 오지정 | mitigate | `helpers/db.ts` 의 운영 DSN 차단 확인 + 실행 명령에 운영 호스트 미포함을 acceptance 로 검사 |
| T-69-37 | Repudiation | "테스트가 결함을 잡는다"는 주장의 근거 부재 | mitigate | 구코드 워크트리 실행 결과를 README 에 sha 와 함께 기록 |
| T-69-38 | Tampering | 임시 워크트리가 작업 트리를 오염 | mitigate | `git worktree add/remove` 만 사용, 기존 트리 checkout 금지 |
</threat_model>

<verification>
- `cd api-ventago && npm run test:tenant` exit 0
- `cd api-ventago && npx jest` (기본 스위트) exit 0 — 기존 회귀 무손상
- `npm run test:concurrency` 8/8 또는 미실행 사유 기록
- DDL 없음 — **마이그레이션 대상 0건**
</verification>

<success_criteria>
- R1~R5 교차매장 시나리오가 단일 명령으로 검증됨
- 구코드에서 실패함이 증거로 기록됨
- Phase 64 동시성 8종 무회귀
</success_criteria>

<output>
완료 후 `.planning/phases/69-tenant-isolation-security-hardening/69-09-SUMMARY.md` 를 작성한다.
SUMMARY 에 구코드 실패 개수, Phase 64 결과, 새 npm 스크립트명을 기록한다.
</output>
