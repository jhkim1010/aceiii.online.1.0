---
phase: 06-reportajes
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - api-ventago/src/app/reports/reportsAlertas.service.ts
  - api-ventago/src/app/reports/reportsChequeEstado.service.ts
  - api-ventago/src/app/reports/reports.controller.ts
  - api-ventago/src/app/reports/reports.module.ts
  - api-ventago/src/app/modules/seed/modules.seed.ts
  - ventago-app/src/views/reports/alertas/hooks/useAlertasReport.tsx
  - ventago-app/src/views/reports/alertas/components/DataConfig.tsx
  - ventago-app/src/views/reports/alertas/components/AlertasReportTable.tsx
  - ventago-app/src/views/reports/alertas/AlertasReport.tsx
  - ventago-app/src/pages/reportes/alertas/index.tsx
  - ventago-app/src/views/reports/cheque-estado/hooks/useChequeEstadoReport.tsx
  - ventago-app/src/views/reports/cheque-estado/components/DataConfig.tsx
  - ventago-app/src/views/reports/cheque-estado/components/ChequeEstadoReportTable.tsx
  - ventago-app/src/views/reports/cheque-estado/ChequeEstadoReport.tsx
  - ventago-app/src/pages/reportes/cheque-estado/index.tsx
  - ventago-app/src/views/reports/hub/ReportesHub.tsx
autonomous: true
requirements:
  - FEAT-05

must_haves:
  truths:
    - "Alertas 보고서가 /reportes/alertas 에서 재고 부족 상품 목록을 표시한다"
    - "Cheque Estado 보고서가 /reportes/cheque-estado 에서 수표 결제 현황을 표시한다"
    - "15개 보고서 모두 허브 페이지에서 접근 가능하다"
    - "15개 보고서 모두 사이드바 메뉴에서 접근 가능하다"
    - "2개 보고서 모두 Excel 내보내기가 동작한다"
  artifacts:
    - path: "api-ventago/src/app/reports/reportsAlertas.service.ts"
      provides: "재고 부족 알림 쿼리 + Excel export"
      exports: ["ReportsAlertasService"]
    - path: "api-ventago/src/app/reports/reportsChequeEstado.service.ts"
      provides: "수표 결제 현황 쿼리 + Excel export"
      exports: ["ReportsChequeEstadoService"]
  key_links:
    - from: "api-ventago/src/app/reports/reportsAlertas.service.ts"
      to: "reportsStocks.service.ts SReal 로직"
      via: "재고 계산 로직 재활용"
      pattern: "ProductBranch.*Stocks"
    - from: "api-ventago/src/app/reports/reportsChequeEstado.service.ts"
      to: "SalePaymentMethod + PaymentMethod"
      via: "cheque slug 필터링"
      pattern: "PaymentMethod.*slug.*cheque"
---

<objective>
Wave 4 — 신규 기능 보고서 2개 (Alertas, Cheque Estado) + 전체 보고서 시스템 완성 확인.

Purpose: 새 로직이 필요한 마지막 2개 보고서를 구현하고, 15개 보고서 전체 시스템을 완성한다.
Output: 백엔드 서비스 2개 + 컨트롤러 엔드포인트 4개 + 프론트엔드 보고서 뷰 2개 + 허브 페이지 최종 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-reportajes/06-CONTEXT.md
@.planning/phases/06-reportajes/06-RESEARCH.md
@.planning/phases/06-reportajes/06-01-SUMMARY.md
@.planning/phases/06-reportajes/06-02-SUMMARY.md
@.planning/phases/06-reportajes/06-03-SUMMARY.md

@api-ventago/src/app/reports/reportsStocks.service.ts
@api-ventago/src/app/reports/reports.controller.ts
@api-ventago/src/app/reports/reports.module.ts
@api-ventago/src/app/stocks/stocks.model.ts
@api-ventago/src/app/sales/sale-payment-method.model.ts
@api-ventago/src/app/sales/payment-method.model.ts
@ventago-app/src/views/reports/hub/ReportesHub.tsx

<interfaces>
<!-- Wave 4에서 필요한 특수 패턴 -->

reportsStocks.service.ts 의 SReal 계산 로직:
- ProductBranch 별로 Stocks 합산하여 현재 재고(SReal) 계산
- 이 패턴을 Alertas에서 재활용: SReal <= 0인 상품 = 재고 부족 알림

SalePaymentMethod model:
- saleId, paymentMethodId, amount
- BelongsTo: PaymentMethod (title, slug), Sale

PaymentMethod model:
- id, title, slug, storeId
- 'cheque' slug가 있는지 확인 필요. 없으면 SalePaymentMethod 전체를 Cheque Estado로 표시

Cheque Estado 접근 방식 (RESEARCH 권장):
- PaymentMethod slug로 'cheque' 결제만 조회하는 방법부터 시도
- 'cheque' slug가 없으면 결제 방법 전체를 표시하되 필터로 선택 가능하게
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 — Wave 4 보고서 서비스 2개 + 컨트롤러 + seed</name>
  <files>
    api-ventago/src/app/reports/reportsAlertas.service.ts,
    api-ventago/src/app/reports/reportsChequeEstado.service.ts,
    api-ventago/src/app/reports/reports.controller.ts,
    api-ventago/src/app/reports/reports.module.ts,
    api-ventago/src/app/modules/seed/modules.seed.ts
  </files>
  <action>
    **1. ReportsAlertasService** (`reportsAlertas.service.ts`):
    - `getReportAlertasData(filters)`: 재고 부족 상품 목록 조회
    - **reportsStocks.service.ts의 SReal 계산 로직 참조하여 구현:**
      - ProductBranch.findAll include Product(sku, name, storeId) + Branch(name)
      - 각 ProductBranch별로 Stocks 합산하여 SReal(현재 재고) 계산
      - SReal <= 0인 상품만 필터링 (재고 부족/소진 알림)
    - 필터: storeId, branchId, filter(Product.name LIKE)
    - **startDate/endDate 필터 불필요** (현재 재고 상태 보고서)
    - 결과 매핑: { sku, producto, sucursal, stockActual, estado } (estado: '부족'='Sin Stock' if SReal<=0, '부족'='Bajo Stock' if SReal <= 임계값(5))
    - `generalReport(data)`: Excel 헤더 = [SKU, Producto, Sucursal, Stock Actual, Estado]
    - **반드시 storeId 필터 적용**

    **2. ReportsChequeEstadoService** (`reportsChequeEstado.service.ts`):
    - **RESEARCH 권장 방법:** PaymentMethod slug='cheque'로 필터
    - `getReportChequeEstadoData(filters)`:
      - 먼저 PaymentMethod.findOne where slug='cheque' AND storeId
      - 있으면: SalePaymentMethod.findAll where paymentMethodId=chequeMethod.id, include Sale(saleNumber, saleDate, status, include client), PaymentMethod(title)
      - 없으면: SalePaymentMethod.findAll include Sale + PaymentMethod 전체 표시 (모든 결제 방법별 현황)
    - 필터: storeId, saleDate BETWEEN startDate~endDate, filter(텍스트 검색)
    - 결과 매핑: { nroVenta, fecha, cliente, metodoPago, monto, estadoVenta }
    - `generalReport(data)`: Excel 헤더 = [Nro Venta, Fecha, Cliente, Metodo Pago, Monto, Estado]

    **3. reports.controller.ts** 수정:
    - 4개 엔드포인트 추가:
      - @Get('alertas-report'), @Get('alertas-report-export')
      - @Get('cheque-estado-report'), @Get('cheque-estado-report-export')

    **4. reports.module.ts**: providers에 2개 서비스 추가

    **5. modules.seed.ts**: reportes 아래에 추가
    - { name: 'Alertas', slug: 'alertas-reportes', url: '/reportes/alertas', icon: 'tabler:alert-triangle', isMain: false }
    - { name: 'Cheque Estado', slug: 'cheque-estado-reportes', url: '/reportes/cheque-estado', icon: 'tabler:file-check', isMain: false }

    **ESLint 준수:** return 문 위 빈 줄, 주석 위 빈 줄. 주석 한국어.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>2개 보고서 서비스 TypeScript 컴파일 통과, 4개 엔드포인트 등록, seed에 마지막 2개 메뉴 추가 완료</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 — Wave 4 보고서 뷰 2개 + 허브 최종 확인</name>
  <files>
    ventago-app/src/views/reports/alertas/hooks/useAlertasReport.tsx,
    ventago-app/src/views/reports/alertas/components/DataConfig.tsx,
    ventago-app/src/views/reports/alertas/components/AlertasReportTable.tsx,
    ventago-app/src/views/reports/alertas/AlertasReport.tsx,
    ventago-app/src/pages/reportes/alertas/index.tsx,
    ventago-app/src/views/reports/cheque-estado/hooks/useChequeEstadoReport.tsx,
    ventago-app/src/views/reports/cheque-estado/components/DataConfig.tsx,
    ventago-app/src/views/reports/cheque-estado/components/ChequeEstadoReportTable.tsx,
    ventago-app/src/views/reports/cheque-estado/ChequeEstadoReport.tsx,
    ventago-app/src/pages/reportes/cheque-estado/index.tsx,
    ventago-app/src/views/reports/hub/ReportesHub.tsx
  </files>
  <action>
    **1. Alertas 보고서 뷰:**
    - Hook: apiConnector.get('/reports/alertas-report', params) — **RangeDate 불필요** (현재 재고 상태)
    - 컬럼: [SKU(width:130), Producto(flex:1), Sucursal(width:150), Stock Actual(width:140, align:right), Estado(width:120, renderCell로 Chip: 'Sin Stock'=error, 'Bajo Stock'=warning)]
    - AlertasReport.tsx: RangeDate 제외, ProductFilterInput(지점) + 검색만 표시
    - page: WithAccess allowedApps={["reportes"]} allowedModules={["alertas-reportes"]}

    **2. Cheque Estado 보고서 뷰:**
    - Hook: apiConnector.get('/reports/cheque-estado-report', params) — 날짜 범위 + 지점 + 검색
    - 컬럼: [Nro Venta(width:120), Fecha(width:130), Cliente(flex:1), Metodo Pago(width:150), Monto(width:140, 통화 포맷), Estado(width:120)]
    - page: WithAccess allowedApps={["reportes"]} allowedModules={["cheque-estado-reportes"]}

    **3. 허브 페이지 최종 확인:**
    - ReportesHub.tsx의 REPORTS 배열에 15개 모두 정확한 url/icon이 있는지 확인
    - 모든 URL이 실제 구현된 페이지와 매칭되는지 확인
    - 미구현 보고서가 없는지 최종 점검

    **공통:** CardFilter(showFilterButton=false) + FullTable + 0.5초 debounce 검색 + Excel 다운로드

    **ESLint 준수:** return 문 위 빈 줄, 주석 위 빈 줄, no-unused-vars. 주석 한국어. semi: false.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /reportes/alertas, /reportes/cheque-estado 접근 가능
    - Alertas에서 재고 부족 상품이 Sin Stock/Bajo Stock Chip으로 표시됨
    - 15개 보고서 전체가 허브 페이지 + 사이드바에서 접근 가능
    - 모든 보고서에서 Excel 내보내기 동작
  </done>
</task>

</tasks>

<verification>
- `cd api-ventago && npx tsc --noEmit` — 백엔드 컴파일 성공
- `cd ventago-app && npx next build` — 프론트엔드 빌드 성공
- `curl http://localhost:5002/api/reports/alertas-report?storeId=1` — 200 + 재고 부족 상품 목록
- `curl http://localhost:5002/api/reports/cheque-estado-report?startDate=2026-01-01&endDate=2026-04-06&storeId=1` — 200 + 결제 현황
- 브라우저에서 /reportes/ 허브 → 15개 보고서 모두 클릭 가능, 데이터 표시 확인
- 사이드바 reportes 메뉴에서 15개 보고서 모두 표시 확인
</verification>

<success_criteria>
- 15개 보고서 전체가 구현 완료 (기존 3개 + 신규 12개)
- 모든 보고서에서 기간별/지점별 필터링 동작 (Alertas, Clientes Credito는 날짜 필터 제외)
- 모든 보고서에서 Excel 내보내기 동작
- 모든 보고서가 허브 페이지 + 사이드바에서 접근 가능
- Alertas가 SReal 계산으로 재고 부족 상품을 정확히 표시
</success_criteria>

<output>
After completion, create `.planning/phases/06-reportajes/06-04-SUMMARY.md`
</output>
