---
phase: 89-qr-public-product-page
plan: 08
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - api-ventago/src/app/reseller/auth/reseller-auth.service.ts
  - api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts
  - ventago-app/src/views/m-stock/ResellerApplyForm.tsx
  - ventago-app/src/views/m-stock/VentagoLeadForm.tsx
  - ventago-app/src/views/m-stock/QrProductView.tsx
  - scripts/check-cta-destinos.sh
autonomous: true
requirements: [REQ-04, REQ-05]
must_haves:
  truths:
    - "CTA① 을 끝까지 눌러 reseller 신청이 **실제로 접수된다**(`reseller.resellers` 에 행이 생긴다)"
    - "CTA② 를 끝까지 눌러 리드가 **실제로 저장된다**(`ventago_leads` 에 행이 생긴다)"
    - "두 CTA 의 도착지가 존재하는 것이 **검사로 못 박혀 있다** — 없어지면 검사가 빨개진다"
    - "reseller 신청은 QR 의 그 매장 하나로 고정된다 — 사용자가 매장을 고르지 않는다"
    - "존재하지 않거나 비활성인 매장으로는 신청이 저장되지 않는다"
  artifacts:
    - path: "ventago-app/src/views/m-stock/ResellerApplyForm.tsx"
      provides: "CTA① 경량 KYC 폼 (storeIds 고정)"
      contains: "storeIds"
    - path: "ventago-app/src/views/m-stock/VentagoLeadForm.tsx"
      provides: "CTA② 리드 폼"
      contains: "public/ventago-leads"
    - path: "scripts/check-cta-destinos.sh"
      provides: "막다른 CTA 방지 — 양쪽 문자열 대조 + 실제 HTTP 확인"
      contains: "대조군"
  key_links:
    - from: "ResellerApplyForm.tsx"
      to: "POST {API}/reseller/auth/register"
      via: "raw fetch + FormData"
      pattern: "reseller/auth/register"
    - from: "VentagoLeadForm.tsx"
      to: "POST {API}/public/ventago-leads"
      via: "raw fetch + JSON"
      pattern: "public/ventago-leads"
    - from: "reseller-auth.service.ts register()"
      to: "publicStores()"
      via: "storeIds 화이트리스트 검증"
      pattern: "publicStores\\(\\)"
---

<objective>
페이지 끝의 **두 갈래 CTA** 를 붙이고, **도착지가 실제로 열리는 것을 검사로 못 박는다**.

Purpose: 이 저장소에는 **없는 페이지로 보낸 전례**가 기록돼 있다(막다른 CTA).
그리고 두 도착지는 지금 **없거나 한 번도 안 쓰였다**:
- CTA① `POST reseller/auth/register` — 백엔드는 완성돼 있지만 `reseller` 스키마 3개 테이블이
  **전부 0행**이다. **실행된 적 없는 코드는 검증된 적 없는 코드다.** 부르는 프론트가
  저장소 전체에 0건이라 그래서 0행이었다(승인 화면 `/admin/revendedores` 는 **이미 있다**).
- CTA② `POST /public/ventago-leads` — 89-05 가 방금 만들었다.

Output: 두 폼 + 마운트 + `scripts/check-cta-destinos.sh`.

★ **이름 혼동 주의:** `api-ventago/src/app/reseller/`(**`reseller` 스키마**, Phase 24, 이번에 쓸 것)와
  `api-ventago/src/app/revendedor/`(**레거시 `public.revendedores`**, 완전히 다른 시스템)가
  둘 다 있다. 스페인어/영어 차이일 뿐 독립된 두 시스템이다. **`reseller` 를 쓴다.**
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 2026-09-16 코드 실측. -->

**CTA① 백엔드 계약 (기존, 여기서 계약을 바꾸지 않는다)**
`api-ventago/src/app/reseller/auth/reseller-auth.controller.ts:30-48`:
```typescript
@Public()
@Post('register')
@UseInterceptors(FileFieldsInterceptor([
  { name: 'dniPhoto', maxCount: 1 },
  { name: 'residenceCert', maxCount: 1 },
  { name: 'selfie', maxCount: 1 },
]))
register(@Body() dto: ResellerRegisterDto, @UploadedFiles() files: {...}) { ... }
```
`dto/reseller-register.dto.ts`:
```typescript
@IsString() @IsNotEmpty() readonly name: string;
@IsString() @IsNotEmpty() readonly phone: string;
@IsString() @IsNotEmpty() readonly document: string;
@IsString() @MinLength(6)  readonly password: string;
@IsArray() @Type(() => Number) readonly storeIds: number[];
```
서비스 동작(`reseller-auth.service.ts:216-291`):
- 파일 3개 **전부 필수** — 하나라도 없으면 400 `RESELLER_DOCS_REQUIRED`
- `email = \`${document}@app\`` 자동 생성
- `document` 중복이면 409 `RESELLER_DUPLICATE`
- MinIO 업로드(트랜잭션 밖) → 트랜잭션에서 `resellers`(status `pending_review`, isActive false)
  + `reseller_documents` + `reseller_tienda_link`(status `pending`, storeIds 각각) 커밋
- ★ **`storeIds` 를 아무도 검증하지 않는다** — 이 phase 가 이 엔드포인트를 처음으로
  **도달 가능하게 만들기 때문에** 그 노출이 여기서 새로 생긴다(Task 1 이 닫는다).

같은 파일에 이미 있는 **권위 있는 매장 목록**(`reseller-auth.service.ts:205-214`):
```typescript
// 가입 폼용 공개(무인증) 매장 목록 — ACTIVE(isActive=true && status='1') 매장만
async publicStores(): Promise<{ id: number; name: string }[]> {
  const stores = await this.storeModel.findAll({
    where: { isActive: true, status: '1' }, attributes: ['id', 'name'], order: [['name','ASC']],
  });

  return stores.map((s) => ({ id: s.id, name: s.name }));
}
```

**CTA② 백엔드 계약 (89-05 가 만든 것)**
`POST /public/ventago-leads` body:
`{ storeId: number, sourceProductId?: number|null, contactName: string, contactPhone: string,
   contactEmail?: string|null, message?: string|null }` → `{ id }`.

**공개 페이지에서의 전송 — `apiConnector` 금지** (401 → `/login` 인터셉터).
`entrega/[token].tsx:34-49` 의 raw `fetch` 를 쓴다.
★ `FormData` 를 보낼 때는 `Content-Type` 헤더를 **직접 세팅하지 않는다** —
  지정하면 boundary 가 깨진다. 브라우저가 알아서 붙인다.
FormData 조립 analog — `ventago-app/src/views/configuracion/tienda/DatosTiendaView.tsx:103-113`.

**업로드 상한:** 이 저장소의 업로드 상한은 **층이 셋**(nginx · 앱 상수 · 클라이언트)이다.
셀피·DNI 사진은 폰 카메라 원본이면 수 MB 다 → 폼에서 **파일당 5MB 상한**을 먼저 걸고
초과하면 「사진을 줄여 주세요」로 막는다(413 을 서버에서 받는 것보다 낫다).

**승인 화면은 이미 있다:** `ventago-app/src/pages/admin/revendedores.tsx`
(`WithAccess allowedApps={["admin"]}`) → `GET /reseller/admin/pending`,
`PATCH /reseller/admin/:id/approve|reject`. **새로 만들지 않는다.**

**신청자에게는 진행 상태를 알릴 수단이 없다** — reseller 로그인 포털이 빌드되는 앱 목록에
없다. 그래서 완료 문구는 그 사실을 감추지 않는다(「매장에서 연락드립니다」).
승인 후 알림은 **이 phase 범위 밖**이다(RESEARCH Open Question #2).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 백엔드 — reseller 신청의 storeIds 를 권위 목록으로 검증</name>
  <files>api-ventago/src/app/reseller/auth/reseller-auth.service.ts, api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts</files>
  <read_first>
    - `api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 의 **205-300행 전체** — `publicStores()` 와 `register()`
    - `api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts` — 기존 시험이 무엇을 덮는지(깨뜨리지 말 것)
    - `api-ventago/src/app/reseller/reseller.model.ts:10` — 「Phase 24 reseller 스키마. legacy public.revendedores 와 별개」
  </read_first>
  <behavior>
    - `storeIds: [유효한 활성 매장]` → 기존과 **똑같이** 동작한다(REQ-04: 기존 계약 유지)
    - `storeIds: [존재하지 않는 매장]` → 400, `resellers` 행이 **생기지 않는다**
    - `storeIds: [비활성 매장]` → 400
    - `storeIds: []` → 400
    - `storeIds: [유효, 무효]` → 400 (부분 수용하지 않는다)
  </behavior>
  <action>
`register()` 의 **맨 앞**(MinIO 업로드보다 **먼저**)에 검증을 넣는다.
★ 업로드 뒤에 막으면 MinIO 에 고아 객체가 남는다 — 기존 코드 주석이 같은 이유로
  파일 필수 검사를 업로드 앞에 두고 있다. 같은 자리에 둔다.

```typescript
// [Phase 89] storeIds 를 **가입 폼이 보여주는 것과 같은 목록**으로 검증한다.
//
// ★ 왜 지금인가: 이 엔드포인트는 부르는 화면이 없어 한 번도 실행된 적이 없었다
//   (reseller 3개 테이블 전부 0행). Phase 89 가 QR 페이지에서 처음으로 도달 가능하게
//   만든다 — 범위를 넓히면 노출도 넓어진다. 그 노출을 같은 커밋에서 닫는다.
// ★ 판정의 근거는 저장의 근거와 같아야 한다 — `publicStores()` 가 폼에 보여주는
//   그 목록이고, 저장 허용 여부도 그 목록으로 정한다. 두 근거가 갈라지면 우회로가 생긴다.
const allowed = new Set((await this.publicStores()).map((s) => s.id));
const requested = (dto.storeIds ?? []).map(Number);

if (requested.length === 0 || requested.some((id) => !allowed.has(id))) {
  throw new BadRequestException({
    code: 'RESELLER_STORE_INVALID',
    message: 'Tienda no disponible',
  });
}
```

★ **기존 동작을 바꾸지 않는다** — 유효한 활성 매장에 대해서는 종전과 동일하다(REQ-04).
★ 부분 수용(유효한 것만 저장)을 하지 않는다. 신청자는 「어느 매장에 신청했는지」를
  확신할 수 있어야 한다.

그리고 `reseller-auth.service.spec.ts` 에 위 5개 케이스를 더한다
(**기존 시험을 지우거나 고치지 않는다** — 더하기만 한다).

★ **대조군 실증**: 방금 넣은 `if` 를 잠시 지워 「존재하지 않는 매장」 케이스가
  빨개지는 것을 확인하고 원복한다. SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/reseller --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RESELLER_STORE_INVALID" api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 가 1 이다
    - 그 검증 블록이 `this.minio.uploadFile` 호출보다 **앞줄**에 있다(행 번호로 확인)
    - `grep -c "publicStores()" api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 가 2 이상이다(정의 + register 내 사용)
    - `npx jest src/app/reseller --maxWorkers=1` 출력에 `reseller-auth.service` suite 가 나타나고 종료코드 0
    - 기존 시험 건수가 **줄지 않았다**(변경 전후 `it(` 개수를 SUMMARY 에 기록)
    - SUMMARY 에 대조군 실증 기록이 있다
  </acceptance_criteria>
  <done>신청 매장이 권위 목록으로 검증되고, 유효한 경우의 기존 계약은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: 두 CTA 폼 + QrProductView 에 마운트</name>
  <files>ventago-app/src/views/m-stock/ResellerApplyForm.tsx, ventago-app/src/views/m-stock/VentagoLeadForm.tsx, ventago-app/src/views/m-stock/QrProductView.tsx</files>
  <read_first>
    - `ventago-app/src/views/m-stock/QrProductView.tsx` — 89-06 이 만든 **현재 상태**(CTA 자리가 비어 있다)
    - `ventago-app/src/pages/entrega/[token].tsx` 의 34-49행 — raw fetch 와 에러 처리
    - `ventago-app/src/views/configuracion/tienda/DatosTiendaView.tsx` 의 103-113행 — FormData 조립
    - `api-ventago/src/app/reseller/auth/dto/reseller-register.dto.ts` — 필드 이름의 권위
    - 목업 6화면: https://claude.ai/code/artifact/fa2784ff-09a2-422d-9f95-fa905de99bf7 (「reseller신청」·「리드」)
  </read_first>
  <action>
**(1) `ResellerApplyForm.tsx`** — props: `{ storeId: number; storeName: string | null; onClose: () => void }`

필드: `name` · `phone` · `document` · `password`(≥6자) +
파일 3개 `dniPhoto` · `residenceCert` · `selfie`(각각 `<input type="file" accept="image/*">`).

```typescript
const fd = new FormData()
fd.append('name', name)
fd.append('phone', phone)
fd.append('document', document)
fd.append('password', password)
fd.append('storeIds', String(storeId))   // ★ QR 의 매장 하나로 고정
fd.append('dniPhoto', dniPhoto)
fd.append('residenceCert', residenceCert)
fd.append('selfie', selfie)

const res = await fetch(`${API_HOST}/reseller/auth/register`, { method: 'POST', body: fd })
```
★ **매장 선택 UI 를 노출하지 않는다.** 화면에는 `storeName` 을 **읽기 전용 문구**로만 보여준다
  (「Vas a solicitar ser revendedor de **{storeName}**」). 기존 가입 폼처럼
  `GET reseller/public/stores` 로 전체 목록을 부르지 **않는다**.
★ `headers` 에 `Content-Type` 을 넣지 않는다(boundary 가 깨진다).
★ 파일당 5MB 상한을 클라이언트에서 먼저 막는다.
★ 오류 코드별 문구:
  - 409 `RESELLER_DUPLICATE` → `Ese documento ya está registrado.`
  - 400 `RESELLER_DOCS_REQUIRED` → `Faltan fotos. Subí las tres.`
  - 400 `RESELLER_STORE_INVALID` → `Esta tienda no acepta solicitudes.`
  - 그 외 → `No pudimos enviar la solicitud. Probá de nuevo.`
★ 성공 화면(막다른 곳이 아니게 **무엇이 다음인지 말한다**):
  `Solicitud enviada. La tienda la va a revisar y se va a contactar con vos.`
  ★ 「로그인해서 상태를 확인하세요」 같은 문구를 **쓰지 않는다** — reseller 포털이 없다.
    없는 곳으로 안내하는 것이 곧 막다른 CTA 다.

**(2) `VentagoLeadForm.tsx`** — props: `{ storeId: number; productId: number | null; onClose: () => void }`

필드: `contactName` · `contactPhone` · `contactEmail`(선택) · `message`(선택, 500자).
```typescript
await fetch(`${API_HOST}/public/ventago-leads`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ storeId, sourceProductId: productId, contactName, contactPhone, contactEmail, message }),
})
```
- 429 → `Demasiados intentos. Esperá un minuto.`
- 성공 → `¡Gracias! Te vamos a contactar.`

**(3) `QrProductView.tsx` 수정** — `mode === 'detail'` 화면 하단의 **비워 둔 CTA 자리**에
두 버튼을 채운다:
- `¿Querés ser revendedor de {storeName}?` → `ResellerApplyForm` 을 MUI `Dialog`(fullScreen on mobile)로
- `¿Querés este sistema en tu negocio?` → `VentagoLeadForm` 을 같은 방식으로
- ★ 두 폼은 `next/dynamic(..., { ssr: false })` 로 지연 로드한다 — 열기 전에는 번들에 안 들어간다.
- ★ `mode` 가 `shop_redirect`/`closed` 일 때는 두 CTA 를 **렌더하지 않는다**
  (닫힘은 「아무것도 안 보인다」이고, 목록전환에는 갈 곳이 이미 하나 있다).

**ESLint:** `return` 위 빈 줄 · `//` 주석 위 빈 줄 · 미사용 import 금지.
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/views/m-stock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reseller/auth/register" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 1 이다
    - `grep -c "revendedor" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 로 **레거시 엔드포인트(`/revendedor/`)를 부르지 않음**을 확인한다(스페인어 표시 문구는 허용, `fetch` 대상 경로에 `/revendedor/` 가 없어야 한다)
    - `grep -c "reseller/public/stores" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 **0** 이다(매장 선택 UI 없음)
    - `grep -c "'Content-Type'" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 **0** 이다(FormData 에 헤더 금지)
    - `grep -c "public/ventago-leads" ventago-app/src/views/m-stock/VentagoLeadForm.tsx` 가 1 이다
    - `grep -c "apiConnector" ventago-app/src/views/m-stock/` 전체가 **0** 이다
    - `QrProductView.tsx` 에 `ResellerApplyForm` 과 `VentagoLeadForm` 이 각각 `next/dynamic` 으로 들어온다
    - `npx tsc --noEmit` 0, `npx eslint src/views/m-stock` 0
  </acceptance_criteria>
  <done>두 폼이 존재하고 상세 화면에서만 열리며, reseller 신청 매장이 QR 의 매장으로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 3: 막다른 CTA 방지 — 도착지 존재를 검사로 못 박고 실제로 끝까지 눌러 본다</name>
  <files>scripts/check-cta-destinos.sh</files>
  <read_first>
    - `api-ventago/scripts/check-tenant-ownership.sh` — 검사 스크립트 관례(종료코드·대조군·「무엇을 세는가」 주석)
    - 방금 만든 두 폼 파일 — 어떤 경로 문자열을 쓰는지
    - `api-ventago/src/app/reseller/auth/reseller-auth.controller.ts` · `api-ventago/src/app/leads/leads-public.controller.ts` — 서버 쪽 경로
    - `ventago-app/src/pages/admin/revendedores.tsx` — 승인 화면이 실재함(도착지 목록에 포함)
  </read_first>
  <action>
**(1) `scripts/check-cta-destinos.sh` 신규:**

```bash
#!/usr/bin/env bash
# [Phase 89] **막다른 CTA 를 막는다.**
#
# ★ 이 저장소에는 «없는 메뉴·없는 페이지로 보낸» 전례가 기록돼 있다. 버튼이 있는데
#   눌러도 아무 일이 없으면 기능은 없는 것과 같다. 그래서 «프론트가 부르는 경로» 와
#   «서버가 실제로 여는 경로» 를 **서로 다른 파일에서 읽어 대조한다.**
#   한쪽만 보면 둘이 같이 틀려도 통과한다.
#
# ★ 대조군: 존재하지 않는 경로를 같은 방식으로 검사했을 때 **반드시 실패해야** 한다.
#   그 판이 통과하면 이 스크립트는 아무것도 검사하지 않고 있는 것이다 → 종료코드 2.
#
# 사용: scripts/check-cta-destinos.sh            # 정적 대조만
#       API=http://localhost:5002/api scripts/check-cta-destinos.sh   # + 실제 HTTP 확인
# 종료코드: 도착지 없음 1 · 대조군이 통과 2 · 정상 0
set -uo pipefail
```

검사 내용:

**(a) 정적 대조 — 프론트 ↔ 서버**

| 프론트에서 찾는 문자열 | 서버에서 찾는 문자열 |
|---|---|
| `reseller/auth/register` (ResellerApplyForm.tsx) | `@Controller('reseller/auth')` + `@Post('register')` (reseller-auth.controller.ts) |
| `public/ventago-leads` (VentagoLeadForm.tsx) | `@Controller('public/ventago-leads')` + `@Post()` (leads-public.controller.ts) |
| `public/qr-stock` (pages/m/stock/index.tsx) | `@Controller('public/qr-stock')` (qr-public.controller.ts) |

- 어느 한쪽이라도 0건이면 **exit 1** 과 함께 **어느 쪽이 없는지** 말한다.
- ★ `grep` 에 **`-a` 를 붙인다** — 이 저장소의 일부 소스가 바이너리로 판정돼
  조용히 0건이 나온 전례가 있다.
- ★ 경로 리터럴에 `.` 이 있으면 정규식 와일드카드가 된다. `grep -F`(고정 문자열)를 쓴다.

**(b) 대조군** — 위와 같은 방식으로 `public/no-existe-jamas` 를 찾는다.
  **찾히면 exit 2**(검사가 아무거나 통과시키고 있다).

**(c) 프론트 라우트 존재** — `ventago-app/src/pages/m/stock/index.tsx` 와
  `ventago-app/src/pages/admin/revendedores.tsx` 가 **파일로 존재**하는지.
  (Pages Router 는 파일이 곧 라우트다.)

**(d) `API` 환경변수가 있으면 실제 HTTP** — **404 가 아닌 것**을 확인한다:
```bash
curl -s -o /dev/null -w '%{http_code}' -X POST "$API/public/ventago-leads" \
  -H 'Content-Type: application/json' -d '{}'      # 400 또는 429 기대 (404 면 실패)
curl -s -o /dev/null -w '%{http_code}' -X POST "$API/reseller/auth/register"   # 400 기대 (404 면 실패)
curl -s -o /dev/null -w '%{http_code}' "$API/public/qr-stock/999999/999999"     # 404 기대 — 이건 정상
```
★ **400 은 성공이다.** 「라우트가 있고 검증까지 갔다」는 뜻이다. **404 만 실패다.**
★ `/public/qr-stock/…` 는 없는 상품이라 404 가 정상 — 여기서는 **연결 자체가 되는지**만 본다
  (curl 종료코드 0).

**BSD 주의:** `\s` 대신 `[[:space:]]`. `set -e` 를 쓰지 않는다(진단 전에 죽는다).

**(2) 실제로 끝까지 눌러 본다 (로컬):**
api(5002) + app(3050) 을 띄우고 시크릿 창에서 `qr_precio_publico=true` 인 매장의
`/m/stock?s=X&p=P` 를 연 뒤:

1. CTA① → 폼 작성 + 사진 3장 첨부 → 제출 → **성공 화면**을 본다
   ```bash
   psql -p 5432 -d ventago -tAc "SELECT id,document,status FROM reseller.resellers ORDER BY id DESC LIMIT 3;"
   psql -p 5432 -d ventago -tAc "SELECT reseller_id,store_id,status FROM reseller.reseller_tienda_link ORDER BY id DESC LIMIT 3;"
   psql -p 5432 -d ventago -tAc "SELECT reseller_id,doc_type FROM reseller.reseller_documents ORDER BY id DESC LIMIT 5;"
   ```
   ★ `reseller` 는 **`public` 이 아닌 별도 스키마**다 — `FROM resellers` 로 쓰면 없다고 나온다.
   ★ **행이 실제로 생겼는지**를 본다. 성공 화면만으로는 증거가 안 된다.
   ★ 그 다음 **다음 버튼을 눌러 본다**: `/admin/revendedores` 를 열어 방금 신청이
     **pending 목록에 보이는지** 확인한다. 「실행된 적 없는 코드는 검증된 적 없는 코드」다 —
     한 번 통과해도 다음 화면이 죽어 있을 수 있다.
2. CTA② → 리드 제출 → `psql -p 5432 -d ventago -tAc "SELECT id,store_id,contact_name FROM ventago_leads ORDER BY id DESC LIMIT 3;"`

★ **만든 시험 데이터는 지우지 않는다**(이 저장소의 상시 규칙). 대신 SUMMARY 에
  **무엇이 남았는지와 그 영향**을 적는다(예: `reseller.resellers` 에 시험 신청 1건,
  status `pending_review`, isActive false — 승인 화면에 보인다).
  </action>
  <verify>
    <automated>bash -n scripts/check-cta-destinos.sh && bash scripts/check-cta-destinos.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cta-destinos.sh` 종료코드 **0**
    - 그 출력에 3개 CTA 도착지가 각각 「프론트 N건 / 서버 N건」 으로 찍힌다(전부 1 이상)
    - 그 출력에 대조군(`public/no-existe-jamas`)이 **0건**으로 찍힌다
    - `API=http://localhost:5002/api bash scripts/check-cta-destinos.sh` 에서
      `/public/ventago-leads` 와 `/reseller/auth/register` 가 **404 가 아닌** 상태코드를 돌려준다
    - 대조군 실증: `leads-public.controller.ts` 의 `@Controller('public/ventago-leads')` 를
      잠시 다른 문자열로 바꾸면 스크립트가 **exit 1** 이 되는 것을 확인하고 원복했다(SUMMARY 에 기록)
    - `reseller.resellers` · `reseller.reseller_tienda_link` · `reseller.reseller_documents` ·
      `ventago_leads` 네 테이블에 **실제로 행이 생긴** psql 출력이 SUMMARY 에 있다
    - `/admin/revendedores` 에서 그 신청이 pending 으로 보인 것을 확인했다
  </acceptance_criteria>
  <done>두 CTA 가 끝까지 도달하고, 도착지 부재가 검사로 잡히며, 검사의 대조군이 실제로 반응한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷(비인증) → `POST /reseller/auth/register` | 이 phase 가 이 엔드포인트를 **처음으로 도달 가능하게** 만든다 |
| 업로드 파일 → MinIO `reseller/{document}/...` | 사용자 입력이 오브젝트 키의 일부가 된다 |
| 인터넷 → `POST /public/ventago-leads` | 89-05 의 threat model 이 덮는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-08 | Tampering | `ResellerRegisterDto.storeIds` | mitigate | `publicStores()`(isActive && status='1') 화이트리스트로 검증하고 **업로드보다 먼저** 막는다. 부분 수용하지 않는다 |
| T-89-26 | Information Disclosure | MinIO 키에 들어가는 `document` | accept | 기존 코드가 `reseller/{document}/{docType}-{ts}-{originalname}` 로 만든다. **슬래시를 포함하므로 `isPubliclyServable()` 이 공개 서빙에서 차단한다** — 공개 URL 로는 못 꺼낸다. 이 phase 는 그 코드를 바꾸지 않는다 |
| T-89-27 | Denial of Service | 대용량 사진 3장 업로드 | mitigate | 클라이언트에서 파일당 5MB 상한. 서버 상한(nginx·앱 상수)은 이 phase 가 바꾸지 않는다 — 초과 시 413 이 나오므로 프론트가 먼저 막는 편이 낫다 |
| T-89-28 | Spoofing | 레거시 `revendedor` 모듈로 잘못 연결 | mitigate | acceptance 가 `fetch` 대상 경로에 `/revendedor/` 가 없음을 센다. 두 시스템은 이름만 비슷하고 스키마가 다르다 |
| T-89-29 | Repudiation | 「버튼은 있는데 아무 일도 안 일어남」 | mitigate | `check-cta-destinos.sh` 가 프론트·서버를 **서로 다른 파일에서 읽어** 대조하고, 대조군이 통과하면 스스로 exit 2 로 죽는다. 그리고 실제 DB 행 생성까지 확인한다 |
</threat_model>

<verification>
```bash
cd api-ventago && npx jest src/app/reseller --maxWorkers=1
cd ventago-app && npx tsc --noEmit && npx eslint src/views/m-stock
bash scripts/check-cta-destinos.sh
API=http://localhost:5002/api bash scripts/check-cta-destinos.sh
psql -p 5432 -d ventago -tAc "SELECT count(*) FROM reseller.resellers;"
psql -p 5432 -d ventago -tAc "SELECT count(*) FROM ventago_leads;"
```
</verification>

<success_criteria>
- CTA① 이 실제로 `reseller.resellers` + `reseller_tienda_link` + `reseller_documents` 에 행을 만든다
- 그 신청이 기존 승인 화면 `/admin/revendedores` 에 **보인다**(다음 버튼까지 눌러 봤다)
- CTA② 가 실제로 `ventago_leads` 에 행을 만든다
- 신청 매장이 QR 의 매장으로 고정되고, 무효 매장은 400 으로 막힌다
- `scripts/check-cta-destinos.sh` 가 통과하고, 도착지를 없애면 exit 1 이 되는 것을 확인했다
- 시험으로 만든 데이터는 지우지 않았고, 남은 것의 영향이 SUMMARY 에 적혀 있다
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-08-SUMMARY.md` 를 만든다.
★ **관측된 부수 사실**(수정 대상 아님, 기록만): `ResellerPublicController`
  (`GET /reseller/public/stores`)에는 `@Public()` 이 **없다** — 전역 JWT 가드 아래서
  무인증 호출이 401 일 가능성이 높다. 이 phase 는 그 엔드포인트를 쓰지 않으므로
  건드리지 않았다. 향후 reseller 포털을 만들 때 먼저 확인할 것.
★ 배포 순서: 89-05(리드 API) + Task 1(reseller 검증) 이 **먼저**, 그다음 프론트.
  반대면 버튼이 404 를 받거나 검증 없이 신청이 들어온다.
</output>
