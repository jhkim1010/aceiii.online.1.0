---
phase: 89-qr-public-product-page
plan: 08
type: execute
wave: 4
depends_on: [05, 06, 12]
files_modified:
  - api-ventago/src/app/reseller/auth/reseller-auth.controller.ts
  - api-ventago/src/app/reseller/auth/reseller-auth.service.ts
  - api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts
  - ventago-app/src/views/m-stock/ResellerApplyForm.tsx
  - ventago-app/src/views/m-stock/VentagoLeadForm.tsx
  - ventago-app/src/views/m-stock/QrProductView.tsx
  - scripts/check-cta-destinos.sh
autonomous: true
requirements: [REQ-04, REQ-05]
must_haves:
  truths:
    - "CTA① 을 끝까지 눌러 reseller 신청이 **실제로 접수된다**(`reseller.resellers` 에 행이 생긴다)"
    - "CTA② 를 끝까지 눌러 **매장 가입 화면 `/register?ref={apodo}`** 에 도달하고 추천인 칸이 채워져 있다"
    - "이탈 받이(작은 「연락처만 남기기」)를 눌러 리드가 **실제로 저장된다**(`ventago_leads` 에 행이 생긴다)"
    - "두 CTA 의 도착지가 존재하는 것이 **검사로 못 박혀 있다** — 없어지면 검사가 빨개진다"
    - "reseller 신청은 QR 의 그 매장 하나로 고정된다 — 사용자가 매장을 고르지 않는다"
    - "존재하지 않거나 비활성인 매장으로는 신청이 저장되지 않는다"
    - "무인증 업로드가 **크기·개수·MIME 으로 제한**되고 `@Throttle` 이 걸려 있다 — 요청 하나로 워커를 못 죽인다"
    - "DB 실패 시 이미 올라간 **신분증·거주증명·셀피가 MinIO 에 고아로 남지 않는다**"
    - "리드 폼이 `sourceProductId` 를 실제로 보낸다 — 스키마에만 있고 폼에 없는 상태가 아니다"
    - "세 도착지(`/reseller/auth/register` · `/register` · `/public/ventago-leads`)의 존재가 **검사로 못 박혀** 있다"
  artifacts:
    - path: "ventago-app/src/views/m-stock/ResellerApplyForm.tsx"
      provides: "CTA① 경량 KYC 폼 (storeIds 고정)"
      contains: "storeIds"
    - path: "ventago-app/src/views/m-stock/VentagoLeadForm.tsx"
      provides: "CTA② 리드 폼"
      contains: "public/ventago-leads"
    - path: "scripts/check-cta-destinos.sh"
      provides: "막다른 CTA 방지 — 양쪽 문자열 대조 + 실제 HTTP 확인"
      contains: "대조군"
  key_links:
    - from: "ResellerApplyForm.tsx"
      to: "POST {API}/reseller/auth/register"
      via: "raw fetch + FormData"
      pattern: "reseller/auth/register"
    - from: "VentagoLeadForm.tsx"
      to: "POST {API}/public/ventago-leads"
      via: "raw fetch + JSON"
      pattern: "public/ventago-leads"
    - from: "reseller-auth.service.ts register()"
      to: "publicStores()"
      via: "storeIds 화이트리스트 검증"
      pattern: "publicStores\\(\\)"
---

<objective>
페이지 끝의 **두 갈래 CTA** 를 붙이고, **도착지가 실제로 열리는 것을 검사로 못 박는다**.

Purpose: 이 저장소에는 **없는 페이지로 보낸 전례**가 기록돼 있다(막다른 CTA).

**CTA① 이 매장의 reseller 신청** → `POST reseller/auth/register`
백엔드는 완성돼 있지만 `reseller` 스키마 3개 테이블이 **전부 0행**이다.
**실행된 적 없는 코드는 검증된 적 없는 코드다.** 부르는 프론트가 저장소 전체에 0건이라
그래서 0행이었다(승인 화면 `/admin/revendedores` 는 **이미 있다**).

**CTA② 당신 매장에도 이 시스템을** → ★ **주 도착지가 바뀌었다 (결정 ⑤-개정)**
- **주(主): `/register?ref={storeApodo}`** — 이미 있고 **실제로 도는** 매장 가입 화면
  (운영 `pending_registrations` 9행, 그중 3행이 `referred_by_apodo` 보유).
  프리필은 89-12 가 넣었다. 귀속이 `referral_credits`(월사용료 50%)로 흐른다.
- **종(從): 이탈 받이** `POST /public/ventago-leads`(89-05) — 「연락처만 남기기」.
  `/register` 가 CUIT·주소·이중 OTP 를 요구해 계산대 앞에서 문턱이 높기 때문이다.

★ **세 도착지의 성숙도가 다르다. 검사도 다르게 한다:**

| 도착지 | 상태 | 이 plan 이 확인할 것 |
|---|---|---|
| `/register?ref=` | **실사용 중** | 링크가 열리고 **프리필이 실제로 채워지는가** · `guestGuard` 제약 |
| `POST /reseller/auth/register` | **0행 — 한 번도 안 돎** | 끝까지 눌러 **행이 생기는가** + 승인 화면에 **보이는가** |
| `POST /public/ventago-leads` | 방금 만듦 | 행이 생기는가 |

★★ **이 phase 가 그 엔드포인트를 처음으로 도달 가능하게 만든다 — 노출을 같은 커밋에서 닫는다.**
  CODEX 자문(2026-09-16)과 코드 대조로 확인된 것:
  - `reseller-auth.controller.ts:30` 의 `FileFieldsInterceptor([...])` 에 **`limits` 도
    `fileFilter` 도 없다.** 컨트롤러 전체에 `Throttle` 문자열이 **0건**이다.
    Multer 는 메모리 저장이라 **무인증 요청 하나가 워커를 OOM** 으로 몰 수 있다.
  - `reseller-auth.service.ts` 가 **250행에서 파일 3개를 업로드하고 264행에야** 트랜잭션을
    시작한다. `catch` 에 MinIO 삭제가 **없다** → DB 실패 시 **신분증·거주증명·셀피가
    고아로 남는다.**
  - `reseller-auth.service.ts:287` 이 `dto.storeIds` 를 **그대로** `bulkCreate` 한다.
    `reseller_tienda_link.store_id` 에는 `stores` FK 도 **없다**(로컬 `pg_constraint` 확인:
    FK 는 `reseller_id` 하나뿐).

Output: reseller 폼 1개 + 이탈 받이 폼 1개 + **가입 링크 1개** + 마운트 +
백엔드 방어 3종 + `scripts/check-cta-destinos.sh`.

★★ **「추천하면 매장이 보상을 받습니다」를 단정해 쓰지 않는다.**
  운영 `referral_credits` **0행** — 보상은 승인 시점에 생기므로 **추천 가입이 승인까지
  간 적이 없다.** 그 경로를 끝까지 확인하는 것은 이 phase 의 범위 밖(승인은 superadmin 행위)이다.
  문구는 그 사실에 맞춰 낮춘다.

★ **이름 혼동 주의:** `api-ventago/src/app/reseller/`(**`reseller` 스키마**, Phase 24, 이번에 쓸 것)와
  `api-ventago/src/app/revendedor/`(**레거시 `public.revendedores`**, 완전히 다른 시스템)가
  둘 다 있다. 스페인어/영어 차이일 뿐 독립된 두 시스템이다. **`reseller` 를 쓴다.**
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 2026-09-16 코드 실측. -->

**CTA① 백엔드 계약 (기존, 여기서 계약을 바꾸지 않는다)**
`api-ventago/src/app/reseller/auth/reseller-auth.controller.ts:30-48`:
```typescript
@Public()
@Post('register')
@UseInterceptors(FileFieldsInterceptor([
  { name: 'dniPhoto', maxCount: 1 },
  { name: 'residenceCert', maxCount: 1 },
  { name: 'selfie', maxCount: 1 },
]))
register(@Body() dto: ResellerRegisterDto, @UploadedFiles() files: {...}) { ... }
```
`dto/reseller-register.dto.ts`:
```typescript
@IsString() @IsNotEmpty() readonly name: string;
@IsString() @IsNotEmpty() readonly phone: string;
@IsString() @IsNotEmpty() readonly document: string;
@IsString() @MinLength(6)  readonly password: string;
@IsArray() @Type(() => Number) readonly storeIds: number[];
```
서비스 동작(`reseller-auth.service.ts:216-291`):
- 파일 3개 **전부 필수** — 하나라도 없으면 400 `RESELLER_DOCS_REQUIRED`
- `email = \`${document}@app\`` 자동 생성
- `document` 중복이면 409 `RESELLER_DUPLICATE`
- MinIO 업로드(트랜잭션 밖) → 트랜잭션에서 `resellers`(status `pending_review`, isActive false)
  + `reseller_documents` + `reseller_tienda_link`(status `pending`, storeIds 각각) 커밋
- ★ **`storeIds` 를 아무도 검증하지 않는다.** `reseller_tienda_link.store_id` 에
  `stores` FK 도 **없다**(로컬 `pg_constraint` 실측: FK 는 `reseller_id` 하나뿐,
  나머지는 `uq_reseller_store(reseller_id, store_id)` 와 `chk_rtl_status`).
  ⤷ 이 phase 가 이 엔드포인트를 처음으로 **도달 가능하게 만들기 때문에** 그 노출이
    여기서 새로 생긴다(Task 1 이 닫는다).
- ★ **파일 업로드가 트랜잭션보다 먼저다**(:250 업로드 → :264 트랜잭션 시작).
  `catch` 에 MinIO 삭제가 없어 **DB 실패 시 KYC 사진이 고아로 남는다**(Task 1 이 닫는다).

**89-04 가 이미 정의한 상수(여기서 다시 만들지 말 것):**
`PUBLIC_RESELLER_REGISTER_THROTTLE`
(ttl 60000 · limit `THROTTLE_PUBLIC_RESELLER_LIMIT` 기본 3 · blockDuration 300000)

같은 파일에 이미 있는 **권위 있는 매장 목록**(`reseller-auth.service.ts:205-214`):
```typescript
// 가입 폼용 공개(무인증) 매장 목록 — ACTIVE(isActive=true && status='1') 매장만
async publicStores(): Promise<{ id: number; name: string }[]> {
  const stores = await this.storeModel.findAll({
    where: { isActive: true, status: '1' }, attributes: ['id', 'name'], order: [['name','ASC']],
  });

  return stores.map((s) => ({ id: s.id, name: s.name }));
}
```

**CTA② 백엔드 계약 (89-05 가 만든 것)**
`POST /public/ventago-leads` body:
`{ storeId: number, sourceProductId?: number|null, contactName: string, contactPhone: string,
   contactEmail?: string|null, message?: string|null }` → `{ id }`.

**공개 페이지에서의 전송 — `apiConnector` 금지** (401 → `/login` 인터셉터).
`entrega/[token].tsx:34-49` 의 raw `fetch` 를 쓴다.
★ `FormData` 를 보낼 때는 `Content-Type` 헤더를 **직접 세팅하지 않는다** —
  지정하면 boundary 가 깨진다. 브라우저가 알아서 붙인다.
FormData 조립 analog — `ventago-app/src/views/configuracion/tienda/DatosTiendaView.tsx:103-113`.

**업로드 상한은 층이 셋이다 — 실측값 (2026-09-16 운영 nginx 직접 조회):**
```
/etc/nginx/sites-enabled/newapi.coolsistema.com.conf:32   client_max_body_size 25m;   ← 도메인 기본값
/etc/nginx/sites-enabled/newapi.coolsistema.com.conf:58   client_max_body_size 128m;  ← /api/legacy-import/ 에만
```
★ 즉 `POST /api/reseller/auth/register` 에 적용되는 값은 **25MB** 다.
★ 그래서 앱 상한을 **파일당 5MB × 3개 = 15MB** 로 잡으면 멀티파트 오버헤드를 더해도
  25MB 아래다. **nginx 보다 크게 잡으면 앱 메시지 대신 뜻 없는 413 이 먼저 난다**
  (이 저장소에서 실제로 있었던 일 — 그 주석이 같은 파일 25-31행에 있다).
★ 클라이언트에서도 같은 5MB 를 먼저 막는다 — 413 을 받는 것보다 「사진을 줄여 주세요」가 낫다.

**승인 화면은 이미 있다:** `ventago-app/src/pages/admin/revendedores.tsx`
(`WithAccess allowedApps={["admin"]}`) → `GET /reseller/admin/pending`,
`PATCH /reseller/admin/:id/approve|reject`. **새로 만들지 않는다.**

**CTA② 주 도착지 — `/register` (89-12 가 프리필을 넣었다, 이 plan 은 링크만 만든다):**
```
ventago-app/src/pages/register/index.tsx   — BlankLayout + guestGuard = true
링크 형태: /register?ref={storeApodo}      ← 89-04 의 응답 필드 storeApodo
apodo 실측: store 6 = `cool` · store 9 = `Stock` (stores.alias_name)
```
★ **같은 호스트**(app.coolsistema.com)다 → `next/link` 또는 상대 경로 `href` 로 충분하다.
  서버가 URL 을 만들어 줄 필요가 없다.
★ `storeApodo` 가 **null 이면 `?ref=` 없이** `/register` 로 보낸다 —
  빈 `ref` 를 보내면 89-12 의 「해석 실패」와 구분이 안 된다.
★ ★ `guestGuard = true` — **로그인 상태면 튕긴다.** QR 을 찍는 사람은 보통 비로그인이라
  문제없지만 **자기 매장 직원이 찍으면 가입 화면에 못 간다.**
  89-12 Task 2 의 관측(E: 어디로 튕기는지)을 받아 이 plan 의 문구에 반영한다.

**신청자에게는 진행 상태를 알릴 수단이 없다** — reseller 로그인 포털이 빌드되는 앱 목록에
없다. 그래서 완료 문구는 그 사실을 감추지 않는다(「매장에서 연락드립니다」).
승인 후 알림은 **이 phase 범위 밖**이다(RESEARCH Open Question #2).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 백엔드 방어 3종 — storeIds 화이트리스트 · 업로드 제한/Throttle · 고아 파일 보상 삭제</name>
  <files>api-ventago/src/app/reseller/auth/reseller-auth.controller.ts, api-ventago/src/app/reseller/auth/reseller-auth.service.ts, api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts</files>
  <read_first>
    - `api-ventago/src/app/reseller/auth/reseller-auth.controller.ts` — **전체**(`FileFieldsInterceptor` 선언과 `@Public()`)
    - `api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 의 **205-300행 전체** — `publicStores()` 와 `register()`, 업로드 250행 / 트랜잭션 264행
    - `api-ventago/src/app/reseller/auth/reseller-auth.service.spec.ts` — 기존 시험이 무엇을 덮는지(깨뜨리지 말 것)
    - `api-ventago/src/common/minio/minio.service.ts` — 삭제 메서드가 있는지, 이름이 무엇인지 **직접 확인할 것**(없으면 `client.removeObject` 를 감싸는 메서드를 그 파일에 추가한다)
    - `api-ventago/src/app/reseller/reseller.model.ts:10` — 「Phase 24 reseller 스키마. legacy public.revendedores 와 별개」
    - `api-ventago/src/app/online-orders/public-delivery.controller.ts` — `@Throttle` 사용 형태
  </read_first>
  <behavior>
    **(가) storeIds 화이트리스트 — B-5**
    - `storeIds: [유효한 활성 매장]` → 기존과 **똑같이** 동작한다(REQ-04: 기존 계약 유지)
    - `storeIds: [존재하지 않는 매장]` → 400, `resellers` 행이 **생기지 않는다**
    - `storeIds: [비활성 매장]` → 400
    - `storeIds: []` → 400
    - `storeIds: [유효, 무효]` → 400 (부분 수용하지 않는다)
    - ★ 무효 케이스에서 **MinIO 업로드가 호출되지 않는다**(검증이 업로드보다 앞이다)

    **(나) 고아 파일 보상 삭제 — B-4**
    - DB 트랜잭션이 실패하면 이미 올라간 **파일 3개가 전부 삭제된다**
    - 삭제 자체가 실패해도 **원래 예외가 그대로 전파된다**(삭제 실패가 원인을 덮지 않는다)
    - ★ **커밋 이후에는 삭제하지 않는다** — 커밋 뒤 단계의 실패가 응답을 바꾸면 안 된다는
      이 저장소 규약과 충돌한다. 보상은 **커밋 전 실패 경로에서만** 돈다

    **(다) 업로드 제한 — B-3** (컨트롤러 선언이라 단위시험보다 **정적 확인**이 주 근거다)
    - `limits.fileSize` 5MB · `limits.files` 3 · `fileFilter` 이미지 MIME 화이트리스트
    - `@Throttle(PUBLIC_RESELLER_REGISTER_THROTTLE)`
  </behavior>
  <action>
**(1) `reseller-auth.controller.ts` — 업로드 제한 + Throttle (B-3)**

```typescript
// [Phase 89] 이 라우트는 **무인증**이고 Multer 는 메모리 저장이다.
// 제한이 없으면 요청 하나가 워커 메모리를 통째로 먹는다(OOM → 전 엔드포인트 다운).
// ★ 상한 근거: 운영 nginx `client_max_body_size 25m`(newapi 도메인 기본값, 2026-09-16 실측).
//   5MB × 3 = 15MB 라 멀티파트 오버헤드를 더해도 그 아래다.
//   **nginx 보다 크게 잡으면 앱 메시지 대신 뜻 없는 413 이 먼저 난다.**
const RESELLER_FILE_BYTES = 5 * 1024 * 1024;
const RESELLER_MIME = ['image/jpeg', 'image/png', 'image/webp', 'image/heic'];

@Public()
@Throttle(PUBLIC_RESELLER_REGISTER_THROTTLE)
@Post('register')
@UseInterceptors(
  FileFieldsInterceptor(
    [
      { name: 'dniPhoto', maxCount: 1 },
      { name: 'residenceCert', maxCount: 1 },
      { name: 'selfie', maxCount: 1 },
    ],
    {
      limits: { fileSize: RESELLER_FILE_BYTES, files: 3, fields: 12 },
      fileFilter: (_req, file, cb) => {
        if (!RESELLER_MIME.includes(file.mimetype)) {
          cb(new BadRequestException({ code: 'RESELLER_FILE_TYPE', message: 'Formato no permitido' }), false);

          return;
        }

        cb(null, true);
      },
    },
  ),
)
```
★ `@Throttle` 은 **`login` 에도 걸어야 하는가?** — 이 phase 의 범위 밖이다.
  건드리지 않고, SUMMARY 에 「`POST reseller/auth/login` 에는 여전히 Throttle 이 없다」를
  관측으로 남긴다(조용히 빠뜨리지 않는다).

**(2) `reseller-auth.service.ts` — storeIds 화이트리스트 (B-5)**

`register()` 의 **맨 앞**(파일 필수 검사와 MinIO 업로드보다 **먼저**):
```typescript
// [Phase 89] storeIds 를 **가입 폼이 보여주는 것과 같은 목록**으로 검증한다.
//
// ★ 왜 지금인가: 이 엔드포인트는 부르는 화면이 없어 한 번도 실행된 적이 없었다
//   (reseller 3개 테이블 전부 0행). Phase 89 가 QR 페이지에서 처음으로 도달 가능하게
//   만든다 — 범위를 넓히면 노출도 넓어진다. 그 노출을 같은 커밋에서 닫는다.
// ★ 판정의 근거는 저장의 근거와 같아야 한다 — `publicStores()` 가 폼에 보여주는
//   그 목록이고, 저장 허용 여부도 그 목록으로 정한다. 두 근거가 갈라지면 우회로가 생긴다.
// ★ reseller_tienda_link.store_id 에는 stores FK 가 **없다**(DB 실측) — 이 검사가 유일한 방어다.
// ★ 업로드보다 **먼저** 막는다. 뒤에서 막으면 MinIO 에 고아 객체가 남는다.
const allowed = new Set((await this.publicStores()).map((s) => s.id));
const requested = (dto.storeIds ?? []).map(Number);

if (requested.length === 0 || requested.some((id) => !allowed.has(id))) {
  throw new BadRequestException({
    code: 'RESELLER_STORE_INVALID',
    message: 'Tienda no disponible',
  });
}
```
★ **부분 수용을 하지 않는다.** 신청자는 「어느 매장에 신청했는지」를 확신할 수 있어야 한다.
★ 유효한 활성 매장에 대해서는 **종전과 동일**하다(REQ-04).

**(3) `reseller-auth.service.ts` — 고아 파일 보상 삭제 (B-4)**

현재 구조: `uploaded` 배열을 만든 뒤(250행) → bcrypt → `transaction` 시작(264행).
트랜잭션의 `catch` 에서 **커밋 전이라면** 올라간 객체를 지운다:
```typescript
} catch (err) {
  await tx.rollback();

  // [Phase 89] 업로드는 트랜잭션 **밖**에서 먼저 일어난다(커넥션 점유를 줄이려고).
  // DB 가 실패하면 신분증·거주증명·셀피가 MinIO 에 **고아로 남는다** — 개인정보다.
  // ★ 보상 삭제는 **커밋 전 실패 경로에서만** 돈다. 커밋 뒤 단계의 실패가 응답을
  //   바꾸면 안 된다는 이 저장소 규약과 충돌하지 않는다.
  // ★ 삭제가 실패해도 **원래 예외를 덮지 않는다** — 원인을 잃으면 진단이 불가능해진다.
  await Promise.allSettled(
    uploaded.map((u) => this.minio.removeFile(u.fileName)),
  );

  throw err;
}
```
★ `MinioService` 에 삭제 메서드가 **있는지 먼저 확인한다.** 없으면 그 파일에 추가한다:
```typescript
// [Phase 89] 보상 삭제용. 없는 객체는 예외가 아니라 조용한 no-op 이다 —
// 존재 확인은 «없음» 이 정상 결과인 조회이고, 예외로 만들면 호출부가 전부 try/catch 로 덮인다.
async removeFile(objectName: string): Promise<void> {
  try {
    await this.client.removeObject(this.bucket, objectName);
  } catch (e) {
    const code = (e as { code?: string })?.code;
    if (code === 'NotFound' || code === 'NoSuchKey') return;

    throw e;
  }
}
```
그 경우 `files_modified` 에 `api-ventago/src/common/minio/minio.service.ts` 를 추가하고
SUMMARY 에 적는다.

**(4) `reseller-auth.service.spec.ts` 에 (가)·(나) 케이스를 더한다**
— **기존 시험을 지우거나 고치지 않는다. 더하기만 한다.**
(다)는 컨트롤러 데코레이터 선언이라 단위시험으로 덮기 어렵다 → **정적 확인**이 acceptance 에 있다.

★ **대조군 실증 2건**(각각 확인하고 원복, SUMMARY 에 한 건씩):
  1. `if (requested.length === 0 || ...)` 블록을 지우면 「존재하지 않는 매장」 케이스가 빨개진다
  2. `Promise.allSettled(... removeFile ...)` 를 지우면 「고아 삭제」 케이스가 빨개진다
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit 2>&1 | head -20 && npx jest src/app/reseller --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    **(가) storeIds**
    - `grep -c "RESELLER_STORE_INVALID" api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 가 1 이다
    - 그 검증 블록의 행 번호가 `this.minio.uploadFile` 호출의 행 번호보다 **작다**
      (`grep -n` 두 번 돌려 숫자로 비교해 SUMMARY 에 적는다)
    - `grep -c "publicStores()" ...service.ts` 가 2 이상이다(정의 + register 내 사용)

    **(나) 고아 삭제**
    - `grep -c "removeFile\|removeObject" api-ventago/src/app/reseller/auth/reseller-auth.service.ts` 가 1 이상이다
    - 그 호출이 `tx.rollback()` 과 `throw err` **사이**에 있다(행 번호로 확인)
    - `grep -c "allSettled" ...service.ts` 가 1 이다(삭제 실패가 원래 예외를 덮지 않는다)

    **(다) 업로드 제한**
    - `grep -c "PUBLIC_RESELLER_REGISTER_THROTTLE" api-ventago/src/app/reseller/auth/reseller-auth.controller.ts` 가 1 이다
    - `grep -c "limits:" ...controller.ts` 가 1 이고 `fileSize` 와 `files: 3` 이 있다
    - `grep -c "fileFilter" ...controller.ts` 가 1 이고 `image/` 화이트리스트가 있다
    - ★ **nginx 상한 확인**: `ssh jhkim-server "grep -n client_max_body_size /etc/nginx/sites-enabled/newapi.coolsistema.com.conf"`
      의 도메인 기본값(**25m**)이 `fileSize × 3` 보다 **크다**는 계산을 SUMMARY 에 적는다

    **공통**
    - `npx jest src/app/reseller --maxWorkers=1` 출력에 `reseller-auth.service` suite 이름이 찍히고 종료코드 0
    - 기존 시험 건수가 **줄지 않았다**(변경 전후 `it(` 개수를 SUMMARY 에 기록)
    - SUMMARY 에 **대조군 2건**이 각각 적혀 있다
    - SUMMARY 에 「`POST reseller/auth/login` 에는 여전히 Throttle 이 없다」는 관측이 적혀 있다
  </acceptance_criteria>
  <done>신청 매장이 권위 목록으로 검증되고, 무인증 업로드가 제한되며, DB 실패가 KYC 사진을 남기지 않는다. 유효한 경우의 기존 계약은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: CTA① reseller 폼 + CTA② 가입 링크(주) + 이탈 받이 리드 폼(종) 마운트</name>
  <files>ventago-app/src/views/m-stock/ResellerApplyForm.tsx, ventago-app/src/views/m-stock/VentagoLeadForm.tsx, ventago-app/src/views/m-stock/QrProductView.tsx</files>
  <read_first>
    - `ventago-app/src/views/m-stock/QrProductView.tsx` — 89-06 이 만든 **현재 상태**(CTA 자리가 비어 있다)
    - `ventago-app/src/pages/entrega/[token].tsx` 의 34-49행 — raw fetch 와 에러 처리
    - `ventago-app/src/views/configuracion/tienda/DatosTiendaView.tsx` 의 103-113행 — FormData 조립
    - `api-ventago/src/app/reseller/auth/dto/reseller-register.dto.ts` — 필드 이름의 권위
    - 목업 6화면: https://claude.ai/code/artifact/fa2784ff-09a2-422d-9f95-fa905de99bf7 (「reseller신청」·「리드」)
  </read_first>
  <action>
**(1) `ResellerApplyForm.tsx`** — props: `{ storeId: number; storeName: string | null; onClose: () => void }`

필드: `name` · `phone` · `document` · `password`(≥6자) +
파일 3개 `dniPhoto` · `residenceCert` · `selfie`(각각 `<input type="file" accept="image/*">`).

```typescript
const fd = new FormData()
fd.append('name', name)
fd.append('phone', phone)
fd.append('document', document)
fd.append('password', password)
fd.append('storeIds', String(storeId))   // ★ QR 의 매장 하나로 고정
fd.append('dniPhoto', dniPhoto)
fd.append('residenceCert', residenceCert)
fd.append('selfie', selfie)

const res = await fetch(`${API_HOST}/reseller/auth/register`, { method: 'POST', body: fd })
```
★ **매장 선택 UI 를 노출하지 않는다.** 화면에는 `storeName` 을 **읽기 전용 문구**로만 보여준다
  (「Vas a solicitar ser revendedor de **{storeName}**」). 기존 가입 폼처럼
  `GET reseller/public/stores` 로 전체 목록을 부르지 **않는다**.
★ `headers` 에 `Content-Type` 을 넣지 않는다(boundary 가 깨진다).
★ 파일당 **5MB** 상한을 클라이언트에서 먼저 막는다 — 서버(Task 1)와 **같은 숫자**다.
  근거: 운영 nginx `client_max_body_size 25m`(newapi 도메인 기본값, 2026-09-16 실측)
  아래에 5MB × 3 = 15MB 가 들어간다. 초과 시 문구:
  `La foto es muy grande (máx 5 MB). Sacala de nuevo con menos calidad.`
  ★ 두 숫자가 갈라지면 「프론트는 통과시키는데 서버가 막는」 상태가 된다 — 같은 값을 쓴다.
★ 오류 코드별 문구:
  - 409 `RESELLER_DUPLICATE` → `Ese documento ya está registrado.`
  - 400 `RESELLER_DOCS_REQUIRED` → `Faltan fotos. Subí las tres.`
  - 400 `RESELLER_STORE_INVALID` → `Esta tienda no acepta solicitudes.`
  - 400 `RESELLER_FILE_TYPE` → `Solo se aceptan fotos (JPG, PNG, WEBP).`
  - 413 (nginx 또는 Multer 상한) → `Las fotos son muy grandes. Sacalas con menos calidad.`
  - 429 → `Demasiados intentos. Esperá unos minutos.`
  - 그 외 → `No pudimos enviar la solicitud. Probá de nuevo.`
★ 성공 화면(막다른 곳이 아니게 **무엇이 다음인지 말한다**):
  `Solicitud enviada. La tienda la va a revisar y se va a contactar con vos.`
  ★ 「로그인해서 상태를 확인하세요」 같은 문구를 **쓰지 않는다** — reseller 포털이 없다.
    없는 곳으로 안내하는 것이 곧 막다른 CTA 다.

**(2) `VentagoLeadForm.tsx`** — props: `{ storeId: number; productId: number | null; onClose: () => void }`

필드: `contactName` · `contactPhone` · `contactEmail`(선택) · `message`(선택, 500자).
```typescript
await fetch(`${API_HOST}/public/ventago-leads`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ storeId, sourceProductId: productId, contactName, contactPhone, contactEmail, message }),
})
```
★★ **`sourceProductId` 를 반드시 보낸다 (CODEX P2 C-3).** 그 컬럼은 89-01 의 스키마와
  89-05 의 DTO 에 **둘 다 있는데 폼이 안 보내면** 스키마와 계약이 갈라진다 —
  「있는데 늘 null 인 컬럼」이 되고 다음 사람이 그것을 버그로 오해한다.
  `QrProductView` 가 `product?.id ?? null` 을 그대로 내려 준다.
★ 이 값은 **참고용 귀속**이다. 서버가 그 상품이 `storeId` 것인지 확인하고 아니면 버린다
  (89-05). 폼이 「정확한 출처」라고 주장하지 않는다.
- 429 → `Demasiados intentos. Esperá un minuto.`
- 성공 → `¡Gracias! Te vamos a contactar.`

**(3) `QrProductView.tsx` 수정** — `mode === 'detail'` 화면 하단의 **비워 둔 CTA 자리**를
**시각적 위계 3단**으로 채운다(결정 ⑤-개정):

```
┌──────────────────────────────────────────────┐
│ [1] ¿Querés ser revendedor de {storeName}?   │  ← Button variant="contained" (골드)
│                                               │     → ResellerApplyForm (Dialog)
├──────────────────────────────────────────────┤
│ [2] ¿Querés este sistema en tu negocio?      │  ← Button variant="outlined"
│     Crear mi tienda                           │     → href=`/register?ref={storeApodo}`
│                                               │        ★ 주 도착지. 폼이 아니라 **링크**다
│ [3] ¿Preferís que te contactemos?             │  ← Link variant="caption" (작게)
│     Dejanos tu teléfono                       │     → VentagoLeadForm (Dialog) — 이탈 받이
└──────────────────────────────────────────────┘
```

- **[2] 가 CTA② 의 주 도착지다.** `next/link` 로 같은 호스트 내부 이동:
  ```tsx
  const registerHref = data.storeApodo
    ? `/register?ref=${encodeURIComponent(data.storeApodo)}`
    : '/register'
  ```
  ★ `storeApodo` 가 null 이면 **`?ref=` 를 아예 붙이지 않는다**(빈 값은 89-12 의
    「해석 실패」와 구분이 안 된다).
  ★ **보상을 단정하지 않는다** — 「50% 할인」·「보상을 받습니다」 같은 문구 금지
    (`referral_credits` 운영 0행). 쓸 수 있는 것은 사실뿐:
    `Vas a entrar con {storeName} como quien te recomendó.`
- **[3] 이 이탈 받이다.** `Button` 이 아니라 **작은 텍스트 링크**로 둔다 —
  [2] 와 같은 크기로 두면 주/종이 뒤집힌다.
  `VentagoLeadForm` 은 `Dialog` 로 연다.
- **[1]** `ResellerApplyForm` 을 `Dialog`(fullScreen on mobile)로.
- ★ 두 폼(`ResellerApplyForm`·`VentagoLeadForm`)은 `next/dynamic(..., { ssr: false })` 로
  지연 로드한다 — 열기 전에는 번들에 안 들어간다. **[2] 는 링크라 지연 로드가 필요 없다.**
- ★ `mode` 가 `shop_redirect`/`closed` 일 때는 **셋 다 렌더하지 않는다**
  (닫힘은 「아무것도 안 보인다」이고, 목록전환에는 갈 곳이 이미 하나 있다).

★★ **왜 이탈 받이를 이 화면에 두는가 (판단과 근거):**
  후보 ⓑ(`/register` 화면 안에서 이탈 감지)는 **`RegisterView`/`RegisterForm` 을 고쳐야**
  한다. 그 파일은 **운영에서 실제로 도는 가입 흐름**(`pending_registrations` 9행)이고
  다단계 OTP 를 품고 있다. 「이탈」을 감지하는 훅도 없다 —
  **되고 있는 것을 깨뜨릴 위험**을 지불하고 얻는 것이 불확실하다.
  ⓐ(이 화면)는 **이 phase 가 단독 소유한 파일** 안에서 끝난다.
  ⤷ **ⓐ 를 택한다.**
  ★ **다만 ⓐ 가 잡지 못하는 것을 분명히 한다:** `/register` 로 넘어간 **뒤에**
    그 화면 안에서 포기하는 사람은 이 받이에 안 걸린다. 그것은 ⓑ 만 할 수 있고
    이 phase 의 범위 밖이다. **「이탈을 잡았다」고 적지 말 것** — 잡는 것은
    「가입 링크를 누르지 않은 사람」과 「눌렀다가 돌아온 사람」뿐이다.

**ESLint:** `return` 위 빈 줄 · `//` 주석 위 빈 줄 · 미사용 import 금지.
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/views/m-stock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reseller/auth/register" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 1 이다
    - `grep -c "revendedor" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 로 **레거시 엔드포인트(`/revendedor/`)를 부르지 않음**을 확인한다(스페인어 표시 문구는 허용, `fetch` 대상 경로에 `/revendedor/` 가 없어야 한다)
    - `grep -c "reseller/public/stores" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 **0** 이다(매장 선택 UI 없음)
    - `grep -c "'Content-Type'" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 **0** 이다(FormData 에 헤더 금지)
    - `grep -c "public/ventago-leads" ventago-app/src/views/m-stock/VentagoLeadForm.tsx` 가 1 이다
    - `grep -c "sourceProductId" ventago-app/src/views/m-stock/VentagoLeadForm.tsx` 가 1 이상이다(C-3 — 스키마와 폼이 갈라지지 않는다)
    - `grep -c "5 \* 1024 \* 1024\|5242880" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 1 이상이다(서버와 같은 숫자)
    - `grep -c "429" ventago-app/src/views/m-stock/ResellerApplyForm.tsx` 가 1 이상이다(Throttle 응답을 사람 말로 번역한다)
    - `grep -c "apiConnector" ventago-app/src/views/m-stock/` 전체가 **0** 이다
    - `QrProductView.tsx` 에 `ResellerApplyForm` 과 `VentagoLeadForm` 이 각각 `next/dynamic` 으로 들어온다
    - `grep -c "/register?ref=" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이다(CTA② 주 도착지)
    - `grep -c "storeApodo" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이고, null 일 때 `'/register'` 로 떨어지는 분기가 있다
    - `grep -ci "50%\|bonificaci\|recompensa\|descuento" ventago-app/src/views/m-stock/QrProductView.tsx` 가 **0** 이다(보상 단정 금지 — `referral_credits` 0행)
    - CTA② 는 `variant="contained"` 가 **아니다**(그것은 CTA① 의 것) — 위계가 [1]contained [2]outlined [3]caption 링크다
    - `npx tsc --noEmit` 0, `npx eslint src/views/m-stock` 0
  </acceptance_criteria>
  <done>두 폼이 존재하고 상세 화면에서만 열리며, reseller 신청 매장이 QR 의 매장으로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 3: 막다른 CTA 방지 — 도착지 존재를 검사로 못 박고 실제로 끝까지 눌러 본다</name>
  <files>scripts/check-cta-destinos.sh</files>
  <read_first>
    - `api-ventago/scripts/check-tenant-ownership.sh` — 검사 스크립트 관례(종료코드·대조군·「무엇을 세는가」 주석)
    - 방금 만든 두 폼 파일 — 어떤 경로 문자열을 쓰는지
    - `api-ventago/src/app/reseller/auth/reseller-auth.controller.ts` · `api-ventago/src/app/leads/leads-public.controller.ts` — 서버 쪽 경로
    - `ventago-app/src/pages/admin/revendedores.tsx` — 승인 화면이 실재함(도착지 목록에 포함)
  </read_first>
  <action>
**(1) `scripts/check-cta-destinos.sh` 신규:**

```bash
#!/usr/bin/env bash
# [Phase 89] **막다른 CTA 를 막는다.**
#
# ★ 이 저장소에는 «없는 메뉴·없는 페이지로 보낸» 전례가 기록돼 있다. 버튼이 있는데
#   눌러도 아무 일이 없으면 기능은 없는 것과 같다. 그래서 «프론트가 부르는 경로» 와
#   «서버가 실제로 여는 경로» 를 **서로 다른 파일에서 읽어 대조한다.**
#   한쪽만 보면 둘이 같이 틀려도 통과한다.
#
# ★ 대조군: 존재하지 않는 경로를 같은 방식으로 검사했을 때 **반드시 실패해야** 한다.
#   그 판이 통과하면 이 스크립트는 아무것도 검사하지 않고 있는 것이다 → 종료코드 2.
#
# 사용: scripts/check-cta-destinos.sh            # 정적 대조만
#       API=http://localhost:5002/api scripts/check-cta-destinos.sh   # + 실제 HTTP 확인
# 종료코드: 도착지 없음 1 · 대조군이 통과 2 · 정상 0
set -uo pipefail
```

검사 내용:

**(a) 정적 대조 — 프론트 ↔ 서버**

| 프론트에서 찾는 문자열 | 짝이 되는 도착지 |
|---|---|
| `reseller/auth/register` (ResellerApplyForm.tsx) | `@Controller('reseller/auth')` + `@Post('register')` (reseller-auth.controller.ts) |
| `public/ventago-leads` (VentagoLeadForm.tsx) | `@Controller('public/ventago-leads')` + `@Post()` (leads-public.controller.ts) |
| `public/qr-stock` (pages/m/stock/index.tsx) | `@Controller('public/qr-stock')` (qr-public.controller.ts) |
| `/register?ref=` (QrProductView.tsx) | **파일 존재** `ventago-app/src/pages/register/index.tsx` + `RegisterForm.tsx` 에 `router.query.ref` |
| `/admin/revendedores` (승인 화면 — 코드가 링크하진 않지만 CTA① 의 **다음 화면**이다) | **파일 존재** `ventago-app/src/pages/admin/revendedores.tsx` |

★ `/register` 는 **API 라우트가 아니라 Pages Router 파일**이다 — Pages Router 는
  파일이 곧 라우트이므로 **파일 존재**가 도착지 존재다.
★ `RegisterForm.tsx` 에 `router.query.ref` 가 **없으면** 링크는 열리되 프리필이 안 된다 →
  그것은 조용한 절반 실패다. 그래서 **프리필 코드의 존재도 함께 센다**(89-12 의 산출물).

- 어느 한쪽이라도 0건이면 **exit 1** 과 함께 **어느 쪽이 없는지** 말한다.
- ★ `grep` 에 **`-a` 를 붙인다** — 이 저장소의 일부 소스가 바이너리로 판정돼
  조용히 0건이 나온 전례가 있다.
- ★ 경로 리터럴에 `.` 이 있으면 정규식 와일드카드가 된다. `grep -F`(고정 문자열)를 쓴다.

**(b) 대조군** — 위와 같은 방식으로 `public/no-existe-jamas` 를 찾는다.
  **찾히면 exit 2**(검사가 아무거나 통과시키고 있다).

**(c) 프론트 라우트 존재** — `ventago-app/src/pages/m/stock/index.tsx` 와
  `ventago-app/src/pages/admin/revendedores.tsx` 가 **파일로 존재**하는지.
  (Pages Router 는 파일이 곧 라우트다.)

**(d) `API` 환경변수가 있으면 실제 HTTP** — **404 가 아닌 것**을 확인한다:
```bash
curl -s -o /dev/null -w '%{http_code}' -X POST "$API/public/ventago-leads" \
  -H 'Content-Type: application/json' -d '{}'      # 400 또는 429 기대 (404 면 실패)
curl -s -o /dev/null -w '%{http_code}' -X POST "$API/reseller/auth/register"   # 400 기대 (404 면 실패)
curl -s -o /dev/null -w '%{http_code}' "$API/public/qr-stock/999999/999999"     # 404 기대 — 이건 정상
curl -s -o /dev/null -w '%{http_code}' "$API/onboarding/referral/check?apodo=cool"  # 200 기대 (@Public)
```
`APP` 환경변수가 있으면 프론트 라우트도 본다:
```bash
curl -sL -o /dev/null -w '%{http_code}' "$APP/register?ref=cool"   # 200 기대 (404 면 실패)
```
★ **400 은 성공이다.** 「라우트가 있고 검증까지 갔다」는 뜻이다. **404 만 실패다.**
★ `429` 도 성공이다(Throttle 이 살아 있다는 뜻) — 연속 호출로 429 를 한 번 받아 보고
  그 사실을 출력한다. **429 가 영영 안 나오면 Throttle 이 안 걸린 것**이다.
★ `/public/qr-stock/…` 는 없는 상품이라 404 가 정상 — 여기서는 **연결 자체가 되는지**만 본다
  (curl 종료코드 0).

**BSD 주의:** `\s` 대신 `[[:space:]]`. `set -e` 를 쓰지 않는다(진단 전에 죽는다).

**(2) 실제로 끝까지 눌러 본다 (로컬):**
api(5002) + app(3050) 을 띄우고 시크릿 창에서 `qr_precio_publico=true` 인 매장의
`/m/stock?s=X&p=P` 를 연 뒤:

1. CTA① → 폼 작성 + 사진 3장 첨부 → 제출 → **성공 화면**을 본다
   ```bash
   psql -p 5432 -d ventago -tAc "SELECT id,document,status FROM reseller.resellers ORDER BY id DESC LIMIT 3;"
   psql -p 5432 -d ventago -tAc "SELECT reseller_id,store_id,status FROM reseller.reseller_tienda_link ORDER BY id DESC LIMIT 3;"
   psql -p 5432 -d ventago -tAc "SELECT reseller_id,doc_type FROM reseller.reseller_documents ORDER BY id DESC LIMIT 5;"
   ```
   ★ `reseller` 는 **`public` 이 아닌 별도 스키마**다 — `FROM resellers` 로 쓰면 없다고 나온다.
   ★ **행이 실제로 생겼는지**를 본다. 성공 화면만으로는 증거가 안 된다.
   ★ 그 다음 **다음 버튼을 눌러 본다**: `/admin/revendedores` 를 열어 방금 신청이
     **pending 목록에 보이는지** 확인한다. 「실행된 적 없는 코드는 검증된 적 없는 코드」다 —
     한 번 통과해도 다음 화면이 죽어 있을 수 있다.
2. **CTA② (주 도착지)** → `Crear mi tienda` 를 누른다 → `/register?ref={apodo}` 가 열리는가?
   ★ **추천인 칸이 채워져 있는가**(89-12 의 프리필)? 확인 표시가 뜨는가?
   ★ 거기서 **가입을 끝까지 하지는 않는다**(CUIT·OTP 필요). 도달과 프리필까지가 이 plan 의 범위다.
   ★ `guestGuard` 확인: **로그인된 창**에서 같은 링크를 눌러 어디로 튕기는지 본다.
     그 동작은 **의도된 제약**이지만 **관측해 기록한다**(89-12 Task 2 의 E 와 같은 것).
3. **이탈 받이** → 작은 `Dejanos tu teléfono` 링크 → 리드 제출 →
   `psql -p 5432 -d ventago -tAc "SELECT id,store_id,source_product_id,contact_name,notify_status FROM ventago_leads ORDER BY id DESC LIMIT 3;"`
   ★ `source_product_id` 가 **채워져 있는지** 본다(C-3 — 폼이 실제로 보내는지의 증거).
   ★ `notify_status` 가 `'pending'` 이 아닌 값으로 바뀌는지 본다(C-4).

★ **만든 시험 데이터는 지우지 않는다**(이 저장소의 상시 규칙). 대신 SUMMARY 에
  **무엇이 남았는지와 그 영향**을 적는다(예: `reseller.resellers` 에 시험 신청 1건,
  status `pending_review`, isActive false — 승인 화면에 보인다).
  </action>
  <verify>
    <automated>bash -n scripts/check-cta-destinos.sh && bash scripts/check-cta-destinos.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-cta-destinos.sh` 종료코드 **0**
    - 그 출력에 **5개** 도착지가 각각 「프론트 N건 / 도착지 N건」 으로 찍힌다(전부 1 이상)
    - 그 출력에 `RegisterForm.tsx` 의 `router.query.ref` 존재가 **1건**으로 찍힌다
      (링크만 있고 프리필이 없으면 조용한 절반 실패다)
    - 그 출력에 대조군(`public/no-existe-jamas`)이 **0건**으로 찍힌다
    - `API=http://localhost:5002/api bash scripts/check-cta-destinos.sh` 에서
      `/public/ventago-leads` 와 `/reseller/auth/register` 가 **404 가 아닌** 상태코드를 돌려준다
    - 같은 실행에서 `/reseller/auth/register` 를 연속 호출해 **429 를 한 번 받았다**
      (Throttle 이 실제로 걸려 있음의 증거 — 안 나오면 실패로 보고한다)
    - 대조군 실증: `leads-public.controller.ts` 의 `@Controller('public/ventago-leads')` 를
      잠시 다른 문자열로 바꾸면 스크립트가 **exit 1** 이 되는 것을 확인하고 원복했다(SUMMARY 에 기록)
    - `reseller.resellers` · `reseller.reseller_tienda_link` · `reseller.reseller_documents` ·
      `ventago_leads` 네 테이블에 **실제로 행이 생긴** psql 출력이 SUMMARY 에 있다
    - `ventago_leads.source_product_id` 가 **null 이 아니다**(폼이 실제로 보냈다는 증거)
    - `/admin/revendedores` 에서 그 신청이 pending 으로 보인 것을 확인했다
    - **CTA② 를 눌러 `/register?ref={apodo}` 가 열렸고 추천인 칸이 채워져 있었다**는 관측이 SUMMARY 에 있다
    - **로그인된 창에서 CTA② 가 어디로 튕겼는지**가 SUMMARY 에 적혀 있다(의도된 제약이지만 기록한다)
  </acceptance_criteria>
  <done>두 CTA 가 끝까지 도달하고, 도착지 부재가 검사로 잡히며, 검사의 대조군이 실제로 반응한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷(비인증) → `POST /reseller/auth/register` | 이 phase 가 이 엔드포인트를 **처음으로 도달 가능하게** 만든다 |
| 업로드 파일 → MinIO `reseller/{document}/...` | 사용자 입력이 오브젝트 키의 일부가 된다 |
| 인터넷 → `POST /public/ventago-leads` | 89-05 의 threat model 이 덮는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-08 | Tampering | `ResellerRegisterDto.storeIds` | mitigate | `publicStores()`(isActive && status='1') 화이트리스트로 검증하고 **업로드보다 먼저** 막는다. 부분 수용하지 않는다 |
| T-89-26 | Information Disclosure | MinIO 키에 들어가는 `document` | accept | 기존 코드가 `reseller/{document}/{docType}-{ts}-{originalname}` 로 만든다. **슬래시를 포함하므로 `isPubliclyServable()` 이 공개 서빙에서 차단한다** — 공개 URL 로는 못 꺼낸다. 이 phase 는 그 코드를 바꾸지 않는다 |
| T-89-27 | Denial of Service | 무인증 대용량/다수 파일 업로드 → 워커 OOM | mitigate | `limits.fileSize` 5MB · `limits.files` 3 · `fileFilter` 이미지 MIME + `@Throttle(PUBLIC_RESELLER_REGISTER_THROTTLE)` 분당 3 / block 5분. Multer 는 **메모리 저장**이라 제한이 없으면 요청 하나가 워커를 죽인다. 상한은 운영 nginx 25m(실측) 아래로 잡는다 |
| T-89-43 | Information Disclosure | DB 실패 시 KYC 사진(신분증·거주증명·셀피) 고아 | mitigate | 커밋 **전** 실패 경로에서 `Promise.allSettled(removeFile)`. 삭제 실패가 원래 예외를 덮지 않는다. 커밋 뒤에는 삭제하지 않는다(응답 코드를 바꾸면 안 된다) |
| T-89-52 | Tampering | `?ref=` 로 임의 apodo 주입 | mitigate | 링크는 서버가 준 `storeApodo` 로만 만든다. 그러나 **사용자가 URL 을 직접 고칠 수 있다** — 최종 방어는 89-12 의 `checkReferralApodo` 검증과 승인 시점의 서버 판정이다. 이 plan 이 새로 여는 구멍은 없다 |
| T-89-53 | Denial of Service | 로그인 상태에서 CTA② 가 막다른 길 | **accept (제약)** | `guestGuard = true` 는 의도된 동작이다(가입은 새 매장용). **어디로 튕기는지 관측해 기록**하고, 필요하면 CTA 문구에 반영한다. 가드를 바꾸지 않는다 |
| T-89-54 | Repudiation | 근거 없는 보상 약속 | mitigate | `referral_credits` 운영 **0행** — 추천 가입이 승인까지 간 적이 없다. acceptance 가 「50%·bonificación·recompensa」 문구 **0건**을 센다 |
| T-89-44 | Denial of Service | `POST reseller/auth/login` 에 Throttle 없음 | **accept (범위 밖)** | 이 phase 는 `register` 만 도달 가능하게 만든다. `login` 은 부르는 화면이 여전히 없다. **관측으로 SUMMARY 에 남기고** 후속 후보로 넘긴다 — 조용히 빠뜨리지 않는다 |
| T-89-28 | Spoofing | 레거시 `revendedor` 모듈로 잘못 연결 | mitigate | acceptance 가 `fetch` 대상 경로에 `/revendedor/` 가 없음을 센다. 두 시스템은 이름만 비슷하고 스키마가 다르다 |
| T-89-29 | Repudiation | 「버튼은 있는데 아무 일도 안 일어남」 | mitigate | `check-cta-destinos.sh` 가 프론트·서버를 **서로 다른 파일에서 읽어** 대조하고, 대조군이 통과하면 스스로 exit 2 로 죽는다. 그리고 실제 DB 행 생성까지 확인한다 |
</threat_model>

<verification>
```bash
cd api-ventago && npx jest src/app/reseller --maxWorkers=1
cd ventago-app && npx tsc --noEmit && npx eslint src/views/m-stock
bash scripts/check-cta-destinos.sh
API=http://localhost:5002/api bash scripts/check-cta-destinos.sh
psql -p 5432 -d ventago -tAc "SELECT count(*) FROM reseller.resellers;"
psql -p 5432 -d ventago -tAc "SELECT count(*) FROM ventago_leads;"
```
</verification>

<success_criteria>
- CTA① 이 실제로 `reseller.resellers` + `reseller_tienda_link` + `reseller_documents` 에 행을 만든다
- 그 신청이 기존 승인 화면 `/admin/revendedores` 에 **보인다**(다음 버튼까지 눌러 봤다)
- CTA② (주)가 `/register?ref={apodo}` 를 열고 **추천인 칸이 채워진다**
- 이탈 받이(종)가 실제로 `ventago_leads` 에 행을 만들고 `source_product_id` 가 채워진다
- 화면 위계가 [1]reseller contained → [2]가입 outlined → [3]연락처 caption 링크다
- 신청 매장이 QR 의 매장으로 고정되고, 무효 매장은 400 으로 막힌다
- 무인증 업로드에 크기·개수·MIME 제한과 `@Throttle` 이 걸렸고, 상한이 **nginx 25m 아래**다
- DB 실패 시 KYC 사진이 MinIO 에 고아로 남지 않는다(커밋 전 경로에서만 보상 삭제)
- 리드 폼이 `sourceProductId` 를 실제로 보낸다
- `scripts/check-cta-destinos.sh` 가 통과하고, 도착지를 없애면 exit 1 이 되는 것을 확인했다
- 시험으로 만든 데이터는 지우지 않았고, 남은 것의 영향이 SUMMARY 에 적혀 있다
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-08-SUMMARY.md` 를 만든다.
★ **관측된 부수 사실**(수정 대상 아님, 기록만): `ResellerPublicController`
  (`GET /reseller/public/stores`)에는 `@Public()` 이 **없다** — 전역 JWT 가드 아래서
  무인증 호출이 401 일 가능성이 높다. 이 phase 는 그 엔드포인트를 쓰지 않으므로
  건드리지 않았다. 향후 reseller 포털을 만들 때 먼저 확인할 것.
★ 배포 순서: 89-05(리드 API) + Task 1(reseller 검증) 이 **먼저**, 그다음 프론트.
  반대면 버튼이 404 를 받거나 검증 없이 신청이 들어온다.
</output>
