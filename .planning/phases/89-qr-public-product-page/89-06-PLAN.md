---
phase: 89-qr-public-product-page
plan: 06
type: execute
wave: 3
depends_on: [04]
files_modified:
  - ventago-app/src/views/m-stock/qr-public.types.ts
  - ventago-app/src/pages/m/stock/index.tsx
  - ventago-app/src/views/m-stock/QrProductView.tsx
autonomous: true
requirements: [REQ-01, REQ-03, REQ-06]
must_haves:
  truths:
    - "계정 없이, 로그인 화면으로 튕기지 않고 `/m/stock/?s=6&p=1` 이 열린다"
    - "매장 · 지점 · 상품 사진 · 가격이 보인다"
    - "사진이 없는 상품(운영 실측 store 9 는 100%)도 화면이 깨지지 않는다"
    - "설정이 꺼지고 공개몰이 켜진 매장은 **공개몰로 가는 버튼**이 보인다 — 빈 화면이 아니다"
    - "설정도 공개몰도 꺼진 매장은 명확히 닫힌 화면을 본다 — 빈 화면이 아니다"
    - "가격이 `fallback` 이면 화면이 그 사실을 숨기지 않는다"
  artifacts:
    - path: "ventago-app/src/pages/m/stock/index.tsx"
      provides: "QR 도착지 공개 라우트"
      contains: "authGuard = false"
    - path: "ventago-app/src/views/m-stock/QrProductView.tsx"
      provides: "상세 / 사진없음 / 목록전환 / 닫힘 / 로딩 / 오류 화면"
      contains: "shop_redirect"
  key_links:
    - from: "pages/m/stock/index.tsx"
      to: "GET {API}/public/qr-stock/{s}/{p}"
      via: "raw fetch (apiConnector 금지)"
      pattern: "fetch\\(`\\$\\{API_HOST\\}/public/qr-stock"
    - from: "QrProductView.tsx"
      to: "{API}/minio/{imageUrl}"
      via: "이미지 조립 + null 폴백"
      pattern: "/minio/"
---

<objective>
**죽어 있는 라벨을 살린다.** `https://app.coolsistema.com/m/stock?s=6&p=1` 은 지금
308 → **404** 다. 매장에 붙어 있는 라벨이 그 404 로 떨어진다.

Purpose: 이 phase 의 첫 가치가 이것이다. 그리고 **URL 을 바꾸지 않는다** — 바꾸면
이미 붙은 라벨이 영구히 죽는다.

Output: `ventago-app/src/pages/m/stock/index.tsx` + 6개 화면 상태.
목업 6화면: https://claude.ai/code/artifact/fa2784ff-09a2-422d-9f95-fa905de99bf7
(이 plan 은 그중 **상세 · 사진없음 · 목록전환 · 닫힘** 4개를 만든다.
 reseller 신청 · 리드 두 화면은 89-08 이 만든다 — CTA 자리만 여기서 비워 둔다.)

★ 이 화면은 **이미지 모지바케 진단(89-02)을 기다리지 않는다.** 사진이 없거나 404 여도
  깨지지 않는 것이 여기서 독립적으로 보장돼야 한다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 발견이 아니라 복제다. -->

**89-04 가 내려주는 응답(그대로 타입으로 옮긴다):**
```typescript
type QrPublicMode = 'detail' | 'shop_redirect' | 'closed';

interface QrPublicProductDto {
  id: number; name: string;
  imageUrl: string | null;      // MinIO 오브젝트 키 또는 절대 URL
  price: number;
  priceLabel: string | null;
  priceSource: 'exact' | 'fallback';
}
interface QrPublicDto {
  mode: QrPublicMode;
  storeName: string | null; logoUrl: string | null;
  branchName: string | null; shopUrl: string | null;
  product: QrPublicProductDto | null;
}
```
경로: `GET {API_HOST}/public/qr-stock/{storeId}/{productId}?b={branchId}` — 404 는 「없음/남의 매장」.

**공개 페이지 3종 세트(실사용 중)** — `ventago-app/src/pages/entrega/[token].tsx:22-24, 34-49, 245-247`:
```typescript
const API_HOST =
  process.env.NODE_ENV === 'development' ? 'http://localhost:5002/api' : 'https://newapi.coolsistema.com/api'

EntregaPage.getLayout = (page: ReactNode) => <BlankLayout>{page}</BlankLayout>

// 로그인 없이 열린다 — 고객은 이 시스템의 사용자가 아니다.
EntregaPage.authGuard = false
EntregaPage.guestGuard = false
```
★ `_app.tsx:247` 의 `Component.authGuard ?? true` 때문에 **명시하지 않으면 기본 true** 이고,
  `AuthGuard` 가 비로그인 사용자를 `/login` 으로 `router.replace` 한다.
  ★ 로컬에서 **로그인된 브라우저로 테스트하면 이 증상이 안 보인다** — 시크릿 창으로 확인할 것.
★ `AclGuard` 의 리다이렉트는 `router.route === '/'` 전용이라 `/m/stock` 에는 걸리지 않는다
  (89-RESEARCH Q1-3 에서 코드로 확인). 단 **`UserLayout` 을 쓰면** 다른 전역 로직에 걸릴 수
  있으므로 `BlankLayout` 을 반드시 명시한다.
★ `apiConnector` 를 **쓰지 않는다** — 401 을 만나면 `/login` 으로 보내는 전역 인터셉터가 있다.
  같은 이유로 `src/services/store-theme.service.ts`(= `storefrontUrl`)도 import 하지 않는다.
  그 모듈이 `apiConnector` 를 import 한다. 공개몰 URL 은 **서버가 `shopUrl` 로 내려준다.**
★ `entrega` 가 보내는 `'x-api-key': '12345'` 는 **필요 없다**(서버에 HTTP x-api-key 가드가 없다 —
  grep 으로 확인됨). 복제하지 말 것.

**308 한 홉:** `next.config.js:123` `trailingSlash: true` 라 `/m/stock?s=..` → `/m/stock/?s=..`.
이미 `/entrega`·`/api/csp-report` 에 있는 **알려진·감수된 비용**이다. 새 위험이 아니다.

**이미지 URL 조립** — `ventago-app/src/components/product/ProductPhotoBox.tsx:8-9`:
```typescript
/^https?:\/\//.test(name) ? name : `${constants.api}/minio/${name}`
```
`constants.api` = `ventago-app/src/configs/constants.tsx` 의 `hostApi`
(dev `http://localhost:5002/api` / prod `https://newapi.coolsistema.com/api`).
★ 운영 실측: `GET /api/minio/<파일명>` 은 **`@Public()` 이고 200 이다.** 새 작업 없음.
★ 운영 실측 사진 없음 비율 — store 9(`stock`) 부모상품 **15개 중 0개**가 사진 있음,
  store 6(`cool`) **20개 중 7개**. **사진 없음은 예외가 아니라 주 경로다.**
★ 파일명의 53%가 모지바케라 200 이 아닐 수 있다(89-02 가 진단 중) →
  `<img onError>` 로도 자리표시자로 떨어져야 한다. **로드 실패는 정상 경로다.**

**성능 규약(CLAUDE.md):** 새 페이지는 `next/dynamic(..., { ssr: false })` 코드 스플리팅.

**ESLint(Warning 도 빌드를 막는다):** `return` 위 빈 줄 · `//` 주석 위 빈 줄 · 미사용 import 금지.

**테마:** 다크 네이비(#1a1a2e 계열) + 골드(#f5a623) — 공개몰 스토어프론트와 같은 톤.
프로젝트 skill `sketch-findings-ace-online` 을 참조한다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 응답 타입 + 공개 라우트 껍데기(authGuard=false · BlankLayout · raw fetch)</name>
  <files>ventago-app/src/views/m-stock/qr-public.types.ts, ventago-app/src/pages/m/stock/index.tsx</files>
  <read_first>
    - `ventago-app/src/pages/entrega/[token].tsx` — **전체**. 이 페이지의 템플릿이다
    - `ventago-app/src/pages/_app.tsx` 의 240-260행 — `authGuard ?? true` 기본값
    - `ventago-app/src/@core/layouts/BlankLayout.tsx` — import 경로 확인
    - `api-ventago/src/app/print/qr-public.dto.ts` — 서버가 실제로 내려주는 필드(권위)
  </read_first>
  <action>
**(1) `ventago-app/src/views/m-stock/qr-public.types.ts` 신규** — 위 `<interfaces>` 의 세 타입을
그대로 옮긴다(`QrPublicMode` / `QrPublicProductDto` / `QrPublicDto`). 주석으로
「서버 `api-ventago/src/app/print/qr-public.dto.ts` 가 권위다」를 남긴다.

**(2) `ventago-app/src/pages/m/stock/index.tsx` 신규:**

```typescript
// ─────────────────────────────────────────────────────────────────────────────
// [Phase 89] 상품 라벨 QR 의 도착지 — 공개 페이지 (/m/stock?s=<storeId>&p=<productId>[&b=<branchId>])
//
// ★ 경로 이름이 `stock` 인 것은 역사적 이유다. **여기에 재고 수량은 나오지 않는다**
//   (사용자 결정 D-02 — 수량은 판매원 전용 앱으로만 본다).
//   URL 을 바꾸면 이미 인쇄돼 매장에 붙은 라벨이 영구히 죽으므로 바꾸지 않는다.
//
// ★ `apiConnector` 를 쓰지 않는다 — 401 을 만나면 `/login` 으로 보내는 전역 인터셉터가 있다.
//   고객이 로그인 화면으로 튕기면 이 기능은 없는 것과 같다.
//   같은 이유로 `src/services/store-theme.service.ts` 도 import 하지 않는다(그 모듈이
//   apiConnector 를 끌고 온다). 공개몰 URL 은 서버가 `shopUrl` 로 내려준다.
// ─────────────────────────────────────────────────────────────────────────────
```

- `const API_HOST = process.env.NODE_ENV === 'development' ? 'http://localhost:5002/api' : 'https://newapi.coolsistema.com/api'`
- `useRouter()` 로 `s`·`p`·`b` 를 읽는다. `router.isReady` 가 true 가 될 때까지 기다린다
  (Pages Router 는 첫 렌더에서 query 가 비어 있다 — 이걸 빠뜨리면 항상 「잘못된 링크」가 뜬다).
- `s`/`p` 가 양의 정수가 아니면 fetch 하지 않고 `invalid` 상태로 간다.
- `fetch(\`${API_HOST}/public/qr-stock/${s}/${p}${b ? \`?b=${b}\` : ''}\`)`
  - `res.status === 404` → `notfound` 상태
  - `!res.ok` → `error` 상태
  - ok → `QrPublicDto`
- 뷰는 코드 스플리팅: `const QrProductView = dynamic(() => import('src/views/m-stock/QrProductView'), { ssr: false })`
- 상태(`loading` | `invalid` | `notfound` | `error` | `ok`)와 데이터를 뷰에 props 로 넘긴다.
- 파일 최하단(Task 1 에서 반드시 넣는다):
```typescript
MStockPage.getLayout = (page: ReactNode) => <BlankLayout>{page}</BlankLayout>

// 로그인 없이 열린다 — QR 을 찍은 사람은 이 시스템의 사용자가 아니다.
MStockPage.authGuard = false
MStockPage.guestGuard = false

export default MStockPage
```
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/pages/m/stock/index.tsx src/views/m-stock/qr-public.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ventago-app/src/pages/m/stock/index.tsx` 가 존재한다
    - `grep -c "authGuard = false" ventago-app/src/pages/m/stock/index.tsx` 가 1 이다
    - `grep -c "guestGuard = false" ventago-app/src/pages/m/stock/index.tsx` 가 1 이다
    - `grep -c "BlankLayout" ventago-app/src/pages/m/stock/index.tsx` 가 2 이상이다(import + getLayout)
    - `grep -c "apiConnector\|store-theme.service" ventago-app/src/pages/m/stock/index.tsx` 가 **0** 이다
    - `grep -c "x-api-key" ventago-app/src/pages/m/stock/index.tsx` 가 **0** 이다
    - `grep -c "router.isReady" ventago-app/src/pages/m/stock/index.tsx` 가 1 이상이다
    - `grep -c "next/dynamic" ventago-app/src/pages/m/stock/index.tsx` 가 1 이다
    - `npx tsc --noEmit` 0, `npx eslint <두 파일>` 0
  </acceptance_criteria>
  <done>라우트가 존재하고 인증 가드를 우회한다. 데이터 로딩 상태 5가지가 뷰로 전달된다.</done>
</task>

<task type="auto">
  <name>Task 2: 4개 본 화면 — 상세 / 사진없음 / 목록전환 / 닫힘</name>
  <files>ventago-app/src/views/m-stock/QrProductView.tsx</files>
  <read_first>
    - `ventago-app/src/pages/entrega/[token].tsx` 의 119-240행 — 로딩/에러/데이터 3단 분기와 MUI 사용 톤
    - `ventago-app/src/components/product/ProductPhotoBox.tsx` — 이미지 URL 조립 한 줄
    - `ventago-app/src/configs/constants.tsx` — `constants.api`
    - `.planning/phases/89-qr-public-product-page/89-CONTEXT.md` § 「④ 3갈래」 표
    - 목업 6화면: https://claude.ai/code/artifact/fa2784ff-09a2-422d-9f95-fa905de99bf7
  </read_first>
  <action>
`ventago-app/src/views/m-stock/QrProductView.tsx` 를 만든다. **모바일 세로 우선**,
다크 네이비 + 골드. MUI `Box/Paper/Typography/Button/CircularProgress/Alert` 만 쓴다.

**상태별 화면(빈 화면은 어느 경우에도 금지):**

| 상태 | 화면 |
|---|---|
| `loading` | 중앙 `CircularProgress` |
| `invalid` | 「Enlace incorrecto」 + 「Escaneá de nuevo el código de la etiqueta.」 |
| `notfound` | 「Producto no disponible」 + 「Puede que ya no esté a la venta.」 |
| `error` | 「No pudimos cargar la información」 + `Reintentar` 버튼 |
| `mode:'detail'` | 아래 ① |
| `mode:'shop_redirect'` | 아래 ② |
| `mode:'closed'` | 아래 ③ |

**① 상세** (목업 「상세」 / 「사진없음」):
- 상단: 매장 로고(`logoUrl` → `{constants.api}/minio/{key}`) + `storeName`.
  `branchName` 이 있으면 그 아래 작은 글씨로. **없으면 그 줄을 렌더하지 않는다**
  (「지점 정보 없음」 같은 문구도 쓰지 않는다 — 없다는 것을 말할 이유가 없다).
- 사진: 정사각 영역.
  ```typescript
  const src = product.imageUrl
    ? (/^https?:\/\//.test(product.imageUrl)
        ? product.imageUrl
        : `${constants.api}/minio/${encodeURIComponent(product.imageUrl)}`)
    : null
  ```
  ★ `encodeURIComponent` 를 쓴다 — 기존 9곳은 안 쓰지만, 이 저장소의 파일명 53%가
    비ASCII 다. **다만 그것으로 404 가 사라진다고 가정하지 않는다**(89-02 가 진단 중).
  - `src === null` **이거나** `<img onError>` 가 발화하면 **자리표시자**를 그린다:
    다크 네이비 배경 + 골드 테두리 + 아이콘 + `Sin foto`.
    ★ 이 폴백은 「예외 처리」가 아니라 **주 경로**다(store 9 는 100%가 여기로 온다).
- 상품명(`name`), 가격(`price`) — 큰 골드 숫자.
- `priceLabel` 이 있으면 가격 옆에 작은 칩으로 표시.
- ★ `priceSource === 'fallback'` 이면 가격 아래에 **반드시** 한 줄:
  `Precio de referencia — puede no coincidir con la etiqueta.`
  (`priceSource === 'exact'` 일 때는 이 줄을 **넣지 않는다**.)
  ★ 이것을 숨기면 종이와 화면이 다른 값을 말할 때 고객이 그 사실을 모른다(D-01 전제 4).
- 하단: CTA 두 자리를 **비워 둔다**. 89-08 이 채운다. 지금은 렌더하지 않는다
  (동작하지 않는 버튼을 먼저 보이면 그것이 곧 막다른 CTA 다).

**② 목록전환 (`shop_redirect`)** (목업 「목록전환」):
- 매장 로고 + `storeName`.
- 문구: `Mirá el catálogo online de la tienda.`
- 버튼: `Ver catálogo` → `href={data.shopUrl}` (`target="_blank" rel="noopener noreferrer"`).
- ★ `shopUrl` 이 null 이면 이 화면을 그리지 않고 `closed` 화면으로 떨어진다
  (서버가 그렇게 안 주지만, **막다른 버튼을 만들지 않는 것**이 프론트의 마지막 방어다).

**③ 닫힘 (`closed`)** (목업 「닫힘」):
- 매장명·로고를 **표시하지 않는다**(서버도 null 로 준다 — D-04 「아무것도 안 보인다」).
- 문구: `Esta tienda no comparte información de productos por QR.` 한 줄.
- 버튼 없음. 어디로도 보내지 않는다. **회색 톤**(골드 강조 없음).

**ESLint:** `return` 위 빈 줄, `//` 주석 위 빈 줄, 미사용 import 금지.
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/views/m-stock/QrProductView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shop_redirect" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이다
    - `grep -c "'closed'" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이다
    - `grep -c "Sin foto" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이다
    - `grep -c "onError" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이다
    - `grep -c "Precio de referencia" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이다
    - `grep -c "encodeURIComponent" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이다
    - `grep -c "rel=\"noopener noreferrer\"" ventago-app/src/views/m-stock/QrProductView.tsx` 가 1 이상이다
    - `grep -ci "stock\b\|cantidad\|unidades" ventago-app/src/views/m-stock/QrProductView.tsx` 로 **재고를 표시하는 문구가 없음**을 확인(경로 주석의 `m-stock` 제외)
    - `npx tsc --noEmit` 0, `npx eslint` 0
  </acceptance_criteria>
  <done>4개 본 화면 + 4개 상태 화면이 전부 있고 빈 화면 경로가 없다.</done>
</task>

<task type="auto">
  <name>Task 3: 로컬 실측 — 비로그인으로 실제로 열리는지, 4갈래가 실제로 갈리는지</name>
  <files>(검증 전용 — 소스 변경 없음. 필요 시 Task 1·2 파일 수정)</files>
  <read_first>
    - 방금 만든 두 파일
    - `api-ventago/src/app/print/qr-public.service.ts` — 3갈래 판정 조건
    - `./CLAUDE.md` § 「화면 검증은 cmux browser 로」 관련 규약
  </read_first>
  <action>
로컬에서 api(5002) + app(3050 또는 5001)을 띄우고 **네 갈래를 실제로 본다.**
로컬 DB 는 이미 89-01 이 적용돼 있다.

```bash
# 0) 판정에 쓸 매장·상품을 고른다 (로컬 5432)
psql -p 5432 -d ventago -tAc \
 "SELECT p.id, p.store_id, s.slug, COALESCE(c.qr_precio_publico,false)
    FROM products p JOIN stores s ON s.id=p.store_id
    LEFT JOIN store_configs c ON c.store_id=s.id
   WHERE COALESCE(p.is_active,TRUE) ORDER BY p.id LIMIT 20;"

# 1) closed  — qr_off + slug 없음  (기본 상태이므로 아무 매장이나 된다)
# 2) shop_redirect — qr_off + slug 있음
psql -p 5432 -d ventago -c "UPDATE stores SET slug='cool' WHERE id=<X> AND slug IS NULL;"   # 없으면
# 3) detail — qr_on
psql -p 5432 -d ventago -c "UPDATE store_configs SET qr_precio_publico=true WHERE store_id=<X>;"
# ★ 로컬 DB 에 대한 변경이다. 시험이 끝나면 되돌린다(되돌린 사실을 SUMMARY 에 적는다).

# API 단독 확인
curl -s "http://localhost:5002/api/public/qr-stock/<X>/<P>" | head -c 300
curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:5002/api/public/qr-stock/<다른매장>/<P>"   # 404 여야 한다
```

브라우저 확인(**반드시 시크릿 창** — 로그인된 창에서는 authGuard 결함이 안 보인다):
```
http://localhost:3050/m/stock?s=<X>&p=<P>
http://localhost:3050/m/stock?s=<X>&p=<P>&b=<지점>
http://localhost:3050/m/stock?s=<X>&p=999999      → 「Producto no disponible」
http://localhost:3050/m/stock                      → 「Enlace incorrecto」
```

확인 항목:
1. `/login` 으로 **튕기지 않는다**
2. 308 한 홉 뒤 페이지가 200 으로 렌더된다
3. 사진 없는 상품에서 `Sin foto` 자리표시자가 나온다 (사진 있는 상품도 하나 확인)
4. `qr_on=false`+slug 있음 → `Ver catálogo` 버튼이 나오고 **클릭하면 실제로 열린다**
5. `qr_on=false`+slug 없음 → 닫힘 문구, 버튼 없음
6. `priceSource='fallback'` 인 상품에서 `Precio de referencia` 줄이 보인다
   (`qr_print_log` 가 로컬에 없으면 대부분 fallback 이다 — 그래서 `exact` 를 보려면
   `qr_print_log` 에 시험용 1행을 넣고 확인한 뒤 지운다)

SUMMARY 에 **각 화면의 스크린샷 또는 관측 문장**과, 로컬 DB 를 원복했다는 사실을 적는다.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' -L "http://localhost:3050/m/stock?s=6&p=1"</automated>
  </verify>
  <acceptance_criteria>
    - 시크릿 창에서 `/m/stock/?s=..&p=..` 이 `/login` 으로 이동하지 **않는다**(관측 문장을 SUMMARY 에)
    - `curl -sL "http://localhost:3050/m/stock?s=<X>&p=<P>"` 최종 상태코드가 **200** 이다
    - `curl -s -o /dev/null -w '%{http_code}' "http://localhost:5002/api/public/qr-stock/<다른매장>/<P>"` 가 **404** 다
    - 네 갈래(detail / 사진없음 / shop_redirect / closed)를 **각각 실제로 봤다**는 기록이 SUMMARY 에 있다
    - `shop_redirect` 의 `Ver catálogo` 버튼을 눌러 **실제로 페이지가 열렸다**(막다른 CTA 아님)
    - 로컬 DB 에 넣은 시험용 변경(`slug`·`qr_precio_publico`·`qr_print_log` 행)을 되돌렸다
  </acceptance_criteria>
  <done>네 갈래가 실제 브라우저에서 갈렸고, 비로그인으로 열렸으며, 이동 버튼이 실제로 도착했다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷(비인증 브라우저) → `/m/stock` | 누구나 URL 의 `s`·`p`·`b` 를 바꿔 요청한다 |
| 페이지 → `{API}/minio/{key}` | 서버가 준 키가 이미지 요청 URL 이 된다 |
| 페이지 → `shopUrl`(외부 오리진) | 서버가 준 문자열이 링크 목적지가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | Information Disclosure | 이미지 키 → URL 조립 | mitigate | `encodeURIComponent(key)` 로 경로 분절을 막는다. 서버 `isPubliclyServable()` 이 슬래시·`..`·비이미지 확장자를 이미 차단한다(변경하지 않는다) |
| T-89-21 | Tampering | `shopUrl` 링크(tabnabbing) | mitigate | `target="_blank"` 에 `rel="noopener noreferrer"` 필수. `shopUrl` 은 서버가 `https://{slug}.{base}` 로 조립한 값만 쓴다 — 클라이언트 입력을 링크에 넣지 않는다 |
| T-89-22 | Elevation of Privilege | `authGuard` 누락으로 `/login` 튕김 | mitigate | `authGuard=false`·`guestGuard=false` 를 acceptance 가 grep 으로 센다. **시크릿 창 확인**이 acceptance 에 들어 있다(로그인 창에서는 증상이 안 보인다) |
| T-89-09 | Elevation of Privilege | 프론트에서만 가리기 | accept | 이 화면은 서버가 이미 걸러 준 것만 그린다. `closed` 에서는 서버가 `storeName` 조차 null 로 주므로 프론트가 «가릴» 데이터를 갖고 있지 않다 |
| T-89-23 | Denial of Service | 라우터 query 미준비로 항상 오류 | mitigate | `router.isReady` 대기 — 빠뜨리면 **모든 방문자**가 「Enlace incorrecto」를 본다 |
</threat_model>

<verification>
```bash
cd ventago-app && npx tsc --noEmit && npx eslint src/pages/m/stock/index.tsx src/views/m-stock/QrProductView.tsx
grep -c "authGuard = false" ventago-app/src/pages/m/stock/index.tsx    # 1
grep -c "apiConnector" ventago-app/src/pages/m/stock/index.tsx         # 0
curl -sL -o /dev/null -w '%{http_code}\n' "http://localhost:3050/m/stock?s=6&p=1"
```
</verification>

<success_criteria>
- `/m/stock/?s=..&p=..` 이 **비로그인 시크릿 창에서 200** 으로 렌더된다
- 4개 본 화면 + 4개 상태 화면이 전부 있고, **빈 화면으로 끝나는 경로가 없다**
- 사진 없음·이미지 로드 실패가 자리표시자로 떨어진다(주 경로로 설계됨)
- `priceSource='fallback'` 일 때 화면이 그 사실을 말한다
- `Ver catálogo` 가 실제로 열린다(막다른 버튼 아님)
- `tsc` · `eslint` 종료코드 0
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-06-SUMMARY.md` 를 만든다.
★ 네 갈래를 각각 봤다는 관측 기록과, 로컬 DB 원복 사실을 반드시 적는다.
★ 배포 순서: API(89-04) → 이 페이지. 반대면 페이지가 404 를 받는다.
</output>
