---
phase: 89-qr-public-product-page
plan: 12
type: execute
wave: 3
depends_on: []
files_modified:
  - ventago-app/src/views/register/components/RegisterForm.tsx
autonomous: true
requirements: [REQ-08]
must_haves:
  truths:
    - "`/register?ref=cool` 로 들어가면 「¿Quién te recomendó?」 칸에 `cool` 이 **미리 채워져 있다**"
    - "프리필된 값도 **기존 blur 검증을 그대로 거친다** — 오타난 apodo 가 조용히 저장되지 않는다"
    - "`ref` 가 해석되지 않으면 칸은 **빈 값**이다 — 아무 매장으로 폴백하지 않는다"
    - "`?ref=` 없이 들어온 기존 가입 흐름이 **하나도 바뀌지 않는다**"
  artifacts:
    - path: "ventago-app/src/views/register/components/RegisterForm.tsx"
      provides: "?ref= 프리필 + 프리필 값의 검증 통과"
      contains: "useRouter"
  key_links:
    - from: "RegisterForm.tsx"
      to: "GET /onboarding/referral/check?apodo="
      via: "프리필 직후 1회 조회(기존 blur 핸들러 재사용)"
      pattern: "checkReferralApodo"
---

<objective>
CTA② 「당신 매장에도 이 시스템을」의 **도착지를 실제로 쓸 수 있게** 만든다.

Purpose: 사용자 결정 ⑤-개정(2026-09-16) — CTA② 는 **새 리드 폼이 아니라 이미 있는
매장 가입 화면** `/register?ref={QR 매장의 apodo}` 다.

★★ **reseller CTA 와 결정적으로 다르다.** reseller 경로는 0행(한 번도 안 돌았다)이지만
  가입 경로는 **실제로 돌고 있다** — 운영 `pending_registrations` **9행**, 그중 **3행**이
  `referred_by_apodo` 를 담고 있다. 추천 필드가 **이미 실사용 중**이다.
  ⤷ 그래서 이 plan 의 위험은 「안 되는 것을 되게 하는 것」이 아니라
    **「되고 있는 것을 깨뜨리는 것」**이다. 변경을 최소로 한다.

Output: `RegisterForm.tsx` 에 `?ref=` 프리필 한 가지. **그 외에는 아무것도 건드리지 않는다.**

★ **귀속이 리드 테이블이 아니라 실제 돈으로 흐른다.** `referredByApodo` →
  `referral_credits`(추천 매장 월사용료의 **50%** bonificación, `percent` 기본값 50).
  그래서 「누가 어느 매장 QR 에서 왔는지」를 새 테이블에 따로 기록할 이유가 사라진다.

★★ **아직 실행된 적 없는 절반이 있다 — 단정하지 말 것.**
  운영 `referral_credits` **0행**이다. 보상은 **승인 시점**에 생기므로
  「추천 가입이 승인까지 간 적이 없다」는 뜻이다.
  ⤷ **「추천하면 매장이 보상을 받습니다」를 화면에 단정해 쓰지 않는다.**
    이 plan 도, 89-08 의 CTA 문구도 그 사실에 맞춰 낮춘다.
    (그 경로를 끝까지 확인하는 것은 이 phase 의 범위 밖이다 — 승인은 superadmin 행위다.)
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
</context>

<interfaces>
<!-- 실행자가 그대로 고칠 현재 코드. 2026-09-16 파일 직접 확인. -->

**`ventago-app/src/pages/register/index.tsx` (전문 — 이 파일은 고치지 않는다):**
```typescript
const Register = () => {
  return (<RegisterView />)
}

Register.getLayout = (page: ReactNode) => <BlankLayout>{page}</BlankLayout>

Register.guestGuard = true
```
★ `guestGuard = true` — **로그인 상태면 이 화면에서 튕긴다.**
  QR 을 찍는 사람은 보통 비로그인이라 문제없지만, **자기 매장 직원이 찍으면 가입 화면에
  못 간다.** 이 동작은 **바꾸지 않는다**(가입은 새 매장을 위한 화면이다).
  다만 **그 사실을 확인하고 기록한다** — 이 저장소에는 「마운트됨 ≠ 도달 가능」 전례가 있다.

**`ventago-app/src/views/register/components/RegisterForm.tsx` 의 현재 상태:**

`import { useEffect, useState } from 'react';`(15행) · `import { useForm } from 'react-hook-form';`(21행)
— ★ **`useRouter` 가 없다.** 이것을 더해야 한다.

```typescript
// :53-55  yup — 추천인 apodo 는 선택이다(불일치해도 가입은 진행)
referredByApodo: yup.string().notRequired(),

// :57-71  ★ defaultValues 가 **정적 객체**다. 그래서 쿼리로 채울 수 없다.
const defaultValues = {
  name: '', lastName: '', username: '', email: '', phone: '',
  companyCuit: '', companyName: '', aliasName: '', companyAddress: '',
  password: '', repeatPassword: '', acceptConditions: false,
  referredByApodo: '',
};

// :116  표시 상태
const [referral, setReferral] = useState<ReferralCheck>({ state: 'idle' });

// :130-132  setValue 를 이미 쓸 수 있다
const { ..., setValue, ... } = useForm({ defaultValues, ... });

// :141-146  같은 파일이 이미 setValue 로 값을 채우는 선례
useEffect(() => {
  ...
  setValue('username', `admin@${cleanAlias}`);
}, [aliasName, setValue]);

// :166-186  blur 검증 (그대로 재사용한다)
const checkReferralApodo = async (raw: string) => {
  const apodo = (raw || '').trim().replace(/^@/, '');
  if (!apodo) { setReferral({ state: 'idle' }); return; }
  setReferral({ state: 'checking' });
  try {
    const res: any = await apiConnector.get(`/onboarding/referral/check?apodo=${encodeURIComponent(apodo)}`);
    if (res?.found) setReferral({ state: 'ok', ownerName: res.ownerName, storeName: res.storeName });
    else setReferral({ state: 'notfound', apodo });
  } catch {
    // 조회 실패는 가입을 막지 않음 — 표시만 초기화
    setReferral({ state: 'idle' });
  }
};

// :422-440  필드. onBlur 에서 checkReferralApodo 를 부른다
label="¿Quién te recomendó? (opcional)"
```

**서버 쪽(변경 없음):** `api-ventago/src/app/onboarding/onboarding.controller.ts:31`
`GET /onboarding/referral/check?apodo=` 가 **`@Public()`** 이다.
`api-ventago/src/app/onboarding/referral-credit.model.ts` — `percent` 기본값 **50**.

**매장 apodo = `stores.alias_name`** — 운영 실측: store 6 = `cool` · store 9 = `Stock`.

**ESLint(Warning 도 빌드를 막는다):** `return` 위 빈 줄 · `//` 주석 위 빈 줄 · 미사용 import 금지.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `?ref=` 프리필 — 값을 채우고 **기존 검증을 그대로 태운다**</name>
  <files>ventago-app/src/views/register/components/RegisterForm.tsx</files>
  <read_first>
    - `ventago-app/src/views/register/components/RegisterForm.tsx` — **전체를 읽는다.** 실사용 중인 가입 흐름이고, 이 plan 의 위험은 되고 있는 것을 깨뜨리는 것이다
    - 특히 15·21행(import), 57-71행(`defaultValues`), 116행(`referral` 상태), 130-132행(`useForm`), 141-146행(`setValue` 선례), 166-186행(`checkReferralApodo`), 422-440행(필드)
    - `ventago-app/src/pages/register/index.tsx` — `guestGuard = true`
  </read_first>
  <action>
**변경은 세 군데뿐이다. 그 외에는 이 파일을 건드리지 않는다.**

**(1) import 추가** (`useRouter` 가 없다):
```typescript
import { useRouter } from 'next/router';
```

**(2) 컴포넌트 안에서 라우터를 잡는다** — `useForm` 선언 **뒤**:
```typescript
const router = useRouter();
```

**(3) 프리필 `useEffect` 를 하나 더한다** (141행의 `setValue` 선례 바로 아래):
```typescript
// [Phase 89] QR 공개 상품 페이지의 CTA② 가 `/register?ref={apodo}` 로 보낸다.
// 추천 매장을 손으로 다시 적게 하면 대부분 안 적는다 — 그러면 귀속이 사라지고
// 추천 매장의 보상(referral_credits, 월사용료의 50%)도 발생하지 않는다.
//
// ★ 프리필된 값도 **기존 blur 검증을 그대로 거친다.** 사용자가 그 칸을 만지지 않으면
//   onBlur 가 발화하지 않으므로 여기서 한 번 직접 부른다.
//   검증을 건너뛰면 **오타난 apodo 가 조용히 저장된다** — 그 상태로 승인되면
//   엉뚱한 매장에 보상이 가거나 아무 데도 안 간다.
//
// ★ 해석되지 않으면 **빈 값**으로 둔다. 아무 매장으로 폴백하지 않는다 —
//   이 저장소의 규칙: «좁히는 필터가 넓어지면 안 된다».
//   `checkReferralApodo` 가 'notfound' 를 표시하므로 사용자가 고칠 수 있다.
//
// ★ 한 번만 돈다. router.isReady 전에는 query 가 비어 있고(Pages Router),
//   이미 사용자가 무언가 입력한 뒤에 덮어쓰면 입력이 사라진다.
useEffect(() => {
  if (!router.isReady) return;

  const raw = router.query.ref;
  const ref = (Array.isArray(raw) ? raw[0] : raw || '').trim().replace(/^@/, '');

  if (!ref) return;

  setValue('referredByApodo', ref, { shouldDirty: false });
  void checkReferralApodo(ref);
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [router.isReady]);
```

★ **`checkReferralApodo` 가 선언된 뒤에** 이 `useEffect` 를 둔다(TDZ 회피).
  파일 안의 선언 순서를 **읽고** 위치를 정한다 — 142행에 그냥 끼워 넣지 말 것.
★ `shouldDirty: false` — 사용자가 아직 아무것도 안 했으므로 폼을 dirty 로 만들지 않는다.
★ `?ref=` 가 없으면 **아무 일도 일어나지 않는다.** 기존 가입 흐름은 그대로다.

**★ 이 파일에서 하지 않을 것 (범위 고정):**
- `guestGuard` 를 바꾸지 않는다(로그인 상태면 튕기는 것이 의도된 동작이다)
- OTP·주소·CUIT 단계를 건드리지 않는다
- 「추천하면 보상을 받는다」 같은 문구를 **추가하지 않는다** —
  `referral_credits` 운영 **0행**이라 그 경로가 승인까지 간 적이 없다.
  단정할 근거가 없다.
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/views/register/components/RegisterForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useRouter" ventago-app/src/views/register/components/RegisterForm.tsx` 가 **2** 이다(import + 호출)
    - `grep -c "router.query.ref" ...` 가 1 이다
    - `grep -c "setValue('referredByApodo'" ...` 가 1 이다
    - 그 `useEffect` 안에 `checkReferralApodo(` 호출이 있다(프리필 값이 검증을 탄다)
    - `grep -c "router.isReady" ...` 가 1 이상이다
    - `grep -ci "bonificación\\|50%\\|recompensa" ...` 로 **보상을 단정하는 문구가 추가되지 않았음**을 확인한다(기존 문구 외 신규 0건)
    - `guestGuard` 는 `ventago-app/src/pages/register/index.tsx` 에서 **여전히 `true`** 다
    - `npx tsc --noEmit` 0, `npx eslint src/views/register/components/RegisterForm.tsx` 0
  </acceptance_criteria>
  <done>`?ref=` 가 채워지고 검증을 타며, 없을 때의 동작은 종전과 완전히 같다.</done>
</task>

<task type="auto">
  <name>Task 2: 도달성 실측 — 프리필·검증·guestGuard 를 실제 브라우저로 확인</name>
  <files>(검증 전용 — 소스 변경 없음. 실패 시 Task 1 수정)</files>
  <read_first>
    - 방금 고친 `RegisterForm.tsx`
    - `ventago-app/src/pages/register/index.tsx` — `guestGuard = true`
    - `api-ventago/src/app/onboarding/onboarding.controller.ts` 의 31행 — `referral/check` 가 `@Public()` 인지 재확인
  </read_first>
  <action>
로컬 api(5002) + app(3050) 을 띄우고 **네 가지를 실제로 본다.**

```bash
# 0) apodo 를 확인한다 (로컬 5432)
psql -p 5432 -d ventago -tAc "SELECT id, name, alias_name FROM stores WHERE alias_name IS NOT NULL ORDER BY id LIMIT 10;"

# 1) 엔드포인트가 무인증으로 여는지 (@Public() 확인)
curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:5002/api/onboarding/referral/check?apodo=cool"
curl -s "http://localhost:5002/api/onboarding/referral/check?apodo=cool"
curl -s "http://localhost:5002/api/onboarding/referral/check?apodo=no-existe-jamas"
```

**브라우저 — 반드시 시크릿 창(비로그인):**

| # | 주소 | 기대 |
|---|---|---|
| A | `/register?ref=<존재하는 apodo>` | 추천인 칸이 **채워져 있고**, 초록/확인 표시(`state: 'ok'`)가 뜬다 |
| B | `/register?ref=no-existe-jamas` | 칸은 채워지되 **`notfound` 표시**가 뜬다 (조용히 통과하지 않는다) |
| C | `/register?ref=` (빈 값) 및 `/register` | 칸이 **비어 있고** 아무 표시도 없다. 기존과 동일 |
| D | `/register?ref=%40cool` (`@cool`) | `@` 가 벗겨져 `cool` 로 조회된다 |

**로그인한 창에서:**

| # | 주소 | 기대 |
|---|---|---|
| E | `/register?ref=cool` | **튕긴다**(`guestGuard = true`). 이것이 **의도된 동작**이다 |

★ E 는 **결함이 아니라 제약**이다. 그러나 **기록해야 한다** —
  자기 매장 직원이 자기 매장 QR 을 찍으면 가입 화면에 못 간다.
  **어디로 튕기는지**(로그인 사용자의 홈?)를 실제로 보고 적는다.
  89-08 의 CTA 문구가 그 사실을 반영해야 할 수 있다.

SUMMARY 에 A~E **다섯 가지 관측**을 각각 한 줄씩 적는다.
「됐다」가 아니라 **무엇이 보였는지**를 적는다.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:5002/api/onboarding/referral/check?apodo=cool" && curl -sL -o /dev/null -w '%{http_code}\n' "http://localhost:3050/register?ref=cool"</automated>
  </verify>
  <acceptance_criteria>
    - `curl .../onboarding/referral/check?apodo=cool` 가 **200**(401 이 아니다 — `@Public()` 이 살아 있다)
    - `curl -sL http://localhost:3050/register?ref=cool` 최종 상태코드 **200**
    - A: 시크릿 창에서 칸이 **채워지고** 확인 표시가 떴다 — 관측 문장이 SUMMARY 에 있다
    - B: 없는 apodo 에서 **`notfound` 표시가 떴다**(조용히 통과하지 않았다)
    - C: `?ref=` 없이 들어갔을 때 **기존과 동일**했다(칸 비어 있음, 표시 없음)
    - D: `@cool` 이 `cool` 로 정규화됐다
    - E: 로그인 창에서 **어디로 튕겼는지**가 SUMMARY 에 적혀 있다
  </acceptance_criteria>
  <done>프리필이 실제로 되고 검증을 타며, 로그인 상태의 제약이 관측으로 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 쿼리 `?ref=` → 폼 필드 → 가입 신청 | 공격자가 임의 문자열을 넣을 수 있다 |
| 폼 필드 → `GET /onboarding/referral/check` | 무인증 공개 조회다 |
| `referredByApodo` → `referral_credits` | **실제 금전 귀속**(월사용료 50% bonificación) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-45 | Tampering | `?ref=` 로 임의 apodo 주입 | mitigate | 프리필 값도 **기존 blur 검증을 거친다**(`checkReferralApodo`). 존재하지 않으면 `notfound` 가 화면에 뜬다. **최종 귀속 판정은 서버가 승인 시점에 한다** — 이 plan 은 그 경로를 바꾸지 않는다 |
| T-89-46 | Tampering | 해석 실패 시 아무 매장으로 폴백 | mitigate | `ref` 가 비거나 해석 안 되면 **빈 값**으로 둔다. 「좁히는 필터가 넓어지면 안 된다」 — 폴백 금지를 acceptance 가 C 케이스로 확인한다 |
| T-89-47 | Repudiation | 프리필이 사용자 입력을 덮어씀 | mitigate | `router.isReady` 에서 **1회만** 돌고 `shouldDirty: false`. 이미 입력이 있는 상태에서 덮어쓰면 사용자는 자기가 적은 것이 사라진 줄 모른다 |
| T-89-48 | Elevation of Privilege | 살아 있는 가입 흐름 손상 | mitigate | 변경은 **import 1 + `useRouter` 1 + `useEffect` 1** 뿐이다. OTP·CUIT·주소·`guestGuard` 를 건드리지 않는다. 운영 `pending_registrations` 9행이 도는 경로다 |
| T-89-49 | Repudiation | 근거 없는 보상 약속 | mitigate | `referral_credits` **0행** — 추천 가입이 승인까지 간 적이 없다. 「보상을 받습니다」를 **단정하지 않는다**. acceptance 가 신규 문구 0건을 센다 |
| T-89-50 | Denial of Service | `guestGuard` 로 직원이 도달 불가 | **accept (제약)** | 가입은 **새 매장**을 위한 화면이다. 로그인 상태에서 튕기는 것은 의도된 동작이므로 바꾸지 않는다. **어디로 튕기는지를 관측해 기록**하고 89-08 의 CTA 문구에 반영한다 |
</threat_model>

<verification>
```bash
cd ventago-app && npx tsc --noEmit && npx eslint src/views/register/components/RegisterForm.tsx
grep -c "useRouter" ventago-app/src/views/register/components/RegisterForm.tsx        # 2
grep -c "setValue('referredByApodo'" ventago-app/src/views/register/components/RegisterForm.tsx  # 1
curl -s -o /dev/null -w '%{http_code}\n' "http://localhost:5002/api/onboarding/referral/check?apodo=cool"   # 200
curl -sL -o /dev/null -w '%{http_code}\n' "http://localhost:3050/register?ref=cool"                          # 200
```
</verification>

<success_criteria>
- `/register?ref={apodo}` 가 추천인 칸을 **채우고 기존 검증을 태운다**
- 없는 apodo 는 `notfound` 로 **보인다**(조용히 통과하지 않는다)
- `?ref=` 가 없을 때 기존 가입 흐름이 **하나도 바뀌지 않았다**
- 해석 실패 시 **빈 값** — 아무 매장으로 폴백하지 않는다
- `guestGuard = true` 의 제약이 **관측으로 기록**됐다(바꾸지 않았다)
- 「보상을 받는다」를 단정하는 문구를 **추가하지 않았다**(`referral_credits` 0행)
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-12-SUMMARY.md` 를 만든다.
★ A~E 다섯 관측을 각각 한 줄씩. 「됐다」가 아니라 **무엇이 보였는지**.
★ E(로그인 시 튕기는 곳)를 **89-08 에 넘길 정보**로 표시한다.
★ 배포 순서: 이 변경은 app 단독이고 api 무변경이다. **어느 쪽이 먼저 나가도 안전하다** —
  `?ref=` 가 없으면 아무 일도 안 일어나고, `referral/check` 는 이미 운영에 있다.
</output>
