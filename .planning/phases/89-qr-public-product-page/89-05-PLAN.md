---
phase: 89-qr-public-product-page
plan: 05
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - api-ventago/src/app/leads/lead.model.ts
  - api-ventago/src/app/leads/dto/create-lead.dto.ts
  - api-ventago/src/app/leads/leads.service.ts
  - api-ventago/src/app/leads/leads-public.controller.ts
  - api-ventago/src/app/leads/leads.module.ts
  - api-ventago/src/app.module.ts
  - api-ventago/src/app/leads/leads-public.controller.spec.ts
autonomous: true
requirements: [REQ-05]
must_haves:
  truths:
    - "`POST /public/ventago-leads` 가 인증 없이 리드를 저장한다"
    - "저장에 성공하면 텔레그램 알림이 나간다 — 그리고 **알림이 실패해도 201 이다**"
    - "리드는 DB 에 남는다 — 텔레그램이 꺼져 있어도 유실되지 않는다"
    - "이 폼은 **주 도착지가 아니다** — 주 도착지는 `/register?ref=`(89-12)이고 이것은 이탈 받이다"
    - "알림의 결과가 `notify_status`/`notified_at` 에 남는다 — 실패가 흔적 없이 사라지지 않는다"
    - "`store_id` 의 신뢰 수준이 **낮음(클라이언트 제출값)** 으로 기록된다 — 확정 사실로 다루지 않는다"
    - "`sourceProductId` 가 **프론트 계약에도 존재한다** — 스키마에만 있고 폼에 없는 상태가 아니다"
  artifacts:
    - path: "api-ventago/src/app/leads/lead.model.ts"
      provides: "ventago_leads 모델 (모든 @Column 에 명시적 type)"
      contains: "tableName: 'ventago_leads'"
    - path: "api-ventago/src/app/leads/leads-public.controller.ts"
      provides: "@Public() + @Throttle 리드 수집 엔드포인트"
      contains: "public/ventago-leads"
    - path: "api-ventago/src/app.module.ts"
      provides: "LeadsModule 등록 — 없으면 라우트가 존재하지 않는다"
      contains: "LeadsModule"
  key_links:
    - from: "leads.service.ts"
      to: "ventago_leads.notify_status"
      via: "sendTelegramMessageDetailed 결과를 저장 뒤 기록"
      pattern: "notify_status|notifiedAt"
    - from: "leads.service.ts"
      to: "products.store_id"
      via: "sourceProductId 소유 매장 재검증"
      pattern: "store_id"
---

<objective>
CTA② 의 **이탈 받이**를 만든다 — 「연락처만 남기기」.

★★ **위상이 바뀌었다 (사용자 결정 ⑤-개정, 2026-09-16).**
  CTA② 「당신 매장에도 이 시스템을」의 **주 도착지는 이 리드 폼이 아니라
  이미 있는 매장 가입 화면 `/register?ref={apodo}`** 다(89-12).
  ⤷ 가입 경로는 **실제로 돌고 있다**(운영 `pending_registrations` 9행, 그중 3행이
    `referred_by_apodo` 보유). 귀속이 리드 테이블이 아니라 **실제 돈**으로 흐른다
    (`referral_credits` — 추천 매장 월사용료의 50% bonificación).

  **그럼 이 plan 은 왜 남는가:** `/register` 는 **CUIT · 주소 · 비밀번호 ·
  방문검증 동의 · 이메일/WhatsApp 이중 OTP** 를 요구한다. 매장 계산대 앞에서 폰으로
  끝까지 채우기엔 문턱이 높다. 그래서 **거기까지 가지 않는 사람을 위한 작은 받이**가 필요하다.
  ⤷ **가입이 주(主), 이 폼이 종(從)이다.**

Purpose: 이 저장소에는 「없는 페이지로 보낸」 막다른 CTA 전례가 있다. 이탈 받이라도
받는 곳이 없으면 수집은 사라진다 — 받는 곳을 먼저 만든다.

★ **어디로 가는가를 먼저 정했다:** 테이블이 **원장**이고 텔레그램은 **통지**다.
  저장 없이 알림만 보내면 「받는 사람이 그 순간 못 보면 영구 유실」이 된다.
  반대로 알림 실패가 저장 실패가 되면 **고객이 다시 보내야 한다** — 그래서 알림은
  커밋 뒤에 보내고 응답 코드를 바꾸지 않는다(CLAUDE.md 쓰기경로 규약).

★★ **그러나 fire-and-forget 만으로 끝내지 않는다 (CODEX P2 C-4).**
  `notifyTelegram()` 은 실패를 호출자에게 **알리지 않는다.** 그대로 두면
  「리드는 들어왔는데 아무도 못 봤다」가 **아무 흔적 없이** 일어난다 —
  이 저장소의 반복 결함이 정확히 **「감시 장치가 부재에서 침묵한다」**이고 같은 형태로
  세 번 조용히 죽었다. 그래서 `sendTelegramMessageDetailed()` 의 **네 가지 결과**를
  `notify_status`/`notified_at` 에 기록한다. 응답 코드는 그대로 두되, **조용해지지는 않게** 한다.

★★ **`store_id` 를 「서버가 검증한 값」이라 부르지 않는다 (CODEX P2 C-3).**
  QR 조회(GET)와 리드 제출(POST)은 **독립 요청**이고 둘 사이에 서명된 상태를 전달하는
  수단이 **없다.** 서버가 할 수 있는 것은 「그 매장이 실재·활성인가」와
  「`sourceProductId` 가 그 매장 것인가」뿐이다. 그 이상을 주장하면 거짓이다.
  ⤷ **신뢰 수준을 낮춰 기록한다** — 컬럼 주석(89-01)과 응답·문서가 「참고용 귀속」이라고
    말한다. 서명된 컨텍스트 도입은 범위를 넓히므로 하지 않는다(후속 후보).

Output: `POST /public/ventago-leads` + `ventago_leads` 모델 + 결과가 기록되는 통지.

---

## ★ 규모에 대한 판단 — **아무것도 줄이지 않았다.** 그 이유를 적는다

위상이 「주 도착지」에서 「이탈 받이」로 낮아졌으니 규모를 줄일 수 있는지 검토했고,
**줄이지 않기로 했다.** 지금 남은 것은 셋뿐이고 각각이 없으면 안 되는 이유가 있다:

| 구성요소 | 줄일 수 있나 | 판단 근거 |
|---|---|---|
| 테이블 + 모델 + 엔드포인트 | 아니오 | 이것이 **전부**다. 이보다 작으면 기능이 없다 |
| `notify_status`/`notified_at` | 아니오 | 이탈 받이일수록 **아무도 안 본다**. 알림 실패가 침묵하면 리드는 테이블에만 쌓인다 — 이 저장소의 반복 결함(「감시 장치는 부재에서 침묵한다」) |
| `sourceProductId` 소유 재검증 | 아니오 | 무인증 쓰기 경로다. 빼면 T-89-06(위조 귀속)이 열린다. 쿼리 한 번이다 |

⤷ **줄일 것이 없다.** 이 plan 은 처음부터 최소였고, 바뀐 것은 **위상과 문구**다.
  89-08 의 화면에서 이 폼은 **작은 보조 링크**로 내려간다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 2026-09-16 실측. -->

**89-01 이 만든 테이블(이미 양쪽 DB 에 적용됨):**
```sql
ventago_leads(
  id BIGSERIAL PK, store_id INT NOT NULL REFERENCES stores(id),
  source_product_id INT NULL REFERENCES products(id),
  contact_name VARCHAR(160) NOT NULL, contact_phone VARCHAR(40) NOT NULL,
  contact_email VARCHAR(160) NULL, message TEXT NULL,
  notify_status VARCHAR(16) NOT NULL DEFAULT 'pending',   -- ★ 알림 결과
  notified_at   TIMESTAMPTZ NULL,                          -- ★ 알림 시각
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now())
```

**★ 결과를 구분해 돌려주는 함수가 이미 있다** — `api-ventago/src/common/telegram/telegram.ts`:
```typescript
export async function sendTelegramMessageDetailed(
  text: string, options: TelegramOptions = {},
): Promise<'sent' | 'deduplicated' | 'unconfigured' | 'failed'>
```
★ `notifyTelegram()`(fire-and-forget 래퍼)은 이 값을 **버린다.** 이 plan 은
  `sendTelegramMessageDetailed()` 를 직접 쓰고 그 결과를 DB 에 기록한다.
  ★ 다만 **await 로 응답을 막지 않는다** — `.then()` 으로 받아 기록한다.
    (커밋 후 단계가 응답 코드를 바꾸면 고객이 재전송해 리드가 복제된다.)

**89-04 가 이미 정의한 상수(여기서 다시 만들지 말 것):**
`api-ventago/src/common/throttle/throttle.constants.ts` 의 `PUBLIC_LEAD_THROTTLE`
(ttl 60000 · limit `THROTTLE_PUBLIC_LEAD_LIMIT` 기본 5 · blockDuration 60000)

**모델 analog** — `api-ventago/src/app/onboarding/pending-registration.model.ts:1-15`:
```typescript
@Table({ tableName: 'pending_registrations', timestamps: false })
export class PendingRegistration extends Model {
  @Column({ type: DataType.BIGINT, primaryKey: true, autoIncrement: true })
  declare id: number;

  @Column({ type: DataType.STRING(255), allowNull: false })
  declare companyName: string;
}
```
★ **모든 `@Column` 에 명시적 `type` 이 있다.** 생략하면 유니온/옵셔널 타입에서
  `design:type` 이 Object 가 돼 **운영 부팅이 죽는다** — 빌드·테스트·CI 는 전부 통과한다.

**모듈 analog** — `api-ventago/src/app/print/print.module.ts`:
```typescript
@Module({
  imports: [SequelizeModule.forFeature([...])],
  controllers: [PrintController],
  providers: [PrintService],
  exports: [PrintService],
})
export class PrintModule {}
```

**app.module.ts** — 등록 위치: `OnboardingModule`(242행) · `PrintModule`(249행) ·
`ResellerModule`(337행) 과 같은 `imports` 배열. import 문도 상단에 더한다(75/82/154행 인근).
★ **이 등록이 없으면 라우트가 아예 존재하지 않는다** — 프론트 버튼은 404 를 받는다.

**텔레그램 유틸(재작성 금지)** — `api-ventago/src/common/telegram/telegram.ts:114-121`:
```typescript
export function notifyTelegram(text: string, options: TelegramOptions = {}): void {
  sendTelegramMessage(text, options).catch(() => { /* already logged */ });
}
```
실사용 형태 — `onboarding-alta.service.ts:218-226`:
```typescript
notifyTelegram(
  `⏳ <b>Aprobación pendiente</b>\n• Tienda: <b>${p.companyName}</b>\n...`,
  { dedupKey: `alta-aviso-${p.id}` },
);
```
★ env(`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`) 미설정이면 **조용히 skip 하되 로그는 남긴다**.
  그래서 알림 부재가 저장 실패가 되지 않는다.

**전역 검증 파이프** — `LoggingValidationPipe`: `whitelist: true`, `forbidNonWhitelisted: false`.
DTO 에 없는 필드는 **제거**되고 warn 이 남는다(400 이 아니다).
★ `@IsOptional()` 은 **null 을 통과시킨다** — 옵셔널 값은 서비스에서 한 번 더 정규화한다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 모델 + DTO + 서비스 (store_id 재검증 + 텔레그램)</name>
  <files>api-ventago/src/app/leads/lead.model.ts, api-ventago/src/app/leads/dto/create-lead.dto.ts, api-ventago/src/app/leads/leads.service.ts</files>
  <read_first>
    - `api-ventago/src/app/onboarding/pending-registration.model.ts` — 모델 형태(모든 컬럼에 명시적 type)
    - `api-ventago/src/common/telegram/telegram.ts` — `notifyTelegram` 시그니처와 dedup 동작
    - `api-ventago/src/app/onboarding/onboarding-alta.service.ts` 의 217-226행 — 알림 문구 포맷
    - `api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql` — 컬럼 이름·길이의 권위
  </read_first>
  <behavior>
    - `sourceProductId` 가 주어지고 그 상품이 `storeId` 소유면 → 그대로 저장
    - `sourceProductId` 가 **다른 매장** 상품이면 → `sourceProductId = null` 로 저장하고 `storeId` 는 상품의 실제 매장으로 **정정하지 않는다**(둘 다 클라이언트 제출값이라 어느 쪽이 참인지 모른다 — 덜 주장하는 쪽을 택한다)
    - `sourceProductId` 가 없으면 → `storeId` 실재·활성만 확인하고 저장
    - 저장 성공 후 통지가 **정확히 1회** 나간다
    - 통지 결과가 `notify_status`/`notified_at` 에 기록된다 — `'sent'` 뿐 아니라 `'failed'`·`'unconfigured'` 도
    - 통지가 던져도 서비스는 예외를 전파하지 않고 응답 코드도 바뀌지 않는다
  </behavior>
  <action>
**(1) `lead.model.ts`:**
```typescript
// [Phase 89] QR 공개 상품 페이지 CTA② 로 들어온 Ventago 잠재고객.
//
// ★ 이 테이블이 원장이다. 텔레그램은 통지일 뿐이라 실패해도 리드는 남는다.
@Table({ tableName: 'ventago_leads', timestamps: true })
export class Lead extends Model {
  @Column({ type: DataType.BIGINT, primaryKey: true, autoIncrement: true })
  declare id: number;

  @Column({ field: 'store_id', type: DataType.INTEGER, allowNull: false })
  declare storeId: number;

  @Column({ field: 'source_product_id', type: DataType.INTEGER, allowNull: true })
  declare sourceProductId: number | null;

  @Column({ field: 'contact_name', type: DataType.STRING(160), allowNull: false })
  declare contactName: string;

  @Column({ field: 'contact_phone', type: DataType.STRING(40), allowNull: false })
  declare contactPhone: string;

  @Column({ field: 'contact_email', type: DataType.STRING(160), allowNull: true })
  declare contactEmail: string | null;

  @Column({ type: DataType.TEXT, allowNull: true })
  declare message: string | null;

  // [Phase 89] 텔레그램 통지 결과. 'pending'|'sent'|'deduplicated'|'unconfigured'|'failed'
  // ★ 이 컬럼이 없으면 알림 실패가 **아무 흔적 없이** 사라진다.
  @Column({ field: 'notify_status', type: DataType.STRING(16), allowNull: false, defaultValue: 'pending' })
  declare notifyStatus: string;

  @Column({ field: 'notified_at', type: DataType.DATE, allowNull: true })
  declare notifiedAt: Date | null;
}
```
★ `underscored: true` 가 전역이라 `field:` 는 생략해도 되지만 **명시한다** — 이 저장소에서
  컬럼명 혼동으로 여러 번 죽었다. `type` 은 **절대 생략하지 않는다**(nullable 유니온은
  `design:type` 이 Object 다).

**(2) `dto/create-lead.dto.ts`:**
```typescript
export class CreateLeadDto {
  @IsInt() @Min(1) readonly storeId: number;

  @IsOptional() @IsInt() @Min(1) readonly sourceProductId?: number | null;

  @IsString() @IsNotEmpty() @MaxLength(160) readonly contactName: string;

  @IsString() @IsNotEmpty() @MaxLength(40) readonly contactPhone: string;

  @IsOptional() @IsString() @MaxLength(160) readonly contactEmail?: string | null;

  @IsOptional() @IsString() @MaxLength(500) readonly message?: string | null;
}
```

**(3) `leads.service.ts`:**
- 주입: `@InjectModel(Lead) private readonly leadRepo: typeof Lead`,
  `@InjectConnection() private readonly sequelize: Sequelize`.
- `async create(dto: CreateLeadDto): Promise<{ id: number }>`:

  a) **옵셔널 정규화** — `@IsOptional()` 은 null 을 통과시키므로 여기서 한 번 더 좁힌다:
  ```typescript
  const productId =
    Number.isInteger(Number(dto.sourceProductId)) && Number(dto.sourceProductId) > 0
      ? Number(dto.sourceProductId)
      : null;
  ```

  b) **출처 재검증 — 서버가 확인할 수 있는 만큼만** (CODEX P2 C-3):
  ```sql
  SELECT store_id FROM products WHERE id = $1 LIMIT 1
  ```
  상품의 `store_id` 가 `dto.storeId` 와 **다르면 `productId` 를 버린다**(null).
  ★ `storeId` 를 상품 쪽으로 「고쳐 주지」 않는다 — 클라이언트가 보낸 매장이 참이라는
    보장도, 상품이 참이라는 보장도 없다. 둘이 어긋나면 **덜 주장하는 쪽**을 택한다.
  ★★ 이 검사를 **「서버가 검증했다」고 부르지 않는다.** QR 조회와 리드 제출은 독립
    요청이고 상태 전달 수단이 없다 — 확인한 것은 「그 매장이 실재·활성인가」와
    「상품이 그 매장 것인가」뿐이다. **귀속은 참고값이다**(89-01 의 컬럼 주석과 같은 서술).

  c) **매장 존재 확인** — `SELECT 1 FROM stores WHERE id = $1 AND COALESCE(is_active,TRUE)=TRUE LIMIT 1`.
     없으면 `throw new BadRequestException('Tienda no encontrada')`.
     ★ FK 만 믿으면 500 이 난다. 400 으로 말해 준다.

  d) `Lead.create({...})` — 필드를 **하나씩 나열**한다(dto 스프레드 금지).

  e) 저장 **성공 후** 통지 — **결과를 버리지 않는다** (CODEX P2 C-4):
  ```typescript
  // ★ HTML parse_mode 본문에 사용자 문자열이 들어간다 → 반드시 이스케이프한다.
  //   이 서비스 안에 작은 헬퍼를 두고 쓴다(외부 의존 추가 금지).
  const esc = (v: string) => v.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');

  const texto =
    `🧲 <b>Ventago 리드 — QR 에서 유입</b>\n` +
    `• 매장(QR 출처, <i>클라이언트 제출값</i>): <b>${esc(storeName)}</b> (id ${lead.storeId})\n` +
    `• 연락처: ${esc(lead.contactName)} · ${esc(lead.contactPhone)}\n` +
    (lead.contactEmail ? `• Email: ${esc(lead.contactEmail)}\n` : '') +
    (productId ? `• 상품: ${productId}\n` : '') +
    (lead.message ? `• 메시지: ${esc(lead.message)}\n` : '');

  // ★ await 하지 않는다 — 커밋 뒤 단계가 응답을 막거나 바꾸면 안 된다.
  //   그러나 **결과는 기록한다.** notifyTelegram() 은 결과를 버리므로 쓰지 않는다.
  void sendTelegramMessageDetailed(texto, { dedupKey: `ventago-lead-${lead.id}` })
    .then((estado) =>
      this.leadRepo.update(
        { notifyStatus: estado, notifiedAt: new Date() },
        { where: { id: lead.id } },
      ),
    )
    .catch(() =>
      this.leadRepo
        .update({ notifyStatus: 'failed', notifiedAt: new Date() }, { where: { id: lead.id } })
        .catch(() => undefined),
    );
  ```
  ★ **커밋 뒤 실패는 응답을 바꾸지 않는다** — 여기서 throw 하면 고객이 재전송해 리드가 복제된다.
  ★ 그러나 **조용하지도 않다** — `notify_status` 가 `'pending'` 으로 남아 있는 행이
    「보내려 했으나 결과를 못 받은」 리드다.

  ★★ **이 phase 에는 리드 조회 화면이 없다.** 그러므로 「받는 곳」은 텔레그램 채널
    하나뿐이고, 그것이 실패하면 **DB 를 직접 봐야 안다.** 그 사실을 SUMMARY 와
    89-09 UAT 에 적고, 리드 목록 화면은 **후속 후보**로 남긴다.
    진단 쿼리(운영자에게 알려 줄 것):
    ```sql
    SELECT id, created_at, notify_status, contact_name FROM ventago_leads
     WHERE notify_status <> 'sent' ORDER BY id DESC;
    ```

  f) `return { id: Number(lead.id) }` — 리드 내용을 되돌려주지 않는다(에코 불필요).
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type: DataType" api-ventago/src/app/leads/lead.model.ts` 가 **9** 이다(컬럼 9개 전부 명시)
    - `grep -c "@Column({ *})" api-ventago/src/app/leads/lead.model.ts` 가 0 이다(빈 `@Column()` 금지)
    - `leads.service.ts` 에 `sendTelegramMessageDetailed(` 가 정확히 1회 있고 그 앞에 `await` 가 **없다**
    - `leads.service.ts` 에 `notifyStatus` 를 쓰는 `update(` 가 2회 있다(성공 경로 + catch 경로)
    - `grep -c "notifyTelegram(" api-ventago/src/app/leads/leads.service.ts` 가 **0** 이다(결과를 버리는 래퍼를 쓰지 않는다)
    - `leads.service.ts` 에 HTML 이스케이프 헬퍼(`&amp;` · `&lt;` · `&gt;`)가 있다
    - `leads.service.ts` 에 `dedupKey: \`ventago-lead-` 가 있다
    - `grep -cE "\.\.\.dto" api-ventago/src/app/leads/leads.service.ts` 가 **0** 이다
    - `leads.service.ts` 에 `SELECT store_id FROM products WHERE id = $1` 가 있다
    - `npx tsc --noEmit` 종료코드 0
  </acceptance_criteria>
  <done>모델·DTO·서비스가 있고 타입 체크 통과. 저장이 원장이고 알림이 통지다.</done>
</task>

<task type="auto">
  <name>Task 2: 공개 컨트롤러 + 모듈 + app.module 등록</name>
  <files>api-ventago/src/app/leads/leads-public.controller.ts, api-ventago/src/app/leads/leads.module.ts, api-ventago/src/app.module.ts</files>
  <read_first>
    - `api-ventago/src/app/online-orders/public-delivery.controller.ts` — `@Public()+@Throttle` 골격
    - `api-ventago/src/app/print/print.module.ts` — 모듈 형태
    - `api-ventago/src/app.module.ts` 의 75·82·154행과 242·249·337행 — import 와 imports 배열의 관례
  </read_first>
  <action>
**(1) `leads-public.controller.ts`:**
```typescript
// [Phase 89] CTA② Ventago 잠재고객 수집 — 인증 없는 공개 제출.
//
// ★ 받는 곳이 없으면 수집은 사라진다. 이 라우트가 «막다른 CTA» 를 막는 도착지다.
@Controller('public/ventago-leads')
export class LeadsPublicController {
  constructor(private readonly service: LeadsService) {}

  @Public()
  @Throttle(PUBLIC_LEAD_THROTTLE)
  @Post()
  async create(@Body() dto: CreateLeadDto): Promise<{ id: number }> {
    return this.service.create(dto);
  }
}
```

**(2) `leads.module.ts`:**
```typescript
@Module({
  imports: [SequelizeModule.forFeature([Lead])],
  controllers: [LeadsPublicController],
  providers: [LeadsService],
  exports: [LeadsService],
})
export class LeadsModule {}
```

**(3) `app.module.ts`:**
- 상단에 `import { LeadsModule } from './app/leads/leads.module';`
- `imports` 배열에 `LeadsModule,` (기존 `PrintModule,` 근처에 둔다)

★ **이 등록을 빠뜨리면 컨트롤러는 컴파일되는데 라우트가 없다** — 프론트가 404 를 받고,
  「버튼은 있는데 눌러도 아무 일 없음」이 된다. Task 3 이 이것을 시험으로 잡는다.
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public/ventago-leads" api-ventago/src/app/leads/leads-public.controller.ts` 가 1 이다
    - 컨트롤러에 `@Public()` 과 `@Throttle(PUBLIC_LEAD_THROTTLE)` 이 둘 다 있다
    - `grep -c "LeadsModule" api-ventago/src/app.module.ts` 가 **2** 이다(import + imports 배열)
    - `npx tsc --noEmit` 종료코드 0
  </acceptance_criteria>
  <done>라우트가 Nest 애플리케이션에 실제로 등록됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 리드 저장·알림·출처검증 시험 (대조군 포함)</name>
  <files>api-ventago/src/app/leads/leads-public.controller.spec.ts</files>
  <read_first>
    - 방금 만든 `api-ventago/src/app/leads/leads.service.ts` — 실제 호출 순서
    - `api-ventago/src/app/print/print-product-ownership.spec.ts` — spec 관례
    - `.planning/phases/89-qr-public-product-page/89-VALIDATION.md` — 「시험이 검증 대상을 부르는지 확인할 것」
  </read_first>
  <behavior>
    - Test 1: 정상 제출 → `Lead.create` 가 호출되고 통지가 **1회** 나간다
    - Test 2 (대조군): 통지가 던져도 `create` 가 예외를 전파하지 않는다
    - Test 3: `sourceProductId` 가 다른 매장 상품 → 저장 인자의 `sourceProductId` 가 `null` 이다
    - Test 4: 없는/비활성 매장 → `BadRequestException`, `Lead.create` **미호출**
    - Test 5: `sourceProductId: null` 이 와도(=`@IsOptional()` 이 통과시킨다) 500 이 나지 않고 null 로 저장된다
    - Test 6: 라우트 메타데이터 — `LeadsPublicController` 의 `@Controller` 경로가 `'public/ventago-leads'` 이고 `create` 가 POST 다
    - **Test 7 (C-4)**: 통지가 `'sent'` 를 돌려주면 `notifyStatus='sent'` 와 `notifiedAt` 으로 **update 가 호출된다**
    - **Test 8 (C-4)**: 통지가 `'failed'` 를 돌려줘도 **응답은 `{ id }` 그대로**이고 `notifyStatus='failed'` 로 기록된다
    - **Test 9 (C-4 대조군)**: 결과 기록 코드를 지우면 Test 7·8 이 빨개진다
    - **Test 10 (D-2)**: `Lead` 모델의 모든 컬럼에 명시적 `type` 이 있다 — `Lead.rawAttributes` 의 각 항목에 `type` 이 정의돼 있고 어느 것도 `Object` 로 축약되지 않았다
  </behavior>
  <action>
`Object.create(LeadsService.prototype)` 로 서비스를 만들고 `leadRepo`/`sequelize` 를 가짜로 심는다.
`notifyTelegram` 은 `jest.mock('src/common/telegram/telegram')` 로 가로챈다.

Test 6 은 Nest 의 `PATH_METADATA`/`METHOD_METADATA` 를 읽는다:
```typescript
import { PATH_METADATA, METHOD_METADATA } from '@nestjs/common/constants';

expect(Reflect.getMetadata(PATH_METADATA, LeadsPublicController)).toBe('public/ventago-leads');
```
★ 이 시험의 목적은 **막다른 CTA 방지**다 — 프론트가 이 경로로 보낸다. 89-08 의
  `check-cta-destinos.sh` 가 프론트 쪽 문자열과 이 값을 맞춰 본다.

★ **대조군 실증 2건**(각각 확인하고 원복, SUMMARY 에 한 건씩 적는다):
  1. 통지 호출 줄을 지우면 Test 1 이 빨개진다
  2. `notifyStatus` 기록 `.then()` 을 지우면 Test 7·8 이 빨개진다
★ **정답을 구현에서 가져오지 않는다** — 기대 경로 `'public/ventago-leads'` 는
  spec 안에 리터럴로 적는다(컨트롤러에서 export 한 상수를 import 하지 않는다).

**★ Test 10 — `@Column type` 누락은 이 저장소에서 운영 부팅을 죽인 실제 결함이다 (D-2).**
  2026-08-19 에 `sales` 모델의 `branchId: number | null` 이 `type` 없이 추가돼
  **전 엔드포인트 502 + PM2 무한 재시작**이 났다. `tsc --noEmit` · `nest build` ·
  jest 107건 · Jenkins 가 **전부 통과했다** — 데코레이터는 런타임 코드이고
  유니온 타입은 `design:type` 이 `Object` 로 축약되기 때문이다.

  ★ **이미 강제 지점이 있다**: `api-ventago/scripts/verify-models.js` 가 **빌드 산출물의
    모든 모델을 실제로 로딩**해 본다. `api-ventago/Dockerfile:23` 에서 돌아
    **실패하면 이미지가 아예 안 만들어진다.** 새 검사를 만들지 말고 **그것을 한 번 돌린다**:
    ```bash
    cd api-ventago && npx nest build && node scripts/verify-models.js
    ```
  ★ Test 10 은 그 위에 얹는 **빠른 국소 검사**다(`Lead.rawAttributes`).
    `rawAttributes` 가 비어 있으면(모델이 Sequelize 에 등록되기 전) **Test 10 을 넣지 말고
    그 사실을 spec 주석에 적는다** — 늘 통과하는 시험은 시험이 아니다.
    그 경우 `verify-models.js` 실행만으로 D-2 를 충족한 것으로 본다.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/leads --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/app/leads --maxWorkers=1` 출력에 **`leads-public.controller` suite 이름이 실제로 찍히고** 종료코드 0
      (「N passed」만 보지 말 것 — 컴파일이 깨진 suite 는 조용히 빠진다)
    - spec 에 `it(` 이 **10개**(또는 Test 10 을 뺀 사유가 spec 주석에 있으면 9개) 있다
    - spec 에 통지 호출 횟수를 단언하는 줄이 있다(`toHaveBeenCalledTimes(1)`)
    - spec 에 `notifyStatus` 기록 `update` 를 단언하는 줄이 **2개**(sent · failed) 있다
    - spec 에 `PATH_METADATA` 를 읽는 단언이 있고 기대값이 리터럴 `'public/ventago-leads'` 다
    - `cd api-ventago && npx nest build && node scripts/verify-models.js` 종료코드 **0**
      (그 출력에 `Lead` 또는 `ventago_leads` 가 로딩됐다는 흔적이 있어야 한다 — 0개 로딩은 미실행이다)
    - SUMMARY 에 **대조군 2건**이 각각 「무엇을 지웠더니 어느 시험이 빨개졌다」로 적혀 있다
  </acceptance_criteria>
  <done>저장·알림·출처검증이 전부 시험되고, 방어를 지우면 빨개진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷(비인증) → `POST /public/ventago-leads` | 임의 storeId·productId·문자열이 도달한다 |
| `leads.service.ts` → 운영자 텔레그램 채널 | 사용자 입력이 알림 본문에 실린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-06 | Tampering | `ventago_leads.store_id` / `source_product_id` | mitigate | `sourceProductId` 의 소유 매장을 `products.store_id` 로 재조회해 `dto.storeId` 와 다르면 **productId 를 버린다**. `storeId` 는 `stores` 존재·활성 확인 + FK. 「판정의 근거는 저장의 근거와 같아야」 |
| T-89-02 | Denial of Service | 공개 폼 봇 제출 | mitigate | `@Throttle(PUBLIC_LEAD_THROTTLE)` 분당 5 / block 60s |
| T-89-18 | Tampering | 텔레그램 HTML 파싱 주입 | mitigate | `contactName`/`message` 는 `@MaxLength` 로 길이를 제한한다. `parse_mode: HTML` 본문에 사용자 문자열이 들어가므로 `<`·`>`·`&` 를 **이스케이프**한 뒤 넣는다(문구 조립 시 `escapeHtml()` 헬퍼를 이 서비스에 두고 쓴다) |
| T-89-19 | Denial of Service | 알림 실패가 저장 실패로 전파 | mitigate | `notifyTelegram` 은 `await` 하지 않고 커밋 뒤 호출한다. 여기서 throw 하면 고객이 재전송해 리드가 복제된다 |
| T-89-20 | Information Disclosure | 응답 에코 | mitigate | 응답은 `{ id }` 뿐이다. 저장한 연락처를 되돌려주지 않는다 |
</threat_model>

<verification>
```bash
cd api-ventago && npx tsc --noEmit && npx jest src/app/leads --maxWorkers=1
cd api-ventago && npx nest build && node scripts/verify-models.js   # ★ @Column type 누락 검사(D-2)
grep -c "LeadsModule" api-ventago/src/app.module.ts        # 2
grep -c "type: DataType" api-ventago/src/app/leads/lead.model.ts   # 9
# 로컬 API 가 떠 있으면 — 404 가 아니라 400(검증 실패)이 나와야 한다
curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:5002/api/public/ventago-leads \
  -H 'Content-Type: application/json' -d '{}'
```
</verification>

<success_criteria>
- `POST /public/ventago-leads` 가 Nest 에 등록됐고 빈 body 에 **404 가 아니라 400** 을 낸다
- 리드가 DB 에 남고, 텔레그램이 꺼져 있어도 저장은 성공한다
- **통지 결과가 `notify_status`/`notified_at` 에 남는다** — 실패가 조용해지지 않는다
- `source_product_id` 는 그 매장 것일 때만 남고, `store_id` 는 **참고값**으로 기록된다(확정 사실이라 부르지 않는다)
- 모든 `@Column` 에 명시적 `type` 이 있고 **`verify-models.js` 가 실제로 통과**한다(운영 부팅이 죽지 않는다)
- 10개 시험이 통과하고 대조군 2건이 실제로 반응한다
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-05-SUMMARY.md` 를 만든다.
★ 배포 순서: 이 API 를 **프론트 폼(89-08)보다 먼저** 배포한다. 반대면 버튼이 404 를 받는다.
★ **이 phase 가 닫지 않은 것**을 SUMMARY 에 적는다:
  - **리드 조회 화면이 없다.** 받는 곳은 텔레그램 채널 하나뿐이고, 그것이 실패하면
    `SELECT ... FROM ventago_leads WHERE notify_status <> 'sent'` 로 DB 를 직접 봐야 안다.
    ROADMAP W5 가 경고한 「받는 곳이 없으면 수집은 사라진다」의 절반만 닫힌 상태다 — 후속 후보.
  - **`store_id` 귀속의 신뢰 수준이 낮다.** 서명된 컨텍스트는 도입하지 않았다(범위) — 후속 후보.
</output>
