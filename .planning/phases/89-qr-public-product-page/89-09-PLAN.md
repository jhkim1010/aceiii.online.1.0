---
phase: 89-qr-public-product-page
plan: 09
type: execute
wave: 5
depends_on: [02, 03, 05, 06, 07, 08, 10, 11, 12]
files_modified:
  - .planning/phases/89-qr-public-product-page/89-UAT.md
autonomous: false
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, REQ-05, REQ-06]
must_haves:
  truths:
    - "매장에 붙어 있는 **실물 라벨을 폰으로 찍어** 페이지가 열린다 — 계정 없이"
    - "그 페이지가 매장 · 지점 · 상품 사진 · 가격을 보여주고, 가격이 **라벨과 같다**"
    - "두 CTA 를 **끝까지 눌러** 각각 실제 화면에 도달한다 — 막다른 곳이 없다"
    - "운영 배포는 사용자 승인을 받고 했고, Jenkins 빌드 성공 + 컨테이너 재생성을 확인했다"
    - "★ **스키마 게이트가 통과하지 못하면 배포하지 않았다** — wave 순서가 아니라 검사가 막는다"
  artifacts:
    - path: ".planning/phases/89-qr-public-product-page/89-UAT.md"
      provides: "성공 판정 3건 + 실물 관측 기록"
      contains: "성공 판정"
  key_links:
    - from: "운영 app.coolsistema.com/m/stock"
      to: "newapi.coolsistema.com/api/public/qr-stock"
      via: "브라우저 raw fetch"
      pattern: "실물 스캔으로 확인"
---

<objective>
**실물로 확인한다.** 이 phase 의 성공 판정 3개는 코드로 증명할 수 없다.

Purpose: 이 저장소에는 「빌드 통과는 배선 검증이 아니다」와 「실행된 적 없는 코드는
검증된 적 없는 코드」가 기록돼 있다. 그리고 **인쇄물·카메라·실제 도메인**이 얽힌
이 기능은 사람이 한 번 찍어 봐야만 끝난다.

Output: `89-UAT.md` — 성공 판정 3건의 관측 기록.

★ **push 는 사용자 승인 후에만.** `git push origin main` 이 곧 Jenkins 빌드이고
  빌드가 곧 운영 배포다. 이 plan 의 첫 task 가 그 승인 지점이다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-VALIDATION.md
</context>

<interfaces>
<!-- 실물 확인에 필요한 좌표. 2026-09-16 운영 실측. -->

**지금 매장에 붙어 있는 라벨(운영 `qr_print_log` 5행):** 전부 **store 6**, 지점 **6**,
최근 인쇄 **2026-09-08**. store 6 은 slug `cool` 이 있어 공개몰이 켜져 있다.
```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT l.branch_id, l.product_id, l.price_type_id, l.printed_price, l.printed_name, l.printed_at
      FROM qr_print_log l ORDER BY l.printed_at DESC\""
```

**현재 상태(고치기 전):**
`https://app.coolsistema.com/m/stock?s=6&p=1` → **308** → `/m/stock/?s=6&p=1` → **404**

**3갈래 판정표 (D-04):**

| `qr_precio_publico` | `stores.slug` | 기대 화면 |
|---|---|---|
| 켜짐 | 무관 | 상품 상세 (매장·지점·사진·가격) |
| 꺼짐 | 있음 | 공개몰로 가는 버튼 |
| 꺼짐 | 없음 | 닫힘 (버튼 없음) |

★ **배포 직후 기본 상태는 「꺼짐」이다**(D-04). store 6 은 slug 가 있으므로
  **아무 설정도 안 하면 「공개몰로 가는 버튼」**이 뜬다 — 그것이 의도된 배포 안전값이다.
  상품 상세를 보려면 사용자가 Configuración › QR del producto 에서 **켜야 한다.**

**Jenkins:** `api-coolsistema`(백엔드) · `front-coolsistema`(프론트).
빌드 확인은 **커밋 SHA 로** 한다(번호를 추측하지 않는다).
로그: `/var/lib/jenkins/jobs/<job>/builds/<n>/log`

**★★ 배포 게이트 — wave 순서는 게이트가 아니다 (CODEX P1 B-7)**

모델에 `qr_precio_publico` 를 가진 코드가 컬럼 추가 **전에 한 워커라도** 올라오면
Sequelize 기본 조회가 없는 컬럼을 SELECT 해 **`store_configs` 조회가 전부 500** 이 된다.
`prices` 쪽은 더 나쁘다 — `ON CONFLICT` 가 인덱스 없이 돌면
`no unique or exclusion constraint matching` 으로 **모든 가격 저장이 500** 이다.

그래서 **검사가 막는다**:

```
① DB expand 적용 (89-01 · 89-10) — 로컬 5432 + 운영 5434 양쪽
② 스키마 게이트 통과:
     PGTARGET=prod bash api-ventago/scripts/verificar-esquema-phase89.sh   → 종료코드 0
③ 구버전 코드가 새 스키마에서 **여전히 정상**인지 확인 (지금 도는 운영이 곧 구버전이다)
④ ③ 까지 통과한 **뒤에만** 애플리케이션 배포
```

★ **② 또는 ③ 이 실패하면 배포하지 않는다.** 승인을 다시 구하지도 않는다 —
  먼저 스키마를 맞춘다.

**그 다음의 순서(짝을 이루는 변경):**
1. **api 먼저** (89-03 T1·T2 · 89-04 · 89-05 · 89-07 · 89-08 T1 · 89-10 T2)
   — 이 시점에 프론트는 아직 새 화면이 없으므로 **아무도 안 부른다**. 안전하다.
2. **app 다음** (89-03 T3 · 89-06 · 89-08 T2)
   — 반대 순서면 토글이 400 을, 페이지·폼이 404 를 받는다.

★ 89-10 안에서도 순서가 있다: **인덱스(T1·T3) → 코드(T2)**. 반대는 전면 500 이다.
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 운영 배포 승인 (push = Jenkins 빌드 = 배포)</name>
  <files>(배포 — 소스 변경 없음)</files>
  <read_first>
    - `./CLAUDE.md` § 「상시 규칙: push 는 사용자 승인 후」
    - 89-01 ~ 89-08 의 SUMMARY 전부 — 무엇이 커밋돼 있는지
    - `.team/reviews/auto-*.md` 중 이번 커밋들에 대한 CODEX 보고서 **전부**
  </read_first>
  <decision>이번 phase 의 커밋들을 `origin main` 에 push 해 운영에 배포해도 되는가</decision>
  <context>
승인을 구하기 **전에** 다음을 준비해 사용자에게 제시한다.

**(a) CODEX 자동 자문 보고서를 읽는다.** 커밋 후 백그라운드로 뜬 `.team/reviews/auto-*.md`
를 **전부 읽고** 지적 사항을 요약한다.
★ **읽는 사람이 없으면 검토는 없는 것이다.**
★ codex 검토가 **서브모듈 수정 파일을 건너뛰는** 전례가 있다 — `api-ventago`/`ventago-app`
  변경이 보고서에 실제로 등장하는지 확인하고, 없으면 diff 를 직접 넘겨 다시 받는다.
★ 지적이 1건 오면 **그 형태를 전수로 센다**(같은 실수가 다른 자리에 더 있다).

**(b) ★★ 스키마 게이트 — 이것이 실패하면 배포하지 않는다 (B-7):**
```bash
# ① 운영 스키마가 준비됐는가. 8개 항목을 [OK]/[FALTA] 로 판정한다.
#    ★ 「파일이 있다」는 적용의 증거가 아니다. DB 에서 직접 본다.
PGTARGET=prod bash api-ventago/scripts/verificar-esquema-phase89.sh; echo "exit=$?"
```
→ **종료코드가 0 이 아니면 여기서 멈춘다.** `[FALTA]` 항목을 먼저 적용한다.

```bash
# ② 구버전 코드(= 지금 운영에 도는 것)가 새 스키마에서 여전히 정상인가
#    - store_configs 조회가 되는가 (컬럼 추가는 additive 여야 한다)
curl -s -o /dev/null -w 'store-config %{http_code}\n' https://newapi.coolsistema.com/api/health 2>/dev/null || true
ssh jhkim-server "docker logs --since 10m api_ventago 2>&1 | grep -ci 'qr_precio_publico\|does not exist'"
#    → 0 이어야 한다. 0 이 아니면 **이미 깨져 있다** — 배포가 아니라 롤백 판단이다.
```

**(c) 로컬 게이트:**
```bash
cd api-ventago && npx tsc --noEmit
cd api-ventago && npx jest src/app/print src/app/leads src/app/store/config src/app/reseller src/app/products src/common/migrations --maxWorkers=1
cd ventago-app && npx tsc --noEmit
bash scripts/check-cta-destinos.sh
PGTARGET=local bash api-ventago/scripts/verificar-qr-public-tenant.sh
PGTARGET=prod  bash api-ventago/scripts/verificar-qr-public-tenant.sh   # 읽기 전용
cd api-ventago && npx nest build && node scripts/verify-models.js       # @Column type 누락(운영 부팅 사망) 검사
```
★ **출력이 비면 「통과」가 아니라 「안 돈 것」이다** — `NODE_OPTIONS` 가 설정돼 있으면
  jest 가 조용히 안 돈다. suite 이름이 실제로 찍히는지 본다.

**(d) 무엇이 바뀌는가 (사용자에게 그대로 보고):**
- 운영 14개 매장 전부 `qr_precio_publico=false` → **켜기 전까지 동작 변화 없음**
- `/m/stock` 이 404 에서 **200** 이 된다. store 6 은 기본값 기준 「공개몰로 가는 버튼」
- 새 공개 엔드포인트 2개(`/public/qr-stock/*`, `/public/ventago-leads`)가 인터넷에 열린다
- `POST /reseller/auth/register` 가 **처음으로 도달 가능**해진다(검증 추가됨)
- 새 인쇄분 QR 에 `&b=`·`&pt=` 가 들어간다 — **기존 라벨은 영향 없음**
  ★ 이것만은 **실물 인쇄 스캔으로만** 확인된다(Task 2)
- `prices` 에 UNIQUE 가 생기고 가격 업서트가 `ON CONFLICT` 가 된다(89-10)
  ★ 경합 시 종전의 **조용한 중복**이 **제약 오류**로 바뀐다 — 의도된 변화다
- ★ **막지 못하는 것 두 가지를 그대로 보고한다**:
  · 숫자 ID 열거 — 공개몰 OFF 매장만 「인쇄 기록 존재」로 범위를 줄였을 뿐이다
  · 구 라벨의 라벨 특정 — `b`·`pt` 가 없어 「최신 인쇄분 기준」 추정이다(화면이 그렇게 말한다)
  </context>
  <options>
    <option id="push">
      <name>push 한다 (api → app 순서로)</name>
      <pros>죽어 있던 라벨이 살아난다. 이 phase 의 첫 가치가 운영에 닿는다</pros>
      <cons>운영 배포다. 공개 엔드포인트 2개가 인터넷에 새로 열린다</cons>
    </option>
    <option id="api-only">
      <name>api 만 먼저 push 하고 관찰</name>
      <pros>프론트 화면이 없으므로 새 엔드포인트를 부르는 사람이 없다 — 가장 보수적</pros>
      <cons>`/m/stock` 은 여전히 404 다(라벨은 계속 죽어 있다)</cons>
    </option>
    <option id="hold">
      <name>보류 — 지적 사항부터 처리</name>
      <pros>CODEX 지적을 먼저 닫는다</pros>
      <cons>—</cons>
    </option>
  </options>
  <action>
승인을 받으면 순서대로 실행한다:

1. `git push origin main`(선별 스테이징된 커밋만 — **`git add -A` 금지**, 공용 워킹트리다)
2. **빌드 결과를 반드시 확인한다** — 번호를 추측하지 말고 **커밋 SHA 로** 내 빌드를 찾는다:
   ```bash
   ssh jhkim-server "grep -rl '<커밋SHA>' /var/lib/jenkins/jobs/api-coolsistema/builds/*/log | tail -1"
   ```
   FAILURE 면 로그를 읽고 고쳐서 다시 승인을 구한다.
3. 컨테이너 재생성 확인(`docker ps` 의 기동 시각).
4. 운영 스모크:
   ```bash
   curl -s -o /dev/null -w '%{http_code}\n' -L "https://app.coolsistema.com/m/stock?s=6&p=1"
   curl -s "https://newapi.coolsistema.com/api/public/qr-stock/6/1" | head -c 300
   curl -s -o /dev/null -w '%{http_code}\n' -X POST "https://newapi.coolsistema.com/api/public/ventago-leads" \
     -H 'Content-Type: application/json' -d '{}'      # 404 가 아니어야 한다
   ```
「push 완료」는 **빌드 성공 + 컨테이너 재생성 확인**까지를 뜻한다.
  </action>
  <resume-signal>`push` / `api-only` / `hold` 중 하나</resume-signal>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' -L "https://app.coolsistema.com/m/stock?s=6&p=1"</automated>
  </verify>
  <acceptance_criteria>
    - CODEX 보고서를 읽은 요약이 사용자에게 제시됐고, 그 보고서가 **`api-ventago`/`ventago-app` 파일을 실제로 언급**한다
    - ★ `PGTARGET=prod bash api-ventago/scripts/verificar-esquema-phase89.sh` 종료코드가 **0** 이고
      마지막 줄이 `배포 가능: 예` 다. **그 출력 전문이 SUMMARY 에 있다**
    - ★ 그 게이트가 **실패했을 때 배포를 멈춘다는 것**이 SUMMARY 에 적혀 있다(실패 시 실제로 멈췄다면 그 기록)
    - 운영 로그에 `does not exist` 가 **0건**이다(구버전 코드가 새 스키마에서 정상)
    - `cd api-ventago && npx nest build && node scripts/verify-models.js` 종료코드 0
    - Jenkins 빌드를 **커밋 SHA 로** 찾아 SUCCESS 를 확인했다
    - `curl -sL https://app.coolsistema.com/m/stock?s=6&p=1` 최종 상태코드가 **200** 이다(종전 404)
    - `POST https://newapi.coolsistema.com/api/public/ventago-leads` 가 **404 가 아니다**
  </acceptance_criteria>
  <done>운영에 배포됐고 빌드 성공 + 컨테이너 재생성 + 스모크 3건을 확인했다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실물 라벨을 폰으로 찍는다 — 성공 판정 1·2</name>
  <files>(실물 확인 — 소스 변경 없음. 실패하면 해당 plan 으로 되돌아간다)</files>
  <read_first>
    - `.planning/phases/89-qr-public-product-page/89-CONTEXT.md` § 「성공 판정」 3건
    - `.planning/phases/89-qr-public-product-page/89-06-SUMMARY.md` — 네 갈래 화면이 로컬에서 어떻게 보였는지
    - `.planning/phases/89-qr-public-product-page/89-07-SUMMARY.md` — `&b=` 가 미해결로 남긴 것(실물 스캔)
    - 위 `<interfaces>` 의 3갈래 판정표 — **배포 직후 기본은 「꺼짐」**이다
  </read_first>
  <action>
확인을 **요청하기 전에** Claude 가 준비한다:

1. 운영에서 실제 라벨의 좌표를 뽑아 사용자에게 알려 준다(어느 상품·어느 가격인지):
```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT l.branch_id, l.product_id, l.price_type_id, l.printed_price, l.printed_name, l.printed_at
       FROM qr_print_log l ORDER BY l.printed_at DESC\""
```
2. 그 상품의 URL 을 그대로 만들어 제시한다: `https://app.coolsistema.com/m/stock?s=6&p=<product_id>`
3. 기대 화면을 **미리** 말해 준다(설정 꺼짐 + slug `cool` → 「공개몰로 가는 버튼」).
   그래야 사용자가 「이상한데?」를 판별할 수 있다.
4. 아래 `<how-to-verify>` 를 그대로 전달하고 기다린다.

사용자 답을 받으면:
- 안 됐다고 하면 **어느 단계에서 무엇이 보였는지**를 받아 해당 plan(89-04/06/07)으로 돌아간다.
- `&b=` 라벨이 안 읽힌다면 89-07 의 `buildQrUrl` 에서 `&b=` 만 되돌린다
  (기존 라벨은 영향이 없다 — 되돌리기가 싸다).
  </action>
  <what-built>
QR 라벨의 도착지 공개 페이지(`/m/stock`)와 공개 조회 API, 설정 스위치,
새 인쇄분의 `&b=` 지점 정보.
자동 확인은 전부 통과했다 — **남은 것은 종이·카메라·실제 도메인이 얽힌 부분**뿐이다.
  </what-built>
  <how-to-verify>
**A. 이미 붙어 있는 라벨 (하위호환 — `b` 없음)**
1. 매장(store 6, 지점 6)에 붙어 있는 라벨을 **폰 카메라**로 찍는다.
   (운영 `qr_print_log` 5건이 그 라벨들이다.)
2. **로그인하지 않은 상태**로 페이지가 열리는가? → 404 나 로그인 화면이 뜨면 실패.
3. 기본 상태(설정 꺼짐 + slug `cool`)이므로 **「공개몰로 가는 버튼」**이 보여야 한다.
   버튼을 눌러 `https://cool.coolsistema.com` 이 **실제로 열리는지** 확인한다.

**B. 설정을 켜고 상품 상세 (성공 판정 2 — 가격 일치)**
4. 웹에서 store 6 admin 으로 로그인 → **Configuración › Operación › QR del producto** 탭을
   찾아 토글을 **켠다**. (탭이 안 보이면 그 자체가 실패다 — 도달 불가.)
5. 같은 라벨을 다시 찍는다 → 이제 **상품 상세**가 떠야 한다.
6. ★ **가격을 세 가지로 나눠 본다** (사용자 결정 ①, 2026-09-16 **개정본**):
   - 6a. 화면의 **큰 가격**이 그 가격유형의 **현재 가격**인가? (주 표시)
   - 6b. 종이 라벨의 숫자와 **같다면** → 화면에 **아무것도 덧붙어 있지 않아야** 한다.
     ★ 「Precio en la etiqueta」 줄이 **없어야** 정상이다. **이 「없음」이 판정 대상이다.**
     (「일치합니다」 같은 문구가 보이면 그것도 실패다.)
   - 6c. 종이 라벨의 숫자와 **다르다면** →
     `Precio en la etiqueta: $X — el precio cambió.` 가 보여야 한다.
     그 `$X` 가 **종이에 인쇄된 그 숫자**와 같은가?
   - 참고: `Precio de referencia — puede no coincidir con la etiqueta.` 가 떠 있다면
     `qr_print_log` 를 못 찾았다는 뜻이고, **그때는 일치 보장이 없다.**
     그 사실이 화면에 적혀 있으면 정상 동작이다.
   - 참고: `Según la última impresión de esta etiqueta.` 가 떠 있다면 **구 라벨**이라
     어느 라벨인지 서버가 모른다는 뜻이다(`b`·`pt` 가 없는 URL). 이것도 정상이다.
7. 매장명이 맞는가? 사진이 나오는가(또는 `Sin foto` 자리표시자)?
   **지점명은 `b` 가 없는 옛 라벨에서는 안 나오는 것이 정상이다.**

**C. 새로 인쇄한 라벨 (`&b=` — QR 크기 회귀)**
8. POS 에서 같은 상품의 QR 라벨을 **새로 1장 인쇄한다**.
9. ★ 그 라벨을 폰으로 찍는다 → **읽히는가?**
   URL 이 `&b=123&pt=11` 만큼(**12자 안팎**) 길어져 QR 이 촘촘해졌다.
   자동 크기맞춤(`effectiveQrModule`)이 이것을 흡수하는지는 **인쇄물로만 알 수 있다.**
   안 읽히면 **실패로 보고한다** — `&pt=` 부터 되돌린다(`&b=` 가 지점 표시라는 더 큰
   가치를 준다). 기존 라벨은 어느 쪽이든 영향이 없다.
10. 읽히면 상세 화면에서 두 가지를 확인한다:
    - **지점명이 표시되는가**
    - ★ `Según la última impresión de esta etiqueta.` 줄이 **사라졌는가**
      (신 라벨은 `b`·`pt` 로 라벨이 확정되므로 그 문구가 **없어야** 한다)

**D. 끝나고**
11. 토글을 **원래대로 끌지, 켠 채로 둘지** 알려 주세요(둘 다 의도된 상태입니다).
  </how-to-verify>
  <acceptance_criteria>
    - A: 옛 라벨이 계정 없이 열렸다 (404 아님, 로그인 화면 아님)
    - A: 「공개몰로 가는 버튼」이 실제로 `cool.coolsistema.com` 을 열었다
    - B: Configuración 에서 QR 탭을 **찾아서** 토글할 수 있었다
    - B: 큰 가격이 그 가격유형의 현재 가격이다(6a)
    - B: 종이와 **같으면** 병기 줄이 **없다**(6b) — ★ 「없음」을 확인한 기록이어야 한다
    - B: 종이와 **다르면** `Precio en la etiqueta: $X` 가 보이고 `$X` 가 종이의 숫자다(6c)
    - C: 새로 인쇄한 라벨이 **읽힌다**
    - C: 새 라벨의 상세 화면에 **지점명이 나온다**
    - C: 새 라벨에서 `Según la última impresión` 줄이 **사라졌다**
    - 위 판정들이 사용자 말로 기록됐다
  </acceptance_criteria>
  <resume-signal>각 항목에 대해 「됨/안 됨」과, 안 됐다면 무엇이 보였는지</resume-signal>
  <done>성공 판정 1(계정 없이 열린다)과 2(가격이 라벨과 같다)가 실물로 확인됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 두 CTA 를 끝까지 누른다 + UAT 기록 — 성공 판정 3</name>
  <files>.planning/phases/89-qr-public-product-page/89-UAT.md</files>
  <read_first>
    - `.planning/phases/89-qr-public-product-page/89-08-SUMMARY.md` — 로컬에서 두 CTA 가 어떤 행을 만들었는지
    - `ventago-app/src/pages/admin/revendedores.tsx` — 승인 화면의 진입 조건(`WithAccess allowedApps={["admin"]}`)
    - `api-ventago/src/app/reseller/reseller.model.ts:10` — `reseller` 는 **별도 스키마**(`public.revendedores` 와 별개)
  </read_first>
  <action>
확인을 요청하기 전에 Claude 가 준비한다:

1. 현재 운영 기준선을 세어 둔다(제출 뒤 **증가**를 보려면 전후가 필요하다):
```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT (SELECT count(*) FROM reseller.resellers), (SELECT count(*) FROM ventago_leads);\""
```
   ★ 시작값은 `reseller.resellers` **0** 이어야 한다(운영 실측 2026-09-16).
     0 이 아니면 누가·언제 넣었는지부터 확인한다.
2. 아래 `<how-to-verify>` 를 전달하고 기다린다.
3. 사용자 확인 후 같은 조회를 다시 돌려 **증가분**을 확인하고,
   `.planning/phases/89-qr-public-product-page/89-UAT.md` 를 쓴다. 구조:

```markdown
# Phase 89 — UAT (실물)

**일시:** <날짜> · 확인자: 사용자 + Claude

## 성공 판정

| # | 판정 | 결과 | 관측 |
|---|---|---|---|
| 1 | 실물 라벨을 폰으로 찍어 페이지가 열린다(계정 없이) | PASS/FAIL | <사용자 말 그대로> |
| 2 | 매장·지점·사진·가격이 보이고 가격이 라벨과 같다 | PASS/FAIL | <종이 가격 / 화면 가격 / priceSource> |
| 3 | 두 CTA 를 끝까지 눌러 실제 화면에 도달한다 | PASS/FAIL | <도달한 화면> |

## 운영 DB 증거
<psql 출력 원문 — 전/후>

## 남긴 시험 데이터와 그 영향
<지우지 않는다. 무엇이 남았고 무엇에 보이는지>

## 이 phase 가 닫지 않은 것
<후속 후보>
```
★ **모르는 것을 아는 척 쓰지 않는다.** 텔레그램이 안 왔으면 「안 왔다」로 적고
  운영 env 미확인 사실을 함께 적는다(추측으로 PASS 를 만들지 않는다).
  </action>
  <what-built>
페이지 끝의 두 갈래 CTA — ① 이 매장의 reseller 신청 ② Ventago 잠재고객 수집.
백엔드는 로컬에서 실제 행 생성까지 확인했다. 운영에서 **끝까지 눌러 보는 것**이 남았다.
  </what-built>
  <how-to-verify>
설정이 켜진 상태(Task 2 의 B)에서 상세 화면 하단의 두 버튼을 각각 누른다.

**CTA① 이 매장의 reseller 신청**
1. 폼이 열리는가? 「{매장명} 의 revendedor 가 되겠다」는 문구가 그 매장을 가리키는가?
2. ★ **매장 선택 목록이 보이면 안 된다** — QR 의 매장으로 고정돼야 한다.
3. 이름·전화·신분증번호·비밀번호를 넣고 사진 3장(아무 사진이나)을 첨부해 **제출**한다.
4. 성공 문구가 뜨는가?
5. ★ **다음 버튼을 누른다** — 웹에서 superadmin/admin 으로 `/admin/revendedores` 를 열어
   방금 신청이 **pending 목록에 보이는지** 확인한다.
   (여기까지 가야 「막다른 CTA 가 아니다」가 증명된다. 이 경로는 운영에서
   **한 번도 실행된 적이 없다** — `reseller` 3개 테이블이 전부 0행이었다.)

**CTA② 당신 매장에도 — ★ 주 도착지는 가입 화면입니다**
6. `Crear mi tienda` 를 누른다 → **`/register?ref={apodo}`** 가 열리는가?
7. ★ **「¿Quién te recomendó?」 칸이 미리 채워져 있는가?** 그 값이 이 매장의 apodo 인가?
   (store 6 이면 `cool`) 확인 표시가 뜨는가?
   ★ **가입을 끝까지 하지는 마세요** — CUIT·주소·이중 OTP 가 필요합니다.
     여기서 확인할 것은 **도달과 프리필**까지입니다.
8. ★ **로그인한 창**에서 같은 버튼을 눌러 보세요 → **튕깁니다**(`guestGuard = true`).
   **어디로 갔는지** 알려 주세요. 이것은 결함이 아니라 제약이지만 기록이 필요합니다.

**이탈 받이 (작은 「연락처만 남기기」)**
9. 상세 화면 맨 아래의 **작은 텍스트 링크**(`Dejanos tu teléfono`)를 누른다.
   ★ 이것이 가입 버튼보다 **작아 보이는가**? 같은 크기면 주/종이 뒤집힌 것입니다.
10. 이름·전화를 넣고 제출한다. 성공 문구가 뜨는가?
11. ★ **운영자 텔레그램 채널에 알림이 도착했는가?**
   (안 와도 리드는 DB 에 남습니다 — 그 경우 「알림만 안 왔다」로 보고해 주세요.
   `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` 운영 설정 여부는 확인되지 않았습니다.)

**기록**
12. Claude 가 운영 DB 에서 행 생성을 확인하고 `89-UAT.md` 를 쓴다:
```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT id,document,status,is_active FROM reseller.resellers ORDER BY id DESC LIMIT 3;\""
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT reseller_id,store_id,status FROM reseller.reseller_tienda_link ORDER BY id DESC LIMIT 3;\""
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT id,store_id,source_product_id,contact_name,notify_status,notified_at FROM ventago_leads ORDER BY id DESC LIMIT 3;\""
```
★ `notify_status` 가 `'pending'` 이면 **알림 결과를 못 받았다**는 뜻이다 — 그 사실을 적는다.
★ `reseller` 는 **별도 스키마**다(`public` 아님).
★ 시험으로 만든 데이터는 **지우지 않는다**(상시 규칙). 대신 남은 것의 영향을 적는다 —
  운영 `reseller.resellers` 에 시험 신청 1건이 `pending_review`/`isActive=false` 로 남고
  `/admin/revendedores` 목록에 계속 보인다. 승인/거부 중 무엇을 할지는 사용자 판단이다.
  </how-to-verify>
  <acceptance_criteria>
    - CTA① 폼에 **매장 선택 UI 가 없다**
    - CTA① 제출이 성공하고 운영 `reseller.resellers` 에 행이 생겼다(psql 출력 원문)
    - `reseller.reseller_tienda_link` 의 `store_id` 가 **QR 의 그 매장**이다
    - 그 신청이 `/admin/revendedores` pending 목록에 **보였다**
    - CTA② 가 `/register?ref={apodo}` 를 열었고 **추천인 칸이 채워져 있었다**(관측 문장)
    - 로그인한 창에서 CTA② 가 **어디로 튕겼는지**가 기록됐다
    - 이탈 받이가 가입 버튼보다 **작아 보였다**(위계가 뒤집히지 않았다)
    - 이탈 받이 제출이 성공하고 운영 `ventago_leads` 에 행이 생겼다(psql 출력 원문)
    - 그 행의 `source_product_id` 가 **null 이 아니다**
    - 텔레그램 도착 여부가 「왔다/안 왔다」로 기록됐고 `notify_status` 값이 적혀 있다(추측 금지)
    - `.planning/phases/89-qr-public-product-page/89-UAT.md` 에 성공 판정 3건이
      **PASS/FAIL 과 관측 문장**으로 기록됐다
    - 남긴 시험 데이터와 그 영향이 `89-UAT.md` 에 적혀 있다
  </acceptance_criteria>
  <resume-signal>각 항목 「됨/안 됨」 + 텔레그램 도착 여부</resume-signal>
  <done>성공 판정 3(두 CTA 가 끝까지 도달한다)이 운영에서 확인되고 UAT 문서가 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발 워크스테이션 → `origin main` → Jenkins → 운영 컨테이너 | push 하나가 운영 배포다 |
| 인터넷 → 새로 열리는 공개 엔드포인트 2개 | 배포 순간부터 누구나 호출한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-30 | Elevation of Privilege | 승인 없는 운영 배포 | mitigate | Task 1 이 `checkpoint:decision` 이다. push 가 승인 지점이라는 CLAUDE.md 규칙을 이 plan 이 구조로 강제한다 |
| T-89-31 | Denial of Service | 마이그레이션 없이 코드 배포 | mitigate | Task 1 의 acceptance 가 **운영 DB 조회 출력**을 요구한다. 「파일이 있다」는 적용의 증거가 아니다 |
| T-89-04 | Information Disclosure | 배포 즉시 가격 공개 | mitigate | 전 매장 `qr_precio_publico=false` 를 배포 전에 운영에서 **직접 세어** 확인한다(on_count=0) |
| T-89-25 | Denial of Service | `&b=` 로 길어진 QR 이 안 읽힘 | mitigate | Task 2-C 가 **실물 인쇄 1장을 스캔**한다. 실패 시 89-07 의 `&b=` 만 되돌린다(기존 라벨 무영향) |
| T-89-32 | Repudiation | CODEX 지적을 안 읽고 배포 | mitigate | Task 1 의 acceptance 가 보고서가 **실제로 이 저장소 파일을 언급**하는지 확인하게 한다(서브모듈 건너뛰기 전례) |
</threat_model>

<verification>
```bash
curl -sL -o /dev/null -w '%{http_code}\n' "https://app.coolsistema.com/m/stock?s=6&p=1"          # 200
curl -s  -o /dev/null -w '%{http_code}\n' -X POST "https://newapi.coolsistema.com/api/public/ventago-leads" \
  -H 'Content-Type: application/json' -d '{}'                                                    # 404 아님
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \"SELECT count(*) FROM ventago_leads;\""
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \"SELECT count(*) FROM reseller.resellers;\""
```
</verification>

<success_criteria>
이 phase 의 성공 판정 3개가 전부 PASS 다:
1. 매장에 붙어 있는 **실물 라벨을 폰으로 찍어** 페이지가 열린다 — 계정 없이
2. 그 페이지가 매장 · 지점 · 상품 사진 · 가격을 보여준다. 가격은 **그 가격유형의 현재
   가격**이고, `printed_price` 와 **다를 때만** 「라벨 표기 $X — 가격이 변경됐습니다」가
   함께 보인다(**같으면 병기가 없다 — 이 「없음」도 판정 대상이다**)
3. 두 CTA 를 **끝까지 눌러** 각각 실제 화면에 도달한다

추가:
- 운영 배포가 승인을 거쳤고 빌드 성공 + 컨테이너 재생성이 확인됐다
- 새로 인쇄한 `&b=` 라벨이 실제로 읽힌다
- `89-UAT.md` 에 관측 기록이 남았다
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-09-SUMMARY.md` 와
`.planning/phases/89-qr-public-product-page/89-UAT.md` 를 만든다.

★ SUMMARY 에 **이 phase 가 닫지 않은 것**을 명시한다. 「해결했다」고 쓰지 말 것:

**구조적 한계 (이 phase 로는 못 고친다)**
- **숫자 ID 열거를 막지 못한다.** 서버는 요청자가 실물 라벨을 들었는지 알 수 없다.
  공개몰 OFF 매장에서만 「인쇄 기록 존재」로 범위를 줄였다. 공개몰 ON 매장은
  `GET /api/public/shop/:id/products` 가 이미 전 상품을 목록화한다.
  ⤷ 불투명 토큰이 유일한 실질 대책 — **후속 후보**(결정 ③의 연장).
- **구 라벨은 어느 라벨인지 서버가 모른다.** URL 에 유니크 키 3개 중 2개만 있다.
  89-07 이 신 라벨에서만 없앴다. 이미 붙어 있는 라벨은 영원히 「최신 인쇄분」 추정이고
  화면이 그렇게 말한다.
- **`qr_print_log` 를 쓰는 경로가 하나뿐이다**(`mark_qr_printed`).
  `POST /print/qr`(단일 라벨)로만 인쇄된 상품은 공개몰 OFF 매장에서 404 가 된다.
  오늘 영향은 0(그런 매장이 없다). emit 시점에 로그를 쓰면 실패한 인쇄가 IGUAL 로 보여
  **재출력이 영영 안 되므로** 고치지 않았다 — 후속에서 ack 기반으로 풀어야 한다.

**범위 밖으로 남긴 것**
- 이미지 파일명 모지바케 **수정**(89-02 는 진단만 했다) — 판정 결과를 근거로 후속 결정
- 공개몰이 재고 수량을 무조건 공개하는 문제 — **Phase 90** 으로 이미 분리돼 있다
- reseller 신청자에게 승인/거부를 알릴 수단 — reseller 포털이 없다
- **추천 가입이 승인까지 간 적이 없다** — 운영 `referral_credits` **0행**.
  그래서 「추천하면 보상을 받는다」를 화면에 단정하지 않았다. 그 경로(승인 → 보상 생성)
  확인은 superadmin 행위라 이 phase 범위 밖이다
- **`/register` 화면 안에서 포기하는 사람은 이탈 받이에 안 걸린다** —
  그것은 `RegisterView`/`RegisterForm` 을 고쳐야 하고(운영 중인 가입 흐름),
  이 phase 는 되고 있는 것을 깨뜨리지 않기로 했다
- **리드 조회 화면이 없다** — 받는 곳은 텔레그램 하나뿐이고, 실패하면
  `SELECT ... WHERE notify_status <> 'sent'` 로 DB 를 직접 봐야 안다
- `POST reseller/auth/login` 에 `@Throttle` 이 없다(이 phase 는 `register` 만 열었다)
- 리드의 `store_id` 귀속은 **클라이언트 제출값**이다 — 서명된 컨텍스트는 도입하지 않았다
</output>
