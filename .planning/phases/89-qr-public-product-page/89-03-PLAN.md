---
phase: 89-qr-public-product-page
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api-ventago/src/app/store/config/storeConfig.model.ts
  - api-ventago/src/app/store/config/storeConfig.controller.ts
  - api-ventago/src/app/store/config/qr-precio-publico.spec.ts
  - ventago-app/src/context/StoreConfigContext.tsx
  - ventago-app/src/views/configuracion/qr/QrConfigView.tsx
  - ventago-app/src/pages/configuracion/index.tsx
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - "매장 admin 이 Configuración 에서 「구입자가 QR 로 가격을 볼 수 있게」 토글을 **찾아서 누를 수 있다**"
    - "토글을 누르면 `store_configs.qr_precio_publico` 가 실제로 바뀐다(400 이 아니다)"
    - "모든 기존 매장은 꺼짐으로 보인다 — `store_configs` 행이 없는 매장도 꺼짐이다"
  artifacts:
    - path: "api-ventago/src/app/store/config/storeConfig.model.ts"
      provides: "qrPrecioPublico 컬럼 선언"
      contains: "field: 'qr_precio_publico'"
    - path: "api-ventago/src/app/store/config/storeConfig.controller.ts"
      provides: "FLAG_FIELDS 화이트리스트 등록"
      contains: "qrPrecioPublico"
    - path: "ventago-app/src/views/configuracion/qr/QrConfigView.tsx"
      provides: "토글 화면"
      contains: "qrPrecioPublico"
    - path: "ventago-app/src/pages/configuracion/index.tsx"
      provides: "Configuración 허브 탭 등록 — 이게 없으면 화면에 도달할 수 없다"
      contains: "QrConfigView"
  key_links:
    - from: "QrConfigView.tsx"
      to: "PUT /store-config/{storeId}/update-flag"
      via: "apiConnector.put"
      pattern: "update-flag"
    - from: "pages/configuracion/index.tsx"
      to: "QrConfigView"
      via: "HUB_TABS 항목"
      pattern: "key: 'qr'"
---

<objective>
사용자 결정 **D-04** 의 설정 스위치를 **네 곳 전부**에 배선한다.

Purpose: `store_configs` boolean 하나를 더하는 일은 이 저장소에서 **4벌 세트**다 —
모델 `@Column` · 컨트롤러 `FLAG_FIELDS` 화이트리스트 · 프론트 `StoreConfigContext` ·
Configuración 화면. **한 곳이라도 빠지면** 저장은 되는데 화면에 안 보이거나(Context 누락),
토글을 눌러도 400(FLAG_FIELDS 누락)이 나거나, 화면이 존재하는데 **아무도 못 본다**
(허브 탭 미등록 — 이 저장소에는 「마운트됨 ≠ 도달 가능」 전례가 있다).

Output: 매장 admin 이 Configuración › QR 탭에서 토글할 수 있는 `qrPrecioPublico`.

★ **배포 순서**: 89-01 의 양쪽 마이그레이션 적용 확인 **전에는 push 하지 않는다** —
  모델에 컬럼이 생기면 마이그레이션 전 운영에서 `store_configs` 조회가 전부 500 이다.
★ **api ↔ app 짝**: **API 를 먼저** 배포해도 안전하다(토글 화면이 아직 없다).
  반대로 app 을 먼저 배포하면 토글이 400 을 받는다. 순서는 api → app.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 그대로 복제할 현재 코드. 2026-09-16 실측. -->

`api-ventago/src/app/store/config/storeConfig.model.ts` — 같은 파일의 기존 선언(복제 대상):
```typescript
@Column({ field: 'vto_enabled', type: DataType.BOOLEAN, defaultValue: true })
vtoEnabled: boolean;

@Column({
  field: 'commerce_auto_sync',
  type: DataType.BOOLEAN,
  allowNull: false,
  defaultValue: true,
})
commerceAutoSync: boolean;
```
★ `type: DataType.BOOLEAN` 을 **반드시** 쓴다 — 생략하면 `design:type` 이 Object 가 돼
  **운영 부팅이 죽는다**(빌드·테스트·CI 는 전부 통과하므로 안 잡힌다).

`api-ventago/src/app/store/config/storeConfig.controller.ts:108-121` — 유일한 화이트리스트:
```typescript
private static readonly FLAG_FIELDS = [
  'useSupplier', 'useSeason', 'useOrigin', 'useSize', 'useColor',
  'useSubcategory', 'useCategory',
  'useRestaurantMode', 'useEnvios', 'allowSaleWithoutStock',
  'vtoEnabled',
  'useFacturaElectronica',
  'afipAutoIssue',
  'afipPrintTermica',
];
```
★ `PATCH` 와 `PUT` 은 **같은 `updateFlag()` 구현**을 부른다(주석 :79-87). 한 곳만 고치면 된다.
★ 권한은 `@Auth(ValidRoles.admin, ValidRoles.superadmin)` + `assertOwnership(user, storeId)`.

`ventago-app/src/context/StoreConfigContext.tsx` — 같은 이름이 **세 곳**에 나온다:
```typescript
// L31  interface
allowSaleWithoutStock: boolean;
// L34
vtoEnabled: boolean;
// L98  default
allowSaleWithoutStock: true,
// L101
vtoEnabled: true,
// L194 API 응답 매핑
allowSaleWithoutStock: res?.allowSaleWithoutStock ?? true,
// L197
vtoEnabled: res?.vtoEnabled ?? true,
```

`ventago-app/src/views/configuracion/inventario/InventarioConfigView.tsx:28-80` — 토글 화면 원형:
```typescript
const { allowSaleWithoutStock, reload } = useStoreConfig()
const [enabled, setEnabled] = useState<boolean>(allowSaleWithoutStock)

const handleToggle = async (next: boolean) => {
  if (!storeId) return
  setEnabled(next)            // 낙관적 업데이트
  setSaving(true); setError(null)
  try {
    await apiConnector.put(`/store-config/${storeId}/update-flag`, {
      field: 'allowSaleWithoutStock',
      value: next,
    })
    await reload()
    toast.success(next ? '...' : '...')
  } catch {
    setEnabled(!next)         // 실패 시 원복
    reportError('No se pudo guardar la configuración. Reintentá en unos segundos.')
  } finally { setSaving(false) }
}
```

`ventago-app/src/pages/configuracion/index.tsx` — 허브 탭 등록 형태(L28, L62):
```typescript
const InventarioConfigView = dynamic(() => import('src/views/configuracion/inventario/InventarioConfigView'), { ssr: false })
...
{ key: 'inventario', label: 'Inventario', icon: 'tabler:packages', section: 'Operación',
  requiredApps: ['admin'], render: () => <InventarioConfigView /> },
```
`HubTab` 타입에 `requiredPrivileged?: boolean` 이 있다 — 쓰기 엔드포인트가
`@Auth(admin, superadmin)` 인 화면은 이것을 켜야 한다(안 켜면 vendedor 에게 보이고 403).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 백엔드 — 모델 컬럼 + FLAG_FIELDS 화이트리스트</name>
  <files>api-ventago/src/app/store/config/storeConfig.model.ts, api-ventago/src/app/store/config/storeConfig.controller.ts</files>
  <read_first>
    - `api-ventago/src/app/store/config/storeConfig.model.ts` — 현재 상태 전체(특히 `commerceAutoSync` 선언)
    - `api-ventago/src/app/store/config/storeConfig.controller.ts` 의 70-140행 — `FLAG_FIELDS` 와 `updateFlag()`
    - `.planning/phases/89-qr-public-product-page/89-PATTERNS.md` § 12·13
  </read_first>
  <behavior>
    - `StoreConfig` 인스턴스에 `qrPrecioPublico: boolean` 이 있고 DB 컬럼 `qr_precio_publico` 로 매핑된다
    - `updateFlag(storeId, { field: 'qrPrecioPublico', value: true })` 가 **거부되지 않는다**
    - `updateFlag(storeId, { field: 'qrPrecioPublicoX', value: true })` 는 여전히 거부된다(화이트리스트가 살아 있다)
  </behavior>
  <action>
**(1) `storeConfig.model.ts`** — `commerceAutoSync` 선언 바로 아래에 다음을 추가한다:

```typescript
// [Phase 89] 구입자가 QR 라벨을 찍었을 때 상품 상세(매장·지점·사진·가격)를 보여줄까.
//
// ★ 기본 false 다(D-04). 이 값이 꺼져 있으면 공개몰 활성 여부와 무관하게
//   상품 상세를 내보내지 않는다 — 공개몰이 켜진 매장은 목록으로 보내고,
//   안 켜진 매장은 닫힘 화면을 보여준다(3갈래는 89-04 의 서비스가 판정한다).
// ★ 이 설정은 «가격을 숨기는» 것이 아니라 «상품 상세 딥링크» 를 통제한다.
@Column({
  field: 'qr_precio_publico',
  type: DataType.BOOLEAN,
  allowNull: false,
  defaultValue: false,
})
qrPrecioPublico: boolean;
```

**(2) `storeConfig.controller.ts`** — `FLAG_FIELDS` 배열 마지막에 한 줄 추가:

```typescript
  'qrPrecioPublico', // QR 공개 상품 상세 딥링크 토글 (Phase 89 D-04, 기본 OFF)
```

★ `useFacturaElectronica` 처럼 「켜는 순간에만」 추가 검증을 거는 조건부 블록은
  **복제하지 않는다** — 이 토글은 서류 요구가 없다.
★ `PATCH`/`PUT` 두 곳에 각각 넣지 말 것. 둘은 같은 `updateFlag()` 를 부른다.
  </action>
  <verify>
    <automated>cd api-ventago && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "field: 'qr_precio_publico'" api-ventago/src/app/store/config/storeConfig.model.ts` 가 1 이다
    - 그 `@Column` 블록에 `type: DataType.BOOLEAN` 과 `defaultValue: false` 가 **둘 다** 있다
    - `grep -c "'qrPrecioPublico'" api-ventago/src/app/store/config/storeConfig.controller.ts` 가 **정확히 1** 이다(두 동사에 중복 추가 금지)
    - `npx tsc --noEmit` 종료코드 0
  </acceptance_criteria>
  <done>모델과 화이트리스트에 컬럼이 한 번씩 들어갔고 타입 체크가 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 화이트리스트가 실제로 이 필드를 통과시키는지 시험 (대조군 포함)</name>
  <files>api-ventago/src/app/store/config/qr-precio-publico.spec.ts</files>
  <read_first>
    - `api-ventago/src/app/store/config/storeConfig.controller.ts` 의 `updateFlag()` 본문 — 무엇을 어떻게 거부하는지
    - `api-ventago/src/app/print/print-product-ownership.spec.ts` — 이 저장소의 spec 관례(`Object.create(Prototype)` + 필요한 것만 주입)
  </read_first>
  <behavior>
    - Test 1: `field: 'qrPrecioPublico'` 로 부르면 서비스의 갱신 메서드가 **실제로 호출된다**
    - Test 2: `field: 'qrPrecioPublicoFalso'` 는 거부되고 갱신 메서드가 **호출되지 않는다** (대조군 — 화이트리스트가 살아 있음을 증명)
    - Test 3: `FLAG_FIELDS` 안의 필드 이름이 전부 `StoreConfig` 모델의 **실재 속성**이다 (오타로 죽은 항목 방지)
  </behavior>
  <action>
`api-ventago/src/app/store/config/qr-precio-publico.spec.ts` 를 새로 만든다.

```typescript
// [Phase 89] 「구입자가 QR 로 가격을 볼 수 있게」 토글이 **실제로 저장되는지** 를 지킨다.
//
// ★ 화이트리스트에 안 넣으면 화면은 멀쩡한데 토글이 400 을 받는다 — 저장이 안 되는 것을
//   사용자는 «가끔 안 먹힌다» 로 경험한다. 그래서 통과(Test 1)와 거부(Test 2)를 같이 단언한다.
// ★ Test 2 가 대조군이다. 이것이 없으면 «화이트리스트를 통째로 지워도» Test 1 이 통과한다.
```

- `StoreConfigController.prototype` 을 `Object.create` 로 만들고 `storeConfigService` 자리에
  `update: jest.fn()` (또는 실제 구현이 부르는 메서드명 — **먼저 코드를 읽고 맞출 것**)을 심는다.
- `assertOwnership` 이 통과하도록 `user` 를 `{ storeId: 6, roles: ['admin'] }` 형태로 준다.
- Test 3 은 `StoreConfig` 모델의 `rawAttributes` 대신 **소스 문자열을 읽지 말고**,
  `new StoreConfig()` 가 불가능하면 `Object.keys(StoreConfig.prototype)` 이 비어 있으므로
  `StoreConfig.rawAttributes` 를 쓴다(sequelize-typescript 데코레이터가 채운다).
  ★ 만약 `rawAttributes` 가 비어 있다면(모델이 Sequelize 에 등록되기 전) **Test 3 을 넣지 말고
    그 사실을 spec 주석에 적는다** — 늘 통과하는 시험은 시험이 아니다.

★ **시험이 검증 대상을 부르는지 확인할 것.** Test 1 은 「예외가 안 났다」가 아니라
  「갱신 메서드가 `qrPrecioPublico` 인자로 호출됐다」를 단언한다.
★ **대조군 실증**: `FLAG_FIELDS` 에서 `'qrPrecioPublico'` 를 잠시 지웠을 때 Test 1 이
  실제로 빨개지는 것을 한 번 확인하고 원복한다. 확인 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/app/store/config --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `api-ventago/src/app/store/config/qr-precio-publico.spec.ts` 가 존재한다
    - `npx jest src/app/store/config --maxWorkers=1` 종료코드 0 이고 출력에 `qr-precio-publico` suite 가 **실제로 나타난다**(0 suites 면 안 돈 것이다)
    - spec 에 「거부」 케이스가 있고 `expect(...).not.toHaveBeenCalled()` 를 단언한다
    - SUMMARY 에 「`FLAG_FIELDS` 에서 항목을 빼면 Test 1 이 빨개지는 것을 확인했다」가 적혀 있다
  </acceptance_criteria>
  <done>통과·거부 두 방향이 시험되고, 방어를 지우면 빨개지는 것을 실제로 확인했다.</done>
</task>

<task type="auto">
  <name>Task 3: 프론트 — Context 3곳 + QrConfigView + Configuración 허브 탭 등록</name>
  <files>ventago-app/src/context/StoreConfigContext.tsx, ventago-app/src/views/configuracion/qr/QrConfigView.tsx, ventago-app/src/pages/configuracion/index.tsx</files>
  <read_first>
    - `ventago-app/src/context/StoreConfigContext.tsx` — `vtoEnabled` 가 나오는 **세 곳**(interface/default/매핑)
    - `ventago-app/src/views/configuracion/inventario/InventarioConfigView.tsx` — 전체 구조를 그대로 복제
    - `ventago-app/src/pages/configuracion/index.tsx` — `HUB_TABS` 와 `HubTab` 타입
    - `./CLAUDE.md` § 「ESLint 규칙」
  </read_first>
  <action>
**(1) `StoreConfigContext.tsx`** — `vtoEnabled` 가 나오는 **세 곳 모두**에 추가한다.
기본값은 `vtoEnabled` 와 **반대인 `false`** 다:

```typescript
// interface
qrPrecioPublico: boolean;
// default
qrPrecioPublico: false,
// API 응답 매핑
qrPrecioPublico: res?.qrPrecioPublico ?? false,
```
★ `?? false` 여야 한다 — `store_configs` 행이 **없는 매장**(로컬 실측 333개)에서 안전한 쪽으로 떨어진다.

**(2) `ventago-app/src/views/configuracion/qr/QrConfigView.tsx` 신규** —
`InventarioConfigView.tsx` 를 그대로 복제하고 다음만 바꾼다:
- `field: 'qrPrecioPublico'`
- 상태 초기값: `useStoreConfig().qrPrecioPublico`
- 제목: `QR del producto`
- 설명 문구(스페인어):
  `¿Permitir que quien escanee el QR de la etiqueta vea el detalle del producto (tienda, sucursal, foto y precio)?`
- 꺼짐 상태 부연:
  `Si está apagado y tenés tienda online, el QR lleva al catálogo. Si no tenés tienda online, el QR no muestra nada.`
- 성공 toast: 켬 `El QR ahora muestra el detalle del producto.` / 끔 `El QR ya no muestra el detalle del producto.`
- 낙관적 업데이트 + 실패 시 원복 패턴은 **그대로** 복제한다.

★ 테마는 다크 네이비 + 골드(프로젝트 skill `sketch-findings-ace-online`) — `InventarioConfigView`
  가 이미 그 테마 안에서 렌더되므로 색을 직접 지정하지 않는다.

**(3) `pages/configuracion/index.tsx`** — 두 줄을 더한다:
```typescript
const QrConfigView = dynamic(() => import('src/views/configuracion/qr/QrConfigView'), { ssr: false })
```
그리고 `HUB_TABS` 의 `'Operación'` 구역, `inventario` 항목 **다음**에:
```typescript
{ key: 'qr', label: 'QR del producto', icon: 'tabler:qrcode', section: 'Operación',
  requiredApps: ['admin'], requiredPrivileged: true, render: () => <QrConfigView /> },
```
★ `requiredPrivileged: true` 는 필수다 — 쓰기 엔드포인트가 `@Auth(admin, superadmin)` 이라
  안 걸면 vendedor 에게 탭이 보이고 저장에서 403 이 난다.
★ **이 등록이 없으면 화면은 존재하지만 아무도 도달할 수 없다.** 이 저장소에는 그 전례가 있다.

**ESLint (Warning 도 빌드를 막는다):**
- `return` 문 바로 위에 빈 줄
- `//` 주석 바로 위에 빈 줄
- import 한 것은 반드시 쓴다
  </action>
  <verify>
    <automated>cd ventago-app && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/views/configuracion/qr/QrConfigView.tsx src/context/StoreConfigContext.tsx src/pages/configuracion/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "qrPrecioPublico" ventago-app/src/context/StoreConfigContext.tsx` 가 **3** 이다
    - `grep -c "?? false" ventago-app/src/context/StoreConfigContext.tsx` 가 1 이상이고, `qrPrecioPublico: res?.qrPrecioPublico ?? false` 가 그대로 있다
    - `ventago-app/src/views/configuracion/qr/QrConfigView.tsx` 에 `field: 'qrPrecioPublico'` 와 `update-flag` 가 있다
    - `grep -c "QrConfigView" ventago-app/src/pages/configuracion/index.tsx` 가 **2** 이다(dynamic import 1 + HUB_TABS render 1)
    - `grep -n "key: 'qr'" ventago-app/src/pages/configuracion/index.tsx` 가 1건이고 그 줄에 `requiredPrivileged: true` 가 있다
    - `npx tsc --noEmit` 종료코드 0, 세 파일에 대한 `npx eslint` 종료코드 0
  </acceptance_criteria>
  <done>토글이 Configuración › Operación › 「QR del producto」 탭에서 도달 가능하고 저장된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(매장 admin) → `PUT/PATCH /store-config/:storeId/update-flag` | 임의 필드명·임의 storeId 를 보낼 수 있다 |
| 프론트 탭 가시성 → 실제 권한 | 탭을 숨겨도 API 는 열려 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-14 | Tampering | `updateFlag` 필드명 | mitigate | `FLAG_FIELDS` 화이트리스트에 정확히 한 번 추가. `PATCH`/`PUT` 이 같은 구현을 부르므로 우회 동사가 없다. Task 2 의 거부 케이스가 이것을 지킨다 |
| T-89-15 | Elevation of Privilege | 남의 매장 설정 변경 | mitigate | 기존 `assertOwnership(user, storeId)` + `@Auth(admin, superadmin)` — 이 plan 은 그 가드를 우회하는 새 경로를 만들지 않는다 |
| T-89-09 | Elevation of Privilege | 프론트 탭 게이트만으로 가리기 | mitigate | `requiredPrivileged: true` 는 **가시성**일 뿐이고 실제 통제는 서버가 한다. 이 plan 은 서버 가드를 그대로 둔다(프론트 플래그는 보안 경계가 아니다) |
| T-89-04 | Information Disclosure | 기본값이 켜짐이 되는 것 | mitigate | 모델 `defaultValue: false` + Context `?? false` + 마이그레이션 `DEFAULT false` — **세 층이 전부 false** 여야 한다. acceptance 가 세 곳을 각각 센다 |
</threat_model>

<verification>
```bash
cd api-ventago && npx tsc --noEmit && npx jest src/app/store/config --maxWorkers=1
cd ventago-app && npx tsc --noEmit && npx eslint src/views/configuracion/qr/QrConfigView.tsx src/context/StoreConfigContext.tsx src/pages/configuracion/index.tsx
grep -c "qrPrecioPublico" ventago-app/src/context/StoreConfigContext.tsx   # 3
grep -c "QrConfigView" ventago-app/src/pages/configuracion/index.tsx       # 2
```
</verification>

<success_criteria>
- 4벌 세트(모델 · FLAG_FIELDS · Context 3곳 · 허브 탭)가 전부 배선됐다
- 기본값이 세 층(마이그레이션 · 모델 · Context) 모두에서 `false` 다
- 화이트리스트 통과·거부 두 방향이 시험되고 대조군이 실제로 빨개지는 것을 확인했다
- `tsc` · `eslint` · `jest` 전부 종료코드 0
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-03-SUMMARY.md` 를 만든다.
★ 배포 순서(마이그레이션 → api → app)를 SUMMARY 에 한 줄로 남긴다.
</output>
