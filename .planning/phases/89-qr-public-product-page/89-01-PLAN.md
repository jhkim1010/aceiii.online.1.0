---
phase: 89-qr-public-product-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql
  - api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql
autonomous: false
requirements: [REQ-03, REQ-05]
must_haves:
  truths:
    - "로컬(5432) `store_configs` 에 `qr_precio_publico` 컬럼이 있고 기존 전 행이 false 다"
    - "운영(5434) `store_configs` 에 `qr_precio_publico` 컬럼이 있고 기존 전 행이 false 다"
    - "로컬·운영 양쪽에 `ventago_leads` 테이블이 있고 owner 가 coolsistema 다(운영)"
    - "마이그레이션 규약 spec 이 이 두 파일에 대해 통과한다"
    - "`ventago_leads` 에 `notify_status`/`notified_at` 이 있어 알림 실패가 흔적 없이 사라지지 않는다"
  artifacts:
    - path: "api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql"
      provides: "store_configs.qr_precio_publico BOOLEAN NOT NULL DEFAULT false"
      contains: "ADD COLUMN IF NOT EXISTS qr_precio_publico"
    - path: "api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql"
      provides: "ventago_leads 테이블 + owner/sequence 이전"
      contains: "ALTER SEQUENCE ventago_leads_id_seq"
  key_links:
    - from: "2026-09-16-phase89-ventago-leads.sql"
      to: "stores(id)"
      via: "store_id FK"
      pattern: "REFERENCES stores \\(id\\)"
    - from: "두 마이그레이션 파일"
      to: "운영 PG18 5434"
      via: "ssh jhkim-server psql -p 5434"
      pattern: "적용 후 information_schema 조회로 확인"
---

<objective>
이 phase 가 쓰는 스키마 변경 **두 건**을 만들고 **로컬 5432 와 운영 5434 양쪽에 적용**한다.

Purpose: 이 저장소에서 반복된 사고 형태가 「한쪽만 적용 → 배포 후 500」 이다.
그리고 **모델에 컬럼을 더하면 마이그레이션 적용 전에는 push 금지** 라는 규칙 때문에,
이 plan 이 끝나기 전에는 89-03·89-04·89-05 의 코드를 배포할 수 없다.
그래서 이 plan 이 wave 1 이고, 다른 모든 plan 이 여기에 매달린다.

Output:
- `store_configs.qr_precio_publico BOOLEAN NOT NULL DEFAULT false` (결정 ④ — 기본값 꺼짐)
- `ventago_leads` 신규 테이블 (결정 ⑤ CTA② 수집처, `notify_status`/`notified_at` 포함)
- 양쪽 DB 적용 확인 기록

★ **세 번째 마이그레이션은 89-10 에 있다** (`prices` UNIQUE, 사용자 결정 ① 개정본에
  「함께 확정된 것」으로 들어온 작업). 그 파일은 `CONCURRENTLY` 라 **`--single-transaction`
  없이** 적용해야 해서 여기와 명령이 다르다 — 그래서 plan 을 나눴다.
  다만 **운영 승인은 같은 대화에서 함께 받아도 된다**(둘 다 wave 1, 둘 다 운영 DDL).
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/89-qr-public-product-page/89-CONTEXT.md
@.planning/phases/89-qr-public-product-page/89-PATTERNS.md
</context>

<interfaces>
<!-- 실행자가 뒤질 필요 없이 그대로 쓸 수 있게 뽑아 둔 현재 상태. 2026-09-16 로컬 DB 실측. -->

`store_configs` 의 기존 boolean 컬럼 (이 명명을 따른다):
```
use_supplier use_season use_origin use_size use_color use_subcategory use_category
use_restaurant_mode allow_sale_without_stock use_envios use_factura_electronica
afip_production afip_auto_issue vto_enabled commerce_auto_sync afip_print_termica
```
★ `qr_precio_publico` 는 **없다**(2026-09-16 로컬 확인). `ventago_leads` 테이블도 **없다**.

★ `store_configs` 행이 없는 매장이 있다 — 로컬 실측 **333개 매장이 config 행 없음**.
  그래서 애플리케이션은 `COALESCE(c.qr_precio_publico, false)` 로 읽어야 한다(89-04 에서 처리).
  이 마이그레이션은 **기존 행에만** 값을 채운다.

analog 전문 — `api-ventago/migrations/2026-08-28-store-commerce-auto-sync.sql`:
```sql
BEGIN;

SET lock_timeout = '5s';

ALTER TABLE store_configs
  ADD COLUMN IF NOT EXISTS commerce_auto_sync BOOLEAN NOT NULL DEFAULT true;

COMMENT ON COLUMN store_configs.commerce_auto_sync IS '...';

DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'coolsistema') THEN
    EXECUTE 'ALTER TABLE store_configs OWNER TO coolsistema';
  END IF;
END $$;

COMMIT;
```

analog 전문(신규 테이블) — `api-ventago/migrations/2026-08-25-phase86-upload-sessions.sql`:
```sql
-- w4-exempt: 이 배포에서 처음 만들어지는 테이블이라 읽는 코드가 아직 없다.
SET lock_timeout = '5s';

BEGIN;

CREATE TABLE IF NOT EXISTS legacy_upload_sessions (
  id         BIGSERIAL PRIMARY KEY,
  store_id   INTEGER     NOT NULL REFERENCES stores (id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'coolsistema') THEN
    ALTER TABLE    legacy_upload_sessions        OWNER TO coolsistema;
    ALTER SEQUENCE legacy_upload_sessions_id_seq OWNER TO coolsistema;
  END IF;
END
$$;

COMMIT;
```

규약 강제 지점 — `api-ventago/src/common/migrations/migration-conventions.spec.ts`:
- 컷오프 `2026-08-21` 이후 파일만 검사한다 → **이 두 파일은 검사 대상이다**.
- 기존 테이블을 `ALTER` 하는 파일은 `SET lock_timeout` 이 **반드시** 있어야 한다.
- 같은 파일 안에서 `CREATE TABLE` 한 테이블에 대한 인덱스는 `CONCURRENTLY` 면제
  (`targetsNewTable()` 이 그렇게 판정한다).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: store_configs 에 qr_precio_publico 추가 (기본값 false)</name>
  <files>api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql</files>
  <read_first>
    - `api-ventago/migrations/2026-08-28-store-commerce-auto-sync.sql` — 그대로 복제할 골격(위 `<interfaces>` 에 전문 있음)
    - `api-ventago/src/common/migrations/migration-conventions.spec.ts` — 이 파일이 무엇을 검사하는지
    - `.planning/phases/89-qr-public-product-page/89-CONTEXT.md` § 「④ 설정 스위치」
  </read_first>
  <action>
파일을 새로 만든다. 내용은 정확히 다음 구조다(값·이름을 바꾸지 말 것):

```sql
-- [Phase 89] 구입자가 QR 로 상품 상세(가격 포함)를 볼 수 있게 할 것인가.
--
-- ★ 기본값 false 다. `commerce_auto_sync`(DEFAULT true) 와 반대이므로 베끼지 말 것.
--   기존 매장이 전부 꺼짐으로 시작해야 «켜기 전까지 아무것도 바뀌지 않는다»(배포 안전).
--   켜는 것이 매장의 의사표시다 — 사용자 결정 D-04(2026-09-16).
--
-- ★ 이 설정은 «가격을 숨기는» 것이 아니라 «상품 상세 딥링크» 를 통제한다.
--   가격은 이미 종이 라벨에 인쇄돼 있다.

BEGIN;

SET lock_timeout = '5s';

ALTER TABLE store_configs
  ADD COLUMN IF NOT EXISTS qr_precio_publico BOOLEAN NOT NULL DEFAULT false;

COMMENT ON COLUMN store_configs.qr_precio_publico IS
  'QR 라벨을 찍은 구입자에게 상품 상세(매장·지점·사진·가격)를 보여줄까. 기본 꺼짐 (Phase 89 D-04).';

DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'coolsistema') THEN
    EXECUTE 'ALTER TABLE store_configs OWNER TO coolsistema';
  END IF;
END $$;

COMMIT;
```

`NOT NULL DEFAULT false` 는 한 파일에서 끝난다 — 컬럼 추가 즉시 기존 행에 값이 채워지므로
백필 단계가 필요 없다(`users.email` 케이스와 다르다).
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/common/migrations --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql` 파일이 존재한다
    - 그 파일에 `SET lock_timeout = '5s';` 가 있다 (없으면 규약 spec 이 빨개진다)
    - 그 파일에 `ADD COLUMN IF NOT EXISTS qr_precio_publico BOOLEAN NOT NULL DEFAULT false` 가 있다
    - 그 파일에 `DEFAULT true` 라는 문자열이 **없다**
    - `npx jest src/common/migrations --maxWorkers=1` 종료코드 0
  </acceptance_criteria>
  <done>파일이 존재하고 마이그레이션 규약 spec 이 통과한다. (아직 어느 DB 에도 적용하지 않았다.)</done>
</task>

<task type="auto">
  <name>Task 2: ventago_leads 신규 테이블 마이그레이션</name>
  <files>api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql</files>
  <read_first>
    - `api-ventago/migrations/2026-08-25-phase86-upload-sessions.sql` — BIGSERIAL 신규 테이블 + owner/sequence 이전 골격
    - `api-ventago/migrations/2026-08-23-mp-oauth-states.sql` — `@Public()` 콜백이 쓰는 테이블의 `store_id` 설계
    - `.planning/phases/89-qr-public-product-page/89-PATTERNS.md` § 10
  </read_first>
  <action>
파일을 새로 만든다. 내용은 정확히 다음이다:

```sql
-- [Phase 89] CTA② «당신의 매장에도 같은 시스템을?» — Ventago 잠재고객 수집.
--
-- w4-exempt: 이 배포에서 처음 만들어지는 테이블이라 읽는 코드가 아직 없다.
--
-- ★ 저장 없이 텔레그램만 보내면 «받는 사람이 그 순간 못 보면 영구 유실» 이 된다.
--   테이블이 원장이고 알림은 통지다. 알림 실패가 수집 실패가 되면 안 된다.
-- ★ store_id 의 신뢰 수준 (2026-09-16 CODEX 자문 반영):
--   이 값은 **클라이언트가 제출한 값**이다. QR 조회(GET)와 리드 제출(POST)은 독립 요청이고
--   둘 사이에 서명된 상태를 전달하는 수단이 **없다**. 서버는 그 매장이 실재·활성인지,
--   그리고 source_product_id 가 그 매장 것인지까지만 확인한다.
--   ⤷ 즉 «어느 매장의 QR 에서 왔는가» 는 **귀속의 참고값이지 확정 사실이 아니다.**
--     근거 없는 귀속은 «모른다» 보다 나쁘다 — 읽는 쪽이 확정 사실로 다루면 안 된다.
--   서명된 컨텍스트를 도입하는 것은 이 phase 의 범위를 넓히므로 하지 않는다(후속 후보).

SET lock_timeout = '5s';

BEGIN;

CREATE TABLE IF NOT EXISTS ventago_leads (
  id                BIGSERIAL    PRIMARY KEY,
  store_id          INTEGER      NOT NULL REFERENCES stores (id) ON DELETE CASCADE,
  source_product_id INTEGER      NULL     REFERENCES products (id) ON DELETE SET NULL,
  contact_name      VARCHAR(160) NOT NULL,
  contact_phone     VARCHAR(40)  NOT NULL,
  contact_email     VARCHAR(160) NULL,
  message           TEXT         NULL,

  -- ★ 알림의 «결과» 를 남긴다. 이 저장소의 반복 결함이 «감시 장치가 부재에서 침묵한다» 이고
  --   같은 형태로 세 번 조용히 죽었다. notifyTelegram 은 fire-and-forget 이라
  --   실패를 호출자에게 알리지 않는다 — 여기에 안 남기면 «리드는 들어왔는데 아무도 못 봤다» 가
  --   **아무 흔적 없이** 일어난다.
  --   notify_status: 'pending' | 'sent' | 'deduplicated' | 'unconfigured' | 'failed'
  --   (sendTelegramMessageDetailed() 가 돌려주는 네 값 + 초기값. CHECK 는 걸지 않는다 —
  --    값이 늘 때 마이그레이션이 또 필요해지고, 이 값은 사람이 보는 진단용이다.)
  notify_status     VARCHAR(16)  NOT NULL DEFAULT 'pending',
  notified_at       TIMESTAMPTZ  NULL,

  created_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),
  updated_at        TIMESTAMPTZ  NOT NULL DEFAULT now()
);

COMMENT ON TABLE ventago_leads IS
  'QR 공개 상품 페이지 CTA② 로 들어온 Ventago 잠재고객 (Phase 89).';

COMMENT ON COLUMN ventago_leads.store_id IS
  '클라이언트가 제출한 유입 매장. 실재·활성 확인만 거쳤다 — 귀속은 참고용이지 확정 사실이 아니다 (Phase 89).';

COMMENT ON COLUMN ventago_leads.notify_status IS
  '텔레그램 통지 결과: pending|sent|deduplicated|unconfigured|failed. 알림 실패가 조용해지지 않게 남긴다 (Phase 89).';

CREATE INDEX IF NOT EXISTS ix_ventago_leads_store_created
  ON ventago_leads (store_id, created_at DESC);

DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'coolsistema') THEN
    ALTER TABLE    ventago_leads        OWNER TO coolsistema;
    ALTER SEQUENCE ventago_leads_id_seq OWNER TO coolsistema;
  END IF;
END
$$;

COMMIT;
```

★ `ALTER SEQUENCE ... OWNER` 를 빠뜨리면 안 된다 — `ALTER TABLE OWNER` 는 시퀀스 owner 를
옮기지 않는다. 빠지면 운영에서 INSERT 가 `permission denied` 500 이 된다.
★ 인덱스에 `CONCURRENTLY` 를 **쓰지 않는다** — 같은 파일에서 만든 새 테이블이라
규약 spec 의 `targetsNewTable()` 이 면제하고, `CONCURRENTLY` 는 트랜잭션 안에서 쓸 수 없다.
  </action>
  <verify>
    <automated>cd api-ventago && npx jest src/common/migrations --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>
    - `api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql` 이 존재한다
    - `grep -c "ALTER SEQUENCE ventago_leads_id_seq OWNER TO coolsistema"` 결과가 1 이다
    - `grep -c "notified_at"` 결과가 1 이상이고 `notify_status` 도 있다
    - `grep -c "w4-exempt"` 결과가 1 이상이다
    - `grep -c "CONCURRENTLY"` 결과가 0 이다
    - `npx jest src/common/migrations --maxWorkers=1` 종료코드 0
  </acceptance_criteria>
  <done>파일이 존재하고 규약 spec 통과. 아직 적용 전.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 로컬(5432) 적용 → 운영(5434) 적용 승인 → 양쪽 확인</name>
  <files>(DB 변경 — 소스 파일 변경 없음)</files>
  <read_first>
    - `./CLAUDE.md` § 「DB 마이그레이션 적용 규칙 (로컬 + 운영 동시)」
    - 위 두 SQL 파일(방금 쓴 것) — 실제로 무엇이 실행되는지 눈으로 확인
  </read_first>
  <decision>운영 DB(srv803182 PG18 5434 `ventago`)에 이 두 마이그레이션을 적용해도 되는가</decision>
  <context>
CLAUDE.md 규칙: 운영 DDL 은 **SQL 내용 + 예상 영향 row 수를 보여주고 동의를 받은 뒤**에만
실행한다. 동의 후 실행은 Claude 가 직접 한다.

**먼저 로컬(5432)에 적용하고 그 결과를 근거로 제시한다.**

```bash
# 1) 로컬 적용 (승인 불필요)
psql -p 5432 -d ventago -v ON_ERROR_STOP=1 --single-transaction \
  -f api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql
psql -p 5432 -d ventago -v ON_ERROR_STOP=1 \
  -f api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql

# 2) 로컬 확인 — 이 세 줄의 출력을 사용자에게 그대로 보여준다
psql -p 5432 -d ventago -tAc \
  "SELECT count(*) FILTER (WHERE qr_precio_publico), count(*) FROM store_configs;"
psql -p 5432 -d ventago -tAc \
  "SELECT column_name FROM information_schema.columns WHERE table_name='ventago_leads' ORDER BY ordinal_position;"

# 3) 운영에서 영향 row 수 미리 세기 (읽기 전용 — 승인 불필요)
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  'SELECT count(*) FROM store_configs;'"
```

**예상 영향:** `store_configs` 전 행에 `false` 컬럼 1개 추가(값 변경 없음, 기능 변화 없음) +
빈 테이블 1개 생성. 기존 동작은 **아무것도 바뀌지 않는다** — 읽는 코드가 아직 배포되지 않았다.
  </context>
  <options>
    <option id="apply">
      <name>양쪽 적용한다</name>
      <pros>89-03·89-04·89-05 코드 배포가 가능해진다. 이 phase 의 모든 후속 작업이 여기에 막혀 있다</pros>
      <cons>운영 DDL — 되돌리려면 `DROP COLUMN`/`DROP TABLE` 이 필요하다(컬럼 DROP 은 옛 백업 복원을 깨뜨린다)</cons>
    </option>
    <option id="local-only">
      <name>로컬만 적용하고 운영은 나중에</name>
      <pros>운영 무변경</pros>
      <cons>★ 한쪽만 적용은 이 저장소에서 반복된 사고 형태다. 그 상태로 코드를 push 하면 그 테이블 조회가 전부 500</cons>
    </option>
  </options>
  <action>
승인(`apply`)을 받으면 **Claude 가 직접** 다음을 실행한다:

```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -v ON_ERROR_STOP=1 --single-transaction" \
  < api-ventago/migrations/2026-09-16-phase89-store-configs-qr-precio-publico.sql
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -v ON_ERROR_STOP=1" \
  < api-ventago/migrations/2026-09-16-phase89-ventago-leads.sql
```

그 다음 **적용 확인**(파일이 존재하는 것은 적용의 증거가 아니다 — DB 에서 직접 본다):

```bash
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT count(*) FILTER (WHERE qr_precio_publico) AS on_count, count(*) AS total FROM store_configs;\""
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT tableowner FROM pg_tables WHERE tablename='ventago_leads';\""
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \
  \"SELECT sequenceowner FROM pg_sequences WHERE sequencename='ventago_leads_id_seq';\""
```

세 출력을 SUMMARY 에 그대로 붙인다.
  </action>
  <resume-signal>`apply` 또는 `local-only` 또는 수정 요청</resume-signal>
  <verify>
    <automated>psql -p 5432 -d ventago -tAc "SELECT count(*) FROM store_configs WHERE qr_precio_publico IS NOT NULL;" && psql -p 5432 -d ventago -tAc "SELECT count(*) FROM ventago_leads;"</automated>
  </verify>
  <acceptance_criteria>
    - 로컬: `psql -p 5432 -d ventago -tAc "SELECT count(*) FILTER (WHERE qr_precio_publico), count(*) FROM store_configs;"` 의 첫 값이 **0** 이다(전부 꺼짐)
    - 로컬: `psql -p 5432 -d ventago -tAc "SELECT count(*) FROM ventago_leads;"` 가 `0` 을 돌려준다(에러 아님)
    - 운영: `ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \"SELECT count(*) FILTER (WHERE qr_precio_publico), count(*) FROM store_configs;\""` 의 첫 값이 **0** 이다
    - 운영: `pg_tables.tableowner` 가 `coolsistema` 다
    - 운영: `pg_sequences.sequenceowner` 가 `coolsistema` 다
    - 위 다섯 출력이 SUMMARY 에 원문으로 붙어 있다
  </acceptance_criteria>
  <done>양쪽 DB 에서 컬럼·테이블·owner 를 **직접 조회해** 확인했다. 이제 89-03/04/05 의 코드가 push 가능하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발 워크스테이션 → 운영 PG(5434) | DDL 이 운영 데이터에 직접 닿는다 |
| 미래의 `@Public()` 요청 → `ventago_leads` | 인증 없는 쓰기가 이 테이블에 도달한다(89-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Information Disclosure | `store_configs.qr_precio_publico` | mitigate | `DEFAULT false` — 기본값이 true 면 배포 즉시 14개 매장 가격이 전부 공개된다. Task 1 의 acceptance 가 `DEFAULT true` 문자열 부재를 명시적으로 센다 |
| T-89-06 | Tampering | `ventago_leads.store_id` | mitigate | `NOT NULL REFERENCES stores(id)` — 존재하지 않는 매장으로 위조 불가. 값의 출처 재검증은 89-05 가 담당 |
| T-89-10 | Denial of Service | 운영 `store_configs` ALTER | mitigate | `SET lock_timeout = '5s'` — 앞선 장기 트랜잭션이 있으면 대기 대신 실패시킨다(줄줄이 막히는 것보다 낫다) |
| T-89-11 | Elevation of Privilege | 운영 `ventago_leads` owner | mitigate | owner DO 블록으로 테이블 **및 시퀀스**를 `coolsistema` 로 이전. 누락 시 앱이 permission denied 500 |
</threat_model>

<verification>
```bash
cd api-ventago && npx jest src/common/migrations --maxWorkers=1
psql -p 5432 -d ventago -tAc "SELECT count(*) FILTER (WHERE qr_precio_publico), count(*) FROM store_configs;"
ssh jhkim-server "sudo -u postgres psql -p 5434 -d ventago -tAc \"SELECT count(*) FILTER (WHERE qr_precio_publico), count(*) FROM store_configs;\""
```
</verification>

<success_criteria>
- 마이그레이션 파일 2개가 `api-ventago/migrations/` 에 커밋됐다
- 규약 spec 통과
- 로컬 5432 · 운영 5434 **양쪽**에서 컬럼·테이블 존재를 직접 조회로 확인했고 그 출력이 SUMMARY 에 있다
- 운영 `ventago_leads` 의 테이블 owner 와 시퀀스 owner 가 둘 다 `coolsistema` 다
- 기존 전 매장의 `qr_precio_publico` 가 `false` 다
</success_criteria>

<output>
완료 후 `.planning/phases/89-qr-public-product-page/89-01-SUMMARY.md` 를 만든다.
★ SUMMARY 에 **양쪽 DB 조회 출력 원문**을 반드시 포함할 것 — 「파일을 만들었다」는 적용의 증거가 아니다.
</output>
