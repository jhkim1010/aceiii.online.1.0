---
phase: 18-ag-grid-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/src/components/table/grid-types.ts
  - ventago-app/package.json
autonomous: true
requirements: [GRID-01]

must_haves:
  truths:
    - "grid-types.ts가 ag-grid-community ColDef 기반 타입을 export한다"
    - "GridRenderCellParams 인터페이스에 row, value, data 속성이 포함된다"
    - "GridColDef 타입이 renderCell, renderHeader, align, headerAlign MUI 호환 속성을 허용한다"
    - "@mui/x-data-grid가 package.json에서 완전히 제거된다"
    - "npm run lint가 에러 없이 통과한다"
    - "npx next build가 성공한다"
  artifacts:
    - path: "ventago-app/src/components/table/grid-types.ts"
      provides: "AG Grid 네이티브 타입 + MUI 호환 확장"
      contains: "ColDef"
    - path: "ventago-app/package.json"
      provides: "@mui/x-data-grid 제거된 의존성 목록"
  key_links:
    - from: "ventago-app/src/components/table/grid-types.ts"
      to: "ag-grid-community"
      via: "import type { ColDef }"
      pattern: "import.*ColDef.*ag-grid-community"
    - from: "58개 DataConfig/View 파일"
      to: "ventago-app/src/components/table/grid-types.ts"
      via: "import { GridColDef, GridRenderCellParams }"
      pattern: "from.*grid-types"
---

<objective>
AG Grid 마이그레이션 완료: 타입 심(shim) 교체 + MUI DataGrid 패키지 제거

FullTable.tsx는 이미 AG Grid Community 35.2.1 기반으로 완전 전환 완료.
adaptColumns() 어댑터가 MUI renderCell/params.row 패턴을 AG Grid cellRenderer/params.data로 자동 변환 중.
남은 작업은 두 가지뿐:
1. grid-types.ts의 @mui/x-data-grid re-export를 AG Grid 네이티브 타입으로 교체
2. @mui/x-data-grid 패키지를 package.json에서 제거

Purpose: MUI DataGrid 의존성을 완전히 제거하여 번들 크기 감소 + 타입 시스템 정합성 확보
Output: grid-types.ts (AG Grid 네이티브), package.json (@mui/x-data-grid 제거됨)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ag-grid-migration-mui-datagrid-ag-grid-community/18-RESEARCH.md

@ventago-app/src/components/table/grid-types.ts
@ventago-app/src/components/table/FullTable.tsx
@ventago-app/src/components/table/columns.tsx
@ventago-app/package.json

<interfaces>
<!-- grid-types.ts 현재 상태 (교체 대상) -->
From ventago-app/src/components/table/grid-types.ts:
```typescript
export type { GridColDef, GridRenderCellParams } from '@mui/x-data-grid'
```

<!-- 58개 파일이 이 경로로 import -->
<!-- 교체 후에도 GridColDef, GridRenderCellParams 이름 유지 필수 -->

<!-- FullTable adaptColumns가 주입하는 params 구조 -->
From ventago-app/src/components/table/FullTable.tsx (adaptColumns 내부):
```typescript
// renderCell → cellRenderer 래핑 시:
col.renderCell({ ...params, row: params.data, value: params.value })
```

<!-- columns.tsx에서 사용하는 params.row 패턴 -->
From ventago-app/src/components/table/columns.tsx:
```typescript
export const dateTimeColumn = (params: GridRenderCellParams, field = 'createdAt') => {
  const date = DateTime.fromISO(params.row[field])...
}
export const actionsColumn = (actions: any) => ({
  renderCell: (params: GridRenderCellParams) => actions(params.row)
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: grid-types.ts를 AG Grid 네이티브 타입으로 교체</name>
  <files>ventago-app/src/components/table/grid-types.ts</files>
  <action>
grid-types.ts 파일을 다음 내용으로 교체한다. 순서가 중요하다 — 이 파일을 먼저 교체해야 package.json에서 @mui/x-data-grid를 제거할 수 있다.

```typescript
// AG Grid 네이티브 타입 기반 (Phase 18 마이그레이션 완료)
// 58개 DataConfig/View 파일이 이 경로로 GridColDef, GridRenderCellParams를 import

import type { ColDef } from 'ag-grid-community'

// MUI DataGrid 호환 확장 — renderCell/renderHeader/align/headerAlign 허용
// FullTable.tsx의 adaptColumns() 어댑터가 renderCell → cellRenderer로 자동 변환
export type GridColDef = ColDef & {
  renderCell?: (params: GridRenderCellParams) => any
  renderHeader?: (params: any) => any
  align?: 'left' | 'center' | 'right'
  headerAlign?: 'left' | 'center' | 'right'
}

// adaptColumns가 { row: params.data, value: params.value } 주입
// columns.tsx 및 DataConfig 파일에서 params.row 패턴 유지
export interface GridRenderCellParams {
  row: any
  value: any
  data?: any
  field?: string
  [key: string]: any
}
```

ESLint 주의:
- 모든 `//` 주석 위에 빈 줄 필요 (lines-around-comment)
- return 문 위에 빈 줄 필요 (newline-before-return)
- 미사용 import 없어야 함 (no-unused-vars)

교체 후 즉시 ESLint 검증:
```bash
cd ventago-app && npx eslint src/components/table/grid-types.ts --no-error-on-unmatched-pattern
```
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/components/table/grid-types.ts src/components/table/columns.tsx --no-error-on-unmatched-pattern 2>&1 | tail -20</automated>
  </verify>
  <done>
- grid-types.ts가 ag-grid-community의 ColDef를 import한다
- GridColDef 타입이 ColDef + renderCell/renderHeader/align/headerAlign 확장을 export한다
- GridRenderCellParams 인터페이스가 row, value, data, field 속성을 export한다
- @mui/x-data-grid import가 grid-types.ts에 없다
- ESLint 에러 없음
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: @mui/x-data-grid 패키지 제거 + 빌드 검증</name>
  <files>ventago-app/package.json</files>
  <action>
Task 1 완료 후 실행. grid-types.ts가 더 이상 @mui/x-data-grid를 참조하지 않으므로 안전하게 제거 가능.

1. ventago-app/package.json에서 두 줄 제거:
   - dependencies 섹션: `"@mui/x-data-grid": "^6.0.3",` (약 50번 줄)
   - overrides 섹션: `"@mui/x-data-grid/@mui/system": "5.12.1",` (약 141번 줄)

2. npm install 실행 (lock 파일 갱신 + node_modules 정리):
   ```bash
   cd ventago-app && npm install
   ```

3. @mui/x-data-grid가 완전히 제거되었는지 확인:
   ```bash
   npm ls @mui/x-data-grid 2>&1
   ```
   "(empty)" 또는 "not found" 결과 예상

4. 전체 빌드 검증:
   ```bash
   cd ventago-app && npx next build 2>&1 | tail -50
   ```

5. 빌드 실패 시 대처:
   - TypeScript 타입 에러 → grid-types.ts의 GridColDef/GridRenderCellParams 정의 보완
   - ESLint no-unused-vars → 해당 파일에서 미사용 import 제거
   - 특정 DataConfig 파일에서 AG Grid ColDef에 없는 MUI 전용 속성 사용 시 → GridColDef 타입에 해당 속성 추가

주의: npm workspaces 모노레포이므로 ventago-app 디렉토리에서 npm install 실행할 것.
ESLint 규칙: newline-before-return, lines-around-comment, no-unused-vars 특히 주의.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npm ls @mui/x-data-grid 2>&1 | head -5 && npm run lint 2>&1 | tail -10 && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- package.json에 @mui/x-data-grid 의존성이 없다
- package.json overrides에 @mui/x-data-grid/@mui/system이 없다
- npm ls @mui/x-data-grid가 "empty" 또는 에러를 반환한다
- npm run lint가 에러 없이 통과한다
- npx next build가 성공한다
- 58개 DataConfig/View 파일이 grid-types.ts 경유로 정상 빌드된다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 타입 시스템 | grid-types.ts 교체 시 58개 소비 파일의 타입 호환성 |
| 패키지 의존성 | @mui/x-data-grid 제거 시 다른 MUI 패키지에 영향 없음 (독립) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | grid-types.ts 타입 불일치 | mitigate | GridColDef에 renderCell/align/headerAlign MUI 호환 속성 포함, adaptColumns 어댑터가 런타임 변환 |
| T-18-02 | Denial of Service | 빌드 실패 | mitigate | grid-types.ts 먼저 교체 후 package.json 수정 순서 엄수, 빌드 검증 |
</threat_model>

<verification>
1. grid-types.ts가 ag-grid-community에서 ColDef를 import하는지 확인
2. @mui/x-data-grid가 package.json과 node_modules에서 완전 제거됐는지 확인
3. npm run lint 통과
4. npx next build 성공
5. 코드베이스에서 @mui/x-data-grid import가 grid-types.ts 외에 없는지 grep 확인
</verification>

<success_criteria>
- @mui/x-data-grid 의존성이 프로젝트에서 완전히 제거됨
- grid-types.ts가 AG Grid 네이티브 타입 기반으로 동작
- 58개 소비 파일이 import 경로 변경 없이 정상 빌드
- ESLint + TypeScript 빌드 모두 통과
</success_criteria>

<output>
After completion, create `.planning/phases/18-ag-grid-migration-mui-datagrid-ag-grid-community/18-01-SUMMARY.md`
</output>
