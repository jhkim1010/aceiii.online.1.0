---
phase: 18-ag-grid-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/package.json
  - ventago-app/src/components/table/FullTable.tsx
  - ventago-app/src/components/table/columns.tsx
autonomous: true
requirements: [GRID-01]

must_haves:
  truths:
    - "ag-grid-community + ag-grid-react 패키지 설치됨"
    - "FullTable 래퍼가 AG Grid 기반으로 동작하며 기존 props 인터페이스 유지"
    - "columns.tsx의 renderCell 헬퍼가 AG Grid cellRenderer 패턴으로 동작"
    - "스페인어 로컬라이제이션 유지"
    - "서버사이드 페이지네이션 + 클라이언트사이드 정렬 유지"
    - "체크박스 선택, 행 클릭, 로딩 상태 기존대로 동작"
    - "컬럼 리사이즈(드래그) 가능"
---

<objective>
Wave 1: AG Grid 핵심 래퍼 교체

1. **패키지 설치**: ag-grid-community + ag-grid-react 설치
2. **FullTable.tsx 교체**: MUI DataGrid → AG Grid Community
   - 기존 props 인터페이스 100% 유지 (data, columns, paginationModel, rowSelected, setPagination 등)
   - GridColDef → AG Grid ColDef 내부 변환 어댑터
   - 서버사이드 페이지네이션 유지
   - 클라이언트사이드 정렬 유지
   - 체크박스 선택 유지
   - 스페인어 로케일
   - 컬럼 리사이즈 활성화 (AG Grid Community 기본 지원)
   - autoHeight / fillHeight 모드 유지

3. **columns.tsx 마이그레이션**: renderCell → cellRenderer 어댑터
   - GridRenderCellParams → AG Grid ICellRendererParams 변환
   - actionsColumn 헬퍼 AG Grid 호환
   - 기타 23+ 헬퍼 함수는 JSX 반환이므로 cellRenderer에서 그대로 사용 가능

4. **AG Grid CSS import**: ag-grid-community/styles 임포트

주의: 이 Wave에서는 DataConfig 파일(51개)의 타입은 변경하지 않음. FullTable 내부에서 GridColDef → ColDef 변환 어댑터로 처리.
</objective>
