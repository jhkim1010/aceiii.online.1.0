---
phase: 26-gastos-categoria-tree-n-niveles
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - api-ventago/migrations/26-05-step5-cleanup.sql
  - api-ventago/src/app/expenses/expenses.model.ts
  - api-ventago/src/app/expenses/expenses.module.ts
  - api-ventago/src/app/expenses/categories/expenses-categories.module.ts
  - api-ventago/src/app/expenses/subcategories/expenses-subcategories.module.ts
  - api-ventago/src/app/app.module.ts
  - .planning/PROJECT.md
autonomous: false
requirements:
  - REQ-5
  - REQ-6
  - REQ-7
  - SC-11
must_haves:
  truths:
    - "운영 서버 PG10 에 4단계 마이그레이션이 무중단으로 적용됨"
    - "운영 데이터 무결성 검증 통과 (백업 vs 마이그레이션 후 행 수 일치, orphan 0)"
    - "Wave 3+4 프론트엔드 빌드가 운영에 배포되어 사용자 트래픽이 새 트리 모델 사용"
    - "2주 모니터링 윈도우 동안 expense_categories 관련 5xx 0건"
    - "윈도우 종료 후 deprecated 테이블/컬럼 drop 완료"
    - "PROJECT.md 의 핵심 결정 로그에 Phase 26 완료 기록"
  artifacts:
    - path: "api-ventago/migrations/26-05-step5-cleanup.sql"
      provides: "deprecated drop SQL (2주 후 실행)"
      contains: "DROP TABLE expenses_subcategories"
    - path: ".planning/PROJECT.md"
      provides: "Phase 26 완료 기록 + 핵심 결정"
      contains: "Phase 26 완료"
  key_links:
    - from: "운영 PG10"
      to: "step1-step4 SQL"
      via: "SSH + sudo postgres psql"
      pattern: "ssh jhkim-server.*psql -d ventago"
    - from: "Jenkins front-coolsistema"
      to: "ventago-app build"
      via: "Wave 3+4 코드 push"
      pattern: "front-coolsistema"
---

<objective>
Phase 26 Wave 5 — 운영 마이그레이션 + 2주 모니터링 + cleanup. 이 wave 가 끝나면: 운영 데이터가 새 트리 모델로 변환되었고, 사용자 트래픽이 새 UI 를 사용 중이며, 모니터링 윈도우 후 deprecated 자산 모두 정리됨. Phase 26 완전 종결.

Purpose: 코드 변경의 가치를 운영 환경에서 실현. 안전한 cutover (트랜잭션 마이그레이션 + 2주 롤백 윈도우 + 데이터 손실 0건 검증) 보장. CLAUDE.md SSH 규칙 + Postgres MCP 규칙 준수.

Output: 1개 cleanup SQL + 모델/모듈 정리 (Wave 4 까지 살아있던 ExpensesSubcategory 흔적 제거) + PROJECT.md 업데이트.

⚠️ 이 wave 는 코드 작성보다 **운영 배포 + 모니터링 + 의사결정** 비중이 큼. 대부분의 task 는 사용자 (개발자) 와 협업 (checkpoint).

Covers: REQ-5/REQ-6 (deprecated 테이블 drop), REQ-7 (옛 모델 삭제), SC-11 (운영 데이터 손실 0건 검증).
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.planning/phases/26-gastos-categoria-tree-n-niveles/26-01-SUMMARY.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.planning/phases/26-gastos-categoria-tree-n-niveles/26-02-SUMMARY.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.planning/phases/26-gastos-categoria-tree-n-niveles/26-03-SUMMARY.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.planning/phases/26-gastos-categoria-tree-n-niveles/26-04-SUMMARY.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/CLAUDE.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/migrations/26-01-step1-schema.sql
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/migrations/26-02-step2-data.sql
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/migrations/26-03-step3-fk.sql
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/migrations/26-04-step4-verify.sql
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/api-ventago/migrations/26-99-rollback.sql

<interfaces>
운영 환경 (CLAUDE.md):
- SSH alias: jhkim-server  (62.72.7.245, IdentityFile ~/.ssh/id_ed25519)
- PG: 호스트 OS 의 PostgreSQL 10 (Docker 아님), pgbouncer 5432 프록시
- DB: ventago, owner: coolsistema
- 운영 매장: CART(3), coolsistema(6), genius(8), ACE(9)
- DDL 은 사용자 확인 필수 (CLAUDE.md SSH 규칙)

배포:
- API: api-coolsistema Jenkins job (NestJS Docker 빌드)
- 프론트: front-coolsistema Jenkins job (Next.js Docker 빌드)
- print-agent / zebra-agent: 이번 phase 변경 없음

PG10 호환성:
- step1~step3 SQL 이미 PG10 검증됨 (Wave 1 에서 EXECUTE PROCEDURE, SERIAL 사용)
- step5-cleanup 도 PG10 호환 작성 (DROP TABLE / DROP COLUMN — 표준 DDL)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 26-05-01: 운영 백업 (사용자 직접 실행)</name>
  <what-built>
- Wave 1~4 의 모든 코드/마이그레이션
  </what-built>
  <how-to-verify>
운영 마이그레이션 직전, 사용자가 직접 백업 수행. Claude 가 자동 실행하지 않음 — 데이터 보호의 마지막 안전장치.

```bash
# 사용자 작업 (Claude 가 사용자에게 보여줄 명령어):

# 1) 운영 PG 의 expenses_categories, expenses_subcategories, expenses 테이블 백업
ssh jhkim-server "sudo -u postgres pg_dump -d ventago \
  -t expenses_categories -t expenses_subcategories -t expenses \
  --data-only \
  > /var/backups/ventago/phase26-pre-migration-$(date +%Y%m%d-%H%M).sql"

# 2) 백업 파일 크기 + 행 수 확인
ssh jhkim-server "ls -lh /var/backups/ventago/phase26-pre-migration-*.sql | tail -1"
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"SELECT 'cat' AS t, COUNT(*) FROM expenses_categories UNION ALL SELECT 'sub', COUNT(*) FROM expenses_subcategories UNION ALL SELECT 'exp', COUNT(*) FROM expenses;\""

# 3) 로컬에 백업 복사 (옵션, 추가 안전망)
scp jhkim-server:/var/backups/ventago/phase26-pre-migration-*.sql ~/backup/
```

체크리스트:
- [ ] 백업 파일이 적어도 100KB 이상 (데이터 있음)
- [ ] expenses_categories, expenses_subcategories, expenses 행 수 기록 (마이그레이션 후 비교용)
- [ ] 백업 파일을 운영 서버 외부 (로컬 머신) 에도 복사
  </how-to-verify>
  <resume-signal>"backup done" + 3개 테이블 행 수 (예: cat=24, sub=87, exp=4521)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 26-05-02: 운영 마이그레이션 step1~step4 실행</name>
  <what-built>
- 4개 idempotent SQL 파일 (Wave 1 산출물)
- 로컬 PG15 검증 완료
  </what-built>
  <how-to-verify>
사용자 동의 후 운영 PG10 에 SSH 로 SQL 실행. CLAUDE.md SSH 규칙: DDL 은 사용자 확인 필수.

Claude 가 사용자에게 보여줄 실행 plan:

```
실행할 SQL 파일 4개:
  step1: api-ventago/migrations/26-01-step1-schema.sql  (DDL — 새 테이블/트리거/인덱스)
  step2: api-ventago/migrations/26-02-step2-data.sql    (DML — INSERT + UPDATE expenses.category_id)
  step3: api-ventago/migrations/26-03-step3-fk.sql      (DDL — FK + 인덱스)
  step4: api-ventago/migrations/26-04-step4-verify.sql  (READ-ONLY — 검증)

영향 범위:
  - 새 테이블 expense_categories 생성
  - expenses 테이블에 category_id 컬럼 추가 + 백필
  - 기존 expenses_categories / expenses_subcategories 변경 없음 (그대로 유지)
  - expenses_subcategory_id 컬럼은 살아있음 (Wave 5 cleanup 까지)

예상 영향 행:
  - expense_categories INSERT: cat({CAT_COUNT}) + sub({SUB_COUNT}) = ~{TOTAL} 행
  - expenses UPDATE category_id: ~{EXP_WITH_SUBCAT_COUNT} 행
```

사용자 승인 후 실행:

```bash
# step1 (DDL)
ssh jhkim-server "sudo -u postgres psql -d ventago" < api-ventago/migrations/26-01-step1-schema.sql

# step2 (DML — 시간 좀 걸림, 매장 4개 × 카테고리 + 매장당 expense backfill)
ssh jhkim-server "sudo -u postgres psql -d ventago" < api-ventago/migrations/26-02-step2-data.sql

# step3 (DDL — 짧음)
ssh jhkim-server "sudo -u postgres psql -d ventago" < api-ventago/migrations/26-03-step3-fk.sql

# step4 (READ-ONLY 검증)
ssh jhkim-server "sudo -u postgres psql -d ventago" < api-ventago/migrations/26-04-step4-verify.sql
```

각 step 의 출력에서 ERROR / EXCEPTION / NOTICE 메시지 확인. step4 결과 출력을 사용자에게 보여줘야 함:
- roots_match: old_count == new_count
- children_match: old_count == new_count
- orphan_expenses: 0
- path_depth_integrity: 0
- cycle_check: 0
- map_coverage: old_total == map_total

⚠️ step2 가 실패하면 트랜잭션이 롤백되므로 부분 적용 위험 없음. 단 step1 만 적용된 채 step2 실패면 expense_categories 테이블만 빈 채 남음 — 사용자가 26-99-rollback.sql 실행해 깨끗이 정리 후 재시도.

체크리스트:
- [ ] 4개 step 모두 ERROR 없이 완료
- [ ] step4 의 6개 검증 쿼리 모두 통과
- [ ] 매장별 카테고리 수가 백업의 행 수와 일치
- [ ] expenses 의 category_id 백필 비율이 expensesSubcategoryId NOT NULL 비율과 일치
  </how-to-verify>
  <resume-signal>"approved + verified" + step4 출력 캡처. 또는 "rollback needed: ..." 로 문제 상황 보고.</resume-signal>
</task>

<task type="auto">
  <name>Task 26-05-03: API + 프론트엔드 운영 배포</name>
  <files>(no new files — 배포만)</files>
  <action>
Wave 2~4 의 코드를 운영에 배포. CLAUDE.md push-both.sh 규칙:

```bash
cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0
./push-both.sh
```

이 스크립트가:
1. api-ventago 변경사항 push → Jenkins api-coolsistema 빌드 트리거
2. ventago-app 변경사항 push → Jenkins front-coolsistema 빌드 트리거
3. print-agent / zebra-agent 변경 없으면 build 안 함

빌드 모니터링:
```bash
# Jenkins UI 또는 SSH 로 확인
ssh jhkim-server "docker ps | grep -E 'api_ventago|ventago_app'"
ssh jhkim-server "docker logs api_ventago --tail 50"
ssh jhkim-server "docker logs ventago_app --tail 50"
```

배포 후 sanity:
```bash
# API: tree 엔드포인트 응답 확인
curl -s "https://newapi.coolsistema.com/api/expense-categories/tree" -H "Authorization: Bearer ${TOKEN}" | head -100

# 프론트: 페이지 로드 확인 (수동 — 사용자가 브라우저로)
# https://ventago.coolsistema.com/configuracion/categorias-gastos
```

빌드 실패 시: CLAUDE.md 의 "빌드 실패 시 로그 파일(#NNN.txt)을 분석해 에러 수정 후 push" 패턴 따라 처리.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://newapi.coolsistema.com/api/expense-categories/tree -H "Authorization: Bearer dummy" | grep -E "401|200" && echo "API responding"</automated>
  </verify>
  <done>
- push-both.sh 실행 완료
- Jenkins api-coolsistema, front-coolsistema 빌드 모두 성공
- API 컨테이너 health check 정상 (401 응답이면 살아있는 것 — 우리가 보낸 토큰이 dummy 라서)
- 프론트엔드 컨테이너 health check 정상
- /configuracion/categorias-gastos 페이지가 admin 으로 정상 로드
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 26-05-04: 운영 회귀 검증 (admin + vendedor 흐름)</name>
  <what-built>
- 운영 환경 전체 (DB + API + 프론트엔드)
  </what-built>
  <how-to-verify>
사용자가 운영 환경에서 직접 회귀 테스트.

체크리스트 (admin 계정):
- [ ] /configuracion/categorias-gastos 페이지 로드 OK
- [ ] 기존 매장 카테고리 트리 표시 (Servicios > Internet 같은 path 가 보임 — 마이그레이션이 정상 변환)
- [ ] [Nueva categoría raíz] 로 신규 루트 생성 → 트리에 즉시 반영
- [ ] 기존 카테고리 [+] 자식 추가 → 정상 동작
- [ ] 인라인 rename (한글 입력) → IME 안전
- [ ] 같은 부모 D&D 정렬 → 정상
- [ ] [Mover] 다이얼로그 다른 부모 이동 → 정상
- [ ] 자식 있는 카테고리 [Delete] → 3 policy 라디오 → cascade 선택 → 자손 archived
- [ ] [Mostrar archivados] 토글 → archived 노드 dim 표시 + Restaurar 동작

체크리스트 (vendedor 계정):
- [ ] 사이드바에 Categorías de Gastos 항목 안 보임 (또는 페이지 접근 시 403/redirect)
- [ ] /gastos 페이지 또는 비용 등록 모달에서:
  - [ ] CategoryTreeSelector 가 보임 (기존 2단 select 대신)
  - [ ] 어느 깊이의 노드든 선택 가능
  - [ ] MRU "Recientes" 가 두 번째 등록부터 동작
  - [ ] inline create 버튼이 검색 결과 없을 때 표시 → 클릭 → 정상 생성
- [ ] /gastos 리스트 페이지에서 카테고리 컬럼이 path 한 줄로 표시
- [ ] 비용 등록/편집/삭제 정상 동작 (회귀 0)

체크리스트 (reports — admin):
- [ ] GastoCockpit 페이지 정상 로드
- [ ] 기존 top-N flat 차트 정상 (회귀 0)
- [ ] 신규 "Gastos por categoría" 카드 표시 + depth 토글 동작
- [ ] depth=1 → 모든 expense 가 루트로 롤업, 합계 정확
- [ ] depth=전체 → leaf 그대로, 행 가장 많음
- [ ] 합계 % 가 정확히 100%

체크리스트 (시스템):
- [ ] `ssh jhkim-server "docker logs api_ventago --tail 200 | grep -iE 'error|exception'"` 으로 5xx 0건 확인
- [ ] `ssh jhkim-server "sudo -u postgres psql -d ventago -c \"SELECT COUNT(*) FROM expenses WHERE category_id IS NULL\""` 으로 NULL 비율 비정상 증가 없는지 확인 (기존에 expensesSubcategoryId NULL 이었던 비율과 일치해야 함)
- [ ] 새 매장 가입 테스트 (테스트 계정으로) → 6개 기본 카테고리 시드 자동 적용

⚠️ 운영 회귀에서 critical 이슈 발견 시:
1. 26-99-rollback.sql 을 사용자 확인 받고 실행 (CLAUDE.md DDL 규칙)
2. 프론트엔드/API 도 직전 git tag 로 redeploy
3. 백업 SQL 로 데이터 복원 검증
  </how-to-verify>
  <resume-signal>"approved" 또는 "issues found: ..." 로 발견된 문제 보고. critical 이면 즉시 rollback.</resume-signal>
</task>

<task type="auto">
  <name>Task 26-05-05: 2주 모니터링 (백그라운드)</name>
  <files>(no new files — 모니터링만)</files>
  <action>
2주 동안 운영 환경의 expense_categories 관련 에러/이슈 모니터링.

매일 또는 며칠 간격으로 사용자가 (또는 Claude 도움받아) 체크:

```bash
# 1) API 에러 로그
ssh jhkim-server "docker logs api_ventago --since 24h 2>&1 | grep -iE 'expense_categor|expense-categor|category_id' | tail -50"

# 2) Sequelize Database Error 검색 (트리거 RAISE EXCEPTION 이 올라온 경우)
ssh jhkim-server "docker logs api_ventago --since 24h 2>&1 | grep -iE 'SequelizeDatabaseError|RAISE EXCEPTION|cycle|depth.*exceeds'"

# 3) DB 무결성 재확인 (step4 검증 쿼리 일부 재실행)
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT
    (SELECT COUNT(*) FROM expenses WHERE category_id IS NOT NULL) AS rebound,
    (SELECT COUNT(*) FROM expenses e LEFT JOIN expense_categories ec ON ec.id=e.category_id WHERE e.category_id IS NOT NULL AND ec.id IS NULL) AS orphans,
    (SELECT COUNT(*) FROM expense_categories WHERE depth>5 OR depth<0) AS bad_depth,
    (SELECT MAX(depth) FROM expense_categories) AS max_depth
\""

# 4) 사용자 피드백 수집 (수동 — feedback log 또는 chat 등)
```

문제 발견 시:
- 즉시 GitHub issue / feedback log 에 기록
- critical 이면 26-99-rollback.sql 실행 옵션 검토
- 비-critical 이면 hot-fix patch (별도 plan)

OK 신호:
- 14일 동안 5xx 0건
- expense_categories 관련 SequelizeDatabaseError 0건 (트리거 RAISE 도 service 의 사전 검증으로 차단되어야 함 — 만약 RAISE 가 올라오면 Wave 2 의 사전 검증 누락이라는 뜻 → bug fix 필요)
- 사용자 피드백 0건 또는 minor only

⚠️ 이 task 는 시간 기반이라 자동화하지 않음. 사용자가 14일 후 다음 task 로 진행.
  </action>
  <verify>
    <automated>echo "Manual monitoring task - see task description for daily checks"</automated>
  </verify>
  <done>
- 14일 모니터링 완료
- 모니터링 결과 요약 작성 (별도 Markdown 또는 SUMMARY 에 기록)
- critical 이슈 0건 또는 모두 해결됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 26-05-06: Cleanup SQL 작성 + 사용자 승인</name>
  <what-built>
- 2주 모니터링 완료, 안정성 확인
- 운영 데이터가 새 트리 모델로만 사용되고 있음
  </what-built>
  <how-to-verify>
이 task 에서 cleanup SQL 작성. 다음 task 에서 운영 실행.

작성할 파일: `api-ventago/migrations/26-05-step5-cleanup.sql`

내용:
```sql
-- Phase 26 Wave 5 — Cleanup (deprecated 자산 drop)
-- ⚠️ 실행 후 롤백 불가능. 운영 마이그레이션 후 2주 모니터링 통과한 후에만 실행.
-- ⚠️ 사용자 명시적 승인 후 실행.

BEGIN;

-- 1) expenses 테이블에서 expenses_subcategory_id 컬럼 drop
--    (Wave 4 부터 새 INSERT 는 NULL 만 사용)
ALTER TABLE expenses DROP COLUMN IF EXISTS expenses_subcategory_id;

-- 2) deprecated 테이블 drop (백업 + _phase26_cat_map 으로 복구 가능 시점은 끝남)
DROP TABLE IF EXISTS expenses_subcategories CASCADE;
DROP TABLE IF EXISTS expenses_categories CASCADE;

-- 3) 매핑 테이블 drop (이제 불필요)
DROP TABLE IF EXISTS _phase26_cat_map;

COMMIT;

-- 검증 쿼리:
-- SELECT to_regclass('public.expenses_categories');     -- NULL 이어야 함
-- SELECT to_regclass('public.expenses_subcategories');  -- NULL 이어야 함
-- SELECT column_name FROM information_schema.columns
--  WHERE table_name='expenses' AND column_name='expenses_subcategory_id';  -- 0 행
```

⚠️ CASCADE 사용 주의: expenses_categories 에 다른 FK 가 매달려 있다면 그것도 drop 됨. 실행 전 다음 쿼리로 의존성 확인:
```sql
SELECT conname, conrelid::regclass FROM pg_constraint
 WHERE confrelid IN ('expenses_categories'::regclass, 'expenses_subcategories'::regclass);
```
expenses 테이블의 옛 expensesSubcategoryId FK 외에 다른 의존성이 있다면 별도 처리.

사용자 승인 체크리스트:
- [ ] step5 SQL 파일 검토 완료
- [ ] 의존성 쿼리 결과 검토 (다른 FK 없는지 확인)
- [ ] 백업 파일이 여전히 안전한 위치에 존재 (만일을 위해)
- [ ] 14일 모니터링 결과 OK
- [ ] cleanup 실행 승인
  </how-to-verify>
  <resume-signal>"approved cleanup" 또는 "delay 1 week more" 또는 "rollback instead"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 26-05-07: 운영 cleanup SQL 실행</name>
  <what-built>
- 26-05-step5-cleanup.sql 작성됨, 사용자 승인 완료
  </what-built>
  <how-to-verify>
사용자 동의 하에 운영 PG10 에서 cleanup SQL 실행.

```bash
# CLAUDE.md SSH 규칙 — DDL 은 사용자 확인 후 실행. DROP TABLE 은 destructive.
ssh jhkim-server "sudo -u postgres psql -d ventago" < api-ventago/migrations/26-05-step5-cleanup.sql
```

검증:
```bash
ssh jhkim-server "sudo -u postgres psql -d ventago -c \"
  SELECT
    to_regclass('public.expenses_categories') AS old_cat,
    to_regclass('public.expenses_subcategories') AS old_sub,
    (SELECT COUNT(*) FROM information_schema.columns
      WHERE table_name='expenses' AND column_name='expenses_subcategory_id') AS old_col_exists
\""
```

기대 결과: old_cat = NULL, old_sub = NULL, old_col_exists = 0.

⚠️ 이 시점부터 데이터 복구 불가. 백업 SQL 만이 마지막 안전장치.

체크리스트:
- [ ] cleanup SQL 실행 ERROR 없이 완료
- [ ] 위 검증 쿼리 통과
- [ ] API 컨테이너 재시작 불필요 (Sequelize 가 expense_categories 만 사용 중이므로)
  </how-to-verify>
  <resume-signal>"cleanup verified" + 검증 쿼리 출력</resume-signal>
</task>

<task type="auto">
  <name>Task 26-05-08: 옛 모델/모듈 삭제 + 코드 클린업</name>
  <files>api-ventago/src/app/expenses/expenses.model.ts, api-ventago/src/app/expenses/expenses.module.ts, api-ventago/src/app/expenses/categories/expenses-categories.module.ts, api-ventago/src/app/expenses/subcategories/expenses-subcategories.module.ts, api-ventago/src/app/app.module.ts</files>
  <action>
운영 cleanup 후 코드에서도 옛 모델/모듈 흔적 제거.

1) **`expenses.model.ts`** — `expensesSubcategoryId`, `expensesSubcategory` 관련 라인 삭제:

```ts
// 삭제할 라인:
import { ExpensesSubcategories } from './subcategories/expenses-subcategories.model';

@ForeignKey(() => ExpensesSubcategories)
@Column({ type: DataType.INTEGER, allowNull: true })
expensesSubcategoryId: number;

@BelongsTo(() => ExpensesSubcategories)
expensesSubcategory: ExpensesSubcategories;
```

남기는 (Wave 4 에서 추가됨):
```ts
@ForeignKey(() => ExpenseCategory)
@Column({ type: DataType.INTEGER, allowNull: true })
categoryId: number | null;

@BelongsTo(() => ExpenseCategory, { foreignKey: 'categoryId', as: 'expenseCategory' })
expenseCategory: ExpenseCategory | null;
```

2) **`expenses.module.ts`** — ExpensesSubcategories / ExpensesCategories import 제거:

```ts
// 제거:
SequelizeModule.forFeature([..., ExpensesSubcategories, ExpensesCategories])
ExpensesCategoriesModule, ExpensesSubcategoriesModule

// 유지/추가 (Wave 4 에서):
SequelizeModule.forFeature([..., ExpenseCategory])
ExpenseCategoryModule
```

3) **`app.module.ts`** — ExpensesCategoriesModule, ExpensesSubcategoriesModule import 제거.

4) **`api-ventago/src/app/expenses/categories/`** + **`api-ventago/src/app/expenses/subcategories/`** 디렉토리 통째 삭제:
```bash
rm -rf api-ventago/src/app/expenses/categories
rm -rf api-ventago/src/app/expenses/subcategories
```

5) **프론트엔드** — 기존 옛 카테고리 관리 페이지/뷰가 있다면 정리:
```bash
# 검색해서 확인
grep -rn "expensesSubcategory\|ExpensesSubcategor" ventago-app/src --include="*.ts" --include="*.tsx"
grep -rn "expenses/categories\|expenses/subcategories" ventago-app/src --include="*.ts" --include="*.tsx"
```

발견된 코드 모두 정리. 사용 위치:
- 옛 useCategoriesByStore 훅 (gastos 전용) → 삭제 또는 새 훅으로 대체
- 옛 ExpenseModal subcategory select 코드 잔여물 → 제거
- ExpensesListView 의 categoryName/subcategoryName 컬럼 정의 잔여 → 제거

6) **빌드 + 테스트**:
```bash
cd api-ventago && npm run build && npx jest --runInBand
cd ../ventago-app && npm run build && npm run lint
```

⚠️ TypeScript 컴파일 에러로 잔여 의존성을 발견 — 모두 정리.

7) **로컬 DB 도 cleanup 적용** (선택, 정합성 위해):
```bash
docker exec -i dbpostgres psql -U coolsistema -d ventago < api-ventago/migrations/26-05-step5-cleanup.sql
```

8) **푸시 + 배포**:
```bash
./push-both.sh
```

⚠️ 운영 cleanup SQL 이 이미 적용되었으므로 옛 모델이 없는 코드가 운영에 배포되어도 안전 (실제로는 옛 모델 import 가 운영에서 startup error 일으키지 않으므로 어느 순서든 OK이지만, cleanup SQL → 코드 deploy 순서가 더 깔끔).
  </action>
  <verify>
    <automated>cd api-ventago && grep -rn "ExpensesSubcategor\|expensesSubcategoryId" src --include="*.ts" 2>/dev/null | wc -l; cd ../ventago-app && grep -rn "expensesSubcategory\|ExpensesSubcategor" src --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l</automated>
  </verify>
  <done>
- api-ventago/src/app/expenses/categories/ 디렉토리 삭제됨
- api-ventago/src/app/expenses/subcategories/ 디렉토리 삭제됨
- expenses.model.ts 에 expensesSubcategoryId 흔적 0
- 위 검증 명령 결과 모두 0 (api-ventago, ventago-app 양쪽)
- npm run build 양쪽 성공
- 운영 배포 후 health check 정상
  </done>
</task>

<task type="auto">
  <name>Task 26-05-09: PROJECT.md 업데이트 + Phase 26 완료 기록</name>
  <files>.planning/PROJECT.md</files>
  <action>
.planning/PROJECT.md (또는 RETROSPECTIVE.md) 의 핵심 결정 로그 또는 완료 phase 섹션에 Phase 26 추가.

기록할 내용:
- Phase 26 완료일
- 주요 결정 5건 요약 (locked decisions L1~L4 + Discuss-phase D1~D4)
- 도입된 새 기술: react-arborist
- 마이그레이션 결과 (백업 vs 마이그레이션 후 행 수)
- 모니터링 결과 (5xx 건수, 사용자 피드백)
- 다음 마일스톤에서 다룰 deferred items (CONTEXT §5):
  - Color/Icon 편집 UI 풍부도
  - Bulk operations
  - CSV/Excel 카테고리 트리 import
  - 카테고리 변경 audit 강화
  - Hard delete UI 노출

PROJECT.md 의 기존 섹션 구조에 맞춰 추가. 기존에 없으면 새 섹션 ## Phase 26 — Gastos N차 카테고리 트리 (완료).

git 커밋 메시지 (CLAUDE.md 스타일):
```
docs(phase-26): mark Phase 26 complete

- 4단계 마이그레이션 운영 적용 완료 (백업 row count 일치)
- 2주 모니터링 통과 (5xx 0건)
- deprecated 테이블 drop 완료
- 6개 기본 카테고리 신규 매장 시드 동작 확인
```

또한 ROADMAP.md 의 Phase 26 entry 를 [x] 완료 마킹 (해당 라인의 체크박스가 있다면).
  </action>
  <verify>
    <automated>grep -q "Phase 26" .planning/PROJECT.md && grep -q "완료\|Complete\|complete" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <done>
- PROJECT.md 에 Phase 26 완료 섹션 추가
- 결정/결과/deferred items 모두 기록
- git 커밋 완료
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 머신 → SSH → 운영 서버 | CLAUDE.md SSH 규칙 — DDL 은 사용자 확인 필수. id_ed25519 키 인증. |
| pg_dump 백업 파일 | 운영 데이터 복사본 — 외부 노출 금지, 로컬 머신 backup/ 으로 옮길 때 권한 주의. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-20 | Tampering | 운영 마이그레이션 SQL | mitigate | 트랜잭션 + idempotent + step4 검증 + 14일 모니터링 + 26-99-rollback 가능 |
| T-26-21 | Information Disclosure | pg_dump 백업 파일 | mitigate | /var/backups/ventago/ 권한 600 (postgres 만), 로컬 ~/backup/ 도 권한 700 |
| T-26-22 | Denial of Service | step2 의 거대 expenses UPDATE | accept | UPDATE 는 expenses 행만 (수만 건) — 인덱스 (expenses_subcategory_id) 가 있어 수초 내 완료. 트랜잭션이 락을 짧게 유지 |
| T-26-23 | Repudiation | DDL 실행 추적 불가 | mitigate | SSH session 로그 + git 커밋 메시지에 실행 일시 + 사용자 승인 메시지 기록 |
</threat_model>

<verification>
1. 백업 파일이 운영 + 로컬 양쪽에 안전 보관
2. step1~step4 운영 적용 + 검증 통과
3. push-both.sh 로 API + 프론트엔드 운영 배포 성공
4. admin/vendedor 회귀 테스트 모두 통과
5. 14일 모니터링: 5xx 0건, RAISE EXCEPTION 0건, 사용자 피드백 OK
6. step5-cleanup 실행 + 검증
7. 옛 모델/모듈/디렉토리 코드 잔여물 0
8. PROJECT.md 업데이트 + git 커밋
</verification>

<success_criteria>
- 운영 데이터 손실 0건 (SC-11)
- expenses_categories / expenses_subcategories / expenses_subcategory_id 모두 drop
- ExpenseCategory 모델만 남고 옛 모델 삭제
- 모든 사용자 트래픽이 새 트리 UI 사용 (회귀 0)
- PROJECT.md 에 Phase 26 완료 기록
- 다음 phase 진행 가능 (deferred items 별도 backlog)
</success_criteria>

<output>
After completion, create `.planning/phases/26-gastos-categoria-tree-n-niveles/26-05-SUMMARY.md` with:
- 운영 마이그레이션 일시 + 영향 행 수
- 14일 모니터링 결과 요약
- cleanup 일시
- 발견된 이슈 + 해결 방법 (있다면)
- Phase 26 전체 완료 선언

또한 RETROSPECTIVE.md 가 있으면 milestone 회고 entry 추가:
- What Worked: react-arborist 채택, materialized path 트리거, 4단계 idempotent SQL
- What Was Inefficient: (있다면)
- Lessons: PG10/PG15 트리거 호환성 (EXECUTE PROCEDURE), Sequelize 자기참조 무한 재귀 회피
</output>
</content>
</invoke>