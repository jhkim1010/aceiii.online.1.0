---
phase: 35-activity-ledger
plan: 06
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - ventago-app/src/views/homes/components/ProductList/ProductList.tsx
autonomous: true
requirements:
  - AL-24  # ProductList.handleSubmitSpecial: 응답 saleId 추출 + toast "Ver detalle" 링크 (D-11)
  - AL-25  # toast 에 ventaVista navigate 액션 (optional 이지만 SPEC D-11 권장)
tags: [frontend, react, toast, ventavista]

must_haves:
  truths:
    - "handleSubmitSpecial 의 response 처리에서 response.saleId 를 추출한다"
    - "toast 메시지에 'Ver detalle' 액션 링크 (또는 onClick → router.push) 추가"
    - "saleId 가 없으면 (legacy 호환) 기존 toast 그대로 표시 (fallback)"
  artifacts:
    - path: "ventago-app/src/views/homes/components/ProductList/ProductList.tsx"
      provides: "handleSubmitSpecial 응답에서 saleId 사용"
      contains: "saleId"
  key_links:
    - from: "ProductList.handleSubmitSpecial"
      to: "POST /stocks/movement response (Plan 02)"
      via: "response.saleId 추출"
      pattern: "response\\?\\.saleId\\|response\\.saleId"
---

<objective>
ProductList.tsx 의 handleSubmitSpecial (POST /stocks/movement 호출) 에서 응답의 saleId 를 추출하여
toast 에 "Ver detalle" 액션 링크 추가. ventaVista 즉시 navigate.

Purpose: D-11 — 등록 후 사용자가 즉시 movido 흔적을 확인할 수 있는 UX.
Output: ProductList.tsx 의 toast.success 호출 확장.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-activity-ledger/35-SPEC.md
@.planning/phases/35-activity-ledger/35-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 02 적용 후 POST /stocks/movement 응답 shape -->
```json
{
  "success": true,
  "saleId": 78901,  // 신규
  "type": "movido" | "fallado",
  "itemCount": N,
  "insertedRows": M
}
```

<!-- 기존 toast 사용 패턴 -->
From ProductList.tsx L517:
```typescript
toast.success(falladosMode ? "Fallados registrados" : "Movimiento registrado")
```

react-hot-toast (또는 동등 라이브러리) — JSX 또는 함수 형태로 action 추가 가능.

<!-- Next.js router push -->
import { useRouter } from 'next/router' (이미 import 되어 있을 가능성, 확인 필요)
router.push('/ventas') 또는 ventaVista 경로
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: handleSubmitSpecial — response.saleId 추출 + toast 액션 링크</name>
  <files>ventago-app/src/views/homes/components/ProductList/ProductList.tsx</files>
  <read_first>
    - ventago-app/src/views/homes/components/ProductList/ProductList.tsx L507-534 (handleSubmitSpecial 의 try/catch 응답 처리)
    - ventago-app/src/views/homes/components/ProductList/ProductList.tsx 파일 상단 import (useRouter, toast 라이브러리 확인)
    - .planning/phases/35-activity-ledger/35-SPEC.md L326-331 (D-11 — 응답 saleId 활용)
    - .planning/phases/35-activity-ledger/35-SPEC.md L351-361 (API 응답 shape)
  </read_first>
  <action>
파일 `ventago-app/src/views/homes/components/ProductList/ProductList.tsx` 의 handleSubmitSpecial 메서드 수정:

**STEP A — useRouter import 확인/추가** (파일 상단):
```typescript
import { useRouter } from 'next/router'
```
파일에 `useRouter` 가 이미 import 되어 있으면 skip. 컴포넌트 함수 본체 내에서:
```typescript
const router = useRouter()
```
가 이미 호출되어 있으면 skip, 없으면 다른 hook 들 근처 (예: useAuth 호출 부근) 에 추가.

**STEP B — handleSubmitSpecial 의 try 블록 안 success 처리 확장**:

기존 코드 (라인 509-522):
```typescript
const response = await apiConnector.post("/stocks/movement", payload);
// ... 기존 console.log ...
toast.success(falladosMode ? "Fallados registrados" : "Movimiento registrado");
resetSale();
setFalladosMode(false);
setMovidosMode(false);
setTargetBranchId('');
if (props.onSaleCreated) props.onSaleCreated();
```

다음으로 교체:
```typescript
const response: any = await apiConnector.post("/stocks/movement", payload);
const dt = Math.round(performance.now() - t0);

console.group(`[MOVIDOS-DEBUG] response ${correlationId} (+${dt}ms)`);
console.log('[MOVIDOS-DEBUG] backend response:', response);
console.groupEnd();
/* eslint-enable no-console */

// Phase 35: 응답에 saleId 가 포함됨 (Plan 02 백엔드 단일 트랜잭션 적용 후).
// toast 에 "Ver detalle" 액션 — ventaVista 로 navigate.
const saleId = response?.saleId ? Number(response.saleId) : null
const successMsg = falladosMode ? "Fallados registrados" : "Movimiento registrado"

if (saleId && saleId > 0) {
  // 액션 가능한 toast (react-hot-toast JSX 형태)
  toast.success(
    (t) => (
      <span>
        {successMsg}
        {' · '}
        <a
          onClick={() => {
            toast.dismiss(t.id)
            router.push(`/ventas?openSale=${saleId}`)
          }}
          style={{ cursor: 'pointer', textDecoration: 'underline', color: '#1976D2', fontWeight: 600 }}
        >
          Ver detalle
        </a>
      </span>
    ),
    { duration: 5000 },
  )
} else {
  // legacy fallback (saleId 없으면 기존 메시지만)
  toast.success(successMsg)
}

resetSale();
setFalladosMode(false);
setMovidosMode(false);
setTargetBranchId('');
if (props.onSaleCreated) props.onSaleCreated();
```

**중요**:
- toast 라이브러리가 react-hot-toast 인 경우 JSX function 형태 지원. sonner 등 다른 라이브러리면 형식 다를 수 있음 — **`read_first` 단계에서 toast import 확인 후** 형식 조정.
- 만약 toast 라이브러리가 JSX action 미지원이면, 단순화: `toast.success(successMsg + ' · Ver detalle')` + 별도 setTimeout 으로 router.push 같은 자동 동작 대신 단순 메시지로 표시. 그래도 saleId 는 콘솔에 기록 (디버깅용).
- `/ventas?openSale={saleId}` 경로는 향후 SalesListView 에서 자동으로 해당 saleId 열기 (별도 작업 필요). 본 plan 에서는 navigate 만.

ESLint 검증:
```bash
cd ventago-app && npx eslint src/views/homes/components/ProductList/ProductList.tsx
```

빌드 검증:
```bash
cd ventago-app && npm run build
```
  </action>
  <verify>
    <automated>
      cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app &amp;&amp; grep -q "response?.saleId\|response\.saleId" src/views/homes/components/ProductList/ProductList.tsx &amp;&amp; grep -q "Ver detalle" src/views/homes/components/ProductList/ProductList.tsx &amp;&amp; grep -q "openSale=" src/views/homes/components/ProductList/ProductList.tsx &amp;&amp; grep -q "useRouter\|router.push" src/views/homes/components/ProductList/ProductList.tsx &amp;&amp; npm run build 2>&amp;1 | tail -5 | grep -qi "compiled\|done" &amp;&amp; npx eslint src/views/homes/components/ProductList/ProductList.tsx 2>&amp;1 | { ! grep -E "^\s*[0-9]+:[0-9]+\s+error"; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "response\?.saleId\|response\.saleId" ventago-app/src/views/homes/components/ProductList/ProductList.tsx` >= 1
    - `grep -c "Ver detalle" ventago-app/src/views/homes/components/ProductList/ProductList.tsx` >= 1
    - `grep -c "openSale=" ventago-app/src/views/homes/components/ProductList/ProductList.tsx` >= 1
    - `grep -c "useRouter\|router\.push" ventago-app/src/views/homes/components/ProductList/ProductList.tsx` >= 1 (only check if router newly added)
    - `grep -c "saleId && saleId > 0" ventago-app/src/views/homes/components/ProductList/ProductList.tsx` >= 1 (fallback guard)
    - `cd ventago-app && npm run build` exits 0
    - `cd ventago-app && npx eslint src/views/homes/components/ProductList/ProductList.tsx` exits with no errors
  </acceptance_criteria>
  <done>
    handleSubmitSpecial 응답에서 saleId 추출, toast 에 "Ver detalle" 액션 링크.
    legacy fallback (saleId 없으면 기존 메시지) 동작.
    빌드 + lint 통과.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-21 | XSS | toast JSX 의 saleId 값 | accept | Number() casting + React 자동 escape — DB-controlled |
| T-35-22 | UX | saleId 누락 시 토스트 깨짐 | mitigate | `if (saleId && saleId > 0)` 가드 + legacy fallback |
</threat_model>

<verification>
- movido 등록 후 toast 에 "Ver detalle" 표시
- 링크 클릭 → /ventas?openSale=X 로 navigate
- 백엔드 응답 saleId 누락 시 (예: rollback) → 기존 단순 메시지만 표시
</verification>

<success_criteria>
- [ ] ProductList.tsx handleSubmitSpecial 에 saleId 추출 + 액션 토스트
- [ ] router.push('/ventas?openSale={saleId}') 호출
- [ ] legacy fallback (saleId 없을 때 기존 메시지)
- [ ] 빌드 + lint 통과
- [ ] D-11 SPEC 충족
</success_criteria>

<output>
After completion, create `.planning/phases/35-activity-ledger/35-06-SUMMARY.md`
</output>
