---
phase: 17-portal-de-talleres-aviso
plan: 05
type: execute
wave: 3
depends_on: [17-02, 17-03]
files_modified:
  - talleres-vendor-app/lib/features/notifications/data/notification_repository.dart
  - talleres-vendor-app/lib/features/notifications/data/notification_dto.dart
  - talleres-vendor-app/lib/features/notifications/providers/notification_provider.dart
  - talleres-vendor-app/lib/features/notifications/views/notifications_screen.dart
  - talleres-vendor-app/lib/features/settlements/data/settlement_repository.dart
  - talleres-vendor-app/lib/features/settlements/data/settlement_dto.dart
  - talleres-vendor-app/lib/features/settlements/providers/settlement_provider.dart
  - talleres-vendor-app/lib/features/settlements/views/settlements_screen.dart
  - talleres-vendor-app/lib/features/home/views/home_screen.dart
autonomous: true
requirements:
  - VP-05
  - VP-06

must_haves:
  truths:
    - "Vendor sees notification list with unread count badge"
    - "Vendor can mark notifications as read (single and all)"
    - "Vendor sees settlement history for selected store"
    - "Settlement list shows period, amounts, status"
    - "Unread notification badge shows on bottom nav icon"
  artifacts:
    - path: "talleres-vendor-app/lib/features/notifications/views/notifications_screen.dart"
      provides: "Notification list with read/unread visual distinction"
      min_lines: 50
    - path: "talleres-vendor-app/lib/features/settlements/views/settlements_screen.dart"
      provides: "Settlement history with date filter"
      min_lines: 50
    - path: "talleres-vendor-app/lib/features/notifications/providers/notification_provider.dart"
      provides: "Unread count provider for badge"
      contains: "unreadCountProvider"
  key_links:
    - from: "notification_repository.dart"
      to: "dio_client.dart"
      via: "GET /vendor-portal/notifications"
      pattern: "vendor-portal/notifications"
    - from: "settlement_repository.dart"
      to: "dio_client.dart"
      via: "GET /vendor-portal/settlements"
      pattern: "vendor-portal/settlements"
    - from: "home_screen.dart"
      to: "notification_provider.dart"
      via: "unreadCountProvider for badge"
      pattern: "unreadCountProvider"
---

<objective>
Flutter notifications + settlements features: notification list with badge, mark-as-read, settlement history with date filter.

Purpose: Completes the vendor portal feature set (per D-09, D-10, D-11) — notifications and settlement history views.
Output: Notifications screen with unread badge, settlements screen with date filter, integrated into home navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-portal-de-talleres-aviso/17-CONTEXT.md
@.planning/phases/17-portal-de-talleres-aviso/17-RESEARCH.md
@.planning/phases/17-portal-de-talleres-aviso/17-03-SUMMARY.md

@talleres-vendor-app/lib/core/network/dio_client.dart
@talleres-vendor-app/lib/features/auth/providers/auth_provider.dart
@talleres-vendor-app/lib/shared/models/store_info.dart
@talleres-vendor-app/lib/shared/widgets/status_chip.dart
@talleres-vendor-app/lib/features/home/views/home_screen.dart

<interfaces>
<!-- Backend API contracts from Plan 02 -->
GET /vendor-portal/notifications?storeId={id}&isRead={bool}
  Response: [{ id, vendorId, storeId, type, title, body, referenceId, isRead, createdAt }]

GET /vendor-portal/notifications/unread-count?storeId={id}
  Response: { count: N }

PATCH /vendor-portal/notifications/:id/read
  Response: { success: true }

PATCH /vendor-portal/notifications/read-all
  Request: { storeId: N }
  Response: { success: true }

GET /vendor-portal/settlements?storeId={id}&from={date}&to={date}
  Response: [{ id, subconOrderId, periodFrom, periodTo, totalGrossAmount, deductionAmount, netAmount, settlementDate, status, notes, subconOrder: { orderNumber } }]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notifications feature (repository + provider + screen with badge)</name>
  <files>
    talleres-vendor-app/lib/features/notifications/data/notification_dto.dart,
    talleres-vendor-app/lib/features/notifications/data/notification_repository.dart,
    talleres-vendor-app/lib/features/notifications/providers/notification_provider.dart,
    talleres-vendor-app/lib/features/notifications/views/notifications_screen.dart
  </files>
  <read_first>
    talleres-vendor-app/lib/core/network/dio_client.dart,
    talleres-vendor-app/lib/features/auth/providers/auth_provider.dart,
    talleres-vendor-app/lib/features/home/views/home_screen.dart,
    talleres-vendor-app/lib/shared/models/store_info.dart
  </read_first>
  <action>
**1. notification_dto.dart**:
```dart
class NotificationDto {
  final int id;
  final String type; // NEW_ENVIO, DUE_SOON, SETTLEMENT_DONE
  final String title;
  final String? body;
  final int? referenceId;
  final bool isRead;
  final DateTime createdAt;
  // fromJson constructor — createdAt은 DateTime.parse로 변환
  
  // 알림 타입별 아이콘 getter
  IconData get icon => switch (type) {
    'NEW_ENVIO' => Icons.local_shipping,
    'DUE_SOON' => Icons.warning_amber,
    'SETTLEMENT_DONE' => Icons.payments,
    _ => Icons.notifications,
  };
}
```

**2. notification_repository.dart**:
- `fetchNotifications(int storeId, {bool? isRead})` → `GET /vendor-portal/notifications` → `List<NotificationDto>`
- `fetchUnreadCount(int storeId)` → `GET /vendor-portal/notifications/unread-count` → `int`
- `markAsRead(int id)` → `PATCH /vendor-portal/notifications/$id/read`
- `markAllAsRead(int storeId)` → `PATCH /vendor-portal/notifications/read-all` body: `{ storeId }`
에러 핸들링: DioException → ApiException 변환.

**3. notification_provider.dart**:
```dart
// 알림 목록 (매장별)
final notificationListProvider = FutureProvider.family.autoDispose<List<NotificationDto>, int>((ref, storeId) async {
  return ref.read(notificationRepositoryProvider).fetchNotifications(storeId);
});

// 미읽음 카운트 (매장별) — 홈 화면 배지에 사용
final unreadCountProvider = FutureProvider.family.autoDispose<int, int>((ref, storeId) async {
  return ref.read(notificationRepositoryProvider).fetchUnreadCount(storeId);
});
```

**4. notifications_screen.dart** (per D-09, D-11):
- ConsumerWidget
- 상단: "Notificaciones" 제목 + "Marcar todo como leído" TextButton (미읽음 있을 때만)
- ListView.builder: 각 알림을 ListTile로 표시
  - leading: 알림 타입별 아이콘 (notification.icon)
  - title: notification.title (미읽음이면 bold)
  - subtitle: notification.body + 시간 표시 (예: "hace 2 horas", 또는 날짜)
  - trailing: 미읽음이면 파란 점 (Container 8x8 원형)
  - 탭 시: 읽음 처리 + (referenceId가 있으면 해당 envio/settlement로 이동 가능 — 선택사항)
- 빈 목록: "No hay notificaciones" 메시지 + 아이콘
- pull-to-refresh
- "Marcar todo como leído" 클릭 시: markAllAsRead → 목록 새로고침 + unreadCount 새로고침
- 시간 포맷: intl 패키지 사용, 스페인어 로케일

주석은 한국어로, 함수/변수명은 영어로. null safety 준수.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/talleres-vendor-app && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - notification_dto.dart has type-based icon getter (NEW_ENVIO→local_shipping, DUE_SOON→warning, SETTLEMENT_DONE→payments)
    - notification_repository.dart has 4 methods: fetchNotifications, fetchUnreadCount, markAsRead, markAllAsRead
    - notification_provider.dart exports notificationListProvider and unreadCountProvider (both family.autoDispose)
    - notifications_screen.dart shows unread notifications in bold with blue dot indicator
    - notifications_screen.dart has "Marcar todo como leído" button that calls markAllAsRead
    - flutter analyze passes
  </acceptance_criteria>
  <done>Notifications screen working with read/unread distinction, mark-all-read, type-based icons</done>
</task>

<task type="auto">
  <name>Task 2: Settlements feature + home screen integration (badge + placeholder removal)</name>
  <files>
    talleres-vendor-app/lib/features/settlements/data/settlement_dto.dart,
    talleres-vendor-app/lib/features/settlements/data/settlement_repository.dart,
    talleres-vendor-app/lib/features/settlements/providers/settlement_provider.dart,
    talleres-vendor-app/lib/features/settlements/views/settlements_screen.dart,
    talleres-vendor-app/lib/features/home/views/home_screen.dart
  </files>
  <read_first>
    talleres-vendor-app/lib/core/network/dio_client.dart,
    talleres-vendor-app/lib/features/auth/providers/auth_provider.dart,
    talleres-vendor-app/lib/features/home/views/home_screen.dart,
    talleres-vendor-app/lib/shared/widgets/status_chip.dart,
    talleres-vendor-app/lib/features/notifications/providers/notification_provider.dart
  </read_first>
  <action>
**1. settlement_dto.dart**:
```dart
class SettlementDto {
  final int id;
  final int subconOrderId;
  final String? orderNumber;
  final DateTime? periodFrom;
  final DateTime? periodTo;
  final double totalGrossAmount;
  final double deductionAmount;
  final double netAmount;
  final DateTime settlementDate;
  final String status; // OPEN, CLOSED
  final String? notes;
  // fromJson constructor — amounts는 double.tryParse로 안전 변환
}
```

**2. settlement_repository.dart**:
- `fetchSettlements(int storeId, {String? from, String? to})` → `GET /vendor-portal/settlements` → `List<SettlementDto>`
에러 핸들링 포함.

**3. settlement_provider.dart**:
```dart
// 기간 필터 상태
final settlementDateRangeProvider = StateProvider<DateTimeRange?>((ref) => null);

// 정산 목록 (매장 + 기간)
final settlementListProvider = FutureProvider.family.autoDispose<List<SettlementDto>, int>((ref, storeId) async {
  final dateRange = ref.watch(settlementDateRangeProvider);
  return ref.read(settlementRepositoryProvider).fetchSettlements(
    storeId,
    from: dateRange?.start.toIso8601String().split('T')[0],
    to: dateRange?.end.toIso8601String().split('T')[0],
  );
});

// 정산 합계 (computed)
final settlementTotalProvider = Provider.family<double, int>((ref, storeId) {
  final settlements = ref.watch(settlementListProvider(storeId)).value ?? [];
  return settlements.fold(0.0, (sum, s) => sum + s.netAmount);
});
```

**4. settlements_screen.dart** (per D-10):
- ConsumerWidget
- 상단: 기간 필터 — 2개 날짜 선택 버튼 (desde/hasta), 또는 DateRangePickerDialog
- 기간 합계 표시: Card에 "Total neto: \$XX,XXX" (settlementTotalProvider)
- ListView.builder: 각 정산을 Card로 표시
  - 주문 번호 + StatusChip (OPEN/CLOSED)
  - 기간: "DD/MM/YYYY - DD/MM/YYYY"
  - 금액: 총액, 감액, **순액 (bold)**
  - 비고 (있으면)
- 빈 목록: "No hay liquidaciones" 메시지
- pull-to-refresh
- 읽기 전용 — 수정 불가 (per D-10)

**5. home_screen.dart 최종 통합**:
- BottomNavigationBar items에서 Notificaciones 아이콘에 **배지** 추가:
  ```dart
  BottomNavigationBarItem(
    icon: Badge(
      isLabelVisible: unreadCount > 0,
      label: Text('$unreadCount'),
      child: const Icon(Icons.notifications),
    ),
    label: 'Notificaciones',
  )
  ```
  - `ref.watch(unreadCountProvider(currentStoreId))` — 현재 매장의 미읽음 카운트
- IndexedStack의 children 업데이트:
  - index 0: EnviosScreen (from Plan 04)
  - index 1: NotificationsScreen
  - index 2: SettlementsScreen
  - index 3: 프로필/로그아웃 (간단 — 전화번호 표시 + 로그아웃 버튼)
- placeholder 제거

주석은 한국어로, 함수/변수명은 영어로. null safety 준수.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/talleres-vendor-app && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - settlement_dto.dart parses amounts safely with double.tryParse
    - settlement_repository.dart calls GET /vendor-portal/settlements with storeId, from, to params
    - settlement_provider.dart has settlementDateRangeProvider, settlementListProvider, settlementTotalProvider
    - settlements_screen.dart has date range picker and total amount card
    - settlements_screen.dart is read-only (no edit/create actions)
    - home_screen.dart shows Badge on notifications icon with unread count
    - home_screen.dart IndexedStack has all 4 screens (envios, notifications, settlements, profile)
    - flutter analyze passes
  </acceptance_criteria>
  <done>Settlements screen with date filter and totals, notifications badge on home nav, all 4 tabs fully integrated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settlement data display | Read-only, no mutation allowed from vendor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | settlements_screen | mitigate | UI is read-only, no POST/PUT endpoints for settlements in vendor portal |
| T-17-13 | Information Disclosure | notification body | accept | Notifications only contain vendor's own data, scoped by vendorId |
</threat_model>

<verification>
1. `flutter analyze` passes
2. All 4 home tabs render correctly
3. Notifications badge shows unread count
4. Settlements show date filter and amounts
</verification>

<success_criteria>
- Notification list shows type-specific icons with read/unread distinction
- Mark-all-read clears unread count and badge
- Settlement list shows amounts with date range filter
- Settlement total card shows sum of net amounts
- Home bottom nav badge shows unread notification count
- All 4 tabs (Envios, Notificaciones, Liquidaciones, Perfil) fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/17-portal-de-talleres-aviso/17-05-SUMMARY.md`
</output>
