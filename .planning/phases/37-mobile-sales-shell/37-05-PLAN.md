---
phase: 37-mobile-sales-shell
plan: 05
type: execute
wave: 5
depends_on: ["37-04"]
blocked_by_external: ["Phase 24 Wave 1-2 (reseller.catalog_unified MV + reseller_tienda_link)"]
files_modified:
  - mobile-sales-app/lib/features/revendedor/README.md
  - mobile-sales-app/lib/features/revendedor/views/store_selector_screen.dart
  - mobile-sales-app/lib/features/revendedor/views/quote_screen.dart
autonomous: false
requirements: [MOBILE-B-01, MOBILE-B-03, MOBILE-C-04, MOBILE-C-05]
user_setup: []

must_haves:
  truths:
    - "Revendedor mode is a thin, explicitly-gated skeleton — it does NOT ship until Phase 24 Wave 1-2 delivers reseller.catalog_unified MV + reseller_tienda_link (D-07)"
    - "When unblocked, revendedor sees a store selector (allowed N stores) and a search-to-list catalog (NO QR, D-14), and creates quotes/orders that behave as pending/보류 (Caja-neutral, D-13)"
  artifacts:
    - path: "mobile-sales-app/lib/features/revendedor/README.md"
      provides: "explicit Phase 24 dependency block + activation checklist"
      contains: "reseller.catalog_unified"
  key_links:
    - from: "revendedor mode"
      to: "Phase 24 reseller schema"
      via: "MultiStoreScope from reseller_tienda_link"
      pattern: "reseller_tienda_link"
---

<objective>
Deliver the revendedor mode as a THIN, explicitly-blocked skeleton. Per D-07, revendedor requires Phase 24
Wave 1-2 (`reseller.catalog_unified` materialized view + `reseller_tienda_link`), which does NOT yet exist in
the schema (verified: absent). Waves 1-4 (vendedor MVP) ship independently; this plan MUST NOT block them.

Purpose: Reserve the revendedor UI surface and document the exact activation contract so that, once Phase 24
completes, revendedor can be built without re-discovery — while making the current blocked state unmistakable.

Output: A revendedor feature folder with a README dependency block + two screen stubs (store selector,
quote screen) that render a "Próximamente — requiere Phase 24" placeholder. NO backend revendedor scope work
here (that lives in a future execution once Phase 24 lands and MobileAuthService's revendedor branch is filled in).

BLOCKED: Do NOT implement revendedor catalog/stock/quote logic until Phase 24 Wave 1-2 is confirmed complete.
The first task is a decision checkpoint to verify that gate.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-mobile-sales-shell/37-CONTEXT.md
@.planning/phases/24-revendedor-marketplace/24-CONTEXT.md
@.planning/phases/37-mobile-sales-shell/37-RESEARCH.md
@.planning/phases/37-mobile-sales-shell/37-03-SUMMARY.md

<interfaces>
<!-- Reuse when unblocked -->
// mobile-sales-app/lib/shared/widgets/store_tab_bar.dart (cloned from talleres-vendor-app in Wave 3) — multi-store selector.
// MultiStoreScope from ScopeProvider (Wave 3) — already models scopeStoreIds.
// GET /mobile/catalog / GET /mobile/stock/:productId (Wave 2) — revendedor branches currently throw NOT_IMPLEMENTED.
<!-- Blocked dependency (Phase 24, NOT YET IN SCHEMA) -->
// reseller.catalog_unified (materialized view) + reseller_tienda_link(store_id, reseller_id).
// NOTE: the existing api-ventago/src/app/revendedor/ module (table `revendedores`) is a DIFFERENT legacy concept —
//   do NOT conflate it with Phase 24's reseller.* schema.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm Phase 24 Wave 1-2 readiness (activation gate)</name>
  <read_first>
    - .planning/phases/24-revendedor-marketplace/24-CONTEXT.md
    - .planning/STATE.md (Phase 24 status)
    - .planning/intel/db-schema-tables.md (search for reseller_tienda_link / reseller.catalog_unified)
  </read_first>
  <decision>Is Phase 24 Wave 1-2 (reseller.catalog_unified MV + reseller_tienda_link) complete and applied?</decision>
  <context>
    Revendedor mode (D-07) depends on Phase 24's reseller schema, which is currently absent. This gate prevents
    building revendedor against a non-existent contract. If not ready, only the skeleton (Task 2) is produced and
    the plan stops — vendedor MVP (Waves 1-4) is unaffected.
  </context>
  <action>Decision-only gate — inspect Phase 24 status (STATE.md + 24-CONTEXT.md) and grep the schema for `reseller_tienda_link` / `reseller.catalog_unified`. No code is written in this task; it routes the plan to full build (ready) or skeleton-only (blocked).</action>
  <options>
    <option id="ready">
      <name>Phase 24 complete — proceed to full revendedor build</name>
      <pros>Can implement store selector + quote flow against a real MV.</pros>
      <cons>Requires a follow-up plan revision to expand scope (this plan stays thin by design).</cons>
    </option>
    <option id="blocked">
      <name>Phase 24 NOT complete — produce skeleton only, stop</name>
      <pros>Ships nothing risky; vendedor MVP unblocked; activation contract documented.</pros>
      <cons>Revendedor deferred until Phase 24.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Decision recorded; if `blocked`, only Task 2 runs and the plan halts (no revendedor backend/logic work)
  </acceptance_criteria>
  <verify>Manual: schema grep for reseller_tienda_link + Phase 24 STATE reviewed; decision recorded.</verify>
  <done>Readiness decision recorded (ready → full build; blocked → skeleton only, halt).</done>
  <resume-signal>Select: ready or blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Revendedor skeleton + explicit dependency block</name>
  <read_first>
    - mobile-sales-app/lib/features/auth/providers/scope_provider.dart (MultiStoreScope)
    - mobile-sales-app/lib/shared/widgets/store_tab_bar.dart (selector to reuse when unblocked)
    - .planning/phases/37-mobile-sales-shell/37-CONTEXT.md (D-07, D-13 revendedor = pending/보류)
  </read_first>
  <action>
    Create `mobile-sales-app/lib/features/revendedor/README.md` documenting: the Phase 24 dependency
    (`reseller.catalog_unified` MV + `reseller_tienda_link`), the activation checklist (backend MobileAuthService
    revendedor scope resolution, /mobile/catalog + /mobile/stock revendedor branches, D-14 search-to-list entry,
    D-13 quote/order = pending/보류 Caja-neutral), and the warning that the legacy `revendedores` module is unrelated.

    Create `store_selector_screen.dart` and `quote_screen.dart` as stubs that render a Ventago-themed placeholder
    ("Modo revendedor — Próximamente (requiere Phase 24)"). Wire them behind the MultiStoreScope route branch only
    (so vendedor navigation is untouched). No API calls, no quote logic.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/mobile-sales-app && flutter analyze lib/features/revendedor 2>&1 | tail -10 && grep -q "reseller.catalog_unified" lib/features/revendedor/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README documents the Phase 24 dependency (`reseller.catalog_unified`) + activation checklist + legacy-module warning
    - stub screens render a "Próximamente / requiere Phase 24" placeholder and make NO API calls
    - `flutter analyze` clean for lib/features/revendedor; vendedor routes unaffected
  </acceptance_criteria>
  <done>Revendedor surface reserved and documented; blocked until Phase 24 without impacting vendedor MVP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revendedor client → /mobile/* | (when unblocked) untrusted store scope across N stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-20 | Elevation of Privilege | revendedor reading a store outside reseller_tienda_link | mitigate (future) | MobileScopeGuard revendedor branch enforces store_id IN (reseller_tienda_link) — built when Phase 24 lands |
| T-37-21 | Tampering | building against a non-existent MV → silent breakage | mitigate | activation gate (Task 1) blocks implementation until Phase 24 confirmed |
</threat_model>

<verification>
- `flutter analyze` clean; vendedor MVP navigation/tests unaffected by the revendedor skeleton.
- README dependency block present and accurate.
</verification>

<success_criteria>
- Revendedor mode is a documented, blocked skeleton (D-07) — vendedor MVP ships independently.
- Activation contract captured for a future Phase-24-unblocked build.
</success_criteria>

<output>
After completion, create `.planning/phases/37-mobile-sales-shell/37-05-SUMMARY.md`
</output>
