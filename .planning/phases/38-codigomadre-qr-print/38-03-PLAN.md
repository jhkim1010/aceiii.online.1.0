---
phase: 38-codigomadre-qr-print
plan: 03
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - zebra-agent/renderer/index.html
autonomous: true
requirements: [QR-08, QR-09]
user_setup: []

must_haves:
  truths:
    - "zebra-agent 에 TAB3 'QR' 이 추가되어 좌(라이브 프리뷰 + 수치 조정) / 우(델타 리스트) 2패널로 열린다 (SC6/D-10)"
    - "우 패널에서 price-type 드롭다운 선택 + 'Buscar cambios' -> get_qr_pending(qrFetchPending) 델타 리스트가 NUEVO/CAMBIO 뱃지 + 체크박스 + 구->신 가격으로 렌더된다 (SC6/D-3)"
    - "좌 패널의 폭/높이/QR모듈/1:3 비율/폰트 수치를 바꾸면 라이브 프리뷰가 즉시 갱신되고 label config 로 저장된다 (SC6/D-10)"
    - "1개/2개(simple/doble) 토글 + 전체선택 토글 + 'Imprimir seleccionados (N)' 로 선택 항목만 qrPrint 된다 (SC6/D-9)"
    - "qrPrint 결과의 실패 항목은 행에 실패 표시되고 인라인 배너 + prominent 토스트로 노출된다; 성공분은 다음 Buscar 에서 사라진다 (SC7)"
    - "테마는 기존 다크네이비(#1a1a2e) + 골드(#f5a623) 를 유지한다 (D-10)"
  artifacts:
    - path: "zebra-agent/renderer/index.html"
      provides: "TAB3 'QR' 2패널 마크업 + JS 배선(qrFetchPending/qrPrint) + 라이브 프리뷰 + 델타 테이블"
      contains: "data-tab=\"qr\""
  key_links:
    - from: "TAB3 Buscar cambios"
      to: "preload qrFetchPending"
      via: "window.electronAPI.qrFetchPending(priceTypeId)"
      pattern: "qrFetchPending\\("
    - from: "TAB3 Imprimir seleccionados"
      to: "preload qrPrint"
      via: "window.electronAPI.qrPrint({ items, layout, mode, priceTypeId })"
      pattern: "qrPrint\\("
    - from: "TAB3 수치 컨트롤"
      to: "라이브 프리뷰 + label config"
      via: "input change -> renderPreview + setLabelLayout/setConfig"
      pattern: "renderPreview|setLabelLayout"
---

<objective>
zebra-agent 렌더러에 TAB3 "QR" 추가 — 2패널(D-10): 좌 = 라이브 프리뷰 + 수치 조정
(widthMm/heightMm/qrModule/splitRatio/fontSize + label config 저장), 우 = price-type 드롭다운 +
1개/2개(simple/doble) 토글 + "Buscar cambios" -> 델타 테이블(NUEVO/CAMBIO 뱃지, 체크박스, 구->신 가격,
전체선택) + "Imprimir seleccionados (N)". 38-02 의 qrFetchPending/qrPrint IPC 에 배선한다.

Purpose: 운영자가 한 화면에서 price-type 선택 -> 변경분만 확인 -> 원치 않는 항목 체크 해제 -> 배치 출력
까지 수행(D-1/D-10). 출력 성공분은 다음 Buscar 에서 자동 제외(38-01/02 델타). 실패는 행 표시 + 에러
가시성(인라인 + 토스트) 규약을 따른다.

Output: renderer/index.html 단일 파일 수정 — TAB3 마크업 + 스타일(기존 테마 재사용) + JS 배선.
기존 TAB1/TAB2 의 탭 전환, price-select, .two-panel, 프리뷰 패턴을 재사용한다.

DECISION - price-type 목록은 기존 priceTypes:fetch(get_price_types) IPC 를 재사용한다(신규 IPC 불필요).
델타 리스트/출력만 38-02 의 qrFetchPending/qrPrint 를 쓴다.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-codigomadre-qr-print/38-CONTEXT.md
@docs/superpowers/specs/2026-07-09-zebra-qr-batch-delta-design.md
@.planning/phases/38-codigomadre-qr-print/38-02-SUMMARY.md

<repo_note>
zebra-agent 는 부모 레포. 이 플랜 커밋은 부모 레포에서 수행(서브모듈 아님). renderer 는 순수 HTML/JS
(빌드 스텝 없음) - 문법 검증은 node --check 불가하므로 구조 grep + (가능 시) HTML 파싱으로 확인.
UI 시각 검증은 phase 검증 단계의 수동 UAT 로 확정(dev 에서 electron 실행). gsd-sdk query 사용 금지.
</repo_note>

<interfaces>
<!-- 재사용 - 기존 탭/패널/테마 구조 -->
From zebra-agent/renderer/index.html:
```html
<!-- 탭 버튼: <button class="tab-btn" data-tab="label">Etiqueta</button> -->
<!-- 탭 콘텐츠: <div class="tab-content" id="tab-label"> ... </div> -->
<!-- 2패널: <div class="two-panel etiqueta"> (좌 430px / 우 1fr) 또는 <div class="two-panel"> (좌 1fr / 우 430px) -->
<!-- 테마: 배경 #1a1a2e, 골드 #f5a623, 패널 #0f3460, 라인 #2a2a4a. .btn.btn-primary/.btn-secondary. -->
<!-- 체크박스: input[type=checkbox]{ accent-color:#f5a623 } . 배지/칩: .mode-chip (골드 테두리). -->
<!-- 탭 전환 JS: data-tab -> #tab-{name} .active 토글 (기존 스크립트 하단에 존재). 신규 탭도 이 로직에 자동 편입되게 data-tab/id 규칙 준수. -->
```
<!-- preload 로 노출된 API (window.electronAPI) -->
```javascript
// 38-02: qrFetchPending(priceTypeId) -> { ok, items } ; qrPrint({ items, layout, mode, priceTypeId }) -> { ok, printed, failed }
// 기존: fetchPriceTypes() -> { ok, priceTypes:[{id,name}] } (get_price_types ack)
// 기존: getLabelConfig()/setLabelLayout(layout)/getConfig(key)/setConfig(key,value) - 수치 저장에 재사용
// 델타 item 형태(38-01): { productId, code, name, price, priceLabel, oldPrice?, oldName?, status:'NUEVO'|'CAMBIO', qrUrl }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TAB3 'QR' 마크업 - 탭 버튼 + 2패널(좌 프리뷰/수치, 우 price-type/토글/델타 테이블)</name>
  <read_first>
    - zebra-agent/renderer/index.html (탭 버튼 영역 L115-118, TAB2 etiqueta 2패널 L291-, 테마 CSS L14-94)
    - zebra-agent/mockups/etiqueta-2panel-mockup.html (2패널 프리뷰 + 컨트롤 시각 참고)
  </read_first>
  <action>
    renderer/index.html 에 TAB3 추가. 기존 테마/클래스만 사용(신규 색상 금지).

    1. 탭 버튼 영역(.tabs)에 추가: `<button class="tab-btn" data-tab="qr">QR</button>` (Imprimir/Etiqueta 뒤).
    2. 새 콘텐츠 블록 `<div class="tab-content" id="tab-qr">` 를 TAB2(#tab-label) 뒤에 추가. 내부 `.two-panel`(좌 1fr / 우 430px 또는 그 반대 - 좌 프리뷰가 크게):
       - 좌 패널(프리뷰 + 수치):
         - 라이브 프리뷰 영역: `#qr-preview` (canvas 또는 div - 1:3 좌 QR 사각형 + 우 이름/가격 미리보기; 실제 ZPL 렌더는 불가하므로 HTML 근사 프리뷰).
         - 수치 입력(number): `#qr-width-mm`(기본 50) / `#qr-height-mm`(25) / `#qr-module`(4) / `#qr-split-ratio`(0.25) / `#qr-font-size`(22). 각 라벨 스페인어(Ancho/Alto/Módulo QR/Proporción 1:3/Fuente).
         - "Guardar configuración" 버튼 `#qr-save-layout`.
       - 우 패널(델타):
         - price-type 드롭다운 `#qr-price-type` (기존 fetchPriceTypes 로 채움).
         - 1개/2개 토글: 라디오/세그먼트 `#qr-mode-simple` / `#qr-mode-doble` (기본 simple).
         - "Buscar cambios" 버튼 `#qr-buscar` (.btn-primary).
         - 델타 테이블 `#qr-delta-table` (.product-table 재사용): 열 = [전체선택 체크박스 th, Estado(뱃지), Código, Nombre, Precio(구->신)]. 각 행 체크박스 + `data-product-id` + status 뱃지(.mode-chip: NUEVO=골드, CAMBIO=주황 톤). CAMBIO 행은 `oldPrice -> price` 표기.
         - 하단 요약바(.summary-bar): 선택 수 `#qr-selected-count` + "Imprimir seleccionados (N)" 버튼 `#qr-print-btn`.
         - 인라인 에러/상태 배너 `#qr-status` (에러 가시성 규약: 실패 시 빨강 배너).
    3. 모든 텍스트 스페인어. 다크네이비 배경/골드 강조 유지. (JS 배선은 Task 2.)
  </action>
  <verify>
    <automated>cd zebra-agent && grep -q 'data-tab="qr"' renderer/index.html && grep -q 'id="tab-qr"' renderer/index.html && grep -q 'id="qr-delta-table"' renderer/index.html && grep -q 'id="qr-price-type"' renderer/index.html && grep -q 'id="qr-preview"' renderer/index.html && grep -q 'id="qr-print-btn"' renderer/index.html && echo "OK markup"</automated>
  </verify>
  <acceptance_criteria>
    - .tabs 에 data-tab="qr" 버튼, #tab-qr 콘텐츠 블록 존재
    - 좌 패널: #qr-preview + 5개 수치 입력(#qr-width-mm/#qr-height-mm/#qr-module/#qr-split-ratio/#qr-font-size) + #qr-save-layout
    - 우 패널: #qr-price-type + simple/doble 토글 + #qr-buscar + #qr-delta-table + #qr-print-btn + #qr-status
    - 신규 색상 코드 없이 기존 테마 클래스/변수만 사용 (#1a1a2e/#f5a623)
  </acceptance_criteria>
  <done>TAB3 마크업 + 2패널 레이아웃 완성; 기존 테마 재사용.</done>
</task>

<task type="auto">
  <name>Task 2: TAB3 JS 배선 - price-type 로드 + Buscar(qrFetchPending) + 라이브 프리뷰 + Imprimir(qrPrint) + 에러 가시성</name>
  <read_first>
    - zebra-agent/renderer/index.html (기존 스크립트: 탭 전환 로직, fetchPriceTypes 사용처, getLabelConfig/setLabelLayout 호출부, 토스트/로그 표시 함수)
    - zebra-agent/src/price-select.js (price-type 표시 참고 - 선택적)
  </read_first>
  <action>
    renderer/index.html 하단 스크립트에 TAB3 로직 추가(기존 함수/토스트 재사용).

    1. 초기화: 탭이 QR 로 열리거나 앱 로드 시 `window.electronAPI.fetchPriceTypes()` -> #qr-price-type 옵션 채움
       (실패 시 #qr-status 에러 배너). `getLabelConfig()` 로 저장된 QR layout 수치를 입력에 프리필(없으면 기본값).
    2. 라이브 프리뷰 `renderPreview()`: 5개 수치를 읽어 좌 QR 사각형(width*splitRatio) + 우 이름/가격 근사
       박스를 #qr-preview 에 그린다. 각 number input 의 `change`/`input` 에서 renderPreview 호출(즉시 반영).
       "Guardar configuración" -> `setLabelLayout({...})` 또는 `setConfig('qrLayout', {...})` 로 저장.
    3. Buscar cambios: `#qr-buscar` click ->
       `const r = await window.electronAPI.qrFetchPending(Number(priceTypeId));`
       - r.ok 이면 r.items 로 #qr-delta-table 렌더(행별 체크박스 기본 checked, status 뱃지, CAMBIO 는 oldPrice->price).
         window 스코프에 `currentDelta = r.items` 보관(출력 시 선택분 매핑). 결과 0건이면 "Sin cambios" 안내.
       - r.ok=false 이면 #qr-status 에러 배너 + prominent 토스트(기존 토스트 함수). (에러 가시성 규약)
    4. 전체선택 체크박스 -> 모든 행 토글; 개별 토글/전체 토글 시 `#qr-selected-count` + 버튼 라벨 "Imprimir seleccionados (N)" 갱신.
    5. Imprimir seleccionados: 선택 행 productId 로 currentDelta 에서 items 구성 ->
       `const layout = { widthMm, heightMm, qrModule, splitRatio, fontSize };`
       `const mode = doble 선택 ? 'doble' : 'simple';`
       `const res = await window.electronAPI.qrPrint({ items:selected, layout, mode, priceTypeId:Number(priceTypeId) });`
       - res.ok 이면 성공 토스트 + 해당 행 제거(또는 재 Buscar 안내: 성공분은 다음 델타에서 빠짐).
       - res.failed?.length 이면 실패 productId 행에 실패 표시(빨강) + #qr-status 배너 + prominent 토스트.
    6. 모든 사용자 문구 스페인어. apiConnector 아님(에이전트는 electronAPI IPC). 에러 항상 인라인+토스트 둘 다.
  </action>
  <verify>
    <automated>cd zebra-agent && grep -q "qrFetchPending(" renderer/index.html && grep -q "qrPrint(" renderer/index.html && grep -q "fetchPriceTypes(" renderer/index.html && grep -qE "renderPreview|qr-preview" renderer/index.html && node -e "const h=require('fs').readFileSync('renderer/index.html','utf8'); if(!/qrFetchPending\(|qrPrint\(/.test(h)) process.exit(1); console.log('OK wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - Buscar 클릭이 qrFetchPending(priceTypeId) 호출 -> 델타 테이블 렌더 (NUEVO/CAMBIO 뱃지 + 체크박스 + 구->신 가격)
    - 수치 입력 변경이 renderPreview 로 라이브 반영, Guardar 로 label config 저장
    - simple/doble 토글 + 전체선택 + "Imprimir seleccionados (N)" 카운트 동작
    - Imprimir 이 선택분만 qrPrint({items,layout,mode,priceTypeId}) 호출; 실패분 행 표시 + 인라인 배너 + 토스트
    - r.ok=false / res.failed 모두 에러 가시성(인라인 + prominent 토스트) 처리
  </acceptance_criteria>
  <done>TAB3 가 델타 조회->선택->출력 전 흐름을 IPC 에 배선; 라이브 프리뷰 + 에러 가시성 완비.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자 -> TAB3 UI | 로컬 데스크탑 앱(신뢰 사용자); 입력은 수치/선택뿐 |
| renderer -> main (electronAPI) | contextIsolation=true, preload 브릿지로만 IPC (nodeIntegration=false) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-10 | Tampering | renderer 가 임의 priceTypeId/items 를 IPC 로 전달 | mitigate | 서버가 branch/store 를 API key 로 도출(38-01) -> 다른 매장 데이터 불가; items 는 currentDelta(서버 반환)에서만 구성 |
| T-38-11 | Information Disclosure | 프리뷰/델타에 타 매장 상품 노출 | accept | 델타는 서버가 store 격리 후 반환; 로컬 운영자는 자기 지점 데이터만 봄(단일 사용자 데스크탑) |
| T-38-12 | Elevation of Privilege | renderer 에서 node API 직접 접근 | mitigate | contextIsolation=true + nodeIntegration=false (기존 설정 유지); electronAPI 화이트리스트만 노출 |
</threat_model>

<verification>
- grep: renderer/index.html 에 data-tab="qr", #tab-qr, qrFetchPending(, qrPrint(, fetchPriceTypes(, renderPreview.
- 수동 UAT(dev, npm start): TAB3 열림 -> price-type 선택 -> Buscar cambios -> 델타 NUEVO/CAMBIO 렌더 ->
  수치 변경 시 프리뷰 갱신 -> 선택 출력(실프린터 연결 시) -> 재 Buscar 시 성공분 제외 -> 프린터 끄고 출력 시 실패 배너/토스트.
- 회귀: 기존 TAB1/TAB2 정상 동작(탭 전환 로직 편입 확인).
</verification>

<success_criteria>
- TAB3 2패널(좌 프리뷰+수치 / 우 델타 리스트+체크박스) 다크네이비+골드 (SC6/D-10).
- price-type 드롭다운 + 1/2 토글 + Buscar cambios(NUEVO/CAMBIO 뱃지, 구->신 가격) + Imprimir seleccionados (SC6).
- 성공분 자동 제외 + 실패 행 표시 + 에러 가시성(인라인+토스트) (SC7).
</success_criteria>

<output>
완료 후 `.planning/phases/38-codigomadre-qr-print/38-03-SUMMARY.md` 생성.
(커밋은 부모 레포에서 수행. zebra-agent 변경 push 시 CI 태그 자동 증가 - push 는 사용자 요청 시.)
</output>
</content>
