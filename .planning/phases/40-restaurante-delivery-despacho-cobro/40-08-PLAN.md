---
phase: 40-restaurante-delivery-despacho-cobro
plan: 08
type: execute
wave: 6
depends_on: [40-05, 40-06, 40-07]
files_modified:
  - ventago-app/src/views/restaurante/DeliveryBoard.tsx
  - ventago-app/src/views/restaurante/components/RiderSettlementView.tsx
  - ventago-app/src/views/restaurante/RestauranteShell.tsx
autonomous: false
requirements: [REQ-6, REQ-7, REQ-9]
must_haves:
  truths:
    - "The dispatch board shows columns Nuevo·En cocina·Listo·En camino·Por cobrar (+ Conciliación) and cards move on status transition"
    - "Board subscribes to /restaurant Socket.io and merges delivery_updated card payloads (no polling)"
    - "A Listo card's Asignar assigns an active rider; En camino is blocked without a rider"
    - "Por cobrar column (red) holds only cash delivered-but-unsettled orders; QR/app never appear there"
    - "Rider settlement view registers rendición → caja and flips orders to liquidado; payout CSV upload flags mismatches red"
  artifacts:
    - path: "ventago-app/src/views/restaurante/DeliveryBoard.tsx"
      provides: "kanban dispatch board with Socket.io push"
      contains: "delivery_updated"
    - path: "ventago-app/src/views/restaurante/components/RiderSettlementView.tsx"
      provides: "rider settlement + rendición + CSV upload UI"
      contains: "rider-settlement"
  key_links:
    - from: "DeliveryBoard"
      to: "/restaurant Socket.io gateway"
      via: "io(HOST+'/restaurant') + on('delivery_updated')"
      pattern: "delivery_updated"
    - from: "RiderSettlementView Registrar rendición"
      to: "POST /rider-settlement/:id/rendition"
      via: "apiConnector.post"
      pattern: "rendition"
    - from: "RiderSettlementView CSV upload"
      to: "POST /restaurant-delivery/payout/reconcile"
      via: "apiConnector.sendFile"
      pattern: "payout/reconcile"
---

<objective>
Build the dispatch board kanban (REQ-6) with Socket.io real-time push and the rider settlement view (REQ-7) including the payout-CSV upload (REQ-9 frontend), then wire both beside SalonView as a Delivery tab/segment. This completes the visible control surface and the central invariant (Por cobrar holds cash until settled).

Purpose: The board is where staff watch and advance orders; the Por cobrar column (red) makes the "Entregado ≠ closed" invariant visible. The settlement view is where cash is rendered into caja. CSV upload reconciles delivery-app orders. Ends with a human-verify checkpoint (visual + flow).
Output: DeliveryBoard + RiderSettlementView + restaurant shell wiring; checkpoint for UAT.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-SPEC.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-CONTEXT.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-PATTERNS.md
@.claude/skills/sketch-findings-ace-online/SKILL.md

<interfaces>
<!-- From plan 07: useDeliveryBoard(branchId) → {cards, mutate}, useRepartidores() (active filter), NuevoPedidoModal
     Backend: PATCH /restaurant-delivery/:id/transition {status, repartidorId?} ; GET /restaurant-delivery/board/:branchId ;
              POST /rider-settlement/build {repartidorId} ; GET /rider-settlement/:id ; POST /rider-settlement/:id/rendition {itemIds?, note?} ;
              POST /restaurant-delivery/payout/reconcile (multipart file, ?branchId=)
     Socket.io: io(`${HOST}/restaurant`, { auth: { token } }); emit('join',{branchId}); on('delivery_updated', card)
     Analogs: views/restaurante/SalonView.tsx (BranchContext+useAuth+branchId fallback L16-26, SWR+useMemo, MUI states),
              apiConnector.sendFile (CSV multipart), NAVY/GOLD theme
     Columns: Nuevo·En cocina·Listo·En camino·Por cobrar (+ Conciliación). Por cobrar styled red.
     next/dynamic(() => import(...), { ssr:false }) for code-splitting (CLAUDE.md 300ms) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeliveryBoard kanban + Socket.io push + Asignar rider + transitions</name>
  <read_first>
    - ventago-app/src/views/restaurante/SalonView.tsx (L16-26 branch resolution BranchContext+useAuth fallback; SWR + useMemo derived maps; MUI loading/Alert)
    - ventago-app/src/hooks/api/useDeliveryBoard.ts + useRepartidores.ts (plan 07)
    - ventago-app/src/views/restaurante/components/NuevoPedidoModal.tsx (plan 07 — board's "Nuevo pedido" button opens it)
    - any existing socket.io-client usage in the app (mp QR side-panel Phase 29 or print) for the io() + auth pattern + HOST resolution
    - .claude/skills/sketch-findings-ace-online/SKILL.md
    - 40-PATTERNS.md section "views/restaurante/DeliveryBoard.tsx" + 40-CONTEXT.md D-03/D-04
  </read_first>
  <files>ventago-app/src/views/restaurante/DeliveryBoard.tsx</files>
  <action>
    Create `DeliveryBoard.tsx` (REQ-6). Branch resolution: `const { user } = useAuth(); const { selectedBranchId } = useContext(BranchContext); const branchId = selectedBranchId ?? user?.branchId ?? undefined`. `const { cards, isLoading, error, mutate } = useDeliveryBoard(branchId)`.
    Columns (left→right): `Nuevo · En cocina · Listo · En camino · Por cobrar` + `Conciliación` (app bucket). Group cards by status via useMemo. Style `Por cobrar` column red (control core highlight per sketch-findings). Card shows: order no, customer, address, elapsed-time timer (fixed-threshold visual aid — compute from orderedAt; amber/red as time grows, display only), total, paymentMode badge (color=mode), rider chip (after assign).
    Socket.io subscription (D-04, NOT polling): `useEffect` — `const socket = io(`${HOST}/restaurant`, { auth: { token: localStorage.getItem('accessToken') } }); socket.emit('join', { branchId }); socket.on('delivery_updated', (card) => mutate(merge card into cards, false)); return () => socket.disconnect()` keyed on [branchId, mutate].
    Card actions: status advance buttons calling `apiConnector.patch('/restaurant-delivery/'+id+'/transition', { status })`. `Listo` card "Asignar" → dropdown of `useRepartidores()` filtered isActive → patch transition to `en_camino` with `{ status:'en_camino', repartidorId }`. Surface the backend BadRequest ("Asigná un repartidor") if En camino attempted without rider (auto via global error banner). "Nuevo pedido" button opens NuevoPedidoModal.
    ESLint compliance; theme via sketch-findings.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "delivery_updated" src/views/restaurante/DeliveryBoard.tsx && grep -q "/restaurant" src/views/restaurante/DeliveryBoard.tsx && grep -qi "Por cobrar" src/views/restaurante/DeliveryBoard.tsx && grep -qi "Conciliaci" src/views/restaurante/DeliveryBoard.tsx && grep -q "transition" src/views/restaurante/DeliveryBoard.tsx && grep -qi "Asignar\|repartidor" src/views/restaurante/DeliveryBoard.tsx && grep -q "useDeliveryBoard" src/views/restaurante/DeliveryBoard.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - board renders the 5 columns + Conciliación; Por cobrar visually red
    - Socket.io `io(.../restaurant)` + `on('delivery_updated')` merges via mutate (no refreshInterval)
    - Asignar uses active riders + transitions to en_camino with repartidorId; status advance via PATCH transition
  </acceptance_criteria>
  <done>Kanban board with live Socket.io updates, rider assign, transition buttons, red Por cobrar column.</done>
</task>

<task type="auto">
  <name>Task 2: RiderSettlementView (rendición → caja) + payout CSV upload</name>
  <read_first>
    - ventago-app/src/views/restaurante/SalonView.tsx (SWR + apiConnector + MUI patterns)
    - ventago-app/src/hooks/api/useRepartidores.ts (rider list)
    - ventago-app/src/services/api.service.ts (apiConnector.post + sendFile for CSV multipart — L179-186)
    - .claude/skills/sketch-findings-ace-online/SKILL.md
    - 40-PATTERNS.md section "views/restaurante/components/RiderSettlementView.tsx" + 40-CONTEXT.md D-05/D-06/D-07/D-08 + 40-SPEC.md REQ-7/REQ-9
  </read_first>
  <files>ventago-app/src/views/restaurante/components/RiderSettlementView.tsx</files>
  <action>
    Create `RiderSettlementView.tsx` (REQ-7 + REQ-9 frontend). Per active rider (useRepartidores): show entregas count, **efectivo a rendir** (emphasized), QR+app (info-only). Build/load settlement via `apiConnector.post('/rider-settlement/build', { repartidorId })` then `GET /rider-settlement/:id`. List the rider's cash orders with a `rendido` checkbox; un-rendido rows stay red. Show Esperado vs Recibido (contado input) → Diferencia (faltante/sobrante). Buttons: "Registrar rendición en caja" → `apiConnector.post('/rider-settlement/'+id+'/rendition', { itemIds: renderedIds, note })` (rendido orders → Liquidado + cash → caja; backend blocks if no open caja — surface that error). "Guardar parcial" → same endpoint with subset.
    Payout CSV section (REQ-9): file input → `apiConnector.sendFile('/restaurant-delivery/payout/reconcile?branchId='+branchId, formData)`. Render result: matched (auto-conciliado) vs flagged rows in RED (unmatched / amount-mismatch). 
    ESLint; theme via sketch-findings; apiConnector.remove() if any delete (not .delete()).
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "rider-settlement" src/views/restaurante/components/RiderSettlementView.tsx && grep -qi "rendition\|rendición\|rendir" src/views/restaurante/components/RiderSettlementView.tsx && grep -q "payout/reconcile" src/views/restaurante/components/RiderSettlementView.tsx && grep -q "sendFile" src/views/restaurante/components/RiderSettlementView.tsx && grep -qi "diferencia\|Esperado\|Recibido" src/views/restaurante/components/RiderSettlementView.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - posts `/rider-settlement/build` + `/rider-settlement/:id/rendition`; Esperado/Recibido/Diferencia shown
    - CSV upload via `apiConnector.sendFile` to `payout/reconcile`; flagged rows rendered red
    - un-rendido cash rows stay red until rendido
  </acceptance_criteria>
  <done>Settlement view renders rendición into caja and CSV reconciliation with red mismatch flags.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Delivery tab beside SalonView (code-split entry point)</name>
  <read_first>
    - ventago-app/src/views/restaurante/SalonView.tsx (how it's mounted in nueva-venta / restaurante route)
    - the page/route that renders SalonView (find with grep for "SalonView")
    - CLAUDE.md (next/dynamic ssr:false code-splitting rule)
    - 40-PATTERNS.md "DeliveryBoard.tsx" entry-point note (Claude's Discretion: tab/segment beside SalonView)
  </read_first>
  <files>ventago-app/src/views/restaurante/RestauranteShell.tsx</files>
  <action>
    Create `RestauranteShell.tsx`: a tab/segment switch between `Salón` (existing SalonView) and `Delivery` (DeliveryBoard) and `Liquidación` (RiderSettlementView) — all restaurant-mode only. Import the heavy views via `next/dynamic(() => import('...'), { ssr: false })` for code-splitting (CLAUDE.md 300ms target). Mount this shell where SalonView is currently rendered (replace the direct SalonView mount with RestauranteShell, preserving the Salón view as the default tab — do NOT break Phase 39 mesa flow). If simpler, add the Delivery/Liquidación tabs adjacent without disturbing the SalonView default.
    Honor ESLint; restaurant-mode gating consistent with Phase 39.
  </action>
  <verify>
    <automated>cd ventago-app && grep -q "DeliveryBoard" src/views/restaurante/RestauranteShell.tsx && grep -q "next/dynamic" src/views/restaurante/RestauranteShell.tsx && grep -qi "Delivery\|Liquidaci" src/views/restaurante/RestauranteShell.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - shell tab-switches Salón / Delivery / Liquidación; heavy views via `next/dynamic` ssr:false
    - SalonView (Phase 39 mesa flow) remains reachable and default — not broken
  </acceptance_criteria>
  <done>Delivery + Liquidación tabs mounted beside Salón with code-splitting; mesa flow preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — delivery lifecycle + control invariant + retail no-regression</name>
  <action>Run the end-to-end UAT below; this is a human-verify checkpoint that confirms the full delivery lifecycle, the Entregado≠closed control invariant, and retail no-regression after all backend + frontend plans are in place. No code is written in this task.</action>
  <what-built>Full delivery layer: order intake (Delivery/Takeaway), dispatch board with live Socket.io updates, rider assign + transitions, Por cobrar cash control, rider settlement → caja, QR auto-close, payout CSV reconcile. Plus the Repartidores config card.</what-built>
  <how-to-verify>
    Run `./dev.sh` (dev print-agent reflects latest code; prod print-agent requires re-install — see STATE.md blocker). In a restaurant-mode store:
    1. Configuración → confirm "Repartidores" card visible; add a rider; toggle activo. Switch to a NON-restaurant store → card must be hidden.
    2. Restaurante → Delivery tab → "Nuevo pedido": create a Delivery (efectivo) order with address + items → expect comanda print + card in "En cocina".
    3. Create a Takeaway order → confirm address/rider are NOT required.
    4. Advance card: En cocina → Listo → "Asignar" rider → En camino (try En camino without rider first → expect "Asigná un repartidor" error) → Entregado.
    5. Confirm efectivo Entregado card lands in red "Por cobrar" (NOT closed).
    6. Liquidación tab → build settlement for the rider → check rendido → "Registrar rendición en caja" (with caja closed first → expect "Abrí la caja" block; then open caja and retry) → order flips to Liquidado; verify a box movement appears in control-de-caja.
    7. Open a second browser/tab on the board → confirm a status change in tab A pushes to tab B via Socket.io (no refresh).
    8. Create a QR order, simulate/confirm MP payment → card closes without Por cobrar.
    9. Create an app order with externalRef → lands in Conciliación → upload a matching payout CSV → row auto-conciliado; upload a mismatching row → stays red.
    10. RETAIL NO-REGRESSION: in a retail (의류) store, run a normal POS sale + check ventas report still works and includes it; confirm delivery sale (source='delivery', activityType='sale') also appears in 매출 reports.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, expected vs actual)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser board → /restaurant Socket.io | Token sent in handshake auth; gateway (plan 03) authenticates + authorizes branch room |
| CSV upload UI → reconcile endpoint | File leaves browser; backend (plan 06) validates + parses safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-28 | Spoofing | board Socket.io connect | mitigate | Client sends accessToken in `io(..., { auth:{token} })`; gateway validates + disconnects invalid (plan 03 T-40-08) |
| T-40-29 | Information Disclosure | cross-branch board cards | mitigate | join authorized server-side by store ownership of branchId (plan 03 T-40-09); UI only requests its own branch |
| T-40-30 | Tampering | UI-driven status skip | mitigate | All transitions go through PATCH endpoint with server-side guard (en_camino rider check) + DB CHECK enum bounds; UI cannot force an invalid state |
| T-40-31 | Business-rule bypass | settle UI without caja | mitigate | Backend (plan 05 D-06) blocks rendición when no open caja; UI surfaces the error — cash cannot bypass control-de-caja |
</threat_model>

<verification>
- grep checks in task verify blocks
- `cd ventago-app && npm run lint` passes for the 3 new files (ESLint build-blockers)
- `cd ventago-app && npx tsc --noEmit` no new errors
- Human-verify checkpoint covers the full lifecycle + retail no-regression
</verification>

<success_criteria>
- Board: 5 columns + Conciliación, Socket.io live, rider assign, transitions, red Por cobrar
- Settlement: rendición → caja, Liquidado flip, CSV reconcile with red flags
- Delivery tab code-split beside Salón; mesa flow preserved
- Control invariant + retail no-regression confirmed via UAT
</success_criteria>

<output>
After completion, create `.planning/phases/40-restaurante-delivery-despacho-cobro/40-08-SUMMARY.md`.
</output>
