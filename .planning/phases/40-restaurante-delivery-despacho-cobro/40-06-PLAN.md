---
phase: 40-restaurante-delivery-despacho-cobro
plan: 06
type: execute
wave: 4
depends_on: [40-04]
files_modified:
  - api-ventago/src/app/restaurant-delivery/delivery-payout-csv.service.ts
  - api-ventago/src/app/restaurant-delivery/dto/payout-match.dto.ts
  - api-ventago/src/app/restaurant-delivery/restaurant-delivery.controller.ts
  - api-ventago/src/app/restaurant-delivery/restaurant-delivery.module.ts
  - api-ventago/src/app/mercadopago/mp-webhook.service.ts
autonomous: true
requirements: [REQ-8, REQ-9]
must_haves:
  truths:
    - "Uploading a payout CSV stores the original file in MinIO and matches rows to deliveries by externalRef + exact amount"
    - "Matched orders auto-flip to conciliado; unmatched/amount-mismatch rows are flagged red"
    - "A paymentMode=qr delivery, when its MP payment webhook confirms, is auto-closed without entering Por cobrar or cash settlement"
  artifacts:
    - path: "api-ventago/src/app/restaurant-delivery/delivery-payout-csv.service.ts"
      provides: "CSV upload + parse + externalRef exact-match reconciliation"
      contains: "uploadFile"
    - path: "api-ventago/src/app/mercadopago/mp-webhook.service.ts"
      provides: "delivery QR auto-close hook"
      contains: "delivery"
  key_links:
    - from: "DeliveryPayoutCsvService.reconcile"
      to: "restaurant_deliveries.external_ref"
      via: "exact externalRef + exact amount match"
      pattern: "externalRef"
    - from: "mp-webhook payment confirm"
      to: "RestaurantDelivery qr auto-close"
      via: "delivery lookup by sale + mark liquidado"
      pattern: "RestaurantDelivery"
---

<objective>
Add the two automated-collection paths: (REQ-9) the delivery-app L1 payout CSV reconciliation service (MinIO store + fixed-template parse + externalRef exact-match) and (REQ-8) the MercadoPago QR auto-close hook that closes a `paymentMode=qr` delivery when its existing webhook confirms payment.

Purpose: These are the two non-cash collection terminals. QR closes automatically via the EXISTING mp-webhook (no new webhook infra — reuse). App orders sit in `conciliacion` until a payout CSV matches them by `externalRef` (D-08, exact match only, no tolerance — out of scope). Both must keep QR/app orders out of cash `por_cobrar`.
Output: CSV service + DTO + controller endpoint + module wiring (MinioModule); minimal additive hook in mp-webhook.service.ts.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-SPEC.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-CONTEXT.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-PATTERNS.md

<interfaces>
<!-- From plan 03/04: RestaurantDelivery model, DeliveryStatus.{CONCILIACION,LIQUIDADO}, PaymentMode.{QR,APP}, externalRef column, RestaurantDeliveryGateway.emitDeliveryUpdated
     MinioService.uploadFile(file, fileName) → { fileName } (CLAUDE.md)
     mercadopago/mp-webhook.service.ts: existing payment-confirm path that resolves a sale/intent → add delivery hook there (additive, no change to retail/POS/mesa behavior)
     apiConnector.sendFile (frontend) → multipart; controller endpoint must accept file upload (@UseInterceptors(FileInterceptor)) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: delivery-payout-csv.service.ts + DTO + controller endpoint + MinioModule wiring</name>
  <read_first>
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.service.ts (TX pattern + gateway emit to reuse)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.controller.ts (existing routes — add the upload route)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.module.ts (add MinioModule + provider)
    - any existing controller using FileInterceptor / MinioService.uploadFile (e.g. store logo upload — find the multipart upload pattern)
    - 40-PATTERNS.md section "app/restaurant-delivery/delivery-payout-csv.service.ts" + "No Analog Found" note + 40-CONTEXT.md D-07/D-08
  </read_first>
  <files>api-ventago/src/app/restaurant-delivery/delivery-payout-csv.service.ts, api-ventago/src/app/restaurant-delivery/dto/payout-match.dto.ts, api-ventago/src/app/restaurant-delivery/restaurant-delivery.controller.ts, api-ventago/src/app/restaurant-delivery/restaurant-delivery.module.ts</files>
  <action>
    Create `delivery-payout-csv.service.ts` (`@Injectable()`, REQ-9). DI: `@InjectModel(RestaurantDelivery)`, `@InjectConnection() sequelize`, `MinioService`. Method `reconcile(storeId, branchId, file)`:
      - D-07: `const { fileName } = await this.minioService.uploadFile(file, `payout-${Date.now()}.csv`)` — store original CSV for audit trail.
      - D-08: parse the fixed-template CSV (columns: externalRef, amount — define exact header names, e.g. `external_ref,amount`). Parse safely (guard malformed rows; validate file is CSV/text and within a size limit before parsing — T-40-20).
      - For each CSV row: find delivery `{storeId, externalRef: row.externalRef, paymentMode: PaymentMode.APP}`. Match requires exact externalRef AND exact amount equality (NO tolerance — SPEC out of scope). Matched + status=conciliacion → update `{ status: DeliveryStatus.LIQUIDADO, settledAt: new Date() }` (conciliado auto-confirm) and emit card via gateway. Unmatched OR amount-mismatch → collect into a `flagged` list (returned for red display).
      - Return `{ matched: [...], flagged: [{ externalRef, reason }], fileName }`.
    Create `dto/payout-match.dto.ts`: response/result shape interface (matched ids + flagged rows + fileName). (Upload itself is multipart, no body DTO.)
    Edit `restaurant-delivery.controller.ts`: add `@Post('payout/reconcile')` `@Auth()` `@UseInterceptors(FileInterceptor('file'))` reconcile(@UploadedFile() file, @Query('branchId') branchId, @GetUser user) → `csvService.reconcile(user.storeId, +branchId, file)`. Inject DeliveryPayoutCsvService.
    Edit `restaurant-delivery.module.ts`: add `MinioModule` to imports; add `DeliveryPayoutCsvService` to providers (and exports if needed). Honor ESLint (no-unused-vars, newline-before-return).
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "uploadFile" src/app/restaurant-delivery/delivery-payout-csv.service.ts && grep -q "externalRef" src/app/restaurant-delivery/delivery-payout-csv.service.ts && grep -q "PaymentMode.APP" src/app/restaurant-delivery/delivery-payout-csv.service.ts && grep -q "DeliveryStatus.LIQUIDADO" src/app/restaurant-delivery/delivery-payout-csv.service.ts && grep -q "flagged" src/app/restaurant-delivery/delivery-payout-csv.service.ts && grep -q "payout/reconcile" src/app/restaurant-delivery/restaurant-delivery.controller.ts && grep -q "FileInterceptor" src/app/restaurant-delivery/restaurant-delivery.controller.ts && grep -q "MinioModule" src/app/restaurant-delivery/restaurant-delivery.module.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - csv service: `minioService.uploadFile` (audit), match by `externalRef` + `PaymentMode.APP`, exact-amount equality, matched → `DeliveryStatus.LIQUIDADO`, `flagged` list returned
    - controller `payout/reconcile` route with `FileInterceptor` + `@Auth()`
    - module imports `MinioModule`, providers include `DeliveryPayoutCsvService`
    - parsing guards file type/size before parse
  </acceptance_criteria>
  <done>CSV reconcile service stores file in MinIO, exact-matches by externalRef+amount, auto-confirms matched, flags mismatches; endpoint + module wired.</done>
</task>

<task type="auto">
  <name>Task 2: MercadoPago QR webhook auto-close hook for delivery</name>
  <read_first>
    - api-ventago/src/app/mercadopago/mp-webhook.service.ts (the existing payment-confirm path — find where a confirmed payment resolves its sale/intent; the hook must be ADDITIVE and not alter retail/POS/mesa behavior)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.model.ts (DeliveryStatus, PaymentMode.QR)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.service.ts (gateway emit helper / toCard)
    - 40-SPEC.md REQ-8 + 40-CONTEXT.md D-01 (qr → Sale PAID + auto-close, no Por cobrar)
  </read_first>
  <files>api-ventago/src/app/mercadopago/mp-webhook.service.ts</files>
  <action>
    In `mp-webhook.service.ts`, at the point where a payment is confirmed and its Sale is resolved, add an ADDITIVE hook (REQ-8): after the existing sale-payment confirmation, look up `RestaurantDelivery` by that `saleId` where `paymentMode=PaymentMode.QR`. If found, set `{ status: DeliveryStatus.LIQUIDADO, settledAt: new Date() }` (QR closes at payment confirm — D-01, no Por cobrar, no cash settlement) and emit the updated card via the RestaurantDeliveryGateway. If no delivery row (normal POS/mesa/retail sale), do nothing — behavior unchanged.
    To access RestaurantDelivery model + gateway from mp-webhook: inject `@InjectModel(RestaurantDelivery)` and `RestaurantDeliveryGateway` (import RestaurantDeliveryModule into MercadopagoModule, or pass via existing module graph — verify MercadopagoModule imports and add RestaurantDeliveryModule to its imports if needed). Keep the change surgical and guarded so it NEVER throws into the webhook 200-response path (wrap in try/catch; webhook must still return 200 — existing Phase 29 invariant).
    ESLint compliance. Do NOT change any existing retail/POS/mesa code path.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "RestaurantDelivery" src/app/mercadopago/mp-webhook.service.ts && grep -q "PaymentMode.QR" src/app/mercadopago/mp-webhook.service.ts && grep -q "DeliveryStatus.LIQUIDADO" src/app/mercadopago/mp-webhook.service.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - mp-webhook.service.ts looks up `RestaurantDelivery` by saleId with `PaymentMode.QR` and sets `DeliveryStatus.LIQUIDADO` on payment confirm
    - hook is guarded (try/catch) so webhook still returns 200 and non-delivery sales are unaffected
    - no existing retail/POS/mesa branch modified (additive only)
  </acceptance_criteria>
  <done>QR delivery auto-closes on webhook payment confirm without entering Por cobrar; non-delivery sales unaffected; webhook 200 invariant preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CSV upload | Untrusted file content; must validate type/size and parse safely |
| MercadoPago → webhook | External callback; existing Phase 29 webhook auth/idempotency reused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-20 | Tampering / DoS | payout CSV upload | mitigate | Validate file is CSV/text and within size limit before parsing; parse defensively (skip malformed rows, no eval); store original in MinIO for audit (D-07) |
| T-40-21 | IDOR | reconcile across stores | mitigate | delivery lookup scoped by `storeId` (from JWT) + externalRef; cannot match another store's orders |
| T-40-22 | Tampering / double-settle | CSV re-upload | mitigate | only deliveries in status=conciliacion are flipped to liquidado; already-liquidado rows are not re-matched (exact-match + status guard) |
| T-40-23 | Spoofing | forged webhook | accept | Reuses Phase 29 mp-webhook auth + canonical re-fetch of payment (existing mitigation); delivery hook only acts on already-confirmed sales |
| T-40-24 | DoS | exception in webhook | mitigate | delivery hook wrapped in try/catch; webhook always returns 200 (Phase 29 invariant) |
</threat_model>

<verification>
- grep checks in task verify blocks
- `cd api-ventago && npm run build` succeeds (MinioModule + RestaurantDeliveryModule DI resolves in both modules)
- Retail no-regression reasoning: mp-webhook change is guarded/additive; grep confirms no edits to POS/mesa sale resolution lines
</verification>

<success_criteria>
- Payout CSV stored in MinIO, exact-matched by externalRef+amount, matched auto-conciliado, mismatches flagged
- QR delivery auto-closes via existing webhook; never enters Por cobrar / cash settlement
- Webhook 200 invariant + retail/POS/mesa behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/40-restaurante-delivery-despacho-cobro/40-06-SUMMARY.md`.
</output>
