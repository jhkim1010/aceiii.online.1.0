---
phase: 40-restaurante-delivery-despacho-cobro
plan: 05
type: execute
wave: 4
depends_on: [40-02, 40-04]
files_modified:
  - api-ventago/src/app/rider-settlement/rider-settlement.model.ts
  - api-ventago/src/app/rider-settlement/dto/rider-settlement.dto.ts
  - api-ventago/src/app/rider-settlement/rider-settlement.service.ts
  - api-ventago/src/app/rider-settlement/rider-settlement.controller.ts
  - api-ventago/src/app/rider-settlement/rider-settlement.module.ts
  - api-ventago/src/app.module.ts
autonomous: true
requirements: [REQ-3, REQ-7]
must_haves:
  truths:
    - "A settlement aggregates a rider's cash (por_cobrar) delivery orders with rendido flags and computes expectedCash/receivedCash/difference"
    - "registerRendition records ONE aggregated box (caja) movement via BoxOperationService.addOperation and transitions rendido orders to liquidado"
    - "Rendition is BLOCKED if no open cashRegister (closingTime=null) exists — stricter than Phase 39"
    - "QR/app orders never appear in a cash settlement"
  artifacts:
    - path: "api-ventago/src/app/rider-settlement/rider-settlement.model.ts"
      provides: "RiderSettlement + RiderSettlementItem models"
      contains: "rider_settlement_items"
    - path: "api-ventago/src/app/rider-settlement/rider-settlement.service.ts"
      provides: "build settlement, registerRendition → box movement"
      contains: "addOperation"
    - path: "api-ventago/src/app/rider-settlement/rider-settlement.controller.ts"
      provides: "REST endpoints for settlement + rendition"
      contains: "@Controller('rider-settlement')"
  key_links:
    - from: "RiderSettlementService.registerRendition"
      to: "BoxOperationService.addOperation"
      via: "single aggregated movement inside TX"
      pattern: "boxOperationService.addOperation"
    - from: "registerRendition"
      to: "RestaurantDelivery.status=liquidado"
      via: "update rendido items"
      pattern: "DeliveryStatus.LIQUIDADO"
---

<objective>
Implement the RiderSettlement backend: parent+child models, service that builds a rider's cash settlement from `por_cobrar` deliveries and registers the rendition as ONE aggregated box (caja) movement, controller, and module. This is the money-path terminus of the control invariant (REQ-3, REQ-7).

Purpose: This is where cash leaves "Por cobrar" and lands in caja. D-05: one aggregated box movement per settlement (not per order). D-06: STRICTER than Phase 39 — if no open cashRegister, the rendition is blocked (cash must reconcile with control-de-caja). rendido orders flip to `liquidado`.
Output: complete `api-ventago/src/app/rider-settlement/` module registered in app.module.ts.
</objective>

<execution_context>
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcoskim/Trabajos_Programming/ACE_online_1.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-SPEC.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-CONTEXT.md
@.planning/phases/40-restaurante-delivery-despacho-cobro/40-PATTERNS.md

<interfaces>
<!-- From plan 03/04: RestaurantDelivery model + DeliveryStatus.{POR_COBRAR,LIQUIDADO} enums + PaymentMode.EFECTIVO
     From plan 02: Repartidor model (RepartidoresModule exports it)
     box-operation.service.ts addOperation({cashRegisterId, userId, terminalId, amount, type, executionType, description}, t)
     cash_registers model: id, terminal_id (terminalId), user_id (userId), closing_time (closingTime), store_id
     Primary analog: restaurant-sale.service.ts recordBoxOperation L650-675 (BUT we THROW on no register, not skip — D-06)
     app.module.ts at api-ventago/src/app.module.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RiderSettlement + RiderSettlementItem models + DTOs</name>
  <read_first>
    - api-ventago/src/app/restaurant-tables/restaurant-tables.model.ts (table shape, field: snake_case)
    - api-ventago/src/app/sales/sales.model.ts (L241-251 @HasMany pattern for items)
    - api-ventago/src/app/sales/sales-payment-methods/sales-payment-method.model.ts (child-row model shape)
    - 40-PATTERNS.md section "app/rider-settlement/rider-settlement.model.ts"
    - 40-03-PLAN.md (column names for rider_settlements + rider_settlement_items to mirror)
  </read_first>
  <files>api-ventago/src/app/rider-settlement/rider-settlement.model.ts, api-ventago/src/app/rider-settlement/dto/rider-settlement.dto.ts</files>
  <action>
    Create `rider-settlement.model.ts` with TWO models:
      `@Table({ tableName: 'rider_settlements', timestamps: true }) class RiderSettlement extends Model`: storeId (`store_id`, FK Store), repartidorId (`repartidor_id`, FK Repartidor), boxSessionId (`box_session_id`, INTEGER nullable — references cash_registers.id), expectedCash (`expected_cash`, DataType.FLOAT defaultValue 0), receivedCash (`received_cash`, FLOAT 0), difference (FLOAT 0), status (DataType.STRING(16) defaultValue 'open'), note (TEXT nullable), openedAt (`opened_at` DATE), closedAt (`closed_at` DATE), `@HasMany(() => RiderSettlementItem) items`.
      `@Table({ tableName: 'rider_settlement_items', timestamps: true }) class RiderSettlementItem extends Model`: settlementId (`settlement_id`, `@ForeignKey(() => RiderSettlement)`), restaurantDeliveryId (`restaurant_delivery_id`, INTEGER), amount (FLOAT 0), rendido (BOOLEAN defaultValue false), `@BelongsTo(() => RiderSettlement)`.
    Create `dto/rider-settlement.dto.ts`: `BuildSettlementDto { @IsInt repartidorId }`, `RegisterRenditionDto { @IsOptional @IsArray itemIds?: number[]; @IsOptional @IsString note? }` (itemIds = rendido subset for partial; note optional). Honor ESLint.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "tableName: 'rider_settlements'" src/app/rider-settlement/rider-settlement.model.ts && grep -q "tableName: 'rider_settlement_items'" src/app/rider-settlement/rider-settlement.model.ts && grep -q "@HasMany" src/app/rider-settlement/rider-settlement.model.ts && grep -q "field: 'box_session_id'" src/app/rider-settlement/rider-settlement.model.ts && grep -q "field: 'restaurant_delivery_id'" src/app/rider-settlement/rider-settlement.model.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - both `rider_settlements` and `rider_settlement_items` tableNames present
    - `@HasMany` items relation; `field: 'box_session_id'`, `field: 'restaurant_delivery_id'`, `rendido` column
    - DTOs export `RegisterRenditionDto`
  </acceptance_criteria>
  <done>Parent + child models compile with HasMany; DTOs present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: settlement service (build + registerRendition → box movement) + controller + module</name>
  <read_first>
    - api-ventago/src/app/sales/restaurant-sale/restaurant-sale.service.ts (recordBoxOperation L650-675 — open-register lookup; NOTE we throw, not skip)
    - api-ventago/src/app/box-operation/box-operation.service.ts (addOperation L16-32 signature + how it's imported as a module/provider)
    - api-ventago/src/app/restaurant-delivery/restaurant-delivery.model.ts (DeliveryStatus.POR_COBRAR/LIQUIDADO, PaymentMode.EFECTIVO)
    - api-ventago/src/app/sales/restaurant-sale/restaurant-sale.module.ts (how BoxOperationService + cash_registers model are wired into a module)
    - api-ventago/src/app.module.ts
    - 40-PATTERNS.md section "app/rider-settlement/rider-settlement.service.ts" (full code excerpt + D-05/D-06 critical-difference note) + 40-CONTEXT.md D-05/D-06
  </read_first>
  <files>api-ventago/src/app/rider-settlement/rider-settlement.service.ts, api-ventago/src/app/rider-settlement/rider-settlement.controller.ts, api-ventago/src/app/rider-settlement/rider-settlement.module.ts, api-ventago/src/app.module.ts</files>
  <behavior>
    - buildSettlement(storeId, repartidorId) → creates/returns open settlement with items = that rider's efectivo deliveries in status=por_cobrar; expectedCash=Σ amounts
    - registerRendition with no open cashRegister → throws BadRequestException('Abrí la caja...')
    - registerRendition with open register → ONE addOperation call (amount=Σ rendido item amounts), rendido deliveries → status=liquidado + settledAt, settlement → closed + receivedCash set
    - QR/app deliveries are never selected (only paymentMode=efectivo + status=por_cobrar)
    - partial: if itemIds provided, only those flip rendido/liquidado; settlement stays partial
  </behavior>
  <action>
    Create `rider-settlement.service.ts` (`@Injectable()`). DI: `@InjectModel(RiderSettlement)`, `@InjectModel(RiderSettlementItem)`, `@InjectModel(RestaurantDelivery)`, `@InjectModel(CashRegister)` (cash_registers model), `@InjectConnection() sequelize`, `BoxOperationService`. Methods:
      - `buildSettlement(storeId, repartidorId)`: TX. find/create open settlement for rider. Select rider's deliveries where `{storeId, repartidorId, paymentMode: PaymentMode.EFECTIVO, status: DeliveryStatus.POR_COBRAR}` (single scoped SELECT — REQ-7 AC③: only efectivo+por_cobrar, never qr/app). Create RiderSettlementItem per delivery {restaurantDeliveryId, amount=sale total, rendido:false}. Set expectedCash=Σ amount. Return settlement with items.
      - `registerRendition(storeId, userId, settlementId, dto)` (REQ-7, D-05/D-06): TX. findOne settlement `{id, storeId}` (NotFound). **D-06: find open cashRegister `findOne({ userId, closingTime: null })`; if none → `throw new BadRequestException('Abrí la caja antes de registrar la rendición')`** (do NOT skip like Phase 39). Determine rendido items (dto.itemIds subset or all). total=Σ rendido amounts. **D-05: ONE** `boxOperationService.addOperation({ cashRegisterId: cashRegister.id, userId, terminalId: cashRegister.terminalId, amount: total, type:'venta', executionType:'automatico', description:`Rendición repartidor #${settlement.repartidorId}` }, t)`. Update rendido items rendido=true. Update their RestaurantDelivery rows → `{ status: DeliveryStatus.LIQUIDADO, settledAt: new Date() }`. Update settlement → receivedCash=total, difference=expectedCash-total, status= (all rendido ? 'closed' : 'partial'), closedAt (if closed). Return settlement.
    Create `rider-settlement.controller.ts`: `@Controller('rider-settlement')`, `@Auth()`: `@Post('build')` build(@Body dto, @GetUser user)→ buildSettlement(user.storeId, dto.repartidorId); `@Get(':id')` get(@Param id, @GetUser user); `@Post(':id/rendition')` rendition(@Param id, @Body dto: RegisterRenditionDto, @GetUser user)→ registerRendition(user.storeId, user.id, +id, dto).
    Create `rider-settlement.module.ts`: imports SequelizeModule.forFeature([RiderSettlement, RiderSettlementItem, RestaurantDelivery, CashRegister]) + the module exporting BoxOperationService + RepartidoresModule; providers [RiderSettlementService]; controllers [RiderSettlementController]; exports [RiderSettlementService]. (Check restaurant-sale.module.ts for how BoxOperationService is provided/imported.)
    Edit `app.module.ts`: add RiderSettlementModule to imports. ESLint compliance.
  </action>
  <verify>
    <automated>cd api-ventago && grep -q "boxOperationService.addOperation" src/app/rider-settlement/rider-settlement.service.ts && grep -q "closingTime: null" src/app/rider-settlement/rider-settlement.service.ts && grep -q "Abrí la caja" src/app/rider-settlement/rider-settlement.service.ts && grep -q "DeliveryStatus.LIQUIDADO" src/app/rider-settlement/rider-settlement.service.ts && grep -q "PaymentMode.EFECTIVO" src/app/rider-settlement/rider-settlement.service.ts && grep -q "DeliveryStatus.POR_COBRAR" src/app/rider-settlement/rider-settlement.service.ts && grep -q "@Controller('rider-settlement')" src/app/rider-settlement/rider-settlement.controller.ts && grep -q "RiderSettlementModule" src/app.module.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - service: `boxOperationService.addOperation` called; `closingTime: null` lookup + `Abrí la caja` throw (no skip)
    - settlement selection filters `PaymentMode.EFECTIVO` + `DeliveryStatus.POR_COBRAR` (qr/app excluded)
    - rendido deliveries set to `DeliveryStatus.LIQUIDADO`
    - exactly one `addOperation` call site (aggregated, D-05) — grep count of `addOperation(` is 1
    - controller `@Controller('rider-settlement')` with build/:id/rendition; module imported in app.module.ts
  </acceptance_criteria>
  <done>Settlement service blocks on no open caja, records one aggregated box movement, flips rendido orders to liquidado; controller + module wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /rider-settlement REST | Authenticated; storeId/userId from JWT |
| settlement → caja money path | Cash must hit caja exactly once; no double-credit, no cash bypassing control-de-caja |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-16 | Tampering / double-credit | registerRendition box movement | mitigate | Whole rendition is one TX; one aggregated addOperation; rendido items flagged so re-running cannot re-credit already-liquidado orders (status=por_cobrar filter excludes them) |
| T-40-17 | Business-rule bypass | cash skipping caja | mitigate | D-06: throws BadRequestException if no open cashRegister — cash cannot be settled outside an open caja session, guaranteeing control-de-caja reconciliation |
| T-40-18 | IDOR | settlement by id / repartidor | mitigate | findOne `{id, storeId}` from JWT; rider/delivery selection scoped by storeId |
| T-40-19 | Information Disclosure | qr/app in cash settlement | mitigate | settlement selection strictly filters paymentMode=efectivo + status=por_cobrar (REQ-7 AC③) — qr/app never enter cash flow |
</threat_model>

<verification>
- grep checks in task verify blocks (incl. single addOperation call site)
- `cd api-ventago && npm run build` succeeds (DI graph for BoxOperationService + CashRegister resolves)
</verification>

<success_criteria>
- Settlement aggregates efectivo por_cobrar orders per rider; expected/received/difference computed
- registerRendition blocks without open caja (D-06), records ONE box movement (D-05), flips rendido→liquidado
- QR/app never in cash settlement
- Controller storeId-scoped; module registered
</success_criteria>

<output>
After completion, create `.planning/phases/40-restaurante-delivery-despacho-cobro/40-05-SUMMARY.md`.
</output>
