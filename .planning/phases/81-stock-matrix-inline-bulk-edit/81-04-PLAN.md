---
phase: 81-stock-matrix-inline-bulk-edit
plan: 04
type: execute
wave: 3
depends_on: [81-01, 81-02, 81-03]
files_modified:
  - api-ventago/src/app/reports/reportsStocksCockpit.adjustBatch.spec.ts
autonomous: true
requirements: [R1, R2, R3, R4]

must_haves:
  truths:
    - "조용히 잘못된 재고가 되는 경로가 테스트로 고정된다"
---

<objective>
배치 조정의 회귀를 고정한다. CODEX 가 제시한 목록을 그대로 채운다.

Purpose: 이 기능은 **한 번에 여러 행을 원장에 쓴다.** 틀리면 피해가 셀 수만큼 곱해지고,
화면은 성공한 것처럼 보인다.
</objective>

<constraints>
- jest 는 `--maxWorkers=1`(메모리 `jest-maxworkers-1-required`).
- 각 테스트는 **검증 대상을 지웠을 때 실패**해야 한다 — 최소 2건은 변이 테스트로 확인한다
  (메모리 `test-can-pass-on-the-wrong-guard`).
</constraints>

<tasks>
1. 한 항목 기준값 불일치 → **INSERT 0건** + 409
2. 중간 INSERT 실패 → 전체 롤백
3. 입력 순서를 뒤섞어도 락 순서가 `product_id, pb_id` 오름차순
4. 중복 PB 거부 / 최대 항목 수 초과 거부
5. 타 store·타 branch PB 가 하나라도 섞이면 전체 거부
6. 부모 PB 가 하나라도 섞이면 전체 거부
7. 모든 INSERT 가 같은 transaction·같은 batchId
8. `available` 사용 + 잔액 없는 PB 의 집합 폴백
9. 같은 Idempotency-Key 재요청 시 원장 행이 늘지 않는다
</tasks>

<verification>
- 전 항목 통과 + 변이 테스트 2건으로 "지우면 실패" 확인
- 운영 적용 후 `stock_adjust_batches` 첫 행 확인(기준선)
</verification>

<success-criteria>
- 곱해지는 피해 경로가 테스트로 막힌다
</success-criteria>
