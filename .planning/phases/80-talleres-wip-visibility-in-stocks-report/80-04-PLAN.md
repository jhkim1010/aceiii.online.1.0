---
phase: 80-talleres-wip-visibility-in-stocks-report
plan: 04
type: execute
wave: 3
depends_on: [80-01, 80-02, 80-03]
files_modified:
  - api-ventago/src/app/reports/wip-allocation.spec.ts
  - api-ventago/src/app/reports/reportsStocksCockpit.wip.spec.ts
autonomous: true
requirements: [R1, R2, R3, R4, R5]

must_haves:
  truths:
    - "합계 보존·원장 복제·모집단·권한 우회가 테스트로 고정된다"
    - "데이터 불일치가 0 으로 숨지 않고 진단 지표로 남는다"
---

<objective>
"조용히 틀리는" 경로를 테스트로 고정한다.

Purpose: 이 phase 의 값은 전부 **추정 또는 파생**이라, 틀려도 화면은 그럴듯하다. 틀림을
드러내는 것은 테스트뿐이다.
</objective>

<constraints>
- jest 는 `--maxWorkers=1` 로만 돌린다(메모리 `jest-maxworkers-1-required`).
- spec 이 `rootDir` 밖을 import 하면 0건 실행된다(메모리 `jest-rootdir-import-oom`).
</constraints>

<tasks>
1. `allocateEqually` 순수 테스트: 1000/2 · 1000/3 · 1/3 · 0/3 · total<N · **N=0 → UNALLOCATABLE**.
   불변식: **분배 합 === 원본 총량** (모든 케이스).
2. 집계 테스트: stocks 원장 100행/0행에서 WIP 동일 / 분할 발송 2공정 동시 / rework 포함 /
   부분 수령·불량 / 과거 due_date · null 혼재 / madre+leaf 1:N / madre 직접 로트.
3. 권한 테스트: 비권한 응답에 **키 부재**, `sortBy=wip*` 400, export 컬럼 부재.
   ★ 가드가 여럿이면 엉뚱한 가드로 통과할 수 있다(메모리 `test-can-pass-on-the-wrong-guard`) —
   **검증 대상을 지우면 실패하는지** 확인할 것.
4. 진단 지표: 열린 상태인데 `pending ≤ 0` / 완료·취소인데 `pending > 0` / `pending > quantity` /
   envío 와 lote 의 `store_id` 불일치 / routing 비정상 / 활성 지점 0 — 카운터로 남긴다.
</tasks>

<verification>
- 전 케이스 통과 + 각 테스트가 대상 코드를 지웠을 때 실패하는지 1회 확인
- 운영에서 진단 카운터 조회 → 현재 값 기록(기준선)
</verification>

<success-criteria>
- 합계 보존이 코드가 아니라 테스트로 보장된다
- 권한 우회 경로 4곳이 고정된다
</success-criteria>
