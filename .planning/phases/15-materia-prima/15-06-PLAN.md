---
phase: 15-materia-prima
plan: 06
type: execute
wave: 2
depends_on: ["15-05"]
files_modified:
  - ventago-app/src/views/materia-prima/components/CategoryDistributionChart.tsx
  - ventago-app/src/views/materia-prima/components/DebtSummaryChart.tsx
  - ventago-app/src/views/materia-prima/MateriaPrimaDashboardView.tsx
autonomous: true
requirements:
  - MPRIMA-05
  - MPRIMA-07

must_haves:
  truths:
    - "Dashboard has a 2-column grid layout with alerts+distribution on top row, debt+movements on bottom row"
    - "Category distribution is shown as a CSS-only vertical bar chart with category colors"
    - "Supplier debt summary is shown as horizontal bar chart with amounts"
    - "KPI cards use the shared KpiCard component with 3px top indicators"
    - "Dashboard alerts section shows stock alerts with category color and critical styling"
  artifacts:
    - path: "ventago-app/src/views/materia-prima/components/CategoryDistributionChart.tsx"
      provides: "CSS-only vertical bar chart for category distribution"
      exports: ["CategoryDistributionChart"]
    - path: "ventago-app/src/views/materia-prima/components/DebtSummaryChart.tsx"
      provides: "Horizontal bar chart for supplier debt summary"
      exports: ["DebtSummaryChart"]
    - path: "ventago-app/src/views/materia-prima/MateriaPrimaDashboardView.tsx"
      provides: "Refactored Dashboard with 2-col grid, KpiCard, charts"
  key_links:
    - from: "MateriaPrimaDashboardView.tsx"
      to: "components/KpiCard"
      via: "import KpiCard"
      pattern: "import.*KpiCard"
    - from: "MateriaPrimaDashboardView.tsx"
      to: "components/CategoryDistributionChart"
      via: "import CategoryDistributionChart"
      pattern: "import.*CategoryDistributionChart"
    - from: "MateriaPrimaDashboardView.tsx"
      to: "components/DebtSummaryChart"
      via: "import DebtSummaryChart"
      pattern: "import.*DebtSummaryChart"
---

<objective>
Refactor MateriaPrimaDashboardView to match the HTML mockup's 2-column grid layout with CSS bar charts for category distribution and supplier debt summary. Replace basic KPI cards with the shared KpiCard component.

Purpose: Deliver the full Dashboard design (per D-05, D-06, D-07, D-08) with category distribution chart and debt summary that are currently missing.
Output: 2 new chart components + fully refactored Dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-materia-prima/15-CONTEXT.md
@.planning/phases/15-materia-prima/materia-prima-mockup.html
@.planning/codebase/CONVENTIONS.md
@.planning/phases/15-materia-prima/15-05-SUMMARY.md
</context>

<interfaces>
<!-- Components created by Plan 15-01 that this plan depends on -->
From ventago-app/src/views/materia-prima/components/constants.ts:
```typescript
export const CATEGORY_COLORS: Record<string, string>
export const KPI_COLORS: { blue: string, orange: string, green: string, red: string }
```

From ventago-app/src/views/materia-prima/components/KpiCard.tsx:
```typescript
export const KpiCard: React.FC<{
  label: string
  value: string | number
  color: 'blue' | 'orange' | 'green' | 'red'
  subtitle?: string
}>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryDistributionChart and DebtSummaryChart components</name>
  <files>
    ventago-app/src/views/materia-prima/components/CategoryDistributionChart.tsx
    ventago-app/src/views/materia-prima/components/DebtSummaryChart.tsx
  </files>
  <read_first>
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    ventago-app/src/views/materia-prima/components/constants.ts
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
**CategoryDistributionChart.tsx** — Per D-06, CSS-only vertical bar chart (no chart library).

Props: `{ distribution: Array<{ category: string, count: number }> }`.

Layout (matches mockup `.dist-chart`):
- Container: `display: 'flex', gap: '12px', alignItems: 'flex-end', height: '120px', background: '#FFFFFF', borderRadius: '12px', border: '1px solid #E2E8F0', padding: '20px'`
- For each item: a vertical flex column (`flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '6px'`):
  1. Count text on top: `fontSize: 11, fontWeight: 700`
  2. Bar div: `width: '100%', maxWidth: '50px', borderRadius: '4px 4px 0 0'`. Background color = `CATEGORY_COLORS[item.category] || '#94A3B8'`. Height calculated proportionally: `Math.max((item.count / maxCount) * 80, 4)px` where maxCount = max of all counts.
  3. Category label below: `fontSize: 10, color: '#64748B', fontWeight: 500`

Import CATEGORY_COLORS from `./constants`.

**DebtSummaryChart.tsx** — Per D-07, horizontal bar chart with supplier names and amounts.

Props: `{ debts: Array<{ supplier: string, amount: number }> }`.

Layout (matches mockup `.debt-summary`):
- Container: `background: '#FFFFFF', borderRadius: '12px', border: '1px solid #E2E8F0', padding: '20px'`
- For each debt row (`display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '8px 0', borderBottom: '1px solid #E2E8F0'`, last child no border):
  1. Supplier name: `fontSize: 13, fontWeight: 500`, min-width ~120px
  2. Bar section (`flex: 1, mx: '16px'`): container `height: '4px', background: '#FEE2E2', borderRadius: '2px', overflow: 'hidden'`, inner fill `height: '100%', background: '#DC2626', borderRadius: '2px'`, width = `(amount / maxAmount) * 100%`
  3. Amount: `fontSize: 14, fontWeight: 700, color: '#DC2626'`, formatted with `$` and toLocaleString('es-CO')

ESLint: blank line before return, blank line before comments, no unused vars.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/components/CategoryDistributionChart.tsx src/views/materia-prima/components/DebtSummaryChart.tsx --no-error-on-unmatched-pattern 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `CategoryDistributionChart.tsx` exports `CategoryDistributionChart` component
    - File `CategoryDistributionChart.tsx` imports `CATEGORY_COLORS` from `./constants`
    - File `CategoryDistributionChart.tsx` does NOT import any chart library (recharts, chart.js, etc.)
    - File `CategoryDistributionChart.tsx` contains `height: '120px'` or `height: 120` for chart container
    - File `DebtSummaryChart.tsx` exports `DebtSummaryChart` component
    - File `DebtSummaryChart.tsx` contains `#DC2626` (danger/red color for debt bars)
    - File `DebtSummaryChart.tsx` contains `toLocaleString` for amount formatting
    - Both files pass ESLint without errors
  </acceptance_criteria>
  <done>Two CSS-only chart components created matching mockup design. CategoryDistributionChart renders vertical bars with category colors. DebtSummaryChart renders horizontal bars with supplier amounts. No external chart library dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MateriaPrimaDashboardView with 2-col grid layout</name>
  <files>
    ventago-app/src/views/materia-prima/MateriaPrimaDashboardView.tsx
  </files>
  <read_first>
    ventago-app/src/views/materia-prima/MateriaPrimaDashboardView.tsx
    ventago-app/src/views/materia-prima/components/KpiCard.tsx
    ventago-app/src/views/materia-prima/components/CategoryDistributionChart.tsx
    ventago-app/src/views/materia-prima/components/DebtSummaryChart.tsx
    ventago-app/src/views/materia-prima/components/constants.ts
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
Refactor the entire MateriaPrimaDashboardView to match the mockup layout. Keep the existing data fetching logic (fetchDashboard, useCallback, useEffect, loading/error states) but completely redesign the render output.

**The DashboardData interface needs expansion.** Add these fields to the existing interface:
```typescript
interface DashboardData {
  // 기존 필드 유지
  totalMateriales: number
  stockBajo: number
  stockAgotado: number
  valorInventario: number
  deudaProveedores: number
  alertas: Array<{ id: number, nombre: string, codigo: string, stock: number, nivelMinimo: number, categoria?: string }>
  ultimosMovimientos: Array<{ id: number, fecha: string, tipo: string, material: string, cantidad: number, referencia: string }>
  // 새 필드 (백엔드가 이미 반환하거나, 없으면 빈 배열로 폴백)
  distribucionCategoria?: Array<{ category: string, count: number }>
  deudaPorProveedor?: Array<{ supplier: string, amount: number }>
}
```

**Layout structure per D-05 (2-column grid):**

1. **Page title**: "Panel de Control - Materia Prima" with tabler:layout-dashboard icon (fontSize 22, fontWeight 700)

2. **KPI row**: 4-column grid (`display: 'grid', gridTemplateColumns: { xs: '1fr', sm: 'repeat(2, 1fr)', md: 'repeat(4, 1fr)' }, gap: '16px', mb: '24px'`). Use KpiCard component:
   - Total Materiales: color='blue', value=data.totalMateriales, subtitle=`${categories} categorias activas` (or just the count)
   - Stock Bajo: color='orange', value=data.stockBajo, subtitle='Requieren reposicion'
   - Valor Inventario: color='green', value=`$${data.valorInventario.toLocaleString('es-CO')}`, subtitle optional
   - Deuda Proveedores: color='red', value=`$${data.deudaProveedores.toLocaleString('es-CO')}`, subtitle optional

3. **First two-col row** (`display: 'grid', gridTemplateColumns: { xs: '1fr', md: '1fr 1fr' }, gap: '20px', mb: '24px'`):
   - Left: **Alertas de Stock** section
     - Section title with warning icon (tabler:alert-triangle)
     - Alert list: each alert row is a Box with `display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '12px 16px', background: '#FFFFFF', borderRadius: '8px', border: '1px solid #E2E8F0'`
     - Critical alerts (stock === 0): `borderLeft: '4px solid #DC2626', background: '#FEF2F2'`
     - Warning alerts (stock > 0 but <= nivelMinimo): `borderLeft: '4px solid #F59E0B'`
     - Left side: alert name (fontSize 13, fontWeight 600) + detail line "{categoria} - {codigo}" (fontSize 11, color '#64748B')
     - Right side: "{stock} / {nivelMinimo} min" text + badge (Agotado/Bajo)
   - Right: **Distribucion por Categoria** — use CategoryDistributionChart component. Pass `data.distribucionCategoria || []`.

4. **Second two-col row** (same grid):
   - Left: **Deuda por Proveedor** — use DebtSummaryChart component. Pass `data.deudaPorProveedor || []`.
   - Right: **Ultimos Movimientos** — keep existing table but styled to match mockup:
     - Wrap in a Card with `borderRadius: '12px', border: '1px solid #E2E8F0'`
     - Table header row: columns Fecha, Tipo, Material, Cant. (right-aligned)
     - Tipo column: Chip with arrow icon. Entrada = `sx={{ backgroundColor: '#DCFCE7', color: '#16A34A' }}`, Salida = `sx={{ backgroundColor: '#FEF3C7', color: '#F59E0B' }}`
     - Use tabler:arrow-up for Entrada, tabler:arrow-down for Salida

**Imports**: Remove old imports (CardContent, Card if not needed). Add imports for KpiCard, CategoryDistributionChart, DebtSummaryChart from `./components/`. Import Icon from `@iconify/react`.

ESLint: blank line before return, blank line before comments, no unused vars/imports.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/MateriaPrimaDashboardView.tsx --no-error-on-unmatched-pattern 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File imports `KpiCard` from `./components/KpiCard`
    - File imports `CategoryDistributionChart` from `./components/CategoryDistributionChart`
    - File imports `DebtSummaryChart` from `./components/DebtSummaryChart`
    - File contains `<KpiCard` JSX usage (at least 4 instances for 4 KPIs)
    - File contains `<CategoryDistributionChart` JSX usage
    - File contains `<DebtSummaryChart` JSX usage
    - File contains `gridTemplateColumns` for 2-column grid layout (the two-col rows)
    - File contains `distribucionCategoria` and `deudaPorProveedor` in DashboardData interface
    - File contains `borderLeft: '4px solid #DC2626'` for critical alerts styling
    - File passes ESLint without errors
  </acceptance_criteria>
  <done>Dashboard fully refactored with 2-col grid (per D-05), CSS bar chart for categories (per D-06), debt summary chart (per D-07), KpiCard with 3px indicators (per D-08). Alert section styled with critical/warning distinction. All mockup sections present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> Dashboard UI | Dashboard data rendered, new optional fields handled with fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | T | DashboardView | accept | Display-only refactoring, no new data mutation paths |
| T-15-04 | D | Chart components | accept | CSS-only rendering, no external library attack surface |
</threat_model>

<verification>
1. `npx eslint src/views/materia-prima/MateriaPrimaDashboardView.tsx src/views/materia-prima/components/CategoryDistributionChart.tsx src/views/materia-prima/components/DebtSummaryChart.tsx` passes with 0 errors
2. `npm run build` completes without errors
3. Visual: Dashboard shows 4 KPI cards with colored top bars, 2-col grid with alerts + chart + debt + movements
</verification>

<success_criteria>
- Dashboard uses KpiCard component with 3px color indicators (per D-08)
- Dashboard has 2-column grid layout (per D-05)
- Category distribution shown as CSS bar chart (per D-06)
- Supplier debt shown as horizontal bar chart (per D-07)
- Alert section distinguishes critical (stock=0) from warning alerts
- All files pass ESLint
</success_criteria>

<output>
After completion, create `.planning/phases/15-materia-prima/15-06-SUMMARY.md`
</output>
