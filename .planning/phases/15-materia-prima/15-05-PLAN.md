---
phase: 15-materia-prima
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ventago-app/src/views/materia-prima/components/constants.ts
  - ventago-app/src/views/materia-prima/components/KpiCard.tsx
  - ventago-app/src/views/materia-prima/components/StockBar.tsx
  - ventago-app/src/views/materia-prima/components/MaterialCard.tsx
  - ventago-app/src/views/materia-prima/components/CategoryChips.tsx
  - ventago-app/src/views/materia-prima/InventarioView.tsx
autonomous: true
requirements:
  - MPRIMA-01
  - MPRIMA-02
  - MPRIMA-05

must_haves:
  truths:
    - "Inventario card has a 4px color bar on top matching its category color"
    - "Tela materials show extra attributes (color, origin, quality) in code line"
    - "Stock status shows a colored progress bar + badge (Normal/Bajo/Agotado) + numeric text"
    - "Category filter chips are color-coded and toggle filled style when selected"
    - "KPI cards have a 3px colored top indicator"
  artifacts:
    - path: "ventago-app/src/views/materia-prima/components/constants.ts"
      provides: "CATEGORY_COLORS map and shared color constants"
      contains: "CATEGORY_COLORS"
    - path: "ventago-app/src/views/materia-prima/components/KpiCard.tsx"
      provides: "Reusable KPI card with 3px top color indicator"
      exports: ["KpiCard"]
    - path: "ventago-app/src/views/materia-prima/components/StockBar.tsx"
      provides: "Stock bar with progress + badge + numbers"
      exports: ["StockBar"]
    - path: "ventago-app/src/views/materia-prima/components/MaterialCard.tsx"
      provides: "Material card with 4px category color bar"
      exports: ["MaterialCard"]
    - path: "ventago-app/src/views/materia-prima/components/CategoryChips.tsx"
      provides: "Color-coded category filter chips"
      exports: ["CategoryChips"]
    - path: "ventago-app/src/views/materia-prima/InventarioView.tsx"
      provides: "Refactored Inventario using extracted components"
  key_links:
    - from: "MaterialCard.tsx"
      to: "constants.ts"
      via: "import CATEGORY_COLORS"
      pattern: "CATEGORY_COLORS"
    - from: "InventarioView.tsx"
      to: "components/MaterialCard"
      via: "import MaterialCard"
      pattern: "import.*MaterialCard"
---

<objective>
Extract reusable UI components from monolithic View files and refactor InventarioView to match the HTML mockup design. Creates the shared component library (constants, KpiCard, StockBar, MaterialCard, CategoryChips) that all other materia-prima views will consume.

Purpose: Establish the component foundation (per D-12) and deliver the Inventario card redesign (per D-01, D-02, D-03, D-04, D-08).
Output: 5 new component files in components/ + refactored InventarioView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-materia-prima/15-CONTEXT.md
@.planning/phases/15-materia-prima/materia-prima-mockup.html
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components — constants, KpiCard, StockBar, MaterialCard</name>
  <files>
    ventago-app/src/views/materia-prima/components/constants.ts
    ventago-app/src/views/materia-prima/components/KpiCard.tsx
    ventago-app/src/views/materia-prima/components/StockBar.tsx
    ventago-app/src/views/materia-prima/components/MaterialCard.tsx
  </files>
  <read_first>
    ventago-app/src/views/materia-prima/InventarioView.tsx
    ventago-app/src/views/materia-prima/MateriaPrimaDashboardView.tsx
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    .planning/phases/15-materia-prima/15-CONTEXT.md
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
Create 4 files in `ventago-app/src/views/materia-prima/components/`:

**constants.ts** — Per D-01 category color map:
```typescript
export const CATEGORY_COLORS: Record<string, string> = {
  Tela: '#3B82F6',
  Boton: '#8B5CF6',
  Cierre: '#EC4899',
  Hilo: '#F97316',
  Accesorio: '#14B8A6'
}
```
Also export KPI color constants used by KpiCard (per D-08):
```typescript
export const KPI_COLORS = {
  blue: '#2563EB',
  orange: '#F59E0B',
  green: '#16A34A',
  red: '#DC2626'
} as const
```

**KpiCard.tsx** — Per D-08, reusable KPI card with 3px top color indicator. Props: `{ label: string, value: string | number, color: 'blue' | 'orange' | 'green' | 'red', subtitle?: string }`. Uses MUI Card/CardContent. The `::before` pseudo-element effect is done via an `sx` Box with `position: absolute, top: 0, left: 0, right: 0, height: '3px'` and the background color from KPI_COLORS[color]. Card has `position: 'relative', overflow: 'hidden', borderRadius: '12px', border: '1px solid #E2E8F0'`. Label: `fontSize: 12, color: '#64748B', textTransform: 'uppercase', letterSpacing: 0.5`. Value: `fontSize: 28, fontWeight: 700`, colored by KPI_COLORS[color]. Subtitle: `fontSize: 11, color: '#64748B', mt: 0.5`.

**StockBar.tsx** — Per D-03. Props: `{ stock: number, minStock: number }`. Renders:
1. Header row: "Stock" label on left, badge chip on right (Normal=green, Bajo=orange, Agotado=red)
2. Progress bar: 6px height, border-radius 3px, background `#E2E8F0`. Fill color: green if stock > minStock, orange if stock <= minStock && stock > 0, red if stock === 0. Width = `Math.min((stock / Math.max(minStock, 1)) * 100, 100)%`.
3. Numbers: `{stock} / {minStock} min.` in `fontSize: 11, color: '#64748B'`

Badge uses MUI Chip with size="small". Badge colors: Normal = `sx={{ backgroundColor: '#DCFCE7', color: '#16A34A' }}`, Bajo = `sx={{ backgroundColor: '#FEF3C7', color: '#F59E0B' }}`, Agotado = `sx={{ backgroundColor: '#FEE2E2', color: '#DC2626' }}`. Do NOT use MUI LinearProgress — use a plain Box with inner Box for the bar fill (matches mockup styling better).

**MaterialCard.tsx** — Per D-01, D-02. Props: `{ material: Material, categoryColor: string, onEdit?: (id: number) => void }`. Where Material interface has: `id, nombre, codigo, categoria, stock, stockMinimo, precioUnitario, proveedor, color?, origin?, quality?`.
Structure:
1. Top: 4px height Box with `backgroundColor: categoryColor` (per D-01)
2. Header: material.nombre (fontSize 14, fontWeight 600) + edit IconButton
3. Code line (per D-02): For Tela, show `"{codigo} · Tela · Color: {color} · Origen: {origin}"`. For non-Tela, show `"{codigo} · {categoria}"`. FontSize 11, color `#64748B`.
4. StockBar component (imported)
5. Footer: price on left (fontSize 13, fontWeight 600), supplier on right (fontSize 11, color `#64748B`). Separated by `borderTop: '1px solid #E2E8F0'`, paddingTop 8.

Card sx: `borderRadius: '12px', border: '1px solid #E2E8F0', overflow: 'hidden', '&:hover': { boxShadow: '0 4px 12px rgba(0,0,0,0.08)' }`.

ESLint: Ensure blank line before every `return` statement. Ensure blank line before `//` comments. Remove all unused imports. Use single quotes, no semicolons, trailing comma: none. Use `(e: any)` for event handlers. Export interface Material from this file so other views can import it.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/components/constants.ts src/views/materia-prima/components/KpiCard.tsx src/views/materia-prima/components/StockBar.tsx src/views/materia-prima/components/MaterialCard.tsx --no-error-on-unmatched-pattern 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `components/constants.ts` exports `CATEGORY_COLORS` with keys: Tela, Boton, Cierre, Hilo, Accesorio
    - File `components/constants.ts` exports `KPI_COLORS` with keys: blue, orange, green, red
    - File `components/KpiCard.tsx` exports `KpiCard` component with props: label, value, color, subtitle
    - File `components/KpiCard.tsx` contains `height: '3px'` for top indicator
    - File `components/StockBar.tsx` exports `StockBar` component with props: stock, minStock
    - File `components/StockBar.tsx` contains strings 'Normal', 'Bajo', 'Agotado' for badge labels
    - File `components/StockBar.tsx` uses Box-based bar (NOT LinearProgress)
    - File `components/MaterialCard.tsx` exports `MaterialCard` and `Material` interface
    - File `components/MaterialCard.tsx` contains `height: 4` or `height: '4px'` for top color bar
    - File `components/MaterialCard.tsx` contains string pattern matching `codigo.*Tela.*Color.*Origen` for D-02 display
    - All files pass ESLint without errors
  </acceptance_criteria>
  <done>4 component files created, each exporting named components/constants. ESLint passes. MaterialCard renders 4px category color bar, Tela extra attributes, StockBar with badge+bar+numbers, KpiCard with 3px indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryChips component and refactor InventarioView</name>
  <files>
    ventago-app/src/views/materia-prima/components/CategoryChips.tsx
    ventago-app/src/views/materia-prima/InventarioView.tsx
  </files>
  <read_first>
    ventago-app/src/views/materia-prima/InventarioView.tsx
    ventago-app/src/views/materia-prima/components/constants.ts
    ventago-app/src/views/materia-prima/components/MaterialCard.tsx
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
**CategoryChips.tsx** — Per D-04. Props: `{ categories: Category[], selectedCategory: string | null, onSelect: (cat: string | null) => void, counts?: Record<string, number> }`.
Where Category = `{ id: number, nombre: string, color: string }`.

Renders a horizontal flex row (`display: 'flex', gap: 1, flexWrap: 'wrap'`):
1. "Todos" chip — always first. When selected (selectedCategory === null): `sx={{ backgroundColor: '#7C3AED', color: '#fff', borderColor: '#7C3AED' }}`. When not selected: `variant="outlined"`.
2. For each category: chip with label = `cat.nombre`. When selected: `sx={{ backgroundColor: CATEGORY_COLORS[cat.nombre] || cat.color, color: '#fff', borderColor: CATEGORY_COLORS[cat.nombre] || cat.color }}`. When not selected: `variant="outlined"`.
3. If `counts` provided, append count to label: e.g. `"Tela (18)"`.
4. onClick: calls `onSelect(null)` for Todos, `onSelect(cat.nombre)` for category chips.

Use MUI Chip component. Import CATEGORY_COLORS from `./constants`.

**InventarioView.tsx** — Major refactoring. Keep existing data fetching logic (fetchCategories, fetchMaterials, handleAddMaterial, filteredMaterials, getStockStatus, getProgressValue). Replace inline rendering with extracted components:

1. Replace search+filter Paper with: search TextField + CategoryChips component (per D-04)
2. Replace inline Card rendering in Grid with MaterialCard component (per D-01, D-02, D-03)
3. Pass `CATEGORY_COLORS[material.categoria]` as categoryColor prop to MaterialCard
4. Card grid: `display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '16px'` on lg, `repeat(2, 1fr)` on md, `1fr` on sm. Use MUI Grid or sx responsive breakpoints.
5. Keep the Dialog for new material as-is (it works fine currently)
6. Filter bar layout from mockup: search input on left, category chips flowing to the right. Use `display: 'flex', gap: 1, alignItems: 'center', flexWrap: 'wrap', mb: '20px'`.

Remove now-unused imports (LinearProgress, Paper if replaced, Chip if only used for categories). Keep Icon, IconButton, Dialog imports.

ESLint: blank line before return, blank line before comments, no unused vars, single quotes, no semicolons.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/components/CategoryChips.tsx src/views/materia-prima/InventarioView.tsx --no-error-on-unmatched-pattern 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `components/CategoryChips.tsx` exports `CategoryChips` component
    - File `components/CategoryChips.tsx` imports `CATEGORY_COLORS` from `./constants`
    - File `components/CategoryChips.tsx` contains string 'Todos' for the all-categories chip
    - File `InventarioView.tsx` imports `MaterialCard` from `./components/MaterialCard`
    - File `InventarioView.tsx` imports `CategoryChips` from `./components/CategoryChips`
    - File `InventarioView.tsx` imports `CATEGORY_COLORS` from `./components/constants`
    - File `InventarioView.tsx` does NOT contain `LinearProgress` import (replaced by StockBar inside MaterialCard)
    - File `InventarioView.tsx` contains `<MaterialCard` JSX usage
    - File `InventarioView.tsx` contains `<CategoryChips` JSX usage
    - Both files pass ESLint without errors
  </acceptance_criteria>
  <done>CategoryChips component created with color-coded chips per D-04. InventarioView refactored to use MaterialCard, CategoryChips, and CATEGORY_COLORS. Inline card rendering replaced with extracted components. ESLint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> UI | Material data from backend displayed in cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | S | MaterialCard | accept | Display-only refactoring, no new data paths. Existing API auth unchanged. |
| T-15-02 | T | InventarioView | accept | No data mutation changes. Existing handleAddMaterial unchanged. |
</threat_model>

<verification>
1. `npx eslint src/views/materia-prima/components/*.ts src/views/materia-prima/components/*.tsx src/views/materia-prima/InventarioView.tsx` passes with 0 errors
2. `npm run build` completes without errors (ESLint + TypeScript)
3. Visual: Inventario page shows cards with colored top bar, stock bar with badge, category chips with colors
</verification>

<success_criteria>
- 5 component files exist in components/ directory (constants.ts, KpiCard.tsx, StockBar.tsx, MaterialCard.tsx, CategoryChips.tsx)
- InventarioView uses extracted components instead of inline rendering
- Category chips are color-coded per D-04 with CATEGORY_COLORS
- Material cards have 4px color bar per D-01
- Tela materials show extra attributes per D-02
- Stock displays progress bar + badge + numbers per D-03
- KpiCard has 3px top indicator per D-08
- All files pass ESLint
</success_criteria>

<output>
After completion, create `.planning/phases/15-materia-prima/15-05-SUMMARY.md`
</output>
