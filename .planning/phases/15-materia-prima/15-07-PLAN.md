---
phase: 15-materia-prima
plan: 07
type: execute
wave: 2
depends_on: ["15-05"]
files_modified:
  - ventago-app/src/views/materia-prima/ProveedoresView.tsx
  - ventago-app/src/views/materia-prima/MovimientosView.tsx
  - ventago-app/src/views/materia-prima/PagosView.tsx
autonomous: true
requirements:
  - MPRIMA-03
  - MPRIMA-04
  - MPRIMA-06

must_haves:
  truths:
    - "Proveedores page uses KpiCard components with colored top indicators for summary stats"
    - "Supplier cards match mockup design with financial section grid and styled pay button"
    - "Entrada modal includes supplier selection + quantity + unit price + payment status (Pendiente/Pagado/Parcial)"
    - "Salida modal supports WorkOrder dropdown OR manual reference number input"
    - "Movimientos table has styled type chips matching mockup (green Entrada, orange Salida with arrow icons)"
    - "Pagos page uses KpiCard components and has styled payment table"
  artifacts:
    - path: "ventago-app/src/views/materia-prima/ProveedoresView.tsx"
      provides: "Refactored Proveedores with KpiCard and mockup-matching supplier cards"
    - path: "ventago-app/src/views/materia-prima/MovimientosView.tsx"
      provides: "Refactored Movimientos with improved Entrada/Salida modals per D-09, D-10"
    - path: "ventago-app/src/views/materia-prima/PagosView.tsx"
      provides: "Refactored Pagos with KpiCard and styled table"
  key_links:
    - from: "ProveedoresView.tsx"
      to: "components/KpiCard"
      via: "import KpiCard"
      pattern: "import.*KpiCard"
    - from: "MovimientosView.tsx"
      to: "API /materia-prima/movements"
      via: "apiConnector.post"
      pattern: "apiConnector\\.post.*movements"
    - from: "PagosView.tsx"
      to: "components/KpiCard"
      via: "import KpiCard"
      pattern: "import.*KpiCard"
---

<objective>
Refactor ProveedoresView, MovimientosView, and PagosView to match the HTML mockup styling. Improve the Entrada/Salida modals per D-09 and D-10. Apply KpiCard component to all summary stat cards.

Purpose: Complete the visual refactoring of remaining 3 views (per D-09, D-10, D-11) and apply consistent mockup styling across all materia-prima pages.
Output: 3 refactored View files with mockup-quality UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-materia-prima/15-CONTEXT.md
@.planning/phases/15-materia-prima/materia-prima-mockup.html
@.planning/codebase/CONVENTIONS.md
@.planning/phases/15-materia-prima/15-05-SUMMARY.md
</context>

<interfaces>
<!-- Components from Plan 15-01 -->
From ventago-app/src/views/materia-prima/components/KpiCard.tsx:
```typescript
export const KpiCard: React.FC<{
  label: string
  value: string | number
  color: 'blue' | 'orange' | 'green' | 'red'
  subtitle?: string
}>
```

From ventago-app/src/views/materia-prima/components/constants.ts:
```typescript
export const KPI_COLORS: { blue: string, orange: string, green: string, red: string }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ProveedoresView with KpiCard and mockup styling</name>
  <files>
    ventago-app/src/views/materia-prima/ProveedoresView.tsx
  </files>
  <read_first>
    ventago-app/src/views/materia-prima/ProveedoresView.tsx
    ventago-app/src/views/materia-prima/components/KpiCard.tsx
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
Keep all existing data fetching, dialog logic, and handlers. Refactor only the render output:

**1. KPI summary row**: Replace the 3 basic Cards with KpiCard components in a responsive grid (`display: 'grid', gridTemplateColumns: { xs: '1fr', sm: 'repeat(3, 1fr)' }, gap: '16px', mb: '24px'`):
- Total Proveedores: color='blue' (use KPI_COLORS to compute, but pass prop as string 'blue' since KpiCard accepts literal union), value=suppliers.length. However KpiCard color prop only accepts blue/orange/green/red. Use 'blue' here.
- Deuda Total: color='red', value=`$${suppliers.reduce((sum, s) => sum + s.deuda, 0).toLocaleString('es-CO')}`
- Total Pagado: color='green', value=`$${suppliers.reduce((sum, s) => sum + s.pagado, 0).toLocaleString('es-CO')}`

**2. Supplier cards grid**: Use `display: 'grid', gridTemplateColumns: { xs: '1fr', sm: 'repeat(2, 1fr)', md: 'repeat(3, 1fr)' }, gap: '16px'`.

Each supplier card styling to match mockup `.supplier-card`:
- Card: `borderRadius: '12px', border: '1px solid #E2E8F0', padding: '20px'`
- Header: supplier name (fontSize 15, fontWeight 700) + Rating stars on right
- Contact info: `fontSize: 12, color: '#64748B', lineHeight: 1.6, mb: 2` — show contacto, telefono, email on separate lines
- Divider: `height: '1px', background: '#E2E8F0', my: 1.5`
- Financials grid: `display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px', mb: 2`
  - Deuda label (fontSize 11, color '#64748B') + value (fontSize 16, fontWeight 700, color '#DC2626')
  - Pagado label (fontSize 11, color '#64748B') + value (fontSize 16, fontWeight 700, color '#16A34A')
- "Registrar Pago" button: fullWidth, `borderRadius: '8px', fontSize: 13, fontWeight: 600`. Use MUI Button variant="contained" with `sx={{ backgroundColor: '#7C3AED', '&:hover': { backgroundColor: '#6D28D9' } }}` to match mockup primary purple color. Keep the existing onClick handler.

**3. Dialogs**: Keep both payment and new supplier dialogs as-is, they work correctly.

Import KpiCard from `./components/KpiCard`. Remove unused imports (Card, CardContent if fully replaced, etc).

ESLint: blank line before return, blank line before comments, no unused vars.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/ProveedoresView.tsx --no-error-on-unmatched-pattern 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File imports `KpiCard` from `./components/KpiCard`
    - File contains `<KpiCard` JSX usage (at least 3 instances)
    - File contains `gridTemplateColumns` for responsive supplier card grid
    - File contains `#DC2626` for debt value color
    - File contains `#16A34A` for paid value color
    - File contains `#7C3AED` for button background (mockup primary purple)
    - File passes ESLint without errors
  </acceptance_criteria>
  <done>ProveedoresView refactored with KpiCard components, mockup-matching supplier cards with styled financials grid, and purple Registrar Pago buttons. ESLint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MovimientosView with improved Entrada/Salida modals and PagosView with KpiCard</name>
  <files>
    ventago-app/src/views/materia-prima/MovimientosView.tsx
    ventago-app/src/views/materia-prima/PagosView.tsx
  </files>
  <read_first>
    ventago-app/src/views/materia-prima/MovimientosView.tsx
    ventago-app/src/views/materia-prima/PagosView.tsx
    ventago-app/src/views/materia-prima/components/KpiCard.tsx
    .planning/phases/15-materia-prima/materia-prima-mockup.html
    .planning/phases/15-materia-prima/15-CONTEXT.md
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
**MovimientosView.tsx** — Keep all data fetching and handlers. Refactor:

1. **Action buttons** styling: Entrada button `sx={{ backgroundColor: '#16A34A', '&:hover': { backgroundColor: '#15803D' }, borderRadius: '8px', fontWeight: 600 }}`. Salida button `sx={{ backgroundColor: '#F59E0B', '&:hover': { backgroundColor: '#D97706' }, borderRadius: '8px', fontWeight: 600 }}`.

2. **Table card** styling: Card with `borderRadius: '12px', border: '1px solid #E2E8F0', overflow: 'hidden'`. Table header cells: `fontSize: 11, fontWeight: 600, textTransform: 'uppercase', letterSpacing: 0.5, color: '#64748B', background: '#F8FAFC'`.

3. **Type chips** in table (per mockup `.type-chip`): Replace current Chip with styled Box inline:
   - Entrada: `display: 'inline-flex', alignItems: 'center', gap: '4px', padding: '3px 10px', borderRadius: '12px', fontSize: 11, fontWeight: 600, backgroundColor: '#DCFCE7', color: '#16A34A'` with up arrow icon
   - Salida: same but `backgroundColor: '#FEF3C7', color: '#F59E0B'` with down arrow icon

4. **Entrada dialog improvement (per D-09)**: The existing dialog already has Material select, Supplier select, Quantity, Unit Price, Date, Reference, Payment Status, and Note fields. This matches D-09 requirements. Just improve the dialog title styling and add a subtitle: "Registrar ingreso de materia prima con datos de proveedor y pago".

5. **Salida dialog improvement (per D-10)**: Add a WorkOrder dropdown above the Reference field. Add state for workOrders:
   ```typescript
   const [workOrders, setWorkOrders] = useState<Array<{ id: number, nombre: string }>>([])
   ```
   Fetch work orders from `/mes/work-orders` (same pattern as fetchMaterials). In the Salida dialog, add:
   - FormControl with InputLabel "Orden de Trabajo" and Select dropdown listing work orders. When a work order is selected, auto-fill the reference field with `"OT-{workOrder.id}"`.
   - Below the WorkOrder select, show the existing Reference TextField with placeholder "O ingrese referencia manual (ej: Despacho #123)".
   - Both are optional — user can use either WorkOrder selection OR manual reference per D-10.

**PagosView.tsx** — Keep all data fetching and handlers. Refactor:

1. **KPI row**: Replace 3 basic Cards with KpiCard in responsive grid:
   - Deuda Total: color='red'
   - Pagado Este Mes: color='green'
   - Proximo Vencimiento: color='blue'

2. **Table card** styling: Same as MovimientosView — `borderRadius: '12px', border: '1px solid #E2E8F0'`. Header cells with uppercase styling.

3. **Method chips** in table: `padding: '3px 10px', borderRadius: '12px', fontSize: 11, fontWeight: 500, border: '1px solid #E2E8F0', background: '#F8FAFC'`. Keep using MUI Chip but apply these sx props.

Import KpiCard from `./components/KpiCard`. Remove unused imports.

ESLint: blank line before return, blank line before comments, no unused vars. Single quotes, no semicolons.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/ACE_online_1.0/ventago-app && npx eslint src/views/materia-prima/MovimientosView.tsx src/views/materia-prima/PagosView.tsx --no-error-on-unmatched-pattern 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - MovimientosView.tsx contains `#16A34A` for Entrada button/chip and `#F59E0B` for Salida button/chip
    - MovimientosView.tsx contains `work-orders` or `workOrders` for the WorkOrder fetch/state (per D-10)
    - MovimientosView.tsx contains string 'Orden de Trabajo' in the Salida dialog
    - MovimientosView.tsx contains `textTransform: 'uppercase'` for table header styling
    - PagosView.tsx imports `KpiCard` from `./components/KpiCard`
    - PagosView.tsx contains `<KpiCard` JSX usage (at least 3 instances)
    - PagosView.tsx contains `borderRadius: '12px'` for table card styling
    - Both files pass ESLint without errors
  </acceptance_criteria>
  <done>MovimientosView refactored with mockup-styled type chips, improved Entrada modal (per D-09), WorkOrder dropdown in Salida modal (per D-10). PagosView refactored with KpiCard components and styled table. ESLint passes for both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> Movement/Payment UI | Movement and payment data rendered in tables |
| User input -> Entrada/Salida dialogs | Form data submitted to existing API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | S | MovimientosView dialog | accept | Same form fields, same API endpoints. WorkOrder dropdown is read-only data display. |
| T-15-06 | T | Payment auto-deduction (D-11) | accept | Backend already handles deduction logic. Frontend only sends the payment POST request. |
</threat_model>

<verification>
1. `npx eslint src/views/materia-prima/ProveedoresView.tsx src/views/materia-prima/MovimientosView.tsx src/views/materia-prima/PagosView.tsx` passes with 0 errors
2. `npm run build` completes without errors
3. Visual: All 3 pages match mockup styling — KPI cards with indicators, styled tables, improved modals
</verification>

<success_criteria>
- ProveedoresView uses KpiCard with colored indicators, supplier cards match mockup
- MovimientosView Entrada modal has supplier + payment status fields (per D-09)
- MovimientosView Salida modal has WorkOrder dropdown + manual reference option (per D-10)
- PagosView uses KpiCard for summary stats
- Tables have mockup-matching header styling and type/method chips
- All 3 files pass ESLint
</success_criteria>

<output>
After completion, create `.planning/phases/15-materia-prima/15-07-SUMMARY.md`
</output>
