# 핸드오프 — 2026-09-11 · Caja Fuerte 구간 · 레거시 임포트 검증과 배선

앞 핸드오프: `HANDOFF-2026-09-10-c-caja-fuerte-완결과-금액-전수.md`

이 세션은 **① Caja Fuerte 일별 원장 4가지를 전부 구현·배포**했고,
**② 레거시 임포트를 실제 백업으로 검증**해 **살아 있는 상품이 조용히 사라지는 결함**을
찾아 고쳤으며, **③ 매퍼를 부를 수 있는 엔드포인트를 만들었다**(판매·팩투라·비용).

---

## 0. 배포 상태

| 빌드 | 커밋 | 결과 |
|---|---|---|
| api #893 · front #728 | `0df9106` / `f3037e9` | **SUCCESS** |

컨테이너 재생성 확인(`ventagoapp`·`api_ventago` Up), 컨테이너 안 `dist` 에서
`ciclo.js` 존재 확인, `app.coolsistema.com` 200 · API 401(정상) ·
재기동 후 SQL 오류 **0건**.

★ **미push 커밋 있음** — 루트 `d075276`(레거시 임포트 매퍼 배선). 승인 대기.

---

## 1. Caja Fuerte — 앞 핸드오프 §6 의 4가지 전부 완료

| # | 내용 | 어떻게 |
|---|---|---|
| 1 | 카하 **이름** | `/operations` 에 `cajas`(세션 id → `boxes.name`). storeId 로 조인. 모르면 「caja no identificada」 |
| 2 | **유출자** | `/balance` 에 `salidas[]` — 일시·사용자·사유·금액. 합계와 **같은 범위** |
| 3 | **구간(ciclo) 누계** | 순수 함수 `ciclo.ts`. 마지막으로 0 이 된 뒤부터 |
| 4 | **일별** | `/daily` 에 `saldoInicial`·`saldoFinal`·출처별 분해. 새 패널 `CajaFuerteDiario` |

### ★★ 구간 전환이 쉬웠던 이유 — 불변식이 살아남는다
경계에서 잔액이 **0** 이었으므로 **구간의 순합 = 현재 잔액**이다. 그래서
`cierreCaja + otrosIngresos − aperturaCaja − salidaExterna − otrosRetiros = balance`
가 그대로 성립하고, 화면의 자기검증(`descuadre.ts`)을 **한 줄도 안 건드렸다.**

### ★★★ 사용자가 잡은 것 둘 (내가 빠뜨렸다)
1. **누계 금액이 없었다.** 요구 5번은 「며칠부터 며칠까지 **누계 얼마**」인데
   날짜만 넣어 문장의 절반이 빠져 있었다.
2. **「비운 날」이 틀렸다.** `ciclo.desde` 는 **구간의 첫 이동**이지 0 이 된 시각이
   아니다. HELGUERA 는 10/09 22:13 에 비우고 11/09 00:00 에 첫 마감이 들어왔는데
   「11/09 에 비웠다」로 읽혔다. → `vaciadoEn` 을 따로 낸다.

### 실측 검산 (운영, 읽기 전용)
- `/daily` 누적 사슬: coolsistema(caja 5) 09-02 가 **788,000 에서 시작**하고
  날짜별 `saldoInicial = 전날 saldoFinal` 이 전부 이어진다(창 밖까지 누적한 결과).
- HELGUERA(caja 7): 마지막 0 이 27번째 행 → 구간은 **1건**, 135,000.
  화면 문장: `Desde que se vació el 10/09/2026 10:13 p. m. · hasta 11/09/2026 12:00 a. m. · entraron $135.000`
- 그래서 화면 숫자가 **전부 바뀐다**(2.618.698 → 135.000). 잔액은 그대로.

### 시험 유효성 — 돌연변이 6개 전부 죽는다
구간 자르기 제거 / 센타보 반올림 제거 / `cerrado` 고정 / `huboCeroPrevio` 고정 /
0 경계를 마지막 것으로 / `salidas` 뒤집기 제거.

★ **처음엔 하나가 살아남았다.** 픽스처로 고른 `0.1+0.2−0.3` 이 double 로 곱해
  더해도 **정확히 0** 이라 구분이 안 됐다. `1.1+2.2−3.3`(5.68e-14)로 바꿔야 했다.

★ 기존 `caja-fuerte-desglose.spec.ts` 는 구간 전환 뒤에도 **한 줄도 안 고치고
  통과했다** — 그 픽스처들이 0 을 한 번도 안 지나 두 구현이 구분되지 않는다.
  그래서 `caja-fuerte-ciclo-balance.spec.ts` 를 따로 만들었다.

---

## 2. ★★★ 레거시 임포트 — 경로가 **둘**이고 뒤쪽은 부를 수가 없었다

| 경로 | 담당 | HTTP |
|---|---|---|
| **한 방** `runImport(sqlText)` | 카탈로그 · **todocodigos** · **codigos** · 고객 · 판매원 | `POST /legacy-import/upload` |
| **스트리밍** staging + 매퍼 | **ventas** · 결제 · facturas · **gastos** · 재고 · 카하개시금 | ★ **없었다** |

매퍼 6종은 모듈에 provider 로만 있고 **부르는 컨트롤러·잡이 없었다.** 돌리는 것은
itest 뿐이었다 — 즉 화면의 「legacy importación」으로는 **판매·비용·팩투라가
한 건도 안 들어왔다.**

→ 사용자 승인(2026-09-11) 후 `ImportRunnerService` + 엔드포인트 둘을 만들었다:
  · `GET  /legacy-import/sesiones/:id/sucursales` — ACE sucursal 의 **근거**(추천 없음)
  · `POST /legacy-import/sesiones/:id/importar` — `{grupo, branchMap}`

★ 실행 가능 묶음: **③ 판매 · ④ 팩투라 · ⑥ 비용**.
  **②(재고)와 ⑤(외상·온라인)는 거절한다** — 재고는 코드↔ProductBranch 지도가
  없고 ⑤ 는 매퍼 자체가 없다. **빈 성공을 내지 않는다.**

★★ 돌연변이 4개 중 **둘이 처음에 살아남았다** — 가짜 DB 가 표 이름만 보고 답해서
  질의에서 `AND store_id = $n` 을 **지워도 검사가 안 죽었다**(매장 격리가
  사라졌는데 「7개 통과」). 가짜가 **SQL 을 해석**하게 고쳐야 했다.

---

## 3. ★★ 찾아낸 결함 — 부모 없는 살아 있는 상품이 사라진다

`buildCodeImportBatch` 가 `borrado=t` 를 부모·자식 **각각에서** 건너뛴다.
그래서 「부모는 지워졌는데 자식은 살아 있는」 조합이 샜다:

- 자식은 batch 에 들어가지만 `parentSku` 가 가리키는 부모가 없어 **조용히 skip**
- `errors[]` 에도 안 남고 `variants.skipped` 안에 섞여 **숫자로도 안 보였다**
- 이관 후 그 상품은 **POS 에서 팔 수 없다**(목록 where 에서 빠지면 끝)

실측: noix `26_185_2599012`(판매 이력 **22줄**) · charo4 **209건**.

### 고친 방식 — 몇 명이 그 부모를 가리키는가로 갈린다
| 경우 | 처리 | 오류 코드 |
|---|---|---|
| `todocodigos` 에 SKU 가 **지워진 채로** 있음 | 되살린다(원본이 정확) | `PARENT_REVIVED` |
| 행이 없고 자식이 **하나** | 자식을 **단품**으로 올린다 | `VARIANT_PROMOTED` |
| 행이 없고 자식이 **둘 이상** | 부모를 만들어 **묶음 보존** | `PARENT_SYNTHESIZED` |

★★★ 단품 처리가 이 모양인 이유. 색·사이즈를 안 쓰는 매장은 ACE 의 todocodigo 와
  codigo 가 **같은 코드로 1:1** 이고, 임포트는 그 둘을 **상품 하나**(자식 0)로
  합친다 — noix 141개, charo4 19,911개가 그 모양이다. 고아만 「없는 부모 SKU +
  자식」 쌍으로 만들면 같은 매장에 **두 모양이 섞이고 원본에 없는 상품이 생긴다.**
  ★ 자식 0 인 단품은 팔린다 — `madre-guard.ts` 판정이 「활성 자식 수」다.

★ 처음엔 자식을 `variants` 에서 **뺐다가 가격이 통째로 빠졌다**(precios=0).
  `ParentRowDto` 에는 `price` 하나뿐이고 **5슬롯은 variant 에만** 있다.
  지금은 자식을 남기고 `parentSku` 를 자기 자신으로 돌린다 → 구조가 완전히 같다.

---

## 4. ★★ 스테이징 서버로는 이 검증을 **할 수 없다**

사용자가 두 번 스테이징을 지정했으나, 거기서 하면 **막히는 게 아니라 틀린 답이 나온다**:

| 문제 | 영향 |
|---|---|
| `stock_balances` 없음 | `stocks` 트리거가 죽는다 |
| `legacy_upload_sessions`·`parts` 없음 | partial-upload 경로 자체가 없다 |
| `legacy_caja_aperturas` 없음 | 카하 개시금을 못 넣는다 |
| **금액 타입 13개 드리프트** | `sales.total_amount`·`subtotal`·`prices.amount`·`sale_payment_methods.amount` 가 **integer**. 소수 단가가 오면 500 이거나 센타보가 잘린다 |

스테이징 재생성은 **영구 제외**(사용자 결정 2026-09-11). → **로컬 PG18 5432 `ventago`**
에서 돌렸다. 운영과 테이블 **238개로 일치**(실측).

---

## 5. ★★ 한 방 경로는 덤프 **전체**를 메모리에 올린다

쓰는 표는 9개인데 `parseSql` 이 **70개를 전부** JS 객체로 만든다.

| 백업 | 크기 | plain SQL | heap |
|---|---|---|---|
| noix | 11.8MB | 92MB | **3.0~4.9GB** (GC 타이밍에 따라 흔들림) |
| charo4 | 33MB | 152MB | **3.3~5.2GB** (rss 3.9~6.1GB) |

★★★ **이건 검사만의 문제가 아니다.** 같은 코드가 `POST /legacy-import/upload`
  뒤에 있고 그 상한은 **65MB** 다. noix 는 11.8MB 인데 이미 Node 기본 상한(4GB)에
  닿아 같은 조건에서 통과와 OOM 이 번갈아 났다.
  → 컨트롤러의 상한 근거(문자열 512MB · `MAX_DECOMPRESSED_BYTES` 400MB)는
    **진짜 천장이 아니다.** 진짜 천장은 **파싱 후 객체 크기**다.
  ★★★ **charo4(33MB)를 화면에서 올리면 API 컨테이너가 죽는다** —
    그 컨테이너의 Node heap 은 **4,144MB** 인데 이 파일 하나가 5.2GB 를 쓴다.
    즉 상한 65MB 는 **지금 이미 안전하지 않다**(가정이 아니라 실측이다).

`catalogo-una-vez.itest.ts` 가 `process.memoryUsage()` 를 직접 찍는다 —
숫자를 베끼지 말고 그 로그를 볼 것.

---

## 6. charo4 실측 결과 — **실질 손실 0건**

백업 33MB · plain SQL 152MB · 표 69 · 행 200,656(스트리밍 적재 기준).

### 한 방 경로 (더미 매장 `ZZ-P86-CATALOGO`)
| 원본 | 목적지 | 판정 |
|---|---|---|
| todocodigos 20,436 (살아있음 **20,421**) | 부모 20,541 | ✅ **누락 0** |
| codigos 20,211 (살아있음 **20,000**) | 상품 20,000 | ✅ **누락 0** |
| clientes 215 | 174(기본 1 포함) | ✅ 삭제 41 · 문서없음 1 |
| vendedores 2 | 1 | ✅ `ADMIN`/`Admin` 동일인 |
| 가격 | **100,000** | ✅ 20,000 × 5슬롯 |

★ **고아 구제가 209건 작동**했다(noix 는 1건). `PARENT_REVIVED` 3 ·
  `VARIANT_PROMOTED` 117 · 나머지 89 는 그 SKU 의 부모가 이미 있어 새로 안 만듦.
  **209건 전부 목적지에 있다**(SQL 집합 대조).

### 스트리밍 경로 (더미 매장 `ZZ-P86-COMPLETO`)
| 원본 | 목적지 | 판정 |
|---|---|---|
| vcodes 36,703 | sales **35,630** | ✅ 카하시재 976 · CierreZ 5 · 빈판매 92 |
| vdetalle 78,670 | sale_items **78,670** | ✅ **100%** |
| 결제 | 36,518 | ✅ |
| fventas 6,277 | **6,277** | ✅ |
| fdetalles 48,562 | 48,414 | ✅ **품명 빈 148행** |
| gastos 333 | expenses **333** | ✅ **100%** |
| 카하개시금 976 | **976** | ✅ 지점없음 0 |
| ingresos 5 | stocks 0 | ⚠ 재고 계산 결과가 전부 0 (원본 입고가 5행뿐) |

거부 92건은 **품목 0 · 금액 0 인 빈 판매**다(SQL 로 확인). 실질 손실이 아니다.

### ★★★ 검사 쪽 결함 3개를 찾아 고쳤다
1. **지점 매핑이 하드코딩**(`1→Centro, 2→Depósito`). charo4 는 **sucursal=4** 라
   **판매 35,630건 전부·비용 333건 전부가 거부**됐다 — 임포터는 멀쩡한데
   **검사가 0건을 만들어 내고 있었다.** 이제 `perfilSucursales` 로 데이터에서 읽는다.
2. **판매 등식이 「거부」를 안 셌다** — 92건이 등식 밖에 있었다. 이제 세고,
   **거부된 판매에 품목이 붙어 있으면 실패**시킨다(그게 진짜 유실이다).
3. 팩투라 품목을 「차이 0」으로 기대했다 — charo4 의 **품명 빈 148행**에서 깨졌다.
   「그 수만큼만 차이」가 옳은 기대다.

★ 그리고 카탈로그 검사에서 **부모 수를 예측하는 기대식**이 89 어긋났다 —
  임포터의 내부 규칙을 검사가 다시 구현하고 있었다. 「세기」를 버리고
  **집합 대조**로 바꿨다. 합 등식은 범주가 **겹쳤고**(고아 209 중 120이 두 번)
  겹침을 고치면 **항등식**이 되어 아무것도 못 잡는다 — 그래서 뺐다.

---

## 7. 다음 세션이 할 일

1. **루트 `d075276` push 승인** — 레거시 임포트 매퍼 배선(API 전용, 프론트 무관)
2. **업로드 상한 재검토** — 65MB 는 파싱 메모리 기준으로 **안전하지 않다**.
   구조를 바꾸거나(쓰는 표만 파싱) 상한을 내려야 한다
3. **② 재고를 `importar` 로** — 코드↔ProductBranch 지도를 만드는 단계가 필요
4. **⑤ 외상·온라인 매퍼** — 아직 없다
5. **프론트 화면** — 새 엔드포인트를 쓰는 UI(지점 매핑 고르기 + 묶음별 실행)
6. 이월: CI step 타임아웃 45→60분 · `afip-iibb.service.spec.ts` CI 전용 실패 ·
   `verify-before-commit.sh` 의 `--pathspec-from-file` 우회 · **Phase 87 착수**

★ **영구 제외**(2026-09-11 사용자 결정): 운영 DB 인터넷 노출(pgbouncer 분리 A안) ·
  스테이징 재생성. 「남은 작업」으로 세지 않는다.

---

## 9. 스테이징 재생성 — **절차 확정, 밤에 실행** (2026-09-11 사용자 지시)

★ 사용자가 오전에 「영구 제외」했다가, charo4 검증에서 **스테이징으로는 틀린 답이
  나온다**는 실측을 보고 **되살렸다.** 그리고 **「밤에 하도록 하자」** 로 시점을 정했다.
  → **낮에 실행하지 않는다.** 재시드가 운영 서버 위에서 돌고 그 서버는 swap 0 이다.

### 실행 전 실측 (2026-09-11 낮)
| | 값 |
|---|---|
| 지금 `ventago_staging` | **2,258 MB** · 매장 315 · 판매 585,359 · 유저 6,085 · 테이블 **207** |
| 원본 `ventago`(운영) | 55 MB · 매장 14 · 판매 192 · 상품 430 · 테이블 **238** |
| staging 접속 세션 | **0** (지금 쓰는 사람 없음) |
| 서버 여유 | RAM available 20GB · 디스크 235G free(40% 사용) |
| 시드 스크립트 | `/home/jhkim/phase63-staging/` 에 `prepare-300-stores.sh` · `seed-dummy-stores.sql` · `cleanup-dummy-stores.sql` 셋 다 있음 |

### 절차 — **두 단계다** (운영만 복원하면 판매 192건이라 부하 시험이 성립하지 않는다)

```bash
# ① 운영 덤프 → 스테이징 재생성
#    ★ DROP 은 위 2,258MB 를 **되돌릴 수 없게** 지운다(판매 585,359건 포함).
ssh jhkim-server
sudo -u postgres pg_dump -p 5434 -Fc -f /tmp/ventago-$(date +%F).dump ventago
sudo -u postgres psql -p 5434 -c 'DROP DATABASE ventago_staging'
# ★★ OWNER 를 coolsistema 로 **만들면서** 준다. 나중에 ALTER 로 옮기면
#    복원이 권한 오류로 줄줄이 실패한다(로컬에서 4,271개 오류 후 0테이블 전례).
sudo -u postgres psql -p 5434 -c 'CREATE DATABASE ventago_staging OWNER coolsistema'
sudo -u postgres pg_restore -p 5434 -d ventago_staging --no-owner --role=coolsistema \
     /tmp/ventago-$(date +%F).dump

# ② 부하 시험용 더미 재시드
cd /home/jhkim/phase63-staging && ./prepare-300-stores.sh
```

### 끝나고 **반드시** 확인할 것
- 테이블 수가 **238** 인가 (지금 207). 아니면 복원이 끝난 게 아니다
  ([[passing-restore-is-not-a-restored-database]] — 「복원 성공」은 복원된 DB 가 아니다)
- 금액 컬럼 타입이 운영과 같은가 — 이 재생성의 **가장 큰 이유**다. 지금
  `sales.total_amount`·`subtotal`·`prices.amount`·`sale_payment_methods.amount` 가
  **integer**(운영은 double)라 스테이징에서 금액을 재면 **다른 것을 잰다**
- `stock_balances` · `legacy_upload_sessions` · `legacy_upload_parts` ·
  `legacy_caja_aperturas` 가 생겼는가
- 덤프 파일(`/tmp/ventago-*.dump`)을 지운다 — 운영 데이터가 평문으로 남는다

---

## 10. Phase 88 — W1(서버) + W2(홈 체크리스트) **완료·배포**

api `67d4501`(#895) · app `8cf7c8d`(#729) · 루트 `ae6e237`.
마이그레이션 `2026-09-11-setup-guide.sql` 은 **로컬 5432 · 운영 5434 양쪽 적용 완료**.

### 사용자 결정으로 SPEC 이 바뀌었다 (§3 결정표가 권위, §4 웨이브 목록은 **낡았다**)
| # | 내용 |
|---|---|
| ~~D-12~~ **폐기** | 기존 매장에도 보인다. 마이그레이션이 **아무것도 숨기지 않는다** |
| **D-18** | 진행률 **≥70% 그리고 생성 후 7일** → 자동 중단. 저장하지 않고 파생 |
| **D-19** | 자동 중단의 **분모는 8 고정**, 「해당 없음」은 **분자에 넣는다** |
| **D-20** | 술어의 뜻 = 「운영 가능한 실데이터가 있나」(레거시 임포트분도 인정) |
| **D-21** | 문구는 **첫 판매 여부**로 갈린다 (`arranque` / `aprovechar`) |

★ **D-12 폐기가 CODEX P1-1 을 통째로 없앴다** — 「기존 매장 전체 숨김」 UPDATE 가
  사라지니 마이그레이션↔배포 사이의 경쟁 구간 자체가 없다.

### ★★ CODEX 가 잡은 **내 실제 버그 3건** (P1)
1. **권한 게이트가 없었다.** 판매 직원도 매장 가이드를 끌 수 있었고, 서버가
   **모든 단계 dismiss 를 허용**해 임의 3개를 끄면 6/8=75% 로 **자동 중단**시킬
   수 있었다. → `DESCARTABLES`(서버) + `AdminRoleGuard`.
   ★ CODEX 의 문장이 핵심이다: **「프론트의 `descartable` 은 보안 경계가 아니다」**
2. **매장 단위 단계를 지점 id 로 저장**했다 → 다지점에서 TTFV 중복·D-3 붕괴.
   → `ALCANCE` 로 매장 단위는 **항상 `branch_id=0`**.
3. **0/지점 행을 코드 하나로 축약**해 PG 반환 순서에 따라 상태가 달라졌다.
   → `code@branch` 키로 **자기 단위의 행만** 본다.

### P2 — 셋 다 처리
- **snooze 가 저장만 되고 아무 판정에도 안 쓰였다**(있는 척). 이제 동작한다:
  미루면 **목록에서 내려가되 진행률은 그대로**(3→3), 「해당 없음」은 오른다(3→4).
- **`dedupingInterval` 을 오해**해 주석까지 틀렸다. 폴링도 재검증도 아니다 →
  **포커스 복귀 재검증**으로 고쳤다(「나가서 하고 돌아온다」가 이 화면의 흐름이다).
- **인덱스**는 CODEX 가 「측정 전 비용 규모 확인 필요」라 해서 **쟀다**:
  `price_types` **32행** · `payment_methods` 52행 · **0.075ms · buffers 9**
  → 지금은 seq scan 이 옳다. 다시 볼 조건을 코드에 적었다.

### 그 밖에 막은 것
- **없는 메뉴를 약속하고 있었다** — 카드가 「Ayuda › Guía de configuración」이라
  적었는데 그 메뉴가 없었다. 그대로 배포했으면 **닫은 사람이 갇혔다.**
  → 문구를 실재하는 길로 바꾸고 `SetupGuideOculta`(「Volver a mostrarla」)를 뒀다.
- **`refreshUser()` 가 없었다** — 컨텍스트가 내주는 것은 `setUser` 뿐이라
  조용한 no-op 이 될 뻔했다.
- **옛 투어를 껐다**(D-9). ★ 탈레레스의 **동명이인 `OnboardingTour` 는 살아 있다.**

### 배포 효과 (운영 14개 매장)
가이드가 **9곳에 보이고 5곳은 자동 숨김**(coolsistema 100% · ACE 88% ·
Asado/Sager/Liverpool 75%). ★ **kim(16)** 이 이 기능의 존재 이유다 —
팔고 있지만 4/8 이라 「아직 100% 활용 못 하고 있습니다」가 뜬다.

### W4 도 완료 (2026-09-12, front #730)
- **공용 `<EmptyState/>`** + 상품 목록 적용. 판정은 `empty-state.rule.ts`(순수 `.ts`)
  ★★ `ProductsList` 에는 **로딩 상태가 아예 없었다** — 그대로 그렸으면
    데이터가 있는 매장도 매 새로고침마다 「상품이 없습니다」를 봤다.
    `cargando` 를 **필수 prop** 으로 둬서 호출부가 명시하게 만들었다.
- **계측은 SQL 로** — `scripts/setup-guide-metrics.sql`
  ★★★ **PostHog 로 안 보낸다.** TTFV·이탈이 이미 `store_setup_events` 에 있고,
    같은 사실을 두 곳에 보내면 어느 쪽이 권위인지 흐려진다.
    그리고 `posthog.service.ts` 규약이 「매장 업무 데이터를 밖으로 안 보낸다」다.
  ★★ 그 쿼리는 **⓪ 「표본이 있는가」를 먼저 말한다** — 원장이 비면 모든 지표가
    0 인데 그걸 「다들 잘한다」로 읽으면 정반대다. 실행 결과 `eventos=0` 이라
    「모른다」라고 답한다. ③ 블록은 **원장 없이도** 현재 상태를 낸다.

### ★ 오늘 **같은 실수를 두 번** 했다 — 막다른 링크
가이드 카드가 **없는 메뉴**(`Ayuda ›`)를 약속했고, 빈 상태가 **없는 페이지**
(`/productos/nuevo`)로 보내려 했다. 둘 다 배포 전에 잡았지만 형태가 같다.
⤷ 규칙으로 적어 둔다: **갈 곳이 없으면 만들지 않고, 어디서 하는지를 문장으로 말한다.**

### 남은 것 — **W3 위저드뿐** (결정은 이미 끝났다)
- **D-22**(2026-09-12): 업태를 **저장하지 않고 응답에서도 뺀다.** 위저드 답은
  1회용 시드로만. 이후 분기는 `use_restaurant_mode`/`use_size` 로.
  ★ 근거: 실측상 구분되는 상태가 **3갈래뿐**(식당 2 · 사이즈 쓰는 소매 9 ·
    안 쓰는 소매 2)이고, 「신발집인가」를 나중에 알아야 할 용도가 아직 없다.
- **D-23**(2026-09-12): 모드 전환 시 완료·dismiss 승계는 **W3 착수 때 다룬다.**
  ★★ 모드별 카탈로그를 만들면 **`store_setup_steps` UNIQUE 키에 mode 를 넣는
    마이그레이션이 필요하다** — 잊지 말 것.
- (선택) 사이드바 「Guía de configuración」 메뉴 — CASL subject 시드가 필요해
  미뤘다. **되살리기는 홈에 있으므로 급하지 않다.**

---

## 8. 이번에 배운 것

1. **대조군이 통과하는 것을 세 번 겪었다.** 돌연변이 픽스처(`0.1+0.2−0.3`),
   가짜 DB 의 SQL 무시, `join` 이 0건 조인. 셋 다 **「통과」가 「지켜짐」이
   아니었다.** 통과를 보면 먼저 「이게 구분을 하긴 하나」를 물을 것.
2. **사용자가 내 구현의 빠진 절반을 잡았다.** 「며칠부터 며칠까지」만 보고
   「누계 얼마」를 흘렸다. 요구를 **문장째로** 옮겨 적고 하나씩 지울 것.
3. **비슷한 이름의 두 값을 섞었다.** `desde`(구간 첫 이동)와 `vaciadoEn`(비운 시각).
   하루가 밀리면 사용자의 검산이 통째로 안 된다.
4. **「기능이 있다」와 「부를 수 있다」는 다르다.** 매퍼 6종이 완성돼 있고 itest 도
   있었는데 **엔드포인트가 없어 아무도 쓸 수 없었다.** 모듈에 등록돼 있다는 것은
   배선의 증거가 아니다.
5. **OOM 을 「재실행 탓」으로 단정할 뻔했다.** 두 번 중 한 번은 populated 상태에서도
   통과해 가설이 틀렸다. 추측 대신 `process.memoryUsage()` 로 쟀다.

---

## 11. 안내를 **셋업 가이드 하나로 통일** (2026-09-12, 사용자 지시)

사용자가 §10 을 읽고 두 가지를 정했다: **「1번은 없애버려줘. 의미가 없어」**,
**「2번 하나로 통일하자고」**. 그리고 가이드를 볼 사람을 **관리자 + 지점장**으로 좁혔다.

| 커밋 | 무엇 |
|---|---|
| app `c724f86` | `.next/cache` 이미지에서 제거(339MB) · 프론트↔서버 `DESCARTABLES` 대조 검사 |
| app `ffd1645` | 옛 온보딩 투어 **삭제** (프론트) |
| api `10072ab` | 역할 게이트 + 투어 백엔드 제거 |

★ **§10 이 「옛 투어를 껐다」고 적은 것은 사실이 아니었다** — 그 변경은
  워킹트리에만 있었고 운영에는 계속 떴다. 「했다」를 적기 전에 `git log` 를 볼 것.

### ① 역할 게이트 — **조회까지** 막는다
종전에는 쓰기만 관리자 전용이고 조회가 전원에게 열려 있었다. 그래서 판매 직원
홈에 「4/8 — 아직 100% 활용 못 하고 있습니다」가 뜨는데 **아무 버튼도 안 눌렸다.**

★★★ **`ADMIN_ROLES` 에 `branch_manager` 를 직접 넣지 않았다.** 그 집합은
  **레거시 데이터 임포트** 권한과 공유한다 — 넣었다면 지점장이 남의 매장 덤프를
  밀어 넣는 권한까지 얻는다. `SETUP_GUIDE_ROLES` 를 **파생**시키고(차이 한 줄),
  검사가 그 차이가 **정확히 하나**인지 지킨다.

★★ 게이트는 `resolve()` **호출부**에 둔다 — 안에 넣으면 캐시 키가
  `store+branch` 라 **먼저 부른 사람의 역할 결과가 굳는다.**

★ 프론트는 **한 줄도 안 고쳤다.** 화면이 이미 `/auth/me` 요약 하나만 보고
  `null` 이면 아무것도 안 그린다. 역할 목록을 프론트에 복제하면 갈라진다.
  비관리자는 SWR 키가 `null` 이라 403 을 볼 일도 없다(확인함).

운영 29명 기준 — 볼 수 있는 사람 **21명**(관리자 20 + 지점장 1), 못 보는 사람 8명.

### ② 옛 투어 삭제 — 남긴 것과 안 남긴 것
지웠다: `OnboardingTour.tsx`(347줄) · `OnboardingWrapper.tsx` ·
`PUT /auth/onboarding-complete` · `completeOnboarding()` · `/me` 응답 필드 3곳 ·
모델의 `onboardingCompleted` · 프론트 `UserDataType` 필드.

★★★ **DB 컬럼 `users.onboarding_completed` 는 남겼다.** 지금 DROP 하면 배포가
  끝나는 몇 분 동안 아직 도는 구 버전 코드가 그것을 SELECT 해서 `users` 조회가
  전부 500 이 된다(무중단 규약의 contract 는 다음 배포 몫). 게다가
  `store/store-restore-columns.txt` 매니페스트가 이 컬럼을 열거한다.

★★ **`app/onboarding/` 은 다른 모듈이다** — 공개 가입 OTP(`@Public()`,
  `POST /onboarding/start`). 이름만 같다. 안 건드렸다.
★★ **탈레레스에 동명이인 `OnboardingTour` 가 살아 있다**
  (`src/views/talleres/components/`). 이름으로 일괄 삭제하면 그것까지 지운다.
★ 외부 소비자 전수 확인 — 로컬 8개 저장소 grep 0건(Flutter 포함).

### 검증
api tsc 0 · `src/app/setup-guide` 55건 · `src/app/auth`+`users` 124건 ·
app tsc 0 · eslint 0 · 프론트 12건. **돌연변이 2종으로 검사가 구분을 하는지 확인**:
`puedeVerGuia → return true` ⇒ 5건 실패, `branch_manager` 를 `ADMIN_ROLES` 로
이동 ⇒ 2건 실패.

### 남은 것
- **push 승인 대기** (api `10072ab` · app `ffd1645` · 루트)
- ★ 짝 배포 판단: **어느 쪽이 먼저 나가도 안전하다.** API 가 먼저면 옛 프론트는
  `setupGuide` 가 `null` 이라 가이드를 안 그리고(폴백 이미 있음), 투어는
  `onboardingCompleted` 가 안 와서 `undefined` → `=== false` 가 거짓이라 안 뜬다.
  앱이 먼저면 투어는 사라지고 가이드는 구 API 로 그대로 뜬다.
- W3 위저드(D-22·D-23)는 그대로 남아 있다.

### CODEX 자문 결과 — P2 둘 · P3 하나 (api `e9801d3` 로 반영)

| 지적 | 판정 | 조치 |
|---|---|---|
| **P2** `/me` 의 `verGuia` 만 **매장 필터가 없다** | **맞다** | `rolesDeLaTienda()` 로 걸렀다 |
| **P2** superadmin 이 집합에 있는데 `/me` 는 늘 `null` | 맞지만 **이번 변경이 만든 게 아니다** | 남겨 둠(아래) |
| **P3** 폐기한 `PUT /auth/onboarding-complete` 가 배포 창에 404 | **맞다** | 한 주기 **no-op** 으로 남김 |

★★★ **P2 는 내가 만든 결함이다.** `issueAccessToken` 과 `roles_local` 은 이미
  `role.storeId === user.storeId` 로 거르는데 내가 넣은 판정만 안 걸렀다 —
  **같은 응답 안에서 근거가 둘로 갈렸다.** 다른 매장에 `admin` 행이 남은 사용자가
  지금 매장의 가이드를 열람·변경할 수 있었다.
  운영 실측 어긋난 행 **0건**이지만, 0건인 것과 막혀 있는 것은 다르다.

★★ **남은 같은 형태 — 별건으로 다룰 것.** `users.service.ts` 의 `shapeAuthUser()`
  가 만드는 `req.user.roles` 도 **매장 필터가 없다**(모든 역할 slug 를 push).
  `SetupGuideRoleGuard` 와 **기존 `AdminRoleGuard`(레거시 임포트)** 가 그것을 읽는다.
  이번 변경이 만든 것이 아니고, 그 함수는 **모든 인증 요청**이 타는 자리라
  (실측 44,231회) 여기서 같이 건드리지 않았다.

★ **P3 의 제거 시점**: 다음 배포 이후 언제든. 함께 지울 것 —
  DB 컬럼 `users.onboarding_completed` · `store/store-restore-columns.txt` 의 그 줄.

★ superadmin 건: 가드가 superadmin 을 통과시키는 것은 옳다(매장이 없으면 서비스가
  `null` 을 준다). 대행 매장의 가이드 요약을 superadmin 에게 계산해 주는 것은
  **별개 기능**이라 하지 않았다.
