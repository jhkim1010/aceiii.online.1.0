# 핸드오프 — 2026-08-12

성격: **código madre 쓰기 경로 마무리 + talleres 생산 입고 설계·1단계 + tienda-admin 앱.**
마지막에 **방향이 틀린 것 하나**가 드러났다 — 아래 「★ 먼저 읽을 것」.

---

## ✅ 2026-08-12 (2차 세션) 처리 완료

| 항목 | 결과 |
|---|---|
| **1. tienda-admin Códigos madre 교체** | 완료. `9b6efa1` — 재고 편집 → 제품 마스터 편집(이름/가격/공개몰). APK `20260812-1650` 재빌드·재배포, 고정 링크(`tienda-admin-android.apk`)도 갱신 |
| 잘못된 APK `20260812-1557` | **삭제**(사용자 승인). 배포 폴더에서 제거. `Personal de m. Marcos` 사본은 개인 보관이라 남겨 뒀다 |
| mockup 교체 | 완료. `1e30d71` — `tienda-admin-codigo-madre-mockup.html` 을 새 화면 기준으로 재작성 |
| **2. talleres 2단계 마이그레이션** | 완료. api `6849a5f` — `talleres_recepcion_items` + 헤더 4컬럼. **로컬 5432 · 운영 5434 양쪽 적용·대조 완료**, owner(테이블/시퀀스/타입) coolsistema 이전 확인. Jenkins #683 SUCCESS |
| **3. talleres 3단계 — corte 변형별 입력** | 완료. api `85855c9` #684 · app `fb5cd74` #608, 둘 다 컨테이너 재생성 + 운영 라우트 확인(401 = 가드 동작). 로컬 API 기동해 실데이터로 격자 조회 3종 · 저장 거부 4종 · 정상 저장 1건 검증 후 테스트 데이터 정리 |
| **4. talleres 4단계 — 최종 공정 자동 입고** | 완료. api `49fadc7` #685 · app `c3108ef` #609, 둘 다 컨테이너 재생성. **로컬 실DB(트리거 포함) 검증**: PENDING / 없는 지점 400 / cortado 초과 400 / 정상 POSTED + 원장 3행 + `stock_balances` 반영 + 누적·경계 검증. drift·leak 0행 유지, 테스트 데이터 전량 정리 |
| **5~8단계 — 완료 전이 · 미결 배너 · 입고 취소 · MES 가드** | 완료. api `e9e7c63`+`2bc0f20` #686/#687 · app `f530ff8` #610, 전부 SUCCESS + 컨테이너 재생성 + 운영 라우트 확인. **talleres 생산 입고 8단계 전부 종료** |
| 운영 데이터 | 로트 10 수령 #3 을 `PENDING` 으로 표시(사용자 승인, 1행). 배너에 노출됨. 재고는 미입고 — 실제 수량 확인 필요 |

**가격 저장 라우트가 핸드오프 기재와 다르다 (정정).** 아래 「확정된 API 계약」의
`PUT /prices/:id` 는 **틀렸다** — 그 행 하나만 바꿔서 **정작 POS 가 파는 자식 가격이
안 바뀐다**. 실제로 쓴 것은 `POST /products/bulk-update-prices` 이고, 이 라우트만
부모 대상일 때 [부모 + 자식 전부] 를 upsert 하고 base 유형이면 `products.price` 도
맞춘다(웹 `CodigoVistaView` 와 동일). `bulk-update-explicit` 도 부모면 자식만 고쳐
부모 행이 어긋난다. CODEX 자문에서도 같은 결론.

**부수 발견**: 부모×가격유형 조합 77개 중 **10개가 자식끼리 금액이 갈려 있다**(로컬 실측).
그래서 편집 화면은 (a) 손댄 가격유형만 전송하고 (b) 갈린 유형에 경고를 띄운다.
저장은 부모+자식을 한 값으로 통일하므로 안 만진 유형까지 보내면 차등가가 조용히 사라진다.

**talleres 생산 입고는 2~8단계가 전부 끝났다.** 남은 것은 §5 의 별건들(3~8번)뿐이다.

**다음 세션 시작점 후보**:
- **로트 10 실물 처리** — 배너에 떠 있다. 25장의 실제 변형별 수량을 확인해
  수동 보정(Ingresar a stock)으로 넣으면 PENDING 이 닫히고 로트가 COMPLETED 가 된다.
  ※ 수동 보정은 **계획 수량**을 넣으므로 실제와 다르면 그만큼 어긋난다.
- §5 3번 지점 미해결 취소 / 4번 `approveReturn` 부분환불 / 5번 미검토 쓰기 경로

---

## ★ 먼저 읽을 것 — tienda-admin 앱 Códigos madre 카드가 잘못 만들어졌다
### (↑ 위 표대로 **해결됐다**. 아래는 경위 기록이다)

**사용자가 원한 것**: 제품의 **이름 / 가격(모든 가격대) / 공개몰 전송 여부** 편집.
**내가 만든 것**: 재고 편집(`edit-madre-variants` — 날짜·지점별 색×사이즈 수량).

`c156d8e` 로 커밋·push 됐고 **APK 까지 빌드해 Dropbox 배포 폴더에 올렸다**
(`tienda_admin_android_20260812-1557.apk`). 즉 매장 admin 손에 **아무도 요청하지 않은
재고 편집 화면**이 들어가 있다. 확인 다이얼로그가 있고 admin 전용이지만 방치하면 안 된다.

### 다음 세션이 할 일

1. `codigo_madre_editor_screen.dart` 를 **제품 마스터 편집으로 교체**한다.
   목록 화면(`codigo_madre_screen.dart`)과 카드는 그대로 재사용 가능하다.
2. 교체 후 APK 재빌드 + Dropbox 재배포(`./build-apk.sh` 한 줄).
3. 사용자와 정한 것: 웹 전송 = `isPublishedShop`(공개몰 Tienda Web),
   가격 = **가격유형별 전체**(대표가 하나가 아니다).

### 확정된 API 계약 (다시 찾지 마라)

| 대상 | 엔드포인트 | 비고 |
|---|---|---|
| 이름 수정 | `PUT /products/:id` | 권한 `editar-un-producto` |
| 공개몰 게시 | `PUT /products/publish-shop` | body `{ productIds: number[], published: boolean }`, 권한 `publicar-o-no-publicar-producto`. **일괄 토글**이라 단건도 배열로 보낸다 |
| 가격 수정 | `POST /products/bulk-update-prices` | `[{ productId, prices:[{priceTypeId, newAmount}] }]`, 권한 `cambiar-precio-individual`. 부모면 **부모+자식 전부** upsert. ~~`PUT /prices/:id`~~ 는 **틀렸다** — 행 하나만 바꿔 POS 가 파는 자식 가격이 안 바뀐다 |
| 가격 조회 | `GET /prices/matrix?productIds=1,2` 또는 상품 상세의 prices include | price_type 별 |
| 부모 목록 | `GET /products/by-parent?parent=false` | ★ `parent=false` 가 **부모**다 |

관련 컬럼: `products.name`, `products.is_published_shop`,
`products.publish_marketplace`(마켓플레이스 — **이번 범위 아님**), `prices`(price_type_id 별).

### APK 처리 — **결정: 그대로 둔다 (2026-08-12 사용자 결정 "A")**

배포된 `tienda_admin_android_20260812-1557.apk` 는 **회수하지 않는다.** 카드만 빼고
즉시 재배포하는 안은 기각됐다. 따라서 다음 세션이 올바른 기능으로 교체한 뒤
**같은 파일명 규칙으로 재빌드·재배포**하면 그때 자연스럽게 사라진다.

그때까지 남는 위험(문서화만 해 둔다):
- 매장 admin 이 Reportes ▸ GESTIÓN ▸ Códigos madre 로 들어가면 **재고 편집 화면**이 뜬다.
- 그 화면은 `PUT /products/:id/edit-madre-variants` 를 호출한다 —
  **특정 날짜 + 특정 지점의 변형 수량을 실제로 바꾼다.** 확인 다이얼로그는 있다.
- 잘못 저장된 경우 되돌리는 방법: 같은 화면에서 원래 수량으로 다시 저장하거나,
  재고 조정(Panel D)으로 보정한다. 원장은 append-only 라 반대 부호 행으로만 상쇄된다.

★ 교체 후 **APK 재배포를 잊지 마라** — 코드만 고치면 사용자 폰에는 옛 화면이 그대로다.
  `cd tienda-admin-app && ./build-apk.sh` (SDK·JDK 는 이 기계에 이미 갖춰 뒀다. 3절 참조.)

---

## 1. 완료·배포된 것

| 커밋 | 내용 | 빌드 |
|---|---|---|
| api `31ee153` | **판매 취소** legacy 부모 가드 + 트리거 오류 변환 | #678 |
| api `ac12468` | **재고 조정 API** 부모 PB 차단 (화면만 막고 API 는 무방비였다) | #679 |
| api `8eaca7d` | PG 부트스트랩 마이그레이션 + CORS 안내 정정 | #680 |
| api `0c89c54` | **talleres 1단계** — `ingresarAStock` 안전화 | #681 |
| api `a9f7305` | **창고 지점** — Caja/Terminal 없이 생성해 POS 격리 | #682 |
| `c156d8e` | tienda-admin Códigos madre 화면 (★ 방향 오류, 위 참조) | — |
| `11f15af` `31fb9b5` `f00410c` `7ddc774` | infra 스크립트 / Phase 77 문서 / 서브모듈 포인터 / gradle 플래그 | — |

전부 Jenkins SUCCESS + 컨테이너 재생성 + 배포본 실물 확인.

**DB 변경 1건**: `branches` 에 `Depósito Central`(id **25**, store 6, `is_warehouse=true`,
Caja 0 / Terminal 0). 되돌리기는 `DELETE FROM branches WHERE id=25` (재고 들어가기 전).

---

## 2. talleres 생산 입고 — 설계 확정, 1단계만 완료

### 지금 상태 (실측)
- 운영 `stocks` 에 `type='production'` **0건** — 생산으로 재고가 들어온 적이 없다.
- 로트 5건 전부 store 6. 로트 10 은 **3개 공정 다 돌고 pendiente 0 인데 재고 0**.
- `talleres_lotes.size_color_matrix` 에 `qty[colorId][sizeId]` 가 실제 ID 로 있고
  자식 상품이 `color_id`/`size_id` 를 가져 **1:1 매핑이 추측 없이 떨어진다**.
- `ingresarAStock`(수동 입고)은 **이미 존재**했다 — 수령 업무와 연결이 없어 아무도 안 눌렀다.

### 확정된 설계 결정
1. **변형별 수량은 두 지점에서만** — `corte`(실제 재단량, 재고 영향 없음)와
   최종 공정(실제 입고량, 재고 기록). 중간 공정은 총량만. 업계 표준과 일치.
2. **기준선은 `cortado`** (corte 수령 양품 누적). 없거나 불완전하면 **plan 폴백**하고
   화면에 출처를 표시한다(`Referencia: corte real` / `Referencia: plan`).
3. **재작업은 정상 입고**다(중복 아님) — 원 수령에서 불량으로 빠졌던 것이 처음 들어오는 것.
   검증식을 `sum(final.received) ≤ cortado` 로 두면(rejected 제외) 재작업이 이 식을
   깨지 않는다. 분기·플래그·새 컬럼 불필요.
4. **최종 공정 판정은 `MAX(order)` 가 아니다.** `buildRoutingPath` 가 order 를 1..N 으로
   재정규화하므로, canonical 검증(비어있지 않음 · order 정확히 1..N · etapaId 중복 없음)
   후 **배열의 마지막 원소**. 검증 실패 시 자동 입고 금지.
5. 데이터는 `talleres_recepcion_items` 자식 테이블(JSONB 아님). 멱등 키
   `UNIQUE(recepcion_id, product_id)`. 헤더-항목 합계 일치를 같은 트랜잭션에서 강제.
6. 부분 수령은 **수령할 때마다 즉시 부분 입고**.

### 남은 단계
2. ~~`talleres_recepcion_items` + 수령 헤더 확장~~ — **완료** (api `6849a5f`,
   `migrations/talleres-recepcion-items.sql`, 로컬 5432 · 운영 5434 양쪽 적용·대조 완료).
   구조만 만들었다. 원장 INSERT 는 4번에서 수령 저장과 **같은 트랜잭션**으로 붙인다.
   Sequelize 모델은 **아직 없다** — 쓰는 코드와 함께 추가한다("빌드 통과 = 배선 아님").
   `inventory_status` 기본값 `NOT_APPLICABLE`, 입고 대상 판정은 저장하지 않고
   매번 `routing_path` 에서 계산한다(캐시 금지 — 아래 ★ 미해결 위험).
3. ~~corte 수령 변형별 입력~~ — **완료** (api `85855c9` #684, app `fb5cd74` #608, 둘 다 배포됨).
   · `lotes/routing-position.ts` — 공정 자리 판정 **단일 출처**(FIRST/FINAL/ONLY/MIDDLE/UNKNOWN).
     canonical 검증 실패 → UNKNOWN, 그 상태에선 기록도 입고도 안 한다. spec 16건 +
     **뮤테이션 3종으로 사멸 확인**(MAX(order) 구현 / canonical 가드 제거 / UNKNOWN 입고 허용).
     ★ 4단계는 이 헬퍼를 그대로 쓴다. 판정을 다시 만들지 마라.
   · `GET /talleres/recepciones/envio/:id/variant-grid` — 격자 + `applicable=false` 의 **이유**.
   · `POST /talleres/recepciones` 가 `items[]` 를 받는다(**선택**). 헤더-격자 합계 일치를
     같은 트랜잭션에서 강제. 지금은 **FIRST 만** 허용 — FINAL 은 4단계에서 연다.
4. ~~최종 공정 수령 → 자동 입고, 한 트랜잭션~~ — **완료** (api `49fadc7` #685, app `c3108ef` #609).
   · `ingresarStockPorProductos` 신설 — 변형 productId 를 직접 받는 원장 입고.
     `ingresarStockPorMatrix` 가 여기로 **위임**하므로 원장 INSERT 구현은 하나뿐이다.
   · 수령 저장과 같은 트랜잭션. 로트 행을 잠근다 — `stocked_quantity` 는
     수동 `ingresarAStock` 과 **공유 카운터**라 안 올리면 이중 입고가 된다.
   · 기준선: `sum(final.received) ≤ cortado`. `corte_real` 이면 하드 블록,
     `plan` 폴백이면 허용 + 경고(계획 초과 생산은 정상).
   · ★ 넣을 수 없으면 `inventory_status='PENDING'` 으로 남긴다(조용히 넘어가지 않는다).
     벤더 포털 경로에도 같은 표시를 넣었다.
5. ~~로트 상태 자동 전이 + 미결 배너~~ — **완료** (api `e9e7c63`/`2bc0f20`, app `f530ff8`).
   · `lote-completion.ts` — 완료 판정 단일 출처. ★ **재고 입고까지 끝나야 완료**다.
     공정만 끝나고 PENDING 이 남았는데 COMPLETED 로 올리면 로트 10 의 구멍을
     **완료로 위장**한다. spec 7건 + 뮤테이션 사멸 확인.
   · `GET /talleres/recepciones/pendientes` + 화면 상단 상시 배너(비면 미렌더).
6. ~~수동 `ingreso-stock` 을 관리자 보정으로 격하~~ — **완료**.
   · 원장 note = `Ingreso manual (corrección)` 로 정상 입고와 구분.
   · ★ 보정이 그 로트의 PENDING 을 **닫는다** — 안 닫으면 배너가 영원히 틀린 경보를
     내고, 사람이 배너 자체를 무시하게 된다.
   · **로트 10**: 운영 수령 #3(planchero, 최종)을 `PENDING` 으로 표시했다(사용자 승인).
     배너에 `LOT-2026-005 · planchero · 25u · falta detalle y sucursal` 로 뜬다.
     재고는 **아직 안 넣었다** — 실제 수량을 사람이 확인해야 한다.
     되돌리기: `UPDATE talleres_recepciones SET inventory_status='NOT_APPLICABLE' WHERE id=3;`
7. ~~입고 취소(반대 부호 행)~~ — **완료**. `POST /talleres/recepciones/:id/revertir-ingreso`.
   · `revertirStockPorProductos` — 부호만 다르고 구현은 입고와 공유(`writeStockRows`).
   · POSTED 만 취소 가능, 공유 카운터 복구, **COMPLETED 로트는 IN_PROGRESS 로 되돌린다**
     (재고를 뺐는데 완료로 남으면 로트 10 과 같은 모양이 된다 — 실DB 검증 중 발견).
   · 음수 재고를 막지 않는다(이미 팔렸으면 음수가 맞다). 대신 응답에 실어 알린다.
8. ~~MES 부모 작업지시 leaf 가드(400)~~ — **완료**. 원장에 손대기 **전에** madre-guard 로
   걸러 400. 종전에는 트리거가 거부하며 트랜잭션이 죽어 원인이 덮인 500 이었다.
   spec 3건 + 뮤테이션 사멸 확인.

### ★ 새로 드러난 것 — `talleres_recepciones` 에 쓰는 곳이 **셋**이다 (3단계에서 발견)

변형별 격자를 붙인 곳은 **하나뿐**이다. 나머지 둘로 수령하면 그 로트의 cortado 기준선이
비게 된다(설계상 plan 폴백으로 degrade 되지만, 4단계 검증식의 정밀도가 떨어진다).

| 경로 | 파일 | 격자 | 비고 |
|---|---|---|---|
| ✅ `/talleres` ▸ Envíos 탭 | `EnviosTab` → `RecepcionFormDialog` | **있음** | 사이드바가 가리키는 **실사용 경로** |
| ❌ `/talleres/envios` (레거시) | `talleres_EnviosListView` 인라인 모달 | 없음 | **네비게이션에 없다** — 직접 URL 로만 도달. QC + 자동 rework 를 들고 있어 함부로 못 지운다 |
| ❌ 벤더 포털 | `vendor-recepciones.service.ts` | 없음 | 벤더가 자기 앱에서 수령 확인. 독립 트랜잭션으로 직접 INSERT 한다 |

**4단계에서 처리한 방식**: 격자를 세 곳에 다 붙이는 대신, 넣을 수 없는 수령을
`inventory_status='PENDING'` 으로 **남기게** 했다(매장 경로 + 벤더 포털 둘 다).
레거시 화면은 `RecepcionService` 를 거치므로 자동 적용된다. 즉 나머지 둘로 들어와도
재고가 조용히 비지 않고 **미결로 보인다** — 5단계 배너가 그걸 드러낸다.

### ★ 미해결 위험 — routing 이 바뀌면
`updateRoutingPath` 는 FREE(envío 없는) 공정의 **추가·제거·재정렬을 허용**한다.
- **시나리오 1**: 입고 후 뒤에 공정 추가 → 새 최종 공정 수령 시 **이중 입고**.
  → `sum(final.received) ≤ cortado` 총량 검증이 잡는다(cortado 실측일 때 하드 블록 가능).
- **시나리오 2**: 최종 뒤 공정을 제거 → 이전 수령이 최종이 됐는데 입고 안 됨 →
  **조용한 구멍**(로트 10 과 동형). 검증으로 안 잡힌다 → **상시 감지 배너**가 필요하다.
  mockup 섹션 5 의 경고를 일회성이 아니라 상시 규칙으로 승격할 것.
- 원칙: **최종 공정 판정을 캐시하거나 수령 시점에 굳히지 마라.** 매번 routing_path 에서 계산.

### 마스터 데이터 문제 — ★ 재확인 결과 **지금은 무해하다** (2026-08-12)
`talleres_etapas`(store 6)에 `order 5` 가 둘인 것은 맞다(`planchero` id 5, `costurero` id 6).
그러나 **`costurero` 는 `is_active = false`** 이고, `buildRoutingPath` 는 기본 경로를
`where: { storeId, isActive: true }` 로 뽑는다 → **애초에 후보에 들어가지 않는다.**
"명시 지정 안 하면 costurero 가 최종이 된다"는 기재는 **성립하지 않는다.**
운영 변경은 하지 않았다. 남는 위험은 **누가 costurero 를 재활성화하면** 그때 동점이
실제로 문제가 된다는 것 — 그때 order 를 6 으로 올리면 된다. 우선순위 낮음.

### mockup
- `mockups/talleres-ingreso-stock-mockup.html` — 7개 화면, 전 화면 색×사이즈 격자 유지
- `mockups/tienda-admin-codigo-madre-mockup.html` — ★ 재고 편집 기준이라 **교체 대상**
- 설계 문서: `.gsd/review-request-produccion-ingreso-stock.md`,
  `.gsd/review-request-nullify-legacy-madre.md`

---

## 3. 환경 변경 (이 기계)

- **Android SDK 가 없었다** → `brew install --cask android-commandlinetools` +
  `platform-tools` / `platforms;android-36` / `build-tools;36.0.0`,
  `flutter config --android-sdk /opt/homebrew/share/android-commandlinetools`
- **Gradle 8.14 가 Java 25 를 못 읽었다**(Studio 번들 JBR) → `brew install openjdk@21` +
  `flutter config --jdk-dir /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`
- `flutter config` 는 전역이라 다른 Flutter 프로젝트도 이 JDK 를 쓴다.

---

## 4. 작업 방식 — 이번에 반복해서 걸린 것

- **★ 쓰기 경로 grep 은 네 패턴을 다 봐야 한다** — `Stocks.create` 만 봐서
  "3곳뿐"이라 단언했다가 틀렸다. `this.stockModel.create` / `bulkCreate` /
  `INSERT INTO stocks` 로 8개 파일이 더 있었다. 같은 함정에 **두 번** 걸렸다:
  `subcon/` 을 grep 해 "재고 연결 없음"이라 했으나 `ingresarAStock` 이
  `products/productStock.service` 로 **위임**하고 있었다.
- **★ 테스트가 엉뚱한 가드로 통과한다** — 음수 수량 테스트가 새 검증을 지워도 통과했다
  (음수만 주면 총합 0 → 기존 "수량 0" 가드에 걸림). 양수와 섞어야 실제로 사멸한다.
  **뮤테이션 없이 "테스트 추가"를 완료로 치지 마라.**
- **CODEX 자문이 1시간 반 넘게 안 끝나 중단했다.** 질문이 길고 파일 탐색이 많으면
  걸리는 것 같다. 범위를 좁혀 던질 것.
- 반복된 형태는 이번에도 **"부재가 침묵한다"** — 수령해도 재고가 안 들어가고,
  화면은 막는데 API 는 안 막고, 음수는 조용히 skip 되고, 로트 10 은 다 끝났는데 0 이었다.

---

## ★ 2026-08-13 — gastos 카테고리: 마이그레이션이 **한 번도 적용된 적이 없었다**

**증상**: gasto 생성 모달의 카테고리 목록이 **색 점만 있고 글자가 없는 행**으로 나왔다.

**원인 (실측)**: `expense_categories` 의 **모든 행**이 `path=''`, `depth=0`(자식 포함).
앱은 이 두 컬럼을 절대 쓰지 않는다 — 서비스 주석대로 **DB 트리거**가 채우는 값이다.
그런데 로컬(5432)·운영(5434) **양쪽 모두** `pg_trigger` 0행, `fn_expense_categories_path()`
없음, 인덱스는 PK 하나뿐이었다 → `migrations/26-01-step1-schema.sql` 이 **한 번도 적용된 적이
없다**. 테이블은 Sequelize sync 가 모델에서 만든 것이라 컬럼만 있고 로직이 전부 빠졌다.
컴포넌트가 `depth > 0 ? name : path` 로 그리는데 depth 가 항상 0이라 늘 빈 path 를 표시했다.

**조치 (로컬·운영 양쪽 적용·검증)**
1. `2026-08-13-expense-categories-path-backfill.sql` — 재귀 CTE 로 path/depth 계산(운영 60행 /
   로컬 54행). 조인에 `store_id` 일치를 넣었다(백필은 테넌트 훅을 우회하는 raw SQL 이다).
2. `26-01-step1-schema.sql` — CHECK 2 + partial UNIQUE 2 + 인덱스 3 + 함수 + 트리거.
3. `2026-08-13-expense-cat-unique-active-only.sql` — 아래 회귀 수정.
4. app `1664f8e` #615 — `labelOf(n) = path || name || '#id'` 로 표시 지점 6곳 방어.

**★ 이건 표시 수정이 아니라 동작 변경이다.** path 가 전부 `''` 이던 탓에:
- `target.path === node.path` 가 **항상 참** → **카테고리 이동이 전부 "사이클 감지"로 거부**되고 있었다.
- `LIKE node.path || ' > %'` 는 `LIKE ' > %'` 가 되어 자손을 못 찾았다 →
  cascade archive 는 자기 자신만, subtree depth 검증은 자기만 계산했다.
- 프론트 `getValidMoveTargets()` 도 빈 path equality 로 거의 모든 노드를 제외했을 것이다.
→ move/promote/delete cascade/depth 검증이 **이번에 처음 의도대로 동작한다.** 회귀 감시 대상.

**내가 만든 회귀 1건 (수정 완료)**: 원본 partial UNIQUE 가 `status` 를 안 봐서 **보관된
카테고리가 이름을 영구 점유**했다(store 6 `Alquiler` id 9 때문에 같은 이름 재생성 불가).
적용 전에는 인덱스가 없어 자유로웠으므로 내가 넣은 제약이다 → `status = 1` 조건을 붙인
`*_active` 인덱스로 교체(사용자 결정). 양쪽 검증: 보관 이름 재생성 OK / 활성 중복 차단 유지.

**오염 점검 (사용자 질문)**: 다른 매장 데이터 섞임 **없음** — 교차 매장 부모 0건,
gasto↔카테고리 매장 불일치 0건. 생성 경로가 부모를 `where {id, storeId}` 로 찾아 애초에 막힌다.
참고: 운영 `expenses` 는 **0행**(레거시 `expenses_categories`·`expenses_subcategories` 도 0행).

### ★ 이어진 두 번째 장애 — 저장이 500 (같은 원인, 다른 마이그레이션)

카테고리 목록은 고쳐졌는데 `POST /expenses` 가 500 이 났다:
`null value in column "expenses_subcategory_id" violates not-null constraint`.

**같은 원인**이다 — Phase 26 마이그레이션 체인이 통째로 실행된 적이 없다.
문서화된 순서는 `26-01` → `26-05` → `26-03` → `26-04` 인데 오늘 **26-01 만** 적용해서
화면만 고쳐지고 쓰기 경로는 레거시 NOT NULL 에 그대로 막혀 있었다.
코드는 이미 이관돼 있었다(모델이 `expensesSubcategoryId: allowNull: true`,
주석도 "새 INSERT 는 NULL 로만") — **DB 제약만 남아 있던 것**이다.

`26-05-reconcile-prod.sql` 은 **2026-05-21 에 오늘과 똑같은 진단**(트리거·인덱스·FK 누락,
store 6 의 16건이 path='' , expenses 0건)을 하고 쓴 파일이다. 계획은 정확했는데 **실행이 안 됐다.**

적용(로컬·운영 양쪽, 사용자 승인):
- `26-05-reconcile-prod.sql` — path 백필(이미 채워져 no-op) + `expenses_subcategory_id` DROP NOT NULL
- `26-03-step3-fk.sql` — FK `fk_expenses_category` + `idx_expenses_category`
- `26-04-step4-verify.sql` — 검증(고아 0 / path·depth 무결성 0 / 사이클 0)
로컬에서 **실패하던 INSERT 를 그대로 재현해 성공 확인** 후 ROLLBACK. 앱 배포 불필요(순수 DB).

★ 교훈: 이 리포에서 마이그레이션 파일의 존재는 **적용의 증거가 아니다.** 26-01 을 적용했을 때
같은 Phase 의 나머지 파일도 확인했어야 했다. 다음에 `migrations/` 에서 파일을 하나 집으면
**같은 Phase 접두사 전체**(여기서는 `26-*`)를 한 번에 대조할 것.

### 남은 것 (CODEX 자문 — 근거까지 대조함, 안 고침)
1. **트리거가 부모의 store_id 를 확인하지 않는다** — `WHERE id = NEW.parent_id` 뿐.
   앱 경로는 막지만 raw SQL 은 뚫린다. `AND store_id = NEW.store_id` + 못 찾으면 예외가 정석.
   더 강하게는 `(id, store_id)` UNIQUE + `(parent_id, store_id)` 복합 FK.
2. `created_at`/`updated_at` 에 DEFAULT 없음(타입도 `timestamptz` vs 마이그레이션 의도 `timestamp`).
   `CREATE TABLE IF NOT EXISTS` 는 기존 테이블의 컬럼을 대조하지 않는다 — 앱은 Sequelize 가
   값을 넣어 정상이고 **raw INSERT 만 실패**한다(실측). reconciliation 마이그레이션 필요.
3. **보관 카테고리 복원 시** 같은 이름의 활성 노드가 있으면 충돌한다(실제 충돌이라 막는 게 맞지만
   복원 경로가 안내를 줘야 한다). promote/move 에도 목적지 sibling 충돌 사전검사 + 400 변환 필요.
4. CODEX 권고: subtree 판정을 path 문자열 대신 **`parent_id` 재귀**로 바꿀 것.
   이름에 `' > '` 가 들어가면 지금 방식은 또 깨진다.
5. 트리거는 `path`/`depth` **직접 변조를 막지 않는다**(name·parent_id 안 바뀌면 조기 반환).
   모델 주석("트리거가 덮어쓴다")은 사실과 다르다.

---

## ★ 2026-08-13 — Tesorería ▸ Registros 깜빡임 (app `7b31f03` #616, 해결 확인)

**무한 루프가 아니었다.** effect 의존 함수(`getCashRegisters`/`getTerminals`/`getBranches`)는
전부 `useCallback` 으로 안정적이고 `useBox`·`useUsers`·`useMpWallets`·AuthContext 도 정상이었다
(CODEX 자문이 내 가설 5개를 전부 기각했고, 직접 추적한 결과와 일치).

**실제 원인 2가지**
1. 컬럼 정의가 매 렌더 재생성 — `FullTable` 은 `columns` 참조가 바뀌면 AG Grid columnDefs 를
   통째로 다시 변환한다. 이 화면은 진입 시 조회 7개가 병렬로 돌고 각각 loading/데이터/loading
   으로 상태를 2~3번 바꿔 **10회 넘게 렌더**된다 → 매번 그리드 재구성 = 깜빡임.
2. 페이지 변경 시 요청 2회 — `setPaginationModel(model)` 직후 같은 스택에서 조회를 불러
   **이전 페이지**를 캡처했고, 이후 effect 가 새 페이지를 또 불렀다. 응답 순서가 뒤집히면
   이전 페이지 데이터가 다시 그려진다.

→ actions/핸들러 `useCallback`, cols `useMemo`(두 표), `setPagination` 은 상태만 변경.

**★ "수십 초 이상 계속된다"는 보고는 배포 전이라서였다.** push 직후에는 아직 옛 청크가
브라우저에 남아 있다 — 증상이 안 바뀌면 **먼저 배포 완료·하드 리로드부터 확인할 것**
([[white-screen-after-deploy-is-stale-html]] 과 같은 형태). 이걸 먼저 안 물어서
"트리 밖 원인"을 한참 찾을 뻔했다.

**함정 재확인**: `BoxControlList.tsx` 는 `file` 이 "Java source" 로 판정해 **일반 grep 이 조용히
0건**을 낸다. `grep -a` 필요. "결과 0건이 이상하다"가 유일한 신호였다.

**남은 것**: `const cols = columns(actions)` 패턴이 리포에 약 90곳(CODEX 집계). 전부 결함은
아니고, 조회가 병렬로 여러 개 도는 화면에서만 드러난다.

---

## ★ 2026-08-13 — gasto 편집·삭제 (api `d158007` #692 · app `253a984` #617)

**요구**: "입력 후 실수를 발견하면 지울 수 있어야 한다. 편집도. 카하를 닫기 전이면
cashier·지점 admin 도. 열린 카하에서는 금액까지."

### ★ 먼저 — 오늘 운영에서 돈이 실제로 어긋났다
gasto 생성이 **트랜잭션이 아니었다**(컨트롤러가 `createExpense(body)` 를 인자 없이 호출 →
`transaction` undefined). 그래서 카하 조작과 expense INSERT 가 따로 커밋됐고, expense 가
NOT NULL 로 죽자 `box_operations` id 213(−10,000)이 **고아**로 남았다 — 현금은 빠졌는데 이유가 없다.
→ 사용자 확인 후 `+10.000` 보정(op #216) 삽입. caja 241 순액 −100,000 = 실제 gasto 1건과 일치.

### 설계 (CODEX 자문 반영)
- 게이트는 **역할이 아니라 카하 상태**다. 열려 있으면 `modificar-gasto`/`borrar-gasto` 보유자가
  편집·삭제(금액 포함). 닫히면 전원 차단.
- `box_operations.expense_id` 신설(FK + 부분 유니크). **백필하지 않는다** — 추정 매칭은 틀린다
  (운영에 같은 금액·설명 조작이 89초 간격 2건 실재: id 69/70, 카하 화면 직접 입력분).
- 생성 순서를 뒤집어 expense 먼저 → 조작에 id 심기, 하나의 트랜잭션.
- 편집 시 금액이 바뀌면 **카하 조작 금액도 같은 트랜잭션에서** 맞춘다(안 하면 장부가 갈라진다).
- 편집·삭제 모두 `cash_registers` 를 **FOR UPDATE 로 잠근 뒤** `closing_time` 재검사(TOCTOU 방지).
- 수표 gasto 는 편집·삭제 차단 — 지우면 `cheques.status` 가 USADO 로 남는다(2차).
- 링크 이전 gasto 삭제 거부 — 고아를 만들지 않는다.
- 보안: `create` 가 body 의 `userId`/`storeId` 를 무시하고 **인증 사용자**로 고정(종전엔 남의
  명의로 기록 가능). 카하 수동 조작 컨트롤러에서 위조 `expenseId` 도 제거.

### ★ 정정 — 편집은 이미 배선돼 있었다
`ExpenseModal.tsx:193` 이 `PUT /expenses/:id` 를 부르고 있었다. 내가 "프론트 호출부 없음"이라 한 것은
**grep 을 `head -12` 로 잘라 본 탓**이다. 그래서 오늘 배포 전까지 그 버튼은 generic CRUD 로 PUT 했고
금액을 고쳐도 카하는 그대로였다. 실제로 없던 것은 **삭제**뿐이었다.
교훈: grep 결과를 자를 때 잘린 뒤에 답이 있을 수 있다 — 개수를 먼저 세라.

### UI
Tesorería 에 **Gastos 탭**(4번째). 기존 `ExpensesView` 재사용 + 삭제 액션 + 확인창.
탭 권한은 자기 모듈(`/gastos`) — Cheques 처럼 caja 권한을 재사용하지 않는다.
mockup: `mockups/tesoreria-gastos-tab-mockup.html`.
마감 카하는 버튼을 숨기지 않고 **누르면 서버 사유를 토스트**한다(목록 응답에 마감 여부가 없다).

### 남은 것
1. **6/4 `Compra de PC` 680,000 × 2건** — 중복 의심. 실제 2회 지출이 아니면 보정 필요
2. 목록 응답에 카하 마감 여부를 실어 mockup 의 🔒 잠금 표시로 전환
3. 수표 gasto 취소(상태 머신), `Anular`(마감 후 반대 조작) — CODEX 권고 2차
4. `@Audit` 가 gasto 편집·삭제를 추적하지 않음
5. `box_operations` 금액 부호 규약이 코드 내부에서 모순(CODEX [HIGH])

---

## 5. 남은 일 (우선순위)

### ★ MP 환불 실패 UI 에 **들어가는 문이 없다** (2026-08-12 발견)

**증상**: Mercadopago 환불이 실패했을 때 그걸 보고 재시도할 화면에 **도달할 수 없다.**

**사실 (실측)**
- `McdpgRefundFailureSection`(인라인 Alert + 재시도 버튼 + 시도 이력 — Phase 29 Plan 09)의
  **유일한 사용처**는 전체 페이지 `views/sales/details/SalesDetailView.tsx` 다.
- 그 페이지의 라우트는 `/ventas/detalle/[id]` 인데, `ventago-app/src` 전체에서
  **`ventas/detalle` 로 보내는 코드가 한 줄도 없다**(링크·`router.push` 전무).
  사이드바 메뉴에도 없다 → **주소창에 직접 쳐야만 도달**한다.
- 판매 상세 패널·드로어에는 이 섹션이 **없다**.
- 운영 실측(5434): `mp_refund_attempts` **0행**, `mp_refunds` **0행**.
  → 아직 한 번도 실행된 적이 없어 **지금 당장 사고가 난 상태는 아니다.**

**왜 남기는가**: 환불 실패는 "돈은 안 돌려줬는데 판매는 취소된" 상태다. 그때 조치할
화면이 없으면 아무도 모른 채 지나간다. 기능은 만들어져 있는데 문이 없는 형태로,
이번 세션 내내 반복된 「부재가 침묵한다」 패턴과 같다.

**→ 결정: 3번(섹션을 패널로 이식). 완료·배포됨** — app `7018827` #614 · api `6f57fa4` #688.

### 처리 내용 (2026-08-12 3차 세션)

- `SaleDetailPanel` 본문 맨 위에 이식. `SaleDetailDrawer` 가 이 패널을 그대로 렌더하므로
  **목록 패널 + 드로어가 함께** 커버된다. 전체 페이지는 손대지 않았다.
- ★ **어느 판매 id 로 이력에 닿는가** — `mp_refund_attempts.sale_id` 는 **원본 판매**에 붙는다
  (`nullifySale` 이 `refundForSale({ id: original.id })` 로 부른다. 호출부는 리포 전체에
  `sales-create.service.ts:1194` **한 곳뿐**). 반면 취소 응답으로 돌아가는 건 역분개다.
  목록에는 두 행이 다 보이므로 원본 id 로 정규화한다:
  `nullifiedSaleId ?? (nullifiedBySaleId ? id : null)`.
  취소 안 된 판매는 둘 다 비어 **SWR 키가 null → 요청이 아예 안 나간다.**
  실측: 원본 101 = Anulado + nullified_by_sale_id 102 / 역분개 102 = Anulación + nullified_sale_id 101.
  ※ 결제수단 title 로 거르지 않았다 — 매장마다 `Mercadopago`/`MercadoPago` 로 갈리고 id 도 매장별이다.
- ★ **권한 게이트를 컴포넌트 안에 넣었다**(단일 출처). 두 라우트가
  `@Auth(admin, superadmin, gerente)` 전용이라 vendedor 가 부르면 403 인데,
  `api.service.ts` 인터셉터가 **모든** 에러를 `errorBus` 로 밀어 GlobalErrorBanner 가
  **사용자가 닫기 전까지 안 사라지는 빨간 배너**를 띄운다(SWR 기본 에러 재시도로 반복까지).
  게이트가 없으면 권한 없는 사람이 취소된 판매를 **열기만 해도 화면이 에러로 덮인다.**
  `isSupervisorRole`(configs/roles.ts) 집합이 백엔드 `user-role.guard` 의
  `LEGACY_ROLE_ALIAS` 와 **정확히 일치**함을 대조했다
  (super_admin→superadmin / store_owner·store_admin→admin / branch_manager→gerente).

### ★ 같이 고친 것 — 재시도 버튼이 원래 **거의 항상 400 이었다** (CODEX 자문에서 발견)

문을 내기만 했으면 **죽은 버튼**으로 이어질 뻔했다.
`POST /mercadopago/refunds/:saleId/retry` 는 body `amount` 를 필수(`@Min(0.01)`)로 받는데,
그 금액을 아는 곳이 **취소 응답(메모리에만 있는 `mpRefundResults`)뿐**이었다 —
`mp_refund_attempts` 에 **금액 컬럼이 없다**. 그래서 화면을 새로 열어 이력만 보고 누르면
프론트가 `0` 을 보내 400 으로 죽었다. 즉 재시도는 **취소 직후 그 화면을 안 떠난 경우에만** 동작했다.
운영에서 환불이 한 번도 안 돌아(0행) 드러나지 않았을 뿐이다.

→ `amount` 를 선택값으로 바꾸고 없으면 intent 의 결제 금액을 쓴다. 값을 받아도 결제액을 넘으면
400(attempt 행을 만들기 **전에** 막아 이력을 더럽히지 않는다). `mp_refunds.amount` 와 지갑
차감도 유도된 금액을 쓴다. spec 2건 추가 + **뮤테이션 2종으로 사멸 확인**
(`?? paidAmount`→`?? 0` / 초과 가드 `if(false)` — 각각 자기 테스트만 실패). 26/26 통과.

### ★ 남은 구멍 (이번에 **안** 고쳤다 — 다음 세션 후보)

1. **환불 실행 자체가 실패하면 아무 기록도 안 남는다.** `retryAttempt` 는 intent/mp_account
   조회 실패 시 attempt 행을 만들기 **전에** throw 한다. `nullifySale` 은 이걸 잡아
   메모리 응답에 `mpRefundError` 만 붙이므로 **DB 에 아무것도 안 남고 패널은 조용하다.**
   전형적인 「부재가 침묵한다」. 지속되는 실패 기록이 필요하다.
2. `status='pending'` 으로 멈춘 attempt 는 **실패로 판정되지 않는다**(마지막이 `failed` 일 때만
   경고). 이력 목록에는 보이지만 Alert·재시도 버튼은 안 뜬다.
3. 이력 GET 이 **네트워크 오류**로 실패하면 "이력 없음"과 구분이 안 된다(훅의 `error` 를 안 읽는다).
   권한 오류는 이번 게이트로 요청 자체를 막아 해소했지만, 이건 남아 있다.
4. `retry` 가 **saleId 소유권을 검증하지 않는다** — 다른 매장 판매 id 로 attempt 행을 붙일 수 있다.
   ※ **CODEX 지적을 근거까지 대조한 결과 범위를 좁혔다**: 돈이 새지는 **않는다**.
   `MpPaymentIntent` 는 `storeId` 를 직접 가진 모델이라 테넌트 훅이 읽기를 좁혀
   **다른 매장 결제건은 조회 자체가 안 된다**(→ 404). 또 CODEX 는 "이력 GET 도 같은 문제"라 했으나
   **틀렸다** — `tenant-scope.registry` 에 `MpRefundAttempt: [{ as: 'sale', parentModel: 'Sale' }]`
   가 있어 `beforeFind` 훅이 부모 Sale 로 매장을 좁힌다. 남는 건 **기록 오염**뿐이다.
5. `listAttempts` 에 상한이 없다(재시도가 쌓이면 전건 반환). 지금은 0행이라 무해.
6. `views/reports/sales/SaleDetailPanel.tsx` 는 **이름만 같은 별개 패널**(리포트 콕핏)이다.
   거기엔 문을 내지 않았다.
7. 전체 페이지 `SalesDetailView` 는 그대로다 — 여전히 도달 경로가 없고 옛 컴포넌트를 쓴다.
   (권한 게이트는 컴포넌트 안에 있으니 나중에 문을 내도 배너 사고는 안 난다.)

**주의**: 전체 페이지에는 `ActionButtons`(Volver/Reimprimir)도 이 페이지에만 있다.
상세 화면을 통일할 때 이 두 블록을 빠뜨리기 가장 쉽다(설계는
`mockups/venta-detalle-unificado-mockup.html` 참조).

### 판매 상세 화면 정리 상태 (2026-08-12)
- 목록 옆 **패널**: 새 레이아웃 배포 완료 (`3880e3c` → 매트릭스 표 복원 `e3a57d3`).
- **드로어**: 패널을 그대로 렌더하도록 통일, 폭 축소(sm 480 / md 560) — `f0453c2`.
- **전체 페이지**: 옛 레이아웃 그대로. 위 이유로 **도달 경로가 없어 우선순위 낮음.**
- 셋 다 새 레이아웃을 쓰게 되면 `views/sales/details/components/*` 6개가 고아가 된다
  (`SaleHeader`/`SaleSummary`/`ProductsTable`/`ClientInfo`/`PaymentMethods`/`AdditionalInfo`)
  — 지울지 남길지 결정 필요.
- 인터넷 미수금 표시 정정: `Por cobrar` 는 **누적**(날짜 무관)인데 오늘 패널 안에 있어
  오해를 샀다 → 라벨을 `acumulado` + "no es de hoy" 로 (`f0453c2`). 값·쿼리는 그대로.

1. ~~tienda-admin Códigos madre 교체 + APK 재배포~~ — **완료** (`9b6efa1`, APK 20260812-1650)
2. ~~talleres 2단계 — `talleres_recepcion_items` + 양쪽 마이그레이션~~ — **완료** (api `6849a5f`)
2-b. ~~talleres 3단계 — corte 수령 변형별 입력~~ — **완료** (api `85855c9` / app `fb5cd74`)
2-c. ~~talleres 4단계 — 최종 공정 자동 입고~~ — **완료** (api `49fadc7` / app `c3108ef`)
2-d. ~~talleres 5~8단계~~ — **완료** (api `e9e7c63`·`2bc0f20` / app `f530ff8`).
     talleres 생산 입고 **전 단계 종료**.
3. ~~지점 미해결 취소가 조용히 재고를 잃는 문제~~ — **완료** (api `1a6af12`). 아래 참조.

### ★ §5-3 처리 — 폴백이 **한 번도 동작한 적이 없었다** (2026-08-12)

`nullifySale` 은 `restoreBranchId` 가 null 이면 재고 복원을 통째로 건너뛰고 `logger.error`
만 남겼다 — **결제는 되돌리고 물건은 안 돌아오는** 상태가 조용히 남는다.

**근본 원인**: `resolveSaleBranchId` 의 2단계 폴백 `completeSale?.store?.branchs[0]` 은
그 값을 만드는 `salesService.findOne` 이 **`Store` 를 include 하지 않아** 항상 undefined 였다.
즉 터미널 없는 판매는 **예외 없이** null → 복원 없음. "폴백이 있다"고 읽히지만 실제로는 없다.

**운영 실측(5434)**: 미취소 판매 중 `terminal_id IS NULL` **16건**(store 6=11, 9=2, 11=3).
터미널→box→branch 체인이 끊긴 터미널은 **0건** — 노출은 전부 이 경로였다.

**해석을 4단으로**(16건 전부 해석되는 것을 실측 확인):
1. terminal → box → branch (기존)
2. `online_orders.mirror_sale_id` → 주문 지점 — **8건**. 온라인 주문 미러 판매
   (`notes='online_order:N:webshop|whatsapp|mercadolibre'`)는 `user_id` 도 `terminal_id` 도
   **없어서** 다른 단계로는 못 푼다. `fulfillment_branch_id` 우선, 없으면 `branch_id`
   (운영에서 fulfillment 는 전부 NULL). 모델 import 대신 raw query — online-orders ↔ sales 는
   이미 forwardRef 로 순환을 피하는 사이다.
3. `user_id` → `users.branch_id` — 나머지 **8건**.
4. 매장에 실지점(창고 제외)이 **딱 하나**면 그 지점. 여러 개면 **고르지 않는다**.

**그래도 못 정하면 취소를 거부한다**(사용자 결정 2026-08-12). madre 가드와 같은 원칙 —
잘못 복원하면 조용한 오염, 거부는 눈에 보인다. 단 **복원 대상이 실제로 있을 때만** 막는다
(제네릭뿐인 판매는 통과 — 안 그러면 멀쩡한 취소를 막는 회귀).
`nullifySale` 호출부는 둘 다 사람이 누르는 경로(`sales.controller` / 배달 취소)라
자동화가 조용히 깨지지 않는다. **현재 이 거부에 걸리는 판매는 0건.**

spec 17건 + 뮤테이션 2종 사멸(지점 가드 무력화 / 제네릭 면제 제거).
※ 해석 4단계 자체는 단위 테스트가 없다 — 운영 DB 에 같은 SQL 을 직접 돌려 16/16 확인했다.
4. `approveReturn` 의 부분 환불 ↔ 전량 재고 복원 불일치 (미확인: 부분 환불 실사용 여부)
5. 미검토 쓰기 경로 — `code-import`, `stocks.service`, `suspended-sales`, `work-order`
6. `talleres_etapas` order 5 중복 정정
7. Phase 78 Wave 2 / Phase 77 미착수
8. 판매 트랜잭션 첫 실패가 앱 로그에 안 남는 문제 — 여전히 미해결
