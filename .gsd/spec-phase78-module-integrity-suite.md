# SPEC: Phase 78 — 모듈 무결성 스위트 (사이드바 기준 전 모듈 연동 검증)

생성일: 2026-08-11
개정: **v2 — codex 교차검토 반영 + codex 가 이미 만든 1차 기반 편입**
상태: **PLAN v2 — 사용자 승인 대기 (Wave 0 결정 5건)**

---

## v2 개정 요약

v1 을 쓰는 사이 **codex 가 1차 기반을 이미 만들어 두었다.** 그리고 codex 가 v1 계획 자체를
교차검토해 결함을 지적했다. 둘 다 반영해 범위를 **줄이고** 구조를 바꿨다.

### 이미 있는 것 (재작성 금지 — 편입 대상)

| 자산 | 위치 | 상태 |
|---|---|---|
| **모듈 테스트 체크리스트** | `docs/sidebar-module-test-checklist.md` (138줄) | 공통 12항목 + 역할별 메뉴 계약 + 모듈별 시나리오 7군 |
| 사이드바 계약 테스트 | `ventago-app/src/__tests__/sidebar-module-contract.spec.ts` | 통과 |
| 통합 모듈 계약 테스트 | `ventago-app/src/__tests__/integrated-module-contract.spec.ts` | 9 통과 / **3 todo** |
| 실행 명령 | `npm run test:modules --workspace=ventago-app` | 있음 |

→ **사용자가 요청한 "체크리스트"는 이미 존재한다.** Phase 78 의 일은 그것을
**실행 가능한 테스트로 바꾸고, 빠지면 빌드가 깨지게** 만드는 것이다.

### codex 가 v1 에서 지적한 것과 처리

| # | 지적 | 처리 |
|---|---|---|
| HIGH | **L2 네트워크 관측만으로 L3 분모를 만들면 안 된다** — 저장·삭제·환불·모달·다음 페이지는 초기 렌더에 안 나온다. 관측 안 된 엔드포인트가 조용히 누락된다 | **수용.** 분모는 **Nest controller 메타데이터**, Playwright 관측은 "화면에서 실제 쓰임" 증거로 **합친다** |
| HIGH | **`afterAll` teardown 은 OOM/SIGTERM 에서 안 돈다.** append-only `stocks` 와 "완전 삭제"도 충돌 | **수용.** teardown 대신 **일회용 DB/schema 를 통째로 폐기**. 이 프로젝트는 jest OOM 전력이 있어 특히 타당 |
| MED | 외부 의존(AFIP/MP/MinIO/Telegram/프린터)이 로컬에 없으면 "4xx/5xx 0" 이 **오탐** | **수용.** 엔드포인트별 `required / local-mock / optional` 정책 명시 |
| MED | 최종 스키마 비교만으로는 마이그레이션 락·재실행·백필을 못 잡는다 | **수용.** 빈 DB + 직전 릴리스 스냅샷 양쪽에서 migration-up 검증으로 교체 |
| MED | `stale HTML` 검증은 로컬 스모크로 재현 안 됨 (빌드 A HTML + 빌드 B asset 필요) | **수용.** 스위트에서 빼고 **배포 검증**으로 분리 |
| 축소 | 전 시나리오 수동 뮤테이션은 비용 과다 | **수용.** 고위험 지점 **8곳 한정** |
| 축소 | 독자 HTML 리포트는 Playwright reporter 와 중복 | **수용. 삭제** |
| 축소 | 카탈로그 × L1~L5 전체 행렬은 과함 | **수용.** 분모는 **L1/L2 만**, L3~L5 는 별도 분모 |
| 축소 | 전 라우트 × 4역할 5분은 비현실적 | **수용.** 렌더 스모크는 대표 역할, 접근거부는 표본 |
| 정정 | T-7 "rootDir 밖 import 금지"는 근거가 약하다 | **수용(더 나은 것으로 교체).** `--listTests` + **최소 테스트 수 단언** — 0건 실행의 *모든* 원인을 잡으므로 원래 검사를 포함한다 |
| **반려** | T-8 로컬↔운영 스키마 비교를 "운영 무접촉과 충돌"이라 제거 권고 | **부분 수용.** `information_schema` **SELECT 는 CLAUDE.md 가 명시 허용**하는 읽기 전용이라 원칙 충돌이 아니다. 다만 *테스트 스위트*에 둘 일은 아니므로 **배포 게이트로 이동** |

---

## 목표

사이드바에서 시작하는 **모든 모듈이 어떤 경우에도 끊어지지 않고, 모듈 경계를 넘어 연동되어
작동하는지**를 자동으로 확인한다.

성공의 정의: **모듈이나 라우트를 추가·변경했는데 검증에서 빠지면 빌드가 깨진다.**
체크리스트는 손으로 유지하지 않는다 — 손으로 쓴 목록은 반드시 드리프트하고,
드리프트한 순간부터 "통과"가 아무 의미가 없다.

---

## 배경

### 0. 로그 확인 (GSD 1단계)

`ventago-app/logs/error-2026-08-07.log` / `combined-2026-08-07.log` — webpack deprecation,
ag-grid autoprefixer 경고뿐. **기능 오류 없음.** 착수를 막는 선행 이슈 없음.

### 1. 실측한 사실

| # | 확인 | 영향 |
|---|---|---|
| F-1 | 사이드바는 **DB 주도**(`apps` 6 / `modules` 50) + `menuRegistry` 오버레이 | 정적 테스트만으로는 **DB 시드 누락으로 메뉴가 사라지는 사고**를 못 잡는다 |
| F-2 | 기존 계약 테스트는 **파일 존재 여부**만 본다 | 페이지가 렌더 중 터져도 통과한다 |
| F-3 | **E2E 도구 없음** (playwright/cypress 부재) | L2 이상은 도구 도입이 선행 |
| F-4 | 백엔드 **56개 모듈이 spec 0건** (`box`·`caja-fuerte`·`credit`·`session`·`despacho`·`cheques`·`audit-log` …) | 커버리지가 극단적으로 갈린다 |
| F-5 | spec 편중: `afip` 19 / `mercadopago` 13 / `sales` 10 vs 대다수 1 | "141개" 총량은 안심의 근거가 못 된다 |
| F-6 | 프론트 테스트 7개 중 4개가 **순수 함수 추출본** | 렌더 회귀를 못 잡는다 |
| F-7 | `tesoreria`(가상그룹), `/cheques`·`/facturacion`(inject)은 **DB 모듈이 아니다** | 인벤토리는 **합집합**이어야 한다 |
| F-8 | 전용 jest config 4종이 이미 있고 전부 `maxWorkers: 1` | 격리 실행 관행이 이미 있다 — **재사용, 중복 생성 금지** |

### 2. 이 프로젝트가 실제로 데인 실패 형태 (테스트 설계의 입력)

| 형태 | 실제 사례 | 스위트가 잡아야 할 것 |
|---|---|---|
| **부재가 침묵한다** | 잘린 응답이 `count` 에 안 남음 / room 가입 실패에 `join_error` 없음 / DTO 에만 있고 저장 안 되던 필드 | 스킵·0건 실행·미커버를 **성공으로 처리하지 않는다** |
| **통과하지만 아무것도 안 지킴** | 가드가 여럿일 때 뒤 가드가 대신 던져 통과 (2026-08-11) | 고위험 지점 뮤테이션 확인 |
| **빌드 통과 ≠ 배선됨** | 호출부 부재·기본값 미확인 | 렌더 + 실제 API 호출까지 |
| **페이지네이션 절단** | `stock-today` 가 지점 행 단위 50개 절단 → 360이 40으로 | **다지점·대량** 필수 |
| **타임존** | `created_at::date` 가 UTC 달력 → 17% 어긋남 | '오늘' 경계 |
| **서버 폴백 부재** | POS 검색은 마운트 시 메모리 카탈로그만 본다 | 데이터 정정이 기능을 죽이는 경우 |

### 3. 하드 제약 (위반하면 기계나 운영이 죽는다)

- **jest 기본 옵션이면 메모리 20GB 초과.** 2 워커면 랜덤 suite 가 SIGTERM. → **`--maxWorkers=1` 고정**
- **Jenkins 는 운영 서버 위에서 돈다(swap 0).** 무거운 스위트는 운영 Postgres 를 위협한다.
  → L2 이상은 **Jenkins 에 넣지 않는다. 야간 실행도 이 서버에서 하지 않는다** (D3)
- **운영 DB 에 테스트 데이터를 만들지 않는다.** 이번 세션에만 제품 66행·원장 1건을 청소했다
- **세션 보안**(ActiveSession 1개 / fingerprint / IP 등록)이 자동 로그인을 막는다 (D2)
- 테스트가 **0건 실행된 것을 성공으로 처리하지 않는다**

---

## 착수 전 확정 필요 — 결정 5건 ★

### D1. 인벤토리의 단일 출처
| 안 | 내용 |
|---|---|
| D1-a | 손으로 쓴 체크리스트를 분모로 | **반대** — 드리프트한다 |
| **D1-b (권장)** | **`module-catalog.generated.ts`** — DB `apps`/`modules` + `menuRegistry`(가상그룹·inject 포함)의 **합집합**을 코드로 생성. 항목: `{ appSlug, moduleSlug, route, permissionSlug, source: 'db'|'registry'|'both', visibilityKind: 'menu'|'injected'|'virtual'|'hidden' }`. 드리프트 시 빌드 실패 |

`visibilityKind` 는 codex 지적 반영 — `source` 만으로는 주입·가상·숨김을 구분 못 한다.

### D2. E2E 인증 — 세션 보안을 어떻게 통과하는가
| 안 | 내용 |
|---|---|
| D2-a | 테스트에서 세션 가드 비활성화 | **반대** — 세션 자체가 회귀 지점인데 검증에서 빼게 된다 |
| **D2-b (권장)** | **worker/context 별 전용 유저** + fingerprint·IP·device token·storageState 를 **사전 시드**. 가드는 켠 채 정상 경로로 통과. 중복로그인·만료는 L5 에서 **의도적으로** 검증 |

역할당 1명이 아니라 **worker 당 1명** — ActiveSession UNIQUE 때문에 병렬 시 서로를 쫓아낸다(codex 지적).

### D3. 실행 위치 — **v2 실측으로 전면 개정**

v1 은 "운영 서버에서 하지 마라"였다. **실측 결과 그 톤은 과했고, 근거도 절반이 틀렸다.**

#### 실측 (2026-08-11)
- 서버는 **하나뿐**이다. `jhkim-server` = `myserver` = 62.72.7.245 = `srv803182` (VM).
  **스테이지 서버는 없다.**
- 31 GiB / 8 vCPU / **swap 0** / load 3.7. `available` **23 GiB**, `buff/cache` **18 GiB**
- 같이 사는 것: Jenkins(JVM ~517MB) + 호스트 PG18(운영) + pgbouncer + 컨테이너 13개
  (`ventagoapp`, `api_ventago`, `minio`, mongo, 별도 PG, `coolinvoice`, `tienda_coolsistema` …)
- 운영 PG: `shared_buffers` 2 GiB / **`effective_cache_size` 12 GiB** / `max_connections` 200
- **cgroup v2 + `systemd-run` 사용 가능**
- **★ Phase 63 이 만든 스테이징 스택이 이미 있다** — `/home/jhkim/phase63-staging/`,
  `loadtest/docker-compose.staging.yml`. 전용 Redis(운영 print-agent 채널 오염 방지),
  `CRON_ENABLED=false`(실제 고객 발송 방지), `restart:'no'`. 컨테이너는 현재 정지 상태
- **★ 그런데 `ventago_staging` DB 는 운영과 같은 PG18 클러스터(5434)에 있다.**
  앱·Redis 는 분리됐지만 **DB 엔진(shared_buffers·WAL·checkpointer)은 공유**된다

#### 위험의 정확한 형태
1. **OOM kill** — 확률은 낮지만 **swap 0** 이라 초과 시 점진적 저하 없이 즉사.
   jest 가 20 GB 를 찍은 전례가 있어 **상한이 없으면** 도달 가능.
2. **페이지 캐시 축출 (더 현실적)** — `available` 23 GiB 에는 **회수 가능한 페이지 캐시가
   포함**되고, 바로 그 캐시가 운영 PG 성능의 전제(`effective_cache_size` 12 GiB)다.
   OOM 없이도 운영 쿼리가 디스크로 내려간다. **로그에 "테스트 때문"이라고 안 남는다.**
   → "23 GiB 여유니 괜찮다"는 판단은 낙관적이다(codex 정정).
3. **DB 공유 — 이것이 진짜 급소.** cgroup 으로 프로세스를 가둬도 **DB 는 공유**된다.

#### **D3-a (권장) — 별도 서버 불필요, 같은 호스트에 3중 격리**

| 계층 | 어디서 |
|---|---|
| L1 (정적) | **Jenkins 기본 빌드 포함** — 가볍고 빠르다. 실패 시 배포 차단 |
| L2~L5 | 같은 호스트, 단 **아래 3가지를 모두** 적용 |

1. **Phase 63 스테이징 스택 재사용** — 새로 만들지 않는다. 전용 Redis / `CRON_ENABLED=false` /
   외부 발송 sink 는 이미 거기 있다
2. **테스트 PG 를 운영 클러스터 밖으로** — 일회용 DB 를 5434 에 만들지 않는다.
   **PG18 컨테이너를 따로 띄운다**(운영과 major 동일). `ventago_staging` 이 5434 에 있는
   현재 구조는 이 phase 에서 쓰지 않는다
3. **cgroup slice 로 상한** — runner 와 테스트 PG 컨테이너를 **모두** `phase78.slice` 아래:
   `MemoryHigh=8G` / `MemoryMax=10G` / `CPUQuota=150%` (+ `--maxWorkers=1`)

**중단 기준** (하나라도 걸리면 상한을 올리지 말고 D3-b 로 간다):
cgroup OOM 반복 / 테스트 중 운영 P95 300ms 초과 또는 평시 대비 20% 상승 / 운영 slow query 100ms 증가

#### D3-b — 별도 스테이지 VPS (위 기준 위반 시에만)
최소 **4 vCPU / 16 GiB / 80 GiB SSD / swap 4 GiB**. 8 GiB 는 family 의 6 GiB heap +
PG + Chromium + OS 를 못 담는다. swap 은 성능용이 아니라 **순간 피크에 머신이 즉사하는 것을 막는 완충재**다.
스키마 동기화는 수동 관리가 아니라 **같은 커밋의 마이그레이션을 매번 처음부터 적용**한다.

### D4. E2E 도구
**D4-a (권장) Playwright** — 콘솔·네트워크·trace 수집이 L2/L3 에 그대로 필요하고, 멀티 컨텍스트(다단말 소켓 검증)가 된다. 리포터·trace 가 이미 충분하므로 **자체 리포트 생성기는 만들지 않는다**.

### D5. 테스트 DB
| 안 | 내용 |
|---|---|
| D5-a | 로컬 공용 `ventago` DB + teardown | **반대(codex)** — OOM/SIGTERM 시 잔여물이 남고, append-only `stocks` 와 "완전 삭제"가 충돌 |
| **D5-b (확정 — 사용자 승인 2026-08-11)** | **일회용 DB 를 전용 PG18 컨테이너 안에** 만들고 통째로 폐기. 잔여물이 원리적으로 불가능 |

**D3 과 맞물린 정정**: 일회용 DB 를 **운영 클러스터(5434)에 만들지 않는다.** 거기 만들면
DB 이름만 다를 뿐 `shared_buffers`·WAL·checkpointer 를 운영과 공유해 격리가 아니다
(현재 `ventago_staging` 이 그 상태다). **PG18 컨테이너를 따로 띄우고** 컨테이너를 버린다.
major 는 운영과 동일한 18 로 맞춘다.

**다지점 필수**: 시드는 지점 **2개 이상**. 절단·누락 사고가 전부 다지점에서만 재현됐다.

---

## 계층 구조

```
L0  module-catalog.generated.ts   ← DB ∪ menuRegistry        (L1/L2 커버리지 분모)
L1  정적 계약                      ← 경로·권한·DB↔레지스트리 정합      [Jenkins 포함]
L2  라우트 스모크(런타임)            ← 실제로 열어 렌더/콘솔/응답 검사
L3  API 계약                       ← 분모=controller 메타데이터, 증거=관측
L4  업무 흐름                       ← 모듈 경계를 넘는 시나리오 (체크리스트에서 도출)
L5  특수 경우                       ← 실제로 데인 형태들
```

---

## 태스크 목록

### Wave 0 — 결정
- [ ] **T-0**: D1~D5 승인

### Wave 1 — 이미 찾은 단절 3건 수정 (**즉시 가치**)
codex 의 `it.todo` 3건은 **실재하는 단절**이다. 고치고 todo 를 활성 테스트로 전환한다.
- [ ] **T-1**: Talleres — 잘못된 `tab` query 를 `overview` 로 정규화
- [ ] **T-2**: Talleres — `/talleres` **직접 URL 에 앱·기능 권한 적용**
      (메뉴에서 숨기는 것은 보안 경계가 아니다)
- [ ] **T-3**: Tesorería — 허용 탭 0개일 때 빈 화면 대신 `/unauthorized` 또는 안내
- [ ] **T-4**: 위 3건의 `it.todo` → 활성 테스트 전환 + **뮤테이션 확인**

### Wave 2 — L0 인벤토리 (**이후 전 계층의 선행**)
- [ ] **T-5**: `scripts/gen-module-catalog.ts` — DB + 레지스트리 합집합 → `module-catalog.generated.ts`
- [ ] **T-6**: **드리프트 게이트** (`--check`) — 생성물이 커밋본과 다르면 실패
- [ ] **T-7**: **0건 실행 방지** — `--listTests` 로 수집된 테스트 수가 하한 미만이면 실패
      (rootDir·transform·roots 등 *모든* 원인을 포괄한다)

### Wave 3 — L1 정적 계약 (기존 spec 확장)
- [ ] **T-8**: `sidebar-module-contract.spec.ts` 를 **카탈로그 기반 전수 검사**로 전환
      (핵심 라우트 15개 하드코딩 → 카탈로그 전체)
- [ ] **T-9**: **DB↔레지스트리 양방향** — DB 모듈인데 라우트 없음 / 레지스트리 경로인데 DB 모듈·권한 없음 (F-1)
- [ ] **T-10**: 권한 slug 정합 — 카탈로그 `permissionSlug` ↔ 백엔드 시드 실재
- [ ] **T-11**: **마이그레이션 up 검증** — ① 빈 DB ② 직전 릴리스 스냅샷 양쪽에서 전체 마이그레이션 실행
      (최종 스키마 비교로는 락·재실행·백필을 못 잡는다 — codex)

### Wave 4 — L2 라우트 스모크 (Playwright)
- [ ] **T-12**: Playwright 도입 + 전용 config. **Jenkins 제외**(D3)
- [ ] **T-13**: **일회용 DB/schema 픽스처**(D5-b) — 생성 → 마이그레이션 → 시드(**2지점 이상**) → `DROP`
- [ ] **T-14**: 인증 픽스처(D2-b) — worker 당 유저, fingerprint/IP/device token/storageState 시드
- [ ] **T-15**: **외부 의존 정책** — 엔드포인트별 `required / local-mock / optional` 표.
      AFIP·MP·MinIO·Telegram·프린터가 없어도 **오탐이 안 나게** (codex MEDIUM)
- [ ] **T-16**: **카탈로그 전 라우트 스모크(대표 역할)** — ① 렌더 성공(스켈레톤 아님)
      ② `console.error` 0 ③ 정책상 허용 외 4xx/5xx 0 ④ 흰 화면 아님
- [ ] **T-17**: **접근 거부 표본** — 역할별 메뉴 가시성은 전수(가볍다), **직접 URL 차단은 표본**.
      프론트 분기는 보안 경계가 아니다

### Wave 5 — L3 API 계약
- [ ] **T-18**: **분모 = Nest controller 메타데이터**로 엔드포인트 인벤토리 생성 (codex HIGH)
- [ ] **T-19**: L2 실행 중 네트워크 관측 → "화면에서 실제 쓰임" 증거로 **분모에 합류**.
      관측 안 된 엔드포인트는 **누락이 아니라 미사용으로 표시**된다(침묵 금지)
- [ ] **T-20**: 엔드포인트 계약 표본 — 200 / 권한 거부 / **타 매장 IDOR 차단**.
      기존 `test/tenant` 스위트 **재사용**, 중복 생성 금지
- [ ] **T-21**: 진입 API 예산 — 라우트당 요청 수·바이트 기록, 초과 시 경고 (POS 진입 33건 전례)

### Wave 6 — L4 업무 흐름
분모는 `docs/sidebar-module-test-checklist.md` 의 **모듈별 시나리오**. 각 흐름 종료 시
**불변식 뷰**(`v_stock_balance_drift`=0, `v_stock_tenant_leak`=0) 확인.
- [ ] **T-22**: 판매 — 상품등록 → POS 검색 → 판매 → 재고 → 카하 → 보고서
- [ ] **T-23**: 발송/외상 — 발송(외상) → `sale_credit` → 수금 FIFO → 취소/되돌리기 → `sale_credit_void`
- [ ] **T-24**: 생산·외주 — BOM → Lote → Cut Ticket → Envío → Recepción → Liquidación 선행 순서
- [ ] **T-25**: 카하/금고 — 개장 → 이동 → 마감 → Caja Fuerte 전송 (+ 멱등성)

### Wave 7 — L5 특수 경우
- [ ] **T-26**: **다지점·대량** — 2지점 × 대량 변형에서 절단·중복 없음 (절단 사고 재현)
- [ ] **T-27**: **타임존** — 매장 타임존 기준 '오늘' 경계 (UTC 와 어긋나는 시각에 실행)
- [ ] **T-28**: **동시성** — 기존 `test/concurrency` **재사용·확장** (중복 작성 금지)
- [ ] **T-29**: **부분 실패** — 트랜잭션 중간 실패 시 부분 저장 없음 (Phase 64 결함 형태)
- [ ] **T-30**: **세션** — 중복 로그인 차단 / 만료 401 / 신규 디바이스·IP 등록 강제
- [ ] **T-31**: **소켓** — store room 가입 + `catalog:changed` 다단말 수신.
      **가입 실패가 침묵하지 않는지**까지 (실제 사고 지점)
- [ ] **T-31b**: **행 식별 — 엉뚱한 줄이 수정되는 사고** (2026-08-06 실사고, 구조적으로 재발 가능)
      같은 제품이 카트에 여러 줄일 때 `findIndex(id)` 가 항상 첫 줄을 줘서 3번째 줄을 고쳐도
      1번째 줄이 바뀌었다. `activeRowIndex` 로 고쳤으나 **그 인덱스는 카트가 바뀌면 낡는다.**
      화면상 수량은 그럴듯하게 바뀌므로 **틀린 것이 조용하다** — 이게 핵심 위험이다.
      - POS: 색·talle 다변형 artículo 여러 개 랜덤 수량 입력 → **첫 줄로 포커스 복귀 후 수정**
        → 그 줄만 변경. 같은 id 두 줄 / 중간 줄 삭제 / CodigosMadres 토글 왕복 포함
      - productos nuevos: 색×talle 매트릭스 랜덤 입력 → 첫 셀 복귀 수정 → 그 셀만 변경.
        색·talle 중간 추가·삭제 후에도 유지. **입고는 append-only 원장에 남아 되돌리기가 비싸다**
      - clientes: 랜덤 다건 등록 → 첫 고객 재편집 → 그 고객만 변경.
        목록 필터·검색·페이지 전환 중에도 편집 대상이 유지되는지(인덱스로 잡으면 어긋난다)
      - 판정: 콘솔에 `[ROW-MUTATION-VIOLATION]` 0건
        (`src/utils/row-mutation-invariants.ts` — 위반을 `console.error` 로 낸다)

### Wave 8 — 커버리지 게이트
- [ ] **T-32**: 커버리지 매트릭스 — **분모는 카탈로그 × L1/L2**. L3~L5 는 별도 분모(엔드포인트 / 체크리스트 시나리오)
- [ ] **T-33**: **미커버는 실패** — 새 모듈이 카탈로그에 들어왔는데 L1·L2 가 없으면 빌드 실패.
      L4/L5 는 `allowlist` 유예 가능하되 **유예 목록을 리포트에 노출**(조용히 넘어가지 않는다)

### Wave 9 — 품질 검증 (스위트 자신에 대한)
- [ ] **T-34**: **고위험 8지점 뮤테이션 확인** — 결제 / 재고 원장 / 외상 원장 / tenant 가드 /
      세션 가드 / 멱등성 / 카하 마감 / 권한 가드. 전 시나리오 의무화는 하지 않는다(codex)
- [ ] **T-35**: **피크 메모리 실측 기록**, `--maxWorkers=1` 고정
- [ ] **T-36**: 실행 시간 예산 — L1 ≤ 30초, L2 ≤ 8분. 초과 시 분할
- [ ] **T-37**: ESLint 0(양쪽) + 프론트 프로덕션 빌드 통과

### Wave 10 — 운영화
- [ ] **T-38**: `test:integrity` / `test:integrity:l1` 스크립트
- [ ] **T-39**: Jenkins 에 **L1 만** 추가 (D3-a)
- [ ] **T-40**: 야간 L2~L5 — D3-a 의 3중 격리(스테이징 스택 + 전용 PG 컨테이너 + `phase78.slice`) 아래.
      실행 중 **운영 P95·slow query 를 함께 계측**해 중단 기준 위반을 자동 판정
- [ ] **T-40b**: **외부 dead-man heartbeat** — 시작/성공/실패를 러너 **바깥**에 기록.
      러너 안의 cron 만으로는 안 된다(꺼지면 실패 보고 프로세스도 같이 사라진다).
      경보 조건: `/start` 만 있고 성공 없음 / heartbeat 자체가 없음(스케줄러 미실행) / 실패.
      ※ 이 프로젝트는 **감시 장치가 부재에서 침묵하는** 사고를 3번 냈다
- [ ] **T-41**: `docs/sidebar-module-test-checklist.md` 갱신 — 각 항목에 담당 테스트 파일 링크
      (체크리스트와 실제 테스트를 연결해 드리프트를 눈에 보이게)

### 스위트 밖으로 분리 (codex 지적)
- **운영 스키마 대조**: `information_schema` **읽기 전용** 조회. 테스트가 아니라 **배포 게이트**로.
- **stale HTML 감지**: 빌드 A HTML + 빌드 B asset 조합이 필요 → **배포 검증**으로.

---

## 완료 기준

- 단절 3건 수정 + `it.todo` 3개가 **활성 테스트**로 전환되고 뮤테이션으로 죽는 것 확인
- `module-catalog.generated.ts` 가 DB∪레지스트리를 담고 **드리프트 시 빌드가 깨진다**
- 카탈로그 **전 라우트**가 L1 + L2 커버 — 예외 0
- 역할별 메뉴 가시성 전수 통과, 직접 URL 차단 표본 통과
- L4 흐름 4종 통과 + 각 종료 시 불변식 뷰 0행
- L5 위험 6종 통과 (기존 concurrency/tenant 스위트 재사용, 중복 0)
- **고위험 8지점 뮤테이션 확인 기록** (죽지 않는 테스트 0)
- **0건 실행 방지 게이트 동작** (테스트 수 하한 단언)
- 피크 메모리 실측 기록, `--maxWorkers=1`
- Jenkins 는 L1 만 — 기본 빌드 증가 ≤ 30초
- **운영 DB 무접촉** — 일회용 DB 를 DROP 하므로 잔여물이 원리적으로 없음

---

## 금지사항 / 주의

### 건드리지 말 것
- `ventago-app/src/navigation/vertical/index.ts` — `menuRegistry.ts` 만
- `api-ventago/src/database/database.module.ts` — pool 설정 변경 금지
- 운영 DB — 쓰기 금지 (읽기 전용 스키마 조회만, 그것도 스위트 밖)
- 기존 `test/family`·`test/tenant`·`test/concurrency` — **재사용한다. 같은 걸 새로 만들지 않는다**

### 주의
- **jest 전체 실행은 `--maxWorkers=1`** — 기본이면 20GB 초과, 2 워커면 SIGTERM
- **스킵·0건 실행을 성공으로 처리하지 않는다**
- 프론트 역할 분기는 **보안 경계가 아니다**
- 테스트 매장은 **2지점 이상**
- 외부 의존이 없을 때의 실패는 **정책표에 없는 한 실패로 친다**(오탐 방지와 침묵 방지의 균형)
- 주석 한국어, 함수/변수명 영어

---

## 롤백

| 단계 | 롤백 |
|---|---|
| Wave 1(단절 수정) | 운영 코드 변경 → `git revert` |
| Playwright | devDependency 제거 + `e2e/` 삭제 |
| 생성 카탈로그 | 파일 삭제 + L1 을 기존 하드코딩 버전으로 |
| Jenkins L1 | 파이프라인 단계 제거 |
| 일회용 DB | 없음 — 원래 폐기된다 |

Wave 1 만 운영 코드를 바꾸고, 나머지는 **검증 계층 추가**라 신규 파일 제거로 완결된다.

---

## 진행 로그

| 날짜 | 단계 | 비고 |
|---|---|---|
| 2026-08-11 | PLAN v1 | 기존 자산 실측, 사실 7건, 하드 제약 5건, 결정 5건, 태스크 43 |
| 2026-08-11 | **PLAN v2** | codex 가 만든 1차 기반(체크리스트+계약테스트) 편입 → 재작성 제거. codex 의 v1 교차검토 반영: L3 분모를 controller 메타데이터로, teardown→일회용 DB, 외부 의존 정책, 마이그레이션 up 검증, 뮤테이션 8지점 한정, 자체 리포트 삭제, 분모 축소. 지적 1건 부분 반려(읽기 전용 스키마 조회는 원칙 충돌 아님 → 배포 게이트로 이동). 태스크 43 → 41, 범위 축소 |
| 2026-08-11 | **v2.1 — D3 전면 개정** | 서버 실측(31GiB/8vCPU/swap 0/available 23GiB/cgroup v2) + **스테이지 서버는 없음** 확인. **Phase 63 스테이징 스택이 이미 존재**(`/home/jhkim/phase63-staging/`, 전용 Redis·`CRON_ENABLED=false`)해 재사용으로 전환. **급소는 `ventago_staging` 이 운영과 같은 PG18 클러스터(5434)에 있다는 것** — 앱은 분리됐지만 DB 엔진 공유. → 별도 서버 불필요, 대신 3중 격리(스테이징 스택 + **전용 PG 컨테이너** + `phase78.slice` MemoryHigh 8G/MemoryMax 10G/CPUQuota 150%) + 중단 기준. D5 는 사용자 승인, 단 일회용 DB 를 운영 클러스터가 아닌 컨테이너 안에 두도록 정정. 야간 실행에 외부 dead-man heartbeat 추가 |
