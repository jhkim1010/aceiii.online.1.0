# 의존성
node_modules

# 빌드 결과
dist
*.tsbuildinfo

# 환경 변수
.env
.env.local
.env.*.local

# 런타임 데이터 (SQLite, 자체 로그)
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
logs/*.log
logs/*.gz

# OS / IDE
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
*.swp
