# 006 — 정산 경로 마무리 (DRAFT 중복 방지 + 잔여 검증)

## 배경
2026-08-19~20 에 정산 초안 생성 경로에서 결함 다섯 겹을 순차로 걷어냈다
(`.planning/HANDOFF-2026-08-19-b-settlements-store-id.md`). 남은 것을 끝낸다.

## 범위

| # | 항목 | 상태 |
|---|---|---|
| A | DRAFT 중복 방지 (advisory lock + 부분 유니크 인덱스 + 멱등 23505 처리) | 구현됨, 미검토·미배포 |
| B | `cancel` / `markPaid` 경로 화면 검증 | 미확인 |
| C | Trello 오류 카드 8건 조사 | 미착수 |
| D | `SHOP_DB_ISOLATED` 선언 여부 | **사용자 결정** |
| E | `corte` 활성 기본단가 2개 정리 | **사용자 결정(영업)** |

## 규칙
- codex 자문을 거친다 (`scripts/codex-review.sh --working`).
- CRITICAL/HIGH 미해소 상태로 push 하지 않는다.
- 운영 DDL/DML 은 사용자 승인 후 실행한다.
- **검증용으로 만든 운영 데이터는 지우지 않는다** (사용자 지시, 2026-08-20).
- 검토 라운드는 **최대 2회**. 그 이상은 미해결 항목을 문서에 남기고 종료한다(무한 루프 방지).
